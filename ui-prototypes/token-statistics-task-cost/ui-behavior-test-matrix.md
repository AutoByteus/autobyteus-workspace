# Token Statistics Task-Cost UI Behavior Test Matrix

| Scenario ID | Area | Surface | Trigger | Initial State | Expected UI State | Expected User-Visible Result | Backend / State Expectation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| task_stats_default_load | task-statistics | Settings > Token Statistics | Page mount | empty/loading | task table loaded | Main content starts with one compact control card; grouping select shows `Task`; date controls follow; no repeated title, `Usage during period`, `Select Date Range:`, `Group by:`, or separate `By Task` / `By Model` tab row is rendered; rows sorted by `Created Time` desc | Uses `tokenUsageTaskStatisticsInPeriod(startTime, endTime)`; no `rangeMode` argument | Default grouping is task/team rows, not model rows |
| task_stats_repeated_team_runs | task-statistics | Task table | Data loaded | five Software Engineering Team runs in selected period | five separate top-level team rows | Rows are ordered by top-level `runCreatedAt` newest first and not collapsed by name/runtime/model/date | Group by `rootTeamRunId`, not team name | Demonstrates per-task/run cost scanning |
| task_stats_expand_team_usage_only | task-statistics | Task table | Click team chevron | team collapsed; team roster may have six live members; only `solution_designer` has selected-period usage | team expanded | Only usage-derived member rows are required; `solution_designer` appears; inactive/no-usage roster members may be omitted | Child rows are grouped from ledger events by `memberAgentRunId` then `memberRouteKey`; no roster merge is required for no-usage members | Page is usage report, not roster viewer |
| task_stats_expand_team_no_double_count | task-statistics | Task table | Click team chevron | team collapsed | team expanded | Member rows appear indented beneath team; member costs sum to team total subject to mixed/partial pricing; member runs do not also appear as standalone top-level rows | Top-level standalone rows exclude records with `rootTeamRunId`; team totals aggregate all team events | Prevents double-counting |
| task_stats_nested_execution_address | task-statistics | Task table | Expand team with nested task/member execution address | usage has backend `executionAddress` such as `member(planning_team) -> task_team(task-team-run-id) -> member(solution_designer)` | recursive children visible | Nested member/task-team/task-agent rows appear under their parent rather than as unrelated top-level rows | Uses backend-provided recursive `children` plus `executionAddress`; legacy `memberPathJson` / `member_path` are not hierarchy authority | Full hierarchy is backend-owned; frontend query depth is finite and deeper trees need an explicit follow-up |
| task_stats_metadata_deleted | task-statistics | Task table | Load historical usage after live run metadata deletion/rename | token-statistics display fields were captured before metadata change | row loaded | Team/agent rows still show persisted `teamName`/`agentName`, `runSummary`, `runCreatedAt`; member rows show `memberName` | Statistics response uses token-statistics-owned display fields, not a permanent live metadata join | Validates self-contained display fields |
| task_stats_legacy_metadata_missing | task-statistics | Task table | Load legacy row with no display fields and no metadata | usage exists | fallback row loaded | Row uses explicit `Unknown` label and `First usage observed` tooltip/details for fallback time | Falls back to ledger ids and earliest `observedAt`; does not suppress observed costs | Legacy limitation is explicit |
| task_stats_member_created_time_absent | task-statistics | Task table | Expand team | usage-derived member rows visible | member rows loaded | Member `Created Time` cell shows `—`, `same as team`, or muted inherited team time; it does not claim member-run creation time | No `memberCreatedAt` persistence/API field is required | Enforces minimal field policy |
| task_stats_click_cost_breakdown | task-statistics | Cost cell | Click total/input/output cost | table row loaded | breakdown panel visible | Input breakdown shows uncached/cache-read/cache-write/total input; output breakdown shows output tokens and thinking included; total cost is not double-counted | Uses backend aggregate fields; frontend does not recompute prices | Thinking is output diagnostic/subset |
| task_stats_partial_price | task-statistics | Status/cost cell | Hover or click partial status | partial price row | status details visible | Missing price dimensions are listed; total shows `partial est.` | Aggregate preserves missing-dimension status | Existing pricing semantics retained |
| task_stats_mixed_team | task-statistics | Team row | Load mixed runtime/model team | mixed data | row shows `Mixed` | Runtime/model columns show `Mixed`; details/member rows expose individual runtime/model | Aggregate contains observed runtime/model sets from ledger | No model-only collapse |
| task_stats_sort_total_cost | task-statistics | Task table | Click Total Cost header | table loaded, some rows expanded | top-level rows resorted | Expanded member rows stay attached under parent team | Sorting operates on top-level rows only | |
| task_stats_sort_created_time | task-statistics | Task table | Click Created Time header | table loaded | newest/oldest toggle | Toggle applies only to top-level rows; repeated team runs remain separate | Sort key is top-level `runCreatedAt` or fallback observed time | |
| task_stats_created_time_last_column | task-statistics | Task table | Table loaded | loaded | visible columns rendered | `Created Time` appears as the last visible Task-grouping column while default sort remains newest-first | Column order matches requirements | User scans task/cost first |
| model_grouping_runtime_grouping | model-statistics | Model grouping | Select `Model` | `Task` grouping selected | model table visible | Runtime column is visible; `Codex + gpt-5.5` and `Autobyteus + gpt-5.5` appear as separate rows | Groups by runtime/model pair | Preserves diagnostics without ambiguity |
| model_grouping_cache_output_terms | model-statistics | Model grouping | Load model table | model rows available | table visible | Columns use Input/Output terminology; cached input and thinking included indicators are visible when data exists | Uses existing aggregate/cache fields | Replaces Prompt/Assistant terminology in Settings display |
| compact_controls_no_helper | controls | Filter/control card | Page load | controls visible | compact card visible | Grouping select, date controls, and fetch action share one card; no `Usage during period` label/tooltip, paragraph card, separate tab row, range dropdown, or `Tasks created in period` option appears | Query remains observed-period only | Prevents fake future mode and stale helper copy |
| empty_task_range | task-statistics | Task grouping | Fetch range with no usage | date range selected | empty state visible | `No agent or team usage found for this date range.` | Empty rows array handled | |
