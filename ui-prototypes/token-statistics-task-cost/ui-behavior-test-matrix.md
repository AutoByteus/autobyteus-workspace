# Token Statistics Task-Cost UI Behavior Test Matrix

> Current scope: these scenarios govern the `Run details` sibling. Analytics is
> the Settings > Token Statistics default. "No separate tab row" below means no
> duplicate Task/Model tabs inside Run details; it does not remove the outer
> Analytics/Run details semantic tablist.

| Scenario ID | Area | Surface | Trigger | Initial State | Expected UI State | Expected User-Visible Result | Backend / State Expectation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| task_stats_default_load | task-statistics | Settings > Token Statistics > Run details | Select `Run details` | empty/loading | task table loaded | Run details starts with one compact control card; grouping select shows `Task`; date controls follow; one short sentence explains that creation time selects runs and values are lifetime totals; no repeated title, `Usage during period`, `Select Date Range:`, `Group by:`, or separate `By Task` / `By Model` tab row is rendered; rows sorted by `Created Time` desc | Uses `tokenUsageTaskStatisticsInPeriod(startTime, endTime)`; no `rangeMode` argument | Analytics remains the outer page default; within Run details the default grouping is task/team rows, not model rows |
| task_stats_repeated_team_runs | task-statistics | Task table | Data loaded | five Software Engineering Team runs selected by creation time | five separate top-level team rows | Rows are ordered by top-level `runCreatedAt` newest first and not collapsed by name/runtime/model/date; values are each run's lifetime totals | Group by `rootTeamRunId`, not team name | Demonstrates per-task/run cost scanning |
| task_stats_expand_team_usage_only | task-statistics | Task table | Click team chevron | team collapsed; team roster may have six live members; only `solution_designer` has a selected current run record | team expanded | Only usage-derived member rows are required; `solution_designer` appears; inactive/no-usage roster members may be omitted | Child rows use exact current `runId` and `rootTeamRunId`; no roster merge is required for no-usage members | Page is usage report, not roster viewer |
| task_stats_expand_team_no_double_count | task-statistics | Task table | Click team chevron | team collapsed | team expanded | Member rows appear indented beneath team; member costs sum to team total subject to mixed/partial pricing; member runs do not also appear as standalone top-level rows | Top-level standalone rows exclude records with `rootTeamRunId`; team totals sum each selected concrete run record once | Prevents double-counting |
| task_stats_nested_execution_address | task-statistics | Task table | Expand a team containing delegated/task-created member runs | selected current records include exact root, run, and task identity | member children visible | Concrete member/task-agent run rows appear under the root Team row rather than as unrelated top-level rows | Uses backend-provided `children`, `rootTeamRunId`, `runId`, and task metadata; current Token Usage does not consume legacy execution paths as hierarchy authority | Current backend emits the supported Team-to-member level; deeper topology needs a separately designed contract |
| task_stats_metadata_deleted | task-statistics | Task table | Load historical usage after live run metadata deletion/rename | token-statistics display fields were captured before metadata change | row loaded | Team/agent rows still show persisted `teamName`/`agentName`, `runSummary`, `runCreatedAt`; member rows show `memberName` | Statistics response uses token-statistics-owned display fields, not a permanent live metadata join | Validates self-contained display fields |
| task_stats_legacy_metadata_missing | task-statistics | Task table | Load migrated run record with no creation/display metadata | usage exists | fallback row loaded | Row uses explicit `Unknown` label and `First usage observed` tooltip/details for fallback time | Falls back to canonical run identity and `firstObservedAt`; does not suppress lifetime costs | Released-data limitation is explicit |
| task_stats_member_created_time_absent | task-statistics | Task table | Expand team | usage-derived member rows visible | member rows loaded | Member `Created Time` cell shows `—`, `same as team`, or muted inherited team time; it does not claim member-run creation time | No `memberCreatedAt` persistence/API field is required | Enforces minimal field policy |
| task_stats_visible_sort_affordances | task-statistics | Task table headers | Table loaded | task table loaded | six sortable headers show persistent compact sort glyphs | `Task / Run`, `Runtime`, `Input`, `Output`, `Total Cost`, and `Created Time` expose visible neutral two-triangle indicators, active current-color direction, and accessible sort labels/state; `Model(s)`, `Input Cost`, and `Output Cost` are plain non-button headers | Sort state remains local to the table and only top-level rows are reordered | Prevents hover-only/hidden sort discovery without oversized icons |
| task_stats_reduced_columns | task-statistics | Task table | Table loaded | task table loaded | nine visible columns rendered | Standalone `Type` and `Status` headers/cells are absent; row kind remains understandable through hierarchy, indentation, chevrons, and metadata | Backend still returns `rowKind`/cost status for behavior and details | Removes redundant width without losing semantics |
| task_stats_click_cost_breakdown | task-statistics | Total Cost value disclosure | Click the value-plus-solid-triangle button in the `Total Cost` cell | table row loaded | breakdown panel visible | The always-visible button text includes the formatted Total Cost value/status plus one solid CSS triangle; `aria-label` and `title` include the row identity and the same formatted cost/status; `aria-expanded` and `aria-controls` link to the detail row; there is no visible text `Details` button; `Input Cost` and `Output Cost` remain plain values; input breakdown shows uncached/cache-read/cache-write/total input; output breakdown shows output tokens and thinking included; total cost is not double-counted | Uses backend aggregate fields; frontend does not recompute prices | Thinking is output diagnostic/subset |
| task_stats_partial_price | task-statistics | Total Cost status/details | View partial row, then click the Total Cost value-plus-solid-triangle disclosure | partial price row | formatted status and details visible | The main row shows non-complete status through formatted cost text such as `partial est.`; duplicate inline `Partial estimate` badge is absent before expansion; expanded details show the full status and missing price dimensions; normal complete rows do not repeat `Complete estimate` in the main row | Aggregate preserves missing-dimension status | Existing pricing semantics retained without a standalone Status column |
| task_stats_mixed_team | task-statistics | Team row | Load mixed runtime/model team | mixed data | row shows `Mixed` | Runtime/model columns show `Mixed`; details/member rows expose individual runtime/model | Aggregate contains identity summaries from the selected current run records | No model-only collapse |
| task_stats_sort_total_cost | task-statistics | Task table | Click Total Cost header | table loaded, some rows expanded | top-level rows resorted | Expanded member rows stay attached under parent team | Sorting operates on top-level rows only | |
| task_stats_sort_created_time | task-statistics | Task table | Click Created Time header | table loaded | newest/oldest toggle | Toggle applies only to top-level rows; repeated team runs remain separate | Sort key is top-level `runCreatedAt` or fallback observed time | |
| task_stats_created_time_last_column | task-statistics | Task table | Table loaded | loaded | visible columns rendered | `Created Time` appears as the last visible Task-grouping column while default sort remains newest-first | Column order matches requirements | User scans task/cost first |
| model_grouping_runtime_grouping | model-statistics | Model grouping | Select `Model` | `Task` grouping selected | model table visible | Runtime column is visible; `Codex + gpt-5.5` and `Autobyteus + gpt-5.5` appear as separate rows | Groups by runtime/model pair | Preserves diagnostics without ambiguity |
| model_grouping_cache_output_terms | model-statistics | Model grouping | Load model table | model rows available | table visible | Columns use Input/Output terminology; cached input and thinking included indicators are visible when data exists | Uses existing aggregate/cache fields | Replaces Prompt/Assistant terminology in Settings display |
| compact_controls_range_helper | controls | Filter/control card | Page load | controls visible | compact card visible | Grouping select, date controls, and fetch action share one card; one short sentence explains creation-time selection and lifetime totals; no `Usage during period` label/tooltip, large paragraph card, separate tab row, or range-mode dropdown appears | Query selects current run records by `runCreatedAt`, falling back to `firstObservedAt` | Makes the changed semantics explicit without inventing another mode |
| empty_task_range | task-statistics | Task grouping | Fetch range with no usage | date range selected | empty state visible | `No agent or team usage found for this date range.` | Empty rows array handled | |
