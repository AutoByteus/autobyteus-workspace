## Fixes

- Fixed Settings → Token Statistics reports failing when supported token totals exceed GraphQL's signed 32-bit integer range.
- Preserved and displayed exact large Input and Output token totals as full locale-aware digits in the Task table.
