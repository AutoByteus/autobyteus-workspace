## Improvements
- Updated the Nodes sidebar icon to a clearer custom network/hierarchy symbol instead of a database-like icon.
- Simplified the MCP Gateway page around endpoint/config copying with clearer layout and copy success feedback.
- Fixed the Gateway endpoint display so it includes the backend host and port in browser-dev mode.
- Removed the duplicate Gateway tools list and extra helper note from the Gateway page.
