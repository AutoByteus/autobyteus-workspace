# Release Notes — Session Discovery UI

- Reworked Workspaces history into a session-first list so standalone agent runs and team runs appear directly under each workspace.
- Simplified sidebar rows by removing redundant source/member initials chips, using compact team subtitles, and tightening hierarchy indentation.
- Improved row alignment with fixed disclosure lanes, equal placeholders for non-expandable rows, and title-row arrow/status-dot alignment.
- Preserved inline team member, subteam, delegated task, and transient execution details within expanded session rows.
- Fixed the team header New (`+`) action for task-trail/team-task runs so it opens a valid cloned team run configuration instead of showing `Definition not found`.
