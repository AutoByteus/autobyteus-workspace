# Investigation Notes

## Investigation Meta
- Package identifier: agy-runtime-image-codex-prep-20260926
- Request: Image-generation capability mismatch and AGY-backed Codex preparation failure
- Workspace root: /Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-runtime-capabilities-20260926
- Repository mode: Git
- Task worktree / branch: /Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-runtime-capabilities-20260926 / task/agy-runtime-capabilities-20260926
- Resolved base remote / branch / revision: origin/personal / ae3aba1bfb7af6fefd8c69994e0b1bc421967d60
- Finalization target remote / branch: origin/personal
- Bootstrap result: Isolated worktree created after git fetch origin personal, clean at creation
- Bootstrap blocker: None
- Current solution revision ID: SR-023
- Investigation status: User-approved DEC-006/E-055 narrows image scope to AGY-native invocation. ARCH-REV-007/F-004 then found SR-022's terminal provider-result public failure path could leak raw `result.error`/failed `response` (E-056). SR-023 corrects that AGY-local return boundary without restoring image copy/Files/finalization. E-048 exact-eight and SR-013 skill approvals remain unchanged. No new provider experiment or app/API-E2E success is claimed.

## Initial Request And Clarifications
- Original request: User's CLI transcript shows native GenerateImage creating a dog JPG; four app screenshots show AGY-backed Solution Designer denying image generation and AGY-backed Codex failing before first response. User asked why and to address both.
- Clarifications received: User confirmed native AGY tools should accompany separately configured MCP tools, and prefers excluding AGY-native subagents and messaging if the CLI permits it.
- User facts: CLI saved dog to /Users/normy/.gemini/antigravity-cli/brain/0a99e02a-13a7-4cee-a114-56ad6a15b662/golden_retriever_puppy_1790401987159.jpg. Screenshots show Org Gemini 3.8 Flash Low, standalone Codex Gemini 3.8 Flash High, auto-approve enabled.
- Initial ambiguity, now resolved: User wants correction, not merely diagnosis. Native AGY GenerateImage—not app-owned media generation—is central. Original broad-native exposure was narrowed by E-048 to the exact eight approved names plus separately configured MCP.
- Approval: In reply to the explicit SR-005 scope-approval question on 2026-09-26, user answered “Okay” and restated AGY-native tools should not be blocked except native subagent/messaging tools where removable because AutoByteus provides collaboration MCP. The original Codex failure request remains in scope. The subsequent user request to warn and continue when a skill does not exist changes that approved behavior; SR-009 was explicitly approved in SR-010. The user later explicitly authorized bounded AGY experiments for tool selection (E-044); E-045–E-047 record those results. E-048 approved the selected eight names.

## Product And Domain Understanding
- Product area: AutoByteus desktop/web Agent and Org runtime integration, external AGY CLI, app-owned Agent Tools/media tools, agent-package configuration.
- Affected actors: user, Solution Designer Org member, standalone Codex agent, app operator.
- Terms: Runtime chooses AGY process; model chooses text inference; generated AGY agent frontmatter independently restricts native built-ins; app Agent Tools MCP exposure is separate from CLI-native GenerateImage.

## Evidence Ledger And Source Log
| ID | Exact source / command | Observation | Confidence / Limit |
| --- | --- | --- | --- |
| E-001 | User CLI transcript; stat of dog path | CLI emitted GenerateImage(golden_retriever_puppy); JPG exists, 863319 bytes. | High for standalone CLI only |
| E-002 | Four user screenshots; user-provided transcript | App Solution Designer denied image ability; Org AGY runtime and auto-approve shown; Codex screenshot shows generic preparation error. | High for displayed state; screenshot alone not root cause |
| E-003 | /Users/normy/autobyteus-org/autobyteus-agents/agent-teams/software-engineering-team/agents/solution-designer/agent-config.json; workspace server src/agent-execution/shared/runtime-agent-tool-exposure.ts; src/agent-execution/backends/antigravity/capsule/agy-run-capsule.ts | Solution Designer toolNames omits generate_image; run exposure derives from toolNames; generated AGY main-agent frontmatter lists coding tools, not native image tool. | High for configuration-based absence |
| E-004 | /Users/normy/.autobyteus/server-data/logs/server.log lines 242148-242153 and 242205-242210; /Users/normy/.autobyteus/server-data/memory/agents/codex_9a0e9743d0cd44339e7d3ae60a20da25/run_metadata.json | Log twice warns Codex's software-engineering-workflow-skill could not resolve, then rejects SEND_MESSAGE with ACTIVATION_FAILED; metadata has AGY runtime, null platform ID, startedAt null. | High for preparation-stage failure; underlying exception not logged explicitly |
| E-005 | /Users/normy/autobyteus-org/autobyteus-agents/agents/codex/agent-config.json and /Users/normy/autobyteus-org/autobyteus-agents/README.md:18; `ls -l`/`stat` of package `.codex/skills/software-engineering-workflow-skill`; inspected configured skills path | Codex declares `software-engineering-workflow-skill`; repository README says a global skill is intended. Package `.codex/skills/software-engineering-workflow-skill` is a dangling symlink to `/home/autobyteus/workspace/autobyteus-skills/...` on this Mac and is not among SkillService's supported package-local discovery locations. A real sibling `/Users/normy/autobyteus-org/autobyteus-skills/software-engineering-workflow-skill` exists, but configured `AUTOBYTEUS_SKILLS_PATHS` points to `/Users/normy/agent-skills/skills`, where this skill is absent. | High for unresolved skill cause and packaging/configuration defect |
| E-006 | workspace server src/agent-execution/backends/antigravity/capsule/agy-configured-skill-materializer.ts:125; src/agent-execution/backends/antigravity/backend/agy-agent-run-backend-factory.ts; src/agent-execution/services/agent-run-manager.ts:381; tests/integration/agent/agent-websocket.integration.test.ts:549-594 | Unresolved skill binding throws AGY_CONFIGURED_SKILL_UNRESOLVED before process start; unexpected error wrapped as generic Failed to prepare agent run; test expressly expects generic UI/WebSocket status for private failures. | Strong causal inference from matching log warning and source path; no raw exception captured |
| E-007 | workspace server src/agent-tools/media/media-generation-service.ts; media-tool-model-resolver.ts; docs/modules/agent_tools.md:273-340; /Users/normy/.autobyteus/server-data/.env model setting only | App's generate_image uses separate model resolver/provider and returns file_path; local selected image model is gemini-3-pro-image. | High for path/model; credential readiness unverified |
| E-008 | workspace server docs/modules/antigravity_cli_runtime.md; src/agent-execution/backends/antigravity/backend/agy-agent-run-backend-factory.ts; inspected AGY capsule under memory/agent_orgs/software_development_department_c648df35dd5b4b75b0a3ecf9d79f7b0e/.../solution_designer_5bbdf4098bad4ddc8bc43e43fbca60a6 | App launches a generated AGY main agent with selected tools, skills and run-scoped MCP; not the same capability context as standalone CLI. | High for observed run |
| E-009 | workspace server src/agent-execution/events/processors/file-change/{file-change-tool-semantics.ts,file-change-output-path.ts,file-change-event-processor.ts}; src/agent-execution/backends/antigravity/stream/agy-stream-event-converter.ts | AGY init lists native tool as generate_image (lower case), which is a known generated-output name; however AGY converter wraps output under result.output and native output payload/path shape is unknown, so automatic app file projection is unproven. The CLI UI display GenerateImage is not proof of canonical event casing. | High for source and probe; native image payload unverified |
| E-010 | workspace server src/agent-execution/backends/antigravity/capsule/agy-run-capsule.ts; src/agent-execution/backends/claude/session/claude-session-tooling-options.ts; docs/modules/agent_execution.md:452-466; src/agent-execution/backends/codex/thread/codex-thread-config.ts; local agy --version/--help | AutoByteus AGY generated agent hardcodes eight coding tools excluding GenerateImage. Claude native built-ins are also explicitly curated to ten names; Codex thread config does not show the same eight-tool AGY restriction. Local AGY version 1.2.11. | High for local policy; AGY YAML semantics to validate |
| E-011 | User follow-up, 2026-09-26 | User specifically asks why AGY-native GenerateImage is unavailable when launched via app and compares with Codex/Claude native tools. | Direct intended-behavior clarification; not approval |
| E-012 | User follow-up; local AGY 1.2.11 stream-json/no-side-effect probes under isolated temporary projects; official https://antigravity.google/docs/cli/subagents and https://antigravity.google/docs/cli/headless/ | User favors all default AGY-native tools plus configured MCP. AGY custom-agent tools frontmatter is an explicit permission list. Restricted custom agent reported no list_permissions; no-tools-field custom agent reported native generate_image; default agent reported image plus orchestration. AGY init.tools includes registry names even when model-declared tools are restricted, so init.tools alone is not proof of exposure. | Strong local evidence for suppression mechanism; exhaustive set/MCP coexistence require validation |
| E-013 | User interruption/clarification 2026-09-26; isolated AGY 1.2.11 custom main-agent probe with tools [view_file, run_command, generate_image, search_web, read_url_content]; official https://antigravity.google/docs/cli/subagents; prior approved AGY ticket design-spec.md:50,271,312 and investigation-notes.md:144,164,168 | User prefers native subagent/messaging exclusion if supported. Probe reported generate_image/view_file/search_web available and invoke_subagent/send_message unavailable. CLI docs show explicit tools allowlist, no documented subtractive custom-agent denylist; SDK docs mention disabled_tools but app uses CLI. Prior AGY design intentionally chose eight verified coding names because earlier no-frontmatter probes lacked file/shell and wildcard/unmapped names failed; native subagent integration was out of scope. | Strong for selective exclusion on installed 1.2.11; versioned full native set and MCP coexistence remain to validate |
| E-014 | User correction, 2026-09-26 | User explicitly says the requested image tool is AGY's own native GenerateImage, not AutoByteus MCP `generate_image`. | Direct scope clarification; not approval |
| E-015 | User approval/continuation, 2026-09-26 | User answered “Okay” to the SR-005 scope approval request, restated the native-tool/collaboration boundary, and asked to continue without further experiments. | Approval of SR-005 intended behavior; live native-image payload still unobserved |
| E-016 | `sed`/`rg` read of `autobyteus-server-ts/src/agent-execution/backends/antigravity/{backend/agy-agent-run-backend-factory.ts,capsule/agy-run-capsule.ts,capsule/agy-mcp-config-materializer.ts,stream/agy-stream-event-converter.ts,stream/agy-stream-process.ts}` | Factory resolves skills, activates separately scoped MCP, writes capsule and launches CLI; capsule hardcodes eight native coding tools; converter maps AGY `step_update` tool state/name/parameters/output to canonical tool events with `result: {provider_state, output}`; headless CLI sees `--agent` and run capsule. | High, static production path |
| E-017 | `sed`/`rg` read of `autobyteus-server-ts/src/agent-execution/events/processors/file-change/{file-change-event-processor.ts,file-change-tool-semantics.ts,file-change-output-path.ts,file-change-payload-builder.ts}`, `src/api/rest/run-file-changes.ts`, `src/run-history/services/run-file-change-projection-service.ts` | Shared file-change processor recognizes `generate_image` and MCP-qualified names, but path extractor does not inspect AGY wrapper `result.output`; even with a path, accessible content requires a projected file and existing regular file. File-content endpoint serves an indexed file path. | High for present code; native result schema unverified |
| E-018 | Existing `tickets/done/antigravity-cli-runtime-redesign-20260924/agy-capsule-mcp-probe/stdout.jsonl` and `agy-mcp-config-precedence-probe/stdout.jsonl` | Captured AGY MCP invocation uses provider `tool_name: call_mcp_tool` with `ServerName`/`ToolName` in parameters, distinct from native `tool_name: generate_image`; previous design reports MCP discovery works without listing `call_mcp_tool` in custom-agent frontmatter. | High for recorded AGY 1.2.10 cases; not a fresh probe |
| E-019 | `ls -l`/`stat` of package `.codex/skills/software-engineering-workflow-skill`; `sed` read of `autobyteus-server-ts/src/skills/services/{skill-service.ts,configured-agent-skill-resolver.ts,skill-discovery.ts}` and app config | Package `.codex` link is dangling, while SkillService searches configured global roots and package `agents/<id>/skills`/team skills, not package `.codex/skills`; local `AUTOBYTEUS_SKILLS_PATHS` points to a different skill directory. The actual source skill exists in sibling `autobyteus-skills` repository. | High for local packaging/discovery mismatch |
| E-020 | `sed` read of `src/agent-execution/services/{agent-run-manager.ts,agent-run-command-coordinator.ts}`, `src/agent-execution/errors.ts`, `tests/integration/agent/agent-websocket.integration.test.ts:549-594` | AGY unresolved-skill error is unexpected to manager and becomes generic `AgentCreationError`; coordinator displays its message in error status/ACK; current test enforces redaction of arbitrary private cause. | High for safe error design constraint |
| E-021 | `design-review-report.md` ARCH-REV-001/F-001; static read of `autobyteus-server-ts/src/agent-execution/backends/antigravity/stream/agy-stream-event-converter.ts:68-94` and `autobyteus-web/services/agentStreaming/handlers/toolLifecycleHandler.ts:404-435` | Converter currently copies arbitrary AGY `tool_info.error` into canonical `error`/`reason`, and web handler stores/displays the canonical error. Public native-image failure is therefore not guaranteed to meet approved AC-002/QR-001. No live credential leak was observed or claimed. | High for current path and design gap; raw native image error payload not observed |
| E-022 | User follow-up, 2026-09-26: “when the skill does not exist, it should be a warning in the backend, it should not block the agy agent from starting up … codex runtime, and claude runtime both follow this design” | Explicit change of intended missing-skill outcome from blocking to backend warning plus continued AGY startup. | Direct user direction; full revised requirements baseline still needs explicit approval |
| E-023 | Static read of `autobyteus-server-ts/src/agent-execution/backends/{codex/backend/codex-thread-bootstrapper.ts:339-387,claude/backend/claude-session-bootstrapper.ts:70-116,shared/workspace-skill-materializer.ts:95-117,265-324}`, `src/skills/services/configured-agent-skill-resolver.ts:50-92`, `src/agent-execution/backends/antigravity/capsule/agy-configured-skill-materializer.ts:119-143`; unit tests `tests/unit/agent-execution/backends/{shared/workspace-skill-materializer.test.ts:148-166,codex/backend/codex-thread-bootstrapper.test.ts:403-421,claude/backend/claude-session-bootstrapper.test.ts:93-115}` | Codex and Claude translate unresolved bindings into `reconcile-unresolved`; shared materializer warns and returns no skill when path is missing, and can remove a matching broken link. AGY instead throws `AGY_CONFIGURED_SKILL_UNRESOLVED` before startup. Shared materializer still throws for collisions or other unsafe states; this does not prove that every source problem is ignorable. | High for static behavior and tests; no new live runtime experiment |

| E-024 | User reply “yesss” on 2026-09-26 to the explicit SR-009 approval question | Approves backend warning/nonblocking startup for genuinely missing configured skills, preservation of available skills, no false loaded-skill claim, continued failure on unsafe/unrelated problems, and portable Codex workflow skill for normal setup. | Direct explicit approval of SR-009 baseline |
| E-025 | Static read of `autobyteus-server-ts/src/agent-execution/backends/antigravity/{backend/agy-agent-run-backend-factory.ts:29-59,capsule/agy-run-capsule.ts:27-70,capsule/agy-configured-skill-materializer.ts:1-143}` | AGY factory knows run ID and obtains bindings; capsule already receives run ID, invokes skill materializer and writes manifest `skills` from returned snapshots. Restore validates only recorded snapshots. Run-scoped warning and omission of absent skill can therefore be owned by the materializer without a manifest-version or restore change. | High for static current path; no new AGY process run |
| E-026 | `design-review-report.md` ARCH-REV-002/F-002 with MP-001; static reread of `autobyteus-server-ts/src/skills/services/{configured-agent-skill-resolver.ts:50-76,130-174,skill-service.ts:72-113,219-231,skill-discovery.ts:25,170-191}`, `src/skills/loader.ts:1-29`, and `src/skills/domain/configured-agent-skill-binding.ts` | Current `kind:"unresolved"` conflates genuinely absent names with a present contextual candidate whose `SKILL.md` is malformed/name-mismatched or whose load fails; a present directory without a valid `SKILL.md` can also be skipped by discovery. AGY materializer cannot infer certified absence from this binding. The configured global lookup likewise filters directory candidates without a manifest before returning a skill. | High for current source and supported local-package path; no AGY process run |
| E-027 | User follow-up after SR-011 review handoff, 2026-09-26 | User prefers future AGY symlink-based live skill content updates like Codex/Claude, but explicitly deferred this to a separate ticket after the current ticket is done. | Direct scope deferral; non-authoritative for this package |
| E-028 | User follow-up after ARCH-REV-003 Pass and implementation handoff, 2026-09-26 | User challenges the invalid-skill hard-failure rule: a present invalid `SKILL.md` should not be rejected by AutoByteus before AGY startup; they ask whether AGY simply copies a skill folder and whether AutoByteus validates it. | Direct intended-behavior change; later E-030 resolves copy-through versus omission |
| E-029 | Static reread of `autobyteus-server-ts/src/skills/{loader.ts,services/configured-agent-skill-resolver.ts}` and current in-progress `src/agent-execution/backends/antigravity/capsule/agy-configured-skill-materializer.ts` | AutoByteus parses/validates `SKILL.md` before AGY copying; loader requires frontmatter with nonblank name/description, and resolver can map malformed/name-mismatched contextual candidates to unresolved. In-progress AGY materializer follows SR-012: `certified_absent` warns/skips, `invalid_candidate` throws, and valid resolved sources are checked/copied. Production files are being changed by Implementation Engineer; this is not a finalized behavior claim. | High for static code at inspection time; no new AGY process run |
| E-030 | User follow-ups, 2026-09-26: “just copy through let agy handle it. i think it depends on how agy handles it right?” and “just let the runtime its self handle it” | User selects content-preserving copy-through for a present skill folder, leaving `SKILL.md` acceptance/rejection to AGY rather than AutoByteus. This is not evidence that AGY will accept malformed content or guarantee a first turn. | Direct clarification of DEC-003; full SR-013 safety/provider-failure boundary still awaits explicit approval |
| E-031 | `code-review-report.md` CRR-001 Blocked — Requirement Gap, 2026-09-26 | Reviewer stopped before source score or API/E2E handoff because IR-001 follows reviewed SR-012 hard-failure policy while SR-013 changed intended skill behavior. Report was authored before E-030 resolved copy-through versus omission; its remaining approval/design block still applies. No implementation defect was attributed. | Downstream review evidence; not user approval |
| E-032 | Static Codex App Server comparison, `codex-thread-bootstrapper.ts:227-260,339-394`, shared `configured-agent-skill-resolver.ts:50-92,184-226`, `loader.ts:7-100`, `workspace-skill-materializer.ts:206-226,285-324,360-368` | AutoByteus does semantically parse `SKILL.md` in shared resolution before Codex materialization (frontmatter name/description). An invalid contextual skill candidate is logged/skipped and may become `unresolved`; Codex maps unresolved to reconciliation that warns/skips. The materializer checks only that `SKILL.md` is a file before linking a resolved source; Codex App Server `skills/list` is also used for discoverability preflight. This is not a pure raw-folder pass-through, nor proof that every invalid/global or path-collision case is nonblocking: global loader errors and destination collisions can still throw. | High for static code, limited to inspected paths; no live Codex run |
| E-033 | User clarification, 2026-09-26: “if for codex app server runtime, we didnt judge, then here we do not judge as well. ... if there we judged, then here we also judge.” | User makes AGY semantic skill-validation policy conditional on actual Codex App Server behavior. E-032 establishes that AutoByteus does parse/validate configured skill metadata for Codex, so the chosen AGY direction is to judge as well; earlier E-028 still requires invalid content not to block an otherwise healthy run. | Direct intended-behavior clarification superseding E-030 copy-through proposal; full SR-013 baseline pending approval |
| E-034 | User reply “approve” on 2026-09-26 to explicit SR-013 scope question | Approves AutoByteus metadata/name validation for AGY as for Codex; missing/invalid configured skills warn and skip without blocking otherwise healthy startup; unsafe filesystem paths/destination collisions remain separate safety failures. | Explicit approval of reconciled SR-013 requirements; native-image/tool scope unchanged |
| E-035 | Static architecture reread after E-034: `autobyteus-server-ts/src/skills/{domain/configured-agent-skill-binding.ts,services/configured-agent-skill-resolver.ts,services/configured-skill-source-fingerprint.ts,loader.ts}`, `src/agent-execution/backends/antigravity/{backend/agy-agent-run-backend-factory.ts,capsule/agy-configured-skill-materializer.ts}` plus existing unit tests | IR-001 detailed resolver returns `invalid_candidate` for unsafe name or present invalid candidate; its broad catch also converts any loader **or fingerprint/provenance** error to `invalid_candidate`. AGY materializer currently throws on all `invalid_candidate`; valid `resolved` sources carry provenance/hash and use no-follow checked snapshot; post-resolution source mutation, out-of-bounds links and destination collision already have coded failure paths. AGY factory passes detailed outcomes and run identity before capsule creation. A narrow correction can make semantic invalidity warn/omit, but must separate fingerprint/provenance failures from skippable content invalidity and sanitize unsafe configured names in warnings. | High for static in-progress IR-001 code; no new AGY process run |
| E-036 | `api-e2e-execution-coverage-report.md` API-REV-001; `code-review-report.md` CRR-004; `api-e2e-evidence/{real-native-image.json,real-codex-skill.json,live-image.log,live-codex.log}`; current `agy-native-tool-policy.ts` | Real installed AGY 1.2.11 initializes with SR-015's 49-name native custom-agent allowlist, then fails first turn/exit 3 because 25 listed non-collaboration registry names are unknown as custom-agent components. `init.tools` inventory is not proof those names are acceptable in custom-agent frontmatter or model-exposed. Both image and bundled-Codex first turns are blocked. | High direct API/E2E/provider evidence; no new recovery probe |
| E-037 | API-REV-001/CRR-004; `api-e2e-evidence/{diagnostic-native-image.json,diagnostic-native-image-file.json,diagnostic-image.log,diagnostic-image-params.log,diagnostic-codex-skill.json}` | Five-name diagnostic-only custom profile invokes genuine native `generate_image` and creates a regular 220,997-byte JPEG; Codex reads bundled skill and replies. In four native generations, streamed DONE `tool_info` has no output/result/path, so SR-015's explicit-step-result-only converter cannot project the image. Final model prose contains a path but is not an approved artifact source. The narrow profile is isolation evidence, not a production fallback. | High for four observed AGY 1.2.11 generations; not full native/MCP/Team parity |
| E-038 | Read-only inspection of the four already-created provider conversation transcripts at `~/.gemini/antigravity-cli/brain/{6be321dd-cd1f-49e9-bc3c-6e14a19ddc4e,09e41b7c-0889-4b28-96e2-e0d6131b0111,143989f0-2abe-4802-878c-ce41d18b92c4,4c97d2c8-4b24-48e5-86f5-e73fba2f143f}/.system_generated/logs/transcript.jsonl`; `python3` JSON inspection of `tool_calls` and `media` only | In every transcript, a `MODEL/PLANNER_RESPONSE/DONE` step has `tool_calls[].name=generate_image`, followed by a `MODEL/GENERIC/DONE` step with structured `media[]` `mime_type=image/jpeg` and a `file://` URI to the generated JPEG inside that exact conversation's brain directory. This is provider transcript structure, not model final prose. It is a candidate image-output source; timing, correlation across multi-turn/multi-image runs, file authenticity, path containment and app projection still need design and executable verification. | High for these four saved conversations; not a documented stable `media[]` API |
| E-039 | Official [AGY CLI custom agents](https://antigravity.google/docs/cli/subagents), [CLI hooks](https://antigravity.google/docs/hooks), [headless](https://antigravity.google/docs/cli/headless/), [SDK tools](https://antigravity.google/docs/sdk/tools/), [CLI changelog](https://github.com/google-antigravity/antigravity-cli/blob/main/CHANGELOG.md), read 2026-09-26 | CLI custom-agent `tools` is an explicit list and warns against unmapped names. CLI `PreToolUse` can deny matching native calls before execution; documented hook common fields include exact `conversationId`, `transcriptPath`, and `artifactDirectoryPath`. CLI 1.2.1 added `excludeDefaultComponents`, implying default components may be inherited unless excluded, but this does not prove all coding tools or a subtractive hidden-tool policy for the selected custom main agent. SDK `disabled_tools` is a different integration, not a CLI switch. A deny hook blocks execution, not model exposure; that distinction requires user approval against current REQ-006/AC-006. | Official provider contract for documented controls; exact custom-main-agent combined behavior unverified |
| E-040 | Static read of `agy-stream-process.ts`, `agy-agent-run-backend.ts`, `agy-stream-event-converter.ts`, `agy-native-tool-policy.ts`; provider transcript E-038 | Stream process preserves conversation identity and emits turn result; backend synchronously converts/publishes events, currently marking a native DONE without explicit path as failure. A run-owned read of the matching transcript before terminal publication may recover structured media, but must not publish after the turn is acknowledged as complete or read another conversation. Profile has 49 names; 25 failed live. | High for code and existing traces; proposed timing/security design not yet proven |
| E-041 | User follow-up after SR-016: asks why 49 names were added when the prior eight-tool AGY run was nearly working, says many names are unnecessary, directs “please only include the names we want,” and requests the exact file/names | This is a material clarification toward a small task-specific native set, not approval of an exact selected list. Do not attribute the 49-name profile to user configuration or treat SR-016's hook option as approved. | Direct user direction; exact minimal list pending approval |
| E-042 | `git show origin/personal:autobyteus-server-ts/src/agent-execution/backends/antigravity/capsule/agy-run-capsule.ts`; current task-worktree `agy-native-tool-policy.ts`/`agy-run-capsule.ts`; prior `tickets/done/antigravity-cli-runtime-redesign-20260924/agy-main-agent-toolset-probe/summary.json`; API-REV-001 evidence JSON | `origin/personal` and pinned base use exactly eight coding names in custom-agent frontmatter; a prior AGY 1.2.10 control with those eight returned SUCCESS. Task branch replaces that constant with 49 names derived from 57 `init.tools` names after excluding eight collaboration/dispatcher entries. All 25 live AGY 1.2.11 rejected names are among the 40 additions beyond the old eight plus `generate_image`. `origin/personal` avoids this specific first-turn profile error but omits native image. | High static/retained test evidence; prior control is 1.2.10, not a fresh 1.2.11 nine-name run |
| E-043 | `git show origin/personal:.../agy-stream-event-converter.ts` and `.../file-change-output-path.ts`; task branch converter; API-REV-001 E-037/038 | In origin/personal, all tool DONE events wrap `info.output`; file projection only extracts explicit result/path fields and the selected custom agent cannot call native image. Therefore the absent native-image output path was *latent/unexercised*, not a second newly broken previously-working image path. Task code newly classifies native DONE without explicit path as safe failure; real AGY DONE lacks output. | High static plus four provider outputs; no baseline native-image success claim |
| E-044 | User follow-up after E-041: explicitly authorizes enough bounded AGY probing before choosing useful names; `agy --version` = 1.2.11. Disposable scripts `agy-minimal-native-profile-probe.py`, `agy-minimal-mcp-probe.py`, `agy-minimal-codex-skill-probe.py`; summaries in `agy-minimal-native-profile-evidence/` | Supersedes the earlier preference against more exploratory AGY experiments for this narrow profile investigation. Probes use temporary capsules/workspaces, fixed harmless prompts, no user-global config change, bounded timeouts, and leave provider-owned brain data intact. | Direct user authorization and reproducible local evidence; not a universal future-version guarantee |
| E-045 | `agy-minimal-native-profile-evidence/{baseline8_first_turn,core9_first_turn,core8_first_turn,core8_high_first_turn,core8_view_file,core8_list_dir,core8_find_by_name,core8_grep_search,core8_run_command,core8_write_to_file,core8_replace_file_content,core8_generate_image,core8_image_no_autoapprove,core8_high_generate_image}.json`; physical AGY brain JPEGs and exact conversation transcript `media[]` | On AGY 1.2.11, old 8, old 8+image (9), and proposed 7 coding+image (8) all return first-turn SUCCESS without unknown names. With the **exact proposed eight-name profile**, each of seven coding names is actually called ACTIVE→DONE in a separate bounded case; write and replace yield verified expected workspace bytes. Native `generate_image` is called ACTIVE→DONE under both Gemini 3.8 Flash Low and High; with and without auto-approve on Low. Three new regular JPEGs from these exact-profile cases are 401,093, 754,626 and 491,503 bytes, each with matching structured provider transcript media URI; streamed DONE still lacks image path. | Strong direct evidence for this installed CLI/profile/models/tasks; no claim of universal stability or app Files projection |
| E-046 | `agy-minimal-native-profile-evidence/{core9_multi_replace_forced,multi_only_exposure,multi_only_exposure_high,core9_multi_replace_file_content}.json` and corresponding retained provider transcripts | `multi_replace_file_content` is accepted as a frontmatter name but was **not actually invoked** in four attempts: model chose `replace_file_content` for a two-change task, and three forced prompts (including Low/High with no alternative edit tool) answered “unavailable” without editing. Its usefulness/actual model exposure on AGY 1.2.11 is unproven, so the proposed minimal set omits it rather than claiming it works because constructor succeeded. | Direct bounded observations; not proof the provider can never expose the tool under other conditions |
| E-047 | `agy-minimal-native-profile-evidence/{core8_mcp,core8_codex_skill_low,core8_codex_skill_high,core10_first_turn,core10_search_web,core10_read_url_content,research11_read_url_content,research11_read_url_approved}.json` | Exact eight-name profile plus disposable capsule-scoped MCP: first turn SUCCESS, real `call_mcp_tool` DONE, server `tools/call` received and marker returned without listing `call_mcp_tool` in native frontmatter. Bundled Codex skill on exact eight-name profile: first turn SUCCESS and native `view_file` reads skill with Stage 0 answer under both Low/High models. Optional `search_web` and `read_url_content` are callable in a ten-name profile; URL read soft-denies when auto-approve is off and succeeds when on. They are useful research candidates but not necessary for the reported image/Codex cases and not in the minimal proposal. | High for isolated provider cases; not full AutoByteus Team/Org/Files/browser validation |
| E-048 | User reply “approve” immediately after Solution Designer presented the exact eight names, the 49-name source file, seven coding-tool invocation proof, native image proof, and the explicit question whether to replace the 49-name policy | Explicit approval of DEC-005 and the narrower REQ-001/AC-005 eight-name native scope. Native image result/path, safe failure, scoped MCP, Codex skill and skill-warning outcomes remain unchanged. No approval of SR-016 hook. | Direct user approval of current intended behavior; architecture work may proceed |
| E-049 | Disposable two-turn installed AGY CLI 1.2.11 stream-json probe using the **exact eight names**, `gemini-3.8-flash-low`, isolated capsule/workspace, auto-approve, `--new-project`, fixed READY then blue-dot image prompts; sanitized summary `agy-minimal-native-profile-evidence/two_turn_transcript_correlation.json`; read-only inspection of retained conversation `~/.gemini/antigravity-cli/brain/3d5ce362-1127-4d6b-8259-4a2c04d7d876/.system_generated/logs/transcript.jsonl` and image metadata | `init` gives conversation ID but no transcript/artifact path. Turn 1 returns SUCCESS and transcript rows 0 USER_INPUT, 1 PLANNER_RESPONSE. Turn 2 returns SUCCESS with stream `generate_image` ACTIVE→DONE at `step_index=4`; at receipt of terminal `result`, transcript already has rows 2 USER_INPUT, 3 PLANNER_RESPONSE with `tool_calls[].name=generate_image`, 4 GENERIC/DONE with one `media[]` `image/jpeg` `file://` URI, 5 PLANNER_RESPONSE. The URI resolves inside that exact conversation brain root to a 370,645-byte regular JPEG with `FFD8FF` magic. The media step index matches the stream tool step index. CLI transcript shape is observed, not provider-documented stable API; app must fail safely if absent/drifted. | High direct multi-turn timing/correlation witness for installed version; one image turn, not full AutoByteus app or multi-image proof |
| E-050 | Static read of `agy-stream-process.ts`, `agy-agent-run-backend.ts`, `agy-stream-event-converter.ts`, `agy-native-image-result.ts`, `file-change-event-processor.ts`, `run-file-change-service.ts`, `run-file-change-projection-service.ts`, `api/rest/run-file-changes.ts`, `file-explorer/file-explorer.ts` | Backend converts/publishes stream messages synchronously and currently fails native DONE with no explicit output before turn result. Shared file-change projection accepts an explicit successful `result.file_path`, indexes absolute paths and REST `/runs/:runId/file-change-content` serves projected regular files; generic workspace FileExplorer is root-constrained and not the artifact endpoint. A provider-brain path might be serveable, but immutable run-owned copied image is safer against provider cleanup/path drift. Terminal publication must await transcript reconciliation and artifact write, and native image tool terminal must be deferred until result. | High static path analysis; target copy/reconciliation contract needs architecture review and executable test |
| E-051 | `design-review-report.md` ARCH-REV-005/F-003 and `architecture-review-revision-record.md`; `design-spec.md` SR-018 sequencing items 1–6; `agy-agent-run-backend.ts` result handler and `dispatchUserInput` gate; `agent-runtime-lifecycle-snapshot.ts` public phase enum; E-049 two-turn provider path | Reviewer found SR-018 both requires image reconciliation/publication before terminal app-turn completion and says not to hold next-turn eligibility; current backend clears `turnId` at provider result. A normal follow-up during async reconciliation could start a second baseline/turn before the prior image outcome is published. This is a **Design Impact** within approved REQ-002/AC-001/002, not a requirement change. The corrected design must distinguish synchronous provider-result receipt from app-turn completion and keep the input gate occupied until ordered terminal publication settles. The shared public lifecycle phase has no `finalizing` value; use a private AGY substate while public phase remains `running`. | High independent review plus static supported path; no new user approval or provider probe needed |
| E-052 | `implementation-handoff.md` IR-004 and `implementation-revision-record.md`; partial server source `77c9ffa28`, doc commit `117abcf44`; Implementation Engineer's source `tsc` and 100 focused unit tests | Partial exact-eight/transcript adapter passes local checks but is **not** Code Review/API-E2E-ready. The engineer found `AgentRun.postUserMessage` queues busy follow-ups before AGY `dispatchUserInput`, and its backend source callback swallows publication exceptions. Backend-only rejection/awaited `deliver` cannot implement SR-019's app ACK and false-idle claims. | High for inspected implementation path; no live app/provider validation in IR-004 |
| E-053 | Static read: `src/agent-execution/domain/agent-run.ts` constructor lines 83–99 and post/reserve lines 130–174; `src/agent-execution/input/{agent-run-input-admission-state.ts,agent-run-input-contract.ts}`; `src/agent-execution/events/{dispatch-processed-agent-run-events.ts,agent-run-event-dispatch-queue.ts}`; `src/agent-execution/backends/shared/runtime-event-dispatch.ts`; `src/agent-execution/services/agent-run-command-coordinator.ts`; `src/services/run-file-changes/run-file-change-service.ts`; `tests/unit/agent-execution/agent-run.test.ts` FIFO case | Admission state accepts/queues input while active and `claimNext` waits for terminal when append unsupported. Coordinator records accepted ACK at admission, not backend dispatch. `AgentRun` catches/logs source publication failure; public listener dispatch catches synchronous throws, while Files projection and other subscribers launch void async work that cannot be acknowledged by the source callback. Under SR-019’s now-disputed image finalization target, a pre-queue AGY-only busy rejection plus a scoped canonical/synchronous publication failure contract would be required. A queued input has no automatic post-backend-release trigger in the partial AGY finalization, so simply waiting on backend dispatch is not equivalent. | High static; downstream async delivery remains independently testable but not part of source Promise acknowledgment |
| E-054 | User message after IR-004, 2026-09-26: “Why do we care about where the image is? ... it's the runtime ... the runtime must have saved it to its own place ... what we only make sure ... the tool can be called ... that's it,” with display described as the agent calling the tool; Implementation Engineer's returned clarification message | User rejects AutoByteus ownership of AGY's image location/copy/serving as overscope. The intended observable output appears to be native invocation/status and ordinary AGY reply, not an app-owned image artifact. This conflicts with approved REQ-002/AC-001 path/Files promise and invalidates the transcript-copy/finalization design as a current implementation target. Confirm exact display/preview boundary before replacing requirements. | Direct user direction, material requirement change; explicit DEC-006 confirmation pending |
| E-055 | User follow-up, 2026-09-26: “Codex ... generated image ... our server doesn't even know where ... runtime itself ... only make sure that this tool is available and the AGY agent is able to call it. Then that's it ... remove the overscoped design you made.” | Explicit approval to remove AutoByteus image discovery/copy/Files/preview responsibility. Native AGY call must remain available and accurately visible as a normal tool event; AGY owns storage. The SR-019 result-bound transcript and special finalization/admission/publisher design is no longer needed for the requested behavior. This does not relax provider provenance, safe error mapping, exact-eight native policy, MCP scope or Codex skill outcomes. | Direct user approval of DEC-006; design may be reduced without another approval question |
| E-056 | `design-review-report.md` ARCH-REV-007/F-004 and review history; static read of current `agy-stream-event-converter.ts` `completeResult` fallback response/error branch (lines 157–162, 183–186); `agy-stream-message.ts`; `agent-run-error-evidence.ts`; `agent-turn-lifecycle-state.ts`; API-REV-001/E-036 real result ERROR without tool step | SR-022 correctly removes image artifact work but retains a public terminal-result failure leak: when no image-tool ERROR set a safe flag, failed `result.response` can become SEGMENT_CONTENT and raw `result.error` can become public AGY_TURN_ERROR. Result failure without any tool step is supported by observed provider behavior, and validated-skill AGY-origin rejection shares this path. Canonical terminal ERROR needs explicit turn/effect evidence; no fallback response/raw provider field, and private bounded diagnostic. | High independent review + static source + observed provider ERROR shape; no new user approval/provider experiment needed |


## Relevant Existing Behavior And Supported Product Paths
| Behavior | Supported Trigger | Current Path / Outcome | Evidence / Confidence |
| --- | --- | --- | --- |
| BEH-001 | User asks image in CLI or app AGY agent chat | Standalone CLI creates image. App-generated AGY agent has an eight-coding-tool native list omitting GenerateImage; Solution Designer also lacks app-owned media grant. | E-001 to E-003, E-010; high for config, native behavior observed |
| BEH-002 | User sends first prompt to standalone Codex with AGY | Prepared record created, activation checks skills, unresolved Codex skill prevents first turn; generic error shown. Package has a nonfunctional absolute symlink to an unregistered global skill source. Unlike AGY, Codex/Claude backends warn-and-skip truly unresolved skills. | E-002, E-004 to E-006, E-023; high |
| BEH-003 | System projects generated output | Live AGY native `generate_image` DONE has no output/path; structured conversation transcript media has the JPEG URI by terminal result, including a two-turn exact-profile probe. Current task-branch converter therefore fails real output, and full app Files projection remains unverified. | E-037/038, E-045/049/050; high for provider shape, app path open |

## Relevant Codebase And Technical Facts
| Component | Current responsibility / behavior | Requirement implication | Architecture question |
| --- | --- | --- | --- |
| Agent package Solution Designer config | Explicit toolNames excluding generate_image | Grant only intended agent capability | Package-source change vs runtime override |
| RuntimeAgentToolExposure and Agent Tools MCP catalog | Project configured supported tools into run session | AGY runtime name/model does not imply tool grant | Exact session tool advertisement validation |
| AGY capsule and backend factory | Pre-IR-001 baseline snapshots generated main-agent frontmatter with fixed native codingTools list; IR-001 task code begins replacing it | Native GenerateImage is absent irrespective of text model or auto-approve | Native tool-policy semantics and safe exposure scope |
| Configured skill resolver/materializer | Pre-IR-001 `unresolved` may mean absent or present-invalid; IR-001 AGY detailed resolver distinguishes them but broad-catches fingerprint errors as invalid candidate; materializer hard-fails invalid. Codex/Claude shared resolver parses metadata and generally warns/skips contextual invalidity | Approved SR-013: AGY uses shared metadata/name validation; missing or semantically invalid content warns/omits without blocking, while source provenance/collision/source mutation remain failures; normal package still supplies Codex skill | SR-015 design separates skippable content from blocking safety causes; preserve Codex/Claude behavior |
| AgentRunManager and WebSocket | Wrap unexpected cause in generic error | UI should not receive missing-skill failure after nonblocking fix; unrelated private errors stay redacted | Avoid weakening general failure redaction |
| MediaGenerationService | Separate app-owned image model/provider and file_path tool | App-owned grant is not native parity | Keep native and app-owned policies distinct |

## Structural And Payload Surface Inventory
- Payload/content: Agent definition JSON in separate autobyteus-agents repository; Codex skill source exists in sibling autobyteus-skills repo but the package's absolute symlink is dangling and the configured global skill root lacks it; AGY native generated output in provider brain; existing AGY capsule manifest in run memory.
- Readers/writers: AgentDefinitionService and SkillService read package; capsule materializer writes per-run snapshot; MediaGenerationService writes output; event/file projection renders artifact.
- Structural surfaces: AGY generated agent native tool policy, stream event converter/file-change projection, skill materializer, run manager error mapping, separate Agent Tools MCP exposure, frontend error display.
- API/external contract change: Potential safe error code/message; unknown until design.
- Persistence schema change: None indicated.
- Security/privacy boundary: Existing MCP run scoping and error redaction must be preserved.
- Concurrency/lifecycle: First-turn activation and capsule snapshot; new run needed for changed grants; no historic migration requirement established.
- Repository boundary: Main workspace and separate autobyteus-agents package repository.

## Runtime, Probe, Or Reproduction Findings
| Method | Scenario | Observation | Implication |
| --- | --- | --- | --- |
| stat dog path | CLI generation | Image exists at reported path | Confirms CLI transcript output |
| targeted server log and run metadata inspection | Codex failure | Missing skill warning before ACTIVATION_FAILED; never started | Failure occurs at preparation, not model response |
| package/global skill search | Codex missing skill | Declared skill absent from configured discovery roots; broken package `.codex` symlink points to a Linux path; real sibling source exists but is not registered | Strong candidate root cause |
| AGY capsule inspection | App Solution Designer | Generated agent frontmatter restricts native tools, separate MCP config materialized | CLI and app runs have different tool contexts |
| No live native image call | AGY-backed app image generation | Native AGY capability and result payload in app capsule not proven | Do not infer output schema; implementation validation must use native provenance and real file before claiming AC-001 |

## Stakeholder And User Evidence
User expects AGY-native tool parity because both surfaces say Antigravity CLI. User favors default AGY-native non-collaboration tools for every AGY-backed run plus configured MCP tools, with native subagent/messaging disabled if supported, as in Claude's separate provider policy. Codex preparation remains in scope. User additionally wants nonexistent skills to be warned in backend and omitted without preventing AGY startup, matching Codex/Claude.

## External Contracts And Dependencies
- AGY CLI default native tools: image generation observed in user's CLI and local AGY 1.2.11 tool-exposure probes; custom-agent frontmatter tools list is restrictive per official AGY docs.
- AutoByteus app generate_image: server-owned, separately configured provider, returns file_path.
- Agent package repo README: Codex workflow skill intentionally declared.
- AGY CLI custom-agent frontmatter contract: https://antigravity.google/docs/subagents?tab=cli documents explicit `tools` list; the SDK's `disabled_tools` is a separate API and does not authorize a CLI denylist assumption.

## Persisted Data And State Facts
- Existing run record: Codex PREPARED metadata with no provider binding/start; Solution Designer existing AGY capsule is immutable snapshot.
- Preserve prior conversations, output files, run IDs and provider bindings.
- No proposed data loss; new runs can consume corrected definitions.
- Unknown: full AutoByteus-launched AGY native-image transcript reconciliation, run-owned Files content serving, Team/Org MCP coexistence and browser redaction have not yet passed live app checks. Isolated installed-CLI native image invocation and transcript structure are observed (E-037/038/045/049). App-owned provider credential health is separate and not primary to this request.

## Product Design Context
- Product Design request: Not stated.
- User requested diagnosis and correction, not UI prototype or new visual design.
- Existing chat error and file display surfaces are in scope.

## Supplemental Artifact Inventory
No behavior-defining supplement and no Product-owned package. User screenshots/CLI transcript and SR-017's `agy-minimal-native-profile-probe.py`, `agy-minimal-mcp-probe.py`, `agy-minimal-codex-skill-probe.py` plus `agy-minimal-native-profile-evidence/` are **technical investigation evidence only**, owned by Solution Designer for the installed AGY 1.2.11 native-name decision (REQ-001/005/006); they do not redefine approved behavior and require no separate user approval. Raw AGY conversation transcripts remain provider-owned under its brain directory, not copied into the ticket.

## Assumptions, Unknowns, Risks
| ID | Type | Description | Resolution / Owner | Status |
| --- | --- | --- | --- | --- |
| U-001 | Unknown | SR-015 full native allowlist and explicit DONE output premise are contradicted by E-036/037. The exact eight-name provider profile is proven in bounded CLI probes and approved. Transcript-media timing/correlation is observed for one two-turn conversation, but provider stability, multi-image behavior and full app/Team/Org coexistence still require API/E2E validation. | Design against E-045–E-050; fail safely on transcript drift; repeat live API/E2E gates after review. | Partly resolved; executable gates open |
| U-003 | Decision | Whether native AGY collaboration tools may remain model-visible but be denied before execution when hiding them would suppress other native capabilities. | SR-016 option not approved. User's E-041 minimal-profile direction offers a cleaner way to retain actual wanted tools while excluding native collaboration; do not pursue the hook exception unless a later verified need returns to user. | Withdrawn from current proposal |
| U-004 | Decision | Whether the wanted AGY native profile is the **seven actually exercised coding tools plus native `generate_image`** (eight total); omit `multi_replace_file_content`, the other 40 from the 49-name policy, and optional web/browser/permission/scheduling tools unless separately requested. | E-048 explicitly approves the exact eight after E-045–E-047 proof. Full app Files/Team/Org remains downstream validation. | Approved; validation open |
| U-002 | Unknown | Exact raw exception for Codex run not logged | Compare logged unresolved binding with E-006; do not run a new live experiment per user request | Strong causal inference; not raw-proven |
| R-001 | Risk | Exposing raw prep exceptions could leak paths/secrets | Safe classification/redaction in design and tests | Open |
| R-002 | Risk | Separate autobyteus-agents repo needs isolated authoring/finalization | Architecture/delivery context | Open |
| R-003 | Risk | AGY native image failure output/error may contain private host paths, URLs or credentials, and current converter forwards it into public tool lifecycle | SR-008 design maps only static safe failure text to canonical events; private bounded run-memory diagnostic sink retains raw evidence outside public projection; tests required | Design correction complete, validation pending |
| R-004 | Risk | Existing detailed AGY resolver hard-fails a present malformed/name-mismatched skill despite user-requested nonblocking content judgment | Proposed AGY path should share Codex-style metadata/name validation and warn/skip invalid content without weakening source-provenance checks | SR-015 design complete; independent review pending |
| R-005 | Risk | Codex validation behavior is not uniformly nonblocking: invalid global loading or a destination collision may throw | Do not claim exact universal parity. Proposed AGY contract warns/skips missing or invalid content, but keeps unsafe provenance/collision and unrelated base failures distinct; preserve safe provider-failure reporting | SR-015 design complete; independent review pending |

## Requirement Implications
The common AGY runtime/model label is not capability parity. AutoByteus writes a generated AGY custom-agent frontmatter list of eight coding tools, excluding default native image generation. Local AGY 1.2.11 probes corroborate that an explicit custom-agent tools list restricts model-declared tools while omission can expose native image generation. SR-005 approved default native non-collaboration tools plus configured MCP, with native subagent/messaging exclusion. The user changed the Codex preparation outcome: unlike the prior blocking-prerequisite baseline, a nonexistent skill should produce backend warning and not block an AGY run. Static Codex/Claude paths support that comparison, but safety/provenance failures are a separate class. SR-009 received explicit approval in SR-010. ARCH-REV-002/F-002 shows that `unresolved` cannot tell true absence from present malformed content. The later conditional user clarification E-033 supersedes E-030 after E-032 established that AutoByteus already parses/validates Codex configured-skill metadata. Proposed AGY behavior likewise judges `SKILL.md` metadata/name, warns/skips absent or invalid content rather than hard-failing it, and keeps unsafe provenance/collision separate. This full SR-013 boundary was approved in E-034. Live symlink updates are expressly deferred to a later ticket.

**Historical SR-016 recovery (superseded as a decision hold):** API-REV-001/CRR-004 validates neither SR-015's 49-name profile nor its explicit-stream-result image path. The four existing provider transcripts gave a *structured* image-media candidate distinct from final prose. At that time, no evidenced CLI contract supplied automatic complete default-native inheritance with hidden collaboration exclusion; a hook could deny execution but not hide names. The later approved exact-eight decision E-048 resolved this breadth question without authorizing a hook, registry-name expansion, prose extraction or MCP image substitution.

**SR-017 proposal and SR-018 approval:** The user rejected the unexplained 49-name expansion as unnecessary and authorized profile experiments. E-045–E-047 support the exact **eight-name** set: seven actually invoked coding tools plus native `generate_image`. It answered under observed Low/High models, generated real native JPEGs, coexisted with capsule MCP and read the bundled Codex skill in disposable turns. `multi_replace_file_content` passed constructor validation but did not invoke in four attempts; optional native web tools worked separately but are outside the approved set. The user explicitly approved the eight names in E-048, narrowing REQ-001/AC-005. E-049 adds two-turn transcript timing/correlation for the unchanged native-image outcome. Full app image projection and Team/Org/Files gates remain open.

## Notes For Architecture Design
Under SR-010 approved requirements, retain SCN-001 mapping to generated AGY native tool policy -> provider invocation -> event/path/file projection, with separately configured run-scoped MCP additive. Verify the image tool-call provenance is AGY-native, not AutoByteus MCP. The user's E-028/E-033 direction reopens SCN-003 and selects Codex-parity AutoByteus semantic validation with nonblocking omission for invalid content. The revised AGY design should use the shared loader rules but not inherit the Codex global-invalid edge as an intentional requirement; keep source/destination safety failures distinct. An AGY rejection after valid materialization is possible and must not be mislabeled an AutoByteus skip. Keep native-image work separate while SR-015 awaits independent review.

## SR-018 Architecture Investigation Update
- E-048 supplies current intended-behavior authority: exact eight native names, no silent extra native tool grant, separately configured MCP, no native collaboration. E-045–E-047 are capability proof for the installed CLI, not release validation.
- E-049 establishes a concrete result-boundary transcript shape for the same eight-name profile and multi-turn index progression; E-050 identifies the existing file-change content route and why the AGY adapter should publish a verified run-owned copy, not a raw provider-brain or final-prose path.
- The provider transcript is not documented as a stable public API. Pin the design to validated CLI 1.2.11 and treat missing/changed transcript structure as a safe native-image failure; do not silently fall back to model prose or AutoByteus MCP.

## Historical SR-019 Lifecycle Design-Impact Recovery (Superseded By E-055)
- ARCH-REV-005/F-003 is a valid normal two-turn chat race in SR-018 target sequencing, not a synthetic edge. Current backend releases `turnId` on provider `result`; the new asynchronous transcript/copy/publication step would outlive that release unless explicitly gated.
- Under the then-approved image-output target, the AGY backend would have had to retain the active turn through `finalizing`, reject/acknowledge follow-up dispatch during that interval, and release only after ordered terminal source-event publication succeeds. Receipt of provider `result` remains a synchronous fact only for distinguishing a normal post-result CLI close from pre-result failure.

## SR-021/022 Requirement And Architecture Recovery
- E-055 changes the approved image outcome: native AGY invocation/status and ordinary reply suffice; AGY owns image storage. The preceding SR-018/019 result-bound transcript/copy/Files/finalizing investigation remains factual history, but its target is no longer required. Do not treat “no output path” on native DONE as tool failure.
- IR-004/E-052/053 app-admission/publication contradictions are real for the **abandoned special finalization target**. They do not establish a need to change shared `AgentRun` FIFO or listener contracts for a normal native tool lifecycle. Any independent generic defect belongs to a separate finding/ticket, not an unapproved expansion here.
- Static comparison of current partial `agy-stream-event-converter.ts` and `agy-agent-run-backend.ts` with `origin/personal` shows the intermediate pending-image/deferred-text/transcript-baseline/finalizing code is task-introduced. `FileChangeEventProcessor` needs an explicit path to project a generated image; a native canonical DONE event with empty arguments and no file path produces no app Files entry. The current AGY-specific stat/projection branch is therefore not part of the narrowed target. No provider or app execution was performed in this recovery round; full native app lifecycle remains an API/E2E gate.
