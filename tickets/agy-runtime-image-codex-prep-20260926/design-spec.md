# Design Specification — AGY Native Tool Availability and Codex Startup

## Solution, Authority, State
- Package `agy-runtime-image-codex-prep-20260926`; current design round **SR-023**. Approved image-scope change: SR-021/E-055 (DEC-006). Exact eight native names: SR-018/E-048 (DEC-005). Nonblocking missing/semantic-invalid configured skill: SR-013/E-034. No Product or behavior-defining supplement applies.
- **Architecture Design Complete — independent re-review required.** This retains SR-022's narrow AGY-native invocation design and corrects ARCH-REV-007/F-004's terminal-result redaction gap. IR-004 partial source at `77c9ffa28` is not a source/API-E2E pass and contains work to remove. ARCH-REV-006 approved only the superseded image-output design; ARCH-REV-007 failed SR-022 and made no implementation handoff.
- Evidence: `investigation-notes.md` E-001–E-056, especially E-036/037 provider first-turn/result-ERROR and native DONE findings, E-045–E-049 exact-eight live probes, E-052/053 IR-004 shared-path contradiction, E-054/055 user scope correction and E-056 ARCH-REV-007/F-004 source-path review. No new provider experiment establishes full AutoByteus app success; that remains API/E2E.
- Workspace: isolated server worktree `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-runtime-capabilities-20260926`, branch `task/agy-runtime-capabilities-20260926`, original base `origin/personal@ae3aba1bfb7af6fefd8c69994e0b1bc421967d60`, finalization target `origin/personal`; separate agent-package worktree `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-codex-skill-bundle-20260926`.

## Approved Behavior and Supported Scenarios
| IDs | Trigger and approved outcome | Production spine |
| --- | --- | --- |
| BEH-001; SCN-001/004; REQ-001/005/006; AC-001/005/006 | New AGY standalone or Team/Org run exposes **exactly** `view_file`, `write_to_file`, `replace_file_content`, `grep_search`, `list_dir`, `find_by_name`, `run_command`, `generate_image` as native custom-agent names on validated CLI 1.2.11. Configured MCP is separate/additive; native subagent/messaging names absent. | Agent activation → AGY capability/version policy → capsule frontmatter and scoped MCP → CLI tool call. |
| BEH-003; SCN-001; REQ-002; AC-001/002; QR-001 | AGY-native image call is genuinely invoked and normal tool ACTIVE/DONE/error/denial plus ordinary assistant reply are visible in AutoByteus. AGY owns generated-image location. No app path, bytes, copy, Files entry, preview, content route or retention promise. Native DONE without output path is not failure. | CLI stream `tool_name=generate_image` → AGY event converter → canonical tool lifecycle/chat. |
| BEH-002; SCN-002/003; REQ-003/004; AC-003/004 | Bundled Codex skill usable on normal first turn; absent or semantically invalid configured content warns/omits without blocking otherwise healthy AGY; unsafe provenance/collision/source mutation and unrelated runtime failures stay safe hard failures. | Definition → detailed skill resolution → AGY materializer/capsule → first reply. |

The image tool must be **AGY native**, not AutoByteus MCP `generate_image` under `call_mcp_tool`; the latter alone cannot pass AC-001. User-visible “display” means existing tool card/trace and normal AGY assistant response, not a new image renderer. SCN-001/002 are supported normal paths; SCN-003/004 are supported edge/security contracts.

## Current State, Root Cause and Design Health
`origin/personal` exposes eight coding names without `generate_image`; the task's former 49-name frontmatter causes 25 unknown-component first-turn rejections on AGY 1.2.11 (API-REV-001/F-API-001). Exact E-048 eight-name probes invoke each selected tool, native image, configured MCP, and bundled Codex skill in isolated runs, but not in a full AutoByteus app path. AGY native image `DONE` omits output/path; that is normal provider behavior, not evidence the tool failed (E-037/045). SR-019 overinterpreted approved output requirements and introduced transcript lookup, run-owned image copy, Files projection and a special turn-finalization barrier; user removed that obligation in E-055. IR-004 found app-level admission/publication contradictions only because of that added barrier. The ordinary app `AgentRun` FIFO queue and source-listener semantics are retained, not refactored to serve an out-of-scope image artifact.

- Posture: bounded bug fix and scope correction, not a provider transcript/media integration.
- Root causes retained: native allowlist omitted image; 49-name registry inventory was mistaken for custom-agent grants; Codex skill packaging/discovery and AGY absent/invalid disposition needed repair. The pathless native DONE was a false failure in the task's intermediate image adapter, not a reason to own AGY storage.
- Refactor needed now: **bounded removal** of intermediate transcript/copy/finalizing branches and AGY image path-required success gate. Preserve the reviewed exact-eight native policy and skill safety work. No new generic media, input-admission or publication abstraction.
- Residual risk: provider/version drift and full app/Team/Org tool presentation are unverified. Validate actual native call and safe public events; do not infer model access from `init.tools` inventory or constructor success.

## Main Spines, Ownership and Interfaces
1. **Native grant/MCP spine:** `agy-native-tool-policy.ts` is the single versioned native allowlist owner. New capsules for validated CLI 1.2.11 write the exact E-048 eight names, no 49-name inventory or `call_mcp_tool` in native frontmatter. Unsupported version fails with safe compatibility error; existing immutable saved capsules are not rewritten. AGY factory/MCP materializer keep configured, run-scoped MCP and Team/Org identity separate. Native subagent/messaging names remain excluded, without a visible-but-denied hook fallback.
2. **Native lifecycle spine:** `AgyStreamEventConverter` alone interprets provider `step_update` tool name/state. For `tool_name=generate_image`, publish canonical STARTED with empty public arguments; on `DONE` with no explicit provider error publish SUCCEEDED with `provider_state=DONE` and no `file_path` or raw provider output. **Do not wait for transcript media or inspect AGY brain storage.** The provider `result` closes the turn using normal AGY and `AgentRun` FIFO mechanics; no image-specific private `finalizing`, `providerResultSeen`, transcript baseline, image copy or publication acknowledgment is needed. On native image step `ERROR`/explicit provider error, publish DENIED only when denial is positively identified, otherwise FAILED, using fixed bounded safe public text; keep raw details only in the restricted AGY diagnostic. On **terminal provider result failure**, use the separate safe rule below even when there was no tool step. Non-image tool step behavior is otherwise retained, subject to the same terminal-result public/private boundary. Ordinary successful AGY assistant text may contain its own path; AutoByteus does not parse that prose into an artifact or claim to verify the path.

3. **Codex skill spine:** retain SR-015/SR-013 detailed resolution (`resolved`, `certified_absent`, semantic `invalid_candidate`) and AGY warn/omit for absent or malformed/unreadable/name-mismatched SKILL.md. Valid sources snapshot to capsule manifest v1. Trusted source-root provenance, unsafe tree/size, destination collision, post-resolution mutation and unrelated runtime failures remain blocking with safe reporting. Preserve Codex/Claude consumer behavior. Bundle `software-engineering-workflow-skill` under supported package-local `agents/codex/skills/` and remove dangling developer-machine link. Live symlink updates remain a future ticket.

### Terminal result failure (ARCH-REV-007/F-004)
Only an exact provider `result.status === "SUCCESS"` **with no nonempty `result.error`** may use `result.response` as ordinary fallback assistant text (and only when no assistant text was already streamed), then emit the normal completed turn. A terminal `result` with `ERROR`, another non-success status, missing/invalid status, or a nonempty `result.error` of any type **must not** convert `result.response`, `result.error`, raw status or other provider failure fields into public SEGMENT_CONTENT, ERROR message, status, ACK, trace or history. Close any already-open text segment without adding failed-result fallback text. Emit a fixed bounded public turn failure, e.g. `AGY_TURN_ERROR` / “Antigravity could not complete this turn.”, with `error_scope=turn`, `error_effect=terminal`, the current `turn_id` and `statusHint=ERROR`. This is the terminal app-turn event; do **not** follow it with a misleading `TURN_COMPLETED`/idle success. Normalize any public status to a safe fixed code rather than echoing arbitrary provider status. The backend may return to its normal input-ready phase after this terminal event, while canonical error state remains visible and the next user turn can start through existing FIFO. Tool-step DONE means AGY reported that step DONE, **not** that AutoByteus delivered an image; a later turn-level failure is separately and visibly failed, never converted into an image file claim. An earlier tool-step failure stays failed even if AGY later reports turn SUCCESS; no retroactive green tool card.

Extend the existing AGY restricted diagnostic sink narrowly for a turn-level record `{kind:turn, runId, turnId, safeReasonCode, bounded raw result.status/error/response}` (or an equivalent AGY-local typed shape), with existing no-follow `0700` directory/`0600` file and size limits; it is never projected to public trace/Files/WebSocket or logged raw in backend warnings. Diagnostic-write failure must not alter the fixed safe public error. Do not introduce a transcript reader, a shared redaction framework, a generic queue/publisher change or an app-owned image path. A result-ERROR without any tool step is a first-class test case, not a pathless-DONE failure. For failed terminal results, omit raw `usage` and any other unrecognized result fields from public canonical events unless separately reduced to a known safe scalar contract.

**Dependency rule:** AGY adapter may emit only canonical tool/turn events. Shared `FileChangeEventProcessor`, Files service, REST content route, UI and MCP media service must not gain AGY transcript/brain lookup or special image copy logic. With no explicit native image `file_path` in canonical success and empty public arguments, generic file-change projection has no image path to index. Keep existing projection for other tools/providers; remove now-unneeded AGY native-image stat/projection special case if it is exclusively from the abandoned attempt. Do not promise that async WebSocket, Files or memory observers are synchronously acknowledged by AGY backend callbacks; ordinary existing event semantics apply.

## Final File Responsibilities and Removals
| Action | File/area | Contract |
| --- | --- | --- |
| Keep/correct | `.../antigravity/capsule/agy-native-tool-policy.ts`, capsule/factory/version handling | Exact eight for 1.2.11, no native collaboration or registry-derived extras; scoped MCP unchanged. |
| Simplify | `.../antigravity/stream/agy-stream-event-converter.ts` | Native image DONE succeeds without a path; safe image step ERROR/denial, empty public args/output; **success-only** fallback `result.response` and fixed-safe terminal result ERROR without raw failure fields. Remove pending-image/deferred-text/reconciliation branches and path-required failure. |
| Simplify | `.../antigravity/backend/agy-agent-run-backend.ts` | Normal result/turn close and existing app admission; remove transcript baseline, run-owned copy, image-specific event queue/finalizing/provider-result close workaround. Retain the partial backend's fixed-safe process-close/input-dispatch failure messages and ordinary interrupt safety/canonical ordering; do not restore `origin/personal` raw `String(cause)` public error while deleting finalization. |
| Remove if no other caller | `.../antigravity/stream/agy-native-image-transcript.ts` and its tests; `memoryDir/agy-native-images` writer | No AGY transcript reader, path validator, image-byte copier or new artifact directory in this ticket. Never delete user/provider-owned existing images or historic run data. |
| Keep/narrow | `.../antigravity/stream/agy-native-image-diagnostic-sink.ts` (may be renamed AGY provider diagnostic sink) | Restricted bounded native tool-step **and turn-result** failure evidence, including result-ERROR without tool step; no public raw provider error/response/params/output. |
| Preserve | `src/agent-execution/domain/agent-run.ts`, input admission/event dispatch contracts; shared file-change/REST/UI | No AGY-specific busy-input rejection, shared terminal-delivery guarantee, Files image route or generic queue change. Remove only image-specific intermediate code that becomes dead. |
| Keep | `src/skills/...` resolver/materializer and separate agent-package Codex workflow skill | Approved valid/absent/semantic-invalid/safety classification and portable first turn; no symlink-live-update change. |

Implementation should first delete the abandoned image-artifact/finalizing path, then make the native lifecycle **and terminal-result safe failure** adjustments in the AGY converter/diagnostic sink, retaining the exact-eight and skill fixes; run focused unit/integration checks before fresh source review. Specialist-owned API/E2E tests/reports must be revised by their owners to the approved AC-001/002; old Files-byte cases are historical coverage of the superseded target, not acceptance gates. No new public endpoint, schema, dependency or top-level subsystem is required.

## Persisted Data and Compatibility
**Directly Usable — No Migration.** Existing run metadata, capsule manifest v1 and generic file-change projection v2 remain readable; new native grants apply only to new capsules. Old capsules are immutable, not silently broadened. The partial task branch's run-owned copied image may exist only in experimental/test runs; this ticket must not run deletion/migration against user or provider files. No new persisted image index or transcript copy is created. This decision relies on retaining existing version-agnostic readers and deleting only unshipped intermediate writer logic.

## Validation and Review Gates
1. Static/unit: exact eight names/version gate and no native collaboration or unconfigured MCP names; `generate_image` ACTIVE→STARTED, DONE with absent output→SUCCEEDED (no path, FILE_CHANGE or fabricated image), ERROR/denial→safe canonical failure with no raw parameters/error/output in public trace/history/tool card. **Result ERROR with no preceding tool step** and secret markers in both `result.error` and `result.response` must emit one fixed-safe terminal error with no fallback SEGMENT_CONTENT, no raw public status/usage, and a bounded restricted diagnostic; missing/unknown status fails safely. SUCCESS may use ordinary fallback response. A prior native step DONE followed by result ERROR leaves the tool's provider-step fact but visibly fails the turn; a prior tool error followed by SUCCESS remains a failed tool. Other tools and Codex/Claude generic FIFO/listener behavior unchanged.
2. Real installed AGY 1.2.11 AutoByteus-launched first turn (standalone and relevant Team/Org member): native provenance `tool_name=generate_image` ACTIVE→DONE, normal tool card and assistant reply, not `call_mcp_tool`; no app image bytes/path/Files/preview check. Configured MCP and Team/Org collaboration still work under exact profile; native collaboration remains unavailable. Do not claim success from `init.tools` or model assertion alone.
3. Codex bundled workflow skill first reply, absent/semantic-invalid warnings with otherwise healthy startup, valid peers, unsafe source/collision/source-mutation failure, and public redaction. Verify backend warning and UI/ACK contain no raw private cause. Unsupported AGY version fails safely. Full app/API/E2E remains a gate; prior API-REV-001 and IR-004 local checks are not signoff on this revision.

`task_size: Medium`: bounded AGY policy/converter/backend cleanup plus retained skill/package work across the two existing repositories; no new media subsystem or shared admission/publisher contract. `architectural_risk: High`: provider-native permission/collaboration and MCP identity, public/private error data and skill-source safety remain material integration/security boundaries despite removing image-file handling. The skill payload's content size does not drive classification. Independent architecture re-review is required before implementation resumes; classify any new requirement or provider contract contradiction honestly rather than restoring transcript/Files work without user approval.
