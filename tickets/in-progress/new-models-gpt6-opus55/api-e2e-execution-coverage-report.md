# API/E2E Execution Coverage Report — API-REV-003

## Round, authority, and route
Third completed API/E2E round, 2026-09-23, triggered by the user-requested complex game-task live signed-thinking check after API-REV-002 Pass. Latest authoritative result: **Pass**. Assigned worktree `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55`; approved requirements SR-002 (`requirements-doc.md`), investigation (`investigation-notes.md`), cumulative `solution-revision-record.md`, design SR-005 retaining SR-004 (`design-spec.md`), `solution-handoff.md`, architecture review ARCH-REV-003 Pass (`design-review-report.md`, `architecture-review-revision-record.md`), IR-001/002 (`implementation-handoff.md`, `implementation-revision-record.md`), source review CRR-002 Pass (`code-review-report.md`, `code-review-revision-record.md`). Supplements N/A. Investigation: `api-e2e-coverage-investigation.md`; ledger: `api-e2e-test-case-ledger.md`; revision: `api-e2e-revision-record.md`. Prior API/E2E result/confidence **API-REV-002 Pass / 96%**; prior C01–C07 evidence was rechecked as the baseline and retained. Classification **Large / High**, reviewed route; successful next recipient **Code Reviewer for proportional test-code review**.

## Investigation, ledger, and compatibility
Investigation was written before execution/test edits; revised before the two durable test edits. Ledger initialized before execution, completed cases/checkpoints recorded, reconciled below. Legacy/compatibility-only behavior added or protected: **No**. Approved persisted-data decision **Directly Usable — No Migration**: existing v5 missing-native metadata and historical generic turns remain readable; signed/reset new turns round-trip through current reader; historical price snapshots are not rewritten. No version-specific dual read/write or migration path observed. No tests removed.

## Boundary and case results
| Case / AC | Evidence and exact outcome | Result |
| --- | --- | --- |
| API-C01 / AC-001–004,007,008 | `pnpm -C autobyteus-ts exec vitest run` focused catalog, native payload, Anthropic adapter and signed continuation: 4 files, **64/64 pass**. Exact IDs/metadata/reasoning, no shorthand aliases, Standard prices, mocked Responses send/stream and Anthropic request/stream/replay. `/tmp/new-models-api-c01.log` | Pass |
| API-C02 / AC-004,008 | Seven shared agent/memory/event suites: **43/43 pass**; signed active-turn replay, independent reset, recovery, accepted compaction, notifier exclusion and v5 snapshot reader. `/tmp/new-models-api-c02.log` | Pass |
| API-C03 / AC-005,008–010 | Six initial server suites **85 assertions pass**; one suite import initially blocked by missing generated workspace contracts, then documented `prepare:shared` succeeded and affected manager plus SQLite/GraphQL unit-price rerun **10/10 pass**. Prisma generate and `tsc -p tsconfig.build.json --noEmit` pass. Added exact server pricing-policy cases, provider suite **21/21 pass**. Registry stable tags independently `0.3.280` Agent SDK and `0.128.0` API SDK, matching exact pins. `/tmp/new-models-api-c03-*.log`, `/tmp/new-models-api-c03-added-pricing.log` | Pass after setup |
| API-C04 / AC-006,007 | `RUN_CODEX_E2E=1` real app-server `model/list` → catalog → GraphQL effort/Fast parity: **1/1 pass**. `/tmp/new-models-api-c04.log` | Pass |
| API-C05 / AC-006,007 | Local Codex account advertised `gpt-6-astra`, `gpt-6-sol`, `gpt-6-luna`. Corrected temporary probe used product `CodexThreadManager`/app-server `thread/start` and `startInput`/`turn/start`; **three real turns completed, 3/3 pass**. First probe's removed `submitInput` was a harness error before any model turn, corrected and not counted as live product failure. `/tmp/new-models-api-c05-rerun.log`; probe source `evidence/api-e2e/codex-gpt6-validation.probe.test.ts` | Pass |
| API-C06 / AC-007,010 | Secret-safe booleans only: candidate `.env` exists, Anthropic API key nonempty, OpenAI API key absent, Claude CLI auth status succeeded. One tiny real Anthropic API SDK `claude-opus-5-5` request returned exact model, `end_turn`, content and usage. Separate subscription-backed Claude Agent SDK live Haiku model discovery/query/session-message case **1/1 pass** after fixing stale integration-test session binding. Three other live SDK cases intentionally skipped to limit cost. `/tmp/new-models-api-c06-anthropic.log`, `/tmp/new-models-api-c06-claude-sdk-rerun.log` | Pass; OpenAI live Not Tested by approved no-key plan |
| API-C07 / AC-003,004,007 | One first real product `AnthropicLLM` streamed Opus 5.5 tool request returned one native `tool_use` and usage, but no signed thinking; probe stopped before continuation at an overstrict signature assertion. A single cost-limited two-request rerun then passed: current `AnthropicLLM.streamMessages` returned native tool use/usage, and `sendMessages` accepted replay of its exact native assistant turn plus tool result, returning nonempty content and usage. **No signed thinking block was emitted in API-C07**, so that case alone did not prove signed replay; API-C08 below closes the live gap. Three small product API requests total in this extension; no bulk test. `/tmp/new-models-api-c07-live.log`, `/tmp/new-models-api-c07-rerun.log`; probe source `evidence/api-e2e/anthropic-opus55-live.probe.test.ts` | Pass for real product tool continuation; signed replay not proven by C07 alone |
| API-C08 / AC-003,004,007 | User-requested more complex small single-file platform-game task on real Opus 5.5, xhigh adaptive thinking. First product `AnthropicLLM.streamMessages` response contained a signed thinking block and **two native tool_use blocks** (`write_html`, `inspect_html`). The two tools ran sequentially against owned temp `index.html`; second real request replayed exact native assistant blocks/tool results through the production renderer and completed with usage. 4,259-byte HTML passed six structural checks; browser gameplay quality not tested. Two requests, 3,733 input/4,211 output tokens. `/tmp/new-models-api-c08-game.log`; probe source `evidence/api-e2e/anthropic-opus55-game-live.probe.test.ts` | Pass — live signed tool-turn replay proven |

No live success is inferred from mocked OpenAI tests. The separate direct Anthropic SDK call proves provider entitlement. API-C07 proved real product tool continuation without thinking on a trivial task; API-C08 additionally proved **live signed thinking plus multiple tool uses and accepted exact replay** on a complex task. Deterministic no-key tests remain the independent-turn reset/compaction and fragmented-signature evidence. The single Claude Agent SDK subscription test proves discovery/query/session retrieval, **not** live tools, permissions or interrupts.

## Confidence scorecard and broader validation
| Category | Post-repository | Final | Final evidence and residual |
| --- | ---: | ---: | --- |
| Requirement/AC proof | 75% | 98% | All approved AC evidence exercised; real Codex all three and live signed Anthropic tool-turn replay now complement no-key contracts. |
| Changed-boundary execution directness | 75% | 98% | Product Codex thread path, server price/GraphQL, and real product AnthropicLLM signed native tool continuation; OpenAI 200 untested per approved no-key plan. |
| Cross-boundary realism/mock gap | 50% | 95% | Real Codex, direct SDK, product AnthropicLLM signed thinking plus two-tool replay, and Claude subscription; OpenAI 200 remains no-key only. |
| Environment/configuration/identity/fixture fidelity | 50% | 100% | Full approved-scope environment: real local Codex/Claude auth, Anthropic key safely assessed, isolated temp workspace and SQLite; OpenAI key absence is an approved test constraint. |
| Failure/edge/lifecycle/recovery | 90% | 95% | Reset, compaction, recovery, missing-price, invalid options tested deterministically; no extra paid lifecycle run. |
| User-surface/browser/desktop shell | N/A | N/A | No changed UI, browser, or shell boundary; browser would not improve provider validation. |
| Durable regression quality/relevance | 90% | 95% | Exact new-model server pricing tests and repaired gated SDK test added to existing focused suites; entitlement-sensitive Codex probe intentionally temporary. |

Simple mean of six applicable categories: **72% post-repository; 97% final** (581/6=96.8%, rounded). No final applicable category below 90%; every critical AC proven in its approved required form. The 95% default clean target is met. Broader validation was **Required and completed** using live Codex app-server, one small direct Anthropic SDK request, one subscription-backed Claude SDK turn, cost-limited real product AnthropicLLM continuation, and a two-request complex signed-thinking/multiple-tool task. Browser/desktop Not Required for provider protocol validation; the generated game received only structural checks, not a gameplay/visual claim. No bulk paid suite or retry loop.

## Durable coverage and execution caveats
Updated durable paths:
1. `autobyteus-server-ts/tests/unit/token-usage/pricing/token-price-config-provider.test.ts` — three exact Sol/Luna/Opus 5.5 server policy cases, including full-request long-context/cache rates; focused suite 21/21 pass.
2. `autobyteus-server-ts/tests/integration/runtime-management/claude/client/claude-sdk-client.integration.test.ts` — current required `sessionBinding` and `systemPrompt` for four gated live scenarios, resume uses current binding; only short discovery/query/history case run live 1/1, remaining three not run because cost.

Added durable paths: none. Removed durable paths: none. Proportional test-code review: the two API-REV-001 durable updates already received CRR-003 **Pass**; API-REV-002 received CRR-004 **Not Applicable** because no durable code changed. API-REV-003 likewise has no durable test-code delta and is routed for an explicit current-round N/A decision. Temporary Codex and both AnthropicLLM probes retained as ticket evidence only, removed from test trees after runs. Generated workspace-contract `dist/` from documented setup cleaned because this run created it; no shared process or datastore stopped/reset. Isolated probe workspace and owned app-server clients were closed/removed by probe teardown. Existing opt-in Codex thread test still calls removed `submitInput`; this unrelated stale test merits later maintenance, but C05 current-path real probe proves the task-specific exact IDs. A general `tsc -p tsconfig.json --noEmit` attempt failed with existing TS6059 `rootDir=src` versus included `tests`; build-target TypeScript passed and is the repository's authoritative build check. `git diff --check` passed.

## Platform and cleanup
Linux host, Node/pnpm workspace (`pnpm@10.28.2`), Vitest 4.0.18, SQLite test storage; UTC. No browser, Electron or device execution. No secret values printed or persisted. Direct API test used key in memory only, without vault import; import was unnecessary and would have mutated persistent secret state. Only self-owned temporary files/processes were cleaned. `/tmp` convenience logs remain outside repository; concise secret-free evidence is retained at `evidence/api-e2e/execution-summary.txt`.

## Result, preliminary classification, and next owner
**Pass**, final confidence **97%**, broader validation **Required and completed**. No implementation/design failure found; setup/harness issues were locally corrected before final result. OpenAI live exact-model entitlement remains untested under the approved no-key constraint. Live signed Anthropic thinking plus two-tool continuation is now proven for the bounded complex task; live independent-turn reset/compaction are not claimed. Next: `/code_reviewer` for the API-REV-003 no-durable-delta review/route decision; prior CRR-003 Pass on the two cumulative durable test edits remains in force, and implementation source review is not reopened. No browser/desktop run or further paid Claude test needed.
