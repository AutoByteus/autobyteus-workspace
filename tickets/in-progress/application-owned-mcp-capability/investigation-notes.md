# Application-Owned MCP Capability — Investigation Notes

## Investigation Status

- Bootstrap Status: Complete
- Current Status: `SR-010` solution correction complete and ready for architecture confirmation after `API-REV-005` / `CRR-012`. `SR-009` passed `ARCH-REV-009`, was implemented in `IR-006`/`IR-007`, and passed source review in `CRR-011`. API-REV-005 then passed the application MCP, tokenless lifecycle, actual Codex/Luna call/result, artifact, publication, handoff, reconciliation, identity, and browser UI path; it failed only because solution-authored AC-039 disqualified shell-created artifacts. The user explicitly stated that this rule was never requested and ordered it removed. SR-010 changes no production architecture or runtime capability policy: it corrects the requirements/design/proof boundary so any already-authorized runtime foundation operation, including shell, is acceptable when the authoritative artifact and business/UI causality are correct.
- Investigation Goal: Establish the current production spines and exact ownership boundaries needed to add application-owned MCP tools without global registry mutation or cross-application leakage.
- Scope Classification: `Large`
- Scope Classification Rationale: The feature crosses package contracts, application worker protocol, ApplicationPlatformRuntime/ApplicationExecutionScope boundaries, Agent Tools MCP session composition/dispatch, reload/shutdown lifecycle, and durable coverage.
- Scope Summary: Application-owned business tools exposed to same-application Agent/Team executions through the existing process Agent Tools capability: deterministic tokenless loopback MCP run sessions for Claude/Codex and bound local tools for AutoByteus. The maintained Brief Studio demonstration uses business-focused researcher/writer prompts, an actual read-only business-tool call, normal artifacts, and the existing UI projection. External SDK distribution, arbitrary package-provided MCP server processes, and a tool-call-owned UI mutation are excluded.
- Primary Questions To Resolve:
  - Where should the application-local tool catalog live relative to package and worker lifecycle?
  - How should an MCP session compose selected platform, configured-global, and application-local routes?
  - How should application tool calls cross into the correct worker with exact ownership and lifecycle guarantees?
  - What static package metadata is required for import-time validation without executing application code?
  - What reload, close, collision, error, and persisted-data behavior is required?
  - How can the maintained Codex/Luna demo prove actual Agent prompt compliance, exact identity, result use, authorized-workspace file creation, publication, and browser outcome without turning a read-only tool into a write capability, claiming unavailable registry tools, prescribing a foundation operation, or creating unsafe payload logs?
  - How do application route/capability materialization and per-application package drain compose with latest-base deterministic `activateForRun`/`deactivateForRun`, stop/restore, and the process-owned local listener without either lifecycle bypassing the other?

## Request Context

The user explicitly deferred the external developer-facing SDK journey and requested a new Application MCP ticket first. The intended capability is that applications own business-specific tools while global/platform MCP continues to supply common tools.

## Environment Discovery / Bootstrap Context

- Project Type: `Git`
- Task Workspace Root: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability`
- Task Artifact Folder: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability`
- Current Branch: `codex/application-owned-mcp-capability`
- Current Worktree / Working Directory: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability`
- Bootstrap Base Branch: `origin/personal`
- Remote Refresh Result: bootstrap fetched successfully on 2026-08-27 at `fd9b33e20ace3e7c221f931dbbcd5f4acf1df65f`; delivery refreshed again on 2026-08-28 to `origin/personal=ebef77eb32bbeaefd4fccdb6998240264c82a3c1`. The merge was correctly aborted after 12 semantic conflicts; the protected reviewed checkpoint is `aaf7e076ed66c5daaf142f896230ad63085330c7`.
- Task Branch: `codex/application-owned-mcp-capability`
- Expected Base Branch: `personal`
- Expected Finalization Target: `personal`, subject to explicit user delivery instruction
- Bootstrap Blockers: None
- Notes For Downstream Agents: This is a new design-first ticket. Do not reopen the finalized logical-addressing/operation-completion work. Do not fold SDK publishing/externalization into this feature.

## Supplemental Task Artifact Inventory

| Artifact Path | Purpose And Scope | Evidence, Context, Or Decision Captured | Core Artifact(s) Supported | Related Requirement / Acceptance-Criteria IDs (When Applicable) | Status | Approval Applicability / State | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/application-owned-mcp-intended-behavior.md` | Intended-behavior contract for the exact meaning of application MCP | Shared-host decision, latest-base run-session ownership, native-fundamental-versus-application-business distinction, visibility/collision rules, invocation identity, reload/stale-route behavior, clean package transition, business-focused prompts, and real maintained Agent-to-UI proof | `requirements.md`; `design-spec.md` | REQ-001–REQ-023; AC-001–AC-044 | Approved / Amended | User approved the baseline, Option A, and the business-focused prompt boundary; DR-004 requires technical reconciliation with established latest-base behavior | Keep aligned through design/rework |

## Source Log

| Date | Source Type | Exact Source / Query / Command | Why Consulted | Relevant Findings | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-27 | Command | `git fetch origin personal` and `git rev-parse origin/personal` | Establish fresh bootstrap authority | Base is `fd9b33e20...`; dedicated worktree created from it | No |
| 2026-08-27 | Code | `autobyteus-server-ts/src/agent-tools/mcp/**` | Identify current MCP host/catalog/session/dispatch owners | One shared host and scoped session authorities exist; configured MCP resolves through the process-global tool registry | Yes |
| 2026-08-27 | Code | `autobyteus-server-ts/src/application-platform/runtime/application-runtime-definition-validator.ts` | Identify current application tool validation | Application-owned agent definitions validate `toolNames` only against the global registry | Yes |
| 2026-08-27 | Code | `autobyteus-application-sdk-contracts/src/index.ts`, `manifests.ts` | Check current application contracts | No application-owned tool declaration or handler exposure exists | Yes |
| 2026-08-27 | Code | `autobyteus-server-ts/src/application-engine/runtime/protocol.ts`, `worker/application-backend-host.ts` | Trace worker boundary | Existing bidirectional protocol can be extended, but has no application tool invocation operation | Yes |
| 2026-08-27 | Code | `autobyteus-server-ts/src/application-orchestration/services/application-run-binding-launch-service.ts`; `agent-run-provisioning-service.ts`; Codex/Claude MCP issuance paths | Trace authoritative application identity into a real session | Application launch creates `ApplicationExecutionContext`; Codex and Claude pass it unchanged into session issuance | No |
| 2026-08-27 | Code | `agent-tool-mcp-session.ts`; `agent-tool-mcp-session-service.ts`; `scoped-agent-tool-mcp-session-authority.ts` | Verify session identity, capability, and lifecycle boundaries | Sessions are immutable route snapshots with scoped issuance/revocation; application context is already carried but unused by the catalog | No |
| 2026-08-27 | Code | `agent-tool-mcp-catalog.ts`; `agent-tool-mcp-tool-route.ts`; `agent-tool-mcp-tool-executor.ts`; `agent-tools-mcp-method-dispatcher.ts` | Identify route composition and execution seam | Only static and configured-global route kinds exist; application route can be added without changing the public MCP endpoint/provider descriptor | No |
| 2026-08-27 | Code | `application-platform/runtime/build-application-platform-runtime.ts`; `application-platform/execution/**`; `create-application-orchestration-services.ts` | Establish platform/scope multiplicity and construction direction | One application execution scope serves the platform lifetime; Studio may host many apps; bundle/controller/store owners exist before scope construction | No |
| 2026-08-27 | Code | `application-platform/runtime/application-platform-lifecycle.ts`; `application-reentry-service.ts`; `application-engine-launcher.ts`; `application-engine-controller.ts` | Trace startup/reload/shutdown and worker ownership | Package scan/readiness precede lazy worker load; reentry stops worker then reloads bundle; tool admission/drain must be inserted explicitly | No |
| 2026-08-27 | Code | `application-bundles/utils/application-manifest.ts`; bundle domain/provider/service; SDK `manifests.ts` | Find import-safe declaration owner and current strict schema behavior | `application.json` is strict, static, copied into generated packages, and read without executing backend code; no tool declaration exists | No |
| 2026-08-27 | Code | `application-engine/worker/application-backend-definition-loader.ts`; `application-backend-host.ts`; SDK `ApplicationBackendDefinition` | Find handler owner and worker validation seam | Backend definition is strict and worker-owned; it can own an exact tool-handler map validated against static package declarations | No |
| 2026-08-27 | Code | `autobyteus-application-devkit/src/package/**`; `validation/**`; maintained `applications/*` manifests/config/backend definitions | Trace source-to-generated contract and maintained proof surface | Devkit copies `application.json`, generates backend bundle metadata, validates strict current contracts, and can regenerate maintained packages | No |
| 2026-08-27 | Code | `application-engine/runtime/application-engine-client.ts`; `json-line-frame-writer.ts`; `application-communication-limits.ts` | Verify completion and transport constraints | Current engine requests await completion without a fixed 30-second deadline; JSON-line transport already has a 4 MiB aggregate writer bound | No |
| 2026-08-27 | Data | `applications/brief-studio/application.json`; `applications/socratic-math-teacher/application.json`; package configs and Agent definitions | Inspect representative current package data | Both maintained apps use manifest v4/backend-definition v6 and select platform tools only; neither declares an application tool | No |
| 2026-08-27 | Code / prior ticket | `runtime-management/runtime-kind-enum.ts`; `agent-execution/providers/agent-provider-factory-builder.ts`; `agent-execution/backends/autobyteus/autobyteus-agent-tool-resolver.ts`; `tickets/done/runtime-agent-tools-mcp-unification/requirements-doc.md` | Verify whether the proposed shared MCP path covers every supported application runtime | Claude/Codex receive Agent Tools MCP sessions; AutoByteus native intentionally remains local and resolves unknown names from the global registry by skipping them. The earlier generic runtime wording was incomplete | Resolved by the approved native-bound/MCP projection decision |
| 2026-08-27 | Code | `application-orchestration/services/application-run-ownership-service.ts` | Find the authoritative current application/binding/run ownership reader | Existing read-only service already reconciles lookup and provenance, validates exact binding membership/status, and should be reused rather than duplicating binding-store policy in a new gateway | Design phase |
| 2026-08-27 | Code | `agent-tools/mcp/agent-tools-mcp-method-dispatcher.ts` | Check whether advertised input schemas are enforced at invocation | Dispatcher checks only that arguments are object-valued; no schema validation occurs before execution | Requirement refinement |
| 2026-08-27 | Code | `application-engine/services/application-engine-launcher.ts`; `application-engine-controller.ts`; platform construction/reentry paths | Evaluate worker-crash recovery options and dependency direction | Controller invokes only an attached worker; launcher can lazily start one but is constructed later and depends on application execution/orchestration context. Attached-worker-only dispatch keeps the new gateway acyclic but means calls after a crash fail until an owning path restores the worker | Approved fail-closed product basis; specify exact boundary in design |
| 2026-08-27 | User clarification | Conversation through the request to proceed with design | Resolve whether application tools must be available across runtimes and whether existing automatic native provisioning should be refactored | Application business tools are application-provided and application-scoped regardless of configured runtime; existing automatic native foundation and Team tools must remain provisioned exactly as today. The user then confirmed the design was clear and asked to proceed | Requirements and intended-behavior supplement approved for design |
| 2026-08-27 | External specification | `https://modelcontextprotocol.io/specification/2025-06-18/server/tools` | Check the advertised schema and server-side invocation-validation contract for a protocol version supported by the repository | MCP tool metadata defines `inputSchema` as the expected parameter schema, and the security requirements direct servers to validate tool inputs | Requirement refined in REQ-010/AC-019 |
| 2026-08-27 | User clarification | Native tools are fundamental/basic and remain local; applications provide additional business-specific tools through MCP. Follow-up clarified that some fundamental/team tools may be provisioned automatically and requested code verification | Resolve the runtime boundary without conflating native fundamentals with application extensions or assuming all exposure is configuration-only | Preserve whatever automatic native/team provisioning production code actually owns, and add application business tools without changing it | Verified in the following source rows |
| 2026-08-27 | Code | `agent-execution/backends/autobyteus/autobyteus-runtime-tool-exposure.ts`; `agent-execution/shared/runtime-agent-tool-exposure.ts` | Verify the user's recollection that native/team tools are automatically provisioned | Confirmed: every non-compactor native Agent prepends `run_bash`, `read_file`, `edit_file`, and `write_file`; any member Team context appends `get_handoff_rules`, `send_message_to`, and `delegate_task`; configured names are additive and deduplicated | Preserve as BEH-007 |
| 2026-08-27 | Code / tests | `autobyteus-agent-run-backend-factory.ts`; `autobyteus-agent-tool-resolver.ts`; `autobyteus-agent-run-backend-factory.integration.test.ts`; native exposure/resolver unit tests | Trace automatic names into real native Agent construction and verify exceptions | Backend resolves exposure before constructing `AgentConfig`; registry materializes the tools; tests prove the four-tool empty-definition baseline and restore behavior; the Memory Compactor receives no tools | Preserve production spine and tests |
| 2026-08-27 | Code | `autobyteus-ts/src/agent/context/agent-config.ts`; `agent/factory/agent-factory.ts`; `agent/bootstrap-steps/agent-bootstrapper.ts` | Check for additional hidden tool injection below the server backend | `AgentConfig` uses exactly the supplied tool instances and `AgentFactory` only indexes them; bootstrap prewarms MCP tools but does not add tools. Automatic tool selection is owned above this layer by the server exposure resolvers | No additional automatic set found |
| 2026-08-27 | Command | `pnpm -C autobyteus-server-ts exec vitest run tests/unit/agent-execution/backends/autobyteus/autobyteus-runtime-tool-exposure.test.ts tests/unit/agent-execution/backends/autobyteus/autobyteus-agent-tool-resolver.test.ts tests/unit/agent-execution/backends/autobyteus/autobyteus-mixed-tool-exposure.test.ts tests/integration/agent-execution/autobyteus-agent-run-backend-factory.integration.test.ts --no-watch` | Execute focused evidence tests | Could not start because this dedicated worktree has no installed `vitest` binary (`ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`); no source or dependency changes were made | Downstream environment setup required before execution |
| 2026-08-27 | User clarification | Application-provided MCP tools must work regardless of the runtime chosen in the application Agent configuration, while the application execution/session boundary prevents another application from receiving them | Make runtime independence and cross-application isolation authoritative | The application owns the declaration/handler; runtime-specific provisioning may differ, but selected tool identity and availability are application-scoped and runtime-invariant across AutoByteus, Claude, and Codex | Reflected in goal, REQ-003, AC-005, and supplement |
| 2026-08-27 | Code | `autobyteus-agent-tool-resolver.ts`; `agent-communication/autobyteus-send-message-tool-factory.ts`; `agent-tools/agent-communication/send-message-to.ts`; `agent-tools/mcp/providers/send-message-to-mcp-adapter-provider.ts` | Trace how `send_message_to` is actually universal across runtimes | One runtime-neutral exposure name maps to two projections over the same dispatcher: AutoByteus gets a sender-bound local `BaseTool`; Claude/Codex call the static MCP adapter through their scoped Agent Tools session | Use as application-tool projection precedent |
| 2026-08-27 | Code | `agent-provider-factory-builder.ts`; Codex `createAgentToolsMcpAppServerConfig`; Claude `ClaudeAgentToolsMcpSessionState`; `agent-tool-mcp-session-service.ts` | Verify which runtimes receive the Agent Tools MCP descriptor | The session issuer is injected into Codex and Claude construction only. Their descriptors carry run/sender/execution context, including `ApplicationExecutionContext`; AutoByteus factory receives no issuer | Correct terminology; do not claim the endpoint currently provisions native tools |
| 2026-08-27 | Code | `autobyteus-ts/tools/mcp/tool-registrar.ts`; native resolver/bootstrap; `agent-tool-mcp-catalog.ts` configured-MCP source path | Compare host-configured MCP tool availability across runtimes | Native materializes registered remote MCP tools locally and prewarms their server; Claude/Codex re-project configured MCP registry tools through Agent Tools MCP. Runtime-independent product availability does not mean one physical transport path | Keep application behavior runtime-neutral while preserving provider projections |
| 2026-08-27 | Code | `application-platform/execution/application-execution-scope-kernel-builder.ts`; `application-platform/runtime/build-application-platform-runtime.ts`; `create-application-orchestration-services.ts` | Prove construction direction for a shared native/MCP application capability | The execution scope and provider factories are constructed before the orchestration host and `ApplicationEngineLauncher`; a direct launcher dependency would cycle. The existing scoped MCP authority uses a begin/complete assembly, establishing a local one-time assembly precedent | Design a sealed application-tool capability assembly; no global setter/service locator |
| 2026-08-27 | Code | `application-run-binding-launch-service.ts`; `agent-run-provisioning-service.ts`; `mixed-agent-member-handle.ts`; `team-member-execution-identity.ts`; `application-run-ownership-service.ts` | Trace configured and task-created Team producer identity | Standalone and configured Team agents receive exact application context. Team descendants inherit the application binding and carry server-minted root Team/member/run identity, but persisted binding members do not enumerate every later task-created descendant | Extend the ownership owner with a discriminated Team-root producer query; do not require task descendants to mutate binding records |
| 2026-08-27 | Code | `autobyteus-ts/src/tools/mcp/schema-mapper.ts`; `src/utils/parameter-schema.ts`; `src/tools/base-tool.ts`; `GenericMcpTool`; Agent Tools MCP schema/result mappers | Determine a runtime-portable schema/result shape | Native maps the core object/property types, required fields, string enums, numeric minimum/maximum, string pattern, and raw array item schemas. It collapses nullable forms to a non-null type, has no nullability or length fields, rejects required `null`, and does not preserve closed-object semantics. Arbitrary JSON Schema, nullable values, length keywords, defaults on object properties, and `additionalProperties: false` therefore cannot be claimed as portable through the current native model | Narrow the application contract to an exact mapper-round-trippable subset; do not modify the fundamental native schema model in this ticket |
| 2026-08-27 | Tests | `autobyteus-ts/tests/unit/tools/mcp/schema-mapper.test.ts`; `autobyteus-ts/tests/unit/utils/parameter-schema.test.ts` | Cross-check documented mapper behavior and current round-trip coverage | Tests confirm primitive/enum/array/nested-object mapping and that nullable forms intentionally collapse to their non-null branch. They do not establish nullable validation parity or length/closed-object support | Add application-contract parity/rejection fixtures rather than treating existing nullable mapping tests as semantic support |
| 2026-08-27 | Code | `application-catalog-refresh-coordinator.ts`; `application-package-command-service.ts`; `application-catalog-reconciliation-service.ts`; `application-reentry-service.ts`; `application-bundle-service.ts` | Trace package refresh/removal beyond explicit reentry | Settings/GraphQL package commands mutate registry/root state and call the refresh coordinator; the coordinator destructively calls `ApplicationBundleService.refresh()` before reconciliation. Explicit reentry separately stops one worker and calls global bundle reload. There is no pre-mutation application-tool drain, package-scoped staged snapshot, or common live catalog commit owner | Replace the two live-mutation paths with one lifecycle-aware application catalog transition owner and package-scoped staged/commit APIs |
| 2026-08-27 | Architecture review | `design-review-report.md` (`ARCH-REV-001`, `ARCH-DI-001`, `ARCH-DI-002`) and `architecture-review-revision-record.md` | Pressure-test the initial design against schema parity and real supported lifecycle callers | Core catalog/route/gateway/worker ownership passed. The accepted nullable/length schema semantics were not native-projectable, and DS-006 skipped the supported Settings -> GraphQL -> package command -> refresh path | Resolve as design-only corrections in `SR-003`; requirements and intended-behavior supplement remain authoritative and unchanged |
| 2026-08-27 | Code / UI path | `autobyteus-web/components/settings/ApplicationPackagesManager.vue`; `autobyteus-web/stores/applicationPackagesStore.ts`; `autobyteus-web/graphql/applicationPackages.ts`; `autobyteus-server-ts/src/api/graphql/types/application-packages.ts`; `application-package-command-service.ts` | Verify the supported initiating surface for package reload/removal | A normal Studio user action reaches `reloadApplicationPackage` / `removeApplicationPackage`; removal can overlap a separately admitted application-tool call, so lifecycle coordination is product-reachable rather than speculative | Stretch DS-006 from this surface and keep package commands above one transition boundary |
| 2026-08-27 | Code | `application-bundles/domain/models.ts`; `file-application-bundle-provider.ts`; package registry snapshot/entry utilities | Determine how affected applications can be known before package mutation | Every current `ApplicationBundle` carries `packageId` and `packageRootPath`. The live bundle snapshot can therefore identify all old application IDs for one package before registry/root mutation; a staged post-mutation package slice supplies the new IDs without touching live state | Transition scope is exact `packageId`; quiesce old IDs first, stage next slice, then commit only that package and its tool declarations |
| 2026-08-27 | Code / data | Brief Studio `brief-binding-repository.ts`, `brief-read-service.ts`, `run-binding-correlation-service.ts`, Agent configs/prompts | Select a maintained read-only production proof | Brief Studio already persists `bindingId -> briefId` and durable brief records in its own `app.sqlite`; the caller binding can select the current brief without accepting a model-supplied routing ID | Design `get_brief_context` as the maintained read-only tool |
| 2026-08-27 | Lockfile / dependency inventory | `pnpm-lock.yaml`; `autobyteus-server-ts/package.json` | Select a direct schema validator dependency | Ajv 8 already exists transitively but the server does not declare it; relying on that copy would be unstable | Add Ajv 8 as a direct server dependency and validate the restricted subset |
| 2026-08-27 | Architecture review | `design-review-report.md` (`ARCH-REV-003`, narrowed `ARCH-DI-001`) and `architecture-review-revision-record.md` | Recheck `SR-003` against raw invocation meaning across native and MCP projections | The v5 keyword subset and mapper round trip are coherent and `ARCH-DI-002` is resolved. The remaining mismatch is native-only: inherited `BaseTool.prepareExecution` coerces application-tool arguments before the common Ajv gateway, whereas Claude/Codex MCP forwards raw JSON | Specialize preparation only in the native application-tool adapter; retain the checked schema for advertisement and leave `BaseTool` plus fundamental tools unchanged |
| 2026-08-27 | Code / tests | `autobyteus-ts/src/tools/base-tool.ts`; `src/agent/loop/tool-phase.ts`; `tests/unit/tools/base-tool.test.ts` | Identify the exact application-only specialization seam and preserved lifecycle behavior | `ToolPhase` calls the public polymorphic `prepareExecution` before publishing start and inherited `BaseTool.execute` calls it again before `_execute`. `BaseTool.prepareExecution` binds `agentId`, coerces and validates via `ParameterSchema`, resolves/normalizes `in_process` or `external_result`, and checks pre-start abort; its argument preparation and mode normalizer are private. A subclass override can preserve `agentId`, abort, mode, and inherited execution behavior while forwarding the same uncoerced object to `_execute`/the common capability | Design an explicit `ApplicationAgentTool.prepareExecution` override with adapter-local mode normalization; do not call `super.prepareExecution` and do not change `BaseTool` |
| 2026-08-27 | Code review | `code-review-report.md` (`CRR-001`, `CR-DI-001`) and `code-review-revision-record.md` | Reconcile implemented collision behavior with approved `REQ-009`/`AC-013` | The reviewed implementation follows the prior design's “protected static” subset, but a focused temporary Vitest probe proved that application `open_tab` shadows the registered browser static adapter because its configured-MCP policy is `prefer_configured_mcp`. The probe was removed after the reviewer recorded the successful command/evidence | Correct the canonical design; route the cumulative package through architecture review before implementation correction and renewed source review |
| 2026-08-27 | Code | `agent-tool-mcp-adapter.ts`; `agent-tool-mcp-catalog.ts`; `agent-tools-mcp-host.ts`; `providers/browser-tools-mcp-adapter-provider.ts`; `application-definition-runtime-readiness.ts`; Studio/standalone runtime composition | Separate the two collision subjects and find their real owner/interfaces | Every adapter in `adaptersByName` is a platform/static Agent Tools MCP name. `configuredMcpCollisionPolicy` governs only configured MCP versus a static adapter; browser adapters deliberately use `prefer_configured_mcp`. The host exports only `protectedStaticToolNames`, readiness checks only that subset, and the application branch rejects only that subset. `listSupportedToolNames()` already demonstrates the complete registered static set. | Expose a clearly named complete static-adapter-name snapshot from the host to readiness and make application route composition reject any registered static adapter before application precedence; retain configured-MCP policy unchanged |
| 2026-08-27 | API/E2E and code review | `api-e2e-execution-coverage-report.md` (`API-REV-001`), `api-e2e-test-review-report.md` (`CRR-003`), `api-e2e-agent-ui-proof-gap.md` | Establish exactly what the passed maintained proof covers and what the user added afterward | The 33-file/234-test feature matrix and shipped Brief integration prove authenticated MCP -> exact Team binding/member ownership -> common gateway -> real worker -> actual Brief handler -> SQLite. The durable test sends MCP directly, no model-backed Agent chooses the tool, and no browser is opened. Delivery correctly paused without invalidating that narrower pass | Treat the added journey as BEH-008/UC-008 rather than rewriting the valid lower-level result |
| 2026-08-27 | User clarification | Post-API direction recorded in `api-e2e-agent-ui-proof-gap.md` | Resolve Option A versus Option B and whether the sample may change | Brief Studio is a demonstration application and may be improved freely. The user specifically requires maintained Agent prompts to make a real Agent call `get_brief_context` and use its result; relying on `agent-config.json` or shared `team.md` alone is insufficient. This selects Option A: keep the tool read-only and use existing publication/projection/UI behavior | Amend requirements/supplement, design prompt and evidence spines, route through full downstream cycle |
| 2026-08-27 | Code | `applications/brief-studio/agent-teams/brief-studio-team/{team.md,agents/*/agent.md,agents/*/agent-config.json}`; `backend-src/services/brief-run-launch-service.ts` | Trace maintained instruction precedence and current fresh-run sequence | Both member configs select `get_brief_context`, and `team.md` says each member begins with it. Neither individual role prompt mentions the tool; their explicit ordered sequences currently start with `write_file` (researcher) or `read_file` (writer), so the most role-specific instructions contradict/omit the intended first-call proof. Launch input also reinforces publication but not the application-tool call | Put the exact first-call, once-only, error/mismatch, and result-use rules in each role prompt and reinforce them in the launch input; do not rely on selection or team text alone |
| 2026-08-27 | Code | `backend-src/agent-tools/get-brief-context.ts`; `brief-binding-repository.ts`; `brief-repository.ts` | Verify read-only result shape and identity source | The handler accepts no routing input, resolves `caller.bindingId -> briefId`, reads the brief, and returns structured `briefId`, title, status, latest binding status, and timestamp. It performs no write or notification | Retain unchanged business semantics; the Agent can make result use observable by copying the returned identity/status into its normal artifact content |
| 2026-08-27 | Code | `agent-memory/domain/memory-recording-models.ts`; `agent-memory/services/runtime-tool-trace-sequencer.ts`; `agent-memory/store/external-runtime-memory-writer.ts`; Codex/Claude tool event projection | Find safe existing Agent-call/result evidence | External-provider run memory records paired `tool_call` and `tool_result` rows with `toolName`, `toolCallId`, arguments, effective result/error. The containing run/memory subject supplies the member `agentRunId`. This is access-controlled Agent history, not process-wide operational logging | Reuse the trace; join it to the authoritative application Team binding instead of adding generic gateway payload logs |
| 2026-08-27 | Code | `brief-artifact-reconciliation-service.ts`; `brief-artifact-paths.ts`; `application-published-artifact-relay-service.ts`; `brief-read-service.ts`; GraphQL schema/executor | Trace result-informed publication to app-owned state | Published researcher and writer artifacts carry producer run/member and the bound application event. Reconciliation resolves the same `bindingId -> briefId`, stores revision/body/producer, keeps research at `researching`, moves final writer publication to `in_review`, and publishes `brief.ready_for_review` with brief/binding/revision/run | Use this existing path as the only state mutation and the binding/brief join from Agent evidence to UI state |
| 2026-08-27 | Code / UI | `frontend-src/brief-studio-runtime.js`; `frontend-src/brief-studio-renderer.js` | Define the exact supported browser assertion | `Generate draft` calls `launchDraftRun`; notifications trigger GraphQL refresh. The selected detail renders `briefId`, status badge, artifact count/final count, artifact path/body, and expandable Workflow diagnostics with latest binding/run | Assert the same brief becomes `in_review`, diagnostics show the launch binding, and the final artifact body visibly contains the tool-derived context marker; no UI component/GraphQL change is needed |
| 2026-08-27 | API/E2E evidence | `api-e2e-execution-coverage-report.md` (`API-REV-002`); `api-e2e-evidence/api-rev-002/{runtime-tool-exposure-analysis.md,researcher-raw-trace.jsonl,writer-raw-trace.jsonl,team-run-execution-tree.json,team-communication-messages.json,db-state-transitions.jsonl,final-db.json,browser-observation.json,failure-browser-blocked.png}` | Execute and retain the expanded supported browser/model journey | Browser `Generate draft` launched the shipped Codex/Luna Team and exact binding. Both members called the real application tool first with matching identity/marker. Researcher then found `write_file` unavailable and shell-created/published a blocker; writer found `read_file`/`write_file` unavailable and published no final. The same UI rendered blocked. | Historical response: treat the entry point and premise as reachable and correct the unavailable registry-file dependency. Its then-added no-shell condition is explicitly superseded by SR-010; retain the real-provider requirement and do not substitute direct MCP/mock evidence. |
| 2026-08-27 | Code review | `code-review-report.md` (`CRR-005`, `CR-DI-002`, `CR-MP-001`) and `code-review-revision-record.md` | Classify API-REV-002 failure origin | The failure is a design-level production composition mismatch, not a fixture, environment, or bounded implementation defect. Existing durable Codex coverage intentionally expects selected non-MCP `read_file` to be absent. | Revise authoritative production/file-operation spine and return through architecture review; do not advance delivery |
| 2026-08-27 | Code | `autobyteus-server-ts/src/agent-execution/backends/codex/backend/codex-thread-bootstrapper.ts`; `agent-tools/mcp/agent-tool-mcp-catalog.ts`; `agent-tools/mcp/providers/default-agent-tool-mcp-adapter-providers.ts`; `agent-tools/mcp/configured-mcp/configured-mcp-agent-tool-source-resolver.ts`; Codex bootstrapper unit coverage | Trace why configured file names are absent | Codex bootstrap sets dynamic registrations to `null` and supplies only the filtered Agent Tools MCP descriptor. Default static adapters omit `read_file`/`write_file`; configured-MCP resolution ignores non-MCP registry definitions; the unit test explicitly expects selected `read_file` absent. | Do not design config-only registry selection as provider exposure and do not broaden the provider boundary for this maintained proof |
| 2026-08-27 | Live integration source/evidence | `autobyteus-server-ts/tests/integration/agent-execution/codex-agent-run-backend-factory.integration.test.ts` (`executes Codex file edits...` case); Codex file-change event conversion | Verify a supported non-shell Codex file operation | The case instructs “Use the edit_file tool,” observes normalized `edit_file` events, and verifies a file, showing file-change normalization exists. Later CRR-007 proves this source normally uses other models, was skipped in IR-004, and has a stale forced-live factory call; it does not prove Luna's model-facing name. | Retain only as historical lower-level evidence; downstream must correct it to `apply_patch`/current factory identity if used and must not substitute it for exact Luna/browser proof |
| 2026-08-27 | Code | `services/published-artifacts/published-artifact-tool-contract.ts`; `published-artifact-publication-service.ts`; `published-artifact-path-identity.ts`; `agent-tools/mcp/providers/publish-artifacts-mcp-adapter-provider.ts` | Verify publication after a provider-owned workspace patch does not require a write-tool-returned absolute path | `publish_artifacts` explicitly accepts workspace-relative paths. The MCP adapter supplies the session working directory, and publication resolves a relative path against that exact workspace before reading/persisting it. | Use canonical `brief-studio/research.md` and `brief-studio/final-brief.md`; assert resolution to the actual member workspace file |
| 2026-08-27 | Code/data | maintained Brief role/team configs and prompts; `team-communication-messages.json` | Find the smallest writer-input replacement for unavailable `read_file` | Research output is intentionally bounded to about 200–500 words, and the Team message path already carries model-authored handoff content. The current handoff includes only marker/path/summary, so the writer depends on a file read. The structured research/final outlines expose `Key findings` and `Key evidence` sections suitable for an exact use witness. | Require the researcher to hand off the complete checkpoint body plus marker/path; writer consumes it without `read_file` and copies at least one complete non-marker key-finding bullet verbatim into final key evidence |
| 2026-08-27 | API/E2E evidence | `api-e2e-execution-coverage-report.md` (`API-REV-003`); `api-e2e-evidence/api-rev-003/{identity-trace-artifact-join.json,runtime-native-edit-failure-analysis.md,final-db.json,db-state-transitions.jsonl,final-browser-no-publications.png,*-execution-tree.json,*-researcher-raw-trace.jsonl,*-writer-raw-trace.jsonl}` | Execute the SR-007 correction through the same real configured provider/Team/browser path | Two independent supported browser launches used exact shipped Codex/Luna roles. Every member called `get_brief_context` exactly once first, but both researchers treated “provider-native edit_file” as unavailable and emitted zero native edits/publications; writers received blockers and also failed closed. Auth, route, worker, binding, Team messaging, DB, and browser were healthy; the same brief remained `not_started` with zero artifacts/revisions. | Treat as valid repeatable production failure, not a fixture or model-compliance flake; correct the model-facing operation name upstream |
| 2026-08-27 | Code review / exact provider diagnostic | `code-review-report.md` (`CRR-007`, reopened `CR-DI-002`, reachable `CR-MP-002`); local `codex-cli 0.150.1` app-server, `gpt-5.6-luna`, `workspace-write`, approval `never`, no dynamic tools, shell forbidden | Separate the fixed model-facing instruction from provider and platform event names | Instructing the current Luna model to use built-in `apply_patch` created the exact file and emitted `item/started` / `item/completed` with type `fileChange`; no command event occurred. `CodexItemEventPayloadParser` and `CodexItemEventConverter` map the `file_change` family to normalized `edit_file`. Therefore `apply_patch`, `fileChange`, and `edit_file` are three distinct boundaries. | Prompts/team/launch say built-in `apply_patch`; evidence retains native `fileChange` and normalized `edit_file`; configs list neither; rerun exact browser path |
| 2026-08-27 | Coverage/source review | `tests/integration/agent-execution/codex-agent-run-backend-factory.integration.test.ts`; `code-review-report.md` CRR-007 residual risk | Audit why CRR-006's prior provider-name proof was insufficient | The live integration prompt says “edit_file”, normally prefers other models, and was skipped in IR-004. When forced live with Luna it stops before inference because its backend-factory invocation lacks the now-required `agentRunId`. This secondary stale coverage issue did not cause API-REV-003. | If retained/executed, update prompt to `apply_patch`, supply current factory identity, and record the exact model; do not use a skip or different model as Luna proof |
| 2026-08-28 | API/E2E evidence | `api-e2e-execution-coverage-report.md` (`API-REV-004`), `api-e2e-revision-record.md`, `api-e2e-evidence/api-rev-004/` | Establish the last fully reviewed production state before new clarification/latest-base movement | The actual configured Codex/Luna Team called `get_brief_context` first, produced both files through provider-native patching, published relative paths, carried the complete handoff, reconciled the same brief to `in_review`, and rendered it in the browser. This proves the application-tool, identity, worker, publication, and UI owners are healthy; only prompt abstraction and latest-base composition are reopened. | Preserve as historical pass; rerun after SR-009 implementation because prompt and session base will change |
| 2026-08-28 | User clarification | Conversation: `agent.md` should focus on business; the LLM is capable of selecting the correct automatically supplied runtime tool without being told `apply_patch` | Resolve whether provider operation vocabulary belongs in the application behavior contract | No. The stable prompt contract is business call/content/artifact/publication/handoff. Runtime/provider owns foundation provisioning; the model selects the operation. `apply_patch -> fileChange -> edit_file` remains diagnostic/current-provider evidence only. | Replace SR-008's provider-name prompt coupling across requirements, supplement, design, guidance, and tests |
| 2026-08-28 | API/E2E evidence and code-review classification | `api-e2e-execution-coverage-report.md` (`API-REV-005`); `code-review-report.md` (`CRR-012`, reopened `CR-DI-002`, reachable `CR-MP-002`); `api-e2e-evidence/api-rev-005/{clean-identity-trace-artifact-ui-join.json,clean-evidence-synthesis.log,clean-researcher-raw-trace.jsonl,clean-writer-raw-trace.jsonl,clean-final-browser-observation.json,clean-final-browser-in-review-summary.png,current-lifecycle-topology-matrix.log}` | Verify the latest-base application MCP and business-prompt journey | All application/lifecycle and real Agent-to-UI criteria except old AC-039 passed. Both actual Codex/Luna members called `get_brief_context` exactly once first, used the result, created correct workspace artifacts using automatically supplied shell, published relative paths, completed the handoff, reconciled the same brief, and rendered the correct `in_review` UI. The only failure was the solution's zero-shell assertion. | Preserve the passing production architecture and execution; correct the acceptance/proof contract rather than adding a runtime policy or changing source behavior |
| 2026-08-28 | User correction | Conversation after `CRR-012`: “Remove this, I never said this”; a correct file created with `run_bash` must not make the entire test fail | Resolve whether shell use is prohibited for the maintained proof | No. The zero-shell rule was introduced by the solution process, not requested by the user. Any already-authorized runtime foundation capability, including shell, may create the artifact. Acceptance is based on the real in-workspace artifact, required content/path, publication, handoff, identity, security boundary, and UI causality. | Supersede the zero-shell portions of SR-007–SR-009; revise AC-039/REQ-020/REQ-021, supplement, DS-013/DS-014, guidance, risks, and coverage without production architecture changes |
| 2026-08-28 | Delivery report | `delivery-latest-base-conflict-report.md`; `delivery-revision-record.md` (`DR-004`); `delivery-integration-evidence.log` | Establish latest-base integration state and whether conflicts are mechanical | Delivery protected the reviewed state at `aaf7e076e`, fetched `origin/personal=ebef77eb3`, encountered 12 conflicts in session/host/provider/kernel/docs/tests, captured evidence, and aborted. The new base changes routing, security, listener topology, stop/restore, and cleanup ownership, so mechanical conflict selection is unsafe. | Reconcile design before implementation; no Electron build until valid integrated state |
| 2026-08-28 | Upstream completed ticket | `git show origin/personal:tickets/done/agent-tools-mcp-session-resume/{requirements.md,design-spec.md,investigation-notes.md}` | Identify authoritative latest-base behavior rather than inferring from conflicts | One process `AgentToolsMcpHost` owns a dedicated `127.0.0.1:0` listener; normalized run ID derives a deterministic non-secret session ID; descriptors have no headers; stop deletes active state; restore activates the same route with fresh current live context; exact managed run/resource finalization owns deactivation; no session state is persisted. | Adopt these owners and terminology unchanged |
| 2026-08-28 | Latest-base code | `git show origin/personal:autobyteus-server-ts/src/agent-tools/mcp/{agent-tool-mcp-session.ts,agent-tool-mcp-session-service.ts,agent-tool-mcp-session-registry.ts,agent-tool-mcp-session-authority.ts,scoped-agent-tool-mcp-session-authority.ts,agent-tools-mcp-host.ts,agent-tools-mcp-local-server.ts,agent-tools-mcp-http-gate.ts}` | Trace the exact activation/materialization/listener boundary | `activateForRun` resolves exposure, builds fresh execution capabilities, stores one active record keyed by derived run-session ID, and returns a headerless descriptor from the host-owned local base. Scoped ledger deactivates exact run/session; host owns listen/close. The current feature must add nullable application capability and application routes inside this activation, not restore issuer/bearer/global defaults. | Map combined interfaces/files/tests in SR-009 design |
| 2026-08-28 | Latest-base and feature code comparison | `git diff --histogram origin/personal..HEAD --` the five conflicted production files; feature `application-agent-tool-capability.ts`, `application-agent-tool-gateway.ts`, `application-agent-tool-call-lifecycle.ts`, `application-execution-scope-kernel-builder.ts`, `application-reentry-service.ts` | Decide lifecycle and dependency composition | Application capability is a scope-owned live reference serving many applications; route identity/fingerprint is immutable data; gateway revalidates current catalog/binding; package lifecycle drains per application. Combined target: application scope completes the latest-base authority with `{publishedArtifactPublisher, applicationAgentTools}`, provider factory receives `agentToolMcpRunSessions` plus application capability, and package transitions never call session deactivation. | Add fresh-restore, general/app, reload-with-live-session, removal, stop, shutdown, and listener coverage |
| 2026-08-28 | Command | `git status --short --branch`; `git show-ref --verify refs/remotes/origin/personal`; `git diff --stat aaf7e076e..origin/personal`; `git ls-tree -r origin/personal tickets/done/agent-tools-mcp-session-resume/` | Verify isolated worktree state and exact upstream reference | Branch remains at protected checkpoint with only delivery artifacts/generated outputs uncommitted; merge is not in progress; latest tracked base exists at `ebef77eb3`; source diff confirms overlapping Agent Tools owners. | Edit only solution-owned artifacts; do not reset downstream evidence or merge during design |

## Relevant Existing Behavior And Production Paths

| Behavior ID | Kind | Current Supported Trigger Or Governing Contract | Current Production Path And Lifecycle | Meaningful Current Outcome / Invariants | Evidence |
| --- | --- | --- | --- | --- | --- |
| BEH-001 | Contract | Agent definition selects `toolNames` | Agent definition -> runtime exposure -> scoped Agent Tools MCP session -> platform/configured-global route | Only registered platform/global tools are available; application packages own none | `application-runtime-definition-validator.ts`; `agent-tool-mcp-catalog.ts` |
| BEH-002 | System | Application-owned Agent/Team launch | application capability -> ApplicationExecutionScope -> latest-base run-session activation or native bound-tool construction | Run activation carries current application execution context; base session catalog remains process-owned and application routing must be composed without global registration | latest-base `agent-tool-mcp-session-service.ts`; feature `application-agent-tool-capability.ts` |
| BEH-003 | System | Provider calls a tool over its deterministic MCP descriptor | provider -> dedicated loopback listener/local gate -> active run-session lookup -> catalog route -> adapter -> result | Latest base provides tokenless deterministic routing, current live context, explicit route selection, and exact-run deactivation; the route ID is not authentication | latest-base host/local-server/gate/session service/registry/executor |
| BEH-004 | Operational | Application package reload, worker stop, run termination, host shutdown | implemented feature transition quiesces the per-application call lane and worker/catalog; latest-base run/resource lifecycle separately deactivates exact run sessions; process host separately closes the listener | These owners are individually healthy but the branch implementation predates the latest-base session authority and conflicts at their composition seam | feature `application-reentry-service.ts`/platform lifecycle; latest-base AgentRun/session authority; DR-004 |
| BEH-005 | Contract/Operational | Strict package/backend contract plus reentry | static package scan -> readiness -> lazy worker load; reentry stops worker then reloads bundle | Import does not execute app code; no tool-call admission/drain or stale route contract exists | application manifest parser; backend definition loader; lifecycle/reentry sources |
| BEH-006 | Data/Contract | Maintained/imported package plus durable runtime data | generated package schemas are strict; durable app/platform data is stored separately in app/platform SQLite and definition files | Package artifacts are rebuildable; no tool-related database field exists | devkit packer/validators; application storage/binding stores; representative manifests |
| BEH-007 | System/Preserved | Native Agent/Team backend construction | `resolveAutoByteusRuntimeAgentToolExposure` -> runtime-neutral exposure builder -> `resolveAutoByteusAgentTools` -> `AgentConfig` -> `AgentFactory` | Non-compactor native baseline is automatically four tools; Team context automatically adds the collaboration trio; configured names are additive/deduplicated; Memory Compactor remains empty | native exposure/resolver/backend sources and focused unit/integration tests |
| BEH-008 | User/System | User clicks Brief Studio `Generate draft` for a selected brief | UI -> GraphQL launch -> configured Codex/Luna Team -> each role's first application business-tool call -> required business artifact and relative publication -> complete Team handoff -> final artifact -> app projection/notification -> UI refresh | API-REV-004 proves the path. The later user clarification moves provider-operation selection below the business prompt boundary; current Codex `fileChange`/normalized `edit_file` remain evidence of how the runtime fulfilled the artifact, not role vocabulary | User clarification; API-REV-004; prompts/config/launch service; provider events; publication, Team message, binding/projection, renderer |
| BEH-009 | System/Operational | Stop/restore an application Agent, or reload/remove its application while a run remains live | exact run stop -> managed resource release -> deactivate deterministic run session; restore -> same URL/fresh route materialization. Separately, package transition -> affected application lane drain -> worker/catalog transition -> reopen/quarantine | Session lifetime and application package lifetime are orthogonal. Reentry cannot deactivate the whole provider run session; activation cannot start/reload workers. Current binding/fingerprint checks join the two safely at call time | latest-base completed ticket/code; feature application call lifecycle/gateway/reentry; DR-004 conflict report |

## Design Health Assessment Evidence

- Change posture: `Feature` / `Larger Requirement`
- Root cause classification: `Boundary Or Ownership Issue`
- Refactor posture evidence summary: The shared latest-base transport/run-session infrastructure is reusable, but the process-global catalog cannot own application-local business tools. The design requires one application-owned catalog/dispatch/lifecycle boundary and thin native/MCP projections, plus a bounded rebase of the feature from removed bearer issuance onto current activation/deactivation owners.

| Evidence Source | Observation | Design Health Implication | Follow-Up Needed |
| --- | --- | --- | --- |
| `agent-tool-mcp-catalog.ts` | Static adapters and configured MCP tools resolve through process catalog/global registry | Direct application registration would create mixed ownership and collision/leakage risk | Define scoped composition contract |
| latest-base `agent-tool-mcp-session.ts` / registry / service | Active session carries current `applicationExecutionContext`; deterministic ID is derived only from run ID and the record is active-only | Exact application identity can govern route inclusion without a locator or persistence; restore must rematerialize it rather than infer it from URL | Trace all activation/restore/deactivation paths |
| `application-backend-host.ts` | Worker owns application business handlers | Application tool handlers should remain worker/application-owned | Define invocation protocol and readiness |
| `application-runtime-definition-validator.ts` | Tool validation is global-registry-only | Application package validation needs a separate application catalog view | Trace package validation lifecycle |
| latest-base `agent-provider-factory-builder.ts`; feature AutoByteus resolver/factory | Claude/Codex receive `AgentToolMcpRunSessionActivator`; native foundation tools remain local. The feature additionally needs the application capability for native bound tools and session composition | Combined factory input should carry the current activator plus optional application capability; general scope supplies null, application scope supplies the live capability | Do not restore the removed issuer/bearer interfaces |
| `application-run-ownership-service.ts` | Existing reader owns exact binding/run membership and terminal-status policy | Application-tool authorization should reuse this owner, not directly duplicate store reads or status rules | Design gateway against the existing reader |
| `autobyteus-runtime-tool-exposure.ts`; `runtime-agent-tool-exposure.ts` | Own native foundation and automatic Team-tool composition before tool materialization | Automatic provisioning is real supported behavior, not configuration shorthand | Extend additively; do not rewrite or route these sets through application MCP |
| Native `send_message_to` bound tool and `SendMessageToMcpAdapterProvider` | Two projections over one dispatcher/policy owner | Existing universal tool behavior does not require AutoByteus to consume the Agent Tools HTTP endpoint | Reuse this owner/projection shape for application tools unless design evidence requires otherwise |
| `BaseTool.prepareExecution`; `ToolPhase.prepareToolExecution` and `BaseTool.execute` | Generic native tools intentionally coerce/validate arguments before execution; both native tool-phase preparation and direct execution dispatch polymorphically through `prepareExecution` | The generic behavior is healthy for fundamental/configured tools but is not valid for application tools whose raw JSON must have one Ajv meaning across runtimes | Override only `ApplicationAgentTool.prepareExecution`; keep the checked `ParameterSchema` as the advertised definition and make the common gateway the sole application-schema validator |
| `AgentToolMcpCatalog.adaptersByName`; `listSupportedToolNames()`; adapter `configuredMcpCollisionPolicy` | The catalog owns all platform/static MCP adapter names and separately applies per-adapter policy to configured MCP collisions; the current complete reader is ambiguously named | Application declarations must reserve the complete static-adapter namespace; the configured-MCP policy subset is a different subject and cannot define application validity | Rename the complete reader to `listStaticAdapterToolNames()`, remove the public protected-subset reader, keep protected/prefer logic internal to configured-MCP resolution, and test both policies against application collisions |
| `AgentToolsMcpHost.protectedStaticToolNames`; Studio/standalone -> application runtime -> readiness | IR-001 exports and propagates only the protected configured-collision subset | The application readiness boundary needs an immutable complete static-adapter-name snapshot, independent of runtime availability and configured-MCP policy | Replace the public property/dependency with `staticAdapterToolNames`; do not expose the protected subset as application policy |
| Brief Studio role prompts versus `team.md`/`agent-config.json` | Tool selection makes the tool available and shared Team text states intent, but the role-local ordered instructions start with other tools | The maintained demonstration has a missing prompt invariant, not a platform routing defect. The closest role instruction must own exact first-call/result-use behavior | Update both `agent.md` files and launch input; keep selection, tool handler, and platform capability unchanged |
| Agent run trace + application Team binding + artifact projection | Existing owners already retain the call/result, exact producer identity, and business/UI correlation keys separately | A joined evidence spine is sufficient and safer than logging generic application payloads process-wide | Define deterministic joins by `toolCallId`, `agentRunId`, `bindingId`, and `briefId`; preserve existing access control and redaction policy |
| API-REV-002 traces + Codex bootstrap/catalog resolution | The real configured members obeyed the application-tool first-call rule, but `toolNames` containing ordinary registry file tools did not cause those tools to appear in Codex; shell fallback and missing writer output followed | CR-DI-002 is a provider-composition/file-operation boundary assumption in the maintained workflow, not a prompt-only defect or test setup issue | Replace the sample's unavailable registry dependency; do not change the shared application-tool architecture or globally expose file operations |
| API-REV-004 + user business-prompt clarification | Exact provider naming produced a pass, but the user rejects that coupling in application `agent.md`; LLM reasoning over automatically supplied runtime capabilities is the intended abstraction | Keep business behavior in prompts and mechanics in runtime/evidence. No new adapter, service, storage shape, runtime, alias, or configured foundation name is needed | Prompts name business artifact/outcome only; current provider events remain verifier evidence; rerun the real journey |
| DR-004 + latest-base run-session code/ticket | Feature and base independently changed the same session/host/provider/kernel owners; base eliminates bearer/random issuance and owns deterministic stop/restore | This is a real boundary rebase, not a line-level merge. Preserve latest-base authority and insert application capability/route composition at its existing activation and provider-factory seams | Update owners/interfaces/files/sequence/coverage; architecture review before merge implementation |

## Relevant Files / Components

Completed architecture-level inventory for the initial baseline and `ARCH-REV-001` follow-up.

| Path / Component | Current Responsibility | Finding / Observation | Design / Ownership Implication |
| --- | --- | --- | --- |
| latest-base `autobyteus-server-ts/src/agent-tools/mcp/agent-tools-mcp-host.ts` and `agent-tools-mcp-local-server.ts` | Dedicated Agent Tools process transport and scoped authority composition | Owns one `127.0.0.1:0` listener, ready base URL, active registry/catalog/dispatcher, listen/close, and no raw main-listener route dependency | Preserve as authoritative; additionally expose only the immutable complete static-adapter-name snapshot required by readiness; no per-app/main-listener transport |
| `.../agent-tool-mcp-catalog.ts` | Platform/configured-global session tool resolution | Process-owned/global | Must consume an application-scoped resolver rather than own application tools |
| `.../agent-tool-mcp-catalog.ts` static adapter index and configured collision policy | Owns registered platform/static adapter names and configured-MCP-versus-static precedence | IR-001 used only the `protect_static_adapter` subset to reject application routes, allowing `prefer_configured_mcp` static names such as `open_tab` to be shadowed | For application routes, reject any `adaptersByName` collision before applying application-over-configured precedence; keep existing configured collision branches unchanged |
| `.../agent-tools-mcp-host.ts` | Public MCP host boundary | IR-001 exposes `protectedStaticToolNames`, which names the wrong policy subject for application readiness | Expose the immutable complete `staticAdapterToolNames` snapshot and stop exporting configured-collision protection as application policy |
| latest-base `.../agent-tool-mcp-session-service.ts` / registry / authority | Activates deterministic tokenless run sessions and materializes current live state | Already receives current execution context and builds execution capabilities; deactivation deletes the exact active record | Compose nullable application capability/routes here; retain `activateForRun`/`deactivateForRun`, headerless descriptor, local base reader, active-only registry, and no global defaults |
| `.../agent-tool-mcp-tool-executor.ts` | Executes a resolved session route | Current availability re-enters shared catalog | Needs exact application route dispatch without global lookup |
| `.../application-runtime-definition-validator.ts` | Validates application-owned Agent/Team definitions | Global tool lookup only and currently lacks application catalog context | Resolve selected names against process tools plus exact application catalog |
| `.../application-engine/worker/application-backend-host.ts` | Loads/executes application business definition | Natural application handler owner | Add one narrow tool handler entrypoint using existing handler context plus caller identity |
| `.../application-engine/runtime/protocol.ts` | Host/worker JSON-RPC contract | No tool operation | Add one host-to-worker tool invocation request/result contract |
| `autobyteus-application-sdk-contracts/src/manifests.ts` | Static package manifest contract | Strict v4 and no tool catalog | Recommended static application-tool declaration owner; current contract must change cleanly |
| `autobyteus-application-sdk-contracts/src/index.ts` | Backend definition/handler contracts | Strict v6 and no application tool type | Add exact handler/result/caller shapes; external publication journey remains out of scope |
| `.../application-engine/services/application-engine-controller.ts` | Exact applicationId-to-attached-worker operations | Created before execution scope and already owns worker request dispatch | Appropriate narrow call dependency for an application-tool gateway; do not expose controller to MCP layers |
| `.../application-orchestration/stores/application-run-binding-store.ts` | Authoritative durable binding read/write | Binding contains application and exact runtime members | Invocation gateway can validate application/binding/producer without global manager lookup |
| `.../application-orchestration/services/application-reentry-service.ts` | Per-application bundle/worker reentry sequence | No tool admission or drain step | Must coordinate application-tool quiesce/drain/reopen around existing stop/reload |
| latest-base `.../agent-execution/providers/agent-provider-factory-builder.ts` | Builds native, Codex, and Claude backends | Supplies `AgentToolMcpRunSessionActivator` to Codex/Claude; AutoByteus remains local | Add optional application capability alongside the activator: native factory consumes it; MCP activation consumes it through the completed application scope; general execution supplies null |
| `.../agent-execution/backends/autobyteus/autobyteus-agent-tool-resolver.ts` | Creates native in-process fundamental tools from selected names | Uses the global registry and silently skips unknown tools | Preserve fundamental resolution while composing selected application MCP tools through a separate scoped path |
| `.../application-orchestration/services/application-run-ownership-service.ts` | Read-only authoritative application run ownership lease | Reconciles provenance/lookup and validates exact binding membership and status | Reuse as the authorization owner behind any new invocation gateway |
| `.../agent-execution/backends/autobyteus/autobyteus-runtime-tool-exposure.ts` | Prepends native foundation tools | Automatically adds `run_bash`, `read_file`, `edit_file`, and `write_file`, except for the Memory Compactor | Preserve exact owner, ordering, eligibility, and persisted-definition non-mutation |
| `.../agent-execution/shared/runtime-agent-tool-exposure.ts` | Normalizes configured exposure and adds Team capabilities | Automatically appends `get_handoff_rules`, `send_message_to`, and `delegate_task` whenever member Team context exists | Preserve automatic Team composition for all runtime families |
| `autobyteus-ts/src/agent/context/agent-config.ts`; `agent/factory/agent-factory.ts` | Accept and index final native tool instances | Do not add another implicit tool set | Application MCP composition belongs before final `AgentConfig`, alongside but not inside the preserved automatic resolvers |
| `autobyteus-ts/src/tools/mcp/schema-mapper.ts`; `src/utils/parameter-schema.ts`; `src/tools/base-tool.ts` | Map MCP JSON Schema into native definitions, advertise those definitions, and perform generic native preparation/validation | Nullability is collapsed, length/closed-object/object-default semantics have no representation. In addition, `BaseTool` changes string integers/numbers/booleans, empty-string arrays, and nested coercible values before `_execute`, so reusing its preparation for application tools changes invalid-input meaning relative to MCP | Reject non-projectable keywords, add a checked native schema projector, and override preparation in the application adapter so the common gateway receives raw arguments; leave all three fundamental files behaviorally unchanged |
| `autobyteus-server-ts/src/application-packages/services/application-package-command-service.ts` | Supported import/reload/remove use cases and registry/install rollback | Mutates registry/root state before calling the current destructive refresh boundary | Keep package mutation mechanics here but require one catalog transition wrapper before mutation |
| `.../application-packages/services/application-catalog-refresh-coordinator.ts`; `.../application-orchestration/services/application-reentry-service.ts` | Global bundle refresh/reconcile versus exact-worker stop/global reload | Two live mutation entrypoints compete and neither owns application-tool drain | Remove the refresh coordinator; replace reentry's bundle-mutation API with participant-only lifecycle operations used by `ApplicationCatalogTransitionService`; do not retain forwarding wrappers |
| `.../application-bundles/services/application-bundle-service.ts` | Live bundle snapshot/maps | `refresh()` clears live state before rescan; there is no staged package/application candidate | Extend the live state owner with non-mutating stage and internally prepared synchronous slice commit APIs used only by the transition service |
| `applications/brief-studio/agent-teams/brief-studio-team/agents/{researcher,writer}/agent-config.json` | Selects routed capabilities and fixes the maintained provider/model | Current Codex/Luna configs still list ordinary registry `read_file`/`write_file`, but API-REV-002 proves those selections are not provider-facing tools | Keep Codex/Luna; remove both unavailable names; retain only routed application/collaboration/publication capabilities actually used |
| `applications/brief-studio/agent-teams/brief-studio-team/agents/{researcher,writer}/agent.md` | Role-local maintained Agent business instructions | IR-005/API-REV-004 proved explicit `apply_patch`, but the user now requires this file to focus on business and trust model reasoning over runtime tools | Retain first application call, marker/content, canonical path, publication, complete handoff/no-cross-member-read, and fail-closed outcomes; remove every foundation/provider operation name |
| `applications/brief-studio/agent-teams/brief-studio-team/team.md` | Shared maintained workflow reinforcement | Currently repeats provider-operation mechanics after IR-005 | Align to business artifact/handoff/relative-path outcomes; remain reinforcement rather than behavior or runtime-tool owner |
| `applications/brief-studio/backend-src/services/brief-run-launch-service.ts` | Constructs the Team's fresh-run application input | Currently reinforces provider-operation mechanics after IR-005 | Reinforce business calls/artifacts/complete handoff/relative publication without passing routing identity, listing foundation tools, or replacing role prompts |
| `autobyteus-server-ts/src/agent-execution/backends/codex/backend/codex-thread-bootstrapper.ts` and `events/{codex-item-event-payload-parser.ts,codex-item-event-converter.ts}` | Own Codex provider thread configuration and native event normalization | Dynamic registry tools are intentionally absent. Provider `item/fileChange` / `file_change` is normalized as `edit_file`; normalization is not model-facing vocabulary | Reuse unchanged; do not add a Brief-specific dynamic tool, generic `apply_patch` route, prompt alias, or provider-wide composition change |
| `autobyteus-server-ts/tests/integration/agent-execution/codex-agent-run-backend-factory.integration.test.ts` | Optional live Codex runtime coverage | IR-005/API-REV-004 updated it to the then-current provider wording and exact Luna. It is lower-level provider evidence, not application prompt authority | Coverage owner decides whether its provider-specific diagnostic wording remains appropriate; the Brief role contract must not copy that wording, and this test never substitutes for browser proof |
| latest-base `agent-tool-mcp-run-session-id.ts`; `agent-tools-mcp-local-access.ts`; `agent-tools-mcp-http-gate.ts` | Deterministic routing identity and tokenless local trust boundary | Run-session ID is non-secret routing; peer/Host/Origin admission occurs before active-record lookup | Preserve unchanged; application identity is live session data and current binding authorization, never URL entropy or a restored bearer |
| latest-base `application-execution-scope-kernel-builder.ts` plus feature input contract | Execution-scope assembly and provider/resource wiring | Base completes scoped authority with publication capability and passes `authority.runSessions` to providers/resource manager. Feature adds application capability for MCP and native | Combined builder completes with both live capabilities, passes current activator/deactivator unchanged, and supplies application capability separately to provider-factory native composition; no mixed-level bypass |
| `autobyteus-server-ts/src/services/published-artifacts/{published-artifact-tool-contract.ts,published-artifact-publication-service.ts,published-artifact-path-identity.ts}` | Owns accepted artifact paths and workspace resolution | Already permits and resolves workspace-relative paths | Reuse unchanged; prompts use exact canonical relative paths rather than needing a writer-tool returned absolute path |
| `applications/brief-studio/backend-src/agent-tools/get-brief-context.ts` | Read-only binding-derived business handler | Already returns exactly the durable identity/status needed by both roles and performs no mutation | Keep read-only; no direct UI projection or new mutation endpoint |
| `applications/brief-studio/backend-src/services/brief-artifact-reconciliation-service.ts` and `brief-artifact-paths.ts` | Application-owned publication projection and status transition | Existing final writer rule moves the same brief to `in_review` and emits a ready notification | Reuse unchanged as the state owner; verify identity/body rather than adding a tool-call side effect |
| `applications/brief-studio/frontend-src/brief-studio-{runtime,renderer}.js` | Existing notification refresh and brief-detail rendering | Already displays exact `briefId`, `in_review`, final artifact body/path/count, and latest binding diagnostics | No product UI change required; browser proof must assert these existing fields |
| `autobyteus-server-ts/src/agent-memory/**` plus run-history projection | Access-controlled per-Agent tool-call/result history | Already records paired call/result for the default external-provider sample runtime | Reuse for evidence; do not add generic process-wide result logging |

## Runtime / Probe Findings

Static tracing confirmed the user's recollection: a native Agent with an empty persisted `toolNames` still receives the four-tool foundation baseline, and Team context independently adds the collaboration trio. The downstream chain through SR-009/IR-006/IR-007/CRR-011/API-REV-005 now proves the complete current application call, tokenless session lifecycle, authorized workspace artifacts, relative publication, complete handoff, reconciliation, identity joins, and browser outcome. The exact roles selected shell from their ordinary runtime foundation and still produced the correct result. That is successful production evidence under the user's actual requirement; the old zero-shell assertion was an invalid proof condition, not an architecture or implementation failure.

DR-004 adds a separate latest-base design correction. Read-only inspection of `origin/personal=ebef77eb3` confirms `AgentToolsMcpHost` now owns one dedicated loopback listener, `AgentToolMcpSessionRegistry` is active-only and keyed by a deterministic run-derived non-secret ID, `AgentToolMcpSessionService.activateForRun` rematerializes current live context/routes/capabilities, and exact managed run/resource finalization calls `deactivateForRun`. The feature checkpoint still contains random bearer issuance/revocation and main-listener composition. The merge conflicts are therefore expected symptoms of two incompatible owners, not missing imports. The combined target retains latest-base authority and inserts application capability only into live execution capabilities/catalog composition plus the existing native factory seam.

## External / Public Source Findings

- The official MCP 2025-06-18 tools specification defines `inputSchema` as the JSON Schema for expected parameters and requires servers to validate tool inputs. The repository accepts this protocol version. This supports making schema validation an explicit pre-worker requirement rather than treating the package schema as discovery-only metadata.

## Reproduction / Environment Setup

- Dedicated task worktree created from fresh `origin/personal`.
- The solution designer made no production-source edits. `IR-001`/`IR-002` subsequently added and corrected the platform implementation in the same worktree and established the pnpm dependency environment; their exact checks are authoritative in `implementation-handoff.md` and `implementation-revision-record.md`.
- `CRR-001` ran `corepack pnpm --filter autobyteus-server-ts test --run tests/unit/application-owned-mcp-static-collision.code-review.test.ts`; the focused temporary probe passed, demonstrated `CR-DI-001`, and was removed by the reviewer. This evidence is recorded in `code-review-report.md`; no probe file is claimed as durable coverage.
- API-REV-001's exact 33-file/234-test command, package builds, and provider preflight are recorded in `api-e2e-execution-coverage-report.md`. They remain valid for the direct-MCP scope but are not represented as the new model-backed/browser proof.
- API-REV-002's documented dev start, authenticated provider preflight, supported browser action, exact identity join, raw member traces, Team communications, database transitions, browser observation, screenshot, and focused repository tests are retained under `api-e2e-evidence/api-rev-002/`. The failed result is authoritative production evidence; it is not reclassified as an environment problem.
- API-REV-003's current package build, two supported browser launches, exact Codex/Luna member traces, identity join, database transitions/final state, and no-publication browser screenshot are retained under `api-e2e-evidence/api-rev-003/`. The 88.6% result is a high-confidence failure, not a confidence pass: the environment and production path were healthy while the maintained model-facing operation wording failed twice. CRR-007's exact current Luna app-server diagnostic supplies the bounded repair evidence; no API/E2E durable coverage changed in Round 3.

## Findings From Code / Docs / Data / Logs

- Platform/common tools and user-configured MCP tools currently share a process-wide lookup/collision domain.
- Application execution sessions already carry authoritative application execution context.
- Application worker is the existing owner of application business code and lifecycle.
- Application package import remains prebuilt-only and must not execute arbitrary app code merely to discover tools.
- Application definitions currently refer to plain tool names. Requirements choose exact-application routing, reject platform/static collisions, allow cross-application reuse, and give the owning application precedence over configured-global MCP inside its session.
- Application session route tables are already immutable snapshots. A declaration fingerprint can preserve that invariant while failing closed after removal/schema change.
- `ApplicationEngineClient.request()` currently has no local fixed completion timeout. Application tools should reuse completion-coupled semantics and never auto-retry a possibly mutating handler.
- The current platform shutdown stops workers before closing the execution scope. The target must block application-tool admission and drain admitted calls before worker stop while retaining the larger owner order.
- The shared Agent Tools MCP path currently projects only to Claude/Codex. The target must additionally project application-owned business tools to AutoByteus-native application runs while leaving native fundamental tools on their existing local path.
- `send_message_to` confirms the current universal-tool pattern: automatic/common exposure policy is shared, but AutoByteus receives a bound local tool that calls `SendMessageToDispatcher`, while Claude/Codex receive a scoped MCP descriptor whose adapter calls the same dispatcher. The Agent Tools endpoint does not currently provision the native tool.
- Host-configured MCP tools follow a similar product/runtime split: native uses its MCP tool wrapper/client path, while external providers receive configured tools re-projected through the Agent Tools MCP catalog.
- `tools/call` currently checks only object shape, so the declared input schema requires a new enforcement boundary before application code receives arguments.
- `ApplicationRunOwnershipService` already centralizes application/binding/run membership and status checks; a new gateway should consume that reader instead of reconstructing authorization from stores.
- The final design keeps worker start/restart policy behind a narrow worker invoker assembled after orchestration exists: a clean stopped/starting worker may be started or joined, while an unexpected failed/stopping worker is rejected until an owning recovery/reentry path restores it. No failed invocation is retried.
- The execution-scope-before-launcher construction order requires a one-time sealed application capability assembly. Passing a launcher into MCP/native adapters or using a module-global setter would invert ownership.
- Persisted Team binding members cover configured members, while task-created descendants carry a server-minted root Team execution identity. Application-tool authorization must validate that root against the exact binding and producer against the sender rather than persisting every descendant as a new binding member.
- Brief Studio's existing binding-to-brief correlation and brief repositories provide a real application-owned read-only tool. Its existing artifact projection and UI provide the stateful downstream half of Option A without a database, GraphQL, or UI component change.
- Automatic native tool provisioning is owned explicitly by `resolveAutoByteusRuntimeAgentToolExposure`: `run_bash`, `read_file`, `edit_file`, and `write_file` precede configured names for every non-compactor native Agent.
- Automatic Team collaboration provisioning is runtime-neutral: member Team context adds `get_handoff_rules`, `send_message_to`, and `delegate_task` independently of persisted `toolNames`.
- The final AutoByteus `AgentConfig` receives the already composed tool instances; neither `AgentConfig` nor `AgentFactory` adds another hidden tool set. Application MCP integration must therefore be additive at the server-owned composition boundary and must not modify the two automatic resolvers' current meaning.
- Generic `BaseTool.prepareExecution` intentionally coerces string integers/numbers/booleans, converts an empty string to an empty array, and recursively coerces supported nested values before `_execute`. The MCP application route reaches the common gateway with raw JSON instead. Application runtime parity therefore requires `ApplicationAgentTool` to override only its own preparation, preserve the raw argument object, and let the common gateway's strict Ajv validator decide schema validity.
- The specialization does not require a global base-class hook: the public method is polymorphic, the generic coercion/validation helpers are private, and the native tool phase plus inherited `execute` both call the override. The adapter must reproduce only the ordinary `agentId`, pre-start abort, and result-mode preparation contract; its checked `ParameterSchema` remains an advertised definition rather than an application-call validator.
- Static adapter registration and configured-MCP precedence are distinct from application declaration validity. `configuredMcpCollisionPolicy: "prefer_configured_mcp"` means a host-configured MCP source may replace that static route; it does not make the platform/static name available for an application package to own. Every name in the MCP catalog's static adapter index is reserved from application declarations even when the adapter is currently unavailable.
- Application readiness is the primary fail-closed boundary for that complete reserved set. MCP session composition repeats the all-static check as defense in depth before selecting an application route. Application precedence then applies only when there is no registered static adapter and a same-name configured MCP source exists. Native construction relies on successful application readiness and must not import MCP catalog internals or reimplement the name policy.
- The current Brief Studio prompt hierarchy is insufficient evidence of Agent behavior: both `agent-config.json` files select the tool and `team.md` states the first-call intent, but the individual role prompts' explicit numbered sequences start with another tool. Updating the role-local prompts is necessary so the closest maintained instruction and its ordered sequence agree.
- The `get_brief_context` result already contains a stable `briefId`, title, and status. Requiring an exact `Brief context:` artifact line gives a deterministic and browser-visible witness that the Agent used—not merely received—the result.
- Existing external-runtime memory is the appropriate result-bearing trace: it pairs `tool_call` and `tool_result` by `toolCallId` under the member run. The Team binding supplies application/binding/member identity; the artifact event and `brief_bindings`/projection rows supply the binding-to-brief/publication join. Adding generic argument/result text to process logs would duplicate sensitive data and violate the existing redaction posture.
- Final writer publication already changes the same brief to `in_review`, stores one writer/final artifact, emits `brief.ready_for_review`, and triggers UI refresh. A standalone read-only tool call changes none of those rows; this causality distinguishes Option A from a direct-mutation design.
- Configured ordinary registry names are not a Codex exposure mechanism. `CodexThreadBootstrapper` has no dynamic registrations, its MCP descriptor includes only catalog-resolved MCP/static/application routes, and non-MCP registry definitions are excluded. Changing only `toolNames` cannot make `read_file`/`write_file` real provider capabilities.
- Provider-native operations have distinct model, protocol, and normalized observability surfaces, but none is an application acceptance boundary. API-REV-004 proved a patch-created artifact and API-REV-005 proved a shell-created artifact through the same healthy business path. The user's corrections mean no runtime operation name belongs in maintained application `agent.md`; protocol/normalized names are optional diagnostics only and neither their presence nor absence decides whether a correct authorized workspace artifact passes.
- `publish_artifacts` accepts a workspace-relative path and resolves it using the issued session's working directory. The sample therefore has no need for an absolute path returned by a separate write tool.
- The research checkpoint is intentionally small enough to travel in the existing Team handoff. Sending the complete body, exact marker, and canonical path removes the writer's provider-facing file-read dependency while keeping the research artifact independently published and UI-visible. Requiring one complete non-marker `Key findings` bullet verbatim in final `Key evidence` makes handoff-body use deterministic without inventing a token or new stored field.
- Latest-base run-session ID is deterministic routing only. Application authorization still comes from the fresh live record's server-minted application/binding/producer identity plus current ownership checks; no application context, declaration fingerprint, capability reference, or bearer is persisted.
- `ApplicationAgentToolCapability` is one live application-execution-scope reference that can resolve routes for many Studio applications using exact input context. It belongs in the session base execution capabilities and AutoByteus factory input, not inside route data, the global MCP host, or persisted state.
- Package transition and run-session deactivation serve different subjects. Application reentry closes/drains one application call lane and may replace its worker/catalog while an Agent continues to use platform/Team routes through the same active run session. Deactivating the session during reentry would break the latest-base cached-route/restore contract; activating a session must likewise never restart an application worker. The gateway's availability/catalog/ownership checks are the join point.
- On exact run stop, an already admitted application call may settle through the existing completion-coupled request, while deletion of the active session blocks new HTTP resolution; no new timeout or detached continuation is introduced. Per-application reload waits on its own admitted-call counter before worker stop. These are complementary, not duplicate, drains.

## Persisted Data Transition Evidence (When Applicable)

- Current stored subject, location, representative shape, and approximate volume: maintained/generated `application.json` and backend definition/bundle artifacts; per-application `app.sqlite`; platform `platform.sqlite`; file-owned Agent/Team definitions; global MCP configuration. Two maintained application sources were inspected.
- Relevant code-model, serialization, semantic, or physical-store change: add package tool declarations and backend handler contract only; no binding, journal, override, application database, Agent/Team definition, or global MCP store field changes.
- Normal readers and writers, including unknown/extra-field behavior: application manifest/backend definition readers are strict and reject unknown fields/versions; devkit regenerates package output from source; database readers do not read package tool fields.
- Representative direct-read or compatibility evidence: at bootstrap both maintained apps were on manifest v4/backend definition v6 and contained no tool declarations. IR-001 moved maintained source to v5/v7 and rebuilt it during implementation checks. Their durable databases and platform binding/journal data remain separate from package schema.
- Required semantics and invariants preserved by direct use: `Yes` for durable application/platform/global MCP data; `No` for generated package artifacts once the current package/definition contract changes, so those artifacts are rebuilt rather than migrated.
- Physical storage, privacy/security, disposal, rebuild, or operational constraints: global MCP credentials/configuration must remain process-owned; application package must not contain host secrets.
- Concrete benefit, cost, and risk of migration if it remains a candidate: no migration should be introduced without evidence.
- Existing migration framework or lifecycle constraints: application package contract versioning is the correct transition mechanism; app.sqlite migration support exists but is not relevant because no database shape changes.

## Constraints / Dependencies / Compatibility Facts

- Preserve latest-base one dedicated loopback Agent Tools listener, deterministic tokenless run route, headerless provider descriptor, local gate, and exact-run activation/deactivation.
- Preserve current global MCP server configuration and common platform tools.
- Preserve ApplicationExecutionScope and GeneralProcessRunSupervisor separation.
- Preserve the exact automatic native foundation, automatic Team collaboration, configured-tool additivity/deduplication, Memory Compactor exclusion, and in-process materialization rules in BEH-007.
- Avoid a global application-tool registry, applicationId/runId execution-manager router, service locator, or compatibility dual path.
- Do not package secrets or execute application install/build scripts at import.
- Do not expand Codex dynamic tools or the Agent Tools MCP static-adapter set with generic file operations for the maintained proof. Do not change the maintained provider/model or configure foundation operations through application `toolNames`; keep application prompts business-focused.
- Do not restore random/bearer issuance, main-listener Agent Tools routes, global session-service defaults, partial-owner revocation, persisted live route/capability state, or application-triggered whole-session deactivation.

## Open Unknowns / Risks

- Resolved product decision: application-owned business tools are explicitly selected, application-provided MCP capabilities whose identity and availability remain invariant across supported runtime selection; exact application session/execution identity prevents cross-application visibility. The automatic native foundation and automatic Team collaboration rules remain unchanged and additive.
- Approved lifecycle basis: calls arriving after a worker crash fail closed until the normal owning lifecycle restores the worker; the tool boundary does not lazily restart or retry the call.
- Design decision: manifest version `5`, backend definition version `7`, canonical `agentTools` declarations and exact `agentToolHandlers` map; no compatibility reader.
- Revised design decision (`ARCH-DI-001`, `SR-004`): a portable self-contained object JSON-schema subset remains limited to exact current native mapper round-trip semantics. A checked native projector supplies the advertised `ParameterSchema`, while `ApplicationAgentTool.prepareExecution` bypasses generic coercion/`ParameterSchema` invocation validation and forwards the same raw JSON object to the common capability/gateway. It preserves ordinary agent-ID setup, pre-start cancellation, and result-mode resolution/normalization locally. `McpSchemaMapper`, `ParameterSchema`, `BaseTool`, fundamental tools, and configured tools remain unchanged.
- Revised design decision (`CR-DI-001`, `SR-005`): the complete set of names registered by `AgentToolMcpCatalog` static adapters is the platform/static namespace forbidden to application declarations. The per-adapter `configuredMcpCollisionPolicy` remains solely the configured-MCP-versus-static rule. `AgentToolsMcpHost` exposes a frozen `staticAdapterToolNames` snapshot to application readiness; MCP application route composition rejects any registered static adapter regardless of policy or current availability, then allows application-over-configured precedence only when no static adapter exists.
- Design decision: one canonical MCP-safe result union and one 1 MiB serialized application-tool payload limit, enforced before host-to-worker request and before worker-to-host result.
- Design decision: Brief Studio demonstrates `get_brief_context`, deriving durable business state from immutable caller `bindingId`.
- Approved post-API design decision (`SR-006`): Brief Studio uses read-only Option A. Researcher/writer `agent.md` files and launch input must agree on first-call/once-only/result-use behavior; the exact tool-derived marker is published through the existing workflow; existing access-controlled run trace plus binding/projection records prove identity; and the existing browser UI proves `in_review`, binding, and final artifact. No gateway payload logger, write-capable tool, database migration, GraphQL operation, or UI component is added.
- Historical maintained-workflow decision (`CR-DI-002`, `SR-007`): preserve the shipped `codex_app_server` / `gpt-5.6-luna` runtime and read-only Option A; remove `read_file`/`write_file` from role configs; publish canonical relative paths; pass the complete research body. SR-007 also prohibited shell; that restriction was solution-authored and is explicitly superseded by SR-010. API-REV-003 proved SR-007's use of normalized `edit_file` as the model-facing name was insufficient.
- Revised prompt/proof-boundary decision (user clarifications, `SR-009`/`SR-010`): role/team/launch prompts describe business calls, content, canonical artifacts, publication, and handoff only. They name no foundation/provider operation. Runtime provisioning and LLM reasoning choose any already-authorized operation, including shell; `toolNames` still contains no foundation operation. Provider/normalized operation events may remain diagnostic, but shell use alone is valid and cannot fail the proof. Fabricated, missing, out-of-workspace, unauthorized, unpublished, or causally unjoined output remains invalid.
- Latest-base composition decision (`DR-004`, `SR-009`): use finalized `AgentToolMcpRunSessionActivator`/`Deactivator`, deterministic run-session ID, active-only registry, dedicated local listener, and managed exact-run cleanup. Add `applicationAgentTools: ApplicationAgentToolCapability | null` to live session base/execution capabilities; resolve application routes during activation; pass the same optional capability to the AutoByteus factory. Package call lifecycle never imports/calls the run-session deactivator.
- Revised design decision (`ARCH-DI-002`): `ApplicationCatalogTransitionService` replaces the separate refresh/reentry live mutation paths, while revised `ApplicationReentryService` retains only participant preparation/recovery/quarantine. Settings/GraphQL package commands enter the transition before registry/root/source mutation; it derives old participants from the live package slice, invokes reentry lifecycle, stages only the target slice, commits prepared bundle/tool state synchronously, then reconciles/recovers or restores and re-stages rollback state. Unrelated application lanes and catalog slices stay untouched.
- Residual implementation risk: rollback source restoration can itself be unreadable or incomplete. Only the affected participants may remain closed/quarantined; the transition lock must always release and the old in-memory snapshot must never be reopened without staging the actual restored source.
- Residual validation risk: API-REV-005 already proves the current latest-base architecture, implementation, business prompts, exact application call/identity, correct artifacts/handoff/publications, authorized shell-created workspace files, same-brief UI outcome, deterministic restore, application-lane/session orthogonality, and exact cleanup. The required correction is to the proof oracle and durable expectations, not production source. Architecture review should confirm no architecture change; implementation review should preserve source unchanged unless stale test text is owned there; API/E2E should reclassify/rerun the same journey under corrected AC-039 and return the three pending durable test edits through proportional code review. Remaining risk is only accidental retention of the invalid zero-shell assertion or weakening the real artifact/workspace/publication/identity/UI checks while removing it.

## Notes For Architecture Reviewer

Review SR-010 as a narrow requirements/proof correction triggered by API-REV-005/CRR-012 and explicit user direction. Confirm that no production architecture, runtime capability provisioning, prompt choreography, route/session lifecycle, application gateway, worker, publication, or UI owner changes. The model may use any already-authorized foundation capability, including shell; operation telemetry is diagnostic only. Preserve the passing SR-009/ARCH-REV-009 application MCP and tokenless-session design. Verification must still require the real in-workspace artifacts, exact marker/content/path, relative publication, complete handoff/result-use witness, exact application/binding/producer joins, read-only causality, and same-brief browser outcome; it must reject fabricated, missing, outside-workspace, unauthorized, unpublished, or unjoined output—not shell use.
