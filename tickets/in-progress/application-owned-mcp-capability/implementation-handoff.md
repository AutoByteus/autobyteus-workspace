# Implementation Handoff

## Upstream Artifact Package

- Requirements doc: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/requirements.md`
- Investigation notes: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/investigation-notes.md`
- Design spec: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/design-spec.md`
- Supplemental task artifacts: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/application-owned-mcp-intended-behavior.md`
- Solution revision record: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/solution-revision-record.md`
- Design review report: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/design-review-report.md`
- Architecture review revision record: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/architecture-review-revision-record.md`
- Source review report and revision history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-report.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-revision-record.md`
- API/E2E coverage investigation: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-coverage-investigation.md`
- API/E2E execution report and revision history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-execution-coverage-report.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-revision-record.md`
- API/E2E test review: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-test-review-report.md`
- Retained `API-REV-002` production-failure evidence directory: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-evidence/api-rev-002/`. Files: `identity-trace-artifact-join.json`; `runtime-tool-exposure-analysis.md`; `researcher-raw-trace.jsonl`; `writer-raw-trace.jsonl`; `team-run-execution-tree.json`; `team-communication-messages.json`; `db-state-transitions.jsonl`; `before-launch-db.json`; `final-db.json`; `researcher-published-artifacts.json`; `browser-observation.json`; `before-launch-browser.png`; `failure-browser-blocked.png`; `focused-repository-tests.log`; `codex-tool-exposure-test.log`; `documented-dev-start.log`; `backend-relevant.log`; `frontend.log`; `same-origin-proxy.log`. Every file is rooted under the absolute directory immediately above.
- Retained `API-REV-003` production-failure evidence directory: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-evidence/api-rev-003/`. Files: `identity-trace-artifact-join.json`; `runtime-native-edit-failure-analysis.md`; `journey-metadata.json`; both run-specific `*-execution-tree.json`, `*-communication-messages.json`, `*-researcher-raw-trace.jsonl`, and `*-writer-raw-trace.jsonl` files; `before-launch-db.json`; `db-state-transitions.jsonl`; `final-db.json`; `before-launch-browser.png`; `first-attempt-no-publication-browser.png`; `first-attempt-browser.txt`; `final-browser-no-publications.png`; `final-browser.txt`; `focused-repository-tests.log`; `publication-owner-tests.log`; `package-build-validation.log`; `runtime-files.txt`; `backend.log`; `frontend.log`; `same-origin-proxy.log`; `db-observer.py`; `same-origin-proxy.mjs`. Every file is rooted under the absolute directory immediately above.
- Triggering post-API Requirement Gap: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-agent-ui-proof-gap.md`
- Delivery pause and evidence history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/delivery-revision-record.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/delivery-integration-evidence.log`
- Paused delivery artifacts: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/docs-sync-report.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/handoff-summary.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/release-deployment-report.md`; `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/release-notes.md`

## Current Implementation Summary

The implementation adds the strict application-owned agent-tool contract and carries it from import-safe package declarations through application-scoped MCP/native projection, one authorization/validation gateway, and exact application-worker dispatch. It also replaces the prior destructive refresh/reentry split with the reviewed serialized, staged target-slice catalog transition and integrates application-tool admission/drain into reload and shutdown. IR-002 reserves every catalog-registered static adapter name from application declarations and routes while preserving configured-MCP precedence. IR-003 adds the binding-derived, read-only Brief context marker and role-local first-call/result-use contract. IR-004 removes unreachable ordinary file routes and establishes relative publication plus the complete Team handoff. IR-005 corrects the remaining model/provider vocabulary mismatch proven by `API-REV-003`: role/Team/launch text tells Luna to use built-in `apply_patch`; roles depend only on the provider-reported patch result and never inspect protocol/normalized traces; Codex `fileChange` and AutoByteus normalized `edit_file` remain independent verification evidence owned by unchanged provider/platform code.

- Implementation cycle: `Rework`
- Implementation revision record: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/implementation-revision-record.md`
- Current implementation revision ID: `IR-005`
- Related solution revision IDs: `SR-001`–`SR-008`
- Related architecture-review revision IDs: `ARCH-REV-004`–`ARCH-REV-008`
- Related code-review revision IDs: `CRR-001`–`CRR-007`; `CRR-006` passed IR-004 source, while `CRR-007` reopened `CR-DI-002` after the `API-REV-003` exact-Luna failure before `SR-008` / `ARCH-REV-008`
- Related API/E2E revision IDs: `API-REV-001`, `API-REV-002`, `API-REV-003`
- Related delivery revision IDs: `DR-001`, `DR-002`; `DR-002` is the current pause authority
- Triggering finding IDs: reopened `CR-DI-002`; material premise `CR-MP-002` (`Reachable`). `SR-008` / `ARCH-REV-008` resolve the design-level model-instruction/provider-event/normalized-evidence mismatch; `API-REV-003` remains valid failure evidence rather than runtime proof of IR-005.

## Reviewed Behavior Implementation Trace

| Behavior ID | Approved Change / Preserved Outcome | Implemented Production Path / Key Files | Result / Notes |
| --- | --- | --- | --- |
| BEH-001 | Add import-safe static declarations and exact worker handler matching. | `autobyteus-application-sdk-contracts/src/application-agent-tools.ts` -> manifest parser/provider/bundle model -> `ApplicationAgentToolCatalog`; backend v7 types -> `application-backend-definition-loader.ts`. | Implemented. Declaration names/schema are normalized and fail closed; worker load requires the declared/implemented handler-name sets to match exactly. |
| BEH-002 | Make application-local declarations part of readiness and selected runtime exposure without global registration. | `agent-tool-mcp-catalog.ts` / `agent-tools-mcp-host.ts` complete static-name snapshot -> `application-definition-runtime-readiness.ts`; `application-runtime-definition-validator.ts`; sealed capability assembly in `build-application-platform-runtime.ts`; application execution-scope injection. | Implemented. Every name in the static adapter registration index is forbidden to every application declaration regardless of availability, selection, or configured-MCP policy; only the immutable names snapshot crosses into readiness. Invalid definitions quarantine/fail readiness, and the capability is absent from the general-process scope. |
| BEH-003 | Project the same selected application routes to shared MCP for Claude/Codex and checked bound tools for AutoByteus. | `agent-tool-mcp-catalog.ts`, `application-agent-tool-mcp-adapter.ts`, `application-agent-tool-composer.ts`, `application-agent-tool-native-schema-projector.ts`, `application-agent-tool.ts`. | Implemented. A defensive route collision with any registered static adapter fails. A non-static application route still outranks configured MCP, while the no-application branch keeps configured-protected, active-static, and `prefer_configured_mcp` precedence distinct (including configured `open_tab` winning over browser static). Native schemas must round-trip exactly through the unchanged mapper/model. |
| BEH-004 | Authorize and validate one raw call, dispatch once to the exact worker, and return a bounded safe result. | `application-agent-tool-gateway.ts` -> `application-run-ownership-service.ts` -> Ajv `application-agent-tool-payload-validator.ts` -> worker invoker/controller/protocol/host/entry. | Implemented. AutoByteus preparation keeps the original argument object and does not call `super.prepareExecution`; configured members and live server-minted Team descendants use the existing ownership/root-run authorities. Input and result envelopes are strict JSON and capped at 1 MiB. |
| BEH-005 | Coordinate reload/removal/crash/shutdown with application-tool admission and one staged catalog owner. | `application-agent-tool-call-lifecycle.ts`, `application-catalog-transition-service.ts`, revised participant-only `application-reentry-service.ts`, staged `application-bundle-service.ts` and tool-catalog deltas, package command integration, platform lifecycle. | Implemented. Old target participants drain and stop before mutation; paired prepared bundle/tool assignments commit without an `await`; actual rollback state is re-staged; added apps remain lazily startable; exact repair reentry can recover a quarantined app; removed/invalid apps stay closed/quarantined; unrelated slices and lanes remain untouched. |
| BEH-006 | Perform the clean v5/v7 transition and regenerate/validate maintained packages. | Contract constants/readers, devkit validator/template, Brief Studio and Socratic Math Teacher source. | Implemented. No v4/v6 production fallback was retained. Maintained packages built and validated from current source; generated output was removed from the worktree afterward because it is reproducible and untracked. |
| BEH-007 | Preserve automatic/configured tools and generic native preparation. | Application-aware composition is confined to the application execution scope and filters only selected application route names before the existing resolver. | Implemented. `BaseTool`, `McpSchemaMapper`, `ParameterSchema`, automatic exposure builders, configured-tool materialization, and general-process capability remain unchanged. |
| BEH-008 | Make actual maintained Brief roles call and use the read-only tool first, request file changes through Luna's actual built-in patch surface, exchange bounded research through the existing Team message, and let only normal publication/reconciliation create UI-visible business state. Keep model instruction, provider protocol event, and platform observability distinct. | Researcher/writer Codex/Luna config and role-local `agent.md` -> binding-derived `get-brief-context.ts` marker -> model-facing Luna built-in `apply_patch` -> Codex `item/fileChange` / `file_change` -> AutoByteus normalized `edit_file` evidence -> member-workspace-relative `publish_artifacts` -> complete researcher `/writer` Team message -> writer no-read built-in patch/final publication -> unchanged `BriefArtifactReconciliationService` / notification / GraphQL / UI. `team.md` and `brief-run-launch-service.ts` reinforce but do not own or route the sequence. | Implemented for source review. Both configs expose only `get_brief_context`, `publish_artifacts`, and `send_message_to`; neither lists `read_file`, `write_file`, `apply_patch`, `edit_file`, or shell. Each role calls context exactly once first at its role point, tells Luna to use built-in `apply_patch`, continues only from the provider-reported patch result, and does not inspect protocol/normalized traces. Researcher relatively publishes `brief-studio/research.md` and hands off exact marker/path/full 200–500-word body. Writer validates `briefId`, performs no file read/cross-workspace access, copies a complete non-marker `Key findings` bullet verbatim under `Key evidence`, then relatively publishes `brief-studio/final-brief.md`. Failures stop normal publication without shell/fabrication. The IR-003 handler and provider parser/converter/publication/reconciliation owners are unchanged. Real `fileChange`/normalized-event/browser proof remains downstream. |

## Key Files Or Areas

- Canonical contract and SDK exports: `autobyteus-application-sdk-contracts/src/application-agent-tools.ts`, `src/manifests.ts`, `src/index.ts`, and `autobyteus-application-backend-sdk/src/index.ts`.
- Application catalog/gateway: `autobyteus-server-ts/src/application-agent-tools/{domain,services}/`.
- Native projection: `autobyteus-server-ts/src/agent-execution/backends/autobyteus/application-agent-tools/` plus the provider factory boundary.
- MCP projection: `autobyteus-server-ts/src/agent-tools/mcp/`.
- Reused Codex observability boundary: `autobyteus-server-ts/src/agent-execution/backends/codex/events/codex-item-event-payload-parser.ts` and `codex-item-event-converter.ts` continue to map provider `fileChange` activity to normalized `edit_file`; IR-005 does not modify them or expose either name to role configs.
- Worker contract and dispatch: `autobyteus-server-ts/src/application-engine/{runtime,services,worker}/`.
- Ownership: `autobyteus-server-ts/src/application-orchestration/services/application-run-ownership-service.ts` and the application execution-scope Team identity adapter.
- Catalog lifecycle: `application-catalog-transition-service.ts`, `application-catalog-transition-plan.ts`, revised reentry/package command/bundle services, readiness, and platform lifecycle.
- Clean contract sources: application devkit validator/template and the two maintained applications; Brief Studio's sample handler is `applications/brief-studio/backend-src/agent-tools/get-brief-context.ts`.
- Maintained demonstration contract: `applications/brief-studio/agent-teams/brief-studio-team/agents/{researcher,writer}/{agent-config.json,agent.md}`, supporting `team.md`, and `applications/brief-studio/backend-src/services/brief-run-launch-service.ts`.
- Dependency: direct Ajv 8 declaration in `autobyteus-server-ts/package.json` and resolved lockfile importer entry.

## Important Assumptions

- Package `dist/` directories are reproducible outputs and are not repository source; they were used for build/package validation and then removed from the worktree.
- The reviewed strict clean cut is authoritative: v4/v6 package artifacts must be rebuilt rather than read through fallback branches.
- Ajv remains configured without coercion, defaults, or removal, so the common gateway validates but does not transform the provider/native argument object.
- Existing application/platform databases, Agent/Team definitions, and configured MCP state do not encode the new declaration/handler fields and therefore require no migration.

## Known Risks

- IR-005 has not yet received renewed source review. It must return through `/code_reviewer` before the renewed API/E2E cycle begins.
- `API-REV-001` remains a valid 97.2%-confidence pass for the earlier platform/direct-MCP scope. `API-REV-002` remains valid evidence for the superseded ordinary-file-tool workflow. `API-REV-003` remains a valid 88.6%-confidence failure for the pre-IR-005 exact Codex/Luna model-facing wording: it proves that normalized `edit_file` was not a viable Luna instruction, not that IR-005 succeeds at runtime.
- Renewed API/E2E must rerun the same real configured Codex/Luna supported-browser flow and join the maintained `apply_patch` instruction to successful Codex `fileChange`, corresponding AutoByteus normalized `edit_file`, exact application/binding/member/workspace/artifact/brief identities, relative publications, complete handoff/verbatim research use, no business mutation before publication, final reconciliation, and the browser outcome. It must also prove zero `run_bash`/`read_file`/`write_file`. Provider/built-in-patch/routed-tool unavailability or model noncompliance is a blocked/failed evidence result, not permission to substitute a mock, runtime switch, shell fallback, or direct MCP call.
- The optional live Codex backend integration is stale/inadequate Luna proof until its prompt, required `agentRunId` input, and exact model execution are corrected. API/E2E owns its coverage-validity decision and any durable repair/execution after source review; even corrected, it cannot replace the mandatory supported-browser journey. Any durable edit must return through proportional `/code_reviewer` review before delivery.
- Project README/module documentation still describes some v4/v6 contracts. This is a delivery documentation impact, not a retained runtime compatibility path.
- The managed-install removal finalizer intentionally runs only after the empty target slice is committed. A filesystem deletion failure reports failure while retaining the already-safe removed catalog state and undeleted managed files for operator cleanup.
- Delivery remains paused at `DR-002`; the prior provisional delivery/docs result is not current authorization.

## Task Design Health Assessment Implementation Check

- Reviewed change posture: `Feature / Larger Requirement`
- Reviewed root-cause classification: `Boundary Or Ownership Issue`
- Reviewed refactor decision (`Refactor Needed Now`/`No Refactor Needed`/`Deferred`): `Refactor Needed Now`
- Implementation matched the reviewed assessment (`Yes`/`No`): `Yes`
- If challenged, routed as `Design Impact` (`Yes`/`No`/`N/A`): `Yes`
- Evidence / notes: The cumulative platform assessment remains valid. SR-006's missing-invariant correction and SR-007's removal of unreachable registry file routes remain intact, but `API-REV-003` / `CRR-007` proved that IR-004 still crossed the provider boundary by presenting AutoByteus's normalized `edit_file` evidence label as Luna's operation name. IR-005 follows `SR-008` / `ARCH-REV-008`: role text owns the built-in `apply_patch` instruction, Codex continues to own `fileChange`, and existing AutoByteus conversion continues to own normalized `edit_file` evidence. It does not modify provider composition/parser/converter, add adapters/routes, expand the application-tool capability, introduce trace inspection or shell fallback, or move publication/reconciliation/UI ownership.

## Legacy / Compatibility Removal Check

- Backward-compatibility mechanisms introduced: `None`
- Legacy old-behavior retained in scope: `No`
- Dead/obsolete code, obsolete files, unused helpers/tests/flags/adapters, and dormant replaced paths removed in scope: `Yes` — `ApplicationCatalogRefreshCoordinator`, `ApplicationBundleService.refresh/reloadApplication`, and the old reentry catalog-mutation method are removed from production.
- Ambiguous prior static-name readers/properties removed without compatibility aliases: `Yes` — `listSupportedToolNames()`, public `listProtectedStaticToolNames()`, and host `protectedStaticToolNames` are absent; `listStaticAdapterToolNames()` and immutable `staticAdapterToolNames` are the sole public names.
- Contradictory or unreachable maintained file instructions retained: `No` — role configs contain none of `read_file`, `write_file`, `apply_patch`, or `edit_file`; model-facing role/Team/launch text names only Luna built-in `apply_patch`, contains no `edit_file`, prohibits `run_bash` and absolute-path assumptions, and forbids role inspection of provider/internal traces. The researcher hands off the full body and the writer requires no read. Shared Team/launch text is reinforcement only.
- Shared structures remain tight (no one-for-all base or overlapping parallel shapes introduced): `Yes`
- Canonical shared design guidance was reapplied during implementation, and file-level design weaknesses were routed upstream when needed: `Yes`
- Changed source implementation files stayed within proactive size-pressure guardrails (`>500` avoided; `>220` assessed/acted on): `Yes`
- Notes: No changed source implementation file exceeds 500 effective non-empty lines. The IR-003 handler remains 44 effective lines; the launch service remains 228 effective lines with only supporting initial-input text replaced for IR-005, so no responsibility split is warranted. Prior IR-002 size assessments remain valid.

## Persisted Data Transition Check (When Applicable)

- Approved decision (`Not Affected`/`Directly Usable — No Migration`/`Discard or Rebuild`/`Migration Required`): `Discard or Rebuild` for generated/importable package artifacts; `Directly Usable — No Migration` for application/platform databases, Agent/Team definition storage, and global MCP configuration.
- Design-spec decision reference: `design-spec.md` -> `Persisted Data / State Transition Decision`.
- Implementation follows the approved decision without an unapproved migration or version-specific runtime fallback: `Yes`
- Direct-use evidence or discard/rebuild result, when applicable: no database/store model or migration changed. IR-003 reads the same Brief rows and its focused test confirms the application call leaves Brief, binding, and artifact rows unchanged. IR-005 changes only maintained prompt/Team/launch wording and focused coverage; built-in patch output still feeds the existing relative publication/reconciliation spine without a persisted schema change. Brief Studio was rebuilt and validated under v5/v7, then reproducible generated output was cleaned.
- Migration implementation and focused checks, only when `Migration Required`: `N/A`
- Deviation from the reviewed transition decision: `None`

## Environment Or Dependency Notes

- Environment used: Node `v22.23.1`, pnpm `10.28.2` through Corepack.
- Established the workspace dependency environment with `corepack pnpm install`; a later `pnpm install --force` relinked the built devkit CLI before package validation.
- Added Ajv as a direct server dependency (`^8.17.1`, resolved `8.18.0`).
- Server prebuild generated Prisma Client and built the shared workspace dependencies before TypeScript compilation.
- Generated application/SDK `dist/` directories were intentionally cleaned after all implementation checks.
- For IR-005, the declared SDK/backend/frontend/devkit workspace dependencies were built before the Brief package; package build, source-plus-package integration, backend typecheck, and validation all passed without a source or environment failure. Generated outputs were then cleaned.

## Local Implementation Checks Run

- IR-005 focused coverage: `corepack pnpm --filter autobyteus-server-ts test --run tests/unit/application-backend/brief-agent-tool.test.ts tests/unit/application-backend/brief-studio-agent-prompt-contract.test.ts` — passed (2 files, 8 tests). It covers the unchanged exact binding-derived/read-only marker and requires the fixed Codex/Luna configs, Luna built-in `apply_patch` model wording, provider-reported patch-result behavior, no role trace inspection, no model-facing `edit_file`, no shell, relative publication, complete handoff, writer no-read/verbatim witness, and fail-closed Team/launch reinforcement.
- IR-005 package coverage: `@autobyteus/application-sdk-contracts`, backend SDK, frontend SDK, and devkit builds passed; Brief Studio `build`, `typecheck:backend`, and `validate` passed; `corepack pnpm --filter autobyteus-server-ts test --run tests/integration/application-backend/brief-studio-team-config.integration.test.ts` passed (1 file, 4 tests) against source and generated package. Generated outputs were removed afterward.
- IR-005 relative-publication owner coverage: `corepack pnpm --filter autobyteus-server-ts test --run tests/unit/services/published-artifacts/published-artifact-path-identity.test.ts tests/unit/services/published-artifacts/published-artifact-publication-service.test.ts` — passed (2 files, 24 tests).
- IR-005 source/invariant checks: `git diff --check` passed; configs remain exactly `get_brief_context`, `publish_artifacts`, `send_message_to` with `codex_app_server` / `gpt-5.6-luna`; maintained model-facing text contains built-in `apply_patch` but no `edit_file` or `provider-native` wording; Codex parser/converter/provider and Agent Tools MCP production have no diff; no Brief migration/repository/reconciliation/frontend/GraphQL area changed; generated outputs are absent; the read-only handler is 44 effective lines and launch service 228.
- The optional live Codex backend integration was neither changed nor executed in IR-005. `SR-008` records its current prompt, required-factory-input, and exact-model evidence limitations and assigns the coverage decision/repair/execution to API/E2E; no live Luna success is claimed by implementation.
- IR-004 focused coverage: `corepack pnpm --filter autobyteus-server-ts test --run tests/unit/application-backend/brief-agent-tool.test.ts tests/unit/application-backend/brief-studio-agent-prompt-contract.test.ts` — passed (2 files, 8 tests). It covers the unchanged exact binding-derived/read-only marker and the corrected Codex/Luna config plus researcher/writer context-first, native-edit, no-shell, relative-publication, complete-handoff, no-read, verbatim-witness, fail-closed, and supporting Team/launch contracts.
- IR-004 package coverage: after building `@autobyteus/application-sdk-contracts`, backend SDK, frontend SDK, and devkit outputs, Brief Studio `build` passed; `corepack pnpm --filter autobyteus-server-ts test --run tests/integration/application-backend/brief-studio-team-config.integration.test.ts` passed (1 file, 4 tests) against both source and generated package; Brief Studio `typecheck:backend` and `validate` passed. Generated outputs were removed afterward.
- Existing relative-publication owner coverage: `corepack pnpm --filter autobyteus-server-ts test --run tests/integration/agent-execution/codex-agent-run-backend-factory.integration.test.ts tests/unit/services/published-artifacts/published-artifact-path-identity.test.ts tests/unit/services/published-artifacts/published-artifact-publication-service.test.ts` passed the two publication suites (2 files, 24 tests). The live Codex backend suite was skipped (10 tests) because `RUN_CODEX_E2E` was unset; real provider execution is intentionally not claimed as implementation evidence and remains assigned to downstream API/E2E.
- IR-004 source/invariant checks: `git diff --check` passed; both role configs exactly equal `get_brief_context`, `publish_artifacts`, `send_message_to` and retain `codex_app_server` / `gpt-5.6-luna`; Codex backend/provider and Agent Tools MCP production areas have no diff; no Brief migration/repository/reconciliation/frontend/GraphQL area changed; no generated application/SDK output remains; the read-only handler is 44 effective lines and launch service 228; whitespace and source-size checks passed.
- IR-003 prior focused coverage remains valid for the read-only handler: the same focused unit command passed before IR-004 and was re-run above. The handler itself is unchanged in IR-004.
- IR-002 focused correction coverage: `corepack pnpm --filter autobyteus-server-ts test --run tests/unit/agent-tools/mcp/agent-tool-mcp-catalog.test.ts tests/unit/agent-tools/mcp/agent-tools-mcp-host.test.ts tests/unit/application-platform/application-definition-runtime-readiness.test.ts` — passed (3 files, 16 tests). This covers all default-provider static names, application `open_tab`, protected and inactive registered static collisions, non-static application-over-configured routing, configured browser precedence without an application route, and all-declaration readiness from the names snapshot.
- `corepack pnpm --filter @autobyteus/application-sdk-contracts build` — passed.
- `corepack pnpm --filter @autobyteus/application-backend-sdk build` — passed.
- `corepack pnpm --filter autobyteus-server-ts build` — passed again after the final IR-002 implementation edits; this included shared builds, Prisma generation, server TypeScript compilation, managed-asset copying, and the sanitized built-in-agent bootstrap smoke check.
- `corepack pnpm --filter @autobyteus/application-devkit build` — passed.
- `corepack pnpm --filter @autobyteus-example/brief-studio-authoring typecheck:backend` and the Socratic Math Teacher equivalent — passed.
- Maintained Brief Studio and Socratic Math Teacher `build` followed by `validate` — both generated current packages and reported them valid.
- Narrow Node implementation checks — passed for the accepted/rejected portable-schema matrix and native mapper round trip; raw strict Ajv parity and 1 MiB/JSON-structure guards; native same-object preparation/inherited execution; worker exact handler maps; MCP application-over-configured precedence/protected collisions; application bundle target-slice preservation/prepared commit; application call admission/drain; package participant recovery policy; added-app lazy readiness; exact quarantined-app repair; and configured/dynamic Team ownership mapping.
- Source/invariant checks — `git diff --check` passed; removed names `listSupportedToolNames`, public `listProtectedStaticToolNames`, host `protectedStaticToolNames`, and prior wiring identifiers are absent from source/tests; readiness/native projection has no MCP-provider/adapter/policy import; no changed production source file exceeded 500 effective non-empty lines; no application adapter calls `super.prepareExecution`; no removed refresh coordinator or direct package/orchestration bundle refresh call remains; and `BaseTool`, `McpSchemaMapper`, and `ParameterSchema` have no diff.

These are implementation-scoped local checks only, not API/E2E sign-off.

## Frontend Rendered-Result Check (When Applicable)

Not Applicable to frontend implementation inspection. IR-005 changes maintained Agent/Team/launch wording and focused coverage; no frontend source, visual state, layout, or interaction component changed. The same-brief `in_review` browser outcome depends on the actual configured Codex/Luna Team workflow and existing notification/GraphQL refresh, so the reviewed contract assigns that real browser journey to renewed API/E2E rather than allowing implementation to substitute source inspection, a standalone patch probe, direct MCP, or a mock.

## Downstream Coverage Hints / Suggested Scenarios

- Exercise zero-tool packages plus recursive accepted schema keywords and explicit nullable/length/default/closed-object/composition rejection.
- Run integer/number/boolean string aliases, empty-string arrays, and nested coercible values through real AutoByteus and Claude/Codex MCP adapters; assert the same invalid-input code, unchanged argument meaning, and zero worker calls.
- Verify selected/unselected App A/App B/general-process isolation, same-name declarations, non-static application-over-configured precedence, every registered-static collision quarantine (including unavailable and `prefer_configured_mcp` names), and unchanged configured-browser precedence when no application declares the name.
- Cover standalone Agent, configured Team member, task-created descendant, stale/forged member identity, wrong binding/application, and terminal binding authorization.
- Validate exact missing/extra handler load failures, one-call completion, caller-aware handler context, rich/error results, non-JSON values, input/result 1 MiB bounds, and unexpected worker crash with no invocation-boundary retry.
- Cover unchanged versus changed fingerprints across old sessions.
- Drive real Settings/GraphQL import, reload, removal, rollback restoration, rollback quarantine, and exact reentry while calls are admitted; assert target-only drain/worker stop and unrelated lane/catalog identity preservation.
- Verify import does not eagerly start a worker, a newly imported ready app is normally startable, package reload only recovers previously active apps, and exact repair reentry can recover a previously quarantined app.
- Verify Studio/standalone shutdown ordering and session revocation.
- Exercise Brief Studio `get_brief_context` across AutoByteus/Claude/Codex using durable binding-derived state, including missing correlation, without accepting model-provided routing authority.
- Rerun the distinct corrected `SR-008` journey with the actual shipped Codex/Luna researcher/writer roles: assert exactly one paired application call/result per member and first-tool ordering; maintained model-facing input says built-in `apply_patch`; each member then emits successful Codex `item/fileChange` / `file_change` and corresponding AutoByteus normalized `edit_file` under the same `agentRunId`; and zero `run_bash`, `read_file`, and `write_file` calls occur. Researcher relative path/body must equal its exact member-workspace file, publication, and complete Team handoff. Writer must use that message without a file read and copy at least one complete non-marker research `Key findings` bullet verbatim into final `Key evidence`; each relative publication must resolve within the correct member workspace. Prove each application read alone changes no business row, then join `toolCallId`, member `agentRunId`, application/binding/member identity, provider/normalized events, Team message, workspace file, artifact revision, and brief ID through final reconciliation. Open the supported browser surface and assert the same brief is `in_review`, the launch binding and both artifacts are visible, exactly one `brief-studio/final-brief.md` exists, and its visible body contains the writer marker. Roles must not inspect traces. Do not substitute shell, runtime/model switch, direct MCP, standalone probe, provider mocks, or source inspection.

## API / E2E / Executable Coverage Investigation And Execution Still Required

Required for the SR-008 correction. `API-REV-001` / `CRR-003` remain valid for their earlier platform/direct-MCP scope; `API-REV-002` remains prior ordinary-file-tool failure evidence; and `API-REV-003` remains valid failure evidence for the superseded normalized-label-as-instruction contract. IR-005 performed implementation-owned source/package coverage only and does not claim real configured-provider or browser success. After source review, API/E2E must refresh coverage validity, decide whether to repair/execute the optional live integration, and rerun the mandatory real browser journey. Any repository-resident durable coverage addition, update, or removal must return through proportional `/code_reviewer` review before delivery resumes.
