# API/E2E Coverage Investigation — AGY runtime

**Current authority:** API-REV-007 at IR-008 source `40641dfca` is **Pass / 95%**, subject to proportional changed-test-code review. Historical round notes remain for traceability; the final API-REV-007 section and current execution report control present status.

## Investigation Meta and route
- Round 1, triggered by Code Review CRR-002 Pass at `575520264`; no prior API/E2E result or record exists. Canonical package directory: this file's directory.
- Authorities read: `requirements-doc.md` (SR-021, REQ-001–011, AC-001–010), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md` (DS-001–004, DR-001, direct-use persisted-data decision), `investigation-result.md`, `design-review-report.md` and `architecture-review-revision-record.md` (ARCH-REV-003), `implementation-handoff.md` and `implementation-revision-record.md` (IR-003), `code-review-report.md` and `code-review-revision-record.md` (CRR-002). Supplements: `agy-cli-experiment-report.md`, `agy-tool-event-capture-analysis.md`, raw capture and command matrix, implementation-local live JSON reports. Delivery revisions N/A.
- Classification: **Large / High**, Reviewed route. Successful validation returns to Code Reviewer for proportional test-code review; a failure returns for focused failure-origin review. No direct-route shortcut.
- Round-1 baseline investigation preceded durable test edits and final execution. Current status is in the round-4 section. Test-case ledger: `api-e2e-test-case-ledger.md`.

## Current behavior and changed boundaries
| Behavior | Change | Boundary and required proof |
| --- | --- | --- |
| BEH-001/002 | Added | CLI availability/model, generated custom main-agent identity including team/org context before task; real AGY process. |
| BEH-003/005 | Added | CLI-generated exact conversation ID, separate run binding, fail-closed restore, distinct capsules in same selected real workspace; process + persistence. |
| BEH-004 | Changed | AGY-only draft default-on, effective team/org policy and explicit-off; UI store to API to CLI permission mode; denied step not green/actionable. |
| BEH-006 | Added/changed | AGY NDJSON to canonical events, normalized raw traces, WebSocket and browser live/reload; DONE->success without invented exit, ERROR/denial non-green. |
| SCN-005 | Preserved | AutoByteus, Codex, Claude launch/execution/restore, status/trace policy. |

Affected surfaces: backend domain, GraphQL/WebSocket API, web-equivalent Nuxt renderer, auth/permissions, external AGY CLI, process lifecycle, local persistence and team/org coordination. Electron shell-specific IPC/window/packaging is **not** changed; browser preview is the right UI boundary. No worker/queue change. Existing data are **Directly Usable — No Migration** using generic reader; no released AGY product runs, no compatibility shim. Representative existing-runtime metadata/history plus new AGY create/restore must be exercised. Implementation handoff reports no legacy mechanism and neutral IR-001 trace path removed; retain no tests for that obsolete path.

## Project execution discovery
- Worktree: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924`, branch `codex/antigravity-cli-runtime-redesign-20260924`; base `40b1783f4`, reviewed commit `575520264`. Pre-existing Code Reviewer report edits will not be overwritten.
- `autobyteus-server-ts/AGENTS.md`: single Vitest file via `pnpm -C autobyteus-server-ts exec vitest run <path> --no-watch`; integration path and all-tests commands documented.
- `autobyteus-server-ts/README.md` §§Install, Environment setup, Build and run, Tests: `pnpm dev` starts real server/web at 8000/3000 with isolated `.autobyteus/development/server-data`; deterministic E2E use test-owned Vitest runtime, not development DB; `pnpm test:e2e` broader suite. `autobyteus-web/AGENTS.md` and README §§Development/Testing: browser dev path, `pnpm test:nuxt <path> --run`; Electron only for shell-specific behavior.
- `package.json`, server/web package manifests, existing `tests/e2e/runtime/*team-inter-agent-roundtrip.e2e.test.ts`, `tests/e2e/helpers/studio-runtime-test-server.ts`: TypeScript/GraphQL/Fastify/WebSocket/Vitest, isolated app-data fixture, service starts in test process and is closed by `afterAll`; Live Codex/Claude tests opt in via env. Root `test:e2e:real` is a separate real-provider secret-management runner, not this Team parity test.
- AGY CLI 1.2.10 available. Existing local authentication is an environmental prerequisite, no secret value recorded. Tests will create temporary workspace/appdata/definitions/team runs, terminate/delete and remove only their own resources. Avoid global AGY settings/user data edits. AGY may retain provider-side conversations; record this residual.

## Existing durable coverage inventory and decisions
| Path/scenario | Decision | Reason/action |
| --- | --- | --- |
| `tests/unit/agent-execution/backends/antigravity/agy-stream-event-converter.test.ts` | Still Valid | SR-021 DONE/error/denial fixture conversion; run. |
| `.../agy-run-capsule.test.ts`, `.../agy-production-live.test.ts`, `.../agy-restore-live.test.ts` | Still Valid | Capsule, selected workspace, standalone provider and restore; opt-in live checks needed, unit/mock boundary noted. |
| `.../agy-mcp-team-live.test.ts` | Still Valid, insufficient | Real AGY scoped MCP but **stub delivery**; cannot prove real member roundtrip. Keep and add distinct E2E. |
| `tests/e2e/runtime/{codex,claude}-team-inter-agent-roundtrip.e2e.test.ts` | Needs Update | Behavioral parity remains valid, but copied first-case GraphQL fixture uses removed `TeamMemberInput.refType`; new AGY test must omit it. Existing files are not changed in this round unless chosen for execution. |
| `tests/e2e/runtime/runtime-capability-graphql.e2e.test.ts`, `mixed-team-runtime-graphql.e2e.test.ts`, `skill-access-mode-graphql.e2e.test.ts` | Still Valid/Needs Update if AGY not represented | API scope/contract; inspect and run affected tests. |
| Web draft/store/editor, streaming/hydration/tool-card tests in implementation handoff | Still Valid | Focused UI/state regression; rerun. |
| Obsolete neutral `TOOL_EXECUTION_COMPLETED` local IR-001 tests | Stale/Remove already completed upstream | SR-021 supersedes; no new deletion by API/E2E. |

## Coverage decisions
- **Add durable** `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts`: real GraphQL Team creation, real WebSocket member input, AGY scoped `send_message_to` to a *real* second member, team communication and recipient response with exact run/member attribution, canonical tool lifecycle and persisted projection; opt-in `RUN_AGY_E2E=1` with live binary/auth guard. A stub-only probe is unacceptable.
- **Update** existing API capability/skill test only if its assertions fail valid new behavior; no speculative removal.
- **Temporary executable probes** may supplement live command outcome, denial/reload, exact mismatch, selected workspace, configured skills/collision and Org member context where durable live E2E is unwieldy; explanation/evidence required. No planned durable removal.

## Execution discovery update
- AGY-02 first attempt revealed stale copied `refType` fixture from otherwise-valid Codex/Claude parity tests; current GraphQL `TeamMemberInput` excludes that field. The new test was corrected before rerun.
- AGY-02 rerun reaches production Team creation and fails **before AGY process**: `validateLaunchConfiguration` in `src/run-history/store/run-execution-tree-shared-record-schemas.ts` permits only three prior runtime strings, not `antigravity_cli`. Both Team and Org current tree validators use it. This is a real AC-002 and mandatory parity-gate failure, preliminarily implementation Local Fix. No test-only workaround is acceptable.

## Planned cases (canonical ledger initialized before execution)
| ID | Case | Requirements/AC | Mode |
| --- | --- | --- | --- |
| AGY-01 | Focused converter/capsule/backend/trace + web draft/hydration | AC-001/002/006/008/009/010 | repository Vitest |
| AGY-02 | Real Team member→member scoped MCP roundtrip and trace | AC-002/006/008, user parity gate | new durable GraphQL/WebSocket E2E |
| AGY-03 | Exact create/restore/mismatch and selected workspace/capsules | AC-003/004 | opt-in live suites/probe |
| AGY-04 | Final-code DONE controls, denial with live/reloaded label/raw trace | AC-007/008/010 | live API + browser |
| AGY-05 | Org effective policy/execution and skills/collision | AC-002/009 | repository + live probe |
| AGY-06 | Non-AGY regression and existing-data reader | AC-005 | focused API/web E2E |

Repository command order: targeted AGY unit/integration first, then new Team E2E, web focused suites, broader affected API/E2E and non-AGY suites. Record exact commands/results after each. Broader validation is **Required**, not preempted by mocks: real team delivery, AGY process, browser final-code reload, and restore are critical. Selected modes live GraphQL/WebSocket, CLI-backed server test, browser preview if reachable; no Electron launch. Pre-execution post-repository confidence pending; calculate after repository runs across mandatory seven categories. Ambiguity/reroute: none now; if provider/tool identity or denied replay contradicts AC, classify and return through Code Reviewer. Proceed to execution: **Yes**.

## Repository plan/results and broader decision — round 1 update
| Order | Command / surface | Result | Evidence |
| --- | --- | --- | --- |
| 1 | `pnpm -C autobyteus-server-ts prepare:shared`; focused converter/capsule + new E2E with live opt-in off | 13 pass, E2E skipped | `/tmp/agy-prepare-shared.log`, `/tmp/agy-api-01-server-rerun.log` |
| 2 | Focused web Org editor, tool card, hydration | 42 pass | `/tmp/agy-api-01-web.log` |
| 3 | `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch` | **Fail** after fixture repair; final corrected test rerun same failure | `/tmp/agy-api-02-team-final.log` |
| 4 | Runtime capability/current GraphQL | 2 pass, 21 opt-in live-agent tests skipped | `/tmp/agy-api-06-server.log` |
| 5 | Codex/Claude/manager/trace-focused server units | 73 pass | `/tmp/agy-api-06-server-focused.log` |
| 6 | Non-AGY launch/store/form web suites | 55 pass | `/tmp/agy-api-06-web.log` |

The first test import failed because documented shared packages had not been built; `prepare:shared` fixed the environment. The first new Team E2E call used legacy copied `refType` omitted by current `TeamMemberInput`; the test fixture was corrected. The final AGY-aware test uses AGY `call_mcp_tool`/nested Arguments rather than Codex's direct tool/segment convention, as proven by real AGY scoped-MCP event capture. The continuing failure is production: current shared run-tree validator omits `antigravity_cli`. `tsc -p tsconfig.json --noEmit` cannot give a useful suite verdict because existing `rootDir=src` conflicts with included `tests` (TS6059); the `--rootDir .` attempt exposed thousands of unrelated pre-existing errors. No typecheck pass is claimed.

## Post-repository confidence scorecard
| Category | Score | Evidence and uncertainty / needed validation |
| --- | ---: | --- |
| Requirement/AC proof | 45% | Standalone and UI tests exist, but critical real Team/Org member launch and delivery fail; fix runtime-kind reader then rerun. |
| Changed-boundary directness | 65% | Real GraphQL Team creation directly rejected; backend converter/capsule tests pass; no post-launch Team path. |
| Cross-boundary realism/mock gap | 35% | Stub MCP delivery is not real member roundtrip; new real test cannot reach it. |
| Environment/config/identity/fixture fidelity | 75% | AGY 1.2.10/auth available and isolated GraphQL fixture; copied fixture corrected; live run not yet completed. |
| Failure/edge/lifecycle/recovery | 50% | Fixture converter and standalone restore exist; no independent live reload or Team continuation yet. |
| User surface/browser/shell | 75% | Focused Nuxt tests and prior implementation UI evidence; final-code fresh browser not yet exercised at this score point; Electron shell N/A. |
| Durable regression quality | 70% | Existing focused suites and new real Team E2E, but Team gate fails before post-launch assertions. |
- Overall: **59%** (simple average rounded). Critical AC-002 and user real-Team parity gate directly **failed**, not merely untested. Categories below 90%: all seven. Clean 95% target not met.
- Broader validation: **Required** despite repository failure, to narrow remaining unknowns and distinguish local standalone/UX behavior from Team launch defect. Modes: live AGY process/API, browser dev preview for web-equivalent UI. Browser not a desktop-shell claim. No external blocker; the Team/Org launch is an implementation failure. Expected gain: final-code denial/DONE, exact restore, selected workspace, skill and scoped MCP evidence. Broader validation cannot turn this round into Pass while Team/Org remain rejected.

## Broader evidence and revised validity
- Independent live standalone tests passed exact provider-ID restore, immutable run-start identity and changed-workspace/nonexistent-ID rejection; selected real workspace file/shell targets (not capsule), configured PRELOADED_ONLY skill. These tests are provider-real but below GraphQL.
- Fresh final-code Chrome 127.0.0.1:3000/workspace runs proved explicit-off denial is **DENIED** live **and after reload**, with `provider_state=ERROR` in normalized raw trace; default-on exit0/exit8/command-not-found are green **SUCCESS** live/reloaded with `provider_state=DONE`, exposed output/error and no shell exit field. No `AGENT_SEGMENT_LIFECYCLE_INVALID` banner. `api-e2e-browser-live-evidence.json` contains run IDs and trace extraction.
- Real AGY scoped AutoByteus MCP calls passed for both Team and Org context when `deliverLogicalMessage` is stubbed; this **does not** satisfy real Team/Org execution. The current Org run-tree validator calls the same shared `validateLaunchConfiguration`; real Org AGY launch has not been executed and is predicted to fail for the same explicit whitelist. It must be directly rerun after source repair.
- Capsule tests prove NONE omits AutoByteus-configured skill and collisions preserve user-owned files. Real `PRELOADED_ONLY` skill passed; provider-native skill discovery is outside `NONE` guarantee.
- Existing Codex/Claude Team parity test fixtures are `Needs Update` for removed `TeamMemberInput.refType`; do not interpret their unexecuted old fixture as AGY failure. New AGY test omits that field. No stale coverage deleted this round.

## Final investigation decision
- Final broader execution improved local evidence but did not close critical integration failure. Preliminary classification: **Local Fix — implementation-owned shared current run-tree validator excludes AGY**; Code Reviewer must confirm failure origin. AC-002/SCN-002 and real Team parity gate fail. Team restore/continuation and Org execution remain not tested because launch cannot complete. Proceed to failure report and focused review; no user/environment dependency prevents progress after repair.

## Round 2 re-entry — CRR-004 / IR-004 at b2e91b83f
- Prior authoritative API-REV-001 is **Fail / 74%**, not a baseline pass. Recheck API-F-001 first: CRR-004 reports current Team/Org run-tree launch-config validation now accepts current RuntimeKind including AGY; focused admission unit 3/3 passed. Source review is not execution acceptance.
- Changed boundary this round: current persisted run-tree admission before Team/Org GraphQL launch; downstream real AGY Team/Org process, WebSocket, scoped MCP delivery and restore remain unproven. Historical released-Team-V2 migration helper was not changed; unrelated historical fixture failures do not prove or disprove AGY behavior. Approved data transition remains Directly Usable — No Migration.
- Existing durable AGY Team GraphQL/WebSocket E2E remains **Still Valid**, including real recipient, exact run attribution, tool lifecycle and persisted projection. The stale Codex/Claude `TeamMemberInput.refType` fixture remains **Needs Update** and is not AGY parity proof. New admission unit is **Still Valid but insufficient** beyond validator.
- Round 2 execution order: (1) prepare documented shared SDK if needed and rerun AGY-02 real Team E2E *first*; (2) if launched, add/execute Team exact restore/continuation and direct Org launch with real member identity/trace; (3) focused admission, standalone identity/workspace/skills/collision/denial/DONE and non-AGY regression as needed. Do not count stubbed MCP as delivery. Record per-case checkpoints in the canonical ledger before later cases.
- Broader validation is **Required**: post-validator launch, real AGY recipient process and return stream are critical, and lower-layer tests cannot close them. Existing final-code browser denial/DONE evidence from API-REV-001 remains applicable unless new behavior contradicts it; no Electron-shell change. Post-repository and final scorecards pending execution.

### Round 2 first execution checkpoint
- AGY-02 real Team E2E is **Pass** at b2e91b83f: `RUN_AGY_E2E=1 ...agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch`, 1/1 in 32.59 s (`/tmp/agy-api-r2-team.log`). GraphQL launch passed, two AGY member runs were published, real ping→pong scoped MCP delivery yielded correlated start/success, exact sender/recipient IDs, recipient TURN_COMPLETED and both persisted member projections. API-F-001 is resolved at this execution boundary. This does not yet establish Team restore or direct Org launch; continue planned cases.
- Expansion checkpoint: added Team post-restore persisted response assertion and direct/nested Org GraphQL/WebSocket E2E. First expanded attempt had two **test-only fixture errors**: the new response assertion was inserted before the continuation command, and Org's no-adjustable-settings model rejects `{}` llmConfig; corrected to assert after send and use `null`. No production defect inferred. Rerun pending.
- **New API-F-002**: after fixing Org test's async WebSocket handshake race, direct AGY Org GraphQL creation succeeds, but native Org WebSocket emits CONNECTED then `AGENT_ORG_STREAM_UNAVAILABLE`, because `autobyteus-collaboration-stream-contracts/src/agent-org-execution-dtos.ts:13` still hardcodes only prior runtime strings in launch-config DTOs. The snapshot validator rejects AGY root, direct agent and nested team/member runtime kinds. This is direct execution evidence, not a fixture or missing auth. The Org test remains durable and should be rerun after contract repair. No workaround in tests. Org real member turns/return trace remain blocked. Preliminary classification Local Fix implementation-owned, Code Reviewer to confirm.

### Round 2 repository regression and broader decision
- Current admission 3/3 and runtime capability GraphQL 1/1 passed; mixed-team live provider test skipped by its opt-in guard. Org stream unit 10/10, AGY converter/capsule 13/13, focused web store/editor/tool 48/48 passed. These mocks/units do **not** model AGY Org WebSocket snapshot and do not override API-F-002.
- Post-repository scorecard, before real opt-in collaboration outcome was reconciled: AC proof 60, changed-boundary directness 70, cross-boundary realism 50, environment fidelity 75, failure/lifecycle 75, user-surface 95 (carried API-REV-001 browser), durable regression 70; mean **71%** rounded. Broader validation decision **Required**: opt-in real AGY Team/Org GraphQL/WebSocket, which was executed. Additional browser rerun **Not Required** for IR-004's backend-only validator change; API-REV-001's DENIED/DONE final-code browser traces remain carried, not newly reproduced. Actual Electron shell remains out of scope.
- Final direct Team+restore evidence removes API-F-001, but Org WebSocket's separate contract enum now fails. No available fixture can make an AGY Org native snapshot valid without production contract repair; no test-only whitelist workaround. Final confidence and result recorded in execution report.

## Round 3 re-entry — CRR-006 / IR-005 at 97f881366
- Prior authoritative API-REV-002 is **Fail / 81%**. Recheck unresolved API-F-002 **first**: CRR-006 reports the current native Org stream launch-config DTO now admits AGY at root/direct/nested Team/member and rejects unknown runtime kinds; contract 8/8 and handler 11/11 passed. These source/unit results do not establish real GraphQL/WebSocket Org member execution.
- Changed boundary this round: typed native Org stream snapshot value set (including checked-in dist) from current Org GraphQL launch to `/ws/agent-org/:orgRunId` CONNECTED/snapshot/lifecycle and subsequent direct/nested AGY member commands, presentations, persisted projections and scoped MCP. No frontend rendering or persisted format changed; prior API-REV-001 browser DENIED/DONE controls remain carried, with no new browser claim unless new evidence warrants it.
- Existing durable Team+Org opt-in E2E remains **Still Valid**. Its Org half now should proceed beyond the previous snapshot failure; inspect any new failure before classifying production versus test fixture. The Team half remains requirement-linked real delivery/restore coverage. Add durable Org scoped recipient assertions if feasible after direct/nested activation; stubbed MCP remains insufficient.
- Round 3 sequence: AGY-05 Org-only live GraphQL/WebSocket first; then full AGY Team+Org real transport with exact attribution and Team restore; then current contract/handler/admission and focused non-AGY regressions. Reuse canonical case IDs and ledger; post-repository/final scorecards after execution. Broader validation **Required** because only actual provider-real Org member turns can close API-F-002 and AC-002/008.
- First Org-only attempt at IR-005 proves native snapshot and first direct AGY member activation/presentation now proceed. It fails later on `getAgentOrgMemberRunProjection` with suspicious parameter labels (`AgentRun '/director' at '<orgId>'`) and teardown AggregateError following abort. This may be test argument construction, GraphQL binding or product projection; instrument variables and rerun before classifying. API-F-002 snapshot failure is resolved at this boundary, but AGY-05 is not yet Pass.
- Diagnostic isolation: intended Org projection variables were logged correctly; actual in-process GraphQL and HTTP `/graphql` both invoke the shared projection service with `address=orgRunId, run=/director` rather than the exact member address/AGY run ID; direct service call with correct args succeeds. This is a public GraphQL resolver argument-binding defect, not a provider process or test-only fixture. A premature presentation event may also have caused teardown noise, so the durable test now waits for exact member `TURN_COMPLETED` before projection. Need final rerun before failure classification; Org scoped delivery and nested member still unproven.

### Round 3 final investigation result
- API-F-002 **resolved**: native Org CONNECTED → typed snapshot/lifecycle works against real AGY root/direct/nested config. The final Org test launches both `/director` and `/team/worker` as actual AGY processes and gets exact attributed TURN_COMPLETED for each. It then proves real scoped Org MCP: director's AGY `call_mcp_tool` `send_message_to` to worker has correlated TOOL_EXECUTION_STARTED/SUCCEEDED, native communication record with exact sender/receiver IDs/content and worker TURN_COMPLETED. No stub delivery.
- **API-F-003** blocks AC-002/008 persisted/reloaded Org member view: the public `getAgentOrgMemberRunProjection` GraphQL query (also reproduced over actual HTTP `/graphql`) receives correct variables but invokes the projection service with `root=orgId,address=orgId,run=/director` rather than root/memberAddress/actual AGY run ID. Direct service call with intended args succeeds. This is not a model turn race (test now waits exact TURN_COMPLETED), CLI issue, fixture error or migration. Preliminary Local Fix in GraphQL resolver/binding path; Code Reviewer to confirm. The analogous public Org trace-page resolver has not been directly queried in this round, so do not infer its status. A source-level test/model for all supported runtimes may be warranted after fix.
- Repository regression: current contract 8/8, server focused 28/28, web focused 48/48 passed. Post-repository scorecard before opt-in live reconciliation: AC proof 60, directness 75, integration realism 50, environment fidelity 90, failure/lifecycle 75, user surface 95 (carried standalone browser), durable relevance 75; mean **74%** rounded. Broader decision **Required and executed** via real AGY Team+Org GraphQL/WebSocket; it closed the DTO gap but exposed public Org projection bug. No new browser run required for IR-005's DTO-only source delta; prior API-REV-001 DENIED/DONE live/reloaded controls remain carried. Final confidence/result in execution report. No requirement/design ambiguity or external blocker; route failure-origin review.

## Round 4 re-entry — CRR-009 / IR-007 at e23a029b2
- Prior authoritative API-REV-003 is **Fail / 84%**. CRR-009 corrects failure origin: the E2E started a real server (one production schema build), then called `buildGraphqlSchema()` again in the same process. TypeGraphQL global argument metadata was mutated; the later HTTP diagnostic inherited the contamination. **Withdraw API-F-003 as a production attribution**. Preserve the observed round-3 failure historically, but do not call it production evidence. IR-006's process-wide schema cache was removed; current production schema is pre-IR-006. Clean one-build public HTTP Org projection/trace/reload is **unproven**.
- Durable test decision: `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` **Needs Update**. Remove its second full schema build and in-process GraphQL executor; issue all GraphQL requests to the running server's actual HTTP `/graphql` endpoint. Add exact-ID Org direct and nested public projection plus trace-page and post-stop/reload assertions as feasible. The real AGY Team and Org WebSocket/scoped-delivery assertions remain **Still Valid**; repeat them under the clean fixture. Focused one-build Org argument unit 3/3 is **Still Valid but insufficient** for public API acceptance.
- Sequence: AGY-05 Org-only clean HTTP public projection/trace/reload **first**; AGY-02/07 full Team+Org real provider transport and restore; AGY-08 focused contract/server/web and non-AGY regression. Reuse canonical case IDs. Browser assessment follows repository results; carried standalone browser DENIED/DONE evidence is not a fresh run. Broader validation **Required**: exact clean-server public Org reads and actual AGY scoped roundtrip cannot be replaced by mocks or the contaminated prior HTTP diagnostic. Post-repository/final scorecards pending.
- Non-AGY evidence gap after successful AGY Team+Org and focused suites: local LMStudio `/v1/models` is available. The old opt-in standalone `agent-runtime-graphql.e2e.test.ts` failed before provider execution because its isolated fixture does not initialize current Studio application API services; this is a **stale fixture**, not AGY/non-AGY product regression. The existing AutoByteus Team E2E also independently starts the real server then builds a second GraphQL schema and still sends obsolete Team node `refType`; classify it **Needs Update** (previously noted stale Team fixtures). Update this one relevant durable Team suite to use only running-server HTTP GraphQL and current Team node input, then execute its real LMStudio create/restore/continue case for AC-005. Do not edit production code or infer a pass from the standalone fixture failure.

### Round 4 final coverage and broader decision
- Corrected AGY durable E2E is **Still Valid** and now uses the running server's HTTP `/graphql` exclusively for GraphQL, with no second schema build. Three clean Org runs after the first uppercase-cursor assertion correction directly proved exact `/director` and `/team/worker` public projections and active trace pages. Org stop/restore retained root/member IDs and fresh HTTP reads retained original markers/trace; final test waits for root checkpoint `hasOpenExecutionWork=false` before stop. The full final AGY Team+Org suite is 2/2; both real scoped deliveries and exact attribution remained passing. API-F-003 is **withdrawn as production**; no production GraphQL fix or cache was required.
- Updated AutoByteus Team durable E2E is **Still Valid after fixture repair**: single server-built schema via HTTP, no obsolete Team `refType`, and explicit LMSTUDIO provider catalog. A real local LMStudio Team projection/terminate/restore/continuation case passed for both members (one selected case; four other opt-in cases skipped). The stale standalone fixture failure and initial cloud-QWEN selection failure are test/environment origins, not product failures; see ledger. Codex/Claude Team parity fixtures remain **Needs Update** and no current provider-real claim is made for them. Their focused backend regressions passed.
- Current contract 8/8, focused server 32/32 plus non-AGY 73/73, focused Org web 78/78 plus other-runtime web 55/55 passed. API-REV-001 fresh browser DENIED/DONE live/reloaded controls and standalone workspace/skill/collision remain carried because IR-007 changes no production UI/CLI behavior. Fresh Org browser UI was considered but **not required** to close the specific CRR-009 gap: real production HTTP member projection/trace after Org restore plus web hydration/inspection tests directly exercise that seam. The absence of a live Org browser journey remains a bounded 90% user-surface residual, not a false browser pass. Actual Electron shell unchanged/out of scope.
- One Org stop returned `success:false` during an intermediate expansion before root quiescence was asserted; its origin was not captured. Three later Org runs, including final durable code, stopped/restored cleanly, so no current reproducible critical failure is established. The test now waits root quiescence; arbitrary mid-turn Org termination is **not** inferred to pass. Final confidence scorecard is in the current report: **95%** overall, each applicable category ≥90%, all critical ACs proven directly or carried from unchanged boundaries. Broader real-provider validation was **Required and executed**; no further targeted surface promises material evidence gain for this source delta. Route reviewed Pass to Code Reviewer for proportional changed-test-code review.

## Round 5 re-entry — explicit process-restart and browser focus/continue gate
- Trigger: user explicitly requires AGY Team **and** Org restore after stopping the actual backend OS process, starting a new backend OS process against the same isolated data, clicking/focusing the same member in the browser and sending a new message. Prior API-REV-004 was Pass/95% for its then-defined scope, but its same-process terminate/restore and reloaded HTTP reads **do not prove this new critical journey**. No round-5 Pass is presumed.
- Boundaries: process lifecycle, persisted run/provider identity, GraphQL/WS reconnection, Nuxt history hydration, exact browser member focus, composer submission and continued AGY response. Existing AGY Team/Org provider-real E2E is **Still Valid but insufficient** for process restart/browser focus; prior browser DENIED/DONE is **Still Valid but unrelated**. Add durable browser/process E2E if maintainable; temporary probe only if a durable fixture is disproportional, with exact gap recorded.
- Planned cases: AGY-09 Team cross-process restart → browser history click same member → new turn, exact run/provider ID and persisted old/new conversation; AGY-10 Org cross-process restart → browser click direct and nested member (at minimum one exact AGY member continuation) → new turn, exact root/member/provider ID and old/new projection/trace; AGY-11 targeted regression of changed fixture and prior AGY Team/Org live suite. Use isolated data root/SQLite and unique ports; stop only owned processes. Do not claim a plain API restore as a browser click.
- Documentation/instructions: `autobyteus-server-ts/README.md` documents `pnpm -C autobyteus-server-ts build` then `node dist/app.js --host --port --data-dir`; `autobyteus-web/README.md` documents `BACKEND_NODE_BASE_URL` for Nuxt proxy; existing `autobyteus-web/tests/e2e/agent-org-role-labels-probe.mjs` demonstrates isolated SQLite migration, process groups, dynamic ports and Playwright-core. Applicable server `AGENTS.md` uses focused `pnpm exec vitest run ... --no-watch`.
- Broader validation **Required**: neither repository same-process tests nor mocked UI cover server OS restart and browser action. First execute owned cross-process/browser fixture, diagnose observed failures before product attribution, then reconcile targeted regression and scorecard. Clean target remains 95% and no critical gap; prior 95% is not rolled forward automatically.

### Round 5 executed evidence and final decision
- Added durable opt-in `autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` rather than a throwaway manual probe: it owns isolated SQLite/ports/processes, seeds real current Team/Org via public GraphQL, obtains real AGY turns on A, shuts down A, starts distinct B on same data, launches fresh Chrome/Nuxt, clicks exact Team/direct Org/nested Org history members and sends new messages. Final result **Pass**; exact prior/new conversation and provider/root/member IDs proven through public projections and browser. Full details/evidence in current execution report and ledger events 56–57.
- Fixture-only early errors: Team current tree snake_case parsing, required WebSocket message/dedupe IDs, and Team surface selector; all corrected before final run (ledger 53–55). No production finding inferred from them. Fresh browser rendered the previous messages and two occurrences of each unique new marker (user and AGY reply), not merely a clicked row or a submitted draft.
- Regression: server build passed; focused history/Org web 80/80 passed; final full provider-real AGY Team+Org 2/2 and targeted Team 1/1 passed. First full round-5 run had intermittent Team `terminateAgentTeamRun.success=false` / `not the current published run` plus teardown AggregateError after prior real delivery; exact cause unresolved and retained as bounded lifecycle risk, not silently erased. The new OS-process-restart journey does not use Team terminate mutation and passed. Prior AC-005 LMStudio and DENIED/DONE controls carried from unchanged boundaries.
- Scorecard and final **Pass / 96%** in current report. Broader validation was **Required and executed**; no Electron-shell claim. No production code or stale-test deletion. Send to Code Reviewer for proportional new test-code review and awareness of intermittent termination origin; independent review can revise this disposition if it attributes a product defect.
- Final durable-script rerun after changing logs to per-run overwrite: first attempt failed at startup because this API round had prematurely removed generated shared SDK `dist`; this was an environment cleanup-order error, not AGY/product failure. `prepare:shared` rebuilt it, and final script passed all Team/direct+nested Org cases with backend PIDs 16695→16771. Final evidence JSON/logs are now single-run. Generated shared SDK `dist` removed only after final execution.

## Round 6 re-entry — CRR-011 test-owned TR-001
- Prior API-REV-005 execution: **Pass / 96%** for actual process-restart Chrome journeys, with current browser screenshots/surfaceText showing prior and new answers. CRR-011's proportional test-code result is **Fail — Local Fix / TR-001**: the durable helper asserted only the new reply in the browser; old reply was asserted in public projection but not the focused UI. This is a **future-regression assertion gap**, not current product failure or a withdrawal of observed execution.
- Scope remains the same approved Team `/second`, Org `/director` and nested `/team/worker` restart→focus→continue cases. Durable test **Needs Update**: assert old AGY reply in the selected visible browser conversation feed *before* sending and both old/new replies there *after* sending, for each member. Use two occurrences of each unique marker within the conversation feed (one user prompt, one AGY reply) rather than the API projection, screenshot or composer. Preserve exact IDs and HTTP projection assertions. No production source or other test should change.
- Execute `node --check`, documented `prepare:shared` (needed for built server runtime), then the affected opt-in browser/process probe with real AGY/Chrome. Do not rerun unchanged broad provider suites unless the focused result indicates a new integration problem. Reconcile evidence/report source provenance: `c9c8373e1` was the pre-test production/source base; `3b22be2a9` introduced the durable browser test; record the final assertion-fix commit separately. Preserve intermittent Team terminate log with **no product/test attribution** per CRR-011, and capture lifecycle state only if it recurs.
- Broader validation **Required** because the missing assertion concerns real browser hydration. Post-repository confidence remains bounded until affected live probe reruns; prior 96% not silently rolled forward to final status.
- Round 6 final: `prepare:shared` passed; updated probe passed with backend A PID 22997 → B PID 23069. For all three exact selected members, the visible focused conversation feed had old marker count 2 before send and both old/new count 2 after send, directly guarding UI hydration. Public exact-ID projections still retained old/new and all owned services/data were cleaned. CRR-011/TR-001 is addressed in durable test code; no production finding arose. `node --check` passed. No broad AGY/LMStudio rerun: no production change and the affected browser/process boundary was directly rerun. Current result/confidence in API-REV-006 report; proportional re-review remains required.

## API-REV-007 re-entry — SR-023 / IR-008 / CRR-013 at 40641dfca
- Prior API-REV-006 **Pass / 96%** applies only to pre-SR-023 source. Current source review CRR-013 is Pass; it is not executable/browser acceptance. Approved user behavior is unchanged SR-016/SR-021; DS-005 adds complete ordered Org preflight, request-local catalog sharing, bounded async AGY discovery, responsive health, and distinct safe discovery-vs-missing-slug diagnostics through the existing browser alert. Delivery remains on explicit user-verification hold.
- Changed boundaries: Org orchestration and model selection, AGY external child process, GraphQL failure transport, persisted current Org tree, Nuxt browser launch/error state, operational REST health concurrency. No stored shape, migration, shell IPC, identity, tool/trace or non-AGY policy change; **Directly Usable — No Migration**. Implementation handoff legacy check reports no compatibility path retained.
- Project instructions: `autobyteus-server-ts/AGENTS.md`, server README and package scripts (prepare shared/build; Vitest `run ... --no-watch`), `autobyteus-web/AGENTS.md`, web README/ARCHITECTURE and package scripts (Nuxt browser dev path, one-shot `--run`). Existing `autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` provides owned isolated backend/Chrome process fixture; `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` provides real Team/Org transport/restore. Existing IR-008 focused capability/Org/model tests are **Still Valid but insufficient**: their fake CLI and unit health overlap bypass real GraphQL/browser process. Prior browser restart probe remains **Still Valid** but does not exercise full 18 placements or failure alert. No stale coverage deletion.
- Add durable opt-in browser/process probe for real 1-root/3-Team/14-Agent AGY Org via Nuxt against freshly built equivalent backend, exact persisted active tree and responsive `/rest/health` during slow discovery; isolated fake CLI modes for timeout/failure and valid catalog missing slug, asserting addressed safe rendered alert and launching reset. Use existing CLI/auth for successful launch; wrapper may delay delegation to real `agy models` for overlap. Avoid user's packaged backend/data. Actual Electron shell is not changed and old test artifact predates IR-008; browser against current equivalent backend is the selected supported surface.
- Planned execution order: focused IR-008 repository suites; new AGY-12 real browser 18-placement launch + concurrent health and persisted tree; AGY-13 safe timeout/failure browser; AGY-14 valid-catalog missing slug browser; AGY-15 real Team/Org and non-AGY regression. Create isolated SQLite/data/workspace and owned backend/Nuxt/Chrome; preserve original user Org and Delivery artifacts. Broader validation **Required** because the critical bug appeared only in real workload/health overlap. No current confidence/pass inferred before execution.

### API-REV-007 repository/broader evidence and final decision
- Initial focused IR-008 source suites 21/21 and fresh production build/bootstrap passed. At that post-initial-repository gate, confidence was **76%** (seven-category mean: 75/75/75/90/75/60/80); browser/real workload/health and error transport were critical missing evidence, so broader validation remained **Required**.
- Added opt-in durable `autobyteus-web/tests/e2e/agy-large-org-launch-health-probe.mjs`. It uses freshly built `dist/app.js`, isolated migrated SQLite/app-data/workspace, real Nuxt/Chrome, real AGY 1.2.10 for positive catalog, and a test-owned wrapper with slow/timeout/failure/missing-catalog modes for deterministic external-process edges. It seeds 14 Agent and 3 Team definitions plus an Org, opens the actual Org config UI, selects its isolated workspace, presses Run, checks active public 18-placement tree, one slow real `agy models` invocation and overlapping health. It verifies the same exact tree/member identities from a second backend OS process on the same isolated store. It then runs browser timeout/failed discovery/valid-missing-slug alerts and reset. The wrapper is a fixture around the real provider for the positive case, not a mock of the backend or browser path. Final command passed; browser errors empty, owned resources cleaned. Initial probe assertions targeted a hidden workspace input and misread untyped tree nodes; corrected fixture-only failures are preserved in ledger, not product defects.
- Broader affected regression: seven server unit suites 70/70, runtime capability/public Org args/current run-tree API suites 7/7, five web Org/Team/runtime config suites 39/39. Real AGY Team+Org GraphQL/WebSocket suite initially had Team stop `success:false` while `/ping` was still `running` and `hasOpenExecutionWork=true`; diagnostic log captured both current-published IDs before the premature stop and the termination error. This was a test sequencing gap for a *quiescent restore* assertion, not proof of arbitrary mid-turn stop correctness or a new IR-008 production defect. Updated the durable Team E2E to wait for quiescence and retain pre/post stop diagnostics only on failure. Targeted Team 1/1, full Team+Org 2/2, then targeted final logging-only code 1/1 passed. Org remains real scoped director→worker delivery and exact attribution/restore; Team remains real ping→pong delivery and exact restore/continuation. Prior process-restart Chrome Team/Org focus/continue API-REV-006 and real LMStudio non-AGY E2E API-REV-004 are carried on their earlier source basis; they are not claimed freshly rerun.
- Existing coverage decisions: AGY Team+Org E2E **Needs Update → Updated** solely for quiescent stop/diagnostic; prior browser restart probe **Still Valid**; current IR-008 unit suites **Still Valid**; new large-Org browser probe **Add Durable Coverage**; no stale coverage removed. Existing non-AGY unit/UI/API regression passed on current code; provider-real LMStudio was not rerun because no LMStudio-specific path changed. No design/requirement ambiguity or invalid compatibility behavior observed.
- Final scorecard after browser/regression: requirement/AC **95**, changed-boundary directness **95**, cross-boundary integration **95**, environment/identity **95**, failure/lifecycle **90**, web-equivalent browser **100**, durable relevance **95** = **95%** mean. Every SR-023 critical criterion directly passed; no category below 90%. Residual: package shell itself was not rebuilt from IR-008 (fresh equivalent backend used per DS-005); controlled wrapper rather than organically failing live provider for timeout; arbitrary mid-turn Team termination is not accepted; other AGY versions and real non-AGY provider rerun not claimed. Result **Pass / 95%**, Large/High reviewed route to Code Reviewer for proportional new/updated test-code review. Delivery's explicit user-verification hold remains.
