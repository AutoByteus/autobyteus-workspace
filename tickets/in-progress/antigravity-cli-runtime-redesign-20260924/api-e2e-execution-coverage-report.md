# API/E2E Execution Coverage Report — AGY runtime

## Latest authoritative round and route
- **API-REV-007 — Pass / 95%** on IR-008 source commit `40641dfca12d030f438075499e713750acf73574`; reviewed **Large / High** package; CRR-013 source Pass is the trigger, not executable acceptance. Prior API-REV-006 **Pass / 96%** was on pre-SR-023 source and is not inherited as the current result. Current durable-test changes require Code Reviewer proportional test-code review. Delivery's explicit user-verification hold remains; no release/finalization/cleanup of original user data.
- Authorities: `requirements-doc.md` (approved SR-016/SR-021, REQ-001/AC-001 and Team/Org scope), `investigation-notes.md` §SR-023, `solution-revision-record.md`, `design-spec.md` DS-005, `investigation-result.md`, `solution-org-launch-recovery-handoff.md`, `design-review-report.md` ARCH-REV-004, `architecture-review-revision-record.md`, `implementation-handoff.md` IR-008, `implementation-revision-record.md`, `code-review-report.md` CRR-013, `code-review-revision-record.md`, prior `api-e2e-coverage-investigation.md`, `api-e2e-test-case-ledger.md`, `api-e2e-revision-record.md`, `api-e2e-test-review-report.md` CRR-012 and Delivery `delivery-revision-record.md` DR-001. All paths are in this ticket directory unless identified otherwise. Product UX supplement N/A — not applicable. Independent reviews selected and present.
- Investigation was updated before durable coverage changes and before final execution. Changed surfaces: backend Org complete-placement orchestration; shared model selection; bounded async external AGY discovery; GraphQL failure/status transport; browser launch/error UI; concurrent REST health; current persisted Org tree. No persistence-format change, migration, Electron IPC/shell change or non-AGY intended-policy change. Implementation handoff legacy/compatibility check is clean; **Directly Usable — No Migration**. No version-specific compatibility shim observed or protected by tests.

## Case ledger reconciliation and observed behavior
Canonical ledger: `api-e2e-test-case-ledger.md`, initialized before execution and updated at each completed case. No case running, blocked or unstarted. Earlier fixture correction/failing stop attempts remain in ledger; final results below do not erase them.

| Case | Final | Direct evidence |
| --- | --- | --- |
| **AGY-12** full browser large Org / health / persisted tree | **Pass** | `RUN_AGY_E2E=1 node autobyteus-web/tests/e2e/agy-large-org-launch-health-probe.mjs` against freshly built real `dist/app.js`, isolated migrated SQLite/app-data/workspace, Nuxt and headless Chrome. Browser selected AGY model and real workspace, clicked `Run Agent Org`, routed active in **6.058 s**. Public GraphQL `getAgentOrgRunConfig` returned active **1 root/3 Teams/14 Agents**, 18 unique exact addresses and Agent run IDs. The successful request had exactly **one** 2.5-second delayed `agy models` call delegated to real CLI; three `/rest/health` requests during it each took **2 ms**. A distinct backend OS process (PID 43471 vs 42407) on the same isolated store returned the **exact same execution tree and identities** for the same run ID. No AGY member process was expected at Org creation; idle members' absent conversation IDs were not treated as launch failure. |
| **AGY-13** bounded discovery timeout and child failure | **Pass** | Real browser Run on the same 18-placement form with controlled external CLI timeout rendered `Invalid configuration for '/': Antigravity model discovery timed out; check the CLI and retry.` in **16.344 s**. Health stayed **1/3/3 ms** during pending discovery; button reset to `Run Agent Org`. Separate nonzero child failure rendered addressed safe `Antigravity model discovery failed; check authentication or network and retry.` in **1.409 s**. Raw private test marker/path absent from rendered alerts; browser errors empty. |
| **AGY-14** valid catalog missing selected slug | **Pass** | Parseable valid model catalog omitting selected slug rendered **distinct** `Invalid configuration for '/': The selected model is unavailable.` in **935 ms**, not a discovery-failure label; button reset. |
| **AGY-15** affected Team/Org and non-AGY regression | **Pass, bounded** | Real AGY GraphQL/WebSocket Team ping→pong scoped delivery, exact attribution, stop/restore/continue; real direct/nested Org scoped delivery, exact public projection/trace and restore: final full suite **2/2**. Seven backend unit suites **70/70**, three API/admission suites **7/7**, five web Org/Team/model config suites **39/39**. These include current AutoByteus/other-runtime model-selection paths. Real LMStudio AutoByteus Team E2E from API-REV-004 and Chrome process-restart Team/Org focus/continuation from API-REV-006 are carried earlier-basis evidence, **not freshly rerun**. |

Evidence: `api-e2e-round7-large-org-browser/evidence.json`, `large-org-active.png`, `timeout-alert.png`, `failed-alert.png`, `missing-alert.png`, `backend.log`, `backend-after-restart.log`, `frontend.log`, `migrate.log`; `/tmp/agy-r7-large-org-final-code.log`, `/tmp/agy-r7-seven-suites.log`, `/tmp/agy-r7-api-regression.log`, `/tmp/agy-r7-web-regression.log`, `/tmp/agy-r7-team-org-quiescent-final.log`, `/tmp/agy-r7-team-final-test-code.log`.

### Team termination observation and test-owned correction
- First fresh-code full Team+Org run: Org **passed**, Team reached real delivery/persisted token but `terminateAgentTeamRun` returned `success:false`, `Agent run ... is not the current published run` (`/tmp/agy-r7-team-org-live.log`). This recurred in focused Team test. Pre-stop diagnostic captured root active, `hasOpenExecutionWork=true`, `/ping` **running**, both member run IDs still current-published; after the failed stop, root inactive/registry empty (`/tmp/agy-r7-team-stop-diagnostic.log`). Token projection had been asserted **before** the sender turn settled. Do not call that failing arbitrary mid-turn termination a product pass or silently discard it.
- The tested requirement is **stop/restore after a completed Team turn**, not interruption of active relay work. Durable E2E now waits current Team checkpoint `hasOpenExecutionWork=false` before its stop mutation, analogous to existing Org quiescent-stop guard, and retains pre/post lifecycle snapshot only if stop unexpectedly fails. Focused Team **1/1**, full Team+Org **2/2**, then final logging-only code targeted Team **1/1** passed. Full-run pre-stop state showed both members IDLE/current-published and no open work. The supported quiescent restore path is passing; arbitrary mid-turn terminate behavior remains unclassified/out of this acceptance claim. This is a test sequencing correction, not a production-source change.

## Execution commands and environment
- Instructions: `autobyteus-server-ts/AGENTS.md`, `autobyteus-server-ts/README.md`, `autobyteus-web/AGENTS.md`, `autobyteus-web/README.md`/`ARCHITECTURE.md`, root/server/web package scripts. Single files use server `vitest run ... --no-watch`; web uses `test:nuxt ... --run`. Browser uses project Nuxt dev path, not user desktop app.
- `pnpm -C autobyteus-server-ts prepare:shared` → Pass; `pnpm -C autobyteus-server-ts exec vitest run tests/unit/runtime-management/antigravity-cli-capability.test.ts tests/unit/agent-org-execution/agent-org-run-service-model-selection.test.ts --no-watch` → **21/21**; `pnpm -C autobyteus-server-ts build` → Pass, production TypeScript/build/bootstrap smoke. Then opt-in browser command above → Pass final.
- Seven focused server unit files: `antigravity-cli-capability`, `agent-org-run-service-model-selection`, `run-model-selection-service`, `model-catalog-service`, `application-launch-host-capability-validator`, `team-run-model-selection-save`, `agent-run-model-selection-commit` → **70/70**; see exact full command in `/tmp/agy-r7-seven-suites.log`. API `runtime-capability-graphql`, public `agent-org-run-config`, `current-execution-tree-runtime-admission` → **7/7**, `/tmp/agy-r7-api-regression.log`. Web `agentOrgRunConfigStore`, `AgentOrgRunConfigPanel`, `AgentOrgRunConfigForm`, `TeamRunConfigForm`, `RuntimeModelConfigFields` → **39/39**, `/tmp/agy-r7-web-regression.log`. `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch` → final **2/2**, plus targeted final-code Team **1/1**.
- Browser fixture: macOS, Node 22, current local AGY **1.2.10** and local auth (no secret value captured), Chrome via `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, 1440×1000 viewport. New temporary backend app-data/SQLite/selected workspace, dynamic loopback ports, fresh migration, Nuxt. `ANTIGRAVITY_CLI_COMMAND` points to test-owned external wrapper: delegates version/help/models to **real AGY** for successful launch, delays positive catalog 2.5 s to prove overlap, and deterministically hangs/fails/returns valid alternate catalog for negative cases. This emulates the failing external dependency only, not GraphQL/backend/browser. No user app PID 302 or normal user data changed.
- Browser is the correct web-equivalent Electron renderer boundary; the older Electron package predates IR-008 and was not used. Current source backend was built and run equivalently. Shell packaging/IPC, other operating systems and organic auth/network failure were not claimed.

## Confidence and decision
Post-initial-repository confidence **76%**, before browser/real workload; broader validation **Required and executed**. Final seven-category scorecard:

| Category | Post-repository | Final | Basis / residual |
| --- | ---: | ---: | --- |
| Requirement and AC proof | 75% | **95%** | Full user-scale launch/health/error distinctions direct; package shell not rerun. |
| Changed-boundary directness | 75% | **95%** | Current built backend and public transport/browser; no raw Electron package. |
| Cross-boundary integration realism/mock gap | 75% | **95%** | Real Chrome/Nuxt/GraphQL/SQLite/AGY positive; controlled wrapper for external failure. |
| Environment/config/identity/fixture fidelity | 90% | **95%** | Exact current workspace/model/tree, fresh-process persistence; one local AGY platform/version. |
| Failure, edge, lifecycle, recovery | 75% | **90%** | Timeout/failure/missing slug and health; quiescent Team stop; arbitrary mid-turn stop not accepted. |
| User surface/browser/desktop shell | 60% | **100%** | Real form click, route, role=alert, button reset, screenshots and empty browser errors; shell-specific scope unchanged. |
| Durable regression relevance/quality | 80% | **95%** | New opt-in full-shape process/browser probe, tightened Team lifecycle fixture, current API/web/non-AGY focused suites; provider-real non-AGY not freshly rerun. |

Simple mean **76% → 95%**. No final category below 90%; every current SR-023 critical criterion directly passed. Residuals are bounded above and do not justify claiming a packaged Electron/user verification pass. No Requirement Gap, Design Impact or current source failure identified. Initial UI fixture errors (hidden input, node-shape assertion) and premature Team stop are preserved in ledger; no test was removed as stale.

## Durable coverage, cleanup and handoff
- **Added** `autobyteus-web/tests/e2e/agy-large-org-launch-health-probe.mjs` — opt-in real browser/backend 18-placement launch, health, process persistence and distinct failure alerts. **Updated** `autobyteus-server-ts/tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` — quiescent Team restore stop and conditional lifecycle diagnostics. No production source or existing test removed. `node --check` and `git diff --check` passed. Both changed paths require proportional Code Reviewer test-code review; no separate final review is yet claimed.
- All backend A/B, Nuxt, Chrome, migrated test database and selected workspace created by this probe were stopped/removed. Test logs, JSON evidence and screenshots retained. Build-generated untracked shared SDK `dist` directories were removed after final execution and were not staged. User's original/stopped Org and packaged app were untouched; no release/deployment/cleanup of them.
- **Latest result: Pass / 95%.** Reviewed Large/High successful route: obtain handoff rules, then send complete cumulative package to `/code_reviewer` for proportional new/updated test-code review. Delivery's user-verification hold remains in force; no delivery acceptance.
