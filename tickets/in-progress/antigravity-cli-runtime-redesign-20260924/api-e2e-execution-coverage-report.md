# API/E2E Execution Coverage Report — AGY runtime

## Current result, provenance and review status
- **API-REV-006 — Pass / 96%**, 2026-09-25. Trigger: Code Reviewer **CRR-011 Fail / Local Fix TR-001** on the durable browser test's missing old-reply UI assertion. That is a test-coverage gap, **not** an observed product/browser failure. The bounded fix and affected real-process/browser rerun now pass; proportional test-code re-review is still required. No delivery acceptance or new production-source review is claimed.
- Production/source base: `c9c8373e1dc02ca44c285cd96286dacf7708208c` (the merged source baseline used for this validation). Initial new durable test and API-REV-005 evidence: `3b22be2a9`. **Final old-reply assertion fix and rerun evidence: `c4f2c3a55`**. These are distinct; the earlier report's unqualified “package at c9c8373e1” was imprecise provenance, not a different source implementation.
- Approved authority remains `requirements-doc.md` (SR-021, REQ-001–011, AC-001–010), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md`, `investigation-result.md`, `design-review-report.md`, `architecture-review-revision-record.md`, `implementation-handoff.md`, `implementation-revision-record.md`, `code-review-report.md`, `code-review-revision-record.md` and AGY supplements indexed in prior API revisions. Current coverage investigation: `api-e2e-coverage-investigation.md`; case checkpoint: `api-e2e-test-case-ledger.md`; cumulative history: `api-e2e-revision-record.md`; triggering independent test review: `api-e2e-test-review-report.md` (CRR-011, not yet superseded).

## TR-001 corrected — direct user journey
- **AGY-09 Team — Pass.** Owned backend OS process A **PID 22997** ran a real two-member AGY Team against isolated SQLite/app-data. `/second` completed an initial provider-real turn. A exited cleanly before a distinct backend OS process B **PID 23069** started with the **same** data root/port. Public Team resume config retained exact root, member and provider IDs. A *fresh* Chrome/Nuxt session clicked the same `/second` Team history row. The updated durable helper asserted the unique old marker appeared **twice in that focused visible conversation feed before sending** (old user prompt plus AGY answer), then typed/clicked **Send message** and asserted the old marker remained twice **and** the new marker appeared twice (new prompt plus AGY answer). Public exact-ID `getTeamMemberRunProjection` retained both turns. No same-process terminate/restore, API-only read or stub is substituted.
- **AGY-10 Org direct and nested — Pass.** The same A→B transition had real AGY turns from direct `/director` and nested `/team/worker`. B retained exact Org root, member and provider IDs. Fresh Chrome clicked each exact persisted member (expanding the nested Team for the worker). **For each**, the visible focused conversation feed showed old marker count **2 before send** and old/new counts **2 each after sending and receiving the new AGY answer**. Exact-address/run-ID public HTTP Org projections retained both turns. This closes CRR-011/TR-001 for both Org paths, not merely the Team path.
- Evidence: canonical `api-e2e-round5-process-restart-browser/evidence.json` records the three per-member `oldReplyCountBeforeSend=2`, `oldReplyCountAfterSend=2`, `newReplyCountAfterSend=2`, exact selected identities, A/B PIDs, result and cleanup. Supporting `team-after-restart.png`, `org-after-restart.png`, `org-worker-after-restart.png`, backend A/B, Nuxt and migration logs are from the **final rerun**. Command: `RUN_AGY_E2E=1 node autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` → **Pass**, `/tmp/agy-r6-probe-final.log`. AGY CLI 1.2.10, Node 22.23.1, real local AGY authentication, model `gemini-3.8-flash-low`; no credential values recorded.

## Execution, scope and residuals
| Check | Result | Evidence / scope |
| --- | --- | --- |
| Documented shared build | **Pass** | `pnpm -C autobyteus-server-ts prepare:shared`, `/tmp/agy-r6-prepare-shared.log`; current server `dist/app.js` was built in API-REV-005. Shared `dist` is required at runtime and was removed **after**, not before, the final browser run. |
| TR-001 durable probe | **Pass** | `node --check` and final real A→B Chrome/Nuxt probe. The old and new AGY answers are asserted in the selected visible feed for all three member paths, not only in public projections/screenshots. |
| Prior real AGY scoped Team+Org | **Carried Pass, not rerun in API-REV-006** | API-REV-005 final 2/2 plus targeted Team 1/1, `/tmp/agy-r5-agy-regression-final.log`, `/tmp/agy-r5-team-rerun.log`; no production change in this local test fix. |
| Focused web history/Org | **Carried Pass, not rerun in API-REV-006** | API-REV-005 80/80, `/tmp/agy-r5-web-regression.log`; affected full browser boundary was rerun directly. |
| Other accepted controls | **Carried** | API-REV-001 DENIED/DONE browser and standalone workspace/skill/collision, API-REV-004 real non-AGY LMStudio Team and contract/server/web suites. No new Codex/Claude provider-real or Electron-shell claim. |

- **Intermittent Team termination observation remains held, not attributed.** First API-REV-005 full suite once returned `terminateAgentTeamRun.success=false` / `AgentTerminationError: ... is not the current published run` with supervisor-close error (`/tmp/agy-r5-agy-regression.log`); targeted Team and later full Team+Org passed. CRR-011 found the invariant source but no pre-stop checkpoint/current-member state, so neither product nor test origin is established. The A→B browser probe never invokes Team terminate mutation and passed. Preserve the failing log; capture lifecycle state if recurrence permits. Do not claim arbitrary mid-turn termination works or silently treat the observation as fixed.
- Persisted-data transition is **Directly Usable — No Migration**: B read A's Team/Org exact current run/provider bindings and conversations without migration or identity substitution. Crash/SIGKILL, another AGY CLI version and Electron-shell lifecycle are untested. Browser execution proves the web-equivalent renderer, not the shell.
- Only the durable browser helper's assertions and evidence were changed in API-REV-006. No production source, old test, or approved intended behavior changed. Isolated SQLite/app-data/workspace, both backend processes and Nuxt/Chrome were cleaned; pre-existing user app and LMStudio processes were untouched. Evidence/logs/screenshots retained. Build-generated untracked shared SDK `dist` removed after the final run. `git diff --check` passed after sanitizing trailing whitespace in generated migration log.

## Confidence and decision
| Mandatory category | Pre-broader | Final | Evidence / bound |
| --- | ---: | ---: | --- |
| Requirement and AC proof | 90% | **95%** | Three explicit same-member cross-process journeys and old/new visible answers; intermittent separate Team terminate held. |
| Changed-boundary directness | 90% | **100%** | Separate backend PIDs, same persisted root and real browser focus/composer/AGY turns. |
| Cross-boundary integration realism/mock gap | 90% | **100%** | Real GraphQL, WebSocket, AGY, Chrome and Nuxt; no stub parity. |
| Environment/config/identity/fixture fidelity | 90% | **95%** | Isolated SQLite, exact root/member/provider binding; other hosts/versions untested. |
| Failure, edge, lifecycle and recovery | 75% | **90%** | Clean A→B recovery; unrelated Team termination intermittent unresolved. |
| User surface/browser/desktop shell | 75% | **100%** | Old reply directly asserted in focused visible Team/direct Org/nested Org feed before send and old/new after send; shell N/A for web journey. |
| Durable regression relevance and quality | 90% | **95%** | TR-001 assertion now guards browser hydration; opt-in provider-real repeatable probe. |
- Simple mean **86% pre-broader → 96% final** (rounded). All explicit critical restart/focus/continue scenarios directly pass and no applicable final category is below 90%. Broader validation **Required and executed**. **Current API/E2E result: Pass / 96%**. Route reviewed Large/High package to Code Reviewer for proportional re-review of the bounded durable-test fix; no delivery acceptance.
