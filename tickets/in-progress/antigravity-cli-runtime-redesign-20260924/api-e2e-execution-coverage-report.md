# API/E2E Execution Coverage Report — AGY runtime

## Latest authoritative result and route
- **API-REV-008 — Pass / 96%**, current integrated merge `678bece5f8567acb74305d8458d084a96d209ab9` (`origin/personal@3e5d6add5`) plus API/E2E-owned test-only corrections. Prior API-REV-007 **Pass / 95%** and CRR-014 proportional review covered pre-merge source/test commit `32fd63dd6`; they were not inferred to pass after Delivery DR-004's zero-test collection. IR-008 production source and CRR-013 source Pass remain unchanged. Classification **Large / High**, Reviewed route; three changed durable test files require a **new proportional test-code review** before Delivery resumes. DR-004's old Electron DMG remains stale and Delivery's explicit user-verification hold persists.
- Authorities: `requirements-doc.md` (approved SR-016/SR-021), `investigation-notes.md` §SR-023, `solution-revision-record.md`, `design-spec.md` DS-005, `investigation-result.md`, `solution-org-launch-recovery-handoff.md`, `design-review-report.md` ARCH-REV-004, `architecture-review-revision-record.md`, `implementation-handoff.md` IR-008, `implementation-revision-record.md`, `code-review-report.md` CRR-013, `code-review-revision-record.md`, prior `api-e2e-coverage-investigation.md`/report/revision/ledger, `api-e2e-test-review-report.md` CRR-014, and Delivery `release-deployment-report.md`/`delivery-revision-record.md` DR-004 plus `/tmp/agy-delivery-r4-team-org.log`. All named artifacts are in this ticket unless stated otherwise. Product UX supplement N/A — not applicable.
- Investigation and canonical ledger were updated before durable edits/execution. Current boundary: latest-base test fixture/import, real Team/Org GraphQL/WebSocket transport and exact restore, full browser Org preflight/health/error, process restart/continued AGY conversations, current non-AGY shared model/config/API. No AGY production code or persisted format changed by API/E2E. **Directly Usable — No Migration** remains; no compatibility alias or version-specific fallback added.

## DR-004 blocker, correction and direct rerun
- Delivery's exact command `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch` collected **zero tests** because the fixture imported removed `src/config/server-runtime-endpoints.js`. This was a collection failure, not an AGY runtime result.
- Removed only the dead test import, saved `AUTOBYTEUS_INTERNAL_SERVER_BASE_URL_ENV_VAR` variable and before/after env cleanup. Current `startStudioE2eRuntimeServer()` supplies `mainUrl`, used by the test's HTTP and WebSocket clients; no source uses the removed constant. No replacement shim or production edit. After `prepare:shared`, the exact opt-in E2E **collected and passed 2/2 in 61.53 s**: real Team ping→pong scoped delivery, exact attribution and quiescent stop/restore/continue; real direct/nested Org turns, scoped director→worker delivery, exact IDs/public projection/trace/restore. Log `/tmp/agy-api-r8-team-org.log`. This is not an arbitrary mid-turn termination claim.

## Reconciled current-base cases
Canonical ledger `api-e2e-test-case-ledger.md` entries 72–79 records planned, checkpointed and final results. No case running/unstarted. Earlier API-REV-007 and DR-004 observations retained historically.

| Case | Final | Evidence |
| --- | --- | --- |
| **AGY-15 Team/Org transport** | **Pass** | Real integrated-base GraphQL/WebSocket opt-in E2E **2/2** after fixture correction; exact member attribution/scoped delivery and quiescent restore. `/tmp/agy-api-r8-team-org.log`. |
| **AGY-12 full 18-placement browser Org** | **Pass** | Current `pnpm -C autobyteus-server-ts build` output, isolated migrated SQLite/backend, Nuxt and Chrome. Browser selected real workspace, clicked Run and reached active 1 root/3 Teams/14 Agents in **5.316 s**. One delayed real `agy models` call; simultaneous health **1/1/1 ms**. Distinct backend A PID 73154→B PID 77057 on same isolated root returned exact same configured execution tree/member IDs. `api-e2e-round8-integrated-browser/evidence.json`, active screenshot, `/tmp/agy-api-r8-large-org.log`. |
| **AGY-13/14 rendered error** | **Pass** | Controlled external CLI timeout returned addressed safe `role=alert` and Run-button reset in **15.803 s**, health **2/1/1 ms** while pending. Child nonzero failure rendered distinct safe discovery-failed alert; parseable catalog missing selected slug rendered ordinary model-unavailable alert; no raw private marker in UI. Same round-8 browser evidence/screenshots. |
| **AGY-09/10 explicit backend restart/focus/continue** | **Pass** | Real backend A PID 88563 cleanly exited before distinct B PID 88768 on same isolated root. Fresh Chrome clicked same Team `/second`, Org `/director`, nested `/team/worker`. Each selected *visible conversation feed* contained old prompt+reply marker twice **before** new send, then old and new prompt+reply markers twice each after real AGY continuation; exact public projections and provider/member IDs remained stable. `api-e2e-round8-integrated-restart-browser/evidence.json`, three screenshots, `/tmp/agy-api-r8-restart-browser-final.log`. No stubbed member delivery. |
| **AGY-15 focused/current non-AGY regression** | **Pass, bounded** | Current server production build/bootstrap Pass; ten server/API/admission suites **77/77**, five web Org/Team/model config suites **39/39**. Current generic AutoByteus model-selection/application paths covered; real LMStudio provider case from API-REV-004 is earlier-basis only, **not freshly rerun**. `/tmp/agy-api-r8-build.log`, `/tmp/agy-api-r8-server-regression.log`, `/tmp/agy-api-r8-web-regression.log`. |

## Execution details and cleanup
- Instructions: `autobyteus-server-ts/AGENTS.md`, server README/package scripts, `autobyteus-web/AGENTS.md`, web README/ARCHITECTURE/package scripts. Server Vitest `run ... --no-watch`; web `test:nuxt ... --run`; project Nuxt/browser development path used for web-equivalent desktop behavior.
- Exact main commands: `pnpm -C autobyteus-server-ts prepare:shared`; DR-004 opt-in E2E above; `pnpm -C autobyteus-server-ts build`; `RUN_AGY_E2E=1 AGY_E2E_OUTPUT_DIR=<absolute ticket round8 integrated-browser dir> node autobyteus-web/tests/e2e/agy-large-org-launch-health-probe.mjs`; same opt-in command using `agy-process-restart-focus-continuation-probe.mjs` and separate restart-browser output dir; focused server ten-file Vitest command and web five-file command recorded in the corresponding `/tmp/agy-api-r8-*-regression.log` and investigation/ledger. `node --check` on changed `.mjs` tests and `git diff --check` passed.
- macOS, Node 22.23.1, local AGY 1.2.10/auth, headless Chrome at 1440×1000, dynamic loopback ports, isolated SQLite/app-data/workspace. The large-Org positive uses actual AGY delegated through a test-owned external wrapper delayed by 2.5 s to prove health overlap; timeout/nonzero/missing-model modes emulate only the CLI dependency, not GraphQL/backend/browser. Browser errors empty. Current Electron shell package was **not** rebuilt or accepted by API/E2E; the accepted DS-005 web-equivalent/current-backend surface was directly tested.
- Owned backend A/B, Nuxt, Chrome, migrated test databases and workspaces were stopped/removed by both probes; round-8 logs/JSON/screenshots retained. User's running packaged app, original Org and normal data were untouched. Generated untracked shared SDK `dist` was removed after final validation and not staged. No finalization, release, deployment or user verification done.
- Earlier Team terminate `success:false` while `/ping` had `hasOpenExecutionWork=true` is retained in API-REV-007 logs; current Team restore test waits quiescence and passed. Arbitrary mid-turn Team termination remains unclassified/not accepted. Other AGY versions, a fresh real LMStudio provider journey and actual rebuilt Electron packaging are residual outside API/E2E proof.

## Confidence scorecard and decision
Post-fixture-repository before fresh browser work **86%**. Broader validation **Required and executed** for the current integrated base. Final categories: requirement/AC proof **95%**, changed-boundary directness **95%**, integration/mock gap **95%**, environment/config/identity **95%**, failure/edge/lifecycle **95%**, user-surface/browser **100%**, durable regression quality **95%**. Simple mean **96%** rounded; no category below 90 and every current critical acceptance criterion directly passed. Remaining risks are bounded above; this is not Electron-package or user acceptance.
- **Added tests:** None this round. **Updated durable tests:** `autobyteus-server-ts/tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` (remove obsolete import/env cleanup); `autobyteus-web/tests/e2e/agy-large-org-launch-health-probe.mjs` (output-dir override/current commit provenance); `autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` (output-dir override). **Removed tests:** None. The removed import/cleanup served only the deleted latest-base internal endpoint module; no assertion was removed. Proportional review of these changed test paths is **Required** on the reviewed route; CRR-014 covered only their pre-merge state.
- Preliminary classification of DR-004: **Local Fix — API/E2E-owned fixture**, now resolved at current test collection/execution boundary. No new source failure, Requirement Gap, Design Impact or unresolved blocker. Latest result **Pass / 96%**; next recipient per handoff rules `/code_reviewer` for proportional changed-test review, then Delivery can resume DR-004 and build a replacement Electron artifact. Preserve Delivery's explicit user-verification hold.
