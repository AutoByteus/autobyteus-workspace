# API/E2E Execution Coverage Report — AGY runtime

## Current result and authority
- **API-REV-002**, 2026-09-24, reviewed **Large / High** route, worktree HEAD `b2e91b83f`; triggered by IR-004 / CRR-004 source Pass. Prior authoritative API-REV-001: **Fail / 74%**. Current authoritative result: **Fail / 81%**. This is not delivery acceptance.
- Approved authority: `requirements-doc.md` SR-021 (REQ-001–011, AC-001–010), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md`, `investigation-result.md`, `design-review-report.md` ARCH-REV-003, `architecture-review-revision-record.md`, `implementation-handoff.md` IR-004, `implementation-revision-record.md`, `code-review-report.md` CRR-004, `code-review-revision-record.md`. Relevant provider/trace supplements remain in this ticket. API authority: `api-e2e-coverage-investigation.md`; case checkpoint: `api-e2e-test-case-ledger.md`; history: `api-e2e-revision-record.md`; focused evidence: `api-e2e-round2-live-evidence.json`. Delivery artifacts: N/A.
- **API-F-001 resolved at execution boundary**: the prior real AGY Team GraphQL creation rejection no longer occurs. A real AGY Team member→member scoped MCP roundtrip and exact Team restore/continuation now pass. A distinct critical Org native stream defect is exposed below.

## Critical failure — API-F-002
**Fail — AC-002, AC-008 / SCN-002 Org execution and stream.** The new direct Org half of the opt-in durable GraphQL/WebSocket test creates an AGY Org with direct `/director` and nested `/team/worker` AGY placements and selected workspace. `createAgentOrgRun` succeeds, confirming the IR-004 current run-tree admission repair. A real `/ws/agent-org/:orgRunId` connection then receives `CONNECTED` followed by `ERROR AGENT_ORG_STREAM_UNAVAILABLE` instead of `ROOT_EXECUTION_VIEW_SNAPSHOT`. The error pinpoints `root_org.execution_tree.rootOrg.defaultLaunchConfiguration.runtimeKind` and direct/nested member launch configs: the stream DTO accepts only `autobyteus`, `claude_agent_sdk`, `codex_app_server`, not `antigravity_cli`. Source `autobyteus-collaboration-stream-contracts/src/agent-org-execution-dtos.ts:13` hardcodes the old enum. The Org WebSocket closes before member command/response/trace, so real Org execution and scoped recipient delivery cannot be claimed. This is a **preliminary Local Fix, implementation-owned**, requiring Code Reviewer focused failure-origin confirmation. Do not widen the test fixture or count stubbed Org MCP as a real recipient.

Exact failing command (from worktree): `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch -t 'launches direct and nested AGY Org'`; `/tmp/agy-api-r2-org-handshake.log` and final durable-code rerun `/tmp/agy-api-r2-org-final.log` (1 failed, 1 skipped, after prior fixture repairs). The durable test remains ready to rerun after source repair. The native Org stream failure is not an AGY CLI/auth/environment blocker: GraphQL launch succeeds and the WebSocket itself reaches CONNECTED. Earlier Org fixture `{}` llmConfig and premature-command handshake errors were corrected and not classified as implementation failures.

## Real Team parity and restore result
- AGY-02 **Pass**. `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch` at `b2e91b83f` first passed its real Team test (1/1, 32.59 s; `/tmp/agy-api-r2-team.log`). Two AGY member runs were published. Ping invoked real AGY `call_mcp_tool` → AutoByteus `send_message_to` for `/pong`; correlated tool start/success, exact sender/recipient run IDs and content in TEAM_COMMUNICATION_MESSAGE, pong TURN_COMPLETED, and the token in both persisted projections passed. No delivery stub was injected.
- AGY-07 **Pass**. The same durable test was extended and rerun: terminate Team; exact restore of both member IDs, AGY provider conversation IDs, model/runtime/workspace bindings; send fresh post-restore input to pong; require pong-attributed TURN_COMPLETED and new `ACK-RESTORED` text in persisted projection. The strengthened Team half passed in 25.86 s (`/tmp/agy-api-r2-team-org-corrected.log`). This directly closes Team continuation; it does not close Org stream.
- AGY-05 **Fail** on Org native stream as above. GraphQL Org creation itself is Pass, but it is not equivalent to executable Org member turns or real scoped Org MCP delivery.
- AGY-08 **Pass, bounded**. Focused admission 3/3, runtime capability GraphQL 1/1, Org stream unit 10/10, AGY converter/capsule 13/13, web store/editor/tool 48/48. The mixed-team live provider test was skipped by its guard, not counted as pass. Logs `/tmp/agy-api-r2-focused.log`, `/tmp/agy-api-r2-server-regression.log`, `/tmp/agy-api-r2-web-regression.log`. Passing Org stream unit tests do not include an AGY snapshot and therefore miss API-F-002.

The ledger records every round-2 attempt, fixture correction and completed case. Prior AGY-01/03/04/06 results remain historical API-REV-001 evidence, not falsely counted as fresh round-2 runs.

## Requirement/acceptance matrix
| AC / boundary | Current evidence | Result |
| --- | --- | --- |
| AC-001 availability/model | Current capability GraphQL 1/1; AGY CLI 1.2.10 and live model selected by catalog | Pass for tested host/version. |
| AC-002 generated Team/Org identity, real scoped delivery | Real Team ping→pong GraphQL/WebSocket and persisted exact attribution pass. Direct/nested AGY Org GraphQL launch succeeds but native snapshot rejects AGY | **Fail critical for Org**; no real Org recipient claim. |
| AC-003 exact binding/restore | Fresh Team terminate/restore exact provider IDs and response, plus API-REV-001 standalone exact restore/mismatch | Pass for tested Team/standalone. |
| AC-004 selected workspace/capsules | Team/Org config selected workspace; prior live file/shell selected workspace and capsule isolation/collision checks | Pass bounded; no filesystem sandbox claim. |
| AC-005 non-AGY preservation | Focused current admission, capability, Org stream unit and web policy/card suites pass; prior 73 server and 55 web non-AGY checks carried | Pass bounded, no real non-AGY provider rerun. |
| AC-006/007 trust/explicit-off | Prior API-REV-001 fresh browser live/reloaded explicit-off **DENIED**, raw ERROR, no approval card; current web store/editor regression pass | Pass carried; no new browser run. |
| AC-008 stream/reload/trace | Team stream and persisted projections pass, prior standalone raw trace/reload pass; Org snapshot emits AGENT_ORG_STREAM_UNAVAILABLE | **Fail critical for Org**. |
| AC-009 draft policy | Current focused Org/agent stores/editor pass; prior fresh browser AGY default-on then explicit-off preserved | Pass bounded; Org runtime stream still fails. |
| AC-010 DONE convention | Prior fresh live/reloaded exit0/exit8/not-found DONE→green SUCCESS, exposed output, no fabricated shell exit; current converter/card suites pass | Pass carried. |

## Execution environment, broader validation, cleanup
- Project instructions: `autobyteus-server-ts/AGENTS.md` and README for single-file Vitest and shared SDK preparation; web AGENTS.md/README for Nuxt test/browser preview. `pnpm -C autobyteus-server-ts prepare:shared` passed (`/tmp/agy-api-r2-prepare.log`), then opt-in live AGY E2Es ran in isolated `startStudioE2eRuntimeServer` app-data/workspace fixtures. macOS host, Node v22.23.1, pnpm 10.28.2, AGY CLI 1.2.10 with existing local auth. No secret value recorded.
- Broader validation decision **Required and executed**: repository-resident but provider-real GraphQL/WebSocket Team and Org E2Es cross actual server, AGY CLI, scoped MCP, persistence and stream boundaries. They revealed API-F-002. No additional browser run was selected for IR-004 because its source delta was backend-only admission; API-REV-001 already captured fresh final-code browser DENIED after reload and DONE green/raw-trace controls. Those browser observations are carried, not newly claimed. Electron shell-specific behavior was unchanged and not launched.
- Owned runtime server, WebSockets, Team/Org runs, definitions, temporary workspace/app-data dirs were stopped/deleted by E2E teardown. Generated untracked SDK dist directories from `prepare:shared` were removed. `/tmp/agy-api-r2-*` logs and ticket JSON evidence retained. No user-running app/process or shared production data was touched; AGY may retain provider-side conversations. `git diff --check` passed. Code Reviewer report edits were pre-existing and untouched.
- The ordinary server `tsc -p tsconfig.json --noEmit` pre-existing rootDir/tests mismatch from API-REV-001 was not repeated or called a pass. Current tests transformed/imported under Vitest. The unrelated historical-candidate `claude_sdk_usage_state_json` fixture failures noted by CRR-004 are not claimed as regression success/failure of IR-004.
- Persisted transition stays **Directly Usable — No Migration**. IR-004 changed the current run-tree predicate only; no version-specific compatibility shim or legacy neutral event was added. Team current-state restore is direct. No released AGY historical population exists; Org stream DTO defect is a current-contract omission, not migration evidence.

## Durable coverage delta and validity
- **Updated** `autobyteus-server-ts/tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts`: retained real Team scoped delivery assertions; added exact Team terminate/restore/continuation and direct/nested Org native GraphQL/WebSocket member/trace assertions with handshake-safe fixture. The Org test fails at the production DTO snapshot before expected member turns. No production source edited, no durable coverage removed. The earlier Codex/Claude parity fixtures remain `Needs Update` for stale TeamMemberInput `refType`; they are not silently counted as AGY evidence.
- Temporary JSON `api-e2e-round2-live-evidence.json` summarizes outcomes and logs; it is not a substitute for durable test assertions. Existing `agy-mcp-team-live.test.ts` uses stubbed delivery and remains lower-boundary evidence only.

## Confidence scorecard
| Category | Post-repository | Final | Final reason |
| --- | ---: | ---: | --- |
| Requirement and AC proof | 60% | 50% | Team/restore and controls direct, but Org AC-002/008 fail. |
| Changed-boundary execution directness | 70% | 95% | Real Team/Org GraphQL/WebSocket and AGY process expose exact boundary; Org fails directly. |
| Cross-boundary realism/mock gap | 50% | 50% | Team gap closed; Org native stream fails before real member delivery; stub not credited. |
| Environment/config/identity/fixture fidelity | 75% | 95% | Actual CLI/auth, catalog model, isolated fixtures, exact Team and Org config; other provider versions remain. |
| Failure/edge/lifecycle/recovery | 75% | 90% | Exact Team restore and prior denial/reload; Org recovery unproven due contract defect. |
| User surface/browser/desktop shell | 95% | 95% | Carried fresh API-REV-001 browser/live-reload evidence; no shell change or new browser run. |
| Durable regression relevance | 70% | 90% | AGY Team and Org opt-in E2E catches successive production omissions; Org post-fix assertions await rerun. |
- Overall: **71% post-repository → 81% final**, simple seven-category mean rounded. Requirement and integration categories are at 50% because a critical Org path fails. Clean threshold (overall ≥95%, each applicable ≥90%, all critical direct proof) is not met; no score can override the Org failure.

## Preliminary classification and route
- **API-F-002 / Local Fix (implementation, pending Code Reviewer origin review)**: current native Org stream DTO launch-configuration enum excludes `antigravity_cli`. Expected CONNECTED → ROOT_EXECUTION_VIEW_SNAPSHOT → accepted direct/nested member commands and attributed AGY presentation/projection; observed CONNECTED → `ERROR AGENT_ORG_STREAM_UNAVAILABLE` with precise runtime-kind validation paths. Recommended repair is within the existing current collaboration-stream contract authority, not a test bypass or migration shim; Code Reviewer determines final owner. Rerun AGY-05 first after repair, then full Team+Org E2E, Org real scoped MCP if supported, and focused regression.
- Current result **Fail / 81%**. Obtain `get_handoff_rules` and send this cumulative package to the returned accountable recipient for focused failure-origin review. No delivery acceptance.
