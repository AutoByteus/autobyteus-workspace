# API/E2E Execution Coverage Report — AGY runtime

## Current result and authority
- **API-REV-005 — Pass / 96%**, 2026-09-25, reviewed Large/High AGY package at `c9c8373e1dc02ca44c285cd96286dacf7708208c`. Trigger: explicit user request to test actual backend shutdown/restart, browser focus of the *same* AGY Team/Org member, and continued message after restart. Prior API-REV-004 was **Pass / 95% for its prior scope**, not evidence for this newly required journey. This report is API/E2E validation, not delivery acceptance or review of new test code.
- Approved authority remains `requirements-doc.md` (SR-021, REQ-001–011, AC-001–010), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md`, `investigation-result.md`, `design-review-report.md`, `architecture-review-revision-record.md`, `implementation-handoff.md`, `implementation-revision-record.md`, `code-review-report.md`, `code-review-revision-record.md` and supplements listed in the prior report/revision record. Canonical current investigation: `api-e2e-coverage-investigation.md`; per-case checkpoint: `api-e2e-test-case-ledger.md`; cumulative results: `api-e2e-revision-record.md`.

## Exact new user journey — direct proof
- **AGY-09 Team — Pass.** Durable `autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` launched backend OS process A (PID **16695**) with an isolated SQLite/app-data root and a real two-member AGY Team. `/second` completed an initial provider-real turn. The probe waited for its attributed `TURN_COMPLETED`, captured the run/provider bindings, sent SIGTERM to only its owned backend A, verified A exited cleanly and its HTTP endpoint was unavailable, then launched a distinct backend OS process B (PID **16771**) against the **same** data root and port. B's public Team resume tree retained exact root, member and provider IDs. A fresh headless Chrome session opened Nuxt `/workspace`, clicked the exact `/second` Team history row, saw its old response, typed and clicked **Send message** in the Team composer, and saw a new AGY response with a unique new marker. Public HTTP `getTeamMemberRunProjection` for that exact member retained both old and new turns. This is neither same-process terminate/restore nor a stubbed provider/recipient.
- **AGY-10 Org — Pass, direct and nested.** The same A→B transition used a real AGY Org root with direct `/director` and nested Team `/team/worker`; both completed initial AGY turns on A. B's public Org config retained exact root, direct/nested member and provider IDs. The fresh browser clicked the direct member, sent a new message and saw old/new replies; it then expanded the nested Team, clicked the exact worker row, sent another new message and saw old/new replies. Public HTTP `getAgentOrgMemberRunProjection` retained both markers under each exact root/address/agent-run ID. No stub, same-process restore, URL-only focus, or API-only continuation was substituted for the browser clicks.
- Evidence: `api-e2e-round5-process-restart-browser/evidence.json` (PIDs, selected IDs, cases, data-root identity, cleanup), `team-after-restart.png`, `org-after-restart.png`, `org-worker-after-restart.png`, backend A/B, Nuxt and migration logs. Executed command from assigned worktree: `RUN_AGY_E2E=1 node autobyteus-web/tests/e2e/agy-process-restart-focus-continuation-probe.mjs` → **Pass** (final `/tmp/agy-r5-probe-checked-final.log`). AGY CLI **1.2.10**, Node **22.23.1**, real local AGY authentication and model `gemini-3.8-flash-low`; no credential values recorded.

## Repository and regression execution
| Case/check | Result | Evidence and bound |
| --- | --- | --- |
| Server build | **Pass** | `pnpm -C autobyteus-server-ts build` generated current `dist/app.js`, Prisma client and sanitized bootstrap; `/tmp/agy-r5-build.log`. |
| New browser/process probe | **Pass** | Real A→B backend OS transition, three exact member browser focus/send/reply paths, both Team/Org old+new public projections; evidence above. Early fixture-only Team snake_case tree decoding, WebSocket command ID and Team surface-selector errors were corrected before the final passing run; ledger events 53–55 preserve them. |
| AGY-11 full provider-real Team+Org | **Pass on final rerun, with intermittent residual** | `RUN_AGY_E2E=1 pnpm -C autobyteus-server-ts exec vitest run tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts --no-watch` → **2/2** in 51.60s (`/tmp/agy-r5-agy-regression-final.log`); focused Team rerun **1/1** (`/tmp/agy-r5-team-rerun.log`). Both retain the real scoped Team/Org delivery and same-process restore coverage from API-REV-004. |
| Focused renderer/history regression | **Pass 80/80** | `pnpm -C autobyteus-web exec vitest run components/workspace/history/__tests__/HistoricalTeamLazyHydration.integration.spec.ts components/workspace/history/__tests__/WorkspaceAgentRunsTreePanel.spec.ts components/workspace/org/__tests__/AgentOrgWorkspaceView.spec.ts --no-watch`; `/tmp/agy-r5-web-regression.log`. Expected mocked network-failure stderr did not fail assertions. |
| Syntax/diff | **Pass** | `node --check` on new durable probe and `git diff --check`. |

**Unresolved intermittent observation:** The first round-5 full AGY regression had Org pass but Team fail at `terminateAgentTeamRun.success=false`, with `AgentTerminationError: ... is not the current published run` and supervisor-close AggregateError (`/tmp/agy-r5-agy-regression.log`). This was *not* a cross-process/browser failure: the new A→B test passed without a Team terminate mutation. Focused Team and subsequent full Team+Org reruns passed; prior API-REV-004 also passed multiple times. Exact cause is **not established**, so this is retained as a bounded lifecycle residual and referred for independent test-code/failure-origin review; no product fix or test workaround is claimed. Do not infer arbitrary mid-turn termination succeeds. The similar prior Org stop `success:false` observation remains historically recorded.

## Coverage decisions and residuals
- New durable browser/process E2E was **added**; no production source, old durable test or approved behavior changed in this round. It uses real public GraphQL, Team/Org WebSockets and Chrome/Nuxt; AGY initial and continuation turns are provider-real. Existing AGY Team/Org suite is **Still Valid**, with one intermittent same-process termination observation. Prior final-code DENIED/DONE, standalone exact restore/workspace/skill/collision and real non-AGY LMStudio Team evidence from API-REV-001/004 remains carried on unchanged source; no new Codex/Claude provider-real run is claimed.
- Persisted-data transition remains **Directly Usable — No Migration**. The fresh B process read A's exact Team/Org current data without migration or identity substitution. This is stronger process-recovery evidence than prior same-process restore. It does not prove an Electron-shell lifecycle, crash/SIGKILL recovery, or a second AGY CLI version. Those are bounded out-of-scope residuals, not claimed passes.
- Isolated app-data/SQLite/workspace under owned OS temp root were removed. Both owned backend processes and Nuxt/Chrome exited; pre-existing user AutoByteus/LMStudio processes were untouched. Evidence/logs/screenshots retained in the ticket. Build-generated untracked shared SDK `dist` directories are removed after execution; no unrelated delivery docs were edited.

## Confidence scorecard and decision
| Mandatory category | Post-build / repository | Final | Basis / uncertainty |
| --- | ---: | ---: | --- |
| Requirement and AC proof | 90% | **95%** | Explicit cross-process Team and direct/nested Org focus/send/response added to prior real scoped delivery and restore; intermittent Team terminate remains. |
| Changed-boundary execution directness | 90% | **100%** | Separate backend PIDs, same real persisted data, fresh browser clicks and provider-real replies. |
| Cross-boundary realism/mock gap | 90% | **100%** | Real GraphQL, WebSocket, AGY, Chrome and Nuxt; no stub parity. |
| Environment/config/identity/fixture fidelity | 90% | **95%** | Current built server, isolated SQLite and exact root/member/provider binding; other hosts and AGY versions not covered. |
| Failure, edge, lifecycle and recovery | 75% | **90%** | Clean A→B recovery for Team and Org; unexplained intermittent Team termination in separate suite prevents higher score. |
| User surface/browser/desktop shell | 75% | **100%** | Fresh browser click/focus/composer and old/new response in Team, direct Org and nested Org; Electron shell remains inapplicable to web-equivalent journey. |
| Durable regression relevance and quality | 90% | **95%** | Checked-in-process browser probe plus existing real scoped E2E and 80 web regressions; intermittent terminate needs future investigation. |
- Simple mean **86% post-build/repository → 96% final** (rounded). Every new critical user scenario has direct passing evidence; no applicable final category below 90%. Broader validation was **Required and executed**. **Current result: Pass / 96%**, explicitly bounded by the intermittent unrelated-to-process-restart termination observation and pending proportional review of new test code. Route reviewed Large/High Pass to Code Reviewer with that observation for independent evaluation; no delivery acceptance claimed.
