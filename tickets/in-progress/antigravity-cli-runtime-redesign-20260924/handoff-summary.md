# User Verification Handoff — AGY CLI Runtime

## Delivery state

**Ready for explicit user verification, not yet finalized or released.** The approved **Large / High** package followed architecture review, independent source review, API/E2E validation and independent changed-test-code review. Latest API-REV-006 passed at **96%** and CRR-012 proportional test-code review passed; CRR-009 remains the source-review authority. The previously reported production GraphQL schema-cache defect attribution was withdrawn: the E2E fixture's second full schema build caused that diagnostic, and the production cache was removed. Current real-server tests use the running server's HTTP GraphQL with one schema build.

The ticket branch `codex/antigravity-cli-runtime-redesign-20260924` is locally integrated with tracked `origin/personal@fdbd07124f0eaaa1310379c1ca8e2449f97b0dcf` by merge `c9c8373e1dc02ca44c285cd96286dacf7708208c`; reviewed initial test/report changes were protected in local checkpoint `9216b64263afaf81ae1bc12121856d14efe3a89b` first. No conflict occurred. After `prepare:shared`, the merged real AGY Team/Org GraphQL/WebSocket E2E passed **2/2** (`/tmp/agy-delivery-postmerge-e2e.log`). The first post-merge attempt had no test collection because generated shared SDK output was missing; preparation corrected that environment-only precondition. At DR-002 re-entry, a fresh `git fetch origin personal` found **no new base commits**, so no base-triggered rerun was necessary. The final A→B real backend/browser journey ran against this same integrated production source. Delivery docs now describe that evidence (`docs-sync-report.md`). Delivery-owned docs/this summary and CRR-012's latest review artifacts are not yet final-committed or pushed, as required by the user-verification hold.

## User-requested clean restart and continuation — now verified

The opt-in durable browser test started actual AGY Team `/second`, direct Org `/director` and nested Org `/team/worker` members under backend process **A (PID 22997)**, cleanly stopped A, then started distinct backend process **B (PID 23069)** on the **same isolated data root**. A fresh Chrome/Nuxt browser selected each member from persisted history. Each selected visible conversation showed the **old user prompt and old AGY answer before Send**, then retained them alongside the **new prompt and new AGY answer after Send**: old-before **2**, old-after **2**, new-after **2** marker occurrences for **each** of all three member feeds. Exact root/member/provider identities and public GraphQL projections remained stable. This was the user's explicit restart→focus→continue journey, not same-process terminate/restore or API-only inference. Final evidence: `api-e2e-round5-process-restart-browser/evidence.json`, `/tmp/agy-r6-probe-final.log`, API-REV-006 and CRR-012. The earlier TR-001 test assertion gap is resolved; no production source was changed in this round.

## Local Electron test artifact for your verification

Per the user's 2026-09-25 request, root and web READMEs were read and a local unsigned macOS **arm64 personal-flavor** build completed from this integrated ticket branch. The DMG is `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/autobyteus-web/electron-dist/AutoByteus_personal_macos-arm64-1.4.79.dmg` (SHA-256 `f6272f29f1844d922c324de11064e393d137b14601ae55b18f43b6fd32d9bd0b`). `hdiutil verify` passed; an isolated packaged launch reached backend health and shut down cleanly. See `electron-test-build-report.md` for command, ZIP alternate, unsigned/notarization warning, normal-data-path caution and AGY CLI PATH guidance. This is a test artifact, **not** a new `v1.4.79` release or proof that the user's manual Electron/AGY journey passed.

## What to verify

Use a disposable real workspace and an available/authenticated `agy` CLI; the reviewed provider version was **1.2.10**.

1. Start a new standalone AGY Agent. Check that the new editable selection defaults **Auto approve tools** on, the Agent can respond, and ordinary file/shell work targets the selected real workspace rather than a generated capsule. This is model-directed behavior, not a sandbox guarantee; avoid destructive commands.
2. Set the AGY switch explicitly **off** on a new run and ask for a permission-gated action. A denial should remain non-green and show **DENIED** live and after reload; there is no in-chat approval response for the headless AGY run.
3. Inspect a completed AGY tool card/Activity item live and after reload. `DONE` should be green with provider output, but must not claim a shell exit code or independently verified command success; explicit error/denial remains non-green.
4. If Team/Org collaboration is part of your intended use, inspect the above A→B evidence or repeat a simple member interaction in your environment. The automated journey verifies **clean** restart and old/new replies in Team, direct Org and nested Org browser feeds. Stop only after work is quiescent; mid-turn termination and crash/SIGKILL recovery remain unproven.

After testing the local Electron artifact, please report **explicitly** whether this integrated handoff is accepted for finalization, with any failures and whether a versioned release is requested. A request to build for testing is **not** acceptance or a request to publish. No release/tag/deployment is inferred from a passing build or automated test alone.

## Evidence and bounds

- Latest API-REV-006 real clean A→B backend/fresh Chrome continuation: **Pass / 96%** for Team, direct Org and nested Org old/new visible replies; CRR-012 changed-test-code review **Pass**. Final pre-delivery AGY real Team+Org: **2/2**, exact scoped MCP delivery and public Org member projection/trace/reload; selected real AutoByteus/LMStudio Team: **1/1** (290s/300s). Contract/focused server/web suites: **8/8**, **32/32**, **78/78**, **73/73**, **55/55** in API-REV-004. Delivery's integrated-base AGY rerun: **2/2**.
- Fresh Team/Org **web-equivalent browser** journey has now been run; an Electron-shell run and current live Codex/Claude provider reruns were **not** performed. API-REV-001 standalone browser DENIED/DONE controls are carried, not fresh. An earlier pre-quiescence Org stop failure and an intermittent later Team terminate failure were not origin-proven; current quiescent stop/restore and clean A→B process restart pass, but arbitrary mid-turn termination is not claimed. LMStudio timing has only 10 seconds of case-timeout headroom.
- Persisted data: **Directly Usable — No Migration**. No old released AGY runs need conversion. AGY provider-side conversations may remain outside AutoByteus cleanup.
- Detailed authority/evidence: `requirements-doc.md`, `design-spec.md`, `implementation-handoff.md`, `code-review-report.md`, `api-e2e-execution-coverage-report.md`, `api-e2e-test-review-report.md`, `docs-sync-report.md`, and `release-deployment-report.md` in this ticket folder.

## Gate

User verification: **Awaiting explicit result after Electron test.** Ticket remains `tickets/in-progress`; ticket-branch push, final-target merge/push, release/tag/deployment and worktree cleanup have **not** occurred. Finalization target is recorded as `origin/personal`; fresh fetch on 2026-09-25 still found `fdbd07124f0eaaa1310379c1ca8e2449f97b0dcf`. The local test DMG/ZIP stay in the worktree during this hold. Any new remote-base advance after user acceptance requires another integration check and renewed acceptance if the user-facing state materially changes.
