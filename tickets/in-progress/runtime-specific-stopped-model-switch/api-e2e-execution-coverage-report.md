# API/E2E Execution Coverage Report — Runtime-specific stopped model switching

## Latest authoritative round and route
**API-REV-004 / round 4: Pass, 94.3% final validation confidence.** Trigger: CRR-008 independent source Pass on IR-005 commit `2c4699a14`. Prior API-REV-003 was **Fail/82.1%**, F-API-003 saved Application Setup restore open; F-API-002 Agent definition→Run was already closed by API-REV-003 real Chromium. The prior failure is retired below by **fresh direct browser proof**, not source/component review alone. Classification remains **Medium / High**, reviewed route. Broader validation **Required and executed**. Default 95% clean target is narrowly unmet with bounded real-provider/mobile-device residuals; no applicable category is below 90 and no critical acceptance scenario remains failing.

Worktree: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch`. Canonical ticket: `tickets/in-progress/runtime-specific-stopped-model-switch/`. The latest `api-e2e-coverage-investigation.md`, this report, `api-e2e-test-case-ledger.md`, and cumulative `api-e2e-revision-record.md` are the API/E2E authority. Cumulative upstream authorities: `requirements-doc.md` (SR-006 REQ-001–008/AC-001–011), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md` (SR-009), `model-picker-verification-investigation.md` (supplemental), `architecture-design-result.md`, `architecture-design-revision-result.md`, `architecture-design-recovery-result.md` (MP-002), `design-review-report.md`, `architecture-review-revision-record.md` (ARCH-REV-003), `implementation-handoff.md`/`implementation-revision-record.md` (IR-005), `code-review-report.md`/`code-review-revision-record.md` (CRR-008). Prior API-REV-001 Pass/94.3% was an older approval basis; API-REV-002/003 failures and resolutions remain indexed, not overwritten.

## Prior failure resolution — F-API-003 closed
**Expected:** Application Agent and Team Launch Setup can save a resource inheriting exact saved Claude `default`, reopen in a new browser page, display the saved selection/current identity and model config, and remain `VALID`/`RUNNABLE` without cloning a Vue Proxy or rewriting the ID. This is ARCH-REV-003 MP-002/BEH-007 preserved normal behavior. API-REV-003 had proven a real Chromium `DataCloneError`/Nuxt 500 on saved Agent restore at `applicationLaunchProfile.ts:156` despite valid REST persistence.

**Fresh executable result:** with new isolated built backend+Nuxt on dynamic loopback ports, public `/Users/normy/autobyteus_org/autobyteus-agents` import and a test-owned `RUNNABLE` Application copy, real Chromium performed:
1. Applications list → test-owned Setup; select a shared Agent definition seeded `claude_agent_sdk/default` and `reasoning_effort=high`. Browser baseline showed exact default. **PUT Save 200**; public REST readback returned savedOverrideState `VALID`, effective leaf `claude_agent_sdk/default` and readiness `RUNNABLE`.
2. Close the page; open the same Application in a **new page**. Saved Agent resource remained selected; exact default and `VALID` displayed; exact-current `RuntimeCurrentModelDescriptors` requested `default`; **no Error 500/DataCloneError or page error**.
3. Select a shared Team definition seeded exact Claude `default` with non-null config. **PUT Save 200**; REST readback `VALID`, effective Team leaf `claude_agent_sdk/default`, readiness `RUNNABLE`. Close/reopen in another **new page**: saved Team resource and default remained displayed, no clone/browser error. Agent saved state also remained intact.

Authoritative direct evidence: `probes/rev004-application-fixture.json`, `rev004-default-definition.json`, `rev004-default-team-definition.json`, `rev004-application-browser-evidence.json`, `rev004-app-agent-saved.png`, `rev004-app-agent-reopened.png`, `rev004-app-team-saved.png`, `rev004-app-team-reopened.png`. F-API-003 is **resolved at the actual previously failing browser boundary**. The package was an isolated copied Brief Studio importable package with optional shared Agent/Team slots and no defaults; only the copy inside this run's owned temporary runtime was changed/imported, because the stock required-slot fixture was quarantined before setup in API-REV-003. No user package/source manifest or indexed local saved-default record was modified.

## Case-ledger reconciliation and changed-boundary proof
| Case | API-REV-004 result | Evidence / precise limit |
| --- | --- | --- |
| API-E2E-007 | **Pass** | `pnpm -C autobyteus-web test:nuxt components/applications/__tests__/ApplicationLaunchSetupPanel.spec.ts components/applications/setup/__tests__/ApplicationAgentLaunchProfileEditor.spec.ts components/applications/setup/__tests__/ApplicationTeamLaunchProfileEditor.spec.ts --run`: 3 suites / **13 tests**. Includes real parent/slot reactive saved Agent exact-default and Team Save→fresh mount regressions; `api-e2e-rev004-app-focused.log`. |
| API-E2E-008 | **Pass** | Built-server GraphQL E2E 2 files/**3 tests**, stopped Agent/Team exact config restore and Org atomic save/restart, `api-e2e-rev004-graphql.log`. Backend source unchanged by IR-005. |
| API-E2E-009 | **Pass, fresh focused spot check** | Public imported package, test-owned stopped Daily Assistant Agent, Classroom Simulation Team, Software Development Department Org. GraphQL current exact `default` descriptor separated from ten normalized Claude replacements; Codex/native options also returned. `probes/rev004-seed-evidence.json`. API-REV-002 provider snapshot, same-model Settings Save, changed-default rejection and Codex CWD equality remain current SR-009-basis evidence, **not rerun** this round. |
| API-E2E-010 | **Pass** | Real Chromium Application Agent and Team Save→new-page restore above; authenticated paired mobile browser setup below. F-API-002 remains closed by API-REV-003 direct Agent card→Run proof; IR-005 did not touch that code. |
| API-E2E-011 | **Not rerun this round** | API-REV-002's real Claude exact-default ALPHA/BETA same-session continuation, preserved prior history/trace at Save and Codex CWD check remain valid under unchanged backend/provider source. No new or universal smaller-window success claimed. |
| API-E2E-012 | **Pass** | Nuxt production build (including `/applications` and `/mobile` prerenders), repository deterministic browser E2E **6/6**, owned cleanup and `git diff --check`. `api-e2e-rev004-web-build.log`, `api-e2e-rev004-durable-browser.log`, `probes/rev004-durable-browser/existing-run-model-config-evidence.json`. |

**Mobile browser follow-on:** Phone Access was enabled only on the owned isolated backend; a real REST pairing session was exchanged in Chromium. Since server policy correctly rejects loopback as a phone-facing URL, Chromium mapped a test-only trusted `.local` alias to the owned `127.0.0.1` backend with a host-resolver rule—**no LAN listener** and no claim of real phone-network fidelity. Authenticated mobile Home/catalog loaded. The test-owned saved-default Agent definition's Runs setup retained Pinia seed/config exact `default`, requested exact-current descriptor, and displayed canonical Claude label/schema; switching to the saved-default Team definition retained its root exact `default` and displayed the same. Evidence: `probes/rev004-mobile-browser-evidence.json`, `rev004-mobile-home.png`, `rev004-mobile-agent-setup.png`, `rev004-mobile-team-setup.png`. **No mobile Create Run** was pressed because no workspace was intentionally selected; mobile provider launch/real device remain untested. Pairing codes/credentials are not persisted in evidence.

IR-005 changed only Web Application launch-profile cloning; it did not change backend catalog/Save policy, GraphQL DTOs, stopped Agent/Team/Org Settings, provider history or the IR-004 Agent Run fix. API-REV-002 direct live browser Settings Saves, normalized provider snapshot/current descriptor, explicit switches, changed-default rejection, real Claude default continuation/history and Codex process-CWD/workspace seven-ID equality are carried on the same SR-009 approval basis, not misrepresented as fresh API-REV-004 executions. No actual Codex workspace-CWD divergence has been evidenced in this environment. The five indexed local saved-default records were never mutated. Persisted decision remains **Directly Usable — No Migration**; no dual DTO, version-specific compatibility branch or compatibility-only durable test was introduced.

## Confidence scorecard and broader-validation decision
The focused 13/13 repository checks preceded broader work. Post-repository scores: requirement 90, changed-boundary directness 90, integration realism 75, environment fidelity 90, failure/lifecycle 90, user/browser surface 85, durable quality 90 = **87.1%** (610/7). Direct browser validation was **Required** because API-REV-003 failed only on a reactive saved override in the real renderer. It closed that gap and added paired mobile setup proof.

| Mandatory category | Final score | Supporting evidence / bounded residual |
| --- | --- | --- |
| Requirement and AC proof | 95% | MP-002 Agent/Team saved exact-default restored in real browser; stopped core AC from current SR-009 basis. No universal provider acceptance is required. |
| Changed-boundary execution directness | 95% | Actual prior failing Application browser sequence, new-page reopen and REST readback. |
| Cross-boundary integration realism/mock gap | 95% | Public package import/GraphQL/REST, backend+Nuxt+Chromium and authenticated mobile setup; no mock on critical Application path. |
| Environment/configuration/identity/fixture fidelity | 95% | Isolated built backend, real package definitions and Claude catalog, test-owned saved definitions/Application, controlled phone pairing. Mobile hostname emulated. |
| Failure, edge-case, lifecycle and recovery | 90% | Real failed-before/fixed-after restore, Agent/Team Save, built restart/atomic tests, prior provider rejection/history; no numerically verified smaller-window provider pair or mobile execution. |
| User surface, browser and desktop-shell | 95% | Real desktop-web-equivalent Application and paired mobile browser render/state checks; Electron shell/real phone network unaffected/unrun. |
| Durable regression quality/relevance | 95% | IR-005 reactive parent/slot tests reproduce the former Proxy boundary, plus descriptor GraphQL 3/3 and deterministic browser 6/6; credentialed full-stack browser cannot run as ordinary CI here. |

Simple mean **94.3%** (660/7); no category below 90%. Default 95% clean target: **No**. The remaining gap is bounded to real-device/mobile Create Run and externally verified numeric smaller-window provider behavior. The targeted executable browser/API/build paths most likely to improve confidence were run; neither residual is an approved guarantee or a failing critical AC. Overall **Pass with bounded residual**; no claim that every provider can continue every history on every smaller model.

## Durable coverage, environment and cleanup
- API/E2E made **no new durable test edit** in API-REV-004. IR-005 added/updated `autobyteus-web/components/applications/__tests__/ApplicationLaunchSetupPanel.spec.ts`; the current round independently reran it with Agent/Team editor suites. API-REV-002's API/E2E-owned durable updates remain pending proportional test-code review on this reviewed route: `autobyteus-server-ts/tests/e2e/run-history/stopped-run-model-config-graphql.e2e.test.ts` (replace obsolete ID-only schema assertion with current/replacement/exact-current descriptors) and `autobyteus-web/tests/e2e/existing-run-model-config-probe.mjs` (normalized descriptor/current-only browser fixture). No durable path removed. The test-code reviewer should review **only those API/E2E-owned changed durable files** proportionately, not reopen this API confidence result.
- New owned stack: backend `127.0.0.1:54039`, Nuxt `:54043`; `probes/live-stack.mjs` used `test-support/live-e2e/test-runtime-bootstrap.mjs`, with unique runtime `autobyteus-server-ts/tests/.tmp/runtime-switch-browser-36286-1790364469635` and SQLite `db/runtime-switch-browser-36286-1790364469635.db`. Local Chrome/Chromium headless Playwright, desktop 1440×900 and mobile 390×844, English locale. Browser work exercised web-equivalent Electron renderer and mobile web, not native IPC/window/packaging.
- Before teardown, revoked all four test-created paired devices and disabled Phone Access in owned backend. Stopped only run-owned backend/Nuxt, verified PIDs gone and unique runtime/SQLite/info absent. Removed this round's generated Brief Studio/devkit/frontend-SDK `dist` outputs; preserved pre-existing shared SDK `dist` and all other user state. Deterministic browser probe closed its own browser/Nuxt. `git diff --check` passed. Temporary scripts and credential-free JSON/screenshots/logs retained under `probes/` for audit. No shared user DB, provider credential store or desktop app was stopped/reset. Full Web typecheck remains unclaimed due upstream `vue-tsc`/TypeScript toolchain issue; production build and affected suites passed.

## Routing
**Pass**, Medium/High reviewed route. Call `get_handoff_rules` and send the cumulative package to the exact returned recipient (expected `/code_reviewer`) for **proportional test-code review** of the two API/E2E-owned durable test changes. This is not failure-origin review; F-API-003 and F-API-002 are closed by direct evidence. Attach all upstream/current artifacts and changed durable files. The reviewer then records its separate test review; Delivery and user verification remain downstream, not claimed here.
