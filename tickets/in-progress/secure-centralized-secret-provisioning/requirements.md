# Secure Centralized Secret Provisioning — Requirements

## Status (`Draft`/`Design-ready`/`Refined`)

`Refined — Original Gemini Metadata Preservation Reconciliation; Architecture Re-review Required` (2026-07-22). Direct comparison with `origin/personal` and code-review round 25 correct the earlier CR-021 premise. Original Gemini LLM/media construction is mode-specific: AI Studio uses `GoogleGenAI({apiKey})`, Vertex Express uses `GoogleGenAI({vertexai:true,apiKey})`, and Vertex Project uses `GoogleGenAI({vertexai:true,project,location})`. Original live metadata is intentionally a different contract: `GeminiModelMetadataProvider` accepts either `GEMINI_API_KEY` or `VERTEX_AI_API_KEY`, calls the same Generative Language models endpoint, and retains curated fallback; Vertex Project has no key-backed live metadata branch. The current implementation preserves that metadata behavior while moving key selection/resolution to Store-backed semantic consumers. Therefore CR-021 is an artifact Requirement Gap, not a source defect: keep exact LLM/media mode propagation, accurately preserve the dual-key metadata endpoint and curated fallback, and authorize no metadata source redesign. The empty-as-absent importer, no-automatic-update decision, exact unpatched `repository_prisma@1.0.8`, Codex preservation, both Claude modes, unchanged Docker topology, Store/target/source boundaries, `DASHSCOPE_API_KEY`-only Qwen mapping, and external AutoByteus endpoint unavailability result remain unchanged. Implementation/API-E2E remain paused until architecture review passes this bounded artifact reconciliation. Preserve CR-002–CR-005, CR-009–CR-020, `LOCAL_HARDENED` with its explicit Codex exclusion, deferred `STRONG_AGENT_ISOLATION`, and `EXT-ANTHROPIC-AGENT-SDK-AUTH`.

## Goal / Problem Statement

AutoByteus currently places many provider credentials in ignored `.env` or `.env.test` files and loads them into broad `process.env` state. Agent file, terminal, PTY, external-runtime, and worktree flows can consequently encounter or inherit credentials, and every new worktree may require a human to locate or copy an ignored test file before real-provider coverage can run.

The product needs a server-owned secret-management boundary that:

- keeps raw AI, search, media, custom-provider, and live-test credentials out of repositories, worktrees, ordinary configuration, agent-visible files, and inherited environments;
- supports Electron, direct local server, Docker, Kubernetes, and company-managed infrastructure without making Electron mandatory;
- treats each local Docker container or single Kubernetes server Pod as an independent node with its own persistent Local Store, while requiring a future separately installed enterprise adapter for multi-node/replica centralized custody;
- lets the server choose a replaceable `SecretStorageBackend` at startup or through non-secret Settings configuration;
- provides an in-process AutoByteus `LocalSecretStorageBackend` for local use without a daemon, local network server, IPC protocol, or Electron-owned custody;
- derives the normal Local Store from each server's configured data directory, so the existing Docker server-data volume persists its own Store without new Compose mounts or environment variables;
- keeps normal local credentials and real-E2E credentials in physically separate encrypted SQLite databases with independent encryption keys;
- lets a trusted local operator separately provision the host default or real-E2E Store through hidden input or a committed explicit-source CLI that safely selects only recognized credentials from a mixed assignment file, after which normal runtime/tests use only Store custody and never load the operator source;
- preserves a typed extension contract for companies to add Kubernetes, Vault, AWS, or company-specific backends, while explicitly deferring those concrete adapters from the first delivery;
- resolves a credential before LLM/client construction without storing it in serializable `LLMConfig` or coupling individual clients to secret storage; preserves exact Gemini LLM/media modes so AI Studio uses `{apiKey}`, Vertex Express uses `{vertexai:true, apiKey}`, and Vertex Project uses `{vertexai:true, project, location}`; separately preserves the original live-metadata contract in which either the selected Gemini or Vertex Express API key authenticates the Generative Language models endpoint and Vertex Project uses curated metadata.
- lets the Claude Agent SDK explicitly opt into the same managed Anthropic definition without duplicating the stored value, while delivering the resolved value only to the exact Claude Code child environment just before spawn.
- leaves Codex App Server authentication and launch ownership with Codex: existing `codex login` account/configuration state and the pre-ticket environment/home behavior remain supported, and AutoByteus adds no Codex secret definition, managed-auth mode, login/status API, or rotation lifecycle.
- preserves AutoByteus remote LLM/audio/image discovery and invocation through one managed `provider.autobyteus.api-key` definition, while keeping `AUTOBYTEUS_LLM_SERVER_HOSTS` as non-secret endpoint configuration and never restoring an ambient key read.
- cleanly replaces the separately packaged `repository_prisma` dependency with verified `1.0.8`, removes the obsolete local package patch, and changes neither Prisma ORM versions, schemas, current production database ownership, nor the default-off SQL query-log policy.

The first delivery claims `LOCAL_HARDENED`, not strong isolation. It removes plaintext repository/worktree/config custody, broad child-environment inheritance for the launchers in this ticket, ordinary value readback, and built-in file-tool Store access. Codex App Server is an explicit user-approved exclusion from the child-environment portion of that claim: its established Codex-owned external account/configuration and launch environment remain unchanged, and any operator-supplied ambient state inherited by that external runtime is outside this ticket's guarantee. In Claude `managed-secret` mode, the exact Claude Code child is an intentionally authorized credential consumer and necessarily receives the Anthropic key in its own process environment; supported process-spawning/environment-inspection tools and settings paths must not propagate or expose it. The tier does **not** claim protection from that authorized Claude process or its SDK/executable, an unrestricted same-user shell/process, the current all-in-one container identity, a privileged host/debugger, a compromised trusted server/provider process, code explicitly granted a raw real credential, or the upstream provider. `STRONG_AGENT_ISOLATION` and its separate identity/container/network enforcement are a future delivery.

## Current And Desired Behavior (Mandatory)

| Behavior ID | Current Behavior | Desired Behavior | Preserved / Unchanged Behavior | Related Requirement / Acceptance-Criteria IDs |
| --- | --- | --- | --- | --- |
| `BEH-001` | Built-in LLM/search keys entered in Settings are written through `AppConfig` to node `.env`; custom-provider keys are persisted in plaintext JSON. | Existing provider/search Settings submit a write-only value to the selected server. `SecretManagementService` owns lifecycle/status and uses the configured backend instance; no saved value is returned. | Existing direct-browser, Electron, and remote-node Settings access behavior remains familiar; no new user/admin model is introduced. | REQ-001, REQ-002, REQ-006, REQ-007, REQ-010 / AC-001, AC-007, AC-008 |
| `BEH-002` | Built-in file tools accept arbitrary absolute paths, shell cwd accepts host paths, and agent/application-worker children inherit broad server environment state. The established Codex App Server path also uses its existing environment/home so Codex-owned `codex login` account/configuration state is available. | First delivery enforces realpath-aware file-tool roots and purpose-built child environments for the in-scope terminal/background/PTY/MCP/Claude/browser/application-worker paths, with no unrelated provider key, Store key/path variable, backend identity, or inherited backend descriptor. The exact Claude Code child in explicit `managed-secret` mode remains the sole Store-resolved provider-key recipient. Codex is not routed through this new policy: preserve its pre-ticket launch/account behavior and exclude it from the `LOCAL_HARDENED` child-environment claim. | Agents and supported application backends retain authorized capabilities. Codex continues to use externally established Codex login state with no AutoByteus-managed auth lifecycle. Strong identity/container isolation is deferred. | REQ-003, REQ-004, REQ-012, REQ-018 / AC-003, AC-013, AC-018 |
| `BEH-003` | LLM, metadata, search, and media implementations independently read credential names from `process.env`; custom endpoint model objects retain plaintext keys. Original Gemini LLM/media use mode-specific Google SDK construction. Original Gemini metadata instead accepts `GEMINI_API_KEY ?? VERTEX_AI_API_KEY`, calls the Generative Language models endpoint, and merges live fields over curated metadata; Vertex Project has no key-backed live branch. | Server-owned provisioning services resolve an explicit semantic credential for one consumer, then pass non-serializable construction authentication to the trusted client/request boundary. LLM/media preserve exact `geminiAiStudio`, `geminiVertexExpress`, and `geminiVertexProject` variants. Metadata resolves only the selected AI Studio or Vertex Express semantic consumer and passes its key to the existing dual-key Generative Language metadata provider; Vertex Project performs no metadata secret lookup and retains curated metadata. | Provider behavior remains functionally equivalent while credential custody changes: exact Google SDK modes remain intact for LLM/media; AI Studio and Vertex Express metadata continue using the same established models endpoint and mapping; curated fallback remains; no ambient alias, cross-definition retry, or Store fallback is introduced. | REQ-005, REQ-011 / AC-005, AC-006, AC-010 |
| `BEH-004` | Core test setup loads ignored `.env.test`; many live tests inspect `process.env`; worktrees historically copy an untracked credential file from another checkout. | The repository tracks non-secret test configuration selecting the machine-global real-E2E Local Store and scenario requirements. Dedicated test credentials are provisioned once through hidden input or the explicit operator importer and reused by every worktree; the test runner never imports a source. Real-provider suites remain substantive. | Deterministic synthetic/fake coverage and real-provider defect detection both remain available. | REQ-009, REQ-016, REQ-017, REQ-020 / AC-006, AC-012, AC-015, AC-016, AC-020 |
| `BEH-005` | Provider-specific and generic Settings paths provide partial configured state, but lifecycle/status/operation-event behavior is fragmented. | `SecretManagementService` provides atomic save (create-or-replace), idempotent remove, per-definition `MISSING`/`CONFIGURED` state when healthy, and a separate backend health state of `READY`, `LOCKED`, `UNAVAILABLE`, `CORRUPT`, or `INCOMPATIBLE`. A degraded control plane remains reachable with value-free instructions; provider consumers fail closed. | The UI never needs or receives the saved plaintext value; subject services still own provider validation. | REQ-006, REQ-008, REQ-010 / AC-004, AC-007, AC-008 |
| `BEH-006` | Electron launches an embedded server; direct web/server and remote nodes start independently. Each currently loads node-local `.env` and propagates parent environment to children. | Every server bootstraps typed non-secret backend configuration before provider/model initialization. Electron, direct, ordinary Docker, and a single Kubernetes Pod construct the Local backend in process against a default Store below their configured data directory; host live-test servers explicitly select the separate real-E2E Store. Future company deployments may install another registered adapter. | Electron remains one packaging/client mode, not a prerequisite for direct web/server operation; existing Docker Compose topology and volumes remain unchanged. | REQ-004, REQ-010, REQ-012, REQ-013 / AC-001, AC-002, AC-013, AC-017 |
| `BEH-007` | Docker/Kubernetes nodes can mount or supply environment configuration, but the current all-in-one trusted server/agent execution topology shares identity and potential secret reach. | First delivery supports one independent Local Store per unchanged Docker node or single Kubernetes server Pod/PVC. Multi-node and centralized enterprise custody remain supported by the backend extension contract but require a separately delivered adapter; unsupported kinds fail value-free at bootstrap. | Companies may implement/register their own backend; this ticket does not prescribe Docker E2E mounts or claim strong all-in-one isolation. | REQ-010, REQ-012, REQ-013, REQ-015 / AC-002, AC-013, AC-014, AC-017 |
| `BEH-008` | Existing installations and test machines may contain plaintext credential aliases in canonical application `.env`, ignored `.env.test`, ambient process environment, and custom-provider-v1 JSON. | Current runtime removes every legacy credential reader/writer/fallback but performs no automatic legacy-file update. Upgrade/startup leave application `.env` and custom-provider-v1 operator-owned and byte-for-byte untouched. The non-secret configuration reader projects only approved non-secret entries and never retains/exposes sensitive assignments; a later explicit supported non-secret Settings change may update only its requested non-secret entry while preserving excluded credential lines unchanged. Custom-provider-v1 is not loaded into the current provider model; it returns value-free reconfiguration guidance. Users explicitly provision through UI/Settings, hidden input, or the importer and perform any legacy cleanup themselves. | No automatic preservation of legacy credential values or custom-provider-v1 metadata is promised; explicit reconfiguration is accepted. | REQ-002, REQ-014, REQ-016 / AC-007, AC-009, AC-012 |
| `BEH-009` | `LLMFactory.createLLM(modelIdentifier, configInput?)` composes an `LLMConfig` and invokes `new LLMClass(model, config)`; OpenAI then reads `process.env`. | The factory retains model/config composition and receives resolved authentication separately. It creates an ephemeral `LLMConstructionContext` for the concrete class; `LLMConfig` remains secret-free. | Model identifiers, config override behavior, and factory-owned default merging remain intact. | REQ-011 / AC-005, AC-010 |
| `BEH-010` | No current supported local secret-store behavior exists. | The in-process Local backend opens exactly one bootstrap-selected physical Store. Normal local use defaults to `secret-store.db`; real-provider tests use `real-e2e-secret-store.db`. Each has its own key, authenticated empty-Store pair verifier, and no automatic cross-Store fallback. Dedicated test credentials may be provisioned through hidden input or an explicit source-to-selected-Store transition. | No Store-to-Store copy exists; hidden-input setup has no default-Store read path, and the explicit importer never uses a Store as its source. | REQ-012, REQ-017, REQ-020 / AC-015, AC-016, AC-020 |
| `BEH-011` | No current supported deployment-neutral backend configuration contract exists. | Startup/deployment configuration and, where supported, Settings configure typed non-secret backend options. First delivery enables InMemory (test) and Local backends; the typed writable/externally-managed capability and registration contracts remain extension points. | Companies may implement/register their own backend in the open-source server and provision it using existing infrastructure. | REQ-007, REQ-010, REQ-013, REQ-015 / AC-002, AC-008, AC-014, AC-017 |
| `BEH-012` | `claude-sdk-auth-environment.ts` supports `auto`, `cli`, and `api-key`; it starts from `{...process.env}` and can forward Anthropic/Claude API-key aliases to the agentic Claude runtime. | First delivery supports exactly `cli` (default) and `managed-secret` (explicit). `cli` performs no secret lookup and uses external Claude CLI/account state. `managed-secret` resolves the catalog-authorized Claude runtime consumer just in time and supplies only `ANTHROPIC_API_KEY` to the exact Claude Code child through the SDK `env` option. Neither mode mutates or copies the parent environment, and no mode fallback exists. | Native Anthropic LLM/metadata API-key use remains supported through their own trusted construction consumers. Existing Claude run/model-discovery behavior remains available subject to the selected authentication mode and managed-mode tool/settings restrictions. | REQ-004, REQ-005, REQ-008, REQ-014, REQ-018 / AC-003–AC-005, AC-009, AC-018 |
| `BEH-013` | A configured `AUTOBYTEUS_LLM_SERVER_HOSTS` value enables remote AutoByteus LLM, audio, and image discovery. One legacy `AUTOBYTEUS_API_KEY` authenticates both catalog requests and constructed `AutobyteusClient` requests. Remote models retain their actual model provider (for example `OPENAI` or `GEMINI`) while `runtime=AUTOBYTEUS` identifies the gateway transport. | Preserve the same remote capabilities through `provider.autobyteus.api-key`. Existing provider Settings exposes write-only save and value-free credential status for `AUTOBYTEUS`; distinct catalog-authorized discovery and construction consumers resolve the same definition. Core discovery/factories receive explicit authentication, atomically synchronize only `AUTOBYTEUS`-runtime models, and tag those models with `credentialProviderId=AUTOBYTEUS` so construction never resolves the downstream model provider's key. | `AUTOBYTEUS_LLM_SERVER_HOSTS`, remote model identifiers/metadata, existing full/provider reload triggers, remote request protocols, and the provider Settings journey remain supported. No hosts means zero AutoByteus secret lookup and an authoritative clear of the matching remote runtime subset. No ambient key fallback is retained. | REQ-001, REQ-005, REQ-008, REQ-009, REQ-011, REQ-014, REQ-016, REQ-017, REQ-019 / AC-004–AC-006, AC-009, AC-010, AC-012, AC-019 |
| `BEH-014` | Legacy local credentials may remain in an operator-owned dotenv-style assignment file, often the current application `.env` or a copied/renamed `.env.test`. Current runtime ignores those aliases and hidden-input E2E setup provisions one definition at a time. The current importer accepts the optional PNPM separator and ignores unrecognized lines, but still rejects the whole import when a recognized current alias is present as an empty placeholder. | One committed PNPM/TypeScript local operator CLI requires the full absolute source path and explicit target `default` or `e2e`, accepts exactly zero or one leading PNPM `--` sentinel, and uses a recognize-first parser: it identifies only exact current importer aliases, parses only those assignments, treats a syntactically valid normalized-empty recognized assignment as absent/non-selected, and validates only populated selected credentials. Empty placeholders produce no plan entry, warning, ignored metadata, or failure. Every unrecognized line is ignored without interpreting or retaining its right-hand side, regardless of spelling or syntax, and produces no ignored-line metadata. Qwen imports only the currently supported `DASHSCOPE_API_KEY` alias. `QWEN_API_KEY` and legacy `ZHIPU_API_KEY` remain unmapped. Dry-run is value-free, configured records are preserved by default, and replacement requires explicit `--overwrite` plus target-specific TTY confirmation. | Hidden-input provisioning remains available. The CLI never runs during startup, UI/API/MCP/agent execution, or tests; it does not mutate/delete the source. Empty/absent recognized aliases, `DATABASE_URL`, `OLLAMA_API_KEY`, `GOOGLE_CSE_API_KEY`, `QWEN_API_KEY`, `ZHIPU_API_KEY`, Claude delivery aliases, malformed unrelated lines, and every other unrecognized input remain source-only and non-authoritative. No runtime fallback, automatic import, or legacy alias preservation is introduced. | REQ-002, REQ-004, REQ-008, REQ-009, REQ-012, REQ-014, REQ-016, REQ-017, REQ-020 / AC-004, AC-006, AC-009, AC-012, AC-015, AC-016, AC-020 |
| `BEH-015` | `autobyteus-server-ts` declares `repository_prisma:^1.0.6`; the lock resolves `1.0.6`; a root exact pnpm patch targets `1.0.6` to suppress default query logging. Current production source does not import this package; reviewed AppConfig/configured-client and bounded lazy-owner paths own database access. | Resolve only the attested `repository_prisma@1.0.8` artifact, which removes package-owned dotenv loading and provides default-off/explicit-opt-in query logging upstream. Remove obsolete patched-dependency metadata/file and preserve clean/import-safe behavior, Prisma ORM/client `5.22.0`, and current AutoByteus database ownership. | No `1.0.6`/`1.0.7` package or patch compatibility path remains. No Prisma schema/SQL migration, operational database URL, restart/reopen, Docker, secret-management, importer, provider, Claude, or UI behavior changes. The library does not become a production owner merely because it offers a similar lifecycle. | REQ-021 / AC-021 |

## Investigation Findings

1. `AppConfig` loads `${dataDir}/.env` through dotenv into `process.env`, writes provider/search credentials back to it, and mixes secret/non-secret ownership.
2. `OpenAILLM` delegates to `OpenAIResponsesLLM`, which reads `process.env[apiKeyEnvVar]`; `LLMFactory.createLLM()` is already async and currently accepts `(modelIdentifier, configInput?)` before invoking `(model, LLMConfig)`.
3. `LLMConfig` serializes/clones and can flow into retained run configuration or provider request parameters; it is not a safe credential carrier.
4. Search, media, metadata discovery, and custom OpenAI-compatible providers have parallel ambient credential paths, so an LLM-only change would be incomplete.
5. Test setup explicitly loads superrepo/package `.env.test`; real LLM/agent/media/search tests gate on environment keys. Fakes cannot replace scenarios intended to detect real provider/model/SDK behavior.
6. All `.env.*` files are currently broadly ignored in package `.gitignore` files. Non-secret test configuration can be tracked after explicit ignore exceptions, but typed `test-config/live-e2e.json` is clearer than global dotenv as the canonical scenario/store contract.
7. Electron, direct Node server, Docker, Kubernetes, and remote-node operation are all supported deployment shapes. The custody boundary must therefore be server-selected and not Electron-owned.
8. The current server database is SQLite through `DATABASE_URL`. Electron resolves it to `~/.autobyteus/server-data/db/production.db`, while test/direct servers can select `test.db` or a worktree/data-directory-specific database. Storing real-E2E secrets as rows in the ordinary application database would therefore recreate worktree coupling or force tests to mount/use normal application state.
9. The current Docker Compose configuration already persists `AUTOBYTEUS_DATA_DIR=/home/autobyteus/data` through the `autobyteus-server-data` named volume. The normal Local Store can live below that directory and persist automatically. This ticket leaves the Docker topology, volumes, and launcher unchanged and does not attempt to share the host real-E2E Store with Docker.
10. Kubernetes supplies Secret values to Pods via explicit volume mounts or environment variables, while workload identities can let the trusted server fetch from external managers. Any mount/environment/service-account identity visible to an agent shell in the same container remains reachable.
11. The product currently has no AutoByteus user, role, owner-principal, or administrator-identity subsystem. Backend workload identity authenticates the server to custody and is distinct from application-user identity. This ticket preserves existing Settings access behavior.
12. A separate Local Store process under the same OS user would add lifecycle/IPC/versioning complexity without creating a meaningful security boundary. SQLite already supports multiple local server processes through transactions/locking, and the server already owns secret resolution. The Local backend therefore runs in Agent Server.
13. Separate default and real-E2E databases satisfy the approved host/local two-context requirement with less policy than arbitrary named profiles. They allow independent keys and independent reset/backup without imposing a Store-sharing model on Docker deployments.
14. The second-principles audit found no supported caller for organization/deployment/environment scope fields, caller-selected storage addresses, Settings CAS/version inputs, a Local Store connection alias, arbitrary profiles, or a general backend cross-store copy port. Those elements are removed rather than standardized.
15. Architecture review round 1 confirmed that format metadata alone cannot authenticate an empty database/key pair. The current Store format therefore requires a random `store_id` plus an authenticated pair verifier created with the root key and validated before any Store becomes ready.
16. The first-delivery security boundary must be explicit: Local custody plus clean environments/file-tool roots is `LOCAL_HARDENED`; strong same-user/container isolation requires a later separate-identity enforcement delivery.
17. The existing `SecretManagementService.resolveForUse(consumer)` API is already the correct generic resolution boundary. Claude support requires a precise `agentRuntime/claude_agent_sdk/apiKey` consumer identity and catalog authorization, not a Claude-specific secret API or a second stored Anthropic key.
18. The pinned Claude Agent SDK accepts an explicit child `env`; the current AutoByteus wrapper instead begins with the broad parent environment. Managed mode can therefore resolve just in time and build an empty-base child environment, but the authorized Claude executable necessarily observes its own credential and JavaScript cannot guarantee zeroization.
19. Managed mode must remove supported descendant leak paths: no caller-provided `env`; SDK `tools: []` disables all Claude built-ins rather than relying on `allowedTools`; no user/project/local settings, hooks, plugins, or external MCP configuration can inject/spawn with the credential. Explicit AutoByteus-owned MCP tools execute outside the Claude child through sanitized server launch policy.
20. Anthropic's current official authentication guidance is inconsistent. The Agent SDK overview/legal page says third-party products should use API keys and may not offer Claude.ai login without approval, while the newer-dated June 15–16, 2026 Help Center update says Agent SDK, `claude -p`, and third-party app usage still draw from subscription limits during the paused billing change and explicitly identifies third-party apps authenticated through the Agent SDK. This establishes that the round-3 premise is not unambiguous. Current AutoByteus also successfully exercises the local CLI path; that proves technical reachability, not permission by itself. The user retains both modes and requires release-time monitoring of the authoritative policy conflict.
21. Source review proved a supported path absent from the earlier solution basis: base `LLMFactory` initialization/provider reload and audio/image factory discovery called AutoByteus remote providers, whose `AutobyteusClient` read `AUTOBYTEUS_API_KEY`. The implemented explicit-auth conversion correctly made that client require a supplied key but removed all production discovery callers, so configured remote models disappeared.
22. One definition is sufficient because the same AutoByteus gateway key authenticates discovery and runtime requests across LLM, audio, and image. Least privilege still requires distinct semantic consumers: `modelDiscovery/{llm|audio|image}/AUTOBYTEUS/apiKey`, `llm/AUTOBYTEUS/apiKey`, and `media/{audio|image}/AUTOBYTEUS/apiKey`.
23. A remote model's creator/provider identity is not necessarily its credential owner. Model registration materializes a required non-secret `credentialProviderId`: native definitions assign their known credential owner once, while AutoByteus-runtime models explicitly assign `AUTOBYTEUS`. The construction target omits displayed/creator provider and exposes only credential owner plus its tagged authentication requirement. This avoids duplicated factory/runtime branches and prevents an AutoByteus-hosted OpenAI model from resolving `provider.openai.api-key`.
24. `AUTOBYTEUS_LLM_SERVER_HOSTS` remains approved non-secret endpoint configuration and may be projected by the non-secret configuration reader without rewriting the legacy file. `AUTOBYTEUS_API_KEY` remains untouched in that file if present but is never retained, returned, or used by current runtime.
25. The existing `src/app-data-migrations` runner is intentionally unchanged. The current automatic startup credential cutover is removed; no scrub, ledger, or automatic credential/value/metadata update remains or is added.
26. Released application `.env` and custom-provider-v1 are reachable installed data. The user explicitly accepts rebuilding/reconfiguring them: legacy files remain untouched, credential values are non-authoritative, and custom-provider-v1 metadata is not automatically converted. An operator may explicitly import assignment-file credentials and separately move/delete legacy files before recreating custom providers through UI.
27. A value-free, name-only audit of the explicitly requested current application source `~/.autobyteus/server-data/.env` found 52 unique valid assignment names and owner-only mode `0600`. It contains multiple approved aliases plus unrelated `DATABASE_URL`, `OLLAMA_API_KEY`, `GOOGLE_CSE_API_KEY`, and other secret-like names. No right-hand-side value was inspected or emitted.
28. The current importer fails that valid mixed source before Store access because it treats every unrecognized secret-like name and `DATABASE_URL` as fatal. Rejecting an unrelated assignment does not protect the target: exact allowlisting already prevents its value from entering the Store, while whole-file rejection breaks the approved direct-application-file journey.
29. Current product source and Qwen integration coverage use `DASHSCOPE_API_KEY` for the DashScope-backed Qwen provider. The user corrected the earlier fallback proposal: `QWEN_API_KEY` is unsupported/legacy local input, remains unmapped, and is ignored like every other unrecognized assignment. The same rule applies to legacy `ZHIPU_API_KEY`; GLM remains mapped only by current `GLM_API_KEY`.
30. The documented root command passes a literal leading `--` to the Node CLI. Normalizing exactly one leading separator before strict option parsing supports the PNPM contract without weakening unknown/duplicate option rejection.

Exact source paths and refreshed evidence are recorded in [investigation-notes.md](./investigation-notes.md). The target diagrams are in [secret-storage-architecture.md](./secret-storage-architecture.md).

## Architecture Review Requirement-Gap Decisions

| Finding | Revised First-Delivery Requirement Decision | Approval State |
| --- | --- | --- |
| `AR-001` | Deliver and report `LOCAL_HARDENED` only; defer `STRONG_AGENT_ISOLATION` and its separate identity/mount/network enforcement. | User re-approved 2026-07-21 |
| `AR-002` | Ship/register Local for product bootstrap and InMemory/test fixtures for tests; defer every concrete enterprise adapter without making one an acceptance dependency. | User re-approved 2026-07-21 |
| `AR-003` | Preserve direct hidden-input E2E provisioning and remove every default-to-E2E Store copy path. The later UC-019 request adds a distinct explicit plaintext-source importer that writes one selected Store without reading/decrypting another Store; it does not reopen the rejected copy design. | Original decision user-approved 2026-07-21; UC-019 revision user-approved 2026-07-22 |
| `AR-004` | Expose exact five-state backend health and healthy-only definition state; keep a value-free degraded control plane for every non-ready state. | User re-approved 2026-07-21 |
| `AR-005` / `MP-001` | Authenticate a current-format Store/key pair with a random `store_id` and domain-separated AES-GCM pair verifier before `READY`, including when no records exist. | User re-approved 2026-07-21 |
| `AR-006` | Replace the reopened removal-only decision with exactly two explicit modes: default external `cli` and opt-in `managed-secret`. Managed mode authorizes the exact Claude runtime consumer to reuse `provider.anthropic.api-key`, resolves just in time, injects only `ANTHROPIC_API_KEY` into the exact child, constrains settings/tools/diagnostics, and never falls back. Legacy `auto`/`api-key` are rejected as invalid non-secret configuration. | User-approved 2026-07-21; architecture re-review requested |
| `AR-007` / `MP-002` | Retain both approved modes. The official June 15–16 Help Center update expressly recognizes current third-party Agent SDK subscription usage, while other official pages still conflict. AutoByteus adds no Claude login UI, credential relay, hosted account broker, or subscription pooling: CLI mode uses external node-local account state and performs zero secret lookup. Current product operation is supporting technical evidence only. Treat the external rule as a maintained release dependency and return the conflicting authoritative evidence for reviewer reassessment rather than assuming either blanket permission or blanket prohibition. | User reaffirmed 2026-07-21; architecture re-review requested |
| `CR-001` | Formalize BEH-013. Add `provider.autobyteus.api-key`, distinct remote-discovery/runtime-construction consumers, runtime-aware `credentialProviderId`, server-owned remote discovery orchestration, runtime-scoped catalog synchronization, cutover retirement of `AUTOBYTEUS_API_KEY`, existing Settings status/save/reload behavior, and synthetic plus real-E2E coverage. Never restore a core/client `process.env` key read. | User approved 2026-07-21; architecture re-review required |
| `CR-002`–`CR-005` | Preserve as implementation-owned rework: CLI maps the real pre-existing node-local account location into an empty-base environment; stdio MCP adds its explicitly authorized `config.env` map instead of treating it as the parent; custom-provider delete is idempotent; implementation handoff describes managed-only tools/settings restrictions accurately. | No new product decision; must be fixed after architecture re-review |
| `AR-009` / `MP-003` | Keep the explicit importer but remove all automatic app-data credential migration/update. Startup does not import, copy, scrub, delete, rewrite, or otherwise transform legacy credential-bearing data. Legacy files remain untouched/operator-owned; credential aliases and custom-provider-v1 are non-authoritative. Users explicitly import/re-provision and clean up legacy files. | User explicitly selected this outcome 2026-07-22; architecture re-review required |
| `DEP-001` | Update only `repository_prisma` from the current `1.0.6` state to the latest verified `1.0.8`. Preserve the current Prisma ORM/client line, schemas, migration history, AppConfig/configured-client ownership, import-safe lazy owners, and default-off query-log policy. Use the upstream artifact directly and delete the obsolete package patch instead of retaining legacy integration code. | User approved exact `1.0.8` clean replacement and no-legacy posture 2026-07-22; architecture re-review required |
| `AR-010` / `MP-004` | The rejected `1.0.7` target evaluated `dotenv/config` at import. Verified `1.0.8` removes that side effect from both entrypoints and removes its dotenv dependency. Retain exact-installed-entrypoint probes with empty temporary cwd, empty-base platform-minimal environment, no parent spread or dotenv/database/Node-loader state, and narrow test-owned `@prisma/client` constructor interception so import safety and default-off logging cannot silently regress. | Resolved by upstream `1.0.8` plus isolated regression evidence; no local package patch or compatibility path; architecture re-review required |
| `API-E2E-DI-001` | Make the explicit importer recognize only exact current aliases from a mixed source, then validate only populated selected assignments. Ignore every unrecognized line without interpreting/retaining/importing its value and report nothing about ignored content. Keep `DASHSCOPE_API_KEY` as the only Qwen mapping; keep `QWEN_API_KEY` and `ZHIPU_API_KEY` unmapped, and normalize exactly one leading PNPM separator. Keep file identity/size/encoding safety, populated selected-assignment syntax/dynamic/duplicate validation, zero mapped definitions, target health, confirmation, transaction, source immutability, and no-fallback controls fail-closed. | User-approved; implemented and source-reviewed at `4c9f01776347d18bac78805800363f8a92a096af`; focused/API-E2E evidence confirms separator and unrelated-line behavior pass; preserved prerequisite to SCSP-E2E-IMPORT-001 |
| `SCSP-E2E-IMPORT-001` | A syntactically valid recognized assignment with an empty normalized value is absent/non-selected, not an invalid credential. It creates no plan entry, ignored-line metadata, warning, count, or failure and does not participate in duplicate validation; one later/earlier populated occurrence of that spelling is the sole selected credential, while two populated occurrences still fail as duplicate. If every recognized assignment is absent or empty, return value-free `IMPORT_NO_MAPPED_CREDENTIALS` before target access. | User explicitly selected empty-as-absent behavior on 2026-07-22 after the current application `.env` demonstrated an empty `GEMINI_API_KEY` placeholder alongside a populated `VERTEX_AI_API_KEY`; architecture re-review required |
| `CR-019` | Codex App Server remains an externally authenticated runtime. Preserve the pre-ticket client launch environment/home and its access to whatever account state the operator established through `codex login`; AutoByteus does not select ChatGPT versus API-key login, resolve a Store credential, call account-login APIs, add auth UI/status/rotation ownership, or synthesize a replacement home. Codex is explicitly outside the `LOCAL_HARDENED` child-environment non-inheritance claim, while generic output redaction and Store non-authority remain. | User explicitly directed “leave Codex alone” on 2026-07-22; architecture re-review required |
| `CR-020` | Replace lossy generic Gemini key construction with one shared exhaustive resolved-authentication union carrying `geminiAiStudio`, `geminiVertexExpress`, or `geminiVertexProject`. LLM and media provisioning select exactly one variant from `GEMINI_SETUP_MODE`; the Gemini helper constructs the corresponding SDK options and never infers/falls back. | Resolved for LLM/media at round-24 source review; preserve unchanged |
| `CR-021` | Correct the artifacts to match proven original behavior: exact Gemini mode variants apply to LLM/media only. Metadata preserves its separate dual-key Generative Language endpoint contract—resolve the selected AI Studio or Vertex Express consumer from the Store, pass that key to the existing provider, preserve live-over-curated mapping/fallback, and perform no metadata secret lookup for Vertex Project. No metadata implementation rework is authorized from the withdrawn mode-loss premise. | User confirmed the original dual-key metadata path works; code review round 25 withdrew the source-defect inference and reclassified CR-021 as an artifact Requirement Gap; architecture re-review required |

## Relevant Supplemental Task Artifacts

| Artifact Path | Type / Purpose | Related Requirement IDs | Related Acceptance-Criteria IDs | Status / Approval | Relationship To Requirements |
| --- | --- | --- | --- | --- | --- |
| [use-case-spine-validation.md](./use-case-spine-validation.md) | Complete UC-001–UC-020 data-flow spines, reachability decisions, and attribute-provenance audit | REQ-001–REQ-021 | AC-001–AC-021 | Original Gemini metadata preservation reconciliation complete; architecture re-review required | Intended architecture authority and removal rationale |
| [threat-model-and-option-analysis.md](./threat-model-and-option-analysis.md) | Threat boundaries, importer trust boundary, assurance limits, custody/test/deployment options, and dependency query-log policy | REQ-002–REQ-005, REQ-008, REQ-009, REQ-012–REQ-021 | AC-002–AC-006, AC-009–AC-021 | Original Gemini metadata preservation reconciliation complete; architecture re-review required | Evidence and explicit assurance limits |
| [secret-storage-backend-contract.md](./secret-storage-backend-contract.md) | Intended service/backend/store/capability/import contract | REQ-001, REQ-002, REQ-004–REQ-008, REQ-010–REQ-020 | AC-001–AC-020 | Original Gemini metadata preservation reconciliation complete; architecture re-review required | Intended behavior authority |
| [credential-consumer-mapping.md](./credential-consumer-mapping.md) | Current aliases/consumers, target construction mappings, legacy-source non-authority, and explicit positive import registry | REQ-001, REQ-002, REQ-004, REQ-005, REQ-011, REQ-014, REQ-016, REQ-018–REQ-020 | AC-003, AC-005, AC-009–AC-012, AC-018–AC-020 | Original Gemini metadata preservation reconciliation complete; architecture re-review required | Evidence plus target mapping authority |
| [live-test-secret-provisioning.md](./live-test-secret-provisioning.md) | Hidden-input/explicit-source host real-E2E Store provisioning and zero-copy worktree workflow; explicit Docker non-impact | REQ-002–REQ-005, REQ-008, REQ-009, REQ-012, REQ-016–REQ-020 | AC-003, AC-004, AC-006, AC-010, AC-012, AC-013, AC-015, AC-016, AC-018–AC-020 | Original Gemini metadata preservation evidence reconciled; architecture re-review required | Intended behavior authority |
| [secret-storage-architecture.md](./secret-storage-architecture.md) | Mermaid architecture, deployment, physical Store, importer, LLM, AutoByteus remote discovery/construction, host E2E, unchanged Docker, Kubernetes, health, and Claude spines | REQ-001–REQ-020 | AC-001–AC-020 | Original Gemini metadata preservation reconciliation complete; architecture re-review required | Intended architecture and behavior supplement |
| [repository-prisma-1.0.8-assessment.md](./repository-prisma-1.0.8-assessment.md) | Exact published metadata/provenance/artifact hashes, ESM/CommonJS import/log probes, current integration evidence, compatibility/security analysis, and clean-replacement decision | REQ-021 | AC-021 | Evidence complete; approval `N/A` | Primary-package evidence supporting BEH-015/UC-020 and AR-010 resolution; intended behavior remains in this requirements doc and the design spec |

Architecture review authority remains [design-review-report.md](./design-review-report.md). Source-review authority is [code-review-report.md](./code-review-report.md), round-1 `Fail / Requirement Gap` on CR-001 with CR-002–CR-005 retained for bounded implementation rework. Both reports are reviewer-owned cumulative artifacts, not intended-behavior supplements.

## Design Health Assessment (Mandatory)

- Change posture: `Larger Requirement` — security-boundary feature plus cross-cutting refactor, with one bounded dependency-integration revision.
- Initial design issue signal: `Yes`.
- Root cause classification: `Boundary Or Ownership Issue` reinforced by `Duplicated Policy Or Coordination` and `Legacy Or Compatibility Pressure`.
- Refactor posture: `Needed` in this change.
- Evidence basis: generic configuration owns secrets; consumers read ambient process state; worktrees duplicate ignored credential bootstrap; agent processes inherit trusted state; custom metadata embeds keys; deployment packaging has no single backend contract.
- Requirement or scope impact: introduce one lifecycle owner above backend adapters, explicit client-construction authentication, in-process local custody with physically separate default/real-E2E Stores, tracked non-secret test configuration, backend capability reporting, a current-runtime legacy non-authority rule, and agent execution isolation. Add one bounded package-integration use case for `repository_prisma@1.0.8`; do not replace reviewed production database owners, upgrade Prisma ORM, or introduce an unrelated user/admin subsystem, daemon, IPC protocol, arbitrary profile framework, or automatic legacy-data updater.
- Dependency revision assessment: `No Design Issue Found` in current database ownership. The package update is local to manifest/lock/test integration; the upstream artifact now owns import safety and default-off query logging, so the obsolete local patch is removed rather than rebased. The dependency's lifecycle does not justify production adoption. Empty-cwd/empty-base probes and clean/frozen install evidence guard the package boundary without retaining legacy code.

## Recommendations

1. Adopt dependency direction `ConsumerProvisioningService -> SecretManagementService -> SecretStorageBackend`.
2. Implement `LocalSecretStorageBackend` as an in-process Agent Server component. Do not create a daemon, TCP/HTTP service, socket/named-pipe protocol, launcher, or Electron-owned store.
3. Use a separate encrypted SQLite file and independent key for normal local custody and real-E2E custody. Bind one backend instance to one bootstrap-configured Store path; requests cannot select another Store.
4. Keep hidden-input real-E2E provisioning and add one committed local import CLI requiring `--source <absolute source path>` and `--target default|e2e`. Accept the PNPM form with one leading `--`. The source filename/extension is unrestricted and may be the current mixed application `.env`; recognize exact current aliases first, validate only selected credential assignments, and ignore every other line without interpreting its right-hand side. Never infer/search a source or target. Target names resolve internally to canonical default/E2E Store pairs; Store paths remain non-selectable.
5. Track all non-secret test configuration. Canonicalize live scenario/Store mapping in typed JSON; retain/commit `.env.test` only for verified non-secret settings if existing tooling still needs it.
6. Keep the existing Docker Compose topology, persistent volumes, and launcher unchanged. A normal Docker node derives its Local Store below `AUTOBYTEUS_DATA_DIR` and users configure credentials on that node through the normal product flow. Do not prescribe a Docker real-E2E mount; operators remain free to customize their deployment outside this ticket.
7. Make each backend expose one tagged lifecycle capability: `WRITABLE` or `EXTERNALLY_MANAGED`. Settings writes are available only for writable backends; externally managed deployments show status/instructions.
8. Keep `LLMFactory` responsible for model lookup/default merging. Pass resolved authentication separately and construct `LLMConstructionContext` internally.
9. Ship only InMemory (test) and Local adapters initially. Preserve a typed registration/capability/conformance extension contract for future Vault/AWS/Kubernetes/company adapters; do not imply that centralized multi-node custody ships in this ticket.
10. Remove environment/custom-record fallback; leave legacy sources untouched and require explicit provisioning or import.
11. Enforce first-delivery realpath-aware file-tool roots, empty-base process-environment allowlists for the governed launchers, descriptor hygiene, redaction, and honest `LOCAL_HARDENED` reporting. Preserve Codex App Server's existing external-login environment/home path unchanged and list it as an explicit exclusion from the environment claim. Defer `STRONG_AGENT_ISOLATION` until separate identity/container/network enforcement is delivered and verified.
12. Replace Claude's ambient auth selection with explicit `cli | managed-secret`. Keep `cli` as the default external-account path. In managed mode, reuse the existing generic `resolveForUse` service with the exact Claude runtime consumer, deliver only to the exact SDK child, block supported settings/tool/diagnostic leak paths, and never copy parent environment or fall back between modes.
13. Preserve AutoByteus remote LLM/audio/image behavior through one managed gateway definition, distinct discovery/construction consumers, and a server-owned discovery service. Keep downstream model provider identity separate from `credentialProviderId`; synchronize remote registrations by runtime/source, not by downstream provider.
14. Treat explicit environment-file provisioning as trusted local operator functionality, not test-runner or runtime functionality. Reuse one exact positive alias registry with only current supported names and a Local batch-provisioning owner; import only recognized aliases from mixed sources, expose nothing about ignored content, retain no ZHIPU compatibility, require explicit target selection, no-replacement default, value-free dry-run, explicit overwrite opt-in, and stronger target-specific confirmation for default-Store writes.
15. Leave legacy credential-bearing files untouched. Current readers must exclude sensitive aliases from runtime state and reject custom-provider-v1 value-free without rewriting it. Users explicitly import/re-provision and perform cleanup; do not add a cutover writer, credential-value transfer, ledger, or app-data migration extension.
16. Cleanly replace `repository_prisma@1.0.6` with verified `1.0.8` as a dependency integration only. Remove obsolete patched-dependency metadata/file; retain no `1.0.6`/`1.0.7` resolution or compatibility path. Keep `prisma`/`@prisma/client` on `5.22.0`, preserve current AppConfig/configured-client and lazy-owner paths, and prove clean/frozen install plus isolated import/log/build/restart regressions without adopting the dependency as a new production database owner.
17. Preserve Gemini mode as data across the shared construction boundary. Use exhaustive `geminiAiStudio | geminiVertexExpress | geminiVertexProject` resolved variants shared by LLM/media; construct the Google SDK exactly for each variant and reject mismatch/fallback.

## Scope Classification (`Small`/`Medium`/`Large`)

`Large`. The change spans `autobyteus-ts`, `autobyteus-server-ts`, `autobyteus-web`/Electron, local storage, startup/configuration, persistence migration, host/worktree test infrastructure, deployment-neutral backend documentation, and process isolation. Existing Docker Compose/launcher files are explicitly unchanged.

## In-Scope Use Cases

- `UC-001` **Built-in provider credential lifecycle:** configure, replace, inspect value-free status, or remove a built-in provider key from direct web or Electron Settings through the server's writable selected backend.
- `UC-002` **Custom provider lifecycle:** probe and create an OpenAI-compatible provider from transient credential input, persist metadata without the value, construct/discover using the derived definition, and delete provider plus credential without readback.
- `UC-003` **Backend configuration:** configure backend kind and non-secret adapter options through deployment/startup configuration and supported Settings surfaces.
- `UC-004` **Local backend bootstrap:** Electron embedded, direct local, normal Docker, and single Kubernetes server-Pod deployments construct the Local backend in process. Normal use derives the default Store below that independent node's configured data directory/persistent-volume domain; tracked host live-test configuration explicitly selects the separate real-E2E Store.
- `UC-005` **Physical Local Store lifecycle:** initialize and inspect one configured encrypted Store database/key pair, authenticate the pair even when no secret records exist, reject mismatched/corrupt/unsupported Stores, and keep each Store independent. No arbitrary named-profile lifecycle exists.
- `UC-006` **Real-E2E Store provisioning:** directly provision one selected definition into the separate real-E2E Store through trusted hidden input, without value readback and without any default-Store access or copy path.
- `UC-007` **LLM construction and invocation:** create an LLM using resolved authentication and a secret-free `LLMConfig`, then complete a provider request/stream.
- `UC-008` **Non-LLM client construction:** provision search, media, and live-metadata clients through explicit subject boundaries and complete their provider operation. Gemini media preserves exact AI Studio, Vertex Express, or Vertex Project SDK construction. Gemini metadata preserves the established separate contract: selected AI Studio or Vertex Express Store credentials authenticate the same Generative Language models endpoint; Vertex Project has no key-backed live provider and uses curated metadata. Curated fallback never causes another secret lookup or ambient credential read.
- `UC-009` **Deterministic testing:** run default unit/integration/UI coverage with synthetic values and temporary stores where storage behavior matters, without real backend/provider credentials.
- `UC-010` **Real-provider worktree testing:** run substantive real-provider API/E2E from any fresh host worktree using tracked non-secret configuration and the once-provisioned host real-E2E Store. Docker E2E Store sharing is not prescribed.
- `UC-011` **Local backend concurrency/reset lifecycle:** multiple local worktree servers may read the same real-E2E Store; writes are transactional/locked. Ordinary server runtime-data reset preserves Local Stores; a confirmed Store reset targets exactly one physical Store.
- `UC-012` **Container deployment and extension boundary:** deploy one independent Local Store per unchanged Docker node or single Kubernetes server Pod/PVC. Multi-node centralized custody requires a separately installed future Vault/AWS/Kubernetes/company adapter; first delivery rejects unregistered kinds value-free.
- `UC-013` **Capability-aware Settings:** represent writable versus externally managed backend behavior so lifecycle actions are enabled only when supported.
- `UC-014` **First-delivery agent hardening and Codex preservation:** prevent built-in file-tool Store access and provider/backend state inheritance for the launchers governed by the new purpose-built policy while preserving authorized workspace behavior. Preserve Codex App Server's established external `codex login` environment/home path unchanged and exclude it from the environment non-inheritance claim. Report `LOCAL_HARDENED` only with that explicit exclusion and withhold strong same-user/process isolation.
- `UC-015` **Legacy-source non-authority:** start the current runtime without importing, copying, scrubbing, deleting, or rewriting legacy credential-bearing files. Approved non-secret application settings may be projected without retaining sensitive assignments; custom-provider-v1 remains untouched and produces value-free reconfiguration guidance. No legacy credential is runtime authority or fallback.
- `UC-016` **Backend conformance:** validate every enabled backend implementation against its base resolve/status/health contract and declared lifecycle capability using synthetic values and isolated temporary stores.
- `UC-017` **Claude runtime authentication cutover:** select exactly default `cli` or explicit `managed-secret`; launch CLI mode without secret resolution, or resolve the exact Claude runtime consumer just in time and deliver `ANTHROPIC_API_KEY` only to the Claude Code child. Both paths use empty-base purpose-built environments, no fallback, value-free failures, early diagnostic redaction, and no caller-supplied environment. Native Anthropic consumers remain separately authorized to the same definition.
- `UC-018` **AutoByteus remote model gateway lifecycle:** save/status the AutoByteus gateway credential through the existing provider Settings flow; discover and reload remote LLM/audio/image catalogs using explicit Store-backed authentication; retain downstream provider metadata while tagging `credentialProviderId=AUTOBYTEUS`; construct/invoke a selected remote model with the same definition; and run the corresponding real-E2E capabilities without `AUTOBYTEUS_API_KEY` in runner/server environment or tracked files.
- `UC-019` **Bounded local recognize-first provisioning:** a trusted local operator invokes the PNPM command with its supported single separator, supplies an absolute source—including the current application `.env`—and explicit `default|e2e` target. The importer recognizes only approved current aliases, treats valid normalized-empty recognized assignments as absent, validates/plans only populated selected credentials, maps Qwen only from `DASHSCOPE_API_KEY`, and ignores every unrecognized line without interpreting it; legacy `ZHIPU_API_KEY` remains unmapped. The operator verifies a dry-run or create/skip/replace plan, optionally opts into replacement, enters the exact target-specific TTY confirmation, and atomically provisions the selected Store without value readback, placeholder metadata, source mutation, target inference, or partial writes.
- `UC-020` **`repository_prisma` 1.0.8 clean dependency replacement:** update the server manifest and lockfile to the attested `1.0.8` package; remove the obsolete package patch registration/file; prove both entrypoints remain dotenv-free, import-safe, and default-off for query logging; and run clean/frozen install, production build, and existing restart/reopen regressions without changing Prisma ORM, schemas, migrations, persisted data, or current AutoByteus database owners.

## Out of Scope

- A new AutoByteus user, login, role, administrator principal, or route-authorization system.
- Protection from a privileged host operator/debugger or compromised trusted provider/control process.
- Making a raw credential unknowable to reviewed code or an SDK process that must receive it directly.
- Any concrete Vault, AWS, Kubernetes Secret, or other centralized enterprise adapter in the first delivery; only the extension contract is in scope.
- General migration of unrelated messaging, release, signing, callback, or build-system credentials.
- Changes to existing Docker Compose topology, named volumes, or launcher behavior, including a prescribed Docker real-E2E Store mount.
- Arbitrary named local profiles, profile inheritance, or per-request Store selection.
- Automatic cross-Store inheritance or fallback.
- Default-to-real-E2E Store copy, relative/inferred/searched source paths, caller-selected Store database/key paths, directory/parent search, or Store fallback. An explicit absolute regular-file source path is in scope regardless of filename/extension; only positively recognized credential assignments must follow the selected-value grammar.
- Automatic/startup import, copy, scrub, deletion, rewrite, metadata conversion, or other migration of credential-bearing application `.env`, custom-provider-v1, `.env.test`, arbitrary paths, parent directories, another checkout, ambient process values, real-E2E custody, another Store, or an enterprise backend. Users re-provision or explicitly run UC-019; the test runner never imports.
- A Local Store daemon, network service, IPC protocol, process launcher, or OS-specific keychain integration.
- `STRONG_AGENT_ISOLATION`, separate OS/container identity enforcement, or a claim that same-user/all-in-one agent processes cannot read the Local database/key pair.
- Automatic Claude authentication selection, ambient raw-key inheritance, and legacy `auto`/`api-key` modes. Explicit managed-secret authentication is in scope.
- Any AutoByteus-owned Codex authentication mode, Secret Store consumer, account-login/status UI/API, cached-account migration, or rotation lifecycle. Codex authentication remains external and is established through `codex login`; this ticket preserves the pre-ticket Codex launch/account behavior.
- Inventing proprietary cryptographic algorithms; the local backend must use vetted authenticated-encryption primitives.
- A Prisma ORM/client 6/7 upgrade, Prisma schema/SQL migration change, or replacement of current AppConfig/configured-client and bounded lazy Prisma owners with `repository_prisma`. The `1.0.8` dependency remains non-authoritative unless a separately reviewed production use case adopts it later.

## Functional Requirements

- `REQ-001` **Typed secret catalog:** every in-scope credential has a stable product-owned definition ID and semantic consumer bindings. Current runtime maps only validated definition IDs to the active backend; adapter-specific physical prefixes/paths remain trusted bootstrap configuration, and historical exclusion/import aliases remain outside the current catalog. Runtime/user requests cannot supply backend paths.
- `REQ-002` **No supported plaintext custody:** current runtime values are not persisted in repository/worktree `.env`, `.env.test`, application `.env`, JSON metadata, ordinary application database columns, command lines, public frontend config, logs, traces, or artifacts. Local Store values occupy authenticated ciphertext in their dedicated databases. UC-019 is the only bounded source-file transition exception: its trusted process may transiently parse approved values but cannot create another plaintext copy, expose them, or make the source a current runtime authority.
- `REQ-003` **First-delivery file boundary:** in-process file tools enforce realpath-aware authorized roots and deny Local Store database/key paths, including symlink/traversal aliases. First delivery does not claim that arbitrary same-user processes are filesystem-isolated from those files.
- `REQ-004` **Purpose-built process environments with an explicit Codex exclusion:** terminal, background, PTY, MCP, Claude, browser/application workers, and future agent-controlled children governed by this ticket start from an empty base plus per-runtime allowlists. They receive no unrelated provider-key aliases, Store key/path variables, backend bootstrap identity, secret-bearing inherited file descriptors, or broad `process.env`. The only Store-resolved provider-key recipient is `ANTHROPIC_API_KEY` delivered to the exact Claude Code child after an explicit `managed-secret` resolve under REQ-018; the parent, siblings, AutoByteus-owned tool children, and other governed runtime children do not receive it. Codex App Server is explicitly excluded: restore/preserve its pre-ticket `options.env ?? process.env` and real HOME/CODEX_HOME behavior so existing external `codex login` account/configuration state keeps working. AutoByteus adds no Codex Store resolve, account RPC, auth mode/status/rotation owner, synthetic account home, or fallback. This ticket makes no environment non-inheritance claim for Codex and must state that exclusion wherever `LOCAL_HARDENED` is reported.
- `REQ-005` **Least-privilege runtime resolution:** only server-owned consumer provisioning services call internal `resolveForUse`; each request names one semantic consumer identity, and `SecretManagementService` maps it through the catalog to one definition. No GraphQL/agent raw-value, list-values, arbitrary-name, backend-path, or secret-dump API exists.
- `REQ-006` **Service above backends and degraded control plane:** `SecretManagementService` owns catalog-bound lifecycle/resolution policy, per-definition status, value-free operation events, and error mapping. `SecretStorageConfigurationService` owns active-backend health. Health is exactly `READY`, `LOCKED`, `UNAVAILABLE`, `CORRUPT`, or `INCOMPATIBLE`; per-definition state is returned only when health is `READY` and is exactly `MISSING` or `CONFIGURED`. Non-ready startup keeps Settings/health reachable in degraded secret mode, disables provider consumers and writes, and returns a value-free instruction code. Subject-specific services still own provider credential validation.
- `REQ-007` **Preserve current Settings access and configure storage separately:** existing subject-specific Settings/GraphQL access behavior remains; no user/admin subsystem is added. Secret inputs are never prefilled or persisted in browser storage, clear after successful submission/when the editor closes, and expose loading/error/status without echo. `SecretStorageConfigurationService` owns typed non-secret backend selection/options separately from provider secret lifecycle. Electron's default Local Store selection requires no ordinary UI control.
- `REQ-008` **Output hygiene:** secret inputs/headers/URL parameters and credential-bearing structures are structurally redacted at GraphQL, error, log, trace, stdout/stderr, telemetry, support, and artifact boundaries. `SecretValue` string/JSON/inspect output is redacted.
- `REQ-009` **Complementary deterministic and real tests:** synthetic/fake coverage remains default where valid. Real-provider suites remain required for scenarios whose purpose includes real authentication, SDK/model/protocol, streaming/tool/media, quota, or provider-specific behavior.
- `REQ-010` **Typed backend selection and capabilities:** server bootstrap selects one backend configuration before provider/model initialization. Every backend supplies the base resolve/status/health contract and one tagged lifecycle capability. A backend instance is bound to one configured custody location; unavailable configuration never falls back to another Store or environment values.
- `REQ-011` **Credential-consuming code refactor:** LLM, metadata, custom discovery, search, and media clients stop reading credential environment variables or retaining keys in model/config records. `LLMFactory.createLLM(id, {configInput, authentication})` composes `LLMConstructionContext` internally. The non-serializable LLM/media construction authentication distinguishes Gemini AI Studio, Vertex Express, and Vertex Project. Their existing provisioning owners read explicit `GEMINI_SETUP_MODE`: `geminiAiStudio` reveals only to `GoogleGenAI({apiKey})`; `geminiVertexExpress` reveals only to `GoogleGenAI({vertexai:true,apiKey})`; and `geminiVertexProject` carries only project/location to `GoogleGenAI({vertexai:true,project,location})`. Metadata retains its narrower established behavior rather than sharing that SDK-mode union: `ModelMetadataProvisioningService` maps `AI_STUDIO` to `llmMetadata/GEMINI/geminiAiStudioApiKey`, maps `VERTEX_EXPRESS` to `llmMetadata/GEMINI/geminiVertexExpressApiKey`, reveals only the selected Store value to `GeminiModelMetadataProvider`, and maps `VERTEX_PROJECT` to no live metadata provider/zero secret lookup. The provider continues calling the Generative Language models endpoint and mapping its response; `ModelMetadataResolver` retains live-over-curated fallback. No consumer reads ambient aliases, retries another definition, or falls back to legacy custody.
- `REQ-012` **In-process AutoByteus Local Secret Storage Backend:** Agent Server directly constructs an encrypted SQLite-backed `LocalSecretStorageBackend`; Electron does not own it. No separate process, server, daemon, socket, named pipe, or protocol is introduced. The default Store is derived below the configured server data directory; Electron therefore resolves under `~/.autobyteus/server-data`, while ordinary Docker resolves inside its existing persistent server-data volume without Compose changes. A Docker container or single Kubernetes server Pod may operate as one independent Local Store node using its own persistent volume. The configured Store has one database and independent 256-bit key, owner-only permissions, authenticated encryption, transactional/locked writes, bounded busy handling, and a reusable backend adapter. Current-format metadata includes a random `store_id` and an AES-GCM authenticated pair verifier under a domain-separated key derived from the root key; open validates it before `READY`, including for an empty Store. One missing file, verifier failure, or database/key mismatch is `CORRUPT`; an unsupported schema/encryption/verifier format is `INCOMPATIBLE`; neither is regenerated or rewritten. Staged pair creation is crash-fail-closed rather than falsely claimed as cross-file atomic. Ordinary server reset preserves Store files; deletion requires an explicit exact-Store reset or include confirmation.
- `REQ-013` **Container deployment and future enterprise extension:** first delivery ships Local custody for direct/Electron/unchanged Docker/single-Pod Kubernetes use. Multiple server containers/Pods/replicas never share one Local SQLite Store. The typed adapter registration/configuration/capability contract permits future Vault/AWS/Kubernetes/company adapters, but no concrete centralized adapter or strong worker-separation deployment is delivered here; selecting an unregistered kind fails value-free without fallback.
- `REQ-014` **Clean runtime cutoff with no automatic legacy update:** remove every normal-runtime environment/custom-record credential reader, writer, fallback, and legacy schema branch. Startup never imports, copies, scrubs, deletes, rewrites, or converts credential-bearing legacy files and never deletes parent-environment aliases. The non-secret configuration boundary may project only approved non-secret names while leaving the source unchanged; sensitive aliases are excluded from `get`, `getAll`, persistence, logs, and child environments. Custom-provider-v1 remains untouched and is rejected with stable value-free reconfiguration guidance; it is not converted and its metadata is not automatically preserved. Users explicitly provision through current UI/Settings, hidden input, or REQ-020 and own legacy cleanup. No compatibility read/fallback remains.
- `REQ-015` **Backend substitutability and first-delivery conformance:** all registered adapters satisfy the common resolve/status/health/error contract; writable adapters additionally satisfy atomic save (create-or-replace) and idempotent remove semantics. First delivery runs the reusable synthetic suite against InMemory and Local read-write/read-only variants, plus a test-only externally-managed capability fixture for projection behavior. No production enterprise adapter is an acceptance dependency. Local conformance uses isolated temporary Stores, not the shared real-E2E Store.
- `REQ-016` **Tracked, secret-free test configuration:** the repository tracks canonical live scenario and real-E2E Store selection/configuration and may track `.env.test` only after it contains verified non-secret settings. Every worktree receives the configuration automatically; tests never search/copy another checkout. The explicit CLI may open only its operator-supplied absolute assignment-file path during local setup, regardless of that file's name, and is never invoked by a test runner.
- `REQ-017` **Physical Store isolation and one-time host E2E setup:** the host real-E2E Store is provisioned once per machine through trusted hidden input or the explicit CLI with target `e2e`, then selected by tracked bootstrap configuration. It uses a database and key distinct from the default Store. Neither setup path accesses/copies/falls back to the default Store. Host runtime real-provider tests open only the real-E2E Store, preferably read-only. Subsequent worktrees require no routine human secret handling. Docker Store sharing/mounting is outside this requirement.
- `REQ-018` **Claude Agent SDK authentication cutover:** server configuration accepts exactly `cli` and `managed-secret`; default is `cli`, and legacy/invalid `auto` or `api-key` fails `CLAUDE_RUNTIME_AUTH_MODE_INVALID` before lookup or spawn. `cli` performs no `SecretManagementService` call and uses only explicitly allowed external Claude CLI/account state. `managed-secret` calls `resolveForUse({kind:"agentRuntime",runtimeKind:"claude_agent_sdk",credentialSlot:"apiKey"})` immediately before each model-discovery or run spawn; the catalog authorizes that consumer to reuse `provider.anthropic.api-key`. `ClaudeSdkClient` builds an empty-base child environment and adds exactly `ANTHROPIC_API_KEY` from the returned `SecretValue`; it never mutates parent `process.env`, accepts caller `env`, uses a command-line argument/file/config/session payload, serializes/persists/logs the value, or falls back to CLI/ambient aliases. Managed mode passes `tools: []`, loads no user/project/local settings, hooks, plugins, API-key helpers, or external MCP configuration, and uses strict explicitly materialized AutoByteus in-process MCP tools; AutoByteus-owned tool execution runs outside the child under sanitized launch policy. AutoByteus drops temporary references after SDK child construction/completion without claiming deterministic JavaScript or SDK-memory zeroization. Missing/locked/unavailable/corrupt/incompatible custody, invalid binding, spawn failure, and provider auth failure are distinct value-free outcomes. Native Anthropic LLM/metadata remain separate authorized consumers of the same definition.
- `REQ-019` **AutoByteus remote gateway preservation:** `provider.autobyteus.api-key` is the single definition for AutoByteus remote LLM/audio/image discovery and runtime requests. The catalog authorizes exactly `modelDiscovery/{llm|audio|image}/AUTOBYTEUS/apiKey`, `llm/AUTOBYTEUS/apiKey`, and `media/{audio|image}/AUTOBYTEUS/apiKey`. The existing provider Settings row exposes write-only save/replace/remove and value-free status for this definition and triggers the established reload flow; an explicit successful removal authoritatively clears all AutoByteus-runtime catalog subsets without lookup. `AUTOBYTEUS_LLM_SERVER_HOSTS` remains non-secret endpoint configuration. With no configured host, discovery performs zero secret lookup and authoritatively clears the matching model-kind AutoByteus runtime subset. With hosts, the server-owned remote discovery service resolves just in time and passes ephemeral authentication to storage-neutral core discovery. A model keeps its downstream provider identity but declares non-secret `credentialProviderId=AUTOBYTEUS`; generic LLM/media provisioning uses that construction descriptor. Successful discovery atomically replaces only `runtime=AUTOBYTEUS` registrations for the corresponding model kind, preserving native models with the same downstream provider; a transient pre-authoritative refresh failure preserves the last-known-good remote set and returns a value-free outcome. Missing/non-ready custody fails closed and never reads `AUTOBYTEUS_API_KEY`; explicit successful removal and absent hosts are authoritative lifecycle/configuration results, not transient failures. A legacy alias remains untouched if present but is excluded from current configuration/runtime and never reported as configured. Real tests resolve the managed definition from the read-only real-E2E Store.
- `REQ-020` **Committed explicit-source Local Store provisioning CLI:** one repository-committed PNPM command is a thin entrypoint over a reviewed TypeScript/Node importer. It requires `--source <absolute path>` and `--target default|e2e`; omission/unknown/duplicate options or a relative source fails before source access. The option adapter accepts exactly zero or one leading literal `--` sentinel so both the documented PNPM form and direct-option form reach the same canonical request; repeated or misplaced sentinels fail. Any filename or extension is accepted, including the current application `.env`, extensionless files, and renamed copies, because trust is established by explicit absolute selection and file identity/access checks—not by basename. It never searches the current/parent directory or infers a source/target. Target is a closed enum: `default` internally resolves only the canonical host Local Store at `~/.autobyteus/server-data/secret-store/{secret-store.db,secret-store.key}` and `e2e` resolves only the independent host `real-e2e-secret-store.{db,key}` pair. The command does not select a custom-data-directory, Docker, Kubernetes, remote, or enterprise Store; those deployments retain their normal Settings/deployment-specific provisioning. Callers cannot supply Store paths, definition IDs, values, backend configuration, or environment overrides; neither target can open/fall back/copy to the other. A selected pair with both files absent is `INITIALIZATION_REQUIRED`: dry-run remains zero-write and reports that state, while a confirmed write uses the existing staged Local initializer before the record batch. A one-file partial pair is `CORRUPT` and is never completed/replaced silently. Before scanning content, the CLI uses `lstat`/realpath/open-and-`fstat` identity checks to reject missing, symlink, non-regular, changed/raced, wrong-owner, or insufficiently private sources; POSIX requires current UID and no group/other permission, while Windows requires a non-mutating current-user-exclusive ACL verifier or fails closed. It rejects files over 1 MiB, NUL, and invalid UTF-8. One immutable positive alias registry maps only approved names to catalog-known definition IDs. The scanner recognizes an alias only when the line's assignment name exactly matches that registry; only then does it parse the assignment using the supported static same-line grammar. After unquoting and the defined outer-horizontal-whitespace normalization, an empty value is treated as absent/non-selected: it creates no credential, plan entry, warning, ignored-line metadata, count, or failure. Only populated recognized assignments receive dynamic-expression validation and participate in duplicate detection. One empty plus one populated occurrence of the same recognized spelling selects the populated occurrence; multiple empty occurrences remain absent; two or more populated occurrences fail `IMPORT_SOURCE_DUPLICATE_ASSIGNMENT`. Malformed recognized assignment syntax still fails before target access. Selected populated right-hand sides receive no shell evaluation, interpolation, command substitution, or assignment to `process.env`. Every line whose assignment name is unrecognized—or which is not a recognized assignment at all—is ignored without parsing or retaining its right-hand side and produces no ignored-line metadata. For Qwen, only `DASHSCOPE_API_KEY` maps to `provider.qwen.api-key`; `QWEN_API_KEY` is unrecognized and ignored without value parsing. `DATABASE_URL`, `OLLAMA_API_KEY`, `GOOGLE_CSE_API_KEY`, `QWEN_API_KEY`, `ZHIPU_API_KEY`, `CLAUDE_CODE_API_KEY`, its file-descriptor alias, malformed unrelated lines, and every other unrecognized input remain unmapped and non-blocking. No ignored-line or empty-placeholder name, content, reason, or count is returned; a source with zero populated approved mapped credentials fails value-free with `IMPORT_NO_MAPPED_CREDENTIALS` before target access or mutation. `--dry-run` performs all source/scanner/mapping/target-status checks but no prompt/write and lists each logical definition ID as `CREATE`, `SKIPPED_CONFIGURED`, or `REPLACE` according to the same request's overwrite policy. Without `--overwrite`, configured records are `SKIPPED_CONFIGURED` and never replaced; with `--overwrite`, they are `REPLACE`. Every non-dry write requires a direct TTY and exact target-specific confirmation: `IMPORT DEFAULT STORE` or `IMPORT REAL-E2E STORE`; cancellation/unconfirmed/non-TTY execution performs no write or initialization. The entire selected credential set and target plan validates before confirmation. The Local provisioning owner initializes an absent selected pair only after confirmation, then revalidates each `CREATE`-still-absent/`REPLACE`-still-present precondition and commits all planned creates and opted-in replacements in one SQLite transaction, preserves absent/unmapped/skipped records, rolls back the record batch on any failure or changed plan, checkpoints/closes, and is idempotent on rerun. A batch failure after new-pair initialization may leave a valid empty selected Store, never partially committed records; a crash-produced partial pair follows the existing fail-closed `CORRUPT` rule. Removal is unsupported. Source bytes remain unchanged and no plaintext temporary/backup/quarantine file is created. Values never enter stdout/stderr, argv, ambient environment, logs, reports, evidence, exceptions, shell commands, or committed artifacts. Owned byte buffers are overwritten and references dropped in `finally`, with an explicit limit that JavaScript/parser/SQLite/runtime string or native copies cannot be proven deterministically zeroized. Output is limited to target status, definition IDs, `CONFIGURED`/`SKIPPED`/`REPLACED` counts, restart/preflight instruction codes, and stable value-free errors. Successful default import reports restart required; successful E2E import directs value-free preflight. Hidden-input setup remains supported.
- `REQ-021` **Bounded `repository_prisma@1.0.8` clean integration:** `autobyteus-server-ts/package.json` declares `repository_prisma:^1.0.8`; `pnpm-lock.yaml` resolves exactly the published/attested `1.0.8` artifact with its unchanged `@prisma/client:^5.22.0` peer; and root package policy contains no `repository_prisma` patched-dependency entry. The obsolete `patches/repository_prisma@1.0.6.patch` is deleted, no replacement patch is created, and no `1.0.6`/`1.0.7` resolution, patch metadata/file, compatibility wrapper, dual-path behavior, or fallback remains. Both installed ESM and CommonJS entrypoints contain no `dotenv/config`, `.env` discovery, or import-time Prisma acquisition. Their upstream query-log policy defaults to `info`, `warn`, and `error`; `query` is added only through documented explicit environment or typed opt-in, typed false overrides environment true, and a post-bind policy conflict fails stably. Exact entrypoint probes execute through absolute paths with `shell:false`, an empty temporary cwd, and an empty-base platform-minimal child environment that omits parent spread, home/path/Node preload/module path, `DOTENV_CONFIG_*`, `DATABASE_URL*`, and provider variables. Narrow test-owned interception supplies only a synthetic `@prisma/client` constructor and delegates every other module. Import-only probes require zero constructor calls and no datasource; separate log-policy probes instantiate only the synthetic constructor and perform no connect/query/database activity. Package lifecycle compatibility must not replace or bypass AppConfig operational URL ownership, `createConfiguredPrismaClient`, or reviewed process-lifetime/migration-owned lazy clients. `prisma`, `@prisma/client`, `schema.prisma`, SQL migrations, SQLite data, Local secret Stores, Docker topology, and all existing security/authentication behavior remain unchanged. No raw dotenv value, query, datasource URL/path, provider output, credential, or diagnostic cause may enter validation evidence.


## Acceptance Criteria

- `AC-001` From direct browser and Electron, a Settings caller stores a synthetic credential through the same provider-specific journey into the writable default Local Store, sees status only, restarts, and completes a provider probe without plaintext files, a separate Store process, or Electron-owned credentials.
- `AC-002` Deployment coverage proves an unchanged local Docker node and a single Kubernetes server Pod with its own PVC can create, restart, and reopen their node-local Stores without Docker Compose changes. Configuration rejects shared-writable-Store replica topology and unregistered centralized backend kinds value-free. No concrete enterprise adapter or strong worker topology is required in first delivery.
- `AC-003` First-delivery hardening tests prove built-in file tools reject absolute/traversal/symlink access to Store files and governed shell, PTY, supported application-worker, MCP, and ordinary child launchers receive no provider-key aliases, Store path/key variables, backend identity, unexpected inherited descriptors, or seeded environment sentinel. Claude `cli` satisfies the same rule. Claude `managed-secret` receives exactly its authorized `ANTHROPIC_API_KEY` and no unrelated alias/sentinel/Store/backend state; parent, sibling, other governed runtime children, and AutoByteus-owned tool children receive no key. A separate Codex regression proves the ticket does not route Codex through the synthetic empty-home builder: default launch preserves pre-ticket environment/home and a pre-authenticated external `codex login` state can list a model and complete a representative turn without any AutoByteus secret lookup or account-login call. No test reads or exports Codex auth data. The result reports `LOCAL_HARDENED` only with the explicit statement that Codex environment inheritance is excluded; it does not claim arbitrary same-user process/filesystem denial or secrecy from Codex or the authorized Claude process.
- `AC-004` Prompts, tool IO, stdout/stderr, buffered Claude diagnostics, server/web/Electron logs, GraphQL errors, traces, support bundles, and test artifacts contain no exact/encoded sentinel; redaction occurs before diagnostic buffering, and a negative control proves the scanner detects a seeded leak.
- `AC-005` Representative LLM, metadata, search, media, and managed Claude runtime consumers resolve only catalog-bound definitions. Raw values unwrap only at their explicitly authorized SDK/request-auth/child-environment boundary and never enter `LLMConfig`, serialized model/run/session state, CLI arguments, or caller-provided configuration. Synthetic constructor capture proves exact Gemini LLM/media modes: AI Studio emits `{apiKey}` with no `vertexai`; Vertex Express emits `{vertexai:true,apiKey}` with no project/location; Vertex Project emits `{vertexai:true,project,location}` with no API key. Separate metadata tests prove AI Studio and Vertex Express resolve only their exact metadata consumer and call the preserved Generative Language provider path, while Vertex Project performs zero metadata secret lookup and returns curated metadata. Missing selected definitions/config, ambient aliases, alternate-definition retry, and legacy credential fallback fail closed or leave only the established curated result.
- `AC-006` Default tests run without real credentials. Tagged real suites execute all capability-available declared scenarios using backend-held real test credentials, with no `.env.test` credential load or runner/agent key environment. When `GEMINI_SETUP_MODE=VERTEX_EXPRESS`, real Gemini LLM and applicable media use the managed Vertex Express definition and `GoogleGenAI({vertexai:true,apiKey})`; model-list/reload metadata independently proves that the same selected Store definition reaches the preserved Generative Language metadata request and returned live-over-curated catalog. Evidence does not reinterpret the metadata endpoint as the LLM/media SDK mode and does not invent a Vertex Project live-key scenario.
- `AC-007` Atomic save (both initial create and replacement), idempotent remove, restart, missing, unavailable, locked, corrupt, and unsupported-format states plus subject-owned unverified/valid/invalid provider-validation states are observable without value echo. When backend health is `READY`, per-definition status is `MISSING` or `CONFIGURED`; otherwise the server keeps Settings/health reachable in degraded secret mode, returns exactly `LOCKED`, `UNAVAILABLE`, `CORRUPT`, or `INCOMPATIBLE` with a value-free instruction code, disables writes/provider construction, and never falls back to legacy environment/file values.
- `AC-008` Existing direct-browser, Electron, and remote-node Settings access behavior remains without new login/user/admin requirements. Backend configuration and provider credential lifecycle remain separate; no generic raw-secret API is exposed.
- `AC-009` Legacy-non-authority tests begin with representative synthetic canonical application aliases, custom-provider v1, ambient/test exclusions, and non-secret config. They prove both legacy files remain byte-for-byte unchanged; no Store/backend/importer/credential-migration operation occurs; approved non-secret settings remain available without sensitive aliases entering `get`, `getAll`, persistence, logs, process/child environment, or runtime consumers; custom-provider-v1 returns stable value-free reconfiguration guidance without exposing/preserving its credential or metadata automatically; and current runtime has no legacy fallback. Operator cleanup/reconfiguration is tested separately as an explicit action.
- `AC-010` Adding a representative API-key provider requires catalog/binding/auth-requirement entries but no environment read, serialized key field, or backend dependency in the concrete client.
- `AC-011` Custom-provider create probes transient input, writes its derived-definition secret, persists metadata-only schema, compensates safely, and never embeds a key in models; delete is idempotent and value-free.
- `AC-012` Repository policy rejects credential-bearing tracked files/values while permitting validated non-secret test configuration. Active `.env.test` credential loaders/copy instructions are removed; only the explicit operator-triggered CLI may open the supplied absolute source during setup.
- `AC-013` First delivery reports `LOCAL_HARDENED` only after governed file-root, empty-base environment, descriptor, redaction, and no-plaintext-custody checks pass. The status/documentation explicitly excludes the preserved Codex external-login environment/home and exposes same-user/all-in-one limitations. It never reports `STRONG_AGENT_ISOLATION`; strong-tier enforcement and acceptance are deferred.
- `AC-014` One conformance suite passes against InMemory and Local read-write/read-only variants for their declared capabilities, including missing/unavailable/locked/corrupt/incompatible health, empty-Store pair binding, configured-location denial, atomic replacement, redacted diagnostics, and no fallback. A test-only externally-managed fixture covers capability projection. No production enterprise adapter is required; Local destructive/write cases use temporary Stores.
- `AC-015` Normal Electron/local startup opens the default Store in process. A fresh worktree opens the physically separate real-E2E Store using tracked configuration, preflights successfully, and runs without copying or requesting credentials. The two Stores have distinct database/key files and authenticated pair verifiers. Empty Store + correct key reaches `READY`; swapped key, one missing file, or tampered verifier reaches `CORRUPT`; unsupported verifier/schema/encryption format reaches `INCOMPATIBLE`; none is rewritten. Ordinary runtime-data reset preserves both; only an explicit exact-Store reset deletes one.
- `AC-016` A trusted setup command directly provisions dedicated test credentials into only the host real-E2E Store and produces status without value readback. It has no source/default-Store parameter or read path. A missing E2E value never falls back. Host real-provider execution opens the E2E Store read-only; Store CRUD tests use a disposable writable Store. Existing Docker Compose/launcher remain unchanged.
- `AC-017` Backend configuration can be supplied at server startup and represented through Settings where supported. First delivery accepts only registered InMemory/test and Local kinds; an unregistered enterprise kind returns value-free unsupported-backend guidance. Writable Local enables labeled write-only input states; an externally-managed test descriptor renders an accessible disabled reason without shipping a concrete enterprise adapter. No saved value is prefilled/browser-persisted, successful/closed editors clear transient input, and saved backend changes report restart-required.
- `AC-018` Claude authentication coverage proves: (a) omitted/`cli` configuration performs zero secret lookups and starts from an allowlist with no ambient Anthropic/Claude key or unrelated parent value; (b) explicit `managed-secret` performs one catalog-authorized just-in-time resolve per model-discovery/run child, injects only `ANTHROPIC_API_KEY` into that exact child, passes `tools: []`, has empty setting sources and strict explicitly materialized AutoByteus MCP, and completes a real SDK authentication/request using the real-E2E Store; (c) synthetic capture proves parent, sibling, unrelated children, AutoByteus-owned tool children, config/session/log/diagnostic/artifact outputs contain no exact/encoded canary, while the real run uses structural redaction/evidence checks without reading or exporting the real value; (d) missing, `LOCKED`, `UNAVAILABLE`, `CORRUPT`, `INCOMPATIBLE`, invalid binding, spawn failure, and provider-auth failure are value-free and never fall back; and (e) `auto`, `api-key`, or any invalid mode fails `CLAUDE_RUNTIME_AUTH_MODE_INVALID` before resolution/spawn. Native Anthropic LLM/metadata continue to use the same definition through their separate consumer identities.
- `AC-019` AutoByteus remote gateway coverage proves: (a) provider Settings can save/replace/remove `provider.autobyteus.api-key`, return only backend/storage status, and run the existing full/provider reload journey; explicit successful removal idempotently clears all AutoByteus-runtime catalog subsets without lookup; (b) no hosts causes zero lookup plus authoritative clear of the matching AutoByteus runtime subset, while configured hosts cause only the exact LLM/audio/image discovery consumer to resolve; (c) remote discovery preserves downstream provider metadata, sets `credentialProviderId=AUTOBYTEUS`, synchronizes only AutoByteus-runtime registrations, and never removes native same-provider models; a transient pre-authoritative failure preserves last-known-good; (d) remote LLM/audio/image construction resolves only its exact AutoByteus construction consumer and places no key in model/config/GraphQL/log/environment/artifact state; (e) a synthetic legacy `AUTOBYTEUS_API_KEY` remains source-file-unchanged but is absent from current configuration/runtime, while `AUTOBYTEUS_LLM_SERVER_HOSTS` remains available as approved non-secret configuration; and (f) capability-available real-E2E scenarios perform authenticated remote discovery plus representative LLM invocation, speech generation, and image generation from the read-only real-E2E Store, or report the exact unavailable capability rather than claiming a pass.
- `AC-020` Provisioning-CLI coverage proves with synthetic canaries that: (a) missing/unknown/duplicate options, repeated or misplaced `--`, relative source, target omission, Store-path/value/definition/backend/environment overrides, directory search, and default-to-E2E access fail before value handling, while both zero/one leading `--` forms and arbitrary source basenames/extensions are accepted; (b) source checks reject missing/symlink/non-regular/raced/wrong-owner/non-private/unverifiable files plus oversize/NUL/invalid-UTF-8 content; (c) recognize-first scanning parses only exact selected aliases, rejects malformed recognized syntax, dynamic populated values, duplicate populated occurrences, and zero populated mapped definitions before target access, while `NAME=`, whitespace-only, `NAME=""`, and `NAME=''` forms for a recognized alias are absent/non-selected; empty plus one populated occurrence selects only the populated value, multiple empty occurrences remain absent, and an empty Gemini placeholder does not block a populated Vertex credential; (d) a mixed current-application-style fixture containing unrelated text, malformed unknown lines, `DATABASE_URL`, `OLLAMA_API_KEY`, `GOOGLE_CSE_API_KEY`, `QWEN_API_KEY`, `ZHIPU_API_KEY`, Claude delivery aliases, and other unknown names succeeds by ignoring them without interpreting their values; (e) the positive registry includes all current import-supported product credentials, maps Qwen only from `DASHSCOPE_API_KEY`, resolves only catalog-known definitions, never maps `QWEN_API_KEY` or `ZHIPU_API_KEY`, and exposes no ignored-line or empty-placeholder metadata; (f) value-free dry-run and write plans show definition IDs/status/counts only, no-overwrite defaults to skip, `--overwrite` is required for replacement, exact direct-TTY confirmations distinguish default/E2E, cancellation makes no change, and default import reports restart-required; (g) both-absent target reports `INITIALIZATION_REQUIRED`, dry-run creates nothing, confirmed execution initializes only the selected pair, one-file partial/crash pairs fail `CORRUPT`, and a failed record batch commits no records while a newly created valid empty Store may remain; (h) confirmed batch import atomically creates/opted-in-replaces mapped records, preserves absent/unmapped/skipped records and source bytes, rolls back the record batch on injected failure or changed create/replace precondition, is idempotent, and never opens the other Store; (i) captured argv, environment, stdout/stderr, logs, diagnostics, exceptions, reports, artifacts, and synthetic fixtures contain no exact/encoded selected or ignored canary outside the intentionally encrypted target, with a seeded negative leak control; and (j) operator validation uses the current application `.env` by explicit absolute path for value-free dry-run/E2E import, proves its empty Gemini placeholder is ignored while populated recognized credentials proceed, then runs real-E2E preflight/full capability-available execution without reading/exporting real values. Hidden-input setup still passes. Current real OpenAI LLM/agent-flow provider-operation failures remain separately classified and are not attributed to credential provisioning because OpenAI audio/image pass with the same configured definition.
- `AC-021` Dependency integration evidence proves: (a) the manifest requests `repository_prisma:^1.0.8`, the lock contains one exact attested `1.0.8` resolution with the existing Prisma 5.22 peer, and no `1.0.6`/`1.0.7` resolution remains; (b) root package metadata has no `repository_prisma` patched-dependency key, the obsolete `1.0.6` patch file is absent, no replacement patch exists, and clean/frozen installation succeeds; (c) static checks of exact installed ESM and CommonJS entrypoints find no `dotenv/config`, `.env` discovery, or import-time client construction; (d) children execute those exact entrypoints from a newly created empty cwd with an environment built from `{}` plus only required platform-minimal variables and explicit synthetic log-policy input—never parent spread, home/path/Node preload/module path, dotenv/database/provider variables; (e) import succeeds with zero synthetic Prisma constructor calls and no datasource; (f) a separate synthetic-constructor phase proves no `query` log by default, explicit environment and typed opt-in add it, typed false overrides environment true, and conflicting post-bind policy returns the stable value-free code, all without connect, database open, SQL, or path/value output; (g) package lifecycle compatibility, production build/sanitized bootstrap, and existing CR-009–CR-014 restart/reopen/import/lazy-owner regressions pass; and (h) production source adds no `repository_prisma` owner while `prisma`/`@prisma/client` versions, generated schema, SQL migrations, persisted SQLite data, Local secret Store files, Docker topology, and every approved secret/authentication behavior remain unchanged. Evidence contains no dotenv value, datasource URL/path, raw query, secret, provider diagnostic, or raw cause.


## Constraints / Dependencies

- Actual existing secret values must never be inspected or included in solution artifacts or conversations.
- Provider SDKs require plaintext in trusted memory unless a gateway is used; JavaScript cannot promise deterministic zeroization.
- The provisioning CLI is a trusted local transition process, not a secrecy boundary against the same OS identity. It necessarily opens/parses approved source bytes and handles them transiently; non-echoing, atomic writes, and buffer cleanup reduce disclosure but do not prove zero residual JavaScript/runtime copies.
- The explicitly supplied source must be operator-owned and access-restricted. If the platform cannot verify the required non-mutating ownership/access policy, import fails and hidden-input provisioning/Settings remain the supported alternatives.
- One-time first-machine provisioning and later rotation/revocation cannot be eliminated because AutoByteus cannot create external provider credentials.
- A committed Store selection/path is configuration, not proof of authority. Local database/key files remain outside the checkout and agent environment.
- Direct real-key execution trusts the reviewed server/provider code receiving that key; use test-scoped credentials and protected execution.
- Automatic local unlock through an owner-only key file is lower assurance unless process/filesystem isolation protects that machine-local key. Physical database/key separation improves defense in depth but does not defeat arbitrary same-user code execution.
- Host read-only E2E use requires the provisioning writer to finish/checkpoint/close before test execution; tests must not provision concurrently with real-provider execution.
- A Local SQLite Store belongs to one independent server node/persistent-volume domain. It is not a shared multi-replica database; multi-node deployment requires a separately delivered/installed centralized backend.
- Kubernetes Secret objects are not encrypted merely by base64 encoding; operators own etcd encryption/RBAC where native Secrets are used.
- The externally-managed lifecycle variant remains an extension/test contract in first delivery; no concrete enterprise adapter is implied.
- Codex CLI/app-server authentication is external runtime-owned state. The operator uses `codex login`; AutoByteus does not inspect, import, resolve, migrate, display, rotate, or choose that state. The Codex launch remains outside the new empty-base child-environment guarantee, and its inherited operator environment/account files are an explicit `LOCAL_HARDENED` limitation.
- Claude CLI/account authentication is external runtime-owned state and is not imported into AutoByteus custody. Managed-secret mode intentionally trusts the exact Claude SDK child with one Anthropic credential; the child/SDK can observe it, JavaScript cannot guarantee zeroization, and a compromised Claude executable remains outside the non-disclosure guarantee.
- `EXT-ANTHROPIC-AGENT-SDK-AUTH` is a maintained external release dependency: release review must re-check Anthropic's official Agent SDK overview, legal/authentication guidance, and dated subscription-usage Help Center notice. The current official sources conflict; technical operability and user/operator consent are not treated as independent proof of contractual permission.
- `AUTOBYTEUS_LLM_SERVER_HOSTS` is non-secret configuration and may be tracked or managed through existing Settings; the corresponding API key must exist only in the selected Store. Remote endpoint availability and account capabilities determine which real LLM/audio/image scenarios can execute.
- `repository_prisma@1.0.8` remains peer-compatible with `@prisma/client:^5.22.0`; exact packed-artifact and isolated-probe evidence, not patch-semver classification alone, establishes its import/log/lifecycle compatibility.
- Both published `repository_prisma@1.0.8` entrypoints omit package-owned dotenv loading and `.env` discovery. Required probes still use an empty temporary cwd and empty-base environment as regression protection; they never open a real source file or database.

## Persisted Data Outcome (When Applicable)

- Stored subject / location: canonical local server-data `.env`, operator-selected absolute source files with arbitrary filenames/extensions, custom-provider schema-v1 JSON, public Nuxt credential fields, dedicated encrypted default/real-E2E Store databases and keys, backend configuration, explicit operator-import state, and ordinary Prisma-backed SQLite application data.
- Required outcome: `Directly Usable — No Migration` for approved non-secret application settings projected from the unchanged legacy file; `Discard or Rebuild` for legacy credential authority and custom-provider-v1 runtime records; `Not Affected` for ordinary Prisma-backed SQLite data under the `repository_prisma@1.0.8` dependency-only update. There is no automatic persisted-data transformation. UC-019 is a separate, explicitly invoked operator import whose source remains unchanged.
- Existing data to preserve, discard/rebuild, transform, or quarantine: leave legacy `.env` and custom-provider-v1 bytes untouched; preserve only approved non-secret application behavior through a non-retaining projection; ignore all legacy credential aliases; require operator cleanup plus UI recreation for custom-provider-v1; and permit assignment-file value transformation only through UC-019. No automatic quarantine/backup/ledger is created.
- Unacceptable data loss or corruption: automatic mutation of either legacy source, exposure/use of a legacy credential, or silent fallback is unacceptable. Loss of legacy custom-provider continuity is explicitly accepted; users recreate it.
- Relevant availability, maintenance-window, or rollout constraints: secret consumers remain unavailable until required values are explicitly provisioned. Legacy custom providers remain unavailable until operator cleanup and recreation; Local Stores remain current-schema-only.
- Related requirement and acceptance-criteria IDs: REQ-002, REQ-012, REQ-014, REQ-016–REQ-021 / AC-007, AC-009, AC-012, AC-015, AC-016, AC-018–AC-021.

## Assumptions

- The AutoByteus server remains the trusted control/provider-client boundary.
- Electron starts its embedded Agent Server; that server constructs the Local backend in process against the default Store.
- Direct local and real-E2E servers can construct the same Local backend implementation against different bootstrap-selected physical Stores.
- Companies can implement/register additional adapters in the open-source server, but none is shipped by this first delivery.
- Worktree creation starts from tracked Git content, so committed non-secret configuration is present without copying.
- A root live-test launcher can derive the canonical host real-E2E Store paths without user-provided secrets.
- The provisioning CLI is explicitly run by a trusted local operator. Agent runtimes, worktree automation, ordinary tests, server startup, and migration cannot invoke it as part of supported execution.
- The team workflow performs source review before real API/E2E direct-secret execution.

## Risks / Open Questions

1. Prove the built-in `node:sqlite`/selected SQLite access and Electron/Node bundling path, multi-process busy handling, read-only opening/checkpoint behavior, owner-only ACL posture, staged fail-closed key/database creation, and authenticated pair-verifier behavior.
2. Legacy application files and the explicit UC-019 source remain operator-owned and unchanged; until separately cleaned up, they remain same-user plaintext residuals. Startup never mutates them. No path proves secure erasure of historical blocks/backups/snapshots.
3. Classify each live scenario as synthetic, gateway, or reviewed direct-secret and assign provider quota/concurrency policy.
4. Future-ticket choices, explicitly not first-delivery blockers: first concrete enterprise adapter, first deployable `STRONG_AGENT_ISOLATION` mechanism, and a stronger non-environment Claude delivery mechanism if the upstream SDK later exposes a stable cross-platform secret channel.
5. Anthropic's June 15–16 Help Center notice explicitly preserves current third-party Agent SDK subscription usage, but its SDK overview and legal/authentication pages retain contrary restriction language. Architecture review must reassess MP-002 against both current official sources. Release owners must monitor or obtain clarification if the conflict persists; this package does not misstate current technical operation as standalone legal proof.
6. Confirm which AutoByteus remote LLM/audio/image capabilities are present on each real-E2E endpoint and apply per-capability execution/reporting without weakening synthetic coverage or silently skipping an advertised capability.

## Requirement-To-Use-Case Coverage

| Use Case | Covered Requirements |
| --- | --- |
| UC-001 built-in provider lifecycle | REQ-001, REQ-002, REQ-006–REQ-008, REQ-010 |
| UC-002 custom provider lifecycle | REQ-001, REQ-002, REQ-005, REQ-006, REQ-008, REQ-011, REQ-014 |
| UC-003 backend configuration | REQ-007, REQ-010, REQ-013, REQ-015 |
| UC-004 local backend bootstrap | REQ-010, REQ-012, REQ-017 |
| UC-005 physical Local Store lifecycle | REQ-010, REQ-012, REQ-015, REQ-017 |
| UC-006 direct real-E2E Store provision | REQ-005, REQ-012, REQ-017 |
| UC-007 LLM construction/invocation | REQ-001, REQ-005, REQ-006, REQ-008, REQ-011 |
| UC-008 non-LLM client construction | REQ-001, REQ-005, REQ-006, REQ-008, REQ-011 |
| UC-009 deterministic testing | REQ-008, REQ-009, REQ-015 |
| UC-010 real-provider host-worktree testing | REQ-004, REQ-005, REQ-008, REQ-009, REQ-012, REQ-016, REQ-017 |
| UC-011 Local backend concurrency/reset | REQ-010, REQ-012, REQ-017 |
| UC-012 container deployment/extension boundary | REQ-004, REQ-010, REQ-013, REQ-015 |
| UC-013 capability-aware Settings | REQ-006, REQ-007, REQ-010, REQ-015 |
| UC-014 first-delivery agent hardening and Codex preservation | REQ-003, REQ-004, REQ-008, REQ-012, REQ-013 |
| UC-015 legacy-source non-authority | REQ-002, REQ-014, REQ-016 |
| UC-016 backend conformance | REQ-008, REQ-010, REQ-012, REQ-015, REQ-017 |
| UC-017 Claude runtime authentication cutover | REQ-004, REQ-005, REQ-014, REQ-018 |
| UC-018 AutoByteus remote model gateway lifecycle | REQ-001, REQ-005, REQ-008, REQ-009, REQ-011, REQ-014, REQ-016, REQ-017, REQ-019 |
| UC-019 bounded local environment-secret import | REQ-002, REQ-004, REQ-008, REQ-009, REQ-012, REQ-014, REQ-016, REQ-017, REQ-020 |
| UC-020 `repository_prisma` 1.0.8 clean dependency replacement | REQ-021 |

## Acceptance-Criteria-To-Scenario Intent

| Acceptance Criterion | Scenario Intent |
| --- | --- |
| AC-001 | Browser/Electron default-Store save, restart, real construction probe |
| AC-002 | Local Docker/single-Pod persistence and unsupported centralized-kind rejection |
| AC-003 | File-tool denial, governed empty-base launcher environment/descriptor matrix, unchanged external Codex-login launch regression, and honest Codex-excluded lower-tier reporting |
| AC-004 | End-to-end redaction/canary success and negative control |
| AC-005 | Consumer/definition authorization, exact three-mode Gemini LLM/media construction, preserved dual-key Generative Language metadata construction, trusted-boundary reveal, Vertex Project metadata zero-lookup, and no alternate-definition/ambient fallback |
| AC-006 | Default deterministic plus substantive real-provider suite, including product-path Vertex Express LLM/media and separately preserved dual-key model-metadata enrichment |
| AC-007 | Lifecycle/status/backend failure/no-fallback matrix |
| AC-008 | Existing Settings access preservation and no generic secret API |
| AC-009 | Synthetic unchanged-source, non-authority, value-free v1 rejection, and no-Store-write matrix |
| AC-010 | Representative new-provider extension test |
| AC-011 | Custom-provider transaction/compensation/delete matrix |
| AC-012 | Repository policy and removal of credential dotenv/copy behavior |
| AC-013 | Verified `LOCAL_HARDENED` reporting and no strong-tier claim |
| AC-014 | InMemory/Local conformance, pair-binding failures, and test-only external capability projection |
| AC-015 | Separate default/E2E Stores with authenticated empty-Store pair validation |
| AC-016 | Hidden-input/direct E2E setup plus optional explicit source import, no default-Store access/fallback, read-only host E2E, Docker non-impact |
| AC-017 | Registered Local startup/Settings behavior and unsupported enterprise-kind guidance |
| AC-018 | Claude two-mode selection, managed JIT child-only delivery, real SDK authentication, failure matrix, and negative leak controls |
| AC-019 | AutoByteus Settings/status, exact discovery/construction bindings, runtime-scoped catalog sync, legacy-alias non-authority, and real remote LLM/audio/image coverage |
| AC-020 | Explicit absolute-source/closed-target isolation, source trust, recognize-first empty-as-absent/populated-value mapping, target-specific confirmation, atomic/idempotent batch, leak controls, default restart, and real-E2E validation |
| AC-021 | Exact 1.0.8 manifest/lock, obsolete patch removal, clean/frozen install, dotenv-free/import-safe/default-off-log compatibility, unchanged Prisma/schema/data/owners, and preserved build/restart regressions |

## Approval Status

BEH-014/REQ-020/AC-020/UC-019 and the no-automatic-update outcome remain user-approved. On 2026-07-22 the user explicitly resolved CR-019 by directing AutoByteus to leave Codex authentication alone because the supported journey is external `codex login`: preserve the established Codex launch/account behavior and add no managed Store consumer, auth mode/UI/status/rotation owner, or synthetic account state. The package therefore excludes Codex from REQ-004/`LOCAL_HARDENED` environment non-inheritance rather than pretending it is sanitized. CR-020 is a behavior-preserving contract correction: the already-approved Gemini modes must cross LLM/media provisioning as distinct resolved variants so Vertex Express reaches `{vertexai:true,apiKey}` and cannot collapse into AI Studio or fall back. For CR-021, the user confirmed that the original dual-key metadata path works. Code review round 25 withdrew the claim that the Generative Language endpoint is invalid for Vertex metadata. The approved outcome is therefore preservation, not redesign: AI Studio and Vertex Express select their exact Store-backed metadata consumer and continue through the same `GeminiModelMetadataProvider`/Generative Language endpoint/live-over-curated flow; Vertex Project remains curated-only with zero metadata secret lookup. On 2026-07-22 the user additionally approved direct mixed current-application-file import through exact positive selection, `DASHSCOPE_API_KEY` as the only Qwen mapping, ignored/unmapped `QWEN_API_KEY` and legacy `ZHIPU_API_KEY`, no ignored-content reporting, and the documented single leading PNPM separator. After real execution exposed an empty recognized Gemini placeholder alongside populated Vertex and other credentials, the user explicitly approved treating every syntactically valid normalized-empty recognized assignment as absent/non-selected: it produces no plan entry, metadata, warning, or failure, populated credentials continue, populated duplicates remain invalid, and an all-empty/absent source returns `IMPORT_NO_MAPPED_CREDENTIALS`. The user also approved BEH-015/REQ-021/AC-021/UC-020 as a strict clean replacement: use latest verified `repository_prisma@1.0.8`, delete obsolete local patch integration, retain no `1.0.6`/`1.0.7` compatibility path, preserve default-off/explicit-opt-in query logging and current database owners, and do not upgrade Prisma ORM/client or change schema/data. All runtime, Store format, target, Docker, Claude, AutoByteus gateway, assurance-tier, and external-dependency decisions otherwise remain unchanged. `EXT-ANTHROPIC-AGENT-SDK-AUTH` remains mandatory in every handoff.
