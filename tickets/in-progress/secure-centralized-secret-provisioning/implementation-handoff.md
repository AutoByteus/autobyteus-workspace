# Implementation Handoff

## Upstream Artifact Package

- Requirements doc: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/requirements.md`
- Investigation notes: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/investigation-notes.md`
- Design spec: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/design-spec.md`
- Supplemental task artifacts:
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/use-case-spine-validation.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/secret-storage-architecture.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/secret-storage-backend-contract.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/credential-consumer-mapping.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/live-test-secret-provisioning.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/threat-model-and-option-analysis.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/repository-prisma-1.0.8-assessment.md`
- Design review report: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/design-review-report.md`
- Prior implementation-source review report: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/code-review-report.md`
- API/E2E coverage investigation: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/coverage-investigation.md`
- Failed execution coverage report: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-coverage-report.md`
- Prior proportional API/E2E test review: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/api-e2e-test-review-report.md`
- Prior delivery/docs/release context (historical; not implementation authority):
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/docs-sync-report.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/delivery-anthropic-auth-recheck.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/release-deployment-report.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/release-notes.md`
  - `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/handoff-summary.md`
- Preserved failure evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/18-browser-backend-runtime.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/23-restart-failure-source-diff.log`
- Preserved Round-2 restart/Docker evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/26-round2-durable-restart-rerun.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/31-round2-docker-build-up.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/33-round2-docker-failure-source.log`
- Preserved Round-6 restart failure evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/53-round6-importer-appconfig-policy.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/54-round6-clean-frozen-install.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/55-round6-server-build.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/56-round6-server-restart.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/57-round6-restart-failure-origin.log`
- Preserved Round-8 environment/importer evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/68-round8-base-refresh-compatibility.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/69-round8-exact-docker-build-up.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/70-round8-docker-lifecycle.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/71-round8-cleanup-scan.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/72-round8-summary-scan.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/73-round8-importer-current-env-key-audit.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/74-round8-importer-current-env-dry-run.log`
- Preserved Round-10 real-provider evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/87-round10-real-preflight.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/88-round10-real-configured-capabilities.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/89-round10-provider-failure-diagnostics.log`; `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning/execution-evidence/90-round10-summary-scan.log`

## What Changed

- Reconciled CR-021 as an artifact-only requirement clarification with no production or test source change. Exact Gemini AI Studio, Vertex Express, and Vertex Project variants remain mandatory for LLM/media construction only. Gemini live metadata remains its separate established contract: `ModelMetadataProvisioningService` resolves only the exact AI Studio or Vertex Express Store consumer, `GeminiModelMetadataProvider` retains the Generative Language models request and mapping, `ModelMetadataResolver` retains live-over-curated merge behavior, and Vertex Project installs no live Gemini metadata provider and performs zero metadata secret lookup. No metadata SDK-mode DTO/union, provider/service/type, endpoint, alias, alternate-definition retry, ambient source, Store fallback, or Vertex Project key inference was added.
- Corrected CR-019 to the round-18 governed-launcher boundary. Codex App Server is explicitly outside this ticket's empty-base child policy, so `CodexAppServerClient` now restores only its exact pre-ticket `env: this.options.env ?? process.env` launch behavior and removes the ticket-added child-environment helper import. This preserves real node-local `HOME`/`CODEX_HOME` account state without adding any Codex Store consumer, account/login RPC, authentication mode, status/rotation owner, synthetic production home, wrapper, fallback, or environment non-inheritance claim.
- Corrected CR-020 by carrying explicit Gemini mode semantics through the existing construction boundary. `LLMConstructionTarget` remains exactly `{credentialProviderId, authenticationRequirement}` with `modelIdentifier` separate; the shared requirement is now `geminiAuthenticationMode`, and provisioning resolves exact `geminiAiStudio`, `geminiVertexExpress`, or `geminiVertexProject` variants from explicit `GEMINI_SETUP_MODE`. `gemini-helper.ts` constructs exactly `{apiKey}`, `{vertexai: true, apiKey}`, or `{vertexai: true, project, location}` respectively. Generic API-key/none variants, unknown modes, and cross-provider Gemini requirements fail rather than infer or fall back.
- Reconciled the explicit Local environment-secret importer to the round-15 clean-cut design. The root `secrets:local:import` PNPM command accepts zero or one leading PNPM `--`, one required absolute source, one required `default|e2e` target, `--dry-run`, and `--overwrite`. File trust/identity, size, UTF-8, and NUL checks remain global; the scanner then recognizes exact positive registry names before parsing and validates only selected assignments. Every unrecognized line is ignored without right-hand-side interpretation or ignored-line metadata. `DASHSCOPE_API_KEY` is the sole Qwen mapping; `QWEN_API_KEY` and `ZHIPU_API_KEY` remain unmapped/non-blocking. Value-free planning, target-specific direct-TTY confirmation, and the selected-Store atomic batch remain unchanged.
- Corrected CR-018 at the source-reader boundary. Bare-carriage-return syntax is no longer validated against the entire mixed source before positive selection: an unrelated physical line containing a bare carriage return is discarded without right-hand-side parsing and cannot block a later recognized credential assignment, while a bare carriage return within a positively selected recognized assignment still fails value-free with `IMPORT_SOURCE_SYNTAX_INVALID`.
- Implemented the round-16 empty-as-absent import rule at that same source-reader boundary. Every recognized line must first satisfy the existing static same-line assignment grammar; the reader then unquotes and removes outer horizontal whitespace. A normalized-empty occurrence is discarded before populated-selection, duplicate tracking, buffer construction, plan metadata, or counts. Repeated empties remain absent, one populated occurrence wins regardless of surrounding empty occurrences, and two populated occurrences still fail. All-empty recognized input returns `IMPORT_NO_MAPPED_CREDENTIALS` before target resolution/access. `IMPORT_SOURCE_EMPTY_CREDENTIAL` is removed from the production error union and tests. This does not create Gemini-to-Vertex fallback or choose a provider mode; each populated alias continues to map independently through the positive registry.
- Renamed the shared domain, service, registry, and service test to `local-environment-secret-import.ts`, `local-environment-secret-import-service.ts`, `local-import-credential-alias-registry.ts`, and `local-environment-secret-import-service.test.ts`, with `LocalEnvironmentSecretImport*` symbols throughout. Every production/test import now uses the current paths. The old files, negative secret-like classifier, ignored metadata concept, obsolete unsupported/alias-conflict errors, wrappers, and compatibility re-exports are absent.
- Removed the automatic legacy credential updater, startup invocation, source rewriter/converter, parent-alias deletion, migration ledger, and its obsolete test. `AppConfig` now imports current positive alias names for its separate non-secret exclusion boundary and retains its broader config-owned credential exclusions; it identifies and masks excluded assignments by name before invoking the generic dotenv parser. The mask spans every ECMAScript/dotenv-accepted leading and post-separator whitespace form (BOM, Unicode spaces, vertical-tab, form-feed, and line terminators), optional `export` whitespace, accepted equals/colon separators, and the complete multiline single/double/backtick-quoted value using dotenv-compatible escaped-quote closure. Excluded values never enter the generic parser input, parsed record, or retained configuration. Matching parent aliases remain ignored; startup stays read-only for the application `.env`; explicit supported non-secret writes preserve all non-target lines and original line endings. Existing v1 custom-provider JSON remains byte-identical and fails with value-free `CUSTOM_PROVIDER_LEGACY_RECONFIGURATION_REQUIRED`; no Store access, conversion, or fallback occurs.
- Extended only the Local setup implementation with absent/present pair inspection and a package-local exact batch. Existing-pair inspection is structurally read-only and non-initializing, including non-mutating Windows ACL verification, so dry-run and pre-confirmation planning do not change journal mode, bytes, records, or sidecars. Confirmed execution may use the existing staged initializer; partial pairs and every other non-ready state retain their exact closed value-free health/instruction with no definition projection; all CREATE/REPLACE record preconditions are validated within one `BEGIN IMMEDIATE` transaction; injected later-write failure rolls back earlier writes. The generic backend and `SecretManagementService` ports were not widened, the other Store is never opened, and hidden-input real-E2E provisioning remains supported.
- Cleanly replaced patched `repository_prisma@1.0.6` with the exact locked and attested upstream `1.0.8` package while retaining `prisma` and `@prisma/client` 5.22.0. The obsolete root `patchedDependencies` entry, `patches/repository_prisma@1.0.6.patch`, and noncanonical stale `autobyteus-server-ts/pnpm-lock.yaml` are removed; installation remains monorepo-root-owned by the canonical workspace lock. Every surviving tracked lockfile is residue-scanned. No replacement patch, wrapper, dual path, fallback, or production import exists. Exact installed ESM/CommonJS entrypoints are checked from an empty temporary cwd and empty-base platform-minimal children using absolute `shell:false` execution and narrow synthetic `@prisma/client` interception. The probe proves no dotenv or `.env` discovery, zero import acquisition, default `info|warn|error`, explicit environment/typed query opt-in, typed-false precedence, stable conflict behavior, and zero real connect/query/database activity.
- Retained the round-4 centralized-secret implementation: server-owned lifecycle/catalog/configuration boundaries, InMemory and encrypted Local SQLite custody, early backend bootstrap, pair-authenticated default/E2E Stores, explicit JIT provider construction, runtime-only credential authority, empty-base execution policy, rich value-free health/status, and unchanged Docker topology.
- Restored AutoByteus remote LLM/audio/image discovery and invocation through one managed definition, `provider.autobyteus.api-key`, without restoring an ambient `AUTOBYTEUS_API_KEY` read.
- Added exact discovery consumers for `modelDiscovery/{llm|audio|image}/AUTOBYTEUS/apiKey`, server-owned JIT resolution, a narrow `SecretValue`-carrying discovery-authentication shape, storage-neutral core discovery ports, runtime-scoped authoritative synchronization, zero-lookup clear for absent hosts or explicit credential removal, and last-known-good retention on transient pre-authoritative failure. Raw reveal now occurs only in each core `AutobyteusClient` construction expression.
- Tightened model construction routing. `LLMConstructionTarget` and the multimedia equivalent contain exactly `credentialProviderId` and `authenticationRequirement`. Native registrations materialize their known credential owner once; discovered AutoByteus-runtime registrations explicitly materialize `credentialProviderId=AUTOBYTEUS`. Generic LLM/media provisioning constructs its consumer only from that field and the tagged requirement-owned slot.
- Preserved downstream provider identity for discovered models, native/remote same-provider coexistence, runtime/model-kind-scoped replacement, startup/list/full/provider reload paths, and LLM/audio/image discovery/invocation hooks.
- Made the existing AutoByteus Settings provider row fully managed: write-only save/replace/status plus idempotent remove. After successful credential storage, replacement advances every discovery generation and invalidates completed-host/cache state before the established full refresh, without retaining or comparing raw secret material. Removal likewise advances generations, invalidates configuration-bound in-flight reuse, and serializes/fences registry publication before clearing all AutoByteus-runtime LLM/audio/image subsets without resolving the removed definition. The web schema binding, generated GraphQL types, Pinia state, runtime notifications, localized controls, pending-state bindings/action guards, and component tests were updated.
- Retained `AUTOBYTEUS_API_KEY` only in the immutable positive Local-import registry and separate non-secret exclusion boundary. Current production reads remain absent; the unchanged core header name is a wire-protocol constant, not an environment lookup. No scrub/reprovision migration remains.
- Completed retained source-review fixes: Claude CLI maps the actual node-local OS home (or a validated existing override) into its empty-base child; stdio MCP composes sanitized operational variables plus exact configured additions; absent custom-provider deletion succeeds idempotently while built-in deletion is still rejected.
- Completed the bounded source-review fixes: discovery authentication stays wrapped through the server coordinator and core ports; stale discovery cannot publish after credential removal, credential replacement, or host replacement; and generic provider removal disables and rejects overlapping input/reveal/save/remove actions.
- Corrected CR-009 at the configuration owner boundary. `AppConfig` validates and explicitly returns the selected non-secret SQLite `DATABASE_URL`; every Prisma migration child receives only that explicit configured URL in addition to its operational process environment, and every in-process Prisma client is constructed through one datasource-override factory. A persisted approved URL is parsed privately; a missing URL is derived deterministically into runtime state without rewriting `.env`. No broad dotenv injection, provider-secret alias, or process-wide credential path was restored.
- Preserved the API/E2E-owned built-server two-process regression and evidence from CR-009. API/E2E already reconciled its first-start assertion to the round-10 byte-identical startup authority; implementation did not rewrite or reset that downstream-owned test/evidence while correcting the production startup writer it exposed.
- Corrected CR-010 without weakening CR-009. Token-usage and app-data-migration repository/database owners no longer acquire configuration or construct Prisma clients during module evaluation or default-argument evaluation. Each owner accepts an injected client or lazily acquires the configured datasource client on its first database operation; owned clients that were never acquired are not created merely for disconnect.
- Made the production build smoke itself a sanitized regression. `build:full` now runs the unchanged built-in-agent bootstrap behavior in an exact child containing only operational host variables and no `DATABASE_URL`. A focused mocked-factory test also proves imports and owner construction perform zero Prisma acquisition, while each first database operation performs exactly one lazy acquisition. No Docker/Compose/launcher file, dummy build URL, ambient Prisma fallback, dotenv injection, or provider alias was added.
- Corrected CR-011 by restoring one bounded process-lifetime Prisma owner for default token-usage repositories. The owner is constructed safely at module import but creates its explicit configured client only on the first database operation; every request-scoped default repository/store then reuses that same client. Caller-injected repository clients remain isolated from the default owner, and migration-owned lazy clients retain deterministic disconnect without disconnecting injected clients.
- Corrected CR-016 by separating built-in agent runtime default selection from explicit Settings persistence. `BuiltInAgentBootstrapper` now registers the resolved retrospective skill improver only as an in-memory runtime default through `ServerSettingsService`; configured operator values remain authoritative, the effective default remains visible/editable through Settings, ordinary repeated bootstrap does not touch the application `.env` or process environment, and an explicit supported Settings update still persists and immediately overrides the runtime default. The bootstrap result/smoke now reports `initializedRuntimeDefault` rather than implying a persistent write.
- Corrected Claude scope wording: both `cli` and `managed-secret` use an empty-base child environment and never fall back. Only `managed-secret` receives the exact child key and enforces `tools: []`, empty setting sources, and strict explicit AutoByteus in-process MCP. CLI uses the existing external account state and normal CLI tools/settings/MCP behavior while performing zero secret-management lookup.

## Reviewed Behavior Implementation Trace

| Behavior ID | Approved Change / Preserved Outcome | Implemented Production Path / Key Files | Result / Notes |
| --- | --- | --- | --- |
| `BEH-001` | Write-only Settings lifecycle through one server owner | `secret-management/services/secret-management-service.ts`; `llm-provider-service.ts`; GraphQL; web provider editor/store | Save/remove/status remains value-free. Custom delete is now idempotent; built-in delete remains rejected. AutoByteus credential removal is idempotent and triggers authoritative scoped clear. The generic editor propagates removal state and rejects conflicting save or duplicate-remove actions. |
| `BEH-002` | Empty-base children, authorized roots, `LOCAL_HARDENED` only for governed launchers | `agent-child-environment.ts`; governed process/PTY/MCP/application launchers; workspace authorization; `codex-app-server-client.ts` | Ticket-governed launch paths remain sanitized. Stdio MCP uses the environment owner's additions boundary, preserving configured entries without broad parent inheritance. Codex App Server is the explicit scope exclusion and restores only its pre-ticket `options.env ?? process.env` launch behavior so real node-local account state remains available. No Codex Store/auth owner or same-user-isolation claim was added. |
| `BEH-003` | Explicit JIT LLM/search/media/metadata authentication | LLM/media provisioning services; exact construction targets; core factories/clients; `gemini-helper.ts`; `model-metadata-provisioning-service.ts`; Gemini metadata provider/resolver | Generic LLM/media consumers use only `target.credentialProviderId` and the tagged requirement. Gemini LLM/media provisioning preserves explicit AI Studio, Vertex Express, or Vertex Project semantics through exact resolved variants and exact SDK options, with no mode inference/fallback. Separately, Gemini metadata selects only the exact AI Studio or Vertex Express Store consumer, retains the established Generative Language request/mapping and live-over-curated merge, and performs zero metadata lookup in Vertex Project mode. AutoByteus discovery carries `SecretValue` through an exact authentication shape and reveals only at core client construction. No displayed provider/runtime/host/model fallback exists. |
| `BEH-004` | Tracked non-secret real-test selection and direct target provisioning | `test-config/live-e2e.json`; hidden-input real-E2E Store CLI; explicit Local importer; live-test supplement | Target-only hidden-input setup remains implemented. The separate importer can target only the canonical host E2E Store from an explicitly selected source and never reads/copies the default Store. AutoByteus real scenarios remain API/E2E-owned. |
| `BEH-005` | Five-state health plus healthy-only definition status | secret domain/backend/service status; GraphQL; web stores/components; built restart regression | Existing `READY/LOCKED/UNAVAILABLE/CORRUPT/INCOMPATIBLE` and `MISSING/CONFIGURED` behavior is preserved. The same-data-dir second process reopens `CONFIGURED` and removes it without value readback. |
| `BEH-006` | Deployment-neutral early bootstrap below `serverDataDir` | `app-config.ts`; `prisma-client-factory.ts`; lazy Prisma repositories/databases; shared token-usage Prisma owner; `startup/migrations.ts`; sanitized built bootstrap smoke; direct/Electron server entrypoints | Approved persisted SQLite configuration is parsed privately; otherwise the same deterministic data-root URL is held in runtime state without rewriting `.env`. Built modules/bootstrap import with no `DATABASE_URL`; request-scoped token-usage stores reuse one lazy process-lifetime client; migration-owned clients disconnect deterministically. Docker/launcher topology remains unchanged. |
| `BEH-007` | First delivery only Local/InMemory; future registration contract | backend/configuration ports and tagged capabilities | Preserved; no enterprise adapter, shared writable SQLite, strong-isolation container, or Kubernetes production manifest was invented. |
| `BEH-008` | Legacy sources stay untouched and non-authoritative | `app.ts`; `config/app-config.ts`; `config/non-secret-environment-projection.ts`; `built-in-agent-bootstrapper.ts`; `server-settings-service.ts`; `provisioning/local-import-credential-alias-registry.ts`; `custom-llm-provider-store.ts` | The automatic updater and ledger are removed. Name-first projection masks complete excluded assignments before generic dotenv parsing or retention; the positive import registry supplies current credential names while broader config-only exclusions remain owned by AppConfig. Built-in bootstrap registers its resolved retrospective improver as an in-memory runtime default, never a persisted startup mutation; configured values win and explicit Settings writes still persist. Startup does not mutate application `.env` or parent aliases, performs no source-to-Store import, and has no fallback. V1 custom-provider data remains unchanged and returns stable reconfiguration guidance. |
| `BEH-009` | Preserve factory config composition while separating authentication | `llm-construction-context.ts`; multimedia construction context; provisioning services; `llm-factory.ts`; concrete LLM/media clients; `gemini-helper.ts` | Preserved. Construction targets contain exactly `credentialProviderId` and `authenticationRequirement`; model identifiers remain separate factory inputs. Gemini's requirement and resolved variants retain the exact selected mode through SDK construction. |
| `BEH-010` | Separate pair-authenticated default/E2E Stores | Local initializer/provisioning/reset/crypto/schema/repository | Preserved with read-only real-E2E runtime and no source/default Store access. |
| `BEH-011` | Typed neutral configuration and extension contract | storage configuration/backend ports; GraphQL capability projection | Preserved with only approved first-delivery implementations. |
| `BEH-012` | Exact Claude `cli` / `managed-secret` modes | Claude authentication service, launch policy, SDK client, diagnostics | CLI uses actual external account home, empty-base environment, and zero secret lookup. Managed mode alone gets exact-child `ANTHROPIC_API_KEY`, empty settings, `tools: []`, strict explicit MCP, and early redaction. Both modes are fallback-free. |
| `BEH-013` | Preserve AutoByteus gateway Settings, discovery, reload, and LLM/audio/image invocation | `autobyteus-remote-model-discovery-service.ts`; `model-catalog-service.ts`; provider lifecycle service; secret catalog; core AutoByteus providers/factories; provisioning services; Settings GraphQL/web | Implemented one definition, exact discovery/construction identities, required credential ownership, wrapped discovery authentication, generation/configuration-aware in-flight reuse, explicit secret-agnostic post-replacement invalidation, serialized stale-publication fencing, per-kind runtime sync, last-known-good behavior, historical alias non-authority, and zero-lookup authoritative clears. The deterministic two-key race resolves both generations but only publishes/caches the post-replacement generation. Real remote execution remains a downstream API/E2E obligation. |
| `BEH-014` | Explicit source-to-one-Local-Store operator transition | `local-environment-secret-import.ts`; `local-environment-secret-import-service.ts`; `local-import-credential-alias-registry.ts`; `local-environment-source-reader.ts`; `cli/import-local-environment-secrets.ts`; Local read-only inspection/writable batch boundaries; root `package.json` | Required absolute source and closed target only; exactly one optional leading PNPM separator; global source trust checks; recognize-first selected-only parsing; valid normalized-empty recognized assignments become absence before buffer/selection/duplicate/plan state; all-empty input fails before target access; populated duplicates/dynamic values remain rejected; sole `DASHSCOPE_API_KEY` Qwen mapping; unrecognized lines ignored without metadata; sorted value-free plan/result; dry-run/no-overwrite defaults; explicit overwrite and exact TTY phrases; non-mutating inspection before confirmation; absent-pair setup only after confirmation; exact closed non-ready health/instruction with no definition projection; transactional preconditions/rollback; source immutability; other-Store non-access. No old file/symbol, wrapper, startup/runtime/UI/API/MCP/test-runner entrypoint, compatibility path, or `IMPORT_SOURCE_EMPTY_CREDENTIAL` variant remains. |
| `BEH-015` | Clean `repository_prisma@1.0.8` dependency replacement | server/root manifests; canonical root `pnpm-lock.yaml`; removed server-local lock and `patches/repository_prisma@1.0.6.patch`; `tests/unit/logging/prisma-query-log-policy.test.ts` | Exactly one attested unpatched 1.0.8 resolution is installed with Prisma/client 5.22.0. No surviving tracked lockfile contains 1.0.6/1.0.7 residue. Empty-cwd/empty-base exact-entrypoint ESM/CJS probes prove no dotenv discovery/import acquisition, default-off query logging, explicit opt-in precedence/conflict behavior, and zero real database/query/datasource use. Production owners, schema, migrations, persisted data, and Docker remain unchanged. |

## Key Files Or Areas

- `autobyteus-server-ts/src/llm-management/services/autobyteus-remote-model-discovery-service.ts`
- `autobyteus-server-ts/src/llm-management/services/model-metadata-provisioning-service.ts`
- `autobyteus-server-ts/src/llm-management/{providers,services,llm-providers}/`
- `autobyteus-server-ts/src/multimedia-management/`
- `autobyteus-server-ts/src/agent-tools/media/media-client-provisioning-service.ts`
- `autobyteus-server-ts/src/secret-management/{catalog,domain,provisioning}/`
- `autobyteus-server-ts/src/secret-management/provisioning/{local-environment-secret-import,local-environment-secret-import-service,local-import-credential-alias-registry,local-environment-source-reader,local-import-target-resolver}.ts`
- `autobyteus-server-ts/src/secret-management/backends/local/{local-secret-store-initializer,local-secret-store-provisioning-service,local-encrypted-secret-repository}.ts`
- `autobyteus-server-ts/src/secret-management/cli/{import-local-environment-secrets,provision-real-e2e-store}.ts`
- `autobyteus-server-ts/src/llm-management/llm-providers/stores/custom-llm-provider-store.ts`
- `autobyteus-server-ts/src/runtime-management/claude/client/claude-sdk-launch-policy.ts`
- `autobyteus-server-ts/src/runtime-management/codex/client/codex-app-server-client.ts`
- `autobyteus-server-ts/src/config/{app-config,non-secret-environment-projection,prisma-client-factory}.ts`
- `autobyteus-server-ts/src/secret-management/windows-exclusive-acl.ts`
- `autobyteus-server-ts/src/startup/migrations.ts`
- `autobyteus-server-ts/src/token-usage/repositories/sql/token-usage-ledger-repository.ts`
- `autobyteus-server-ts/src/app-data-migrations/{repositories,migrations}/`
- `autobyteus-server-ts/src/built-in-agents/built-in-agent-bootstrapper.ts`
- `autobyteus-server-ts/src/services/server-settings-service.ts`
- `autobyteus-server-ts/scripts/{run-sanitized-built-in-agents-bootstrap-smoke,smoke-built-in-agents-bootstrap}.mjs`
- `autobyteus-server-ts/tests/unit/config/prisma-import-lifecycle.test.ts`
- `autobyteus-server-ts/tests/e2e/secret-management/server-restart-secret-lifecycle.e2e.test.ts`
- `autobyteus-server-ts/tests/unit/logging/prisma-query-log-policy.test.ts`
- `autobyteus-server-ts/tests/unit/secret-management/{local-environment-source-reader,local-environment-secret-import-service,import-local-environment-secrets-cli,legacy-source-non-authority}.test.ts`
- `autobyteus-server-ts/package.json`; root `package.json`; canonical root `pnpm-lock.yaml`; removed `autobyteus-server-ts/pnpm-lock.yaml`; removed `patches/repository_prisma@1.0.6.patch`
- `autobyteus-ts/src/llm/{llm-construction-context,llm-factory,models,autobyteus-provider}.ts`
- `autobyteus-ts/src/utils/gemini-helper.ts`
- `autobyteus-ts/src/llm/metadata/{gemini-model-metadata-provider,model-metadata-resolver}.ts`
- `autobyteus-ts/src/clients/autobyteus-discovery-authentication.ts`
- `autobyteus-ts/src/multimedia/`
- `autobyteus-ts/src/tools/mcp/server/stdio-managed-mcp-server.ts`
- `autobyteus-web/components/settings/`
- `autobyteus-web/stores/llmProviderConfig.ts`
- `autobyteus-web/graphql/mutations/llm_provider_mutations.ts`
- `autobyteus-web/generated/graphql.ts`

## Important Assumptions

- First delivery runs on the repository's Node 22/Electron runtime where `node:sqlite` is available; the build emits Node's current experimental SQLite warning but succeeds.
- Local Store protection is explicitly `LOCAL_HARDENED`; server and agent workloads can still share one host identity.
- Codex App Server is explicitly excluded from the ticket-governed child-environment hardening claim and inherits the supplied environment or the real parent environment to preserve its existing node-local account state.
- `AUTOBYTEUS_LLM_SERVER_HOSTS` is non-secret endpoint configuration. A configured host requires managed gateway custody; no host means an authoritative zero-lookup clear.
- A successful provider response containing an empty model array is authoritative; transport/invalid-response/no-authoritative-host failure is transient and preserves the prior remote subset.
- The generic `dataDir`/PVC design remains the single-Pod hook; the repository still has no production Kubernetes manifest in scope.

## Known Risks

- `LOCAL_HARDENED` does not prevent arbitrary same-user filesystem/process inspection; `STRONG_AGENT_ISOLATION` remains deferred.
- Codex App Server retains its pre-ticket parent-environment/account-state behavior and is not covered by the ticket's empty-base or environment non-inheritance assurance; no managed Codex secret mode is implemented.
- Real Vertex Express LLM/audio/image execution must be rerun downstream to prove the corrected exact `{vertexai: true, apiKey}` SDK construction on substantive provider operations.
- JavaScript/SDK memory cannot be reliably zeroized. Managed Claude intentionally entrusts one exact SDK child with one Anthropic key.
- The sanitized built-module/bootstrap smoke and production build pass. Clean source-image build, container start, same-volume persistence/restart/removal, cross-platform ACL/owner behavior, busy contention, and single-Pod/PVC restart/reopen still need downstream rerun/evidence.
- The installed-package probe uses exact locked production bytes but a deliberately narrow synthetic `@prisma/client`; it proves package import/logging policy without database access, not production adoption of `repository_prisma`. Downstream must retain the existing configured-client owners and rerun restart/reopen and clean source-image lifecycle evidence.
- Architecture round 10 requires ordinary startup to leave the application `.env` byte-identical while deriving a missing deterministic SQLite URL and the retrospective improver default into runtime-only state. Focused implementation coverage proves both runtime values remain effective without an automatic file write. The already reconciled downstream-owned two-process test remains the required real built-runtime confirmation after source review.
- The server full test-tree TypeScript command remains structurally non-green because `tsconfig.json` includes `tests` while fixing `rootDir` to `src`, producing TS6059 across the test tree. Production `tsconfig.build.json` compilation and focused runtime tests are green.
- The broad core unit suite remains 1,718/1,719 because the pre-existing `event-types.test.ts` expects 28 enum values while the reviewed base contains 29. This ticket does not change that source or test.
- The full Nuxt repository typecheck remains non-green from broad pre-existing unrelated errors; production build, Electron transpilation, focused tests, and guards are green.
- Rich configured/removal UI state was mounted and interacted with in focused component tests, but no live provider backend was connected for a full-page configured-state render.
- `EXT-ANTHROPIC-AGENT-SDK-AUTH` remains a maintained delivery/release dependency, not legal clearance. Authentication modes must not be silently changed.

## Task Design Health Assessment Implementation Check

- Reviewed change posture: `Larger Requirement` plus bounded source-review/API/E2E-origin fixes, the round-10 explicit-import revision, the bounded round-13 dependency integration, the round-15 recognize-first importer clean cut, round-18 Codex/Gemini corrections, and the round-19 artifact-only metadata reconciliation.
- Reviewed root-cause classification: `Boundary Or Ownership Issue`, `Duplicated Policy Or Coordination`, and `Legacy Or Compatibility Pressure`; AR-009 resolved the legacy transition explicitly in favor of operator ownership and no automatic updater. CR-009 is a `Local Implementation Defect`: the AppConfig owner must explicitly deliver operational SQLite state to Prisma consumers. CR-010/CR-011 preserve import-safe and bounded lazy Prisma ownership. CR-012–CR-014 are bounded invariant/implementation defects at the AppConfig projection and Local inspection/import-result boundaries. CR-015 is stale noncanonical package-lock residue and is removed rather than maintained as a second lock authority. CR-016 is a `Local Implementation Defect`: built-in bootstrap selected a valid default through the explicit persistence path rather than the runtime-default path owned by Settings. AR-010/AR-011 resolve the package policy with a clean upstream 1.0.8 replacement rather than retaining a local patch. Round 15 removes a duplicated negative importer classifier and naming/ownership drift: one positive registry now exclusively selects importer credentials, and current environment-secret names replace legacy-named domain/service boundaries without compatibility exports. CR-018 is a bounded `Local Implementation Defect`: the correct source-reader owner performed one control-syntax check before, rather than after, its positive alias-selection boundary; no refactor or design change was needed. The round-16 empty-placeholder failure is also a bounded `Local Implementation Defect`: the selected-value owner incorrectly represented normalized emptiness as an error after parsing, rather than as absence before populated-selection state; the existing boundary remains correct and no refactor is needed. CR-019 was incorrect universal policy application: architecture narrowed the governed-launcher scope and the implementation restores only the Codex pre-ticket environment path. CR-020 was `Shared Structure Looseness`: the generic API-key variant discarded required Gemini runtime semantics, so exact specialized variants now carry the selected mode through the existing provisioning/helper owners. CR-021 was an artifact-only `Requirement Gap`: the approved original dual-key Gemini metadata contract was obscured by wording that appeared to apply the LLM/media SDK-mode variants to metadata; architecture reconciled the artifacts and authorized no source redesign.
- Reviewed refactor decision (`Refactor Needed Now`/`No Refactor Needed`/`Deferred`): `Refactor Needed Now` for the in-scope gateway/custody boundary; enterprise adapters and strong isolation remain deferred.
- Implementation matched the reviewed assessment (`Yes`/`No`): `Yes`.
- If challenged, routed as `Design Impact` (`Yes`/`No`/`N/A`): `Yes`; CR-001 and AR-009 returned through solution design and architecture review before implementation resumed.
- Evidence / notes: the server discovery owner resolves exact semantic identities and calls storage-neutral core discovery; factories own model registration; generic provisioning owns construction consumers. The exact Gemini requirement and specialized resolved variants preserve LLM/media mode semantics without widening the two-field construction target. The separate metadata owner keeps exact AI Studio/Vertex Express consumer selection, the existing Generative Language provider, and live-over-curated resolution; Vertex Project creates no live Gemini metadata provider. Codex remains outside governed launcher hardening and restores the exact base launch expression without gaining a parallel auth owner. AppConfig remains the non-secret configuration owner, excludes credential assignments before generic parsing, and supplies one validated SQLite URL to Prisma boundaries without startup source mutation. `LocalEnvironmentSecretImportService` owns the explicit source/target plan and batch; its source boundary selects only exact positive registry names before value parsing, while every unrecognized line is discarded without metadata. Non-mutating preview still uses the read-only Local boundary, and confirmed writes still use one selected-Store batch. Sanitized imports/bootstrap do not acquire database configuration. The token-usage repository module owns one import-safe lazy default client; injected and app-data-migration-owned clients preserve distinct lifecycle semantics. `ServerSettingsService` owns in-memory runtime defaults separately from explicit persisted mutations, and built-in bootstrap uses only that narrow runtime-default boundary. `repository_prisma@1.0.8` is installed and validated only at its dependency boundary and does not become a production database owner.

## Legacy / Compatibility Removal Check

- Backward-compatibility mechanisms introduced: one explicitly invoked, closed Local operator importer; it is not a runtime fallback or automatic compatibility path.
- Legacy old-behavior retained in scope: `No`; legacy files remain physically untouched but are non-authoritative.
- Dead/obsolete code, obsolete files, unused helpers/tests/flags/adapters, and dormant replaced paths removed in scope: `Yes`; the automatic cutover updater, startup call, rewriter/converter, parent-alias deletion, ledger, obsolete test, old importer domain/service/registry files, negative classifier, and obsolete error variants are removed.
- Shared structures remain tight (no one-for-all base or overlapping parallel shapes introduced): `Yes`.
- Canonical shared design guidance was reapplied during implementation, and file-level design weaknesses were routed upstream when needed: `Yes`.
- Changed source implementation files stayed within proactive size-pressure guardrails (`>500` avoided; `>220` assessed/acted on): `Yes`.
- Notes: the target contains no displayed provider/runtime or duplicate credential slot. `AUTOBYTEUS_API_KEY` exists only as a wire-header constant, positive explicit-import alias, and non-secret exclusion name; no normal runtime environment read or optional fallback was added. `QWEN_API_KEY` and `ZHIPU_API_KEY` are absent from the import registry.

## Persisted Data Transition Check (When Applicable)

- Approved decision (`Not Affected`/`Directly Usable — No Migration`/`Discard or Rebuild`/`Migration Required`): `Directly Usable — No Migration` for approved non-secret application settings; `Discard or Rebuild` for legacy credential authority and custom-provider v1; `Not Affected` for ordinary Prisma-backed SQLite data under the `repository_prisma@1.0.8` dependency-only update. The optional explicit importer is operator-invoked provisioning, not an automatic migration.
- Design-spec decision reference: `design-spec.md` -> “Persisted Data / State Transition Decision”.
- Implementation follows the approved decision without an unapproved migration or version-specific runtime fallback: `Yes`.
- Direct-use evidence or discard/rebuild result, when applicable: approved non-secret settings, including AutoByteus hosts, remain readable; mapped credential aliases and parent aliases remain unchanged but unread; custom-provider v1 remains byte-identical and returns reconfiguration guidance.
- Migration implementation and focused checks, only when `Migration Required`: `N/A`; no automatic credential migration owner remains. The explicit importer has separate focused synthetic coverage.
- Deviation from the reviewed transition decision: `None`.

## Environment Or Dependency Notes

- Worktree: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning`
- Branch: `codex/secure-centralized-secret-provisioning`
- Reviewed base: `534210b9e1dffff6c22855ae89ddb3d2afef5a9b`
- Round-6 rework starting implementation HEAD: `240d722070864e0ed960f552cdafc03d05d0ffeb`
- CR-009 rework starting implementation HEAD: `69d5442c0f8eb7c293097d939f79c272d0c56fad`
- CR-010 rework starting implementation HEAD: `3068d0fad00a6adba302199c857b01d2ede7ebc5`
- CR-011 rework starting implementation HEAD: `71e922a1396819e2a5bbc40877b1a810597449ab`
- Round-10 explicit-import/no-automatic-update rework starting HEAD: `09343ae17e016fa68cceda304df257563fc07cdc`
- Round-13 CR-012–CR-014 and dependency-integration rework starting HEAD: `6693846fe3ab249cbb23fabdc34d9bb206281368`
- Round-12 source-review CR-012/CR-015 local-fix starting HEAD: `3fdaf0c629419257f9a7bdf2ac081f9ba78d4680`
- Round-13 source-review complete-assignment CR-012 local-fix starting HEAD: `96fb1a887232b95a24aefb907993d311e17cc778`
- CR-016 startup Settings persistence local-fix starting HEAD: `5b3c1b58c8e5d98247c0986ec5d63815ebd376fc`
- Round-15 recognize-first importer rework starting HEAD: `977948fe68695374afbcd5e9516693d64533230e`
- Round-15 implementation reviewed by code review: `5db1385396e39e2d3c5ac9fa8f38e4f0a2ef4ae1` (the previously supplied `5db13853982934d03c7b40fc63af510c36f88be0` was invalid and is superseded by this exact object ID).
- CR-018 local-fix starting HEAD: `5db1385396e39e2d3c5ac9fa8f38e4f0a2ef4ae1`
- CR-018 final implementation source HEAD: `4c9f01776347d18bac78805800363f8a92a096af`
- Round-16 empty-as-absent rework starting HEAD: `4c9f01776347d18bac78805800363f8a92a096af`
- Round-16 final implementation source HEAD: `9e9315d58dbd164dc080b1270ef8b7fc9de4ba1c`
- Round-18 Codex/Gemini rework starting HEAD: `9e9315d58dbd164dc080b1270ef8b7fc9de4ba1c`
- Round-18 final implementation source HEAD: `ad629bc55ed5c653db957ce46bdbc5092c7738ac`
- Round-19 CR-021 artifact reconciliation starting/final source HEAD: `ad629bc55ed5c653db957ce46bdbc5092c7738ac` (no source or test commit required).
- Recorded base/finalization branch: `origin/personal`
- Local toolchain: Node `v22.23.1`, pnpm `10.28.2`.
- Dependency outcome: server requests `repository_prisma:^1.0.8`; the canonical root lock and installed tree select exact `1.0.8` with integrity `sha512-1ivDY5bVHd0rqgYHORfMl7jDhVxx/0MKZtxO8bM6fJUI1JOFwAs3/C8e520qoYKXNyLDUBOHPqH8wqF8H5ellQ==`; `prisma`/`@prisma/client` remain 5.22.0; the stale noncanonical server-local lock is removed; no surviving tracked lockfile, patched-dependency metadata/file, or package resolution retains 1.0.6/1.0.7.
- Installed 1.0.8 verification hashes match the reviewed assessment: ESM `8aff4c475a30b462a22fa213a08becd6992d642e5276a71af066c0b93a6dd884`; CommonJS `7dbc90a637dc7c7f3b3f45a6e846eaef178bed8d703456d4f8dd9807c7626d2c`; package manifest `c9d6f2d83dd1c22c63643deefdf89f7712efc490df4e8686575d2f4c4963f980`.
- Existing Docker Compose, launcher, ports, and volumes were not changed.
- `EXT-ANTHROPIC-AGENT-SDK-AUTH` must be carried through code review, API/E2E, and delivery. Before release, delivery must recheck the four official Anthropic sources recorded in the solution package. This is not legal clearance. If later authoritative guidance unambiguously forbids the exact self-hosted path, return the behavior decision through solution design rather than silently changing modes.

## Local Implementation Checks Run

- Round-19 artifact-only reconciliation — no implementation tests were rerun because architecture authorized no source/test change. `git diff` is empty for `model-metadata-provisioning-service.ts`, `gemini-model-metadata-provider.ts`, and `model-metadata-resolver.ts`; the round-18 source range likewise contains no metadata-file delta. Focused source inspection confirms exact AI Studio/Vertex Express credential-slot selection, no Vertex Project metadata provider/lookup, the established Generative Language endpoint/mapping, and live-over-curated merge. The Codex client has no diff from `origin/personal`, and `git diff --check` passes. The prior round-18 55/55 core, 15/15 server, and production-build results below remain the applicable implementation evidence.
- `pnpm --filter autobyteus-ts exec vitest run --silent=true tests/unit/utils/gemini-helper.test.ts tests/unit/multimedia/audio/api/gemini-audio-client.test.ts tests/unit/multimedia/image/api/gemini-image-client.test.ts tests/unit/multimedia/video/api/gemini-video-client.test.ts tests/unit/multimedia/audio/audio-client-factory.test.ts tests/unit/multimedia/image/image-client-factory.test.ts tests/unit/multimedia/video/video-client-factory.test.ts tests/unit/llm/api/provider-native-request-payloads.test.ts tests/unit/llm/supported-model-definitions.test.ts` — round-18 final rerun passed 9 files / 55 tests. Coverage proves exact two-field construction targets; model identifiers remain separate; AI Studio, Vertex Express, and Vertex Project use their exact resolved variants and SDK option shapes; ambient aliases do not select a mode; and generic API-key/none authentication does not infer a Gemini mode.
- `pnpm --filter autobyteus-server-ts exec vitest run --silent=true tests/unit/llm-management/llm-provisioning-service.test.ts tests/unit/agent-tools/media/media-client-provisioning-service.test.ts tests/unit/runtime-management/codex/client/codex-app-server-client.test.ts` — round-18 final rerun passed 3 files / 15 tests. Coverage proves explicit LLM/media mode mapping, exact credential-slot resolution, no unknown-mode fallback, cross-provider Gemini-requirement rejection, Codex default inheritance of the exact real `process.env` including `HOME`/`CODEX_HOME`, and unchanged caller-supplied Codex environment identity.
- `pnpm --filter autobyteus-server-ts exec vitest run --silent=true tests/unit/secret-management/local-environment-source-reader.test.ts tests/unit/secret-management/local-environment-secret-import-service.test.ts tests/unit/secret-management/import-local-environment-secrets-cli.test.ts tests/unit/secret-management/legacy-source-non-authority.test.ts tests/unit/config/app-config.test.ts` — final round-16 rerun passed 5 files / 101 tests. Coverage includes arbitrary filenames, exact positive registry mapping, sole `DASHSCOPE_API_KEY` Qwen selection, non-blocking `QWEN_API_KEY`/`ZHIPU_API_KEY`, malformed/unrelated-line selected-only parsing, unrelated bare-carriage-return non-blocking behavior, selected bare-carriage-return rejection, all four required empty forms plus quoted-whitespace normalization, multiple empty occurrences, empty-before/after-one-populated selection, populated duplicates, populated dynamic rejection, all-empty `IMPORT_NO_MAPPED_CREDENTIALS` before target resolution, current-application empty-Gemini plus populated-Vertex/OpenAI value-free planning, no empty-placeholder metadata, source immutability, source trust/TOCTOU/buffer cleanup, zero/one leading PNPM separator, read-only planning, exact health, target confirmation/isolation, atomicity, and AppConfig non-authority.
- `pnpm --filter autobyteus-server-ts exec vitest run --silent=true tests/unit/logging/prisma-query-log-policy.test.ts` — final round-16 rerun passed 1 file / 11 tests, preserving exact installed unpatched `repository_prisma@1.0.8` ESM/CommonJS isolation and log policy.
- `pnpm --filter autobyteus-server-ts exec vitest run tests/unit/secret-management/local-secret-storage-backend.test.ts tests/unit/secret-management/secret-catalog-autobyteus.test.ts tests/unit/llm-management/llm-providers/llm-provider-service.test.ts tests/unit/api/graphql/types/llm-provider.test.ts tests/unit/api/graphql/types/server-settings.test.ts tests/unit/services/server-settings-service.test.ts tests/unit/config/prisma-import-lifecycle.test.ts tests/unit/startup/migrations-prisma-engine-env.test.ts` — passed 8 files / 102 tests, preserving Local pair/health/restart behavior, provider lifecycle, Settings, import-safe Prisma ownership, and explicit migration-child database delivery.
- `pnpm --filter autobyteus-server-ts exec vitest run --silent=true tests/unit/built-in-agents/built-in-agent-bootstrapper.test.ts tests/unit/built-in-agents/built-in-agent-templates.test.ts tests/unit/services/server-settings-service.test.ts tests/unit/api/graphql/types/server-settings.test.ts tests/unit/config/app-config.test.ts tests/unit/secret-management/legacy-source-non-authority.test.ts` — passed 6 files / 115 tests. The CR-016 cases prove missing configuration gets an effective visible/editable runtime default with zero Settings persistence/process alias, repeated bootstrap leaves `.env` byte-identical, an operator-selected value remains authoritative and byte-identical, and an explicit Settings update persists and immediately wins. AppConfig/legacy non-authority coverage remains green.
- Clean archive-based workspace installation with the reconciled manifests/lock and no patch file: `pnpm install --frozen-lockfile` — passed and installed exact `repository_prisma 1.0.8`; temporary workspace was removed.
- Root workspace `pnpm install --frozen-lockfile` after removal of the obsolete server-local lock — passed; the canonical root lock was up to date and the installed exact-package hashes remained identical to the reviewed assessment.
- `pnpm --filter autobyteus-server-ts build` — final round-18 rerun passed, including shared/core compilation with exact Gemini variants, Prisma 5.22.0 client generation, server production TypeScript compilation, copied assets, runtime-default-aware built-in agent bootstrap, and the sanitized no-`DATABASE_URL` built-module smoke.
- Sanitized built importer invocation with no arguments — exited nonzero with stable `LOCAL_SECRET_IMPORT_FAILED IMPORT_OPTIONS_INVALID` before any source/target access. Node also emits its existing value-free experimental SQLite warning.
- `pnpm --filter autobyteus-server-ts typecheck` — not green for the repository-structural baseline: `tsconfig.json` includes `tests` while `rootDir` is `src`, producing TS6059 across test files. No focused implementation error appeared before that structural failure; production build is the authoritative source compilation check.
- `git diff --check` — passed. Focused scans found no old importer file/symbol/path, negative classifier, obsolete unsupported/alias-conflict errors, ignored-line metadata, automatic cutover owner/call/ledger, production `repository_prisma` import, old package/patch residue, stale `googleAuthenticationMode`/`googleWorkloadIdentity` variant, or Codex child-environment helper import. The Codex production file has no delta from the reviewed base; `LLMConstructionTarget` and its multimedia equivalent each contain only the two canonical fields; Docker, Prisma schema, SQL migration, and ordinary Prisma app-data-migration source remain unchanged. All round-18 changed source files remain below 500 effective non-empty lines and below the 220-line changed-file pressure threshold.
- No API/E2E, browser, live-provider, Docker, canonical-host Store, real Codex account state, real credential file, or secret-bearing source was executed or inspected during this implementation rework. Tests use synthetic values and temporary Stores only. Downstream-owned tests/reports/evidence were preserved.

## Frontend Rendered-Result Check (When Applicable)

- Round-18 applicability: `Not Applicable`; the bounded Codex launch and Gemini construction changes affect backend/core behavior only and do not alter a rendered frontend. The cumulative earlier Settings UI result below remains unchanged historical implementation evidence.
- Affected surfaces / journeys: Settings -> API Key Management -> configured built-in provider save/replace/remove.
- Approved UI/UX, interaction, requirement, or design references: `requirements.md`, `design-spec.md`, `credential-consumer-mapping.md`, and the existing Settings component system.
- Existing design system, shared components, and adjacent product surfaces reviewed: existing provider editor controls, destructive-action styling, configured/missing status, loading/disabled behavior, and notification patterns.
- Project development / preview instructions and rendered surface used: production Nuxt build plus mounted Vue component/runtime surfaces under the repository's Nuxt test harness; prior full Settings browser-equivalent render remains applicable to the unchanged surrounding layout.
- States, layouts, viewports, and interactions inspected: configured and missing editor states, configured-only remove control, removal click emission, disabled/saving/removing state bindings, parent-to-editor pending propagation, blocked input/reveal/save/duplicate-remove actions during removal, runtime refresh, and success notification.
- Visual or interaction issues found and corrected: added a configured-only destructive control using the existing spacing, border, focus, disabled, and localization conventions; added explicit removing state so save/remove cannot overlap.
- Supporting evidence and remaining unverified states or limitations: mounted component interaction passed. A full-page configured provider state was not connected to a live backend during this bounded rework, so end-to-end focus/keyboard behavior and all degraded backend states remain downstream coverage work. This is implementation self-validation, not API/E2E sign-off.

## Downstream Coverage Hints / Suggested Scenarios

- From a clean frozen install, rerun the exact installed `repository_prisma@1.0.8` ESM/CommonJS policy suite and confirm the locked hashes/registry integrity, obsolete patch absence, no 1.0.6/1.0.7 residue, no production package import, and no Prisma/schema/migration/Docker diff. Keep the children empty-cwd/empty-base and synthetic; do not add a real database or datasource to this package-policy probe.
- Treat the importer as an operator-only executable scenario, not a startup/test-runner fixture. Using only synthetic temporary sources and non-canonical target fixtures where applicable, cover the documented PNPM command with zero/one leading separator, arbitrary filenames including mixed current-app `.env` content, recognize-first selected-only parsing, `DASHSCOPE_API_KEY` as the sole Qwen mapping, non-blocking unknown/malformed/`QWEN_API_KEY`/`ZHIPU_API_KEY` lines, strict selected-value and source-trust rejection, absence of ignored metadata, value-free dry-run, absent-pair creation, no-overwrite idempotency, explicit replacement, both exact TTY phrases, injected transactional failure, source immutability, other-Store non-access, and stdout/stderr leak scanning. A real operator run may use the canonical target only without reading values.
- Exercise an existing synthetic Store in DELETE journal mode through dry-run, mismatched confirmation, EOF/cancellation, and non-TTY paths; compare database/key bytes, record identities, journal mode, and WAL/SHM/journal sidecars before/after. Cover `LOCKED`, `UNAVAILABLE`, `CORRUPT`, and `INCOMPATIBLE` exact value-free health/instruction results with no definition projection.
- Prove legacy startup leaves application `.env`, parent aliases, `.env.test`, and custom-provider v1 unchanged; mapped aliases never enter AppConfig/runtime authority; approved non-secret settings remain usable; v1 returns only `CUSTOM_PROVIDER_LEGACY_RECONFIGURATION_REQUIRED`; no automatic Store/importer/ledger activity occurs.
- Prove Settings save/replace/status/remove for `provider.autobyteus.api-key`, including repeated remove, zero value readback, secret-agnostic generation invalidation before post-replacement full refresh, provider-scoped LLM refresh, full LLM/audio/image refresh, and authoritative all-kind clear without lookup after removal.
- With no AutoByteus hosts, prove zero secret lookup and per-kind remote-subset clear. With configured hosts, prove only the exact model-kind discovery identity resolves, and failure output remains value-free.
- Discover remote models whose displayed providers are OpenAI/Gemini, then assert `credentialProviderId=AUTOBYTEUS`, native same-provider coexistence, runtime/model-kind scoped replacement, transient last-known-good retention, and authoritative empty-response clear.
- Run Store-backed real AutoByteus discovery plus representative LLM invocation, speech generation, and image generation. Migrate remaining old live integration files away from environment gates and removed no-argument discovery calls without adding fallback.
- Re-run Claude `cli` and `managed-secret` separately: CLI maps existing account state and performs zero lookup with normal CLI tools/settings/MCP; managed alone receives the exact child key plus empty settings, `tools: []`, and strict explicit MCP. Both remain empty-base and fallback-free.
- Re-run Codex App Server with the existing node-local authenticated account state and a representative turn. Confirm the default child receives the exact real parent environment (including `HOME`/`CODEX_HOME`), an explicitly supplied environment is passed unchanged, and no Codex Store lookup, managed-auth flow, synthetic home, or ticket-governed environment-hardening claim is introduced.
- Re-run substantive real Gemini Vertex Express LLM, audio, and image operations and prove provisioning selects `geminiVertexExpress` and the Google SDK receives `vertexai: true` with the exact managed Express key. Also retain focused AI Studio and Vertex Project coverage, and prove unknown/missing setup modes do not infer or fall back.
- Re-run Gemini metadata independently of the LLM/media SDK-mode path. In AI Studio and Vertex Express modes, prove only the matching metadata Store consumer is resolved and the established Generative Language request/mapping feeds live-over-curated resolution; in Vertex Project mode, prove zero metadata secret lookup and curated-only resolution. Do not add an alternate-definition retry or infer metadata mode from key presence.
- Validate stdio MCP configured additions alongside required operational variables while unrelated parent/provider/Store variables remain absent.
- Rerun the already reconciled `SCSP-E2E-RESTART-001` first. Confirm both sanitized starts leave `.env` byte-identical while the retrospective runtime default remains effective, the second process receives no parent `DATABASE_URL`, both processes deterministically select the same data-root SQLite URL through AppConfig-owned runtime delivery, listen succeeds, status reopens value-free as `CONFIGURED`, and removal succeeds.
- Rerun `SCSP-E2E-DOCKER-001` from a clean source-image build. Confirm the sanitized built bootstrap passes before image creation, the container starts, Store state is saved value-free, and the same named volume reopens/removes it after restart without adding any Docker-time database URL.
- Exercise unchanged Docker create/save/restart/reopen and a realistic single server Pod/PVC equivalent. Validate Local ACL/owner modes, read-only reopen/checkpoint, bounded contention, staged pair recovery, swapped empty-Store key, and every five-state degraded path.
- Scan logs, GraphQL, diagnostics, events, generated files, child environments, and artifacts with synthetic raw/encoded markers.

## API / E2E / Executable Coverage Investigation And Execution Still Required

Implementation-scoped builds and focused synthetic/unit checks for the round-15 recognize-first importer clean cut, bounded CR-018 selection-order correction, round-16 empty-as-absent rule, and round-18 Codex/Gemini LLM/media corrections are complete; round 19 reconciles metadata requirements only and requires no source change. Earlier clean/frozen dependency installation, exact installed-package probes, and CR-016 checks remain applicable. API/E2E-owned durable changes, reports, and evidence were preserved rather than reset. After full source review passes, `api_e2e_engineer` must validate the documented mixed-current-`.env` importer journey, real node-local Codex account/turn continuity, substantive Vertex Express LLM/audio/image behavior, the separate preserved AI Studio/Vertex Express metadata path with Vertex Project zero lookup, and the applicable restart, unchanged-Docker, and configured real-provider matrix while refreshing confidence/evidence. The dedicated real-E2E Store availability and real-provider outcomes remain downstream facts; no real execution is claimed here.
