# Secure Centralized Secret Provisioning — Investigation Notes

## Investigation Status

- Bootstrap Status: complete; refreshed again for the post-implementation CR-001 requirement-gap investigation.
- Current Status: CR-001 is substantively resolved, but architecture review round 5 failed on bounded `AR-008 / Design Impact`: one concrete LLM example used displayed `target.providerId` instead of authoritative `target.credentialProviderId`. The canonical target and example are corrected without changing the user-approved behavior basis. CR-002–CR-005 remain implementation-owned fixes. Architecture re-review is required before rework.
- Investigation Goal: replace worktree/node plaintext credential distribution with deployment-selectable server custody, an in-process AutoByteus Local Secret Storage Backend using physically separate default and real-E2E encrypted Stores, explicit client construction, and agent isolation while preserving substantive real-provider coverage.
- Scope Classification: `Large`.
- Scope Classification Rationale: spans core LLM/search/media construction, server configuration/GraphQL/startup/migration, Electron/direct/Docker data-directory composition, Kubernetes/company deployment, host-worktree test infrastructure, and agent filesystem/process boundaries. Existing Docker Compose/launcher files remain out of scope.
- Scope Summary: `autobyteus-ts` owns secret-free consumer contracts, runtime-aware construction descriptors, storage-neutral AutoByteus remote discovery clients, and execution-security primitives. `autobyteus-server-ts` owns backend configuration, `SecretManagementService`, complete health/status projection, subject-owned credential validation, consumer provisioning, AutoByteus remote discovery orchestration, the in-process pair-authenticated Local backend, startup, migrations, and Claude authentication. Separate encrypted default and real-E2E Store files supply machine-global local custody. Claude and the AutoByteus remote gateway reuse the central service through exact semantic consumers; neither receives a backend dependency or ambient-key fallback. First delivery registers Local in product and InMemory/test fixtures in tests; concrete enterprise adapters and strong process isolation are deferred.
- Primary Questions Resolved:
  1. Does Electron own custody? No. Electron starts its embedded Agent Server; the server constructs `LocalSecretStorageBackend` in process. The same server-owned contract supports direct/server/container modes.
  2. Is a separate local service required? No. A same-user daemon/IPC boundary would add lifecycle and compatibility machinery without meaningful isolation. SQLite transactions/locking are sufficient for the approved local workflow.
  3. How do tests avoid per-worktree setup? Git tracks non-secret scenario and real-E2E Store configuration. `real-e2e-secret-store.db` and its independent key are provisioned once outside Git and every worktree selects them automatically.
  4. Can `.env.test` be committed? Yes only after it is verified secret-free; typed root test configuration is the canonical Store/scenario contract and global credential dotenv loading is removed.
  5. How is LLM authentication delivered? Server provisioning resolves it before factory construction; `LLMFactory` retains config composition and creates an ephemeral `LLMConstructionContext`.
  6. Is an administrator identity required? No. Backend workload identity is server-to-custody authentication, not an AutoByteus user system; current Settings access remains unchanged.
  7. How do Kubernetes/company deployments work in first delivery? One Docker node or single Kubernetes server Pod/PVC may own one Local Store. Multiple replicas require a future separately installed adapter; no Vault/AWS/Kubernetes/company adapter ships now, and unknown kinds fail closed.
  8. Where is Local Store state held? The normal Store is below each server's configured data directory. Electron therefore uses `~/.autobyteus/server-data/secret-store/`; ordinary Docker uses `/home/autobyteus/data/secret-store/` inside its existing named volume. The host real-E2E pair remains in the host AutoByteus server-data domain. An in-process backend instance opens exactly one bootstrap-selected Store.
  9. Why separate databases rather than profiles? The approved host/local workflow needs default and real-E2E custody contexts, not arbitrary profile lifecycle. Separate database/key pairs remove profile schema/policy and improve test/default isolation without imposing Store sharing on Docker.
  10. Which proposed attributes survived use-case validation? Only attributes used by UC-001–UC-018 or an approved security/operational contract. Generic product scope/address/version fields, duplicate consumer-plus-definition resolution input, combined storage/provider-validation status, a connection alias, arbitrary profile fields, and every cross-Store copy port were removed or split. Pair-verifier format, random `store_id`, and verifier AEAD fields remain because they authenticate an empty database/key pair before `READY`. `credentialProviderId` is retained because a remote model's displayed provider may differ from the gateway credential owner.
  11. Does this ticket alter Docker deployment for E2E? No. The existing Compose topology, launcher, and named volumes stay unchanged. A normal Docker node persists its own default Local Store in its existing server-data volume; operators may customize deployments themselves, but AutoByteus does not prescribe a Docker E2E mount in this ticket.
  12. How does the node model scale to Kubernetes? A local Docker container or single Kubernetes server Pod may own one persistent Local Store. Multiple containers/Pods/replicas never share the writable SQLite Store; centralized multi-node custody is unavailable until a future adapter is implemented/installed.
  13. What first-delivery assurance is claimed? `LOCAL_HARDENED` only: built-in file-tool Store denial plus empty-base child environments/descriptors and redaction. Arbitrary same-user/all-in-one filesystem/process denial and `STRONG_AGENT_ISOLATION` are deferred.
  14. How is an empty Store bound to its key? A random `store_id` and AES-GCM pair verifier under a domain-separated HKDF key are authenticated on every open before `READY`; swapped/partial/tampered pairs are `CORRUPT`, unsupported versions are `INCOMPATIBLE`.
  15. Does Claude Agent SDK receive the Anthropic definition? Only in explicit `managed-secret` mode. The existing generic `SecretManagementService.resolveForUse` is reused with `{kind:"agentRuntime",runtimeKind:"claude_agent_sdk",credentialSlot:"apiKey"}` and maps to the same `provider.anthropic.api-key` definition as native Anthropic consumers. Default `cli` mode performs no lookup. There is no `auto`, ambient raw-key, or fallback mode.
  16. How is the Claude value delivered? `ClaudeRuntimeAuthenticationService` resolves immediately before model-discovery/run child construction. `ClaudeSdkClient` builds an empty-base environment and supplies exactly `ANTHROPIC_API_KEY` through the SDK `env` option to that child only; it never mutates the parent environment or accepts an arbitrary caller environment.
  17. What security limit remains? The authorized Claude executable/SDK can observe its own environment, and JavaScript/SDK memory cannot be deterministically zeroized. Managed mode therefore removes supported descendant leak mechanisms (process/environment-inspection built-ins, user/project/local settings/hooks/plugins/external MCP config), redacts before diagnostic buffering, and reports only `LOCAL_HARDENED`.
  18. What existing behavior did CR-001 uncover? `AUTOBYTEUS_LLM_SERVER_HOSTS` activated remote LLM/audio/image discovery, and one `AUTOBYTEUS_API_KEY` authenticated both discovery and later `AutobyteusClient` requests. The explicit-auth implementation removed the ambient read but supplied no production replacement, so remote models disappeared.
  19. How is that behavior preserved cleanly? One `provider.autobyteus.api-key` definition is authorized to distinct discovery and construction consumers. A server-owned `AutobyteusRemoteModelDiscoveryService` resolves it and calls storage-neutral core discovery. Remote models keep their downstream provider metadata but declare `credentialProviderId=AUTOBYTEUS`; generic provisioning therefore resolves the gateway key for invocation.

## Request Context

The user reported that ignored `.env`/`.env.test` credentials can be read by AI agents and can enter conversations, that copied ignored files break the worktree workflow, and that real provider credentials remain necessary for meaningful E2E. Through iterative discussion, the user selected a provider/backend architecture, corrected service-over-backend layering, required server-first deployment neutrality, rejected an unrelated administrator identity subsystem and OS-specific desktop keychain design, and requested a simplified AutoByteus local vault/store. The user initially explored profiles, then explicitly pinned down the cleaner target: the Local backend runs in Agent Server and normal versus real-E2E custody uses different encrypted databases. After initially approving Claude CLI-only cutover, the user reopened that decision and selected a universal-store design with exactly two Claude modes: external `cli` by default and explicit `managed-secret` using the same central secret service.

The agreed local operating model is:

```text
AutoByteus LocalSecretStorageBackend (inside Agent Server)
  ${serverDataDir}/secret-store/secret-store.db + secret-store.key
    -> normal Electron/direct/Docker server use

  host AutoByteus server-data/secret-store/real-e2e-secret-store.db + key
    -> host real tests reused by every worktree
```

The agreed worktree model is one-time machine provisioning followed by zero routine human involvement. A first machine/new provider/expired key still requires legitimate provisioning or rotation.

## Environment Discovery / Bootstrap Context

- Project Type: `Git` superrepository/workspace.
- Task Workspace Root: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning`.
- Task Artifact Folder: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning/tickets/in-progress/secure-centralized-secret-provisioning`.
- Current Branch: `codex/secure-centralized-secret-provisioning`.
- Current Worktree / Working Directory: `/Users/normy/autobyteus_org/autobyteus-worktrees/secure-centralized-secret-provisioning`.
- Bootstrap Base Branch: `origin/personal`.
- Initial Remote Refresh Result: initial worktree created from refreshed `origin/personal` at `fbd7b6764bd43751956d69ffe22b943d06188444`.
- Second-Round Remote Refresh Result: `git fetch origin personal && git merge --ff-only origin/personal` succeeded on 2026-07-20 at `bda6615a7`.
- Final User-Review Refresh Result: `git merge --ff-only origin/personal` succeeded on 2026-07-21; current design baseline is `534210b9e`. The intervening application-worker changes were inspected and added to the process-isolation inventory; credential-management/LLM paths were unchanged.
- Task Branch: `codex/secure-centralized-secret-provisioning`, tracking `origin/personal`.
- Expected Base Branch: `personal`.
- Expected Finalization Target: `origin/personal` through delivery workflow.
- Bootstrap Blockers: none.
- Implementation State: implementation commit `240d722070864e0ed960f552cdafc03d05d0ffeb` exists and source review failed. Do not revert or layer rework until architecture re-review authorizes the revised basis.
- Notes For Downstream Agents: never inspect/copy actual secret values. Preserve all AutoByteus remote LLM/audio/image behavior; only credential sourcing changes. Preserve CR-002–CR-005 and `EXT-ANTHROPIC-AGENT-SDK-AUTH` in every handoff.

## Supplemental Task Artifact Inventory

| Artifact Path | Purpose And Scope | Evidence, Context, Or Decision Captured | Core Artifact(s) Supported | Related Requirement / Acceptance-Criteria IDs | Status | Approval Applicability / State | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [use-case-spine-validation.md](./use-case-spine-validation.md) | Complete use-case spine and design-principles validation | UC-001–UC-018 primary/return/bounded spines, AR/CR remediation reachability, pair/status/Claude/AutoByteus data provenance, minimal target shapes | Requirements, investigation, design | REQ-001–REQ-019 / AC-001–AC-019 | Complete; AR-008 target shape corrected | User-approved behavior unchanged | Architecture re-review |
| [threat-model-and-option-analysis.md](./threat-model-and-option-analysis.md) | Threat model and custody/assurance option analysis | `LOCAL_HARDENED` boundary, empty-pair, managed-Claude, and AutoByteus gateway threats; Local-only first delivery; rejected shortcuts | Requirements, investigation, design | REQ-002–REQ-005, REQ-008, REQ-009, REQ-012–REQ-019 / AC-002–AC-006, AC-009–AC-019 | Complete | `N/A` for evidence; revised guarantees/exclusions user-approved | Architecture re-review |
| [secret-storage-backend-contract.md](./secret-storage-backend-contract.md) | Intended domain/backend/Store/capability contract | Complete health mapping, pair verifier, Store-bound Local/InMemory first delivery, target-only E2E setup, Claude and AutoByteus gateway consumers | Requirements and design | REQ-001, REQ-005–REQ-008, REQ-010–REQ-013, REQ-015, REQ-017–REQ-019 / AC-001, AC-002, AC-004, AC-005, AC-007, AC-008, AC-010, AC-013–AC-019 | Complete; AR-008 target shape corrected | User-approved behavior unchanged | Architecture re-review |
| [credential-consumer-mapping.md](./credential-consumer-mapping.md) | Current evidence and target consumer/data mapping | Legacy aliases, LLM/search/media/metadata/AutoByteus gateway boundaries, runtime-aware credential owner, Claude managed consumer, spawn groups | Requirements and design | REQ-001, REQ-002, REQ-005, REQ-011, REQ-014, REQ-016, REQ-018, REQ-019 / AC-005, AC-009–AC-012, AC-018, AC-019 | Complete; AR-008 example/shape corrected | User-approved behavior unchanged | Architecture re-review |
| [live-test-secret-provisioning.md](./live-test-secret-provisioning.md) | Intended zero-touch host/CI real-test workflow and explicit Docker non-impact | Direct-only separate E2E setup, tracked config, pair-aware preflight, read-only runtime, real/synthetic/gateway/managed-Claude classification including remote AutoByteus capabilities | Requirements and design | REQ-002–REQ-005, REQ-008, REQ-009, REQ-012, REQ-016–REQ-019 / AC-003, AC-004, AC-006, AC-010, AC-012, AC-013, AC-015, AC-016, AC-018, AC-019 | Complete | User-approved CR-001 revision | Architecture re-review |
| [secret-storage-architecture.md](./secret-storage-architecture.md) | Mermaid architecture and data-flow spines | Local-only first delivery, complete health, pair binding, direct E2E, unchanged Docker/single Pod, future extension, local assurance, Claude two-mode cutover, AutoByteus remote discovery/construction | Requirements, investigation, design | REQ-001–REQ-019 / AC-001–AC-019 | Complete; AR-008 target flow corrected | User-approved behavior unchanged | Architecture re-review |

## Source Log

| Date | Source Type | Exact Source / Query / Command | Why Consulted | Relevant Findings | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- |
| 2026-07-17 | Command | `pwd`, Git root/status/worktree/remote/default-branch commands; `git fetch origin personal`; dedicated `git worktree add` | Bootstrap task isolation | Shared checkout was unsuitable; dedicated ticket worktree created from refreshed `origin/personal` | No |
| 2026-07-20 | Command | `git fetch origin personal && git merge --ff-only origin/personal` | Refresh source baseline before second-round design | Fast-forwarded task branch from `fbd7b6764` to `bda6615a7`; no relevant core/server secret implementation was introduced by intervening commits | No |
| 2026-07-17 | Doc | Solution-designer skill, templates, design principles and examples | Load authoritative artifact/design rules | Requires three core artifacts, behavior IDs, design health, explicit migration/removal, supplemental inventory and user approval | No |
| 2026-07-17/20 | Code | `autobyteus-server-ts/src/config/app-config.ts`, `app-config-provider.ts`, provider/search GraphQL/services | Trace configuration lifecycle | Application `.env` is loaded into global process state and written by Settings; secret/non-secret ownership is mixed | Yes — implementation removal |
| 2026-07-17/20 | Code | `autobyteus-ts/src/llm/llm-factory.ts`, `models.ts`, `utils/llm-config.ts`, `api/openai-llm.ts`, `api/openai-responses-llm.ts` | Verify exact LLM API and config behavior | Current factory signature is `(modelIdentifier, configInput?)`; it composes `LLMConfig` and calls `(model, config)`. OpenAI base reads env and builds SDK. `LLMConfig` serializes/clones | Yes — reviewed API cutover |
| 2026-07-17 | Code | LLM provider/metadata/custom-provider, search and multimedia implementations | Complete consumer inventory | Credentials are independently read from environment or retained in custom endpoint models; metadata discovery also needs provisioning | Yes — complete mapping |
| 2026-07-17 | Code | `autobyteus-server-ts/src/agent-execution/backends/autobyteus/autobyteus-agent-run-backend-factory.ts` | Find server construction seam | Server calls injectable async `LLMFactory.createLLM`; good boundary for `LLMProvisioningService` | No |
| 2026-07-17 | Code | file tools, terminal/background/PTY launchers, Codex/Claude clients, Electron platform server managers | Trace agent reach/inheritance | Absolute path and broad process environment paths expose current secret files/state; all spawn sites need central policy | Yes — implementation inventory |
| 2026-07-17 | Code | server GraphQL context/remote-access policy | Determine whether an application user identity exists | No AutoByteus user/admin principal exists. Second-round decision is to preserve current access and not create one | No auth work in this ticket |
| 2026-07-17/20 | Code/Command | `autobyteus-ts/tests/setup.ts`; server Prisma setup; repository `rg` for `.env.test`, dotenv and provider env reads | Verify worktree/test problem on refreshed baseline | Core setup loads superrepo/package `.env.test`; multiple live suites gate on provider env keys; `.env.*` is broadly ignored | Yes — replace credential loading; allow tracked non-secret config |
| 2026-07-20 | Command | `git ls-files` for env/test config; `rg` package `.gitignore` files | Determine commitability of secret-free test config | No canonical tracked live Store selection exists. Explicit ignore exceptions are required if `.env.test` remains tracked; typed `test-config` needs no exception | Yes — design exact paths |
| 2026-07-20 | Code/Command | Electron `appDataPaths.ts`, `AppDataService.resetAppDataDir`, provider GraphQL/service shapes, repository searches for version/scope identity callers | Validate Local Store placement, reset reachability, and proposed interface attributes | Canonical base is `~/.autobyteus`, server data is `server-data`; ordinary reset currently removes all server data; current Settings has save/delete with no version input; no product organization/deployment/environment identity exists | Yes — canonical path/reset contract; remove unsupported fields |
| 2026-07-21 | Code/Command | `autobyteus-server-ts/prisma/schema.prisma`, `src/config/app-config.ts`, Electron `serverRuntimeEnv.ts`, `AppDataService.ts`; `rg` for `DATABASE_URL`, SQLite, and server-data paths | Decide separate Store DB versus another application table | Main application DB is SQLite but follows `DATABASE_URL`: Electron uses `~/.autobyteus/server-data/db/production.db`; test/direct servers can use `test.db` or checkout/data-directory state. A secret table there would not provide one stable real-E2E custody location and would couple secrets to application reset/migrations | Use a separate Local Store SQLite file |
| 2026-07-21 | Code/Command | `autobyteus-server-ts/docker/docker-compose.yml`, Dockerfile/entrypoint/supervisor configuration and Docker README | Validate normal Docker persistence and the proposed Docker E2E design | Current Compose already maps `autobyteus-server-data` to `/home/autobyteus/data` and sets that as `AUTOBYTEUS_DATA_DIR`. A normal Local Store can persist below the existing directory. Worktree-to-Docker E2E sharing would require extra policy/mounts, but the user rejected prescribing them | Preserve Docker Compose/launcher unchanged; host E2E only |
| 2026-07-21 | User/Design audit | Discussion of Local Store process, in-process backend, separate DB/table, security, separate real-E2E DB, and Docker | Re-evaluate earlier local-process/profile assumptions | User approved an in-process Local backend and physically distinct host normal/real-E2E Store databases. Separate processes under one identity and arbitrary profiles are not required. Separate keys improve defense in depth. The intermediate Docker-E2E mounting idea was later superseded by the Docker-preserving correction below | Superseded in part; preserve the in-process/separate-Store decisions |
| 2026-07-21 | User/Design correction | Discussion of normal Docker persistence versus prescribed real-E2E bind mounts | Separate product deployment from test-harness mechanics | User directed that existing Docker Compose, launcher, and persistent-volume setup remain unchanged. Each Docker node stores its normal Local Store under its existing server data volume; no AutoByteus Docker E2E mount/environment contract is introduced | Remove Docker E2E machinery from every artifact |
| 2026-07-21 | Command/Code | `git merge --ff-only origin/personal`; inspected `application-engine-host-service.ts`, `application-worker-supervisor.ts`, worker runtime/protocol and the intervening path diff | Refresh to the latest tracked base before user review and check design impact | Fast-forwarded `bda6615a7` to `534210b9e`. New supported application backend workers inherit `process.env` and run with the server filesystem identity; they already fit REQ-004 but must be named in launcher/isolation implementation and denial coverage. No intervening credential/LLM construction change invalidated the design | Reflected in source/launch inventories |
| 2026-07-21 | Code | `autobyteus-server-ts/src/runtime-management/claude/client/claude-sdk-auth-environment.ts`, `claude-sdk-client.ts`, `claude-sdk-setting-sources.ts`, Claude session/diagnostics and tests | Reinvestigate reopened AR-006 | Current modes are `auto`, `cli`, and `api-key`; the environment builder starts from `{...process.env}`; caller start options accept arbitrary `env`; model discovery and run queries both reach SDK spawn; settings currently load user/project/local; diagnostics retain raw text before summarization redaction | Replace with exact two-mode owner, no arbitrary env, early redaction, managed-mode settings/tool controls |
| 2026-07-21 | Upstream package inspection | Project pin `@anthropic-ai/claude-agent-sdk@0.2.71`; `npm pack` scratch inspection of `sdk.d.ts`, `sdk.mjs`, and bundled `cli.js` (no secret files/values) | Verify supported delivery/control mechanisms instead of inferring | SDK `Options.env` is supplied to the Claude Code child; absent `env` defaults to process environment. Pinned types expose `settingSources`, `strictMcpConfig`, `tools`, `disallowedTools`, and `spawnClaudeCodeProcess`. Bundled Claude Code recognizes `ANTHROPIC_API_KEY`. An FD path exists internally but is not a stable public cross-platform contract | Use explicit SDK `env` first delivery; defer FD/broker channel |
| 2026-07-21 | Official documentation | `https://code.claude.com/docs/en/agent-sdk/typescript`, `https://code.claude.com/docs/en/agent-sdk/permissions` | Validate SDK option semantics and tool-policy limits | `env` controls child environment; empty `settingSources` excludes user/project/local settings; `strictMcpConfig` restricts MCP config. `allowedTools` grants permission rather than forming a security allowlist, so managed mode must use explicit `tools`/`disallowedTools` and sanitized AutoByteus MCP/tool execution | Encode managed-mode launch invariant and tests |
| 2026-07-21 | User/reviewer workflow | User reopened AR-006; architecture reviewer retracted pass/handoff and recorded `Blocked / Requirement Gap`; implementation engineer confirmed no implementation | Preserve gate authority and containment | AR-006 now requires consumer identity, JIT owner/delivery, failures, CLI relationship, trust limit, migration/removal, files, and negative API/E2E controls | Revise complete package, obtain user approval, re-review |
| 2026-07-21 | User decision | Discussion of `resolveForUse`, Claude CLI, managed secrets, and environment injection | Select a clean universal-store behavior | Exactly two modes: `cli` default and explicit `managed-secret`; managed mode resolves through existing generic service and injects into the exact child environment. No 100% secrecy claim for the authorized process | Reflected across all intended-behavior artifacts |
| 2026-07-17/21 | Code | Electron `AppDataService`, `serverRuntimeEnv`, platform server managers and app-data paths | Trace embedded startup and data ownership | Electron starts an embedded server and forwards parent env. The embedded server can construct the same in-process Local backend as a direct server; no Electron-owned or separately supervised Store process is needed | Yes — backend composition and env cutover |
| 2026-07-17 | Code/Doc | Docker compose/readme and remote-node docs | Verify server/container shapes | Nodes persist data independently; current all-in-one/root container combines server and agent reach | Yes — deployment/isolation design |
| 2026-07-17 | Web | AWS/GCP/Azure secret-manager best practices and AWS KMS docs | Compare enterprise patterns | Workload identity, least privilege, rotation/audit and envelope encryption are established; product contract should remain vendor-neutral | No |
| 2026-07-17/20 | Web | https://developer.hashicorp.com/vault/docs/secrets/kv/kv-v2 and Vault Agent docs | Evaluate Vault and separate-daemon patterns | KV v2 supplies versioned static-secret semantics; a local daemon is established integration style but not universally required | No |
| 2026-07-17/20 | Web | https://kubernetes.io/docs/concepts/configuration/secret/ and Kubernetes Secrets good practices/service-account docs | Validate Kubernetes provisioning | Secrets enter Pods through explicit volumes/env; base64 is not encryption; etcd/RBAC matter; service-account token automount can be disabled. Same-container agent access remains a product risk | Yes — docs and topology |
| 2026-07-17/20 | Web | https://www.electronjs.org/docs/latest/api/safe-storage | Evaluate OS-specific desktop storage | Main-process/OS-specific semantics and Linux fallback complexity conflict with the selected cross-platform local store; safeStorage is not the canonical design | No |
| 2026-07-17 | Web | OWASP Secrets Management and LLM prompt-injection guidance; Node child-process docs | Validate controls | Central management, least privilege, output hygiene and explicit child env are required; model instructions are not a security boundary | No |
| 2026-07-17 | Probe | File metadata and assignment-name-only probes for existing local `.env*`/custom-provider files | Establish current exposure without reading values | Secret-bearing files existed with permissive local mode and relevant provider aliases; no values were printed or retained | No |
| 2026-07-17–21 | User | Deployment, testing, naming, LLM config, local Store, physical Store separation, Docker, and non-secret config discussions | Refine intended behavior | Service sits above backend; real tests remain first-class; Local backend is in-process; host default and real-E2E use separate databases/keys; E2E is set up once; test config is committed; Docker remains on its existing data volume with no prescribed E2E mount; no admin, daemon, arbitrary profile, or OS-keychain subsystem | Reflected in revised package |
| 2026-07-21 | User | Final local Docker/Kubernetes node-model confirmation and architecture-review authorization | Confirm deployment flexibility and close user-review gate | A local Docker container is one independent Agent Server node using its existing persistent volume; a single Kubernetes server Pod may follow the same Local Store model with its own PVC; multiple replicas use centralized custody rather than shared SQLite. User requested final document alignment and architecture review | Align all nine artifacts and hand off the cumulative package |
| 2026-07-21 | User/design-principles audit | User requested a second audit of every use-case spine, data tightness, reuse, redundancy, boundaries, and complexity, then authorized architecture re-review if sound | Revalidate the complete revised AR-006 package rather than relying on the earlier audit | All 17 use cases and 24 inventoried primary/return/bounded-local spines remain complete. UC-008/013 composite nodes and owner statements were clarified. Status results were tightened to a discriminated union and unused definition/message/retry fields were removed. No new subsystem or unresolved requirement gap was found | User-approved package returns to architecture review; implementation remains gated |
| 2026-07-21 | Architecture review round 3 | [design-review-report.md](./design-review-report.md) and reviewer message | Re-review user-approved AR-006 package | AR-001–AR-006 and MP-001 resolved. New sole gate AR-007/MP-002 claims external CLI/subscription mode needs prior Anthropic approval based on the Agent SDK overview | Investigate complete current official source set; preserve no-implementation gate |
| 2026-07-21 | Official Anthropic Help Center | `https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan`, dated June 16, update June 15 | Verify user's remembered June/July policy change and round-3 premise | The current update says the planned Agent SDK billing change is paused and, for now, Agent SDK, `claude -p`, and third-party app usage still draw from subscription limits. The preserved plan explicitly names third-party apps authenticating through a Claude subscription via the Agent SDK | Material contrary authority for MP-002; send to reviewer |
| 2026-07-21 | Official Anthropic SDK/legal/help pages | `https://code.claude.com/docs/en/agent-sdk/overview`; `https://code.claude.com/docs/en/legal-and-compliance`; `https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account` | Determine whether the June notice removes every approval restriction | SDK overview/legal pages still direct products to API keys and restrict Claude.ai login; the account page says Anthropic may allow certain third-party tools. These sources conflict with the unqualified June subscription-usage update | Do not claim certainty; maintain release dependency and request authoritative reviewer reassessment |
| 2026-07-21 | User/current-product evidence | User reaffirmed two modes and reported the existing personal-branch Claude Agent SDK path already succeeds through local CLI authentication | Separate technical reachability and deployment model from external permission | AutoByteus is local/self-hosted and does not add a Claude login UI, hosted auth broker, credential relay, or subscription pooling. Existing success proves the product path is real, not that technical success alone grants permission | Retain design; present as supporting context, not standalone policy proof |
| 2026-07-21 | Architecture review | [design-review-report.md](./design-review-report.md), round-1 reviewer message | Review complete cumulative solution package | Gate `Fail / Requirement Gap`; AR-001–AR-006 identify undecided strong-isolation scope, placeholder production adapter, copy inconsistency, incomplete corrupt/incompatible outward health, empty-Store key mismatch, and missing Claude runtime decision. MP-001 establishes the empty-Store pair mismatch as reachable under the approved Store/key lifecycle | Revise all affected artifacts; preserve IDs; obtain user re-approval before round 2 |
| 2026-07-21 | Code | `autobyteus-server-ts/src/runtime-management/claude/client/claude-sdk-auth-environment.ts`, `claude-sdk-client.ts`, unit/integration tests; repository `rg` for Claude/Anthropic aliases | Resolve AR-006 using current runtime evidence | Current `ClaudeSdkAuthMode` includes `api-key`; `buildClaudeSdkSpawnEnvironment` begins with `{...process.env}` and returns the copied environment unchanged in `api-key` mode. Other modes delete only three API-key aliases conditionally. Claude SDK client uses this environment. This is a distinct agentic runtime path from native `AnthropicLLM` | Remove ambient mode/fallback and broad base; if managed is selected, require its own explicit consumer/delivery boundary |
| 2026-07-21 | Design-principles revalidation | Shared `design-principles.md`; revised UC-001–UC-017 spines and product-reachability audit | Resolve architecture review gaps proportionately | First delivery can be coherent without speculative strong isolation or a placeholder enterprise adapter: Local/InMemory plus fixture coverage, explicit lower-tier guarantee, direct E2E setup, closed health mapping, pair verifier, and Claude raw-key removal cover all approved first-delivery paths | User re-approved complete correction set on 2026-07-21 |
| 2026-07-21 | Command | final round-1 revision validator over all artifacts; `rg` contradiction scans; Git status/diff/base checks | Verify the revised cumulative package without reading secret values | PASS: nine solution artifacts plus the unchanged round-1 report; relative links, Markdown fences/tables, exact BEH-001–012/REQ-001–018/AC-001–018/UC-001–017 sets, 24 aligned design/spine IDs, 14 structurally balanced Mermaid blocks, AR-001–AR-006/MP-001 presence in every core artifact, and credential-literal heuristics. Semantic scans found no positive first-delivery enterprise/copy/strong/Claude-key contradiction. No tracked implementation or outside-ticket file changed; HEAD equals `origin/personal` at `534210b9e`. | User re-approved the complete correction set on 2026-07-21; Mermaid received manual syntax/semantic inspection because no local Mermaid CLI is installed |
| 2026-07-21 | Historical user approval, later superseded for AR-006 | Discussion of the complete round-1 correction set and Claude runtime authentication | Record the then-current approval basis | User initially selected external Claude CLI/account-only behavior and approved the aligned correction set. The user later reopened only AR-006 and selected managed-secret support; AR-001–AR-005/MP-001 remain approved. | Do not use this row as current AR-006 authority; use the later two-mode decision |
| 2026-07-20 | Historical validation | artifact-local Python/`rg` checks for the then-current IDs/Markdown/Mermaid/credential heuristics | Validate the pre-review package without reading secret files | The then-current 17-requirement/17-AC/11-behavior package passed its structural checks | Superseded by round-1 findings and the final revision validator above |
| 2026-07-20 | Historical design audit | full shared `design-principles.md`, `references/design-examples.md`, then-current UC-001–UC-016 spine derivation | Revalidate the pre-review design rather than confirming prior structure | Then-current spines removed unsupported scope/address/version/connection-alias/status/binding structures | Superseded and extended by the round-1 UC-001–UC-017 audit |
| 2026-07-21 | Historical validation | artifact-local Python/`rg` validation over the ticket directory; Git source-path/status checks | Verify the pre-review in-process/separate-Store/Docker-preserving package | The then-current 9-file solution package passed structural/ID/source-isolation checks | Superseded by AR-001–AR-006 and current revalidation |
| 2026-07-21 | Command | final post-approval artifact validator; Git worktree/base/source-diff checks | Verify the then-user-approved independent-node Docker/Kubernetes revision and round-1 handoff package | Historical PASS for the round-1 package: exact 9-file solution set; links/fences/tables/ID sets/spines/diagrams/source isolation passed at baseline `534210b9e` | Superseded by AR-001–AR-006 and the current round-1 requirement-gap revision |
| 2026-07-21 | Code review round 1 | [code-review-report.md](./code-review-report.md), implementation `240d722070864e0ed960f552cdafc03d05d0ffeb`, base `534210b9e1dffff6c22855ae89ddb3d2afef5a9b` | Confirm source preserves every approved and pre-existing behavior | Review failed on CR-001: AutoByteus remote LLM/audio/image discovery and construction was absent from the approved basis and disconnected. CR-002–CR-005 are bounded implementation/packaging defects. | Revise requirements/design, obtain user approval, architecture re-review |
| 2026-07-21 | Base/current source comparison | `git show 534210b9e:...`; `git diff 534210b9e..240d72207`; `rg` for `AUTOBYTEUS_API_KEY`, `AUTOBYTEUS_LLM_SERVER_HOSTS`, discovery/reload and `AutobyteusClient` callers | Reconstruct the omitted production spine without reading values | Base LLM factory initialized/reloaded `AutobyteusModelProvider`; audio/image factories invoked remote discovery; all used one ambient `AUTOBYTEUS_API_KEY`. Current providers require an explicit key, but no production caller supplies one and the secret catalog has no AutoByteus definition. Existing Settings still exposes hosts, built-in `AUTOBYTEUS`, save-triggered full reload, and provider reload. | Formalize BEH-013 and reconnect only through managed provisioning |
| 2026-07-21 | Data-tightness trace | Current `LLMModel`, audio/image model, construction descriptors, server provisioning services, runtime enums, remote model parser | Determine how to select the correct definition for remote models | Remote model `provider` is the downstream creator/provider, while `runtime=AUTOBYTEUS` is the transport. Existing construction resolves by model provider, which would select the wrong key for a remote OpenAI/Gemini model. One required non-secret `credentialProviderId`, materialized during native/gateway registration and set to `AUTOBYTEUS` for gateway models, supplies the missing fact once. | Return only that fact plus tagged auth requirement from construction target; no server-secret type or displayed-provider fallback |
| 2026-07-21 | User decision | User response to CR-001 explanation | Confirm behavior preservation versus intentional removal | User explicitly rejected removal and approved the rule that all existing AutoByteus remote functionality remains; only key provisioning moves through the Secret Store. | Treat BEH-013/REQ-019/AC-019/UC-018 as approved intended behavior |
| 2026-07-21 | CR-001 design-principles audit | Shared `design-principles.md`; BEH-013/UC-018 base/current production trace; four DS-UC018 spines; complete cumulative artifact reconciliation | Validate span, ownership, data tightness, reuse, synchronization, transition, and product reachability before re-review | One definition, exact semantic consumers, one typed discovery owner, and one non-secret credential-owner field preserve the full path without server/core boundary reversal or three duplicated coordinators. Runtime/model-kind scoped replacement preserves native models; no-host and failure paths are explicit. All prior 24 spines remain; total is 28. CR-002–CR-005 remain proportional local fixes. | Third audit passed; run structural validator after final artifact alignment |
| 2026-07-21 | Final CR-001 artifact validator | Local Python/`rg`/`git diff --check`; all nine solution artifacts; no credential files or values opened | Verify the revised cumulative package before architecture re-review | PASS: exact BEH-001–013, REQ-001–019, AC-001–019, UC-001–018 definition sets; 28 aligned spines including DS-UC018A–D; links, Markdown fences/tables, 16 Mermaid blocks, sequence nesting, Docker/Store/Claude/AutoByteus contradiction scans, and clean diff whitespace. No local Mermaid CLI was installed, so Mermaid syntax/semantics also received manual inspection. | Ready for architecture re-review; no implementation rework authorized until pass |
| 2026-07-21 | Architecture review round 5 | [design-review-report.md](./design-review-report.md), AR-008 reviewer message | Review the CR-001 revision | CR-001 is substantively resolved. One mandatory design example contradicted the authoritative credential-owner contract by using `target.providerId`, which would select a native provider key for an AutoByteus-runtime target. Classification is bounded `Design Impact`; every other package validation passed. | Correct example and target shape, rerun package validation, preserve CR-002–CR-005 and external dependency, resubmit |
| 2026-07-21 | AR-008 data-tightness correction | Concrete LLM example plus construction-target shapes across design, backend contract, architecture, spine validation, and consumer mapping | Make credential ownership unambiguous by type and example | Canonical target is exactly `{credentialProviderId, authenticationRequirement}`. Displayed/creator provider and duplicate top-level slot are absent. Registration materializes credential owner; provisioning reads only that field and the tagged requirement-owned slot. | Bounded design-only correction; no requirement/user-visible behavior change |
| 2026-07-21 | Final AR-008 package validator | Local Python/`rg`/`git diff --check`; nine solution artifacts; no credential files or values opened | Verify the bounded correction and ensure no collateral package drift | PASS: all five construction-target definitions expose exactly `credentialProviderId` plus `authenticationRequirement`; mandatory example uses `target.credentialProviderId` and requirement-owned slot; active design contains no `target.providerId`/top-level target slot/provider fallback. Exact BEH/REQ/AC/UC sets, links, fences/tables, 28 spines, 16 Mermaid blocks/sequence nesting, and diff whitespace remain valid. | Ready for architecture re-review; implementation still unauthorized |

## Relevant Existing Behavior And Production Paths

| Behavior ID | Kind | Current Supported Trigger Or Governing Contract | Current Production Path And Lifecycle | Meaningful Current Outcome / Invariants | Evidence |
| --- | --- | --- | --- | --- | --- |
| `BEH-001` | User | Settings saves provider/search/custom credentials | Vue/Pinia GraphQL -> provider/search resolver/service -> `AppConfig.set`/custom JSON | Configured status is visible; saved values are generally not returned, but custody is plaintext | UI/resolver/AppConfig/custom store sources |
| `BEH-002` | System | Agent uses file, shell, PTY or external runtime tools | absolute-capable path resolution or spawned process with broad environment | Useful tools run, but can retrieve secret files/environment | file/terminal/runtime sources |
| `BEH-003` | Contract | LLM/search/media/metadata consumer authenticates | dotenv/AppConfig -> `process.env` or model-held key -> constructor -> SDK | Provider requests authenticate, but each consumer owns ambient lookup | consumer inventory |
| `BEH-004` | Operational | Developer/CI runs Vitest/live integration/E2E | ignored `.env.test` -> dotenv -> global test process -> module-import gating | Real coverage exists, but setup is worktree-dependent and agent-readable | refreshed test scan and historical tickets |
| `BEH-005` | User/Operational | Caller updates/removes/diagnoses provider configuration | subject/generic resolver -> AppConfig/custom store -> status checks | Partial lifecycle/status exists; no central owner/capability model | resolver/service/config sources |
| `BEH-006` | System | Direct, Electron or remote server starts | each node loads local `.env`; Electron starts child from parent env | Multiple deployment forms work, but custody/bootstrap is node-local ambient state | README/Electron/server startup sources |
| `BEH-007` | Operational | Operator deploys Docker/Kubernetes/remote node | all-in-one server/agent process/container with local data/env/mount reach | Deployment works but does not isolate custody identity from agent execution | Docker docs/compose and spawn sources |
| `BEH-008` | Operational | Existing plaintext files/records are present | startup silently consumes them and Settings updates them | Services remain available; no secure cutover/reprovision workflow | AppConfig/custom/test sources |
| `BEH-009` | Contract | Server constructs selected model | backend factory -> `LLMFactory.createLLM(id, config?)` -> `(model, LLMConfig)` -> env read | Effective config merging is centralized; auth remains ambient | factory/model/config/OpenAI sources |
| `BEH-010` | Operational | Local encrypted Store lifecycle | `No Current Path` | No Local backend or dedicated Store behavior exists | repository-wide absence plus user proposal |
| `BEH-011` | Operational | Deployment chooses secret backend/capability | `No Current Path` beyond generic env configuration | No typed backend selection or external/writable behavior exists | startup/config evidence |
| `BEH-012` | System | Server starts Claude Agent SDK runtime | runtime auth mode -> `buildClaudeSdkSpawnEnvironment(process.env)` -> Claude SDK query/spawn | `api-key` returns a broad copied parent environment containing permitted Anthropic/Claude key aliases; `auto`/`cli` conditionally delete only selected aliases | Claude auth environment/client sources and tests |
| `BEH-013` | User/System | `AUTOBYTEUS_LLM_SERVER_HOSTS` is configured; startup/list/reload or selected remote model triggers work | base factory discovery -> `AutobyteusModelProvider`/audio/image provider -> `AutobyteusClient(process.env.AUTOBYTEUS_API_KEY)` -> remote catalog/request | Remote LLM/audio/image models are discovered, retain downstream provider metadata, use `runtime=AUTOBYTEUS`, and authenticate discovery plus invocation with the AutoByteus gateway key | base/current diff, Settings host card, built-in provider/reload paths, core providers/client |

## Design Health Assessment Evidence

- Change posture: `Larger Requirement`.
- Candidate root cause classification: `Boundary Or Ownership Issue`, `Duplicated Policy Or Coordination`, `Legacy Or Compatibility Pressure`.
- Refactor posture evidence summary: refactor is required now; adding encrypted persistence while retaining global env readers, file/process reach and worktree dotenv would preserve the failure mode.

| Evidence Source | Observation | Design Health Implication | Follow-Up Needed |
| --- | --- | --- | --- |
| AppConfig/provider paths | generic config owns secret lifecycle | create dedicated lifecycle/configuration boundaries | Yes |
| LLM/search/media/metadata | ambient lookup duplicated | explicit provisioning/construction boundary | Yes |
| `LLMConfig` | serializable/retained/request-adjacent | credentials must remain separate | Yes |
| Local/test discussion | secret files copied per worktree are the workflow break | one in-process backend plus tracked selection of a machine-global, physically separate real-E2E Store | Yes |
| Kubernetes/container evidence | same trust boundary sees mounts/env/identity | storage and agent isolation must be designed together | Yes |
| User/admin discussion | no application identity exists or is requested | remove invented guard/principal scope | Completed in requirements; align all supplements |
| AutoByteus remote source trace | model creator/provider and gateway credential owner differ | represent `credentialProviderId` once and resolve through generic provisioning | Yes — CR-001 revision |

## Relevant Files / Components

| Path / Component | Current Responsibility | Finding / Observation | Design / Ownership Implication |
| --- | --- | --- | --- |
| `autobyteus-server-ts/src/config/app-config.ts` | application config load/write | loads/writes secrets and global env | retain non-secret config only; reject catalog secret names |
| provider/search GraphQL/services | Settings subject lifecycle | good user-facing subject entry but calls generic config | preserve entry; delegate to configuration/lifecycle services |
| `autobyteus-ts/src/llm/llm-factory.ts` | registry/config composition/construction | async and injectable seam | retain composition; accept resolved auth separately |
| `autobyteus-ts/src/llm/utils/llm-config.ts` | serializable model behavior | unsafe secret carrier | remain secret-free |
| concrete LLM/search/media/metadata code | provider SDK construction | environment/model key reads | accept explicit auth or injected executor |
| `autobyteus-ts/src/llm/autobyteus-provider.ts`, multimedia AutoByteus providers, `clients/autobyteus-client.ts` | AutoByteus remote discovery/request protocol | base read one ambient key; implementation requires explicit key but has no production discovery caller | keep storage-neutral; accept ephemeral auth and return discovered models without backend access |
| core LLM/audio/image model and factory types | model metadata/registry/construction target | remote models retain downstream provider while gateway owns authentication; provider-scoped replacement can delete native same-provider models | add `credentialProviderId` and runtime-scoped synchronization; generic provisioning consumes descriptor |
| server model catalog/provider services and cache preloader | startup/list/reload lifecycle | existing user-visible triggers remain reachable but no service resolves the AutoByteus key | add one shared `AutobyteusRemoteModelDiscoveryService`; call it from LLM/audio/image server provider boundaries before cache publication |
| built-in provider Settings/service/store | write-only API-key lifecycle and reload | `AUTOBYTEUS` row and save-triggered reload exist, but credential status is skipped and catalog binding absent | map lifecycle to `provider.autobyteus.api-key`; preserve UI and reload behavior |
| custom provider store/model | endpoint metadata/runtime model | plaintext key in v1/model | migrate to metadata-only; derive definition from provider UUID |
| `autobyteus-ts/tests/setup.ts` | global test initialization | loads ignored `.env.test` and announces key presence | remove credential dotenv loading |
| server test setup | Prisma/test env | some `.env.test` settings may be non-secret | classify; permit tracked non-secret config separately |
| package `.gitignore` files | ignore local config | broad `.env.*` ignore | add narrow exception only if tracked secret-free `.env.test` retained |
| Electron server managers | embedded server launch | clone parent env | pass non-secret backend configuration only; embedded server constructs default Local backend in process |
| `autobyteus-server-ts/src/application-engine/runtime/application-worker-supervisor.ts` | supported application backend worker launch | currently spreads `process.env` and runs application code with the server filesystem identity | route through the empty-base process-environment/descriptor boundary; explicitly retain the same-user limitation and report only `LOCAL_HARDENED` |
| `autobyteus-server-ts/src/runtime-management/claude/client/claude-sdk-auth-environment.ts`, `claude-sdk-client.ts`, setting-source policy, session and diagnostics | Claude auth selection, JIT provisioning, SDK child spawn and output boundary | supports `auto`, `cli`, and `api-key`, begins from a copied full parent environment, accepts caller `env`, loads user/project/local settings, and buffers raw diagnostics | replace with `ClaudeRuntimeAuthenticationService` plus exact `cli` and `managed-secret`; managed mode calls central resolution, client composes child-only env/tool/settings policy, and diagnostics redact before buffering |
| Docker compose/image | node packaging and persistent server data | existing named volume maps `/home/autobyteus/data`; all-in-one/root prevents strong assurance | leave Compose/launcher unchanged; Local Store default derives below existing data directory; report lower-tier assurance without prescribing E2E mounts |
| app-data migrations | versioned migration ledger | available but currently runs after early dotenv load and may continue on error | secure cutover must precede secret consumers and fail closed |

## Runtime / Probe Findings

| Date | Method | Exact Command / Method | Observation | Implication |
| --- | --- | --- | --- | --- |
| 2026-07-17 | Probe | `stat` metadata only on local secret-bearing files | representative files were locally present and permissive; values not read | plaintext-file custody is not an acceptable target |
| 2026-07-17 | Probe | assignment-name-only extraction from secret-bearing files | confirmed provider/search aliases without printing values | catalog/migration aliases can be defined without value inspection |
| 2026-07-20 | Script | refreshed `git ls-files`, ignore-rule and test env-reference scan | no tracked live Store configuration; `.env.*` broadly ignored; active dotenv/provider env reads remain | typed tracked Store selection and loader removal are required |
| 2026-07-21 | Code trace | Prisma/AppConfig/Electron runtime path inspection | normal application DB location varies by runtime/data directory and Electron reset deletes the whole server-data directory | real-E2E custody should not be another ordinary application table; reset must explicitly preserve dedicated Store files |
| 2026-07-21 | Docker trace | current Compose/Dockerfiles/entrypoint | `AUTOBYTEUS_DATA_DIR=/home/autobyteus/data` is already backed by the `autobyteus-server-data` named volume | derive the normal Docker Local Store below that directory; make no Compose, launcher, or Docker E2E mount change |
| 2026-07-21 | Base refresh/code trace | fast-forward to `534210b9e`; inspect application worker host/supervisor/protocol | supported application backend worker currently clones `process.env` and executes with server filesystem identity | include this exact launcher in REQ-004 empty-base enforcement and `LOCAL_HARDENED` evidence; do not claim same-user denial |
| 2026-07-21 | Claude code trace | inspect auth environment, client/model-list/session/diagnostics call sites and tests without reading any secret value | `api-key` returns copied `process.env` unchanged; `auto`/`cli` still begin broad; callers may provide `env`; model discovery and run both spawn; diagnostics buffer unredacted text | AR-006 is reachable; one launch-auth owner must cover both SDK use paths, remove broad/caller env, and redact on append |
| 2026-07-21 | Pinned SDK scratch probe | inspect 0.2.71 package types/runtime and grep recognized auth variable names | SDK uses its supplied `env` for the child and recognizes `ANTHROPIC_API_KEY`; `settingSources: []`, strict MCP, explicit tools/disallowed tools are available | Managed-secret can use supported child `env` without mutating parent; settings/tool restrictions are enforceable at the wrapper boundary |
| 2026-07-21 | AutoByteus remote production trace | compare factory/provider/client files at base and implementation commit; inspect server catalog/reload/Settings callers | `AUTOBYTEUS_LLM_SERVER_HOSTS` remains reachable, base discovery called all three remote providers, current explicit-key providers have no production callers, and existing UI reloads all after saving `AUTOBYTEUS` | CR-001 is reachable and must be repaired through Store-backed discovery/construction rather than feature removal or ambient fallback |

## External / Public Source Findings

- Public API/spec/upstream sources: Kubernetes Secrets/service accounts/good practices; HashiCorp Vault KV v2/Agent; cloud secret-manager/KMS guidance; Electron safeStorage; OWASP; Node child process.
- Version/freshness: official documentation rechecked 2026-07-20 where deployment behavior is material.
- Relevant constraints learned: workload identity is preferred; Kubernetes volume/env delivery does not protect against a shell in the same container; native Secrets need at-rest/RBAC controls; a daemon pattern is established but not mandatory; OS-specific Electron storage is not deployment-neutral. Claude Agent SDK supports explicit child `env`, empty setting sources, strict MCP configuration, and tool controls, while `allowedTools` alone is not a security allowlist.
- Why it matters: backs the selected in-process local backend, capability-aware enterprise adapters, explicit environment construction, physical Store isolation, trusted-server/agent separation, and the bounded managed-Claude launch policy.

## Reproduction / Environment Setup

- Required services/mocks/emulators for investigation: none; no live secret store was started and no provider call was made.
- Required config/flags/accounts: none.
- External repos/samples: none cloned.
- Setup commands affecting investigation: dedicated worktree creation and second-round fast-forward to `origin/personal`.
- Cleanup: no temporary service/process/data created.

## Findings From Code / Docs / Data / Logs

### 1. Configuration and custody are distinct responsibilities

`SecretStorageConfigurationService` selects a typed backend configuration and persists only non-secret options. `SecretManagementService` owns catalogued values/status and uses the constructed backend. Provider/search subject services coordinate their metadata with lifecycle. This avoids both the old generic AppConfig blob and the previously invented user/admin scope.

### 2. One in-process backend with separate Stores directly solves the worktree problem

Agent Server constructs `LocalSecretStorageBackend` directly. Electron, direct, and normal Docker startup derive the default database/key below their configured server data directory. Host worktrees track the separate host real-E2E database/key selection and preflight value-free status. The user provisions that host real-E2E Store once per machine/provider, not per branch/worktree. No additional service is started and Docker deployment is not altered.

### 3. Physical Stores are stronger and simpler than arbitrary profiles

A backend instance is bound at bootstrap to one database/key pair, and normal resolve calls do not accept a Store selector. There is no fallback/inheritance. First-delivery E2E setup is constructed with only the writable E2E target and accepts dedicated test credentials directly; it cannot open or copy from the default Store. Separate host default/E2E files and keys improve operational and cryptographic isolation; they do not defeat arbitrary code running as the same unrestricted OS user and do not impose a Docker Store-sharing model.

### 4. Non-secret `.env.test` can be tracked, but should not own live-secret semantics

The user correctly identified that a credential-free file can be committed. The canonical live Store/scenario contract should be typed root JSON for validation and multi-package reuse. A tracked `.env.test` may remain for ordinary non-secret test settings or point to the typed config. Broad ignore rules need narrow exceptions and repository scans must prevent regression.

### 5. Capability contracts remain useful without pretending a production enterprise adapter ships

Writable backends support the existing Settings-shaped atomic save (create-or-replace) and idempotent remove. Externally managed backends support resolve/status/health while writes are disabled. First delivery proves the contract with InMemory, Local read-write/read-only, and a test-only externally-managed fixture. Concrete Kubernetes/Vault/AWS behavior is deferred until an adapter and its deployment identity are actually implemented; an unknown kind fails value-free without Local fallback. This preserves the user's company extension model without a placeholder production claim.

### 6. LLM construction needs two narrow shapes

Public factory input becomes `{configInput, authentication}` while the factory composes final `LLMConfig` and creates `LLMConstructionContext`. The context exists only between factory and concrete client constructor. Individual clients never import the server secret subsystem or backend.

### 7. Kubernetes/cloud custody is a future adapter boundary, not first-delivery behavior

Company infrastructure may later mount a value, let the server read Kubernetes API, or grant workload identity to Vault/AWS through a separately installed adapter. Environment injection remains rejected for provider keys. A mount or service account in the same container as an unrestricted agent shell is not a protection boundary. First delivery supports only one Local Store per Docker node or single Pod/PVC and reports `LOCAL_HARDENED`; multi-node centralized custody and strong identity/container/pod enforcement are future work.

### 8. First-delivery assurance is explicitly lower-tier

The agreed Local backend uses cross-platform encrypted SQLite databases and vetted authenticated encryption, not Electron/OS keychain APIs. Zero-touch server startup reads an owner-only machine-local key file. First delivery also removes plaintext custody, blocks Store paths through built-in file tools, and builds child environments/descriptors from empty allowlists. Without a separate OS identity or enforced agent sandbox, same-user code may still reach the database/key pair or trusted memory. The product therefore reports `LOCAL_HARDENED` only after the implemented checks pass and never reports `STRONG_AGENT_ISOLATION` in this delivery.

### 9. Use-case spines removed speculative contract fields

The UC-001–UC-017 audit found no product path for a generic organization/deployment/environment scope DTO, a caller-visible physical storage address, a Settings expected-version/CAS field, or a selectable Local Store connection alias. `SecretManagementService` receives one semantic consumer identity and owns catalog lookup; callers do not resolve a binding and pass a duplicate definition. Management returns healthy-only definition storage state, configuration/backend ownership returns complete five-state health, and provider/search subject services own validation state. These corrections remove boundary bypasses and attributes that the product cannot currently produce or consume.

### 10. Local Store persistence, concurrency, and reset are concrete

The normal Store directory is `${serverDataDir}/secret-store/`. Electron resolves that to `~/.autobyteus/server-data/secret-store/`; ordinary Docker resolves it below `/home/autobyteus/data` in the existing named volume. The host real-E2E Store uses `real-e2e-secret-store.db` plus `real-e2e-secret-store.key` in the host AutoByteus server-data secret-store directory. Each database holds format/pair-binding metadata and exact encrypted `(definition_id, nonce, ciphertext)` records; no profile table exists. Each Agent Server opens its configured Store directly. SQLite transactions, journal/busy handling where writable, and explicit read-only mode coordinate processes. Because current Electron `AppDataService` resets the complete `server-data` directory, ordinary reset behavior must preserve `secret-store/`; deletion requires an explicit reset naming exactly one Store.

### 11. Persistent Store formats require pair authentication and compatibility checks, not IPC

Electron, direct servers, and worktrees may open a Store created by another package version, while independent database/key files may be swapped or left partial before any record exists. UC-004/UC-005/UC-010/UC-011 therefore require a random `store_id`, format versions, and AES-GCM pair verifier authenticated under a domain-separated HKDF key before `READY`, including an empty Store. A wrong/swapped key, partial pair, missing current verifier, or verifier failure is `CORRUPT`; unsupported schema/encryption/verifier versions are `INCOMPATIBLE`. Neither is silently repaired, rewritten, or downgraded. There is no process protocol or connection handshake.

### 12. Corrupt and incompatible Stores are outward control-plane states

The approved failure states are operationally reachable through file-pair integrity, record authentication, persistent-version, lock, and dependency contracts. Health is exactly `READY`, `LOCKED`, `UNAVAILABLE`, `CORRUPT`, or `INCOMPATIBLE`. Per-definition `MISSING`/`CONFIGURED` exists only at `READY`. For every non-ready state the server starts a value-free Settings/configuration/health control plane, disables writes/validation/provider construction, and returns a stable instruction code without paths or values. This closes AR-004 without overloading definition state.

### 13. Claude can reuse the generic service only through a dedicated runtime-authentication owner

`SecretManagementService.resolveForUse(consumer)` already owns catalog lookup, authorization, backend resolution, and value-safe error mapping. Adding a Claude-specific resolution method or letting `ClaudeSdkClient` access the catalog/backend would duplicate or bypass that boundary. The clean extension is a precise semantic identity `{kind:"agentRuntime",runtimeKind:"claude_agent_sdk",credentialSlot:"apiKey"}` authorized to the existing `provider.anthropic.api-key` definition. `ClaudeRuntimeAuthenticationService` owns mode selection and is the sole Claude caller of `resolveForUse`; native Anthropic LLM/metadata retain distinct identities over the same definition.

### 14. Two explicit modes are safer and clearer than current `auto|cli|api-key`

Default `cli` mode performs no secret lookup and uses explicit external Claude CLI/account state. Explicit `managed-secret` resolves just in time and delivers only `ANTHROPIC_API_KEY` in the exact Claude Code child environment. Legacy `auto` and `api-key` are invalid configuration rather than aliases or fallbacks. This removes implicit ambient selection and makes the trust decision observable without adding a new UI requirement; `CLAUDE_AGENT_SDK_AUTH_MODE` remains non-secret startup configuration.

### 15. Managed child environment delivery is useful but deliberately bounded

The pinned SDK supports an explicit child `env`, so first delivery does not require a local broker, descriptor protocol, or custom SDK fork. The wrapper builds from an empty base, never mutates parent `process.env`, and does not accept caller `env`. Managed mode adds exactly `ANTHROPIC_API_KEY`; no config/session/log/CLI argument or readback API contains it. Because the authorized Claude process can observe its own environment, supported environment/process inspection and descendant-spawn paths are removed: empty user/project/local setting sources, no hooks/plugins/API-key helper/external MCP configuration, strict explicit MCP, and SDK `tools: []`. `allowedTools` contains only explicitly materialized AutoByteus MCP names. AutoByteus-owned tools execute server-side with sanitized environments. The design makes no deterministic JavaScript or upstream SDK-memory zeroization claim.

### 16. Claude diagnostics are a separate outward leak boundary

Current `ClaudeProcessDiagnostics` buffers raw stderr and redacts only while summarizing. With managed delivery, redaction must happen before text enters the buffer, then again at outward formatting. Spawn/provider-auth failures use stable value-free codes; raw child errors never enter logs, session events, GraphQL, support bundles, or test artifacts.

### 17. AutoByteus remote discovery is supported product behavior, not dead code

The base branch called AutoByteus remote LLM discovery from `LLMFactory` initialization and targeted reload, and called remote audio/image discovery from their factories. `AUTOBYTEUS_LLM_SERVER_HOSTS` remains a supported server setting, the built-in provider catalog still contains `AUTOBYTEUS`, the web client reloads all models after saving that provider key, and provider-scoped reload still declares AutoByteus reloadable. Removing those callers removes reachable user functionality rather than cleaning obsolete code.

### 18. Gateway credential ownership must be separate from model provider identity

Remote catalog payloads can describe an OpenAI, Gemini, or other model while all network traffic is sent to an AutoByteus host. Resolving by `model.providerId` would therefore use the downstream provider key rather than the gateway key. The minimal reusable correction is a required non-secret `credentialProviderId` materialized during registration: native models assign their credential owner once and `runtime=AUTOBYTEUS` models assign `AUTOBYTEUS`. Construction targets omit displayed provider, so LLM/media provisioning remains generic and has no provider fallback or AutoByteus branch.

### 19. Discovery has its own semantic consumers but shares one definition

The same upstream AutoByteus key authenticates LLM/audio/image catalog reads and runtime requests, so duplicating stored values would be wrong. The catalog maps `modelDiscovery/{llm|audio|image}/AUTOBYTEUS/apiKey`, `llm/AUTOBYTEUS/apiKey`, and `media/{audio|image}/AUTOBYTEUS/apiKey` to `provider.autobyteus.api-key`. A server-owned remote discovery service performs the JIT resolution and passes an ephemeral authentication value to storage-neutral core providers. If no hosts are configured it performs no lookup and clears only the matching model-kind AutoByteus runtime subset. Successful results synchronize only the corresponding AutoByteus-runtime subset, preserving native models with the same downstream provider. A transient pre-authoritative configured-host refresh failure preserves last-known-good, while explicit successful credential removal is authoritative and clears all AutoByteus runtime subsets without lookup.

### 20. Source-review local fixes do not change the approved product model

CR-002 maps the real existing Claude account location into the empty-base CLI child instead of a newly created empty app-data home. CR-003 applies explicit stdio MCP `config.env` as the authorized additions map over the sanitized operational base. CR-004 makes repeated custom-provider deletion successful. CR-005 corrects the handoff so managed-only tool/settings restrictions are not asserted for CLI. These corrections remain with implementation engineering after architecture re-review.

## Persisted Data Transition Evidence (When Applicable)

- Current stored subject/location/shape/volume: small key/value `.env` and `.env.test` files, including the legacy `AUTOBYTEUS_API_KEY` alias where configured; custom-provider v1 JSON with `apiKey`; runtime model `endpointApiKey`; public frontend config field; exact values intentionally not inspected.
- Relevant model change: secret values leave AppConfig/test/custom metadata and become backend records in a configured custody location; custom provider becomes metadata-only current schema; test configuration becomes tracked and secret-free.
- Normal readers/writers: dotenv/AppConfig, GraphQL/provider services, test setup, custom store/sync, LLM/search/media/metadata constructors.
- Direct-use preservation: `No` for plaintext values/old readers; `Yes` for classified non-secret metadata/settings after explicit transformation.
- Physical/security constraints: no plaintext import/backup/quarantine; atomic scrub/rewrite; migration before consumers; restrictive file permissions; upstream keys should be revoked after reprovision.
- Benefit/cost/risk: clean removal materially reduces agent/worktree disclosure; availability is temporarily lost until reprovision; bad ordering could reload old values or lose non-secret metadata.
- Existing framework: `autobyteus-server-ts/src/app-data-migrations`; needs a critical pre-consumer gate rather than current continue-on-error behavior.

## Constraints / Dependencies / Compatibility Facts

- No actual secret value may enter artifacts, logs or conversation.
- Every backend requires a bootstrap identity/key source; moving a credential-bearing bootstrap source into `.env` would recreate the problem. Local database/key paths themselves are non-secret bootstrap configuration.
- The real-E2E Store/scenario file is tracked configuration, not a credential authority.
- `LocalSecretStorageBackend` runs inside Agent Server and directly opens exactly one configured encrypted database/key pair. Runtime callers above `SecretManagementService` never open it.
- Real direct-provider tests require reviewed trusted execution and test-scoped credentials.
- Electron starts the embedded Agent Server; the server constructs the Local backend. Direct Node/test servers do the same without Electron.
- Existing Settings access is preserved; no user/admin model is part of this ticket.
- No environment fallback is permitted after cutover.
- `AUTOBYTEUS_LLM_SERVER_HOSTS` remains non-secret configuration; `AUTOBYTEUS_API_KEY` becomes migration-only and normal runtime must not inspect it.
- Claude `managed-secret` intentionally places one credential in the authorized Claude child process environment. This is not persistence or broad parent inheritance, but it is plaintext in trusted runtime memory and observable by that process/SDK.
- The first-delivery supported delivery mechanism is the pinned SDK's explicit `env` option. The package-internal file-descriptor mechanism is not adopted because it is not a stable public cross-platform contract.

## Architecture Review Round 1 Resolution Record

| Finding | Reviewer Classification | User-Approved Resolution In Revised Package | Approval State |
| --- | --- | --- | --- |
| `AR-001` | Critical requirement gap | First delivery is explicitly `LOCAL_HARDENED`; strong identity/mount/network/process isolation is deferred and cannot be reported | User re-approved 2026-07-21 |
| `AR-002` | High requirement gap | Ship/register only Local product backend, InMemory tests, and test-only external capability fixture; no concrete production enterprise adapter is an acceptance dependency | User re-approved 2026-07-21 |
| `AR-003` | Medium requirement gap | Remove default-to-E2E copy completely; target-only setup directly accepts dedicated E2E credentials | User re-approved 2026-07-21 |
| `AR-004` | High requirement gap | Outward health is exactly `READY/LOCKED/UNAVAILABLE/CORRUPT/INCOMPATIBLE`; definition state exists only at ready; non-ready server exposes value-free degraded control plane | User re-approved 2026-07-21 |
| `AR-005` / `MP-001` | High requirement gap / reachable premise | Bind database/key through random `store_id` plus domain-separated AES-GCM pair verifier authenticated on every open, including empty Stores | User re-approved 2026-07-21 |
| `AR-006` | High requirement gap, reopened after round-2 hold | Exactly two modes: default external `cli` and explicit `managed-secret`. Add the exact Claude runtime consumer to the catalog, reuse `resolveForUse`, deliver only to the child, constrain settings/tools/diagnostics, define failures/migration/tests, and reject legacy `auto` and `api-key` without fallback | User-approved 2026-07-21; architecture re-review requested |
| `AR-007` / `MP-002` | External-authority premise raised in round 3 | Retain both modes. Record the newer June 15–16 official Help Center evidence expressly recognizing current third-party Agent SDK subscription usage, the conflicting SDK/legal wording, AutoByteus's local/self-hosted no-broker shape, current technical operation, and a maintained release policy dependency. Ask the reviewer to reassess rather than treat the overview sentence as the only current authority. | User reaffirmed 2026-07-21; architecture re-review requested |
| `AR-008` | Medium bounded design impact | Correct the mandatory construction example to use `target.credentialProviderId`; canonical target exposes only credential owner plus tagged authentication requirement, with slot nested in that requirement and displayed provider absent. | No behavior change/user reapproval needed; architecture re-review required |

## Code Review Round 1 Resolution Record

| Finding | Classification | Revised Package Decision / Owner | Approval State |
| --- | --- | --- | --- |
| `CR-001` | Requirement Gap | Preserve AutoByteus remote LLM/audio/image behavior through `provider.autobyteus.api-key`, exact discovery/construction consumers, runtime-aware credential ownership, server-owned discovery, runtime-scoped synchronization, migration, Settings/status, and real coverage. Never restore ambient key reads. | User approved 2026-07-21; architecture re-review required |
| `CR-002` | Local Fix | CLI empty-base environment explicitly maps the pre-existing node-local Claude account location; default uses the real OS home and an explicit validated override may select another existing account root. | Implementation owner after re-review |
| `CR-003` | Local Fix | Stdio MCP uses sanitized operational parent input and passes the exact configured server-specific map as additions; unrelated parent/Store/provider values remain excluded. | Implementation owner after re-review |
| `CR-004` | Local Fix | Repeated deletion of an absent custom provider is a value-free success while built-in deletion remains rejected. | Implementation owner after re-review |
| `CR-005` | Local Fix | Correct handoff: empty-base environment applies to both Claude modes; `tools: []`, empty setting sources, and strict explicit MCP are managed-only. | Implementation owner after re-review |

## Open Unknowns / Risks

1. Which enterprise adapter is prioritized in a future delivery (Vault, Kubernetes, AWS, or company-specific). This no longer blocks first delivery because no concrete adapter is claimed or accepted now.
2. Select/prove the SQLite access and Electron/Node packaging path, multi-process locking/busy behavior, staged pair creation, host read-only E2E opening/checkpoint discipline, and owner-only ACLs for the fixed logical schema. Encryption format 1 is fixed to Node crypto HKDF-SHA-256 + AES-256-GCM; auto-unlock retains explicitly lower same-user assurance.
3. Whether tracked `.env.test` remains for non-secret compatibility or active settings move entirely to root typed config.
4. Which enforceable OS/container identity, mount, and network controls later qualify for `STRONG_AGENT_ISOLATION`. They are explicitly outside first delivery.
5. Exact real scenario/gateway/direct classification and provider quota/concurrency policy.
6. Complete spawn/path inventory may expand during implementation; central policy must fail closed on missing security context.
7. Replacing current `auto|cli|api-key` with `cli|managed-secret` is an intentional compatibility break. Invalid legacy mode values must fail before spawn with remediation rather than silently mapping; ambient API-key aliases are scrubbed and never select authentication.
8. Pinned-SDK verification must prove `tools: []` disables all Claude built-ins and that `allowedTools` contains only explicitly materialized AutoByteus MCP names. Any SDK version where this invariant cannot be demonstrated fails managed-mode startup rather than weakening policy.
9. Anthropic's current official Help Center and SDK/legal pages conflict on third-party Agent SDK subscription authentication. This is a maintained external release dependency. Current working behavior proves technical reachability only; the dated June notice is the material authority supporting retention while reviewer reassessment or upstream clarification proceeds.
10. The authorized Claude executable/SDK may retain the environment in its own memory after AutoByteus drops references. This is an explicit `LOCAL_HARDENED` trust limit, not a blocked implementation question.
11. Real AutoByteus remote endpoints may advertise different LLM/audio/image capability sets. The live harness must treat each advertised/declared capability explicitly and must not turn an unavailable capability into a false pass.

## Notes For Architecture Reviewer

Round 5 confirmed CR-001 is substantively resolved and raised only bounded AR-008. The mandatory example now uses `target.credentialProviderId`; the canonical construction target omits displayed provider and duplicate top-level slot, so the exact CR-001 wrong-key path is unrepresentable at that boundary. The complete existing AutoByteus remote LLM/audio/image path still changes only key provisioning: one managed definition, exact discovery/construction consumers, `credentialProviderId=AUTOBYTEUS` for gateway models, and no environment fallback. No requirement or user-visible behavior changed. CR-002–CR-005 remain implementation-owned and must travel with the package. `EXT-ANTHROPIC-AGENT-SDK-AUTH` remains a release dependency and no Claude mode changes are proposed.
