# Token Usage: One Cumulative Row Per Agent Run — Requirements

## Status (`Design-ready`)

Approved by the user on 2026-08-19 after repository, release-tag, representative production-data, and historical-upgrade investigation. The approval adopts the recommended strict one-row model and `REQ-011` run-created-in-range/lifetime-total semantics (Option A), and directs the design to follow `autobyteus-server-ts/README.md` production data-migration conventions. Architecture-review refinements in `SR-002` and `SR-003` made the bounded-state and direct/skip-version upgrade contract actionable. User-directed `SR-004` centralized migration governance. `SR-005` makes current runtime strictly forward-only, confines all legacy-schema knowledge to migration code, classifies critical current-schema failure separately from capability-scoped app-data failure, and replaces the `SR-003` runtime overlap guard with a degraded restore/history gate. User-directed `SR-006` makes the current-application-contract test and detailed database/file-format warning examples explicit without changing those mechanics. The one-row product choice remains unchanged.

## Goal / Problem Statement

Deliver two explicit upgrade-safe change groups in the same ticket. First, repair the already-released `20260730_token_usage_provider_name_snapshot_backfill` **in place under the same migration ID** so an installation whose upgrade is currently stuck at that historical migration can install the newer version, retry it with the corrected bounded implementation, and start successfully; the transition-critical sibling model-value definition must be bounded under its unchanged ID for the same direct/skip-version path. Second, replace the append-per-notification token-usage ledger with one authoritative cumulative token-usage row per canonical agent run through a separate new migration. The invariant applies equally to standalone runs, direct/nested team members, and delegated/task-created agent runs. Existing installations must consolidate safely without losing lifetime run/team totals.

The current representation is not merely inefficient. It models cumulative run state as immutable wide events, repeats raw and pricing JSON, requires readers to reconstruct totals by summing rows, and lets a nonessential display-metadata migration materialize the entire table through Prisma/NAPI.

## Governing Migration Convention

The normative deterministic transformation rule, forward-only current-runtime boundary, migration-only legacy ownership, operating assumptions, unsupported premises, product-reachability gate, current-application-contract test, worked database/file-format classifications, and proportionate default are defined in [`data-migration-conventions.md`](./data-migration-conventions.md). Task-specific source/target shapes and transformation mechanics remain in `design-spec.md`. The intended durable repository destination is `autobyteus-server-ts/docs/design/production_data_migration_conventions.md`, linked from the README; delivery owns that documentation sync.

## Current And Desired Behavior (Mandatory)

| Behavior ID | Current Behavior | Desired Behavior | Preserved / Unchanged Behavior | Related Requirement / Acceptance-Criteria IDs |
| --- | --- | --- | --- | --- |
| BEH-001 | Every enriched `TOKEN_USAGE_UPDATED` event is asynchronously appended as a new `token_usage_ledger_events` row. Codex can emit cumulative snapshots every few seconds; native AutoByteus emits per-call deltas and Claude emits terminal per-turn deltas. Existing runs can be restored under the same canonical `run_id`, so a provider may replay the last persisted observation. | After consolidation succeeds, the forward-only persistence owner atomically folds every supported update shape into exactly one current row keyed by canonical `agent_run_id`/`run_id`. While consolidation is incomplete, pre-existing-run restoration is gated before a provider can replay legacy usage; newly allocated runs use only the current table. | Runtime adapters continue to emit live token events and current token fields for admitted current runs; all supported runtime kinds remain accounted. | REQ-001–REQ-005, REQ-023, REQ-026; AC-001–AC-005, AC-022, AC-025 |
| BEH-002 | Agent, member, team, and statistics summaries load event arrays and sum accounting deltas and costs. Team rows group by `root_team_run_id`, then exact `run_id`. | Run summaries read the one cumulative row; team/parent totals sum the latest cumulative rows of their concrete member agent runs without double counting. | Existing GraphQL summary shapes, cache/reasoning components, nullable cost/status semantics, and exact run/root grouping remain stable except for approved period semantics. | REQ-006–REQ-010; AC-006–AC-009 |
| BEH-003 | Settings > Token Statistics filters individual ledger events by `observed_at` in an arbitrary start/end period, then aggregates only those deltas. | **Approved choice:** the date range selects runs by `run_created_at` (or the established first-usage fallback), and the table shows each selected run's lifetime cumulative totals. | Task/team hierarchy, per-model grouping, display labels, and cost breakdowns remain available. | REQ-011; AC-010 |
| BEH-004 | `20260730_token_usage_provider_name_snapshot_backfill` selects every wide row twice (pre/post) plus all null-name candidates, snapshots every non-target field as JSON, accumulates unbounded skip details, and is a hard prerequisite of a startup-fatal custom-provider migration. Its earlier sibling `20260730_token_usage_custom_provider_model_value_backfill` also reads the full ledger before/after and accumulates a detail for every skipped row. A machine already recorded as `FAILED` retries the definition shipped by the newly installed version. | Repair both released definitions under their unchanged IDs: filter to potential candidates in SQL, select only required columns in bounded keyset batches, use compare-and-set updates, bounded diagnostics, and scalar invariants, and keep the provider-name display repair from gating global startup or independent provider-identity safety. | Eligible model-value/provider-name fields are still normalized or filled by compare-and-set; unknown/unrecoverable labels remain truthful fallbacks. | REQ-012–REQ-016, REQ-022, REQ-024–REQ-025; AC-011–AC-015, AC-021, AC-023–AC-024 |
| BEH-005 | Existing databases retain one row per usage report; the representative local database has ~154k rows for 1,269 runs and ~774.5 MiB of table/index pages. Without a degraded gate, a failed consolidation could be followed by restored-run replay into current storage. | A **separate new migration**, ordered after both bounded released source-shaping backfills, produces one current cumulative row per distinct canonical run. On capability-scoped failure, new-run current rows and legacy run IDs remain disjoint because pre-existing-run restoration is unavailable. Retry validates disjointness, imports legacy aggregates, validates, decommissions legacy rows, and makes their pages reusable. | Latest required usage, cost, attribution, display, and prompt/context facts remain accurate; event-level raw history is intentionally not retained in the authoritative store. | REQ-017–REQ-026; AC-016–AC-025 |
| BEH-006 | A failed provider-name backfill prevents the dependent readable-provider migration and the embedded server exits with `APP_DATA_STARTUP_GATE_FAILED`. A failed consolidation state can also retain legacy facts while current execution resumes. | Classify failure against current invariants. Provider-display or consolidation app-data failure is capability-scoped: the app, settings, unrelated features, and newly allocated runs start, while historical token reads and pre-existing-run restoration remain unavailable. Failure of required current Prisma/schema/platform invariants may stop startup; no old-schema runtime fallback is added. | Secret-vault and provider-identity safety failures remain distinct from statistics-label backfill failures. A user can install a corrected release externally even when startup is critically blocked. | REQ-015–REQ-016, REQ-019, REQ-023–REQ-026; AC-014–AC-015, AC-017, AC-019, AC-022–AC-025 |

## Investigation Findings

- Current schema: `TokenUsageLedgerEvent` has 83 columns, unique per-event/idempotency keys, no uniqueness on `run_id`, and wide raw/pricing JSON fields.
- Current writer: `TokenUsageEventPersistenceProcessor -> TokenUsageLedgerStore.appendTokenUsageEvent -> SqlTokenUsageLedgerRepository.appendUsageEvent` schedules one insert for every enriched update.
- Current accounting is mixed-shape:
  - native AutoByteus: `per_call` deltas;
  - Claude SDK: `per_turn` deltas;
  - Codex: `cumulative_snapshot`, normalized against the preceding snapshot into an accounting delta before append.
- Therefore, “upsert the latest incoming payload” would be incorrect. The target row must accumulate normalized deltas and keep compact per-series reconciliation state for cumulative sources.
- Representative local production evidence on 2026-08-19:
  - database file: ~806 MiB;
  - ledger: 154,096–154,108 rows while the running app continued writing, 1,269 distinct runs, average 121.43 rows/run, maximum 4,499;
  - 148,391+ Codex cumulative rows;
  - table plus indexes: ~774.5 MiB (table alone ~690.1 MiB);
  - raw-event, raw-usage, and pricing JSON payloads alone total ~475 MiB;
  - the current `SELECT *` shape contains ~635.8 MiB of text across the ledger and ~620.8 MiB among null-name candidates;
  - all 149k null-name candidates in this sample are non-AutoByteus, so a correctly filtered provider-name query would read zero rows;
  - 228 runs cross UTC-day boundaries, proving strict one-row storage cannot preserve the existing “usage deltas observed during arbitrary period” result by itself;
  - two observed runs contain more than one cumulative snapshot series, so the target must reconcile per source series even though the authoritative subject remains one run row;
  - all 1,269 observed runs use one runtime/model identity, but 48 runs contain more than one pricing tuple and aggregate price/status semantics must not be replaced with the latest event's price.
- Release tags `v1.4.49` and `v1.4.52` both contain the full-wide provider-name migration and startup-fatal dependent provider gate. Current `origin/personal` still performs full-table `SELECT *` pre/post snapshots and retains the fatal prerequisite coupling.
- Current `origin/personal` also makes the earlier custom-provider model-value backfill transition-critical: it reads all ledger rows before and after, builds whole-ledger invariant arrays, and stores a skip detail for every non-target row. A supported direct/skip-version upgrade can therefore fail before reaching a bounded consolidation unless this unchanged-ID sibling is repaired too.
- Canonical run identity is already global and opaque: standalone and team topology creation use `AgentRunIdentityAllocator`; team and delegated executions carry exact agent run IDs plus separate root/task/address metadata.

Detailed evidence and the period-semantics decision table are in [`token-usage-data-model-analysis.md`](./token-usage-data-model-analysis.md).

## Relevant Supplemental Task Artifacts

| Artifact Path | Type / Purpose | Related Requirement IDs | Related Acceptance-Criteria IDs | Status / Approval | Relationship To Requirements |
| --- | --- | --- | --- | --- | --- |
| `tickets/in-progress/token-usage-one-row-per-agent-run/token-usage-data-model-analysis.md` | Evidence/context analysis of current storage, update shapes, migration failure, one-row constraints, and failed-consolidation replay overlap | REQ-001–REQ-023 | AC-001–AC-022 | Evidence is complete; decision table supports approval but does not supersede this document | Explains why a naive latest-payload upsert, unbounded distinct-series state, unchanged period semantics, an unbounded predecessor, and unguarded cross-schema replay are incompatible with the requested invariant |
| `tickets/in-progress/token-usage-one-row-per-agent-run/data-migration-conventions.md` | Normative migration principles: deterministic mapping, forward-only current runtime, migration-only legacy knowledge, failure classification, reachability, and proportionality | REQ-012–REQ-026 | AC-011–AC-025 | Approved by explicit user direction; governs implementation and review | Defines why capability gating or critical startup failure replaces backward-compatible runtime code and records the delivery-owned durable-doc destination |

## Design Health Assessment (Mandatory)

- Change posture: `Larger Requirement` combining `Bug Fix`, `Performance`, `Behavior Change`, `Refactor`, and persisted-data contraction.
- Initial design issue signal: `Yes`.
- Root cause classification: primary `Boundary Or Ownership Issue` plus `Missing Invariant` and `Shared Structure Looseness`.
- Refactor posture: `Likely Needed` / required in this change.
- Evidence basis: the persistence boundary owns events rather than the cumulative agent-run accounting subject; the 83-column event shape mixes current state, reconciliation state, audit/debug payloads, price inputs, derived costs, display snapshots, and topology metadata. Every reader compensates by rebuilding summaries from arrays. The migration independently bypasses bounded repository behavior with full-wide raw queries.
- Requirement or scope impact: replace the persisted subject and repository APIs, move cumulative folding into the authoritative token-usage boundary, update run/team/statistics readers, add an isolated released-data transition, and decommission append-only paths and full-ledger migration queries.

## Recommendations

1. Repair both released 20260730 token-ledger source-shaping migrations under their existing IDs before introducing or running the new ledger-consolidation migration. This lets a database already marked `FAILED` retry corrected code immediately after upgrading and prevents a still-`NOT_RUN` sibling from blocking a direct/skip-version upgrade.
2. Make the current persisted subject a `TokenUsageRunRecord` (exact final name belongs in design) with a database-enforced unique `run_id`.
3. Treat incoming event DTOs as transient observations. Fold normalized deltas into cumulative numeric/cost fields and keep only compact reconciliation/display/status state necessary for future updates and queries.
4. Do not create a replacement durable per-event audit table in this ticket. Event history is not required by the user's requested model; raw runtime traces remain a separate concern.
5. Approve run-created-period/lifetime-total semantics for Settings statistics. Preserving exact “usage during arbitrary period” requires additional time-bucket/history rows and conflicts with a strict one-row-only token-usage store.
6. Decouple provider-name display backfill from the startup-fatal provider-identity prerequisite. Do not weaken independent secret/provider identity safety.

## Required Upgrade Sequence

1. The newest application version ships corrected implementations for the existing migration IDs `20260730_token_usage_custom_provider_model_value_backfill` and `20260730_token_usage_provider_name_snapshot_backfill`.
2. On a direct/skip-version or previously blocked installation, the migration runner executes or retries those corrected definitions in their existing registry order against the still-present legacy ledger.
3. Both released backfills operate through narrow SQL eligibility, bounded keyset batches, compare-and-set updates, scalar validation, and capped details; neither materializes a full-ledger result.
4. A separate newly identified consolidation migration, with successful-or-warning completion of both repaired source-shaping backfills as prerequisites, then aggregates legacy rows into the one-row-per-run current store and validates them before legacy-table removal.
5. If a source-shaping backfill or consolidation returns a capability-scoped failure after the current schema exists, the legacy source remains intact, the application and new work may start, historical token reads are unavailable, and restoration of pre-existing canonical runs is rejected before provider startup. Newly allocated runs write only the current table; normal runtime never reads or writes legacy rows.
6. On retry, migration code validates that current target `run_id` values are disjoint from legacy `run_id` values, imports the entire legacy aggregate once, validates, and deletes the source in the same transaction. Any overlap or missing required current schema fails before cleanup. If Prisma/current platform schema deployment itself fails, global startup failure is acceptable and a corrected externally installed release retries without a legacy runtime fallback.

## Scope Classification (`Large`)

The change crosses live event ingestion, persisted identity/invariants, cost and component aggregation, GraphQL statistics semantics, released-data transition, migration diagnostics, and application startup availability.

## In-Scope Use Cases

- Fold native AutoByteus per-call token deltas into one standalone/member/delegated agent-run row.
- Fold Claude terminal per-turn token deltas into that same run-owned model.
- Reconcile frequent Codex cumulative snapshots, including restart/resume and more than one snapshot series, without adding a row per notification.
- Read standalone and team-member lifetime summaries directly from cumulative run rows.
- Derive root-team totals by summing concrete member/delegated agent-run rows.
- Show task/team and model statistics under the approved date-range semantics.
- Transition existing event-ledger data into one cumulative row per distinct canonical run with bounded work, forward-only degraded capability gating, ordinary relaunch safety, validation, and legacy-table decommission.
- Correct and isolate `20260730_token_usage_provider_name_snapshot_backfill`.
- Correct the transition-critical sibling `20260730_token_usage_custom_provider_model_value_backfill` under its unchanged ID so it cannot block a large direct/skip-version upgrade before consolidation.

## Out of Scope

- A new durable detailed token event-history/audit table.
- Preserving raw event, raw usage, or raw pricing snapshot JSON in the authoritative run-total table.
- Exact arbitrary-time “usage deltas during period” reporting if the recommended strict-one-row period semantics are approved.
- Changing provider token-source normalization (for example, choosing Claude `modelUsage` over `usage`) beyond what is necessary to fold already-normalized accounting facts.
- Rebuilding TeamRun execution topology inside token usage; TeamRun remains the topology authority.
- Automatically running a full-database `VACUUM` on the critical startup path. Freed pages must be reusable; optional physical-file compaction is a separate maintenance concern.
- Physically dropping the legacy table/model/index contract in the same release. Because Prisma schema migrations run before app-data migrations, the safe current-ticket contract phase is to empty and decommission the legacy table after validated consolidation while retaining its dormant migration-only schema declaration. Physical contract removal requires a separately sequenced future mechanism that cannot destroy data for skip-version upgrades.
- Weakening independent secret-vault or custom-provider identity safety gates.

## Functional Requirements

- **REQ-001 — One authoritative row per run.** The current token-usage persistence store shall contain exactly one authoritative cumulative record for each canonical agent run ID, enforced by a database uniqueness constraint on `run_id` (or an exactly equivalent canonical agent-run identity column).
- **REQ-002 — Uniform run-kind invariant.** `REQ-001` shall apply without identity special cases to standalone, direct team-member, nested team-member, delegated task-agent, and task-team member agent runs. Root team and task metadata remain attributes; they do not replace the concrete agent run key.
- **REQ-003 — Correct update-shape folding.** The persistence owner shall add normalized `per_call`/`per_turn` accounting deltas and shall reconcile `cumulative_snapshot` sources against compact per-run/per-series checkpoints before adding only the non-regressing advancement.
- **REQ-004 — Atomic rapid-update safety.** Concurrent or rapidly scheduled updates for one run shall not lose increments, create duplicate run rows, or let an older cumulative snapshot regress stored lifetime totals. The update and reconciliation checkpoint shall commit atomically.
- **REQ-005 — Bounded deduplication state.** Supported replay of the same cumulative snapshot or immediately repeated update within the current store shall not double count. Cross-schema replay shall be prevented by `REQ-023` gating pre-existing-run restoration before its provider starts while consolidation is incomplete; current runtime shall not inspect legacy facts. Reconciliation bookkeeping shall have enforced entry-count and serialized-size limits: at most eight cumulative-series checkpoints and at most 64 fixed-length idempotency-key digests per run. Series and event identifiers shall be stored as fixed-length digests, not provider-sized raw keys. When a ninth distinct cumulative series is admitted, the least-recently admitted checkpoint shall be evicted deterministically, the incoming series shall establish a baseline without charging its first cumulative snapshot, and a bounded quality flag shall disclose possible undercount; subsequent advancements for the retained series remain independently correct. The encoded checkpoint and idempotency state shall also have hard byte caps and shall never reintroduce one durable wide row per notification.
- **REQ-006 — Cumulative facts retained.** The run row shall retain cumulative token components, cumulative estimated component/total costs, aggregate pricing/currency/status semantics, usage-report count, latest prompt/context values, first/latest observation timestamps, runtime/model/provider display identity, run/team/task display attribution, and quality/missing-dimension state required by supported APIs.
- **REQ-007 — No repeated raw audit payloads.** The authoritative run row shall not retain per-notification `raw_event_json`, `raw_usage_json`, or `pricing_snapshot_json`. Only compact source checkpoint or summary state demonstrably required for correct folding/querying may be persisted.
- **REQ-008 — Direct run summary.** `getAgentRunTokenUsageSummary(runId)` shall return the same lifetime token/cost meaning from the single cumulative run row without loading or reconstructing an event list.
- **REQ-009 — Team/member aggregation.** Member summary shall select the exact root/run record. Root-team summary shall sum each included concrete agent-run record once, including delegated/task-created runs, and shall not store a separate duplicative team-total row.
- **REQ-010 — Public summary stability.** Current GraphQL field names and live websocket token-update behavior shall remain stable unless explicitly changed here. Cache read/write, standard/gross input, output/reasoning, billable output, nullable/mixed cost, unit-price explanation, usage-report count, current prompt, and context-window meanings shall remain truthful.
- **REQ-011 — Statistics period semantics.** Settings > Token Statistics date controls shall select runs by `run_created_at`, falling back to `first_usage_observed_at` with the existing truthful fallback label. Task/team and runtime/model results shall show the selected runs' lifetime cumulative totals. The UI copy shall no longer claim “Usage during period.” No separate time-rollup/event-history table is in scope.
- **REQ-012 — Historical migration repaired in place.** The corrected provider-name implementation shall remain registered as `20260730_token_usage_provider_name_snapshot_backfill`, so existing `NOT_RUN`, stale `RUNNING`, or `FAILED` records retry the corrected code after application upgrade. It shall select only null/blank-name AutoByteus candidates and only the columns required to classify/update them. Non-AutoByteus null-name rows shall be excluded in SQL.
- **REQ-013 — Provider backfill batching.** The provider-name backfill shall use bounded keyset batches and compare-and-set updates. It shall never materialize the full ledger, full candidate set, or full before/after row snapshots in Node/Prisma.
- **REQ-014 — Bounded validation and diagnostics.** Migration invariants shall use scalar/database-enforced checks appropriate to the actual single-column update. Per-reason counts and example/failure details shall be capped; logs and `summary_json` shall not grow linearly with ledger row count.
- **REQ-015 — Provider backfill retry/isolation.** A normal migration failure or an attempt that ends before completion shall leave the migration idempotently retryable through the existing runner with truthful status/error evidence. All abrupt-termination labels share this one ordinary relaunch disposition; no cause-specific recovery machinery is required. A failed provider-name display backfill alone shall not terminate global application startup.
- **REQ-016 — Dependency decoupling.** The nonessential provider-name snapshot backfill shall not remain a hard prerequisite of the independently safety-critical readable custom-provider identity migration. Independent provider/secret safety failures may retain their current gate; statistics labels fall back truthfully when the backfill is pending.
- **REQ-017 — Separate legacy consolidation required.** A new migration with its own later ID shall transform existing `token_usage_ledger_events` data into the current one-row-per-run store. Successful or warning completion of both repaired 20260730 token source-shaping steps shall be its prerequisite so a failed historical retry leaves the legacy source intact for another retry. Direct use without transformation is insufficient because duplicate rows violate the target invariant and contain delta facts that cannot be replaced by selecting only the latest row.
- **REQ-018 — Aggregate-preserving transition.** For every migrated run, pre-transition lifetime accounting-token components and estimated component/total costs shall equal the post-transition cumulative facts, subject only to already-null/unknown semantics. The transition shall also preserve root/team/task attribution, display fields, report count, observation bounds, latest prompt/context values, and truthful mixed/unknown identity or pricing state.
- **REQ-019 — Bounded relaunch-safe transition.** Consolidation shall process bounded source/result sets inside one SQLite transaction and shall not add a bespoke per-row journal. If the transaction does not commit, standard SQLite rollback shall leave the source intact and permit the existing runner or a corrected later release to retry; no shutdown-, power-, kernel-, device-, or filesystem-specific branch is required. Current rows admitted after a capability-scoped failure shall belong only to newly allocated run IDs. A later retry shall validate zero `run_id` intersection with legacy rows before importing the complete legacy aggregate once; any intersection fails before source cleanup.
- **REQ-020 — Validation before decommission.** The legacy table shall be absent from all normal runtime readers and writers immediately in the target code. Only migration-owned code may query or decode it. After migration validation proves distinct-run coverage and aggregate preservation, the same transaction shall delete all supported legacy event rows so the table is empty and its pages are reusable. The dormant physical table/model declaration remains migration-only because normal Prisma schema migrations execute before app-data migration; physical contract removal is deferred. Failure shall leave recoverable source evidence and shall not present partial historical statistics as complete.
- **REQ-021 — Reusable space, nonblocking physical compaction.** After successful validation the legacy pages shall be reusable by SQLite. Any physical file shrink/VACUUM mechanism shall be explicit, disk-space-aware, retryable, and outside the global startup critical path.
- **REQ-022 — Sibling source-shaping migration repaired in place.** `20260730_token_usage_custom_provider_model_value_backfill` shall remain registered under that unchanged ID but replace both whole-ledger reads, whole-ledger invariant arrays, and linear skip details with SQL-filtered potential candidates, a minimal projection, bounded keyset batches, compare-and-set `model_value` updates, scalar row-count/update invariants, per-reason counters, and capped examples. An attempt that ends before completion and the later same-ID runner retry shall remain idempotent; all non-`model_value` fields remain protected by the update statement rather than copied into Node snapshots.
- **REQ-023 — Forward-only degraded transition gate.** When consolidation is not successful, stored historical token reads and restoration/continuation of any pre-existing canonical agent run shall be unavailable before provider startup. The application, settings, unrelated capabilities, and newly allocated runs remain available when the current schema is valid; new runs write only `token_usage_run_records`. Admission is based on migration readiness and the supported restore/new-run lifecycle, not on runtime queries against legacy rows. The migration retry shall validate zero legacy/current `run_id` intersection before import. No runtime legacy reader, legacy writer, checkpoint seed, overlap marker, dual read/write, or compatibility fallback is permitted.
- **REQ-024 — Deterministic, proportionate migration boundary.** All ticket migrations and migration-adjacent fallbacks shall comply with [`data-migration-conventions.md`](./data-migration-conventions.md): treat known supported released source shapes to one fixed current target as a deterministic transformation; assume one writer, a stable normal attempt, sufficient permissions, and normal SQLite/filesystem behavior; use the existing runner and real transaction semantics; and add recovery/lifecycle machinery only for an independently supported reachable product path. Quit, kill, shutdown, and power-off are one incomplete-attempt category, not separate product requirements. Hostile tampering, theft, arbitrary corruption, kernel/device/syscall failure, compromised processes, and adversarial writers shall not drive in-scope code or tests absent a separately approved contract. `REQ-023` is the smallest current-only capability gate that makes cross-schema replay Not Reachable.
- **REQ-025 — Classified failure and corrected-release recovery.** Every migration outcome shall record truthful bounded evidence and be classified against the schema, current-format data, and integrity/safety invariants actually required by the forward-only current application. When the current platform/schema is valid and only token history or provider display is unavailable, bootstrap/health, settings, unrelated capabilities, and new runs shall remain available while affected reads/restores are gated. When required current tables/columns/constraints, required current file attributes, or another core current invariant are unavailable, global startup failure is acceptable and no legacy runtime fallback shall be added. `SUCCEEDED_WITH_WARNINGS` is permitted only when the required current target is independently validated and any remaining old column/table, obsolete structured-file attribute, superseded file, stale legacy value, or other cleanup residue is bounded, unreachable from current code, and not prohibited by an independent security/privacy/retention/storage contract. It shall not hide an incomplete required target, an observable source/target ambiguity, or cleanup failure that rolled back the target transaction. A user may recover from a critical state by externally downloading/installing a corrected release, whose schema/app-data migration retries without requiring fabricated success or manual production-data surgery.
- **REQ-026 — Forward-only current source and migration-only legacy ownership.** Current domain, service, repository, provider, GraphQL, and runtime code shall understand only the current schema and current token-usage model. It shall contain no old-table/old-column decoder, legacy JSON parser, dual reader/writer, read-old-if-new-missing branch, optional-schema compatibility branch, runtime legacy query adapter, or compatibility wrapper. All supported released legacy row types, queries, classification, and old-to-current transformation shall remain inside registered migration files/boundaries retained for direct and skip-version upgrades. Failure disposition shall gate a capability or startup; it shall never reactivate the old runtime path.

## Acceptance Criteria

- **AC-001:** Three Codex cumulative snapshots for one run and one series (for example totals 100, 150, 210) leave exactly one run row whose lifetime advancement is correct and whose reconciliation checkpoint is 210; no raw snapshot rows are added.
- **AC-002:** Re-delivery of the same Codex cumulative snapshot within the current store does not change cumulative totals or report count, and a later regressing snapshot does not subtract usage and records a truthful quality outcome. A pre-existing legacy run cannot reach the current writer while consolidation is incomplete because restore admission is gated by `REQ-023`.
- **AC-003:** Two native per-call deltas and two Claude per-turn deltas for their respective runs leave one row per run with component/cost totals equal to the sum of normalized deltas and the correct report counts.
- **AC-004:** Rapid updates scheduled before earlier persistence finishes cannot overwrite each other; after drain, row count remains one and totals include every admitted advancement once. If two admitted observations share `observed_at`, latest prompt/context/identity fields follow their monotonic durable admission order rather than lexical event ID; the event ID is used only for identity/deduplication.
- **AC-005:** A run with up to eight supported cumulative series retains one authoritative run row while independently checkpointing each series and adding each series' valid advancements. Admission of a ninth series deterministically evicts the least-recent checkpoint, keeps both checkpoint count and encoded bytes within their named caps, baselines the newcomer without charging its first cumulative snapshot, records a bounded quality flag, and correctly accounts its later advancement. Reappearance of an evicted series follows the same no-overcount baseline rule.
- **AC-006:** One standalone run, two direct team members, one nested member, and one delegated task-agent each produce one row keyed by their exact agent run ID; the root team result equals the sum of its concrete member/delegated rows with no extra team-total row.
- **AC-007:** Agent, team, and member GraphQL summaries preserve component token totals, costs/statuses, usage report count, latest prompt/context values, and current identity/display fields without an event-list query. Inside migration code, legacy equal-time facts use numeric ledger `id` as their admission ordinal; current runtime facts use committed per-run fold revision. Migration rejects a same-`run_id` legacy/current overlap instead of applying a cross-schema latest-wins rule.
- **AC-008:** A run whose historical events contain more than one price tuple preserves exact summed costs and reports truthful single/mixed unit-price/status semantics rather than using only the latest tuple.
- **AC-009:** Live `TOKEN_USAGE_UPDATED` websocket behavior remains available for the Token Meter even though durable persistence folds updates into one row.
- **AC-010:** Under the recommended `REQ-011`, selecting a date range includes runs created in that range (or first-observed fallback) and displays lifetime totals; visible copy does not claim that only usage deltas observed in the range are shown.
- **AC-011:** Given an installation whose record for `20260730_token_usage_provider_name_snapshot_backfill` is `FAILED` after the released oversized query, installing the corrected version retries that same migration ID. Against a fixture with ~147k null-name Codex rows and 141 eligible AutoByteus rows, it reads/processes only the eligible rows in configured batches, updates the 141 recoverable names, and never issues `SELECT *` or a full-ledger row snapshot.
- **AC-012:** Provider-name migration row-count/non-target safety is established with bounded scalar/database checks; skip/failure logs contain aggregate reason counts and capped examples rather than ~147k detail entries.
- **AC-013:** Given an earlier normal attempt committed one or more bounded provider-name batches but did not complete the migration, a later startup through the existing runner completes the remaining candidates without reverting populated names or modifying non-target columns; Quit, kill, shutdown, and power-off do not receive separate recovery branches or test matrices.
- **AC-014:** If the provider-name backfill returns `FAILED`, its migration record remains retryable and the embedded HTTP server can become healthy. No normal runtime legacy lookup is added; affected historical token labels/reads remain unavailable or use only an already-current truthful fallback after successful consolidation.
- **AC-015:** A provider-name backfill failure does not by itself block the readable custom-provider identity migration. A separately failing provider-identity/secret safety condition is still reported/gated under its own status.
- **AC-016:** Upgrade sequencing executes both corrected 20260730 source-shaping definitions against the legacy table before the separately identified consolidation step is allowed to decommission that table. A large released-style ledger then migrates to exactly `COUNT(DISTINCT run_id)` current run rows. For every run and overall, token component sums, estimated component/total costs, and usage report counts match the legacy aggregate.
- **AC-017:** Given consolidation does not commit after partial in-transaction processing, standard SQLite rollback leaves attempted target imports unapplied and the legacy source intact. After the normal returned failure, newly allocated runs can create current rows while pre-existing-run restoration remains gated. Retry proves the current and legacy `run_id` sets are disjoint, imports legacy facts exactly once, and rejects any intersection before deleting source.
- **AC-018:** Target runtime repository/query/service code has no append/list-event API or dependency on the legacy event table. Full-event mappers, raw JSON persistence, event-summary reconstruction, runtime legacy identity/checkpoint adapters, protocol markers, dual reads/writes, and compatibility wrappers are absent. Any retained legacy Prisma/table declaration and decoder/query code are explicitly migration-only, empty/dormant after success, and never called by current runtime.
- **AC-019:** Capability-scoped consolidation failure does not make unrelated desktop functionality or newly allocated runs unavailable and does not expose partial historical statistics as complete; migration status/logs identify retry state, and attempting to restore a pre-existing run returns a clear migration-incomplete capability error before provider startup.
- **AC-020:** After successful validation, the legacy event table contains zero rows and its former data/index pages appear on SQLite's reusable freelist. The dormant empty table/index contract is not used by runtime code, and a full `VACUUM` or unsafe same-release Prisma drop is not required on global startup.
- **AC-021:** Against a synthetic released-style ledger with approximately 147k unrelated rows plus eligible, malformed, conflicting, and concurrently changed custom-provider composite model-value candidates, `20260730_token_usage_custom_provider_model_value_backfill` transfers only SQL-filtered potential candidates in batches of at most 250 and never issues a full-ledger row query. When an earlier attempt does not complete, a later normal runner retry updates each eligible value once, preserves total row count and every non-`model_value` column through statement-level scope, and emits scalar counts plus capped examples rather than one detail per unrelated/skipped row.
- **AC-022:** Synthetic sequence: persist direct and cumulative facts for run `R-old` in legacy; make consolidation return `FAILED`; verify application health; attempt to restore `R-old` and observe rejection before provider/token persistence starts; create `R-new` and persist current usage only in its single current row; retry consolidation. Migration proves legacy/current run IDs are disjoint, imports `R-old` exactly once, preserves `R-new` unchanged, and deletes legacy source only after per-run/global validation. An injected current row for `R-old` makes retry fail before cleanup. No overlap guard, legacy runtime query, checkpoint seed, or protocol marker exists.
- **AC-023:** Migration implementation and coverage use one deterministic source-to-target path, bounded data/evidence, existing runner retry, and SQLite commit/rollback. They contain no Quit-, kill-, shutdown-, power-, kernel-, device-, filesystem-corruption-, tampering-, theft-, compromised-process-, or adversarial-writer-specific journal, backup, state machine, fallback, branch, or dedicated test matrix. Any additional recovery behavior identifies and traces an independent supported trigger before implementation. The capability gate in `AC-022` makes cross-schema replay Not Reachable without compatibility machinery.
- **AC-024:** Failure-classification fixtures prove the current-application contract. With valid current schema plus failed provider-display/consolidation app-data migration, the application reaches health and permits settings, unaffected features, and new runs while affected history/restores are gated. With a missing required current table/column/constraint, a missing or invalid required current file attribute, or another unavailable core current invariant, startup fails with bounded actionable evidence and never calls legacy runtime code; after installing a corrected release externally, migration succeeds or retries. Database and structured-file fixtures whose required current target is complete and validated but whose old column/table, obsolete attribute, or superseded file remains inert produce bounded `SUCCEEDED_WITH_WARNINGS` and start on the current format only. The same residue is not a warning when current discovery observes both shapes, when cleanup rollback removed the target, or when an independent security/privacy/retention/storage contract requires removal. Partial/unvalidated data is never presented as complete, and no fabricated migration-record success is required.
- **AC-025:** Static dependency review and runtime tests show that every reference to legacy token table columns, legacy row types, and old-to-current token decoding exists only under registered app-data migration boundaries. Current token domain/services/repositories/providers, run activation, GraphQL, and server runtime contain no legacy decoder/query adapter, dual path, or read-old fallback. In incomplete-consolidation state they consult only current migration readiness to gate history/restore; in success state they execute the single current path.

## Constraints / Dependencies

- SQLite is the production store; local evidence has `auto_vacuum=NONE`, so deleting/dropping rows does not automatically shrink the physical file even though pages become reusable.
- Prisma/NAPI and V8/Node string-result limits prohibit unbounded wide result materialization.
- Current event enrichment is serialized per run, but persistence is scheduled asynchronously; target persistence must supply its own atomicity/ordering guarantee.
- The target must handle delta and cumulative source shapes; “replace row with latest payload” is not correct for native/Claude deltas or for legacy aggregate preservation.
- TeamRun execution tree remains authoritative for topology; token usage stores only root/run/task/display attribution needed by statistics.
- The app-data migration registry/runner already records statuses/retries and is the required relaunch mechanism; this ticket shall not add a durable per-batch recovery journal. Capability-scoped degradation is enforced by current readiness/restore admission, not by legacy reads. Existing-run restoration reuses canonical run/provider identity and is therefore unavailable until consolidation succeeds.
- Canonical IDs allocated for genuinely new runs are globally unique. Consolidation migration code must validate that current run IDs admitted during degradation do not intersect legacy run IDs; current runtime must not inspect legacy identity/event indexes.
- Prisma schema deployment precedes app-data migration. Absence of the required current table/columns is a platform-critical failure and shall not activate an old-schema runtime path.

## Persisted Data Outcome (When Applicable)

- Stored subject / location: current `autobyteus-server-ts` SQLite table `token_usage_ledger_events`, Prisma model `TokenUsageLedgerEvent`, and new current-schema `token_usage_run_records` / `TokenUsageRunRecord` target.
- Required outcome: `Migration Required`.
- Existing data to preserve, discard/rebuild, transform, or quarantine:
  - preserve/transform: lifetime normalized token components, component/total estimated costs, currency/cost/missing-dimension semantics, usage-report counts, root/run/task/display attribution, observation bounds/order, latest prompt/context fields, model/runtime/provider identity, and at most the eight latest compact cumulative-source checkpoint facts;
  - intentionally discard after validation: superseded event rows, raw event/usage/pricing JSON, and per-event IDs/timestamps not required for the preserved aggregates;
  - preserve until successful validation: the populated legacy event table plus disjoint current rows for newly allocated runs; a failed transaction rolls back without a custom per-row journal;
  - retain dormant after success: the empty legacy physical table/model/index contract required by safe Prisma-before-app-data ordering; all legacy decoder/query code remains migration-only.
- Unacceptable data loss or corruption: changed lifetime run/team totals, legacy/current `run_id` overlap accepted, restored-run replay admitted during incomplete consolidation, false single-price/model labels for mixed data, lost canonical run/root attribution, or marking a partial transition complete.
- Relevant availability, maintenance-window, or rollout constraints: no query/result proportional to hundreds of MiB; no global app fatal caused solely by reporting metadata; full physical database compaction cannot assume 2x free disk and cannot be a mandatory startup action.
- Related requirement and acceptance-criteria IDs: REQ-001–REQ-026; AC-001–AC-025.

## Assumptions

- Deterministic transformation, forward-only current source, migration-only legacy ownership, failure classification, operating prerequisites, abrupt-termination equivalence, unsupported premises, and the product-reachability gate are authoritative in [`data-migration-conventions.md`](./data-migration-conventions.md); they are not redefined per migration.
- The user intends event-level token history to be removed rather than copied into another durable event table.
- A canonical agent run is the lifetime accounting subject even when it belongs to a team/delegation; no separate team-total persistence row is wanted.
- The approved period behavior is run-created-in-range with lifetime totals; no time-rollup store is required.
- Exact source-normalization and event/idempotency identity rules already established for native, Codex, and Claude remain authoritative; this ticket changes durable folding/transition admission, not provider interpretation.

## Risks / Open Questions

- Exact arbitrary-period usage is intentionally no longer available under the approved strict one-row model; UI/API wording must remain truthful about run-selection semantics.
- More than eight cumulative series is outside the exact supported checkpoint envelope; the enforced overflow path prevents overcount, records a bounded quality flag, and may undercount the first interval after eviction/reappearance. Multiple pricing tuples remain exact in aggregate finite-state summaries.
- Normal provider/run behavior currently appears single-runtime/model per run in the representative database, but migration must not silently mislabel conflicting legacy identities.
- Freed SQLite pages will not shrink the file under `auto_vacuum=NONE`; safe physical compaction needs explicit disk-aware maintenance if desired.
- A capability-scoped incomplete-consolidation state makes historical statistics and pre-existing-run restoration unavailable; newly allocated runs remain current-only. This intentionally restricts continuation rather than retaining runtime legacy compatibility. If required current schema is absent, startup failure is acceptable until an externally installed corrected release repairs it.

## Requirement-To-Use-Case Coverage

| Use Case | Requirement IDs |
| --- | --- |
| Persist standalone/member/delegated updates | REQ-001–REQ-007 |
| Reconcile Codex cumulative sources | REQ-003–REQ-005 |
| Read run/member/team summaries | REQ-006, REQ-008–REQ-010 |
| Settings task/model statistics | REQ-006, REQ-009–REQ-011 |
| Repair provider-name backfill/startup blast radius | REQ-012–REQ-016 |
| Consolidate released ledgers | REQ-017–REQ-021 |
| Apply deterministic migration assumptions and the product-reachability gate | REQ-024 |
| Classify capability-scoped versus critical migration failure and corrected-release recovery | REQ-025 |
| Keep current source forward-only and legacy knowledge migration-only | REQ-026 |
| Bound the source-shaping predecessor on direct/skip-version upgrades | REQ-022 |
| Gate pre-existing-run restoration and historical reads after failed consolidation | REQ-023 |

## Acceptance-Criteria-To-Scenario Intent

| Scenario Intent | Acceptance-Criteria IDs |
| --- | --- |
| Cumulative/delta correctness and concurrency | AC-001–AC-005 |
| All run kinds, aggregation, GraphQL/live preservation | AC-006–AC-010 |
| Large provider-backfill safety, retry, availability | AC-011–AC-015 |
| Large legacy consolidation, transaction rollback/relaunch, cleanup | AC-016–AC-020 |
| Large sibling model-value backfill safety/retry | AC-021 |
| Forward-only degraded gate, disjoint new run, and exact retry import | AC-022 |
| Proportionate migration recovery and unsupported-premise exclusion | AC-023 |
| Current-contract warnings versus capability-scoped/critical failure and corrected-release retry | AC-024 |
| Migration-only legacy dependency enforcement | AC-025 |

## Approval Status

Approved by the user on 2026-08-19. The strict one-row-per-canonical-run model and run-created/lifetime-total semantics remain unchanged. Current application source is forward-only and all legacy schema/data knowledge stays inside migration code. This supersedes the `SR-003` runtime overlap-guard continuation mechanic: while consolidation is incomplete, historical token reads and pre-existing-run restoration are gated, new runs remain current-only, and migration retry validates run-ID disjointness. Global startup may fail when required current schema/core platform invariants are unavailable; recovery may occur through an externally installed corrected release without a backward-compatible runtime path. The user further requires the convention to judge the final persisted state by facts the current application and independent integrity/safety contracts actually need: validated current data plus inert nonrequired legacy cleanup residue may be `SUCCEEDED_WITH_WARNINGS`, whereas missing current facts, observable old/new ambiguity, rollback of the target, or required removal may not. These decisions are captured in `REQ-023`–`REQ-026` and `AC-022`–`AC-025`, and the durable convention/README documentation remains required.
