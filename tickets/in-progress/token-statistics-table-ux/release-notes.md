## What's New
- Settings > Token Statistics task rows now use a cleaner nine-column table that removes redundant standalone Type and Status columns.

## Improvements
- Sortable task-table headers now show persistent sort indicators, including neutral glyphs for inactive sortable columns and an active direction indicator for the current sort.
- Cost breakdowns now open from one always-visible Details control in the Total Cost cell, instead of hidden hover-only cost-cell actions.
- Non-complete pricing states remain visible inline near Total Cost and in the expanded breakdown, while normal complete estimates no longer repeat in every main row.

## Fixes
- Input Cost and Output Cost now render as plain values, avoiding duplicate controls that all opened the same row details.
- Expanded member rows stay attached to their parent after Total Cost sorting with the reduced table layout.
