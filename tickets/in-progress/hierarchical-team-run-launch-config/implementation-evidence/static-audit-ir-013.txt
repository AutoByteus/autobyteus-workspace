# IR-013 static audit
Source commit: 554e1baaa2e455ca8c73e0414a100e1c8e24829a

## Unsupported multiline scope removed cleanly
PASS: the five SR-015 paths contain none of: `canTextInputRepresentExactly`, `ordinary_prompt`, `multiline_prompt`, `line_feed_prompt`, `carriage_return_prompt`, `removed_parameter`, `legacy_shape`, `whitespace-pre-wrap`.
PASS: `historicalModelConfigFields.ts` has no CR/LF-specific predicate or classification branch.
PASS: `HistoricalModelConfigFallback.vue` retains its compact residual layout without multiline-only whitespace styling.
PASS: the affected direct/mounted suites contain no ordinary/LF/CR synthetic fixture or assertion and no renamed equivalent.

## Producer-backed retained fixture vocabulary
PASS: direct classifier, root/nested-Team consumer, and Agent consumer coverage all use stale `reasoning_effort` plus removed optional `service_tier=fast`.
PASS: retained coverage still asserts stable residual ordering, one occurrence per stored key, and zero mutation.

## Stored authoring boundary preserved
PASS: `autobyteus-web/types/agent/StoredTeamRunFormModel.ts` and `autobyteus-web/services/teamExecution/storedTeamRunFormModel.ts` contain no imports/references to `TeamLaunchDraft`, `WorkspaceSelectionState`, `TeamWorkspaceOperationState`, `teamRunConfigStore`.
PASS: the stored type/projector fabricate none of the prior authoring selection/operation/catalog/null-override sentinels.

## Changed production source effective non-empty lines
- 77: `autobyteus-web/utils/historicalModelConfigFields.ts`
- 41: `autobyteus-web/components/workspace/config/HistoricalModelConfigFallback.vue`
PASS: both changed production files remain below 500 effective non-empty lines, and this cleanup reduces rather than grows their responsibilities.

## Source commit whitespace
PASS: `git diff --check 003413b05..HEAD` for all five SR-015 source/test paths.
