# AD001 diagnostic scope and preparation disposition

- `attachment-probe.cjs`: exit 0. Actual unchanged current first-submission helper, UserMessage SFC and opener; installed Vue 3.5.28; synthetic reactive context/jsdom. History, presentation-effect and peripheral stores controlled. Current raw handle opens draft even after underlying record becomes final and component force-update. Diagnostic canonical-proxy handle control opens final. Not actual Pinia/AgentRun preparation, server finalization, provider, network, original hosted build or acceptance. window.open is captured, not executed. The preexisting backend URL default in output is only URL normalization, no request to that service occurred.
- `navigation-probe.cjs`: final exit 0. Actual current sidebar selection action, inspection store action/coordinator; installed Vue memory router; projection promise, Team view and store dependencies controlled; AppLeftPanel event body mirrored. Pending Team click → explicit Org choice → late Team projection completion → old Team selected plus bare Workspace route. This establishes the missing intent guard in a supported selection path, NOT the timing/cause of the original API38 publication incident. No actual task/provider execution.
- Navigation initial attempt exit 1: vue-router not directly resolvable from old web package. Initial script/log retained. A sed replacement then failed because the package name contains the chosen delimiter, followed by the same import error; no assertion ran in either attempt. Corrected only the explicit installed-package module path using Python; final assertions unchanged.
- Read-only discovery commands also encountered nonexistent candidate source names/globs; subsequent exact file discovery corrected those paths. These are investigation/preparation misses, not application test failures.
- Probes loaded dependencies read-only from the old finalized worktree, source from this new worktree. No install/build, source/test edit, old-ticket write, hosted process/tab, live provider/root, runtime-data or migration action. These are architecture diagnostics, not durable regression tests. Durable production-composition tests and ordinary frontend before/after evidence remain downstream obligations.
- Initial staged-document whitespace check flagged nine generated blank excerpt line labels ending in a space. Removed only label trailing whitespace, not original Git objects or source; final staged whitespace check rerun. This is artifact formatting, not a product-test outcome.
