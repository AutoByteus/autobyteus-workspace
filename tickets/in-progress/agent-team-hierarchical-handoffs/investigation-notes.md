# AgentTeam Hierarchical Communication And Handoffs — Investigation Notes

## Investigation Status

- Bootstrap Status: `Complete`
- Current Status: `SR-028 corrects ARCH-REV-020 DR-013/DR-014; complete architecture re-review required; implementation, API/E2E, and delivery paused`
- Investigation Goal: Preserve the integrated hierarchical addressing, handoff, task-delegation, and send-delivery behavior while making `recipient_address` the truthful shared selector, `AgentTeamAddress` the only resolved logical placement identity, Team collaboration tools intrinsic, the system instruction explicitly filesystem-like and completion-driven, and `get_handoff_rules` an action-only `{when,recipient_address}` projection, while retaining one self-contained rooted TeamRun execution snapshot throughout runtime, persistence/history, APIs, SDKs, integrations, storage, and frontend state.
- Scope Classification: `Large`
- Scope Classification Rationale: The clean cut crosses Team definition mounting, TeamRun snapshots and child/task execution, runtime registries/contexts, communication, task delegation, conversation/event/token identity, restoration, file and database persistence, startup migration policy, GraphQL/REST/WebSocket contracts, project-owned SDKs and integrations, physical storage boundaries, production web state, and broad durable coverage. It intentionally excludes native AgentOrg, live topology mutation, cross-process routing, external pure-text packages, opaque provider-trace rewriting, physical memory-directory relocation, and unrelated product expansion.
- Scope Summary: AutoByteus project code and released framework-owned durable data only. SR-028 is a bounded authority correction: the supported Claude one-string-query interrupt remains `Options.abortController` -> `AbortController.abort()` -> active query settlement/established reference cleanup -> canonical `TURN_INTERRUPTED` -> AgentRun FIFO drain. It does not adopt `Query.interrupt()`, streaming input, priority, or receipt machinery. SR-028 also repairs the revision and live-contract navigation defects identified by ARCH-REV-020. ARCH-REV-018 passed SR-024's cumulative rooted identity, collaboration, migration/token, direct V5 application, Team stream/status/frontend, task, storage, run-owned segment lifecycle, exact Codex ingress, and complete consumer cut. SR-025 changed only exact prompt copy. API-REV-039/CRR-086 reopened one bounded runtime owner: all ordinary inputs already converge on exact AgentRun, but active-input admission was split across AgentRun lifecycle, Codex, Claude, AutoByteus, and the command registry. SR-026 introduces one non-persisted AgentRun FIFO and explicit provider start/append mechanics, and ARCH-REV-019 passed that complete cumulative design. SR-027 adds the user-authorized exact latest compatible Claude/Anthropic/MCP dependency cut, confirms by official API/types and isolated live probe that Claude still lacks exact active-turn append, and adapts only required first-turn intrinsic MCP loading. It changes no address, task eligibility, durable identity, event, migration, application, or compatibility boundary.
- Primary Questions Resolved: Which identity fields describe the same logical placement; which run/storage/configuration identities remain independent facts; how reusable nested definitions mount deterministically; how persistent and task executions share logical addresses without collision; which persisted stores require migration or rebuild; where migration must block startup; how API/SDK/frontend contracts converge; which component owns exact Team wire admission; which frontend owner governs live/restore/focus/presentation/cleanup; which common owner validates/enriches provider segment lifecycles; which post-pipeline processors/listeners must consume the canonical shape; which provider owns exact segment-turn attribution; whether any supported product path produces a turnless Agent segment after that ingress; which one owner defines acceptance, FIFO, active-turn append/next-turn behavior, and pending-input interruption/termination across providers; and whether the current Claude Agent SDK exposes an official exact-active-turn append mechanic or only provider-owned interruption/later-turn queueing, including its concrete upgrade impacts.

## Request Context

The user split the earlier native AgentOrg effort into two independent tickets:

1. **Ticket 1 (this package):** make AgentTeam addressing and handoffs truthful, consistent, and reusable.
2. **Ticket 2 (later):** add native AgentOrg definition/run ownership and explicit-refresh dynamic behavior on top of Ticket 1.

The user explicitly decided:

- use filesystem-like `/` and `./` paths consistently for standalone and nested AgentTeams;
- stop faking a child Team coordinator as a parent Team member;
- retain handoff JSON endpoints named `from` and `to` and allow natural-language `rules[]`;
- make handoff retrieval outgoing/sender-centric only;
- provide `get_handoff_rules` rather than repeatedly injecting the whole rule set;
- target an Agent directly or target an AgentTeam through its configured coordinator;
- keep handoffs as Agent/LLM guidance rather than hard-coded predicates;
- keep AgentTeam static per run; dynamic updates belong to AgentOrg;
- use no automatic file-system watching; and
- exclude repository-owned external/pure-text Agent package edits from Ticket 1.

During SR-004 clarification, the user explicitly rejected a separate task target naming system: task delegation selects a recipient just as message delivery does, so `delegate_task` must use the same canonical `/...` and `./...` logical addressing model as `send_message_to`. Different execution semantics may follow resolution, but there must not be a second flat task address authority.

During final user verification after SR-005 implementation and downstream delivery preparation, the user further clarified that mounting the Team definition graph deterministically decides each placement's one canonical address. `memberPath`, immediate-Team path/address, route key, owner Team, and local name are derived views and must not be duplicated as independent shared identity. The user requested another design-principles-led simplification round and initially instructed the solution designer to stop for user review. The user subsequently approved SR-006 and authorized architecture re-review, while deferring broader whole-TeamRun execution-identity normalization until the focused contraction was integrated.

After the SR-006 implementation passed architecture, source, API/E2E, and delivery re-entry and reached integrated checkpoint `66b02ae...`, the user explicitly reopened that deferred decision in this same ticket. The user prefers a larger clean refactor—including structured history, metadata, SDKs, and frontend—over retaining parallel identity merely to avoid migration. The user asked the solution designer to complete the revised requirements and design, but not send the package to a reviewer until the user has reviewed it.

During review of the first comprehensive proposal, the user challenged the normalized `topology + launch profiles + bindings` JSON because it separated facts that are naturally inspected and restored together. Reinspection confirmed that current metadata already keeps genuine runtime/model/tool/skill/workspace/application/platform facts locally on each Agent, reconstructs `workspaceId` from `workspaceRootPath`, and derives `memoryDir` from storage ownership. The user and solution designer therefore converged on SR-008: retain one recursive TeamRun aggregate, remove only parallel logical name/path/route and ambiguous run identities, use native `agent_team` and `coordinatorAddress` terminology, and derive indexes rather than persist joins.

During the subsequent public-tool naming review, the user observed that “send a message to a recipient address” is more natural than `recipient_name` and better expresses the domain intent than `recipient_path`. The distinction is now explicit: `/...` and `./...` are filesystem-like **address grammar**; the public field is `recipient_address`; the raw request value is `RecipientAddressExpression`; and the resolved absolute node identity is `AgentTeamAddress`. This is a current-contract rename, not a persisted TeamRun schema change.

During the handoff-tool and system-instruction review, the user rejected the generic `{accepted,code,message,result}` rule-query response as internal machinery that does not help the acting Agent. The user defined the useful decision as exactly two facts—under what condition to hand off and which canonical recipient address to use—and approved `{handoffs:[{when,recipient_address}]}`. The user also required every Team Agent to call the tool after finishing work, with an explicit filesystem-like logical-address explanation and examples in the system prompt. This implies intrinsic Team runtime exposure of both retrieval and delivery tools; configuration gating cannot remain an independent prerequisite for a mandatory protocol.

After SR-013 was sent for architecture re-review, the user refined only the literal Agent-facing copy. The user rejected “before completing your work, or before stopping because you are blocked” as lifecycle jargon and selected a natural explanation-first block: first explain filesystem-like logical addresses and the Agent's own address, then say, “When you finish your work or are blocked, call `get_handoff_rules` to check your configured handoff rules.” The user requested that exact prose in a dedicated file for direct implementation use and explicitly classified it as pure text with no architecture impact.

After approving that protocol, the user added a mandatory downstream test requirement and authorized architecture review once it was documented: create an example AgentTeam from the imported nested-classroom package; run it under AutoByteus, Codex App Server, and Claude Agent SDK; use `gpt-5.6-luna` for AutoByteus and `gpt-5.6-luna` with medium reasoning for Codex; and allow credentials from `$HOME/.autobyteus/server-data/.env` to be imported with the repository pnpm command. SR-011 treats these as live API/E2E requirements, not as solution-design execution.

ARCH-REV-006 then failed only DR-004 as `Design Impact`. The reviewer confirmed the rooted aggregate, execution/address ownership, migration, handoff protocol, providers, storage, and live contract, but demonstrated that current application catalog/open/launch treated backend-definition/frontend-SDK V4 as current. Because those V4 SDK types carry fields the clean cut removes, SR-012 added an explicit V5 semantic boundary and atomic artifact sequence. The later user decision keeps that forward V5 cut while explicitly removing application migration and special old-bundle compatibility/quarantine machinery because the feature has no supported users.

ARCH-REV-007 passed SR-012 and implementation proceeded. During API/E2E migration work, CRR-022 classified CR-F-011 as `Design Impact`: the approved migration rule incorrectly required legacy `memberName` to equal the route/path basename, even though the real maintained historical fixture stores `Program Manager` / `program_manager` and `QA Specialist` / `qa_specialist`. The review also proved a supported upgrade where `20260517_team_run_metadata_member_tree` is already recorded `SUCCEEDED` or `SUCCEEDED_WITH_WARNINGS`; `runPending()` skips that ID, so changing its code cannot normalize its prior output. The user reviewed SR-013, confirmed that the coordinator-required operational AgentTeam invariant remains valid and that a coordinatorless organizational container belongs to the later AgentOrg scope, and explicitly approved architecture re-review without further solution change.

ARCH-REV-008 passed the complete SR-013 package. IR-014 then repaired the token planner's nested task-Team root/chain/member reconstruction. CRR-025 independently confirmed those row semantics but exposed the broader supported-upgrade spine: the pre-ticket `20260703_token_usage_execution_address_backfill` ID can already be terminal after writing old `{segments}` JSON, so `runPending()` skips the revised converter; separately, the migration's per-row database interface cannot uphold the already-approved all-or-nothing transaction contract. These are design ownership/atomicity corrections to BEH-015, not new user behavior. SR-015 therefore reused the already pending/exact-gated `20260801_team_canonical_identity` record as the target owner and made token writes transactional; SR-016 tightens that boundary further so semantic conversion and obsolete-column/index contraction are one transaction rather than later cleanup records.

After the cumulative SR-015 implementation advanced through repeated source/API rounds, the user requested a full-ticket rather than delta review. CRR-050 superseded bounded CRR-049 as the global gate. It preserved the improved backend routing, migration/startup, V5 SDK admission, provider, storage, and launcher work, but found two reachable design defects: `TeamRunEvent` and Team WebSocket payloads are structurally uncorrelated/generic and the browser trusts them; and frontend immutable topology, concrete task execution, task lifecycle, focus, and presentation are conflated in a public mutable node/map model. Real browser/API failures API-F-012 through API-F-015 demonstrate the consequence. CR-F-030 also enumerates dormant route branches/dead helpers.

The user then explicitly instructed the solution designer to refresh the ticket worktree onto the greatly updated latest `origin/personal` and continue improving with the design principles. The solution branch protected the dirty downstream/API state, fetched and merged the latest base, resolved the only conflict by composing latest selected-TeamRun gating with exact canonical execution focus, restored the protected state, and verified the focused workspace-history seam at 6/6. This base integration changes no SR-016 architecture decision but is now the authoritative design baseline.

ARCH-REV-010 completed a full cumulative review of SR-017 rather than a delta-only review. It passed the rooted aggregate, logical/concrete addresses, shared message/task resolver, provider protocol, released-data migration and token transaction, task activation ownership, valid frontend execution aggregate, exact cleanup inventory, and direct forward-only V5 application cut. It also resolved CR-F-029 and CR-F-030 at design level and found CR-F-028 substantially resolved. Two gaps remain. DR-005 proves that normal Team connection/open/restore status snapshots and unmaterialized send/delegation initializing/error feedback are reachable supported Agent-status producers that were not assigned to the exact correlated projection boundary. DR-006 identifies a stale UC-019 sentence that still described application data as converted despite the governing discard/rebuild decision. SR-018 preserves every passed boundary, closes those two status spines without manufacturing a connection `TeamRunEvent`, and corrects the requirement mapping.

ARCH-REV-011 then passed the complete cumulative SR-018 solution. After implementation/source review resumed, API-REV-035 exercised the real imported AutoByteus Team browser path and displayed repeated `Rejected SEGMENT_CONTENT: segment_type is required` cards. CRR-076 classified the origin as Design Impact rather than a bounded mapper defect: native AutoByteus establishes type only on start; the common converter preserves that shape; Team admission/application streaming expect per-content type; the Team adapter is recreated per event; and the browser still contains an optional-type/guessed-text path. The user-visible requirement is unchanged. The design must assign one lifecycle owner before those divergent consumers and preserve the strict no-fallback policy. CRR-076 also records CR-F-043, an API/E2E-owned cleanup defect for one nested disposable journal. Solution and implementation work must not inspect/delete it; API/E2E must correct only its owned residue/evidence before a new live run.

ARCH-REV-012 then performed a complete cumulative SR-019 review. It accepted the sole non-persisted `AgentSegmentLifecycleState` per AgentRun, queue placement, provider-minimal facts, and stateless Team/application/browser projection. DR-007 is a reachable completeness defect: `FileChangeEventProcessor` currently reads type from `SEGMENT_END` and overwrites its context on repeated start, while memory/history, external-channel, compaction, and skill-improvement consumers retain source aliases/defaults or terminal-end text. SR-020 correctly preserves the accepted owner, drops same-type active replay before fan-out, and assigns exact behavior/removal/proof to every affected consumer. Its DR-008 response, however, accepted a mechanically constructible missing-turn object as product-reachable without tracing provider ownership and added `RUNTIME_DIAGNOSTIC`. The user invoked the design-principle product-reachability gate. SR-021 reinspection proves valid segment output is always turn-owned before AgentRun, restores the original three-variant evidence model, and moves missing/empty/inactive/conflicting turn rejection to provider ingress. CR-F-043 remains untouched and downstream-owned.

After SR-024 passed as ARCH-REV-018 and SR-025/IR-045 completed its copy-only implementation, API-REV-039 exercised a real Nested Classroom task-Team reverse reply. The sender resolved and invoked the correct `send_message_to`; the common resolver reached the exact active Claude AgentRun; Claude rejected because the turn remained active. CRR-086 superseded the local-fix classification after comparing the common path: `AgentRun` lacks an input-admission owner, Codex selects steer, Claude selects rejection, AutoByteus queues, and `AgentRunCommandRegistry` independently rejects a different input. SR-026 corrects this as one AgentRun-owned live FIFO. The public acceptance point is queue ownership rather than model completion, preventing collaboration deadlock for next-turn-only runtimes while retaining live-proven Codex exact-turn steer through an explicit capability.

After ARCH-REV-019 passed SR-026, the user requested direct investigation against the real latest Claude Agent SDK and authorized updating the related libraries to mutually compatible current versions in the worktree. The exact installed set is Agent SDK `0.3.231`, Anthropic SDK `0.116.0`, MCP SDK `1.30.0`, and existing Zod `4.3.6`; package peers are satisfied. Official docs/types describe streaming input as a persistent provider queue with interruption. Isolated live probes show `priority:"now"` aborts current work and starts the new instruction, while `priority:"next"` processes later; neither accepts an exact active turn ID. This preserves SR-026's Claude `activeTurnAppend:"unsupported"` decision. The upgrade remains a bounded dependency/configuration change: use current one-string `query()`/`resume`/full-env/session semantics, require only the intrinsic Agent Tools MCP server in turn 1 with `alwaysLoad:true`, and do not adopt provider queue/command-lifecycle ownership.

The bootstrap package was received by the current software-engineering team as the approved Ticket 1 requirements basis. The solution designer independently re-read the current definition, topology, runtime, tool, metadata, and documentation paths and produced the initial design. No implementation handoff is produced by this role.

## Environment Discovery / Bootstrap Context

- Project Type: `Git`
- Task Workspace Root: `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs`
- Task Artifact Folder: `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs`
- Current Branch: `codex/agent-team-hierarchical-handoffs`
- Current Worktree / Working Directory: `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs`
- Bootstrap Base Branch: `origin/personal`
- Remote Refresh Result: Bootstrap originally resolved `origin/personal@2a7271c...`; delivery and later solution rounds integrated newer bases. On 2026-08-13 the solution designer freshly fetched and verified `origin/personal@54890a07f74e941a7a12b6daaa26364f4c927b72`; current reviewed source HEAD is `42e42a9471c251075af07c3e0805d43858246e67`, 97 commits ahead and zero behind, with merge-base equal to `origin/personal`. The implementation engineer reset the superseded Claude-only experiment; source/tests remain unchanged while downstream evidence/docs stay preserved.
- Task Branch: `codex/agent-team-hierarchical-handoffs`
- Expected Base Branch: `origin/personal`
- Expected Finalization Target: `personal`
- Bootstrap Blockers: `None`
- Notes For Downstream Agents: This remains the dedicated ticket worktree. ARCH-REV-019 passed the complete cumulative SR-026 design; ARCH-REV-018 remains the SR-024 baseline and SR-025/IR-045 is copy-only and implemented. API/E2E and delivery are paused. Existing dirty downstream evidence/docs, protected stashes, backups, operational database, and API-owned residue are preserved untouched. SR-027 adds user-authorized exact dependency manifest/lock changes plus solution artifacts only; existing dirty SR-026 source/tests and all downstream/protected state remain untouched. Complete architecture re-review, corrected implementation, and focused/full source review are required before API/E2E resumes.

## Supplemental Task Artifact Inventory

| Artifact Path | Purpose And Scope | Evidence, Context, Or Decision Captured | Core Artifact(s) Supported | Related Requirement / Acceptance-Criteria IDs | Status | Approval Applicability / State | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/agent-team-addressing-handoff-contract.md` | Normative Ticket 1 protocol supplement | `recipient_address` grammar, handoff authoring/projection, intrinsic Team tools, filesystem-like system instruction, provider results/errors, and use-case spans | Requirements; design spec | R-001–R-031, R-043, R-047, R-057 / AC-001–AC-025, AC-039, AC-043, AC-052 | `Refined — SR-026 input-admission linkage aligned; protocol unchanged` | Intended-behavior authority for collaboration and LLM-facing handoff protocol | Complete SR-028 architecture review, then implementation/full source review |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/agent-team-collaboration-system-instruction.md` | Exact implementation-copy supplement | Two sibling natural Agent-facing renderer sections, `{{member_address}}` substitution, three-provider system-instruction seams, exclusions, and exact-text/order verification | Requirements; design spec; addressing contract; live-validation contract | R-012–R-014, R-021 / AC-013, AC-019, AC-043 | `Refined — SR-025 user-approved implementation copy; no architecture impact` | Exact prompt-copy authority for implementation | Exact copy already implemented through IR-045; no new SR-026 change |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/team-run-canonical-identity-refactor.md` | Normative SR-024-aligned identity/data-model supplement | Rooted TeamRun tree, recipient/handoff seam, canonical identity, typed run IDs, corrected TeamRun/token migration chains, direct forward-only V5 application target, strict Team stream/frontend/status linkage, upstream AgentRun segment lifecycle linkage, storage, removals, and verification | Requirements; investigation notes; design spec; Team stream and segment supplements | R-011–R-013, R-021, R-026, R-028–R-043, R-047, R-049–R-057 / AC-012, AC-013, AC-019, AC-023–AC-039, AC-043, AC-045–AC-052 | `Accepted by ARCH-REV-019; SR-027 dependency linkage aligned` | Intended-behavior and target-model authority for the comprehensive refactor | Complete SR-028 architecture review, then implementation/full source review |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/team-stream-execution-projection-contract.md` | Normative Team stream/frontend execution supplement, closed through SR-018 and aligned through SR-024 | Correlated TeamRun events, one shared Agent execution binding/status snapshot, strict shared Team WebSocket DTO/schema package, nineteen exact producer/browser spines, immutable topology, valid execution union, `TeamExecutionState` authority, removal inventory, six-path allowlist, and consumption of canonical AgentRun segment/error events | Requirements; investigation notes; design spec; canonical identity and segment-lifecycle supplements | R-036, R-039, R-043, R-049–R-057 / AC-029, AC-035, AC-036, AC-038, AC-039, AC-045–AC-052 | `Accepted by ARCH-REV-019; SR-027 dependency linkage aligned` | Design authority for already-approved preserved behavior; no new user capability | Complete SR-028 architecture review, then implementation/full source review |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/agent-segment-lifecycle-contract.md` | Normative SR-024 segment-lifecycle design supplement | Exact provider turn admission, minimal source facts, sole AgentRun state owner, canonical enrichment, exact order/replay/cleanup, complete processor/listener contract, original three error variants, removals, and real boundary coverage | Requirements; investigation notes; design spec; Team stream and live-validation supplements | R-043, R-053–R-057 / AC-045, AC-046, AC-048–AC-052 | `Accepted by ARCH-REV-019; SR-027 dependency linkage aligned` | Structural design authority for already-approved response streaming; no new product capability | Complete SR-028 architecture review, then implementation/source review |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/nested-classroom-live-validation-contract.md` | Normative downstream validation supplement | Staged fixture import, isolated secret preparation, exact runtime/model matrix, strict Team stream/concrete frontend execution, initial/pre-run status, real source-to-canonical segment observations, redacted evidence, failure classification, and cleanup | Requirements; investigation notes; design spec; all domain supplements | R-021, R-044–R-058 / AC-019, AC-040–AC-053 | `Refined — SR-028 exact Claude interrupt proof aligned` | Intended-behavior authority for later API/E2E live validation | Later API/E2E execution after architecture/full source gates and CR-F-043 cleanup |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/agent-run-input-admission-contract.md` | Normative SR-026 input-admission design supplement | One AgentRun-owned live FIFO, exact accepted meaning/timing, explicit backend start/append mechanics, provider capability matrix, typed internal lifecycle, interruption/termination, removal inventory, nine case spines, and real API-F-025 proof | Requirements; investigation notes; design spec; addressing, segment, Team stream, and live-validation supplements | R-057, R-058 / AC-052, AC-053 | `Refined — SR-028 input and interrupt authority aligned` | Structural intended-behavior authority for already-approved ordinary Agent input delivery; no new public capability | Complete architecture re-review, then implementation/full source review |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/agent-team-hierarchical-handoffs/tickets/in-progress/agent-team-hierarchical-handoffs/claude-agent-sdk-upgrade-contract.md` | Normative SR-027 dependency/capability supplement | Exact compatible versions, official/latest type and breaking-change evidence, isolated now/next probe, retained query/resume/env/executable behavior, intrinsic MCP turn-1 adaptation, eight case spines, file/removal map, and verification | Requirements; investigation notes; design spec; input-admission and live-validation supplements | R-057, R-058 / AC-052, AC-053 | `Refined — SR-028 ready for complete architecture re-review` | Intended-behavior and dependency-boundary authority approved by the user | Complete architecture review, then implementation/full source/API-E2E proof |

## Source Log
| Date | Source Type | Exact Source / Query / Command | Why Consulted | Relevant Findings | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-13 | Official Claude Agent SDK sources | `https://github.com/anthropics/claude-agent-sdk-typescript/blob/main/CHANGELOG.md`; `https://code.claude.com/docs/en/agent-sdk/streaming-vs-single-mode`; `https://code.claude.com/docs/en/agent-sdk/user-input`; installed `0.3.231` `sdk.d.ts`/`package.json` | Determine latest version, breaking changes, peer requirements, and exact active-input semantics | `0.3.231` is current and declares Claude Code parity `2.1.231`. Streaming input provides sequential queued messages, priorities, interruption, receipts, and cancellation, but no API takes the AutoByteus exact active turn ID. `0.3.142` removes V2 APIs, backgrounds MCP connections, and changes Task tools; `0.3.143` moves Anthropic/MCP SDKs to peers. | Pin exact compatible peers; keep query/resume; add `alwaysLoad:true` to required intrinsic MCP only; do not adopt provider input queue |
| 2026-08-13 | Package registry / installed graph | `pnpm view @anthropic-ai/claude-agent-sdk version peerDependencies`; equivalent Anthropic/MCP queries; manifest/lock inspection; installed package metadata/import smoke | Select mutually compatible current direct versions and verify real resolution | Exact current set is Agent SDK `0.3.231`, Anthropic SDK `0.116.0`, MCP SDK `1.30.0`, existing Zod `4.3.6`. Peers `>=0.93.0`, `^1.29.0`, `^4.0.0` are satisfied. Root `query`, `getSessionMessages`, and `createSdkMcpServer` imports succeed. | Keep exact manifest pins and one lock resolution; verify in CI/source review |
| 2026-08-13 | Current Claude integration source | `claude-sdk-client.ts`; executable resolver; runtime availability; Claude session/backend/event converters; Agent Tools MCP materializer/builders; project-wide `TodoWrite`/Task/V2/streamInput/priority scan | Map breaking-change impact to real product owners | Project already uses `query(prompt:string)`, `options.resume`, complete environment construction, selected external executable, and no removed V2 or Claude TodoWrite consumer. Intrinsic Agent Tools MCP lacks `alwaysLoad`. No current streamInput/priority owner exists. | Preserve current adapter boundary; add only exact dependency/config/test changes |
| 2026-08-13 | Isolated latest-SDK live probes | SDK `0.3.231`, selected Claude Code `2.1.231`, disposable HOME/workspace, `priority:"now"` during active Bash and `priority:"next"` during active Bash; redacted observation only | Determine whether latest can append/steer the exact current Claude turn | `now` aborts the running tool/assistant work and starts the new command; `next` queues sequential later work. Neither input contains an exact active turn selector. Capabilities include interrupt receipt/cancel queued/command lifecycle, but those are provider queue mechanics. | Keep Claude append unsupported and AgentRun FIFO authoritative; retain isolated capability controls in downstream evidence |
| 2026-08-13 | Verification commands | `pnpm install --lockfile-only`; root import smoke; `pnpm --dir autobyteus-ts build`; `pnpm --dir autobyteus-server-ts exec tsc -p tsconfig.build.json --noEmit`; focused Claude SDK/client/session/MCP/factory tests | Verify the actual latest graph before final design | Lock-only install reports no Claude/Anthropic/MCP peer issue; shared build and server production typecheck pass; focused result is 7 files passed, 1 live integration file skipped by its existing gate, 51 tests passed, 4 skipped. Standard server `tsconfig.json` typecheck remains unsuitable because it includes tests outside `rootDir`; production config passes. | Re-run authoritative implementation checks after source adaptation; live rows cannot pass by skipping |
| 2026-08-13 | Operational investigation incident | Malformed local shell inspection command during the SDK probe | Preserve truthful security/operational evidence without reproducing sensitive values | The shell expanded and printed inherited environment values into private command output. No credential value was written into repository files or durable evidence. The solution owner stopped reproducing the command, retained only key-name/redacted checks, and recommends rotating any credential that may have appeared in that output. | User/operator credential rotation; downstream evidence must remain redacted |
| 2026-08-13 | ARCH-REV-020 interrupt-path and authority audit | `design-review-report.md`; `architecture-review-revision-record.md`; current `claude-session.ts`, `claude-sdk-client.ts`, and focused session tests | Resolve DR-013/DR-014 without reopening the accepted input/dependency architecture | The supported product interrupt starts one string-query with `Options.abortController`; `ClaudeSession` clears pending approvals, aborts that controller, awaits the active execution, cleans its registered query/reference through the established turn finalizer, clears active state, and emits canonical `TURN_INTERRUPTED`. Focused coverage proves `sdkClient.interruptQuery()` is not called. The installed declaration places `Query.interrupt()` among streaming controls, which SR-027 does not adopt. The revision index and live-contract metadata also lagged SR-027. | Preserve AbortController as the only active one-string-query interrupt mechanism; forbid Query.interrupt/receipt substitution; align navigation as SR-028 |
| 2026-08-13 | CRR-086 design-impact review | `code-review-report.md`; `code-review-revision-record.md`; `/tmp/crr086-active-input-ownership-audit.log`; API-REV-039 failure analysis/trace | Reclassify the live Claude task-peer failure after cross-provider comparison | Common routing reaches the exact run. AgentRun forwards after `beginCommand()` returns null; Codex steers, Claude rejects, AutoByteus queues, and command registry rejects another command. No common accepted/FIFO/termination contract exists. | Assign one AgentRun admission owner and route through complete architecture review |
| 2026-08-13 | AgentRun/command source | `agent-run.ts`; `agent-turn-lifecycle-state.ts`; `agent-run-backend.ts`; `agent-operation-result.ts`; `agent-run-command-coordinator.ts`; registry/types/status projection; provisioning/restore services; every `postUserMessage` caller | Trace the real shared boundary, lifecycle race, other admission owner, and inactive-run activation concern | All supported callers converge on AgentRun. The lifecycle has one pending-command slot; command coordinator races raw events for turn association and enforces singular in-flight. Prepared activation already has a single-flight; restore needs the same provisioning discipline but must not own active-input order. | Add run-local FIFO/typed observer; retain activation only as outer single-flight; remove busy/raw inference |
| 2026-08-13 | Provider source and established coverage | Codex backend/thread plus live steer-memory E2E; Claude backend/session; AutoByteus backend and runtime queue integration test | Determine supported active-input behavior and whether one uniform provider mechanism is possible | Codex same-turn steer is live-proven with two user traces under one turn. Claude supports one turn at a time and rejects active input. AutoByteus queues a second turn-start until the first completes. Provider mechanics differ, so AgentRun must select append when declared and otherwise next-turn wait. | Preserve mechanics through explicit `start_turn | append_to_active_turn`; remove provider policy/tails |
| 2026-08-13 | Collaboration deadlock analysis | API-F-025 trace plus `InterAgentMessageRouter`/mixed-handle synchronous tool return path | Define truthful acceptance timing for an active next-turn-only recipient | Waiting to return acceptance until the recipient becomes idle can deadlock two active Agents participating in the same request/reply. Queue ownership is the earliest truthful common acceptance point; provider forwarding and completion remain later lifecycle facts. | Define stable admission result and once-only Team projection; no retry/republish |
| 2026-08-13 | Complete architecture review | `design-review-report.md` ARCH-REV-012; `architecture-review-revision-record.md`; MP-008/MP-009 | Reinvestigate the two bounded failures after SR-019's owner was accepted | DR-007: current file/memory/external/compaction/skill consumers remain on source/fallback assumptions. DR-008: missing-turn candidate cannot truthfully emit a turn diagnostic. No requirement gap; cumulative SR-018/SR-019 structure otherwise accepted. | Produce complete consumer cut; apply product-reachability gate to missing-turn premise before final DR-008 design |
| 2026-08-13 | Default pipeline / file-change source | `default-agent-run-event-pipeline.ts`; `agent-run-event-pipeline.ts`; `agent-run-event-processor.ts`; `file-change-event-processor.ts`; `file-change-invocation-context-store.ts`; file-change payload accessors | Trace exact processor order and the concrete DR-007 witness | File change runs after transformers; segment start and later tool start can replace the same run+invocation context; content/end inspect type before context; end will lack type in the target. Cached processor state needs explicit turn/run release while the segment state remains per AgentRun. | Non-replacing segment initialization, content-preserving matching tool enrichment, exact run/turn/invocation file context, no end type, owner-level repeated-start drop, pipeline run-release hook |
| 2026-08-13 | Complete AgentRun listener inventory | repository `rg` for `SEGMENT_START|SEGMENT_CONTENT|SEGMENT_END`; `runtime-memory-event-accumulator.ts`; compaction collector; improver watcher; external channel parser/eligibility/collector; application projector; standalone mapper/coalescing; Team adapter/domain/contract/projector; browser DTO/handlers | Identify every post-pipeline consumer and distinguish valid projection state from lifecycle authority | Memory manufactures segment turn/ID/type; compaction/skill accept aliases and end text; external channel treats missing end type as text and Team errors lose evidence; application/browser terminalize generic diagnostics; coalescing is safe only with exact non-delta identity. No separate run-history segment reader exists; memory recorder is the transcript/history consumer. | Assign exact target/removal/proof per consumer; no source fallback after transformer |
| 2026-08-13 | Original error-history and producer audit | `git show 9ea4c079b`; `agent-run-error-evidence.ts`; `agent-turn-runner.ts`; `llm-phase.ts`; `tool-phase.ts`; `llm-response-pipeline.ts`; lifecycle/failure/command observers | Determine whether the original asymmetric evidence union omitted a real product state | `TURN_DIAGNOSTIC` has many normal AutoByteus recoverable turn producers; `TURN_TERMINAL` and `RUNTIME_GLOBAL` own distinct real terminal effects. Origin/personal has no runtime-diagnostic variant. Ticket commit `a0a1073cb` added it solely for a new transformer branch over missing-turn synthetic input. | Preserve original three variants; do not standardize a fourth state without a supported producer |
| 2026-08-13 | Provider turn-ownership audit | AutoByteus `llm-streaming-response-handler.ts` / `segment-events.ts` / server converter; Codex `codex-thread.ts` and segment item/reasoning normalizers; Claude session/text/tool projectors | Trace whether normal supported segment output can cross provider ingress without a turn | AutoByteus segment creation requires the current handler turn; Claude allocates and passes an exact session turn; Codex owns exact `activeTurnId`, although some native notifications omit the repeated field. No supported path requires a post-ingress turnless segment. Current permissive null/raw fallthrough is a boundary defect, not a product state. | Require provider-owned exact turn; explicit Codex turn must match active turn; only an exhaustively named field-omitting Codex event may use that exact active turn; otherwise reject before enqueue |
| 2026-08-13 | ARCH-REV-015 cumulative review | `design-review-report.md`, `architecture-review-revision-record.md` | Reassess SR-021 completely rather than delta-only | DR-007 and DR-008 are resolved. DR-009 is stale current-authority wording that promotes provider rejection into downstream transport. DR-010 is reachable through normal Codex turns because the prior event-family phrase does not define which native omission may inherit the session turn. | Align every normative/navigation surface and make the provider-local Codex allow set, field equality, rejection, and tests exhaustive |
| 2026-08-13 | ARCH-REV-016 cumulative review | `design-review-report.md`, `architecture-review-revision-record.md`; MP-011/MP-012 | Reassess the full SR-022 package and verify real dominance of the exact Codex admission owner | DR-009/DR-010 are resolved. The actual client/router -> `CodexThread.handleAppServerNotification()` -> notification handler -> backend listener -> converter path lets MCP start/completion mutate pending state or emit `LOCAL_MCP_TOOL_EXECUTION_COMPLETED` before converter-local admission. Converter raw debug also writes the complete candidate before rejecting it. | Keep the single pure resolver/rules, invoke it in `CodexThread` before the handler, forward one immutable admitted canonical notification, suppress all rejected raw capture, and prove valid MCP/debug behavior unchanged |
| 2026-08-13 | ARCH-REV-017 cumulative review | `design-review-report.md`, `architecture-review-revision-record.md`; MP-013 | Reassess SR-023 completely and apply the product-reachability gate to its native-name applicability | DR-011 is resolved. Current source has exactly four segment-producing native names and no supported otherwise-unlisted item family or provider-evolution contract. The nine established non-segment names already own exact routes; an unknown event is MP-013 Not Reachable and creates no segment after exact converter routing. | Keep first-boundary four-family admission/opaque message/MCP/debug behavior; remove runtime exemption registry, unknown-event branch/rationale, and synthetic proof |
| 2026-08-13 | Codex segment-turn admission audit | `codex-thread-event-name.ts`; `codex-thread-event-converter.ts`; `codex-item-event-converter.ts`; `codex-item-event-payload-parser.ts`; `codex-reasoning-event-normalizer.ts`; `codex-reasoning-block-tracker.ts`; `codex-segment-source-payload-normalizer.ts`; `codex-agent-run-backend.ts`; focused converter tests | Identify every native event that directly creates source segment facts and the existing active-turn owner | Direct source segment creation occurs only for `item/started`, `item/agentMessage/delta`, `item/completed`, and `item/reasoning/completed`. The enum has exactly nine other current `item/*` names with established non-segment paths; any reasoning close uses tracker-owned turn. Candidate turn fields are top-level snake/camel, nested item snake/camel, and `turn.id`. Current broad item routing, first-candidate parsing, null reasoning tracker input, and segment-normalizer/raw fallback do not enforce a single admission invariant. No current or approved future item family exists outside these current paths. | Add one provider-local resolver before mutation whose exact four-name constant is both production applicability and omission-inheritance authority; require every present governed field to be valid/equal to active; keep all other current events on their exact routes; remove broad-prefix segment construction and define no open unknown-event policy |
| 2026-08-13 | Codex first-native-boundary dominance audit | `codex-client-thread-router.ts`; `codex-thread.ts`; `codex-thread-notification-handler.ts`; `codex-app-server-message.ts`; `codex-agent-run-backend.ts`; `codex-thread-event-converter.ts`; `codex-thread-event-debug.ts`; `runtime-raw-event-file-debug.ts`; current thread/converter tests | Trace every effect before the SR-022 converter-local resolver and determine the smallest authoritative boundary | The router selects a `CodexThread`; `CodexThread.handleAppServerNotification()` immediately invokes the handler. MCP `item/started` adds pending state; MCP `item/completed` deletes it and emits a derived local completion before listeners. The backend listener alone invokes the converter, whose first action is raw JSONL/debug capture. The first per-thread boundary already owns `activeTurnId` and can stop all these effects. | Move only invocation/accepted-value construction to `CodexThread`; handler/listener/converter consume the same admitted canonical value; make the listener value opaque with one thread-owned brand/private constructors and admitted-native versus local-derived provenance; converter raw debug remains admitted-only; rejection uses a separate sanitized logger |
| 2026-08-13 | User product-reachability clarification | User discussion applying design-principle Product Reachability Rule to DR-008 / MP-009 | Decide whether a mechanically constructible missing-turn object may drive domain machinery | The user explicitly requires a complete supported trigger/caller/lifecycle witness for hypothetical edge cases. No such witness exists after the provider turn-ownership audit. MP-009 is therefore Not Reachable for current supported behavior. | Supersede SR-020 runtime-diagnostic design with SR-021 provider-ingress rejection and request complete architecture re-review |
| 2026-08-12 | Code review / live failure evidence | `code-review-report.md` CRR-076; `code-review-revision-record.md`; API-REV-035 `failure-api-f024-autobyteus-team-segment-type-admission-analysis.md`; `/tmp/crr076-api-rev035-failure-origin-audit.log`; `/tmp/crr076-segment-contract-focused.log` | Reinvestigate the real AutoByteus Team content rejection and reviewer classification | CR-F-042/API-F-024 is reachable Design Impact: the native producer omits content type by contract and the reviewed Team design has no lifecycle correlation owner. CR-F-043 preserves an API-owned cleanup prerequisite. | Revise requirements/design, architecture review; preserve API residue for its owner |
| 2026-08-12 | Provider/runtime source | `autobyteus-ts/.../segment-events.ts`; AutoByteus converter; Codex item/reasoning converters/trackers; Claude text projector/event converter/tool coordinator | Compare exact start/content/end facts and current state owners across all three runtimes | AutoByteus start alone owns type. Codex/Claude repeat type on later events and Codex unknown item handling defaults to text; Claude text and Codex reasoning can begin at content in the normalized stream. Provider-local code already has explicit item/block creation seams capable of emitting truthful starts. | Minimal source contract and explicit start changes; no Team branches/defaults |
| 2026-08-12 | Common AgentRun source | `agent-run.ts`; `agent-run-event-dispatch-queue.ts`; event transformer/pipeline/default pipeline/dispatch; lifecycle state/transformer; error-evidence resolver | Locate the first common serialized owner and a non-terminal rejection channel | Every provider batch crosses one run-owned queue before processors/listeners; run-owned turn state is already passed through the pipeline; `{error_scope:"turn",error_effect:"diagnostic"}` is observable without terminating the turn. | Add parallel run-owned segment state and pre-listener transformer; reuse diagnostic semantics |
| 2026-08-12 | Team/application/browser source | `team-agent-event-adapter.ts`; Team Agent domain/strict DTO/projector; `application-agent-stream-event-projector.ts`; browser `messageTypes.ts`, `segmentHandler.ts`, `segmentIdentity.ts`, `segmentTypes.ts` | Trace every consumer that currently needs or guesses segment type | Team and application require per-content type but own no correlation; browser content type is optional, creates missing segments with `?? "text"`, stores a serialized type/ID lookup key, and falls back unknown starts to text. Typed content is also needed for truthful late subscription. | Keep canonical content self-contained; remove downstream state/defaults/keys; compound turn+segment identity |
| 2026-08-13 | Git/base/worktree audit | `git fetch origin personal`; `git rev-parse HEAD origin/personal`; `git rev-list --left-right --count origin/personal...HEAD`; `git status --short` | Reconfirm current design work is based on latest personal without disturbing downstream state | `origin/personal@54890a07...`; HEAD `0d32ff255...`; 90 ahead/0 behind; dirty implementation/test/API/delivery artifacts and CR-F-043 journal remain present and untouched. | Solution-artifact edits only |
| 2026-08-11 | Full cumulative code review | `code-review-report.md` CRR-050; `code-review-revision-record.md`; `/tmp/crr050-full-ticket-structural-audit.log` | Reinvestigate the whole implemented ticket after repeated bounded review/API fixes | CR-F-028: uncorrelated/generic Team event/wire/browser boundary; CR-F-029: mutable topology/task/public raw-map projection; CR-F-030: remaining route/alias/dead-helper cleanup. CR-PREM-022/023 are reachable through supported message/task browser paths. | Close both authoritative boundaries; architecture re-review before implementation/API resumes |
| 2026-08-11 | Server source | `team-run-event.ts`; `task-delegation-event-publisher.ts`; `task-delegation-record.ts`; `models.ts`; `agent-run-event-message-mapper.ts`; Team mapper/broadcaster/handler/egress; member-input and communication builders | Trace producer -> domain event -> mapper -> serialized Team WebSocket contract | Event discriminator/data are independent; task payload/publisher erase to `unknown`; mapper casts into generic records; task domain already has four exact produced detail types; unproduced terminal alias exists; member input repeats the receiving execution. | Correlated domain/task variants, exact Team DTOs, exhaustive mapper, singular identity |
| 2026-08-11 | Task event/record derivation source | `task-delegation-record.ts`; `task-delegation-record-derived.ts`; `task-delegation-event-publisher.ts`; GraphQL `types/task-delegation.ts`; browser `teamTaskExecutionProjection.ts` / `teamTaskExecutionTree.ts` | Tighten the proposed task event and one-copy frontend projection after the first SR-016 model draft | Durable record owns content, sender, references, taskRun timestamps, and complete ordered updates; `deriveTaskLabel` proves label is presentation; terminal status is derivable; current publisher repeats base/derived facts on every event and invents a separate activation time; current browser stores task facts on mutable execution nodes and also builds history/presentation. GraphQL returns the append-only full root list, so overlapping queries may omit a concurrently later activation without meaning deletion. | Activation carries persisted base create/start facts; result events carry event-specific facts and refresh; remove duplicate status event; one monotonic task projection supplies active/history; preserve known rows on stale/omitting concurrent responses; reject equal-time conflicts atomically |
| 2026-08-11 | Backend task/token identity tightness audit | `domain/task-agent-instance.ts`; `domain/task-team-instance.ts`; `task-agent-instance-identity.ts`; `task-team-run-identity-factory.ts`; `task-execution-instance.ts`; `records/task-delegation-task-id-allocator.ts`; `records/task-delegation-records-service.ts`; task activation/ledger/directories/settlement/notifications/work packets/member context/status; token Prisma schema/domain/repository/store | Apply the shared-structure tightness and one-authority rules beyond the reported frontend symptom | `taskAgentInstanceId` and `taskTeamInstanceId` are deterministic aliases of `taskId`; their identity objects also copy task run, owner/parent run, and creation time already owned by `taskRun.address`, root execution, and task record. The allocator proves `taskId` is sequential and unique only inside a root TeamRun's records file, not globally. Generic task context/status/wire/token shapes propagate the duplicates. Token rows additionally persist writable root/member/task-run/task-instance columns beside canonical JSON and actual Agent `run_id`; root lookup is the only evidenced performance need. | Use root-TeamRun-scoped `taskId` for management and non-null `taskRun.address` for concrete execution; cross-root lookup accepts `{rootTeamRunId,taskId}` explicitly; derive one non-persisted `{kind,taskId,executionAddress}` active binding; retain only nullable task ID beside member execution context; remove generic task context/result run IDs; contract token identity and use a JSON-root expression index |
| 2026-08-11 | Prisma/app-data migration ordering audit | `server-runtime.ts`; `startup/migrations.ts`; Prisma migration history/schema; app-data registry; canonical/token store; token provider/model backfills; both narrow column-drop migrations | Prove the proposed token schema contraction can preserve predecessor inputs and fresh-install behavior | Prisma `migrate deploy` runs before app-data. Historical migrations create the obsolete columns even on a fresh store, while the provider-name backfill currently selects unrelated/renamed legacy columns and the registry currently runs canonical before it. A physical Prisma drop would destroy conversion inputs; separate drop definitions split one invariant across records. SQLite supports transactional DDL and JSON expression indexes. | Omit obsolete fields from target schema/client but add no pre-conversion physical drop migration; order/tighten model/provider backfills before canonical; let the one `20260801...` raw store transaction update addresses, remove columns/old index, create/verify JSON-root index, and roll back all |
| 2026-08-11 | In-memory SQLite capability probe | `sqlite3 :memory:` transactional update + seven `ALTER TABLE ... DROP COLUMN` operations + JSON expression-index creation + `EXPLAIN QUERY PLAN` | Verify the proposed physical operation against the repository runtime SQLite rather than assume DDL/index behavior | SQLite 3.51.0 committed the row update and seven column drops in one transaction; exact JSON root remained readable; a non-partial expression index over `json_extract(execution_address_json,'$.rootTeamRunId'), observed_at` was selected as `SEARCH ... USING INDEX`. A partial index required an additional non-null predicate and is unnecessarily fragile. | Require exact `token_usage_ledger_events_execution_root_observed_at_idx` and real rollback/query-plan durable coverage; do not use a partial-index variant |
| 2026-08-11 | Frontend source/audit | `messageParser.ts`; `messageTypes.ts`; `AgentTeamContext.ts`; `teamTaskExecution*.ts`; `TeamStreamingService.ts`; `teamRunContextHydrationService.ts`; hydration/open/history/navigation/mobile/token/presentation consumers; `rg` inventory from CRR-050 | Locate wire admission and concrete execution lifecycle owners | Browser validates only type string; task DTO admits aliases/any; one node base mixes topology/task/presentation; task Team cloning creates empty IDs; 30 production consumers touch the raw map and 11 parse keys; live/restore/focus/cleanup are distributed. The supported open path gets `isActive` from `TeamRunResumeConfig` separately from metadata and builds per-Agent contexts from workspace plus loaded/historical-unloaded projections, so canonical metadata alone is not a truthful complete execution-builder input. | Immutable topology + valid execution union + one aggregate/query boundary; closed metadata/lifecycle/focus/per-Agent-seed input; delete raw consumers/modules |
| 2026-08-11 | Frontend supported launch path | `teamRunConfigStore.ts`; `agentTeamContextsStore.ts`; `agentTeamRunStore.ts`; `agentSelectionStore`; `teamDefinitionMembers.ts` | Test the “every concrete execution has a real ID” invariant and one-config/selection authority against pre-launch behavior | `teamRunConfigStore` owns the editable config, while `createRunFromTemplate` copies it into a fake context, creates `temp-team-*`, synthetic member run IDs, draft AgentContexts/conversation keys, and selection that looks like a TeamRun; `promoteTemporaryTeamRunId` then parses/rewrites keys and mutates root/focus/context identity. This is a supported pre-launch path and would contradict a concrete-only execution aggregate even after task placeholders are fixed. | Make the refactored config store the sole identity-free `TeamLaunchDraft` owner; query definition topology, do not copy it; discriminate draft/run selection; create canonical topology/execution only after server allocation; transfer focus/input once; delete promotion/rebase code |
| 2026-08-11 | Team Agent/collaboration/approval boundary | `mixed-agent-member-handle.ts`; `team-communication-service.ts`; `team-communication-message-event-processor.ts`; `TeamToolApprovalTracker.ts`; `team-tool-approval-command-handler.ts`; repository `rg approval_token` | Test whether a strict Team event contract could truthfully reuse generic Agent payloads and the apparent approval token | The member bridge republishes generic Agent payloads; derived `INTER_AGENT_MESSAGE`/`TEAM_COMMUNICATION_MESSAGE` overlap the canonical Team `COMMUNICATION`/`MEMBER_INPUT` path. The Team server has no approval-token producer or consumer, while the frontend alone stores/sends a token behind `unknown`/`as any` and tests manufacture it. | Add exhaustive Team Agent ingress adapter; filter duplicate Agent collaboration events; retain canonical communication/input; remove no-op Team approval-token shapes |
| 2026-08-11 | Workspace/Team transport boundary | `pnpm-workspace.yaml`; root/package manifests; server `models.ts`, `agent-team-stream-handler.ts`, `team-runtime-snapshot-service.ts`, Team command handlers/broadcaster/egress; web `TeamStreamingService.ts`, `handlers/teamHandler.ts`, protocol modules; repository `rg zod` | Verify whether a real shared runtime-valid Team transport package is feasible and whether it would improperly replace standalone Agent streaming | The root is a pnpm workspace; server already uses Zod 4, while the web currently has no strict runtime schema. Team handling currently reuses generic standalone `ServerMessage` helpers (`CONNECTED.team_id`, generic `ERROR`, generic broadcaster) and permissive client parsing. The WebSocket endpoint/session is already bound to one TeamRun, but `CONNECTED` repeats that ID while the browser ignores the message and marks subscribed on low-level socket open; lifecycle repeats and defensively compares the same root ID. A Team-only workspace package can be added atomically without changing the standalone Agent stream. | Register a Team-only contracts package, move Team handler/broadcaster/client commands to it, preserve standalone Agent `models.ts`, make exact post-binding `CONNECTED {session_id}` the application-ready handshake, scope lifecycle to the bound root without a repeated TeamRun ID, and forbid execution/domain policy in the package |
| 2026-08-11 | Team application execution binding | server `team-run-config.ts`, `team-run-metadata-schema.ts`, `team-definition-topology-planner.ts`, `mixed-agent-member-handle.ts`, `agent-run-config.ts`, published-artifact/application-stream producer reads; web `runHistoryTypes.ts` / `runHistoryMetadata.ts`; `@autobyteus/application-sdk-contracts` V5 `ApplicationExecutionProducer` | Verify whether every proposed frontend topology field is actually mounted/configuration state, whether runtime Agent construction preserves exact attribution, and whether the proposed typed frontend field has one contract owner | The one recursive disk aggregate stores `ApplicationExecutionContext` locally on each Agent, but its V5 `producer.executionAddress` is concrete Team execution identity. Current `MixedAgentMemberHandle.buildAgentRunConfig()` passes the source node context directly for persistent and task Agents alike; a task Agent can therefore publish artifacts or application events under the persistent producer address even though the handle already derives the exact task execution address. The server redeclares `ApplicationExecutionContext` while the browser models it as `Record<string,unknown>` and casts object-shaped metadata, so “typed execution-owned context” would otherwise keep a loose cross-process structure. | Preserve recursive persisted locality. Make V5 SDK contracts own the exact context type and have the server alias it; use server-domain and frontend-projection exact mappers at their canonical-address boundaries. At server AgentRun construction, validate a persistent context and rebind task/task-Team-Agent contexts to the handle's exact execution before building `AgentRunConfig`. Separately, have `TeamRunFrontendProjectionBuilder` consume the validated context into the concrete persistent Agent execution and rebind frontend task variants. Frontend topology remains execution-address-free. |
| 2026-08-11 | Base integration / focused probe | `git fetch origin personal`; protected stash `8a46238a...`; merge commits `88b2acb...` and `307b13a...`; `pnpm --dir autobyteus-web exec vitest run components/workspace/history/__tests__/WorkspaceHistoryWorkspaceSection.spec.ts` | Honor the user's latest-base requirement and preserve current personal-branch selection behavior | Latest `origin/personal@023f4f55...` integrated; one conflict combined selected-TeamRun gating with exact execution focus; restored downstream state; focused result 1 file / 6 tests passed. | Preserve this selection/focus invariant in SR-016 and downstream coverage |
| 2026-08-09 | Code review / probes | `code-review-report.md` CRR-025; `code-review-revision-record.md`; `/tmp/crr025-terminal-id-{audit,probe}.log`; `/tmp/crr025-token-transaction-probe.log`; `/tmp/crr025-ir014-probe.log` | Reinvestigate the complete supported token-upgrade and mutation spine after IR-014 | IR-014 row/index semantics pass; supported predecessor terminal record causes zero current converter executions and strict-reader `null`; forced second write leaves row one converted. CR-F-013 is Design Impact and CR-F-014 becomes a bounded source fix after ownership is corrected. | Assign one executable target record/gate owner and atomic store boundary; architecture re-review before source work |
| 2026-08-09 | Code / history | `app-data-migration-registry.ts`; `app-data-migration-runner.ts`; `server-runtime.ts`; `team-canonical-identity-migration.ts`; token migration/planner/task-index and both token legacy-column-drop migrations; `git show origin/personal:.../token-usage-execution-address-backfill-migration.ts` | Locate exact record ownership, order, gate, transaction capability, and cleanup dependencies | Registry currently registers revised target code under historical `20260703...`; runner skips both terminal statuses; `20260801...` is separately pending/exact-gated but does not compose token conversion; cleanup depends on the wrong old ID; migration DB interface exposes only per-row update. | Compose the token item into `20260801...`, remove the old semantic and narrow cleanup definitions from current registry authority, and give one transaction API the complete row/schema invariant |
| 2026-08-09 | Read-only operational data | `sqlite3 -readonly $HOME/.autobyteus/server-data/db/production.db` count/shape/status queries | Validate product reachability and choose a proportionate transaction design without mutating user data | Snapshot observed about 143.8k token rows, including 139,130 legacy `{segments}` Team rows and 4,710 standalone null-address rows; `20260703...backfill=SUCCEEDED`; `20260801...` and route-drop records absent. Counts changed slightly across reads because the active system continued recording usage. | Preserve token facts, plan deterministically, transact only required mutations, and report progress/failure observably |
| 2026-08-08 | User prompt-copy clarification | Ticket conversation after SR-013 architecture handoff | Replace unnatural lifecycle jargon and decide whether the literal prompt copy needs architecture review | User approved one natural explanation-first system-instruction block, required it to apply to AutoByteus, Codex App Server, and Claude Agent SDK system-instruction seams, requested a dedicated implementation-copy file, and explicitly classified the pure-text change as no architecture impact | Notify `implementation_engineer` with the exact file; retain the independent SR-013 architecture gate |
| 2026-08-08 | User solution approval | Ticket conversation after the completed SR-013 review | Confirm whether coordinatorless organizational containers require another Ticket 1 revision and whether SR-013 may proceed | User agreed that an operational AgentTeam normally has an accountable coordinator, retained the existing coordinator-required invariant, treated pure organizational containers as later AgentOrg scope, and explicitly approved SR-013 | Return the cumulative SR-013/rework package to `architecture_reviewer` |
| 2026-08-03 | Command | `git fetch origin personal`; `git rev-parse origin/personal`; `git ls-remote --heads origin personal`; `git worktree add -b codex/agent-team-hierarchical-handoffs ... origin/personal` | Establish isolated current ticket base | Both remote checks resolved to `2a7271c...`; dedicated branch/worktree created successfully | No |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-definition/domain/models.ts` | Inspect Team definition domain | Definition/update contain members/coordinator but no handoffs | Extend in later design/implementation |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-definition/providers/team-definition-config.ts` | Inspect shared/team-local config schema and normalization | `TeamConfigRecord` owns members/coordinator/avatar/launch config; missing fields normalize; writer reconstructs known fields | Add optional handoffs and canonical validation |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-definition/providers/file-agent-team-definition-provider.ts` | Trace shared/team-local/application-owned file paths | Shared/team-local reads use `normalizeTeamConfigRecord`; application-owned has a separate path; update rewrites config | All paths must round-trip handoffs |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-definition/providers/application-owned-team-source.ts` | Inspect application-owned schema | Separate config type, parser, definition builder, and writer omit handoffs | Must extend, not assume shared normalizer covers it |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-definition/services/agent-team-definition-service.ts`; `services/team-definition-graph-validator.ts` | Inspect create/update and recursive graph validation | Service validates members/coordinator and recursive refs; graph validator can be extended for endpoint/topology rules | Use as definition boundary; also validate composed graph at launch |
| 2026-08-03 | Code | `autobyteus-server-ts/src/api/graphql/types/agent-team-definition.ts`; `api/graphql/converters/agent-team-definition-converter.ts` | Trace API round trip | Output/create/update/converter independently map current fields; no handoffs | GraphQL additions required even though frontend is out of scope |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/services/team-definition-topology-planner.ts` | Find canonical recursive topology | Planner recursively expands definitions, validates sibling names case-sensitively, and assigns path/route keys | Strong foundation; tighten path segments and compose handoffs here or an owned collaborator |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/domain/team-run-member-identity.ts`; `domain/team-run-config.ts` | Inspect identity/parser and run config | Paths/route keys exist; route-key normalizer collapses separators and strips leading/trailing `/`; run config persists recursive member tree | Public logical-address parser must reject malformed input rather than reuse lossy normalization directly |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/services/member-communication-roster-builder.ts`; `domain/member-team-context.ts`; `services/member-team-context-builder.ts` | Trace Agent-facing logical recipients | Flat roster exposes `local_agent`, `subteam_representative`, `parent_boundary_agent`; context stores recipients and `allowedRecipientNames` | Clean-cut target for replacement; do not keep as parallel authority |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/services/member-team-roster-manifest.ts`; `services/member-run-instruction-composer.ts` | Trace LLM-visible communication protocol | Prompt renders allowed short names, representative badges, and restricted parent recipients | Replace with truthful identity/path protocol, not inline handoff rules |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/backends/mixed/delivery/team-message-recipient-resolver.ts` | Trace actual `recipient_name` resolution | Rebuilds a sender roster independently and exact-matches short name; only child coordinator can reach parent boundary | Second flat policy owner; rooted resolution must become singular |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/backends/mixed/delivery/team-member-delivery-coordinator.ts`; `mixed-team-manager.ts`; `mixed-parent-boundary-delivery-intent.ts` | Trace delivery/event spine | Resolver result flows through recursive member handles; accepted input publishes communication with address-first participants; parent bridge prefixes child sender | Delivery/event infrastructure is reusable if resolver/binding is made root-aware |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/backends/mixed/members/mixed-agent-member-handle.ts`; `mixed-sub-team-member-handle.ts`; `mixed-sub-team-run-factory.ts` | Trace member construction and nested execution | Each child TeamRun strips parent path, gets a parent boundary, and lazy-starts; parent builds coordinator representative data | Design must preserve lazy lifecycle while removing synthetic Agent-facing projection |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-communication/services/send-message-to-dispatcher.ts`; `domain/send-message-target-selector.ts`; `agent-team-execution/services/inter-agent-message-delivery-intent-builder.ts` | Inspect shared tool boundary | Shared dispatcher separates `recipient_name` Team context from exact global run ID; intent binds sender identity | Keep selector separation; change Team logical-address contract only |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-tools/agent-communication/*`; `agent-tools/mcp/providers/send-message-to-mcp-adapter-provider.ts`; provider tool exposure files | Inspect reusable server-owned tool infrastructure | AutoByteus local tool and Codex/Claude Agent Tools MCP adapters share dispatcher; exposure is selected by Agent `toolNames` | Add `get_handoff_rules` through the same capability area and gating policy |
| 2026-08-03 | Code | `autobyteus-server-ts/src/agent-team-execution/services/team-run-metadata-mapper.ts`; `run-history/store/team-run-metadata-{types,schema,store}.ts` | Trace run snapshot persistence/restoration | Recursive topology is strictly parsed, normalized, written on create/restore/refresh; no handoffs are persisted | Add optional canonical handoff snapshot; restore must not re-read changed definitions |
| 2026-08-03 | Data | `applications/*/agent-teams/*/team-config.json` and repository-wide Python scan | Inspect representative Team config data and unsafe member names | Two current application configs plus test artifacts; 10 observed member names, zero unsafe path segments/collisions | No bulk rewrite signal; missing handoffs can normalize empty |
| 2026-08-03 | Doc | `autobyteus-server-ts/docs/modules/agent_communication.md`; `docs/modules/agent_team_execution.md` | Verify documented current contract | Docs explicitly define flat roster, representatives, coordinator-only parent exposure, configured tool gating, and exact-run separation | Durable docs will require update |
| 2026-08-03 | Tests | Relevant `tests/unit`, `tests/e2e/runtime`, and GraphQL test inventory via `rg` | Identify coverage impact | Unit tests assert flat recipients; nested/live runtime and definition roundtrip coverage already exist | Receiving API/E2E engineer must classify and update/replace stale cases |
| 2026-08-03 | Command | `pnpm -C autobyteus-server-ts exec vitest run ... --no-file-parallelism` | Attempt focused current-behavior test run without modifying repo setup | Failed before test collection: `vitest` not found because this fresh worktree has no installed dependencies | Run after receiving team performs normal dependency setup |
| 2026-08-03 | Command | `git fetch origin --prune`; `git status --short --branch`; `git rev-list --left-right --count HEAD...origin/personal` | Re-verify isolated ticket bootstrap before solution design | Dedicated branch still tracked fresh `origin/personal` with divergence `0 0`; only the ticket artifact folder was untracked | No |
| 2026-08-03 | Doc | `solution-designer/design-principles.md`; `references/design-examples.md` Team Run Orchestration example | Apply the shared design authority to the target structure | Design must remain spine-led, keep TeamRun vs AgentRun subjects explicit, and avoid turning a thin dispatcher into the delivery owner | No |
| 2026-08-03 | Code | `agent-team-execution/backends/mixed/members/mixed-persistent-member-registry.ts`; `mixed-sub-team-member-handle.ts`; `mixed-team-manager.ts`; `mixed-parent-boundary-delivery-intent.ts` | Validate a root-aware delivery design against real nested mechanics | Root selectors can already enter a top-level subteam and be stripped one segment at a time; child managers have a parent callback. The clean target can canonicalize once, forward unchanged to root, then reuse downward handles | No |
| 2026-08-03 | Code | `agent-communication/services/send-message-to-dispatcher.ts`; `agent-team-execution/services/inter-agent-message-delivery-intent-builder.ts`; `backends/mixed/delivery/team-message-recipient-resolver.ts` | Verify selector ownership and dead overlap | Public exact-run dispatch already bypasses Team delivery, so Team resolver exact/task-run branches are not the public exact route and can be removed while preserving `GlobalAgentRunMessageRouter` | No |
| 2026-08-03 | Code | `agent-execution/shared/configured-agent-tool-exposure.ts`; AutoByteus tool resolver/factory; `agent-tools/mcp/agent-tool-mcp-catalog.ts`; Codex/Claude tool bootstrap files | Pin down `get_handoff_rules` exposure | AutoByteus requires a bound local wrapper; Codex/Claude use the server Agent Tools MCP catalog. One shared read service plus two thin adapter forms preserves configuration gating | No |
| 2026-08-03 | Code | Every production `new TeamRunConfig(...)` call plus `team-run-launch-identity-assignment.ts`, `mixed-team-run-backend-factory.ts`, and `mixed-sub-team-run-factory.ts` | Trace snapshot propagation and child mount context | Effective handoffs must be copied through every root config reconstruction. Child contexts should inherit a root collaboration binding rather than reread definitions or persist duplicate topology | Implementation audit required |
| 2026-08-03 | Code | `task-team-run-identity-factory.ts`; `mixed-task-team-member-handle.ts`; task delegation context uses | Verify task separation constraints | Task executions already retain logical template identities plus exact task run IDs. Collaboration may project the logical placement but must add no task address aliases or handoff ACL semantics | No |
| 2026-08-03 | Doc | `docs/modules/agent_communication.md`; `docs/modules/agent_team_execution.md` focused roster/representative/metadata sections | Identify durable documentation impact | Docs explicitly describe the flat roster, representatives, coordinator-only parent reachability, provider tool materialization, and current metadata; delivery must update these statements | Delivery follow-up |
| 2026-08-03 | Review | `tickets/in-progress/agent-team-hierarchical-handoffs/design-review-report.md`; `architecture-review-revision-record.md` (`ARCH-REV-001`) | Investigate architecture findings `DR-001` and `DR-002` | Round 1 failed for two design omissions: task-Team ingress ownership after representative removal and provider-visible collaboration code preservation | Resolved in SR-002 design; architecture re-review required |
| 2026-08-03 | Code | `agent-team-execution/domain/member-team-context.ts`; `services/delegation-target-roster-builder.ts`; `agent-tools/task-delegation/task-delegation-context-member-mapper.ts`; `task-delegation/task-delegation-{target,input-resolver}.ts` | Trace the supported Team-target `delegate_task` path before removing representatives | `SubTeamMemberTeamDescriptor.representative` is mapped to `TaskDelegationTeamIdentity.ingress`; the resolver throws `TASK_TEAM_TARGET_INGRESS_NOT_FOUND` when ingress is null | SR-002 proposed a task projection; SR-004 supersedes its address authority with shared placement plus exact current-local mapping |
| 2026-08-03 | Code | `backends/mixed/members/mixed-agent-member-handle.ts`; `domain/team-run-config.ts`; `backends/mixed/mixed-sub-team-run-factory.ts`; `agent-execution/backends/autobyteus/autobyteus-managed-team-context-builder.ts` | Identify a truthful construction source and all task context adapters | Each active Team context has a runtime-identity-attached direct `memberTree`; child factories localize it for the child's task lifecycle; AutoByteus managed context currently remaps generic member descriptors | Share root caller addressing, then map an eligible resolved placement to the exact current-local config/ingress |
| 2026-08-03 | Code | `agent-communication/services/send-message-to-dispatcher.ts`; `global-agent-run-message-router.ts`; `agent-tools/agent-communication/send-message-to.ts`; `agent-tools/mcp/agent-tools-mcp-result-mapper.ts`; `mcp/providers/send-message-to-mcp-adapter-provider.ts` | Trace operation codes through public tool providers | Dispatcher/global router return stable codes, including exact-run codes; AutoByteus returns message-only text and the generic MCP mapper returns message-only content, dropping `code` | Add a collaboration-specific canonical envelope/serializer and switch both collaboration adapters to it |
| 2026-08-03 | Review | `tickets/in-progress/agent-team-hierarchical-handoffs/design-review-report.md`; `architecture-review-revision-record.md` (`ARCH-REV-002`) | Investigate narrowed round-2 `DR-001` | `DR-002` resolved. `DR-001` remains because DS-010 assumed nested coordinator routes were already child-local | Correct the TeamRun topology localization boundary in SR-003 |
| 2026-08-03 | Review | `tickets/in-progress/agent-team-hierarchical-handoffs/design-review-report.md`; `architecture-review-revision-record.md` (`ARCH-REV-003`) | Investigate new `DR-003` after shared task/message placement approval | `DR-001` and `DR-002` are resolved. The proposed common placement leaks full member/Team configs and ambiguously labels a static snapshot run ID as the active owner on task-scoped instances | Narrow the common value to immutable logical coordinates, owner-local pairing, and exact Team ingress only in SR-005 |
| 2026-08-03 | Code | `agent-team-execution/domain/team-run-config.ts` (`stripMemberPathPrefix`); `backends/mixed/mixed-sub-team-run-factory.ts` | Verify nested member/coordinator localization mechanics | Recursive member paths/routes are stripped, nested `coordinatorMemberRouteKey` values are not; factory separately strips only the created child's top-level coordinator key | Replace both partial mechanisms with one recursive subteam-topology localizer |
| 2026-08-03 | Code | `backends/mixed/members/mixed-{sub-team,task-team}-member-handle.ts`; unit/integration uses of `MixedSubTeamRunFactory` | Verify persistent, restore, and task-child entrypoints | Both persistent and task Team handles call the same `MixedSubTeamRunFactory.createOrRestore`; restore supplies runtime context but the factory still constructs config from `subTeamConfig` | One factory-localization owner covers all three lifecycle forms; add three-level tests |
| 2026-08-03 | User clarification | Conversation following SR-003 handoff | Confirm whether task delegation should retain its separate target-name model | User stated that delegating a task and sending a message both select a recipient and therefore must use the same addressing model | Supersede SR-003 task-selector design in SR-004 |
| 2026-08-03 | Code | `agent-tools/task-delegation/task-delegation-tool-{parameter-schemas,input-parsers,manifest}.ts`; `task-delegation/task-delegation-{record,input-resolver}.ts`; `services/delegation-target-roster-builder.ts` | Trace the current public task target language and authority | Tool input is `{target:{kind:"member"\|"team",name}}`; resolver matches `memberName` in a direct-member context; prompt roster advertises flat names | Historical SR-004 replaced this with shared `recipient_name`; SR-009 truthfully renames the shared field to `recipient_address` and retains common kind inference |
| 2026-08-03 | Code | `task-delegation/task-delegation-{service,activation-coordinator,target,address-builder}.ts`; `task-team-run-identity-factory.ts`; `backends/mixed/members/mixed-task-{agent,team}-instance-registry.ts` | Trace post-resolution task execution and eligibility constraints | Task identities already carry route/path and records persist structured conversation addresses, but each service/registry intentionally activates against its current TeamRun's direct runtime contexts | Share address resolution, then preserve current direct-target task eligibility; do not silently add cross-TeamRun task ownership |
| 2026-08-04 | User clarification | Ticket conversation during explicit delivery verification | Decide whether canonical caller/placement data should retain multiple path/address/owner forms | User established that mounting topology deterministically decides one canonical address and every other logical path/owner/route view is derived; requested a principles-led refactor and user review before reviewer handoff | Produce SR-006 and stop for user review |
| 2026-08-04 | User approval and scope decision | Ticket conversation after SR-006 user review | Decide whether to submit SR-006 and whether whole-execution identity normalization belongs in this round | User approved the focused SR-006 collaboration-boundary contraction, authorized architecture re-review, and explicitly deferred the larger whole-TeamRun refactor to a later phase | Submit the cumulative SR-006 package to `architecture_reviewer`; do not expand this round |
| 2026-08-04 | Doc | `solution-designer/design-principles.md` | Reapply shared-structure tightness, ownership, spine, removal, and persisted-data principles | Shared reusable structures must remove redundant parallel representations; topology remains authoritative; derived execution projections must not cross the shared collaboration boundary | Applied in SR-006 |
| 2026-08-04 | Code | `agent-team-execution/domain/member-logical-address-context.ts`; `services/member-team-context-builder.ts`; `agent-tools/task-delegation/task-delegation-autobyteus-context.ts` | Audit the implemented caller address shape and construction seams | Context stores five fields. Factory accepts `memberPath` plus `immediateTeamPath`, derives strings, and checks only relative lengths; the native adapter independently parses both arrays | Replace input/result with `{rootTeamRunId,memberAddress}` and derive every view through the address domain |
| 2026-08-04 | Code | `agent-team-execution/services/resolved-team-logical-placement.ts`; `team-logical-placement-resolver.ts`; `task-delegation-target-mapper.ts`; `mixed-team-manager.ts` | Audit shared placement consumers and minimum required facts | Agent route key is derivable from address; owner Team/local path/route are derivable from subject address; message needs only effective Agent address, and task mapping needs target parent/basename plus Team ingress address | Tighten placement to Agent `{kind,address}` or Team `{kind,address,ingressAddress}` |
| 2026-08-04 | Command | `rg -l "memberPath" autobyteus-server-ts/src \| wc -l`; equivalent commands for `memberRouteKey` and `coordinatorMemberRouteKey` | Assess whether the entire execution topology should be normalized inside SR-006 | Fields appear in 78, 128, and 34 production files and cross run history, events/status, selectors, task identities, message participants, and metadata | Defer whole-execution normalization to a separately investigated ticket; remove the fields only from the new collaboration context/result |
| 2026-08-04 | Code | `run-history/store/team-run-metadata-{types,schema}.ts`; `agent-team-execution/services/team-run-metadata-mapper.ts`; `task-delegation/task-delegation-record.ts` | Determine persisted-data effect of the focused refactor | Caller context and resolved placement are ephemeral. Task record files persist conversation addresses and kind, not `MemberLogicalAddressContext`; TeamRun metadata remains unchanged | `Not Affected`; no migration or compatibility reader |
| 2026-08-04 | Git / downstream artifacts | `git log -6 --oneline`; `git status --short`; implementation/review/API-E2E/delivery reports | Establish current branch state before design rework | SR-005 implementation commits are `2ed26efb9` and `93cc7ed34`; downstream checkpoint is `c3cafa6a4`; delivery-owned doc changes remain untouched | Solution designer edits only authoritative solution artifacts |
| 2026-08-04 | User scope decision | Ticket conversation after the integrated SR-006 delivery checkpoint | Decide whether to retain the previous broad-refactor deferral or pursue the clean end state now | User explicitly selected the larger same-ticket refactor, accepted required data migration when justified, included production frontend/history/SDKs, and asked for user review before reviewer handoff | Produce SR-007 package and stop for user review |
| 2026-08-04 | Git / delivery evidence | `git rev-parse HEAD`; `git rev-list --left-right --count HEAD...origin/personal`; delivery handoff/revision artifacts | Establish the authoritative SR-007 starting state | Reviewed checkpoint is `66b02ae7...`; final delivery refresh recorded seven ahead/zero behind; delivery-owned dirty docs/finalization files predate SR-007 and remain untouched | No implementation or finalization action in solution design |
| 2026-08-04 | Code | `agent-team-execution/domain/team-run-config.ts`; `services/team-run-launch-identity-assignment.ts`; `domain/member-team-context.ts`; mixed runtime contexts/registries/factory files | Inventory static/runtime identity overload and child localization | `TeamRunConfig` combines topology, launch settings, handoffs, generic and kind-specific run IDs; Team `memberRunId` equals `childTeamRunId`; contexts/registries repeat path and route identity; child factories copy and localize trees | Historical SR-007 proposal split persisted topology/profiles/bindings; SR-008 supersedes that split but retains typed IDs and localization removal |
| 2026-08-04 | Code | `agent-team-execution/domain/team-run-member-identity.ts`; collaboration address domain; topology planner/definition graph resolver | Identify the correct owner for canonical absolute address | Current address semantics are collaboration-local while TeamRun path/route helpers normalize equivalent shapes. Definition graph resolution already knows every mount edge | Historical SR-007 proposed a new topology domain; SR-008 instead reuses the existing Agent collaboration address owner and derives the TeamRun index |
| 2026-08-04 | Code | `conversation-target-address` and conversation mapping uses; `token-usage-execution-address`; Team event/status source address uses; task Agent/Team identity files | Compare concrete execution locators | Conversation, token, task, status, and event paths independently encode the same root/task/member placement, often with structural/source/local route variants | Replace with one `TeamExecutionAddress`; keep operation-specific IDs outside it unless they disambiguate execution |
| 2026-08-04 | Code / data | TeamRun metadata types/schema/mapper/store plus one representative `team_run_metadata.json` | Determine whether old metadata is directly usable | Persisted recursive metadata contains independent name/path/route/coordinator and duplicate Team run identity; target strict current model cannot derive a trustworthy address when those values contradict | `Migration Required`; validate all equivalent legacy facts rather than guessing |
| 2026-08-04 | Code | `app-data-migrations` registry/runner/record/backup/atomic-write files; `server-runtime.ts`; Prisma/token-usage migration startup | Inspect available migration mechanics and startup semantics | App-data migration framework already orders, records, backs up, writes atomically, and retries, but runtime currently logs a failed app-data migration and continues. Operational DB migrations already block startup | Add explicit required/blocking migration policy for canonical identity while retaining existing best-effort behavior elsewhere |
| 2026-08-04 | Code | Agent memory scope/path builders; context-file locator, draft, and final storage paths | Separate topology identity from physical storage identity | `teamRunPath` is actually a concrete TeamRun-ID ancestor chain; memory directories must not be addressed by logical topology. Context-file storage segments can be derived byte-compatibly from canonical address | Rename/encapsulate as `ancestorTeamRunIds`; no physical memory or final-context relocation |
| 2026-08-04 | Code | GraphQL TeamRun/history types/converters; REST context-file contracts; WebSocket command/event payloads; production web AgentTeam context/store/task projection code | Trace project contract/frontend identity | API and frontend expose path/route bundles and synthesize scoped task routes; logical topology maps and concrete execution state are conflated | Ship `memberAddress`/`coordinatorAddress`/`TeamExecutionAddress`, recursive `rootTeam`, a derived address index, and execution-address-keyed runtime maps |
| 2026-08-04 | Code | Project-owned application SDK Team context types; application launch/run-binding stores; external-channel bindings; token-usage schema/repositories | Identify non-server structured consumers/stores | SDKs and integrations persist or exchange path/route/generic-run identity and would preserve a competing address authority if omitted | Update contracts atomically; migrate only released external/token stores; rebuild/reset unused project-owned application state directly to target; do not rewrite opaque provider raw arguments |
| 2026-08-11 | User requirement clarification | Direct conversation: application framework has no users and must remain forward-only | Decide whether SR-012's proposed application DB migration and old-bundle compatibility workflow have a real supported cohort | No application predecessor state is supported. Migration/quarantine/upgrade machinery would preserve speculative complexity for a feature that is not used. Exact target validation remains appropriate, but it is ordinary current-contract validation rather than compatibility policy. | Remove application bundle/database migration, predecessor decoder, special quarantine/upgrade workflow, and preservation claims; atomically rebuild all project-owned application artifacts/fixtures/databases to V5/current schema |
| 2026-08-11 | Complete architecture review | `design-review-report.md` ARCH-REV-010; `architecture-review-revision-record.md`; MP-006; MP-007 | Review the complete SR-017 package and test the closed Team status/event boundary against real supported producers | DR-005: initial connection/restore status and pre-run command status overlay bypass the exact assigned domain/wire path. DR-006: UC-019 contradicts the authoritative discard/rebuild application outcome. The remainder of SR-017 passes and must not be reopened. | Close both status spines with the same binding/status/projector owners; correct UC-019; request another complete review |
| 2026-08-11 | Server Team connection/status source | `services/agent-streaming/agent-team-stream-handler.ts`; `team-runtime-snapshot-service.ts`; `team-stream-agent-identity-payload.ts`; `agent-team-execution/domain/team-leaf-agent-status-snapshot.ts`; TeamRun/backend/manager/member status-snapshot interfaces; `run-history/services/team-run-live-projection-service.ts` | Trace the supported Team workspace connection/open/restore status path from server binding to browser | After `CONNECTED`, `sendInitialStatusSnapshot()` obtains persistent, task-Agent, and task-Team-Agent snapshots and maps them directly to generic `ServerMessage`. The current snapshot repeats root TeamRun ID, execution address, Agent ID/name, and task aliases. A task-Team Agent's genuine AgentRun ID is available in its mixed member context/config but is not represented through the target correlated binding. Run-history list projection also strips the wrapper by returning its generic payload. | Replace with `TeamAgentStatusSnapshot {execution,details,statusHint}` built by the one binding/status constructors; project it directly through the same exact Team Agent status projector as live events; give run-history a separate typed list mapper; delete the generic snapshot/identity mapper |
| 2026-08-11 | Server pre-run command-status source | `agent-team-execution/services/member-command-status-overlay-store.ts`; `team-member-command-start-status-events.ts`; `backends/mixed/members/mixed-agent-member-handle.ts`; task Agent/task-Team registries; `tests/unit/agent-team-execution/team-command-start-status.test.ts` | Trace initializing/error feedback when send/delegation targets an Agent whose AgentRun is not materialized | `MixedAgentMemberHandle` already owns the exact execution address and an allocated `agentRunId` before `ensureReady()`. It sends those facts plus display/runtime/task aliases to an overlay store, which builds a generic Agent status payload/event. A later live status deletes the overlay by serialized execution address, but the current code casts the uncorrelated Team event. | Construct the shared binding at the handle/config owner, store only exact status details by private execution key, publish via `createTeamAgentStatusEvent(snapshot)`, and clear it on the first matching correlated live status; do not invent a second parser or fallback |
| 2026-08-11 | Requirements consistency audit | `requirements.md` UC-019, AC-033, persisted-data table; SR-017 application decision | Reconcile the architecture review's Requirement Gap against the governing user clarification | UC-019 alone still described existing application data as converted. AC-033 and the persisted-data matrix already classify application project databases as discard/rebuild and predecessor application input as unsupported. | Limit the conversion cohort to released framework-owned TeamRun/history/communication/task/token/external data and retain application target-only rebuild |
| 2026-08-04 | Command | Repository-wide `rg` inventory for `memberPath`, `memberRouteKey`, `coordinatorMemberRouteKey`, local/source/scoped route variants, `ConversationTargetAddress`, and `teamRunPath` | Define removal surface and a post-change audit seam | The identity is distributed broadly across production server, history, SDK, integration, and web code rather than isolated to collaboration | Use an explicit legacy allowlist limited to migration input/fixtures, definition `memberName`, unrelated filesystem paths, derived storage encoding, and opaque payload keys |
| 2026-08-04 | Design authority | `solution-designer/design-principles.md` plus existing requirements/investigation/contract | Re-evaluate the earlier proportionate deferral after the user expanded scope | Shared-structure tightness, one authoritative boundary, clean removal, and current-schema runtime favor the comprehensive cut now that migration/API/frontend are approved scope | Applied first in SR-007, then corrected by SR-008's cohesive aggregate/derived-view distinction |
| 2026-08-04 | User design review | Ticket conversation reviewing the proposed TeamRun metadata JSON and naming | Test whether SR-007's separated topology/profile/binding sections improve understanding and ownership | User found the separated tables less natural than the original recursive member tree, preferred native `agent_team` and coordinator language, and asked to retain useful existing Agent fields locally while removing redundant logical identity | Supersede SR-007's persisted normalization with SR-008 rooted TeamRun tree; stop before reviewer handoff |
| 2026-08-04 | Code | `run-history/store/team-run-metadata-types.ts`; `agent-team-execution/services/team-run-metadata-mapper.ts` | Reinspect genuine persisted Agent facts and restore derivation | Metadata persists runtime kind, platform/Agent definition IDs, model/config, auto-execute, skill mode, workspace root, and application context per Agent; restore derives `workspaceId` from the root path and storage supplies `memoryDir` | Retain genuine fields on Agent nodes; do not invent `workspaceId`, `memoryDir`, profile, or binding sections |
| 2026-08-04 | Code | `autobyteus-web/stores/teamRunConfigStore.ts`; `autobyteus-web/utils/teamRunMemberConfigBuilder.ts` | Check whether workspace/model/runtime values are structurally per-Team or per-Agent | Primary web launch supplies shared Team-level defaults with per-Agent runtime/model/tool/config overrides, but supported metadata can hold per-Agent workspace roots | Preserve current per-Agent persisted values; avoid hoisting until all launch surfaces and stored data prove equality |
| 2026-08-04 | Code | `agent-team-definition/domain/models.ts`; `services/team-definition-graph-resolver.ts` | Confirm how nested AgentTeams are authored and mounted | Parent Team definitions reference direct members; a nested AgentTeam has its own definition/config. The resolved definition graph supplies deterministic mount edges and parent-visible handoff scope | Definition compiler assigns absolute addresses; parent-authored cross-Team handoffs remain at the nearest parent where both endpoints are visible |
| 2026-08-04 | Design authority | `solution-designer/design-principles.md` | Re-evaluate SR-007 after user feedback | Semantic separation does not require spatial JSON normalization; one authority per meaning and reuse of existing capabilities favor a cohesive aggregate with derived in-memory views | Applied in SR-008 rooted schema, native naming, ownership, spines, migration, and removal plan |
| 2026-08-04 | User naming decision | Ticket conversation comparing `recipient_name`, `recipient_path`, and `recipient_address` | Choose one truthful public field for both recipient-oriented tools | `recipient_name` describes the removed flat roster; `recipient_path` overemphasizes representation/physical-filesystem language; `recipient_address` naturally describes the logical recipient while `/...` and `./...` remain its grammar | Record SR-009: public `recipient_address`, internal `RecipientAddressExpression`, resolved `AgentTeamAddress`, no aliases; stop before reviewer handoff |
| 2026-08-04 | Code | `agent-communication/services/get-handoff-rules-service.ts`; `agent-tools/agent-communication/get-handoff-rules.ts`; `agent-tools/mcp/providers/get-handoff-rules-mcp-adapter-provider.ts` | Reinspect the integrated model-visible rule-query result | Bound service already receives only caller outgoing edges, but reconstructs `{from,to,rules}` and wraps it with `accepted`, `HANDOFF_RULES_RETRIEVED`, generic `message`, `result`, and caller `member_address`; both adapters serialize that machinery | Replace only this query success contract with ordered `{when,recipient_address}` rows; keep send results separate |
| 2026-08-04 | Code | `member-team-context-builder.ts`; `member-collaboration-context.ts`; `member-collaboration-instruction-renderer.ts`; `member-run-instruction-composer.ts`; `configured-agent-tool-exposure.ts`; AutoByteus/Codex/Claude bootstrap/exposure paths | Trace caller binding, tool availability, and system-prompt semantics | Context already filters exact caller outgoing edges. Prompt currently prints caller/immediate-Team addresses and grammar, uses `recipient_name`, offers no concrete filesystem examples, and only suggests optional rule lookup. Exposure depends on package `toolNames`, so a mandatory completion rule could be unexecutable | Make tools intrinsic to Team runtime; render one provider-neutral mandatory completion protocol from canonical caller context |
| 2026-08-04 | User protocol decision | Ticket conversation reviewing LLM needs and the proposed minimal JSON | Decide the acting Agent's exact decision data and lifecycle | The Agent needs only condition plus destination; user approved `{handoffs:[{when,recipient_address}]}` and required `get_handoff_rules` after finishing work, with filesystem-like logical-address context/examples in the system prompt | Record SR-010 across requirements/design/supplements; stop before reviewer handoff |
| 2026-08-04 | Design authority | `solution-designer/design-principles.md` | Apply tight shared structures, truthful boundaries, and executable invariants | A read query should not leak service acknowledgement or redundant identity; a mandatory Team protocol must be owned by Team runtime rather than duplicated in Agent package configuration; operation-specific result shapes are clearer than one generic envelope | Applied in SR-010 projection, intrinsic exposure, prompt spine, removal plan, and verification seams |
| 2026-08-04 | User testing requirement / approval | Ticket conversation after SR-010 review | Fix the required later live validation and decide whether the package may proceed to architecture review | User required one imported nested-classroom example AgentTeam across AutoByteus, Codex App Server, and Claude Agent SDK; GPT-5.6 Luna for AutoByteus; GPT-5.6 Luna with medium reasoning for Codex; optional pnpm import from `$HOME/.autobyteus/server-data/.env`; then explicitly authorized architecture review | Record SR-011 and the live-validation supplement; do not execute providers during design |
| 2026-08-04 | External local fixture | `/Users/normy/autobyteus_org/autobyteus-private-agents/agent-teams/nested-classroom-test/**` | Verify the named test Team's real topology, coordinators, tools, and prose | Root coordinator is `Teacher`; nested Team is `StudentStudyGroup`; nested coordinator is `student_one`; `student_two` is its peer. Source configs have no handoffs and prose still uses removed `{target:{kind,name}}` task input | Stage a test-owned package copy/overlay; never edit source |
| 2026-08-04 | Code | `api/graphql/types/agent-packages.ts`; `agent-packages/services/agent-package-service.ts`; `agent-packages/utils/package-root-summary.ts` | Find the supported import seam | GraphQL `importAgentPackage` accepts `sourceKind` and absolute `source`; `LOCAL_PATH` imports a package root containing `agent-teams/` | Use the public import surface in live validation |
| 2026-08-04 | Code / command | root `package.json` `secrets:import`; `secret-management/cli/import-local-environment-secrets.ts`; file-existence and key-name-only inspection of `$HOME/.autobyteus/server-data/.env` | Validate the user's proposed credential setup without exposing values | The file exists and the importer accepts `--source` plus `--database-url`; required provider assignments are present by identifier. No secret value was copied into artifacts or output | Import only into an isolated test database; redact evidence |
| 2026-08-04 | Code / tests | `runtime-management/runtime-kind-enum.ts`; `autobyteus-ts/src/llm/supported-model-definitions.ts`; existing E2E `reasoning_effort:"medium"` launch configs | Verify exact runtime/model identifiers | Runtime kinds are `autobyteus`, `codex_app_server`, `claude_agent_sdk`; catalog includes `gpt-5.6-luna`; Codex launch config uses `llmConfig.reasoning_effort` | Fix exact AutoByteus/Codex rows; select and record an authenticated catalog-exposed Claude model because user did not name one |
| 2026-08-05 | Review artifact | `code-review-report.md` CRR-022 / CR-F-010 / CR-F-011; `code-review-revision-record.md` | Reinvestigate the failed supported predecessor transition | Real flat display names diverge from structural routes; terminal prerequisite records are skipped; classification is Design Impact and API-REV-010 remains paused at 54% | Revise BEH-015, migration owners/version chain, spines, and coverage before architecture re-review |
| 2026-08-05 | Code | `app-data-migration-runner.ts`; `app-data-migration-registry.ts`; `team-run-metadata-member-tree-migration.ts`; `team-run-member-tree-prerequisite-converter.ts`; `team-canonical-identity-migration.ts`; `team-canonical-metadata-converter.ts`; `server-runtime.ts` | Trace exact execution/record/startup semantics | `runPending()` skips `SUCCEEDED` and `SUCCEEDED_WITH_WARNINGS` by ID; registry orders stable `20260517...` before ticket-owned `20260801...`; current canonical converter rejects display-name divergence; startup gates exact canonical success | Make the later ID the sole final-v3 owner, retain the old ID as pending predecessor-write owner, and share one migration-only flat decoder |
| 2026-08-05 | Historical code | `git show 392669a3...:.../team-run-metadata-{types,store}.ts`; `git show 392669a3...:.../team-run-metadata-mapper.ts`; `/tmp/crr022-pre-ir005-prerequisite-source.ts` | Recover the real predecessor field semantics | Flat writer stored `memberName` from launch/member configuration and normalized `memberRouteKey` independently; store trimmed both without equality; stable converter preserved both and constructed `memberPath` separately | Classify `memberName` as display-only migration input, never structural evidence |
| 2026-08-05 | Fixture/test/log | `legacy-flat-safe-team-run-metadata.json`; `legacy-flat-unsafe-nested-team-run-metadata.json`; `team-run-metadata-member-tree-history.integration.test.ts`; `/tmp/crr022-real-historical-flat-focused.log`; `/tmp/crr022-historical-prerequisite-gaps.log` | Validate safe and unsafe supported history | Maintained safe fixture is `Program Manager`/`program_manager` and `QA Specialist`/`qa_specialist`; unsafe nested flat route cannot reconstruct topology; current implementation rejects the safe fixture and the old output is rejected by canonical conversion | Preserve fixtures; add fresh and terminal-record chains; retain unsafe byte stability |
| 2026-08-05 | Representative operational data | Read-only shape inventory of 475 `$HOME/.autobyteus/server-data/memory/agent_teams/*/team_run_metadata.json` files and migration-record query in `$HOME/.autobyteus/server-data/db/production.db` | Confirm current stored shapes and terminal-record reachability without mutation | All 475 sampled files are predecessor `memberTree`; their observed name/path/route triples agree. The operational record for `20260517_team_run_metadata_member_tree` is `SUCCEEDED`, proving the skip state is real. No `20260801_team_canonical_identity` record exists, consistent with its unshipped ticket-owned status | Use existing later ID; no additional migration ID or runtime fallback is justified |

## Relevant Existing Behavior And Production Paths

BEH-001–BEH-012 retain the integrated SR-006 production baseline; BEH-013–BEH-017 cover the comprehensive rooted identity cut; BEH-018 covers live validation; BEH-019 closes segment lifecycle ownership and its full canonical consumer fan-out. SR-018 closes the two reachable Agent-status producer paths and passed complete ARCH-REV-011. SR-019 adds the accepted common provider-to-consumer lifecycle; SR-020 maps every affected processor/listener; SR-021 removes the product-unreachable runtime-diagnostic branch and makes exact provider turn admission explicit without changing supported product behavior; SR-022 aligns the sole downstream diagnostic and makes Codex omission inheritance exhaustive; SR-023 moves that unchanged policy to the first per-thread native boundary and closes its accepted-value path. IR-014's strict planner result is preserved, and API/E2E remains paused pending solution/architecture/implementation/source review.

| Behavior ID | Kind | Current Supported Trigger Or Governing Contract | Current Production Path And Lifecycle | Meaningful Current Outcome / Invariants | Evidence |
| --- | --- | --- | --- | --- | --- |
| BEH-001 | Contract | File/GraphQL AgentTeam definition read/write | Team config/application-owned parser -> `AgentTeamDefinition` -> service/cache -> GraphQL converter or writer | Integrated SR-006 handoffs and members/coordinator round-trip; missing handoffs normalize empty | Definition domain/providers/GraphQL files plus integrated coverage |
| BEH-002 | System | Team Agent calls `send_message_to({recipient_name:"/..."|"./...",...})` | Runtime adapter -> `SendMessageToDispatcher` -> shared logical placement resolver -> root mixed manager/private handle -> AgentRun input | Integrated address grammar/rooted delivery works; the wire field still carries the misleading flat-roster name | Dispatcher, recipient parser/resolver, delivery coordinator, SR-006 coverage |
| BEH-003 | System | Parent/child nested Team communication | Canonical caller address -> root placement resolver -> configured AgentTeam coordinator or exact Agent -> recursive/private handle delivery | Integrated SR-006 has no communication representative or coordinator-only upward gate; rooted/upward/cross-branch messaging is supported | Integrated source and API/E2E artifacts |
| BEH-004 | Contract | TeamRun launch from nested Team definition | `TeamDefinitionTopologyPlanner` -> recursive `TeamRunConfig.memberTree` -> run identity assignment -> strict child localization -> mixed contexts/handles | Integrated localization is recursively correct, but name/path/route/coordinator-route and copied child-local shapes remain parallel identity | Topology planner, strict localizer, child factory, SR-003/SR-006 evidence |
| BEH-005 | Contract | Configured Team Agent calls `get_handoff_rules()` | AutoByteus/MCP adapter -> `GetHandoffRulesService` -> caller-bound `MemberCollaborationContext.outgoingHandoffs` -> generic communication envelope | Only outgoing edges are selected, but result repeats caller/source and exposes `{accepted,code,message,result:{member_address,handoffs:[{from,to,rules}]}}`; tool may be absent by configuration | Current service/wrappers/adapters and exposure source inspected 2026-08-04 |
| BEH-006 | System | Team member runtime bootstrap/turn | Provider bootstrap -> `composeMemberRunInstructions` -> `renderMemberCollaborationInstruction` | Integrated prompt includes caller/immediate-Team address and `/...`/`./...` grammar, but uses `recipient_name`, lacks explicit filesystem examples, and says only to call rules “when needed” when configured | Current renderer/composer and provider bootstraps inspected 2026-08-04 |
| BEH-007 | Contract | Agent calls `send_message_to({target_agent_run_id,...})` | Shared dispatcher -> `GlobalAgentRunMessageRouter` -> active AgentRun input -> code-preserving AutoByteus/MCP send adapter | Live-only exact ID, no Team resolution/communication projection, stable delivery/rejection codes in the send envelope | Integrated send dispatcher/router/result mapper and downstream coverage |
| BEH-008 | Operational | Create/terminate/restore TeamRun | Topology plan -> config with handoffs -> metadata mapper/store; restore metadata -> config/context -> member restore | Integrated recursive topology, run IDs, and handoff snapshot survive without rereading definitions; parallel identity remains | Team run service/metadata files and restore coverage |
| BEH-009 | Contract | Agent package selects server-owned communication tools | Configured exposure -> AutoByteus bound tool or Agent Tools MCP descriptor -> shared service/dispatcher -> communication-envelope mapper | Integrated provider paths preserve codes and canonical names, but both rule retrieval and delivery use the same generic envelope and availability is package-gated | Configured exposure, wrappers, MCP providers, provider bootstrap files |
| BEH-010 | User | Post TeamRun user message without member selector | `TeamRun.postMessage` -> default target selection -> configured root coordinator -> backend/member | Root coordinator remains default Team entry | `domain/team-run.ts` and integrated tests |
| BEH-011 | Contract | Team member calls `delegate_task({recipient_name:"/..."|"./...",...})`, delegates/reviews, or addresses an exact task AgentRun | Shared recipient parser/resolver -> current-Team direct eligibility -> Agent/AgentTeam task activation -> existing task lifecycle | Integrated message/task placement is shared and AgentTeam target uses configured coordinator; only the public field name and underlying route-bearing execution state remain to change | Task tool/parser/resolver/mapper/activation and SR-006 coverage |
| BEH-012 | Contract / Refactor | Any Team member context construction followed by message or task recipient resolution | Mounted config -> `{rootTeamRunId,memberAddress}` caller -> shared resolver -> Agent `{kind,address}` or AgentTeam `{kind,address,coordinatorAddress}` -> operation consumer | SR-006 collaboration boundary is address-minimal; older TeamRun/runtime/history/event/API/frontend structures still encode the placement with path/route bundles | Integrated checkpoint `66b02ae7...`; implementation/review/API-E2E/delivery artifacts |
| BEH-013 | Contract / Runtime | Create, restore, or lazily materialize a root, persistent child, or task TeamRun | Definition topology planner -> `TeamRunConfig` recursive member tree -> run-ID assignment -> child config copy/localization -> mixed contexts/registries | One placement is represented by name/path/route/coordinator-route; static config and runtime binding are mixed; child-local copies require rebasing and can drift | `TeamRunConfig`, launch identity assignment, mixed subteam factory, contexts/registries |
| BEH-014 | System / Contract | Emit/consume communication, task, status, conversation, token, or WebSocket data for persistent or task execution | Each subsystem constructs its own path/route/task-chain locator and boundary mapper; frontend synthesizes additional structural/source/scoped routes | Equivalent concrete execution identity has several schemas and map-key encodings; same logical address may legitimately have several runtime instances | Conversation/token/task/event/API/frontend identity inventory |
| BEH-015 | Operational | Upgrade/start against fresh flat/legacy-token data or an application-data directory whose stable TeamRun/token migration records are already terminal | Runner skips terminal `20260517...` and historical `20260703_token_usage_execution_address_backfill`; pending `20260801...` consumes predecessor/residual TeamRun input, then strict current task records plus legacy/current token rows. Current source instead still registers revised token code under the skipped old ID and writes planned rows independently. Startup checks only exact `20260801...`. | Historical TeamRun display/structure semantics and IR-014 row reconstruction are now correct. Target token conversion still lacks an executable pending record and one database transaction; without correction strict readers drop old token placement and a later write failure leaves partial conversion. | CRR-025; runner/registry/server gate; pre-ticket/current token migration; planner/index; reviewer probes; read-only operational records |
| BEH-016 | User / Contract | Launch, hydrate, focus, message, delegate, inspect, terminate, or restore a Team through production web or project-owned SDK/integration surfaces | GraphQL/REST/WebSocket -> generated/project SDK shapes -> frontend Team context/task projection or application/external binding store | Route/path bundles cross the boundary and become state keys, preserving a second logical identity beyond the server | GraphQL/converter, WebSocket, SDK, application, external-channel, and web files |
| BEH-017 | Operational | Resolve Agent memory/context storage for a nested persistent or task execution | Memory scope uses `rootTeamRunId + teamRunPath`; context files encode route/path segments in stable directories | The memory lineage is concrete run containment, not topology; stable file layout can remain while public logical selectors change | Memory scope/path builders and context-file stores |
| BEH-018 | Verification | Downstream live AgentTeam validation across supported runtimes | Existing package import, secret importer, runtime catalog, TeamRun launch, provider tool/prompt lifecycle, task/message/history surfaces | No single mandatory current scenario imports the nested-classroom fixture and produces attributable AutoByteus/Codex/Claude evidence under the user-selected model matrix | Fixture/config, package import, runtime/model, secret CLI, and live-test sources inspected 2026-08-04 |
| BEH-019 | System / Contract | Supported provider emits ordinary exact-turn segment output; every default processor/listener receives the resulting AgentRun stream | Provider session exact-turn admission -> source converter -> common queue/pipeline -> current file/lifecycle processors -> AgentRun listeners for memory, compaction, skill, external, application, Team/standalone transport, browser | Current paths disagree on where type/turn/ID/text comes from; file end requires type and repeat start resets context; several consumers manufacture identity/type or end text. Mechanical missing-turn objects are rejected at provider ingress and are not product states. | CRR-076/API-F-024, ARCH-REV-012 DR-007/DR-008, source-log provider-turn/complete-consumer audit |

## Design Health Assessment Evidence

- Change posture: `Comprehensive Refactor` with preserved product behavior and a required persisted-contract migration.
- Candidate root cause classification: `Boundary Or Ownership Issue`, `Duplicated Policy Or Coordination`, `Shared Structure Looseness`, `Legacy Or Compatibility Pressure`, and `File Placement Or Responsibility Drift`.
- Refactor posture evidence summary: SR-006 proved that one minimal canonical address can serve shared recipient resolution. The remaining TeamRun, task, event, history, API, SDK, integration, storage, and frontend structures still model the same logical placement independently. Keeping that split would make collaboration clean only locally while normal runtime and public boundaries continue to authorize route/path identity. The user has explicitly accepted the broader cut and migration cost, so the refactor is required now rather than deferred.

| Evidence Source | Observation | Design Health Implication | Follow-Up Needed |
| --- | --- | --- | --- |
| `TeamDefinitionTopologyPlanner` / `TeamRunConfig` | Truthful recursive paths are already authoritative for run topology | Reuse/tighten existing identity; do not create a separate handoff matrix | Receiving design must name the compiling/resolving owner |
| `MemberTeamContextBuilder` | Builds flat communication recipients and stores many unrelated Team/task/token fields | Current shared structure is too loose for reusable collaboration semantics | Define a tight collaboration sub-contract/composition boundary |
| `TeamMessageRecipientResolver` | Rebuilds the same flat roster at delivery time | Prompt and runtime policy can drift; duplicated resolution authority | One resolver must own canonical logical address parsing/traversal |
| Task tool schema, roster, and `TaskDelegationInputResolver` | Define a second `{kind,name}` direct-member address language over the same Team topology | The ticket would retain two recipient identities if task selection stayed flat | Both recipient-oriented operations must consume one typed logical placement resolver; task policy starts afterward |
| `mixed-sub-team-member-handle.ts` | Parent/child bridge preserves recursive delivery and event prefixing | Underlying traversal can be reused, but projection/authorization semantics must be removed | Preserve lazy Team lifecycle and event truth |
| Definition provider split | Application-owned config has a separate parser/writer | A partial schema addition would silently drop data on one ownership scope | Explicitly cover all readers/writers/API converters |
| Metadata schema | Strict current-format parser rewrites canonical metadata | Run-stable handoffs need explicit snapshot fields | Keep optional field directly usable; no old/new business path |
| Current tests/docs | Explicitly assert short roster representatives | Compatibility retention would keep the wrong public contract | Replace stale assertions/docs cleanly |
| `member-logical-address-context.ts` | One context stores two paths and their two formatted strings | Shared structure looseness; contradictory independently supplied paths remain representable | Store only root run ID plus canonical member address |
| `resolved-team-logical-placement.ts` | Subject address is accompanied by address-derived route and owner fields | Shared result carries redundant parallel identity | Return only kind/address and Team ingress address |
| Run-history/event/task execution identity inventory | Path/route fields cross many older persisted and runtime contracts | Whole-system normalization is a distinct large refactor with persisted-data implications | SR-007 explicitly owns the refactor and migration |
| `TeamRunConfig` plus run identity assignment | Static topology, launch settings, handoffs, generic/kind-specific IDs, and derived leaf projection share one tree | Shared-structure looseness and responsibility drift create duplicate Team IDs and require child copying | Split topology, launch profiles, and runtime binding sets |
| Mixed child factory/localizers/registries | Each child consumes a rebased config and route-keyed registries | Root/local identity shapes are lifecycle machinery rather than domain meaning | Give each TeamRun the rooted snapshot plus `teamAddress`; delete localization |
| Conversation/token/task/event identity types | Equivalent execution position is encoded differently | Cross-subsystem identity has no single owner | Add topology-owned `TeamExecutionAddress` and adapt each boundary |
| Metadata sample and strict target invariants | Old equivalent fields can contradict and current target cannot safely infer which is correct | Direct use would require forbidden guessing or permanent compatibility | Migrate in an isolated blocking boundary and keep runtime current-only |
| App-data migration runner / `server-runtime.ts` | Exact `20260801...` success now gates bootstrap, but revised token semantics are still registered under terminal historical `20260703...` | Record ownership is incomplete even though the public startup boundary is correct | Compose token conversion into independently pending `20260801...`; do not add/reset a record or a second gate |
| Token migration database adapter | Plans are valid before writes, but row updates and column cleanup have separate boundaries/records | The migration owner cannot enforce one canonical row+schema invariant | Replace per-row/drop mutation with one store-owned immutable row+schema transaction, expression-index replacement, and rollback/verification proof |
| GraphQL/SDK/web state | Route keys leave server and key client state; task projections synthesize more | Server-only refactor would violate the authoritative-boundary rule | Change server/generated contracts/project SDK/web atomically |
| Memory/context storage | Physical TeamRun-ID lineage and logical route segments are named similarly | Simplification must not erase a real physical identity | Rename and encapsulate run lineage; preserve physical paths |

## Relevant Files / Components

| Path / Component | Current Responsibility | Finding / Observation | Design / Ownership Implication |
| --- | --- | --- | --- |
| `autobyteus-server-ts/src/agent-team-definition/domain/models.ts` | Team definition/update domain | No handoff edge type/field | Extend with one tight edge shape |
| `.../providers/team-definition-config.ts` | Shared/team-local config normalization/write | Canonical `team-config.json` owner | Likely authoring location for handoffs |
| `.../providers/application-owned-team-source.ts` | Application bundle Team config | Separate schema path | Must remain semantically identical |
| `.../providers/file-agent-team-definition-provider.ts` | File source routing and read/write | Chooses source-specific parser/writer | Do not put validation/orchestration here |
| `.../services/agent-team-definition-service.ts` | Definition create/update boundary | Owns validation coordination | Should enforce edge validity via domain/topology owner |
| `.../services/team-definition-graph-validator.ts` | Recursive reference integrity | Has graph traversal lookup | Candidate extension point or collaborator for authored endpoint validation |
| `.../api/graphql/types/agent-team-definition.ts` | Definition API | Independent input/output types | Add handoff input/output fields |
| `.../services/team-definition-topology-planner.ts` | Run topology compilation | Already walks every nested mount | Natural launch-time composition seam; avoid bloating it without a focused handoff compiler |
| `.../domain/team-run-member-identity.ts` | Internal route-key/path helpers | Normalizes malformed strings permissively | Public address grammar needs a strict owned parser; internal helpers can remain canonical identity support |
| `.../domain/team-run-config.ts` | Immutable launch config/member tree | No effective handoff snapshot | Extend or compose a semantically tight run collaboration snapshot |
| `.../services/member-communication-roster-builder.ts` | Flat Agent-visible recipients | Owns synthetic representatives/parent recipients | Remove/decommission after replacement |
| `.../domain/member-team-context.ts` | Team instruction, communication, task/token binding | Overloaded; stores flat recipients | Separate collaboration contract from Team/task/token concerns |
| `.../services/member-team-context-builder.ts` | Per-Agent Team context construction | Builds flat roster once | Must bind root-aware collaboration snapshot instead |
| `.../services/member-team-roster-manifest.ts` | Prompt flat roster | Advertises synthetic model | Replace with truthful topology/identity rendering or equivalent protocol |
| `.../services/member-run-instruction-composer.ts` | Shared Team member runtime instruction | Correct cross-provider composition owner, wrong recipient model | Extend stable protocol; do not inline rules |
| `.../backends/mixed/delivery/team-message-recipient-resolver.ts` | Sender-specific Team recipient resolution | Second flat roster owner | Replace with strict rooted address resolver |
| `.../backends/mixed/delivery/team-member-delivery-coordinator.ts` | Delivery, input tracing, communication event | Can remain main delivery owner | Feed it resolved real Agent endpoints |
| `.../backends/mixed/members/mixed-sub-team-member-handle.ts` | Lazy child Team lifecycle and recursive delivery | Strips prefix and bridges parent | Preserve lifecycle while removing Agent-facing fake representative |
| `.../services/team-run-metadata-mapper.ts` | Run config/metadata conversion | Definition summary read is separate from run topology | Add handoff snapshot mapping; restoration cannot compile current definitions |
| `.../run-history/store/team-run-metadata-schema.ts` | Strict current metadata parser/normalizer | No optional handoffs | Missing new field can safely normalize empty |
| `.../agent-communication/services/send-message-to-dispatcher.ts` | Shared selector dispatch | Correct public authority for selector separation | Consume a collaboration binding for Team address route |
| `.../agent-tools/agent-communication/*` | AutoByteus server-owned communication tool | Established wrapper/registry pattern | Reuse for handoff retrieval |
| `.../agent-tools/mcp/providers/*` | Codex/Claude/external Agent Tools MCP adapters | Static adapter registry | Add one canonical handoff adapter |
| `.../agent-team-execution/task-delegation/task-delegation-target.ts` | Task-owned member/Team/ingress identities | Already owns typed post-resolution identities and `TaskDelegationTeamIngressIdentity` | Construct these from the shared resolved placement; do not make them an address index |
| `.../agent-team-execution/task-delegation/task-delegation-input-resolver.ts` | Flat task input/name validation and target lookup | Matches caller-supplied kind/name against direct members and rejects null Team ingress | Replace target lookup with common logical placement resolution followed by task eligibility/identity mapping; preserve ingress invariant |
| `.../agent-tools/task-delegation/task-delegation-{tool-parameter-schemas,tool-input-parsers,tool-manifest}.ts` | Public `delegate_task` contract | Requires `{target:{kind,name}}` and describes direct roster names | Replace cleanly with `recipient_address: RecipientAddressExpression` using the shared grammar; remove kind/name, `recipient_name`, and `recipient_path` compatibility |
| `.../services/delegation-target-roster-builder.ts` | Renders direct flat task targets | Advertises `targetName` values as authoritative | Either render canonical absolute addresses from the root topology for discovery or rename/rebuild accordingly; never resolve calls |
| `.../agent-tools/task-delegation/task-delegation-context-member-mapper.ts` | Maps generic Team descriptors into task targets | Converts `representative` to `ingress` | Remove target-list mapping; retain only caller identity projection if still necessary |
| `.../agent-execution/backends/autobyteus/autobyteus-managed-team-context-builder.ts` | Projects server Team context to native AutoByteus context | Re-maps generic target descriptors today | Copy shared caller addressing/task-instance identity only; remove targets and `representative` fallback |
| `.../agent-tools/agent-communication/send-message-to.ts` | AutoByteus send wrapper and parameter contract | Uses the inherited `recipient_name` field and returns message-only success/error strings in the original path | Expose `recipient_address`, reject aliases, and return canonical JSON envelope text preserving dispatcher code |
| `.../agent-tools/mcp/agent-tools-mcp-result-mapper.ts` | Generic MCP operation-result mapping | Drops `AgentOperationResult.code` | Do not use its operation-result branch for collaboration tools; use a dedicated envelope-to-MCP mapper to avoid broad changes to unrelated tools |
| `.../agent-tools/mcp/providers/send-message-to-mcp-adapter-provider.ts` | MCP send adapter | Currently returns a generic operation result | Build the same collaboration envelope as AutoByteus and return matching text/`structuredContent` |
| `.../agent-team-execution/domain/team-run-config.ts` (`stripMemberPathPrefix`) | Current member-tree path localization | Recursively rebases members and requires parallel coordinator-route repair | SR-008 removes copied persistent-child trees and all logical localization; children share the rooted metadata/index plus their absolute Team address |
| `.../agent-team-execution/backends/mixed/mixed-sub-team-run-factory.ts` | Persistent/restored/task child config construction | Calls path stripper and separately strips the top-level coordinator route | Persistent/restored children select an exact `agent_team` node from the shared root tree; task AgentTeams materialize fresh run IDs without changing absolute addresses |
| `.../agent-team-execution/task-delegation/task-delegation-activation-coordinator.ts`; mixed task instance registries | Activate task execution against one TeamRun's direct contexts | Shared root resolution can select a placement beyond current task ownership | Preserve direct-target eligibility after resolution; keep lifecycle/ledger/review semantics and current manager ownership unchanged |
| Provider prompt/tool exposure files | Per-runtime materialization | Current contracts converge through shared composer/exposure but Team tools remain package-gated | Compose intrinsic Team tools after package tools, de-duplicate canonical names, and render one provider-neutral filesystem-like completion protocol |
| `.../agent-communication/services/get-handoff-rules-service.ts` | Caller-bound handoff query | Currently wraps cloned `{from,to,rules}` edges plus caller address in generic success/rejection envelope | Return exact `{handoffs:[{when,recipient_address}]}`; stable filter/flatten order; typed throw only for impossible missing binding |
| AutoByteus/MCP `get_handoff_rules` adapters | Model/provider result projection | Serialize the generic communication envelope | Serialize only the minimal handoff object; use native tool-error channel for internal invariant failure |
| `.../services/member-collaboration-instruction-renderer.ts`; `member-run-instruction-composer.ts` | Provider-neutral Team protocol text | Correct shared seam, but optional/gated lookup wording and no explicit filesystem examples | Render caller address plus mandatory filesystem-like completion/blocked protocol whenever Team context exists; never inline roster/topology/rules |
| `.../domain/member-logical-address-context.ts` | Implemented shared caller coordinate | SR-006 contracts it to `{rootTeamRunId,memberAddress}` | Preserve this logical shape and use the shared `AgentTeamAddress` value |
| `.../services/resolved-team-logical-placement.ts` | Implemented shared message/task placement | SR-006 returns exact Agent/AgentTeam address union and configured coordinator address only | Preserve one identical operation-neutral `{kind,address,coordinatorAddress?}` placement for both tools |
| `.../services/team-logical-placement-resolver.ts` | Root tree traversal | SR-006 derives caller/target views from addresses, but currently traverses the older route-bearing `TeamRunConfig` tree | Retarget it to the derived `TeamRunTreeIndex`; do not expose metadata nodes, configs, run IDs, or handles |
| `.../task-delegation/task-delegation-target-mapper.ts` | Post-resolution task eligibility/current-Team mapping | SR-006 derives parent/basename from addresses, then matches the old direct config | Retain direct-current-Team eligibility; select the exact rooted node by address and let task activation allocate/resolve concrete run facts |
| `.../services/inter-agent-message-delivery-intent-builder.ts`; `backends/mixed/mixed-team-manager.ts` | Sender intent and private delivery endpoint | SR-006 derives private selection from canonical Agent address but underlying registries/config remain route-bearing | Resolve address through the rooted tree/index and private address-keyed registries; keep handles private |
| `.../agent-team-execution/domain/team-run-config.ts`; launch identity assignment | Recursive topology, genuine Agent launch facts, handoffs, runtime IDs, and parallel logical projections | Useful execution locality is mixed with redundant name/path/route and ambiguous generic/kind-specific run identity | Refactor to one `TeamRunMetadataV3.rootTeam` discriminated tree; retain genuine Agent fields locally, typed IDs on nodes, top-level handoffs, and no persisted derived index |
| Existing `agent-collaboration/domain/agent-team-address.ts`; derived `TeamRunTreeIndex` | Shared logical-address meaning and runtime lookup | Existing collaboration capability is already the smallest shared owner; a new topology subsystem would duplicate ownership | Tighten/reuse `AgentTeamAddress`; TeamRun execution owns the derived node index and `TeamExecutionAddress` contextual resolution |
| Definition graph resolver plus TeamRun-tree compiler | Recursive definition reference resolution and launch planning | Existing resolution already has every mount edge and coordinator/member definition | Compile one uniform rooted Agent/AgentTeam node tree with canonical addresses directly; no intermediate persisted path/route or topology/profile/binding join |
| Mixed TeamRun context/manager/member registries | Team lifecycle and runtime handles keyed by localized routes | Persistent child execution needs the same source tree; task Team execution needs fresh IDs but not a local address namespace | Key logical selection by `AgentTeamAddress`; share root tree/index for persistent children and key concrete task executions by task TeamRun ID |
| Conversation target / Team communication mapper | Concrete participant/history locator | Carries structural/source/task route variants | Replace serialized identity with one `TeamExecutionAddress`; presentation is derived |
| Task Agent/Team instance identity types | Task lifecycle and materialization | Repeat definition/name/path/route/template/coordinator facts | Contract to exact task/run/owner IDs only; receiver execution address and bindings own logical placement/concrete-run mapping |
| Token usage execution-address types/schema/repository | Usage attribution/grouping | Independently mirrors task/member path | Persist the shared execution address plus token-specific facts |
| Run-history metadata schema/mapper/store | TeamRun restoration snapshot | Current recursive model persists parallel identity but already keeps genuine Agent execution facts locally | Introduce schema v3 with one uniform `rootTeam` tree and top-level `handoffs`; current parser accepts v3 only and derives indexes after parse |
| `app-data-migrations/*`; `server-runtime.ts` | Ordered app-data file/database migrations and bootstrap | Useful mechanics exist, but failure is currently best effort | Add canonical-identity composite migration and blocking completion policy for supported released predecessor state only |
| External-channel binding store | Durable external target binding | Selector includes old path/route identity | Migrate to canonical member address plus kind-specific run identity |
| Application platform SDK/contracts and launch/run-binding databases | Application-owned Team execution | Persist/transport old route/path and generic member identity, but the feature has no supported users | Update project-owned contracts/artifacts atomically and rebuild/reset test databases directly; add no migration or compatibility path |
| GraphQL/REST/WebSocket types/converters | Public/project transport | Expose parallel path/route bundles | Publish `memberAddress`, `coordinatorAddress`, kind-specific IDs, and `TeamExecutionAddress` only |
| Production web AgentTeam context/stores/selectors/task projections | Topology, execution, focus, tasks, status, history UI | Keyed by route and synthetic scoped variants | Put recursive `rootTeam` plus its private derived address index behind immutable `TeamTopologySnapshot`; put live contexts/focus/lifecycle behind `TeamExecutionState`; derive labels/breadcrumbs through typed queries |
| Agent memory scope/path builders | Physical memory ownership | `teamRunPath` is a TeamRun-ID ancestor lineage, not topology | Rename to `ancestorTeamRunIds`, keep within storage boundary, preserve directory layout |
| Context-file locator/draft/final stores | Stable file access | Uses route/path storage parts | Accept canonical address and derive the identical encoded parts; no data move |
| `autobyteus-server-ts/src/agent-execution/domain/agent-run.ts`; event pipeline/dispatch/queue | First common serialized provider event owner | Owns turn lifecycle state but no segment correlation state; every listener receives its output | Add one run-owned `AgentSegmentLifecycleState`, pass it to a pre-listener transformer, and clear it on terminal lifecycle/termination |
| `autobyteus-ts/.../segment-events.ts`; server AutoByteus/Codex/Claude converters/projectors/trackers | Provider source normalization | Source lifecycle shapes disagree; AutoByteus truthfully emits content/end without type, while other adapters repeat/default type or omit an explicit normalized start | Emit explicit finite typed start from provider-known semantics and minimal content/end; reject unknowns, never infer from ID |
| Team Agent domain/adapter/DTO/projector; standalone Agent mapper | Canonical stream projection | Team requires per-content type but currently receives raw source shape; standalone/browser contract is looser | Consume only canonical post-AgentRun segment events; require exact type/turn; keep Team adapter stateless |
| `application-agent-stream-event-projector.ts` | Standalone/Team application response projection | Both paths filter content by type through separate shapes | Share one pure typed text-delta projection over canonical content; no lifecycle map/default |
| Browser `messageTypes.ts`, `segmentHandler.ts`, `segmentIdentity.ts`, `segmentTypes.ts`, `toolLifecycleHandler.ts` | Segment admission, lookup, presentation | Optional type, guessed text, unknown-to-text fallback, serialized type/ID key, and ID-only mutations create a second authority | Use exact `{turnId,segmentId,segmentType}` identity, required canonical type, typed late subscription, exhaustive factories, and mutation-free mismatch |

## Runtime / Probe Findings
| Date | Method | Exact Command / Method | Observation | Implication |
| --- | --- | --- | --- | --- |
| 2026-08-12 | Static actual-boundary trace | Native AutoByteus `SegmentEvent.start/content/end` -> server converter -> AgentRun queue/pipeline -> `MixedAgentMemberHandle` -> `TeamAgentEventAdapter` -> strict Team DTO -> browser handler | Start has type; converted content is `{id,turn_id,delta}`; Team requires type and rejects; current browser would otherwise guess text. The new API test fabricated type and therefore contradicted the actual converter test. | Put correlation before all listeners at AgentRun; actual-boundary coverage must begin with native content lacking type |
| 2026-08-03 | Script | Repository-wide Python scan of `team-config.json` member names | 10 observed names; none blank, trimmed incorrectly, reserved, slash-containing, or backslash-containing | Proposed path-segment validation has no observed stored-data rewrite need |
| 2026-08-03 | Test attempt | `pnpm -C autobyteus-server-ts exec vitest run tests/unit/agent-team-execution/member-team-context-builder.test.ts tests/unit/agent-team-execution/member-run-instruction-composer.test.ts tests/unit/agent-team-execution/mixed-sub-team-member-handle.test.ts tests/unit/agent-tools/team-communication/send-message-to.test.ts --no-file-parallelism` | Command exited 254 before collection: `Command "vitest" not found`; fresh worktree dependencies are not installed | No behavioral test result claimed; receiving team must install/prepare normally before execution |
| 2026-08-04 | Static construction probe | Read `createMemberLogicalAddressContext` and evaluate its sole relationship guard against `memberPath=['research_team','research_lead']`, `immediateTeamPath=['another_team']` | Length condition passes although the derived member and parent Team addresses contradict each other | The data shape, not another guard, is the root issue; remove the second supplied path |
| 2026-08-04 | Downstream evidence read | Implementation handoff, code review, API/E2E execution report, delivery revision record | SR-005 behavior passed implementation review and broad deterministic coverage before this refactor request | SR-006 acceptance requires preserved behavior plus focused minimal-shape proofs; no claim that SR-006 code has run yet |
| 2026-08-04 | Integrated-baseline evidence read | Architecture review, implementation revision, code review, API/E2E, test re-review, and delivery artifacts through SR-006 | Canonical collaboration behavior and the minimal caller/placement boundary passed review and deterministic suites at the `66b02ae7...` checkpoint | SR-007 is a new design round; preserve those outcomes but do not claim SR-007 execution |
| 2026-08-04 | Static identity equivalence probe | Compared Team member `memberPath`, `memberRouteKey`, Team `coordinatorMemberRouteKey`, generic/kind-specific run assignment, and address derivation | Route key is the address without the leading slash; name and parent path are address-derived; Team generic and child run IDs are assigned the same value | Remove parallel logical fields and generic Team member run identity; keep configured ingress and kind-specific run IDs |
| 2026-08-04 | Static three-level execution probe | Traced root -> persistent child -> nested child and task-child construction through current config localization | Each child receives a copied/localized topology; nested coordinator correction was needed solely because equivalent root/local route shapes coexist | One rooted snapshot plus each run's `teamAddress` eliminates the entire localization class; no fallback is permitted |
| 2026-08-04 | Representative persistence read | Inspected a current recursive TeamRun metadata record and corresponding schema/mapper invariants | Persisted paths/routes/names/run duplicates are real durable fields, not merely TypeScript conveniences | Migration is required; conversion must prove equivalence or fail with backup |
| 2026-08-04 | Startup-policy trace | Followed app-data migration execution into `server-runtime.ts` and compared operational DB migration startup | File/app migration failure is logged and startup continues, whereas DB migration failure blocks | Canonical identity conversion needs a required migration result checked before services listen |
| 2026-08-04 | Frontend keying trace | Followed GraphQL/WebSocket member identity into AgentTeam context maps and task scoped-route projection | Same logical address can have persistent and task executions, while route-only maps also synthesize alternate keys | Keep topology keyed by address; key runtime executions by canonical serialization of `TeamExecutionAddress` |
| 2026-08-04 | Static live-fixture/environment probe | Read the nested-classroom configs/prose, package-import types/service, runtime enum/model catalog, secret-import command, and existing live/E2E launch patterns; no provider was called | The fixture is suitable after a test-owned overlay; the public import and isolated secret seams exist; exact AutoByteus/Codex model settings are supported; Claude must select from the authenticated live catalog | Define a downstream live spine and evidence matrix; make source/operational state immutable |
| 2026-08-04 | Architecture review | `design-review-report.md` ARCH-REV-006 / `DR-004`; `architecture-review-revision-record.md`; validate the then-proposed comprehensive cut against application boundaries | The existing parser/loader would admit a V4 application bundle if SDK shapes changed without a version advance; classification was Design Impact, not Requirement Gap | Retain the exact V5 atomic cut. The later explicit user decision removes application migration and special backward-compatibility/quarantine workflow because no supported application predecessor cohort exists |
| 2026-08-04 | Code | `autobyteus-application-sdk-contracts/src/index.ts`, `manifests.ts`, `application-agent-bindings.ts`, `application-agent-events.ts`; `autobyteus-application-backend-sdk/src/{application-agent-target-address,launch-profile}.ts`; `autobyteus-application-frontend-sdk/src/application-agent-event-validator.ts`; inventory the current application SDK semantic contract and legacy identity-bearing shapes | Backend definition and frontend SDK constants are V4; V4 types carry `targetMemberRouteKey`, `targetMemberPath`, `memberRouteKey`, `memberName`, `teamPath`, and generic run identity | Replace current V4 contract exports/types with canonical V5 shapes; no V4 adapter |
| 2026-08-04 | Code | `autobyteus-server-ts/src/application-bundles/utils/{application-manifest,application-backend-manifest}.ts`; `application-bundles/providers/file-application-bundle-provider.ts`; `application-engine/worker/application-backend-definition-loader.ts`; `application-orchestration/services/application-availability-service.ts`; trace exact application admission, exclusion, diagnostics, and worker-load boundary | File scan already rejects invalid bundles and the worker separately verifies definition version before handlers | Reuse ordinary strict target validation with V5; add no version-specific compatibility/quarantine/upgrade workflow |
| 2026-08-04 | Code/data | `applications/{brief-studio,socratic-math-teacher}` source manifests, backend definitions, build scripts, `dist`, `ui/vendor`, and `dist/importable-package`; repository `rg` version inventory; map every project-owned source and generated/vendored/importable output | Both applications advertise V4 at source and generated copies; build scripts copy SDK artifacts and hard-code compatibility values | Update source/constants/scripts, then regenerate all artifacts atomically and assert consistency |
| 2026-08-04 | Code | `autobyteus-server-ts/src/application-storage/stores/application-platform-state-store.ts`; `application-storage/utils/application-storage-paths.ts`; verify whether durable application DB discovery could be independent of bundle admission | `listExistingPlatformDatabasePaths()` can enumerate `<appData>/applications/*/db/platform.sqlite` independently of catalog membership | Capability exists but is not used by this ticket: the later user decision classifies unused application DBs as rebuild/reset, not migration inputs |
| 2026-08-04 | Code | `autobyteus-application-sdk-contracts/src/application-iframe-contract.ts`; frontend/server/web iframe-version use; decide whether the iframe transport must advance with the SDK identity cut | Bootstrap/ready envelopes contain application/transport facts but no Team member route/path identity; iframe V4 is independently versioned | Retain iframe protocol V4; frontend SDK semantic compatibility advances to V5 |
| 2026-08-05 | Built/reviewer evidence read | `/tmp/crr022-historical-prerequisite-gaps.log` and `/tmp/crr022-real-historical-flat-focused.log` | Current IR-012 returns `FAILED` on the maintained safe fixture; pre-IR-005 predecessor output is rejected by exact-name canonical conversion | The source behavior matches CR-F-011; no new executable claim is made by solution design |
| 2026-08-05 | Read-only operational inventory | Python shape scan of 475 TeamRun metadata files; SQLite select of `app_data_migration_records` | 475 files are predecessor `memberTree`; stable `20260517...` is terminal `SUCCEEDED`; later `20260801...` is not recorded | Terminal-predecessor upgrade is product-reachable and the existing later migration ID is executable |

## External / Public Source Findings

- Public API / spec / issue / upstream source: None consulted.
- Version / tag / commit / freshness: N/A.
- Relevant contract, behavior, or constraint learned: N/A.
- Why it matters: This is an internal domain/runtime change; the fresh repository code and user-approved product contract are authoritative.

## Reproduction / Environment Setup

- Required services, mocks, emulators, or fixtures: None started for solution design. Later live coverage must stage the existing nested-classroom fixture and use real AutoByteus, Codex App Server, and Claude Agent SDK lifecycles under the dedicated contract.
- Required config, feature flags, env vars, or accounts: No credential was imported or provider called during investigation. Later coverage may import `$HOME/.autobyteus/server-data/.env` with `pnpm secrets:import` into an absolute isolated database.
- External repos, samples, or artifacts cloned/downloaded for investigation: None. The existing local agents package was read-only evidence.
- Setup commands that materially affected the investigation: Dedicated worktree creation only; later command shape is recorded in [nested-classroom-live-validation-contract.md](./nested-classroom-live-validation-contract.md).
- Cleanup notes for temporary investigation-only setup: None. No dependency install, secret import, staged package, database, TeamRun, or provider process was created during design.

## Findings From Code / Docs / Data / Logs

### 1. Recursive topology is present but not the Agent logical-address authority

`TeamDefinitionTopologyPlanner.buildSkeleton` recursively assigns each Agent and child Team a `memberPath` and slash-joined `memberRouteKey`. `TeamRunConfig` stores the recursive tree and derives a flat leaf list only as a non-authoritative projection. Nested Team handles already traverse and strip route prefixes at child boundaries.

This makes the desired filesystem analogy an evolutionary correction: use the current truthful topology for Agent logical addressing instead of inventing a separate graph.

### 2. Flat recipient projection is an explicit workaround

`MemberCommunicationRosterBuilder` exposes:

- direct Agents as `local_agent`;
- child coordinators as `subteam_representative`; and
- selected parent Agents as `parent_boundary_agent` only for the child coordinator.

`MemberTeamContextBuilder` stores this list and `allowedRecipientNames` for prompt use. `TeamMessageRecipientResolver` rebuilds the same sender-specific roster at delivery time, then exact-matches `recipient_name` against it. The duplicated build creates two coordination points and prevents a truthful cross-branch namespace.

### 3. Synthetic projection is not required for delivery mechanics

The mixed runtime already has recursive Team/member handles. Parent-to-child delivery enters a `MixedSubTeamMemberHandle`, strips the top segment, and posts to the child run. Child-to-parent delivery prefixes the actual child Agent sender path before it enters the parent delivery coordinator. Communication payloads use concrete participant addresses.

The implementation challenge is therefore root-aware resolution/binding, not inventing a new message transport.

### 4. Definition schema ownership is distributed across explicit source adapters

Shared/team-local definitions and application-owned definitions do not use one identical parser/writer. GraphQL also has independent input/output types and conversion. A handoff field added only to the domain or shared config would be lost on update through another surface.

### 5. Handoff rules need a launch snapshot

Active AgentTeam dynamics are outside Ticket 1. The effective edge graph depends on recursive mounting: a child definition's `/research_lead` may become `/research_team/research_lead` in its parent run. That effective graph must be compiled after topology resolution and stored with the run. If restoration re-reads definitions, an old run could silently receive new rules or broken endpoints.

### 6. A strict public address parser is required

Current internal route-key normalization replaces backslashes, collapses repeated slashes, and strips leading/trailing separators. Those behaviors are useful for some internal canonical identities but would silently reinterpret malformed model input. The Team logical-address contract must have a strict parser that preserves the semantic difference between `/`, `./`, and invalid bare/parent-traversal forms.

### 7. Tool infrastructure can be extended without provider-specific business logic

`send_message_to` is registered as a server-owned AutoByteus tool and a static Agent Tools MCP adapter. Codex and Claude materialize the shared MCP descriptor and normalize provider names back to canonical tool names. `get_handoff_rules` should follow that established path and use a bound collaboration context rather than provider sessions as the rule owner.

### 8. Existing data supports no-migration addition

Observed Team configs use path-safe names and omit handoffs. The normal version-agnostic definition reader can map absence to `[]`. Current recursive TeamRun metadata can accept a new optional snapshot and map absence to `[]`; no existing field needs reinterpretation or transformation. This is `Directly Usable — No Migration`, not a reason to rewrite every file.

### 9. Existing durable coverage has both reusable and stale parts

Reusable coverage areas:

- AgentTeam definition GraphQL create/update/file roundtrip;
- nested mixed Team launch/delivery/restore;
- all-runtime `send_message_to` matrix;
- AutoByteus/Codex/Claude tool gating and prompt projection;
- TeamRun metadata schema/mapper/history;
- exact-run selector tests.

Stale coverage areas after the clean cut:

- `MemberTeamContextBuilder` assertions for visible subteam representatives;
- parent recipients only for the child coordinator;
- flat `allowedRecipientNames` roster manifests;
- nested E2E calls using bare coordinator/parent names.

The later `api_e2e_engineer` must produce its own formal coverage investigation before editing/executing durable coverage.

### 10. Root-canonical delivery can reuse the current parent callback and downward handle mechanics

The child `MemberTeamContext` currently constructs a child-local sender identity, and `mixed-parent-boundary-delivery-intent.ts` prefixes that identity while walking upward. That rewrite exists to support the flat parent-boundary projection. A focused member collaboration binding can instead construct the caller's root-canonical path and root TeamRun ID before dispatch. Non-root managers can forward that intent unchanged through their existing parent callback until the root manager is reached. At the root, `MixedPersistentMemberRegistry.resolveContext` already returns the top-level subteam wrapper for a nested selector, and each `MixedSubTeamMemberHandle` already strips one segment while delivering downward. The target design therefore needs one root resolver, not a new transport.

### 11. The Team resolver's exact-run/task-run branch is not the public exact-run route

`SendMessageToDispatcher` returns immediately to `GlobalAgentRunMessageRouter` when the selected target is `target_agent_run_id`; only `recipient_name` reaches `buildInterAgentMessageDeliveryIntent` and Team delivery. No production builder found in the search constructs a Team intent with an exact-run selector. The exact/task/recovery branches inside `TeamMessageRecipientResolver` are overlapping unreachable policy for the public tool path and should be removed when the Team intent is tightened to a logical address subject. This does not change exact-run behavior.

### 12. Runtime snapshot propagation has several explicit reconstruction seams

`TeamRunConfig` is reconstructed by topology planning, launch identity assignment, metadata restore, `AgentTeamRunManager`, `MixedTeamRunBackendFactory`, and child TeamRun creation. An effective handoff field added only to the initial planner would be silently dropped. The implementation must thread the root snapshot through every root config reconstruction and give lazy child contexts an inherited root collaboration binding containing root ID, Team mount path, and caller-filtered outgoing edges. Persisting a second member topology is unnecessary and would create an overlapping authority.

### 13. Task-Team ingress must be split from communication before representative removal

The synthetic `SubTeamRepresentativeDescriptor` is overloaded. Communication uses it to advertise a child coordinator as a fake parent member, while task delegation maps the same data to the legitimate `TaskDelegationTeamIngressIdentity` required to start a delegated task Team. `DelegationTargetRosterBuilder` also reads it for task guidance, and `TaskDelegationInputResolver.resolveTeamTarget` rejects a Team target without ingress.

The authoritative source already exists independently of communication rosters: the root `TeamRunConfig.memberTree` identifies every placement, while each active owning TeamRun has a canonical local config containing its direct Team coordinator and runtime IDs. The common address resolver can return a typed root placement plus the owning-Team route. Task delegation then maps that result to `TaskDelegationMemberIdentity` or `TaskDelegationTeamIdentity`, requiring a real direct Agent coordinator for a Team target. Task guidance, native AutoByteus context, and MCP task adapters need caller/root addressing identity, not a copied flat target list or communication representative.

### 14. Historical SR-002 finding: stable collaboration codes stopped at provider adapters

`SendMessageToDispatcher` and `GlobalAgentRunMessageRouter` already return `AgentOperationResult` with codes such as `DELIVERED`, argument failures, grant failures, and `TARGET_AGENT_RUN_NOT_ACTIVE`. `AutoByteusSendMessageToTool._execute` reduces the result to a human-readable string. `SendMessageToMcpAdapterProvider` returns an operation result that `AgentToolsMcpResultMapper.toolResultFromOperationResult` reduces to text plus `isError`. Both paths discard `code`.

SR-002 correctly required the send path to preserve dispatcher/global-router codes, and integrated SR-006 now does so. It also applied the same `{accepted,code,message,result}` wrapper to `get_handoff_rules`. SR-010 supersedes only that read-query projection after user review: send delivery still copies exact codes, while successful rule retrieval exposes only `{handoffs:[{when,recipient_address}]}`. Missing Team binding is prevented by intrinsic Team materialization and remains a typed internal/provider tool error rather than normal guidance.

### 15. Historical SR-003 finding: child TeamRun localization was incomplete below one nested boundary

`stripMemberPathPrefix(memberConfigs, prefix)` recursively slices every descendant `memberPath` and derives a new `memberRouteKey`, but the Team branch spreads the original `coordinatorMemberRouteKey` unchanged. `MixedSubTeamRunFactory` separately calls `stripRoutePrefix` only for `input.subTeamConfig.coordinatorMemberRouteKey`, the coordinator of the child being created. It never repairs nested Team entries inside `childTree`.

For a root config containing `/research_team/field_team/field_lead`, localizing `/research_team` produces the Agent route `field_team/field_lead` while the nested `field_team.coordinatorMemberRouteKey` can remain `research_team/field_team/field_lead`. Exact current-local task mapping then correctly refuses the inconsistent tree. A fallback that attempts both shapes would create two route authorities and mask invalid configs.

SR-003 correctly placed the then-proportionate correction in one TeamRun-config localization boundary rather than task delegation. That localizer was required for the SR-006 baseline and remains valid historical evidence that parallel root/local identity is costly. SR-008 supersedes the mechanism itself: root, persistent child, and restored TeamRuns share one rooted metadata/index and select by absolute Team address; task AgentTeams materialize fresh run IDs while preserving the selected subtree's absolute addresses. No localizer or alternate route shape remains.

### 16. `delegate_task` currently duplicates the recipient identity model

The public task schema and parser require `{target:{kind:"member"|"team",name}}`. `TaskDelegationInputResolver` then matches that flat name against `TaskDelegationContext.members`, and `DelegationTargetRosterBuilder` teaches the model those direct-member names. By contrast, the approved message contract resolves `/...` or `./...` against the full collaboration tree. Both operations first answer the same domain question—“which Agent or Team placement is the recipient?”—so retaining the task form would leave a second address authority and force callers to know a target kind the topology already owns.

The clean target is one common recipient resolver returning Agent `{kind:"agent",address}` or AgentTeam `{kind:"agent_team",address,coordinatorAddress}`. `send_message_to` maps that result to ordinary Agent delivery; `delegate_task` first preserves current direct-member/current-TeamRun eligibility, then maps an eligible result to task-owned identity and activation. Task-specific eligibility occurs after shared resolution and does not change address meaning.

Current task activation is scoped to a `TeamRun`: `TaskDelegationService` holds a run-bound ledger and calls `TeamRun.startTaskAgentInstance` / `startTaskTeamInstance`, while mixed task registries search only that run's direct member contexts. The user clarified addressing, not cross-TeamRun task ownership. The proportionate target therefore lets the common resolver identify any root placement, then requires the resolved placement's owning Team path to equal the caller's immediate Team path. Non-direct/deeper/cross-branch placements receive a task-specific eligibility error without name fallback. Historical task records already store structured `ConversationTargetAddress` and receiver kind rather than the live flat selector, so this public input cleanup does not itself require record migration.

### 17. SR-005 finding: the proposed shared placement must not become a config or lifecycle carrier

`ARCH-REV-003` confirmed the common resolver and current-TeamRun task policy, then identified a boundary contradiction in the proposed return type. `ResolvedAgentPlacement.memberConfig` and `ResolvedAgentTeamPlacement.teamConfig` would expose complete root snapshot configs—including provider, workspace, runtime, and recursive child settings—through `TeamRun.resolveLogicalPlacement`, even though that facade exists specifically to prevent task tooling from reading TeamRun config. The proposed `owningTeamRunId` is also not a truthful active-owner identity when a task-scoped TeamRun instantiates the same logical Team placement under a distinct run ID.

Neither operation needs those config or lifecycle values. Root message delivery already owns the config, registry, and handle lookup behind `MixedTeamManager`; after shared resolution it needs only the selected logical Agent address or configured Team ingress address. Task delegation obtains task runtime IDs, role/description, Team definition ID, and coordinator materialization identity from the caller's **current** canonical local config.

SR-005 therefore narrowed the shared value to immutable logical coordinates and omitted all config objects, member/run IDs, provider/runtime settings, handles, and lifecycle identity. That was the correct response to `DR-003`; §18 records the later post-implementation evidence that the remaining route/owner coordinates are themselves derivable and should now be removed by SR-006. The enduring conclusion is one resolver and one identical placement shape without either operation consuming the other's state.

### 18. SR-005 still models one canonical placement more than once

The SR-005 implementation proves that the resolver boundary can remain config-independent, but its shared values are not yet minimal. `MemberLogicalAddressContext` stores a canonical member string, its segment array, the parent-Team string, and the parent-Team segment array. Its factory reduces risk by deriving the two strings, yet still accepts two independent arrays and checks only their relative lengths. It does not require `immediateTeamPath` to be the exact prefix of `memberPath`, so contradictory caller identity remains constructible.

The shared placement repeats the same issue one layer later. For an Agent, `memberRouteKey` is the canonical address without its leading slash. For any non-root subject, `owner.teamPath` is `parentAddress(subject)`, `localMemberPath` is `[basename(subject)]`, and `localMemberRouteKey` is `basename(subject)`. These values do not add domain meaning; they create parallel representations that every constructor and consumer must keep synchronized.

The principles-led SR-006 correction was a contraction, not a new abstraction:

- `MemberLogicalAddressContext = {rootTeamRunId, memberAddress}`;
- `ResolvedTeamLogicalPlacement = {kind:"agent",address} | {kind:"team",address,ingressAddress}`;
- strict address-domain operations own `segments`, `basename`, `parentAddress`, and root selector derivation;
- the resolver derives addresses from its topology traversal cursor rather than copying stored config paths/routes;
- message delivery derives the private selector from the selected effective Agent address; and
- task policy compares `parentAddress(target)` to `parentAddress(caller)`, then performs one exact direct current-local config match by the derived basename and resolved kind. Team ingress must be a direct Agent whose canonical parent equals the Team address.

This does not make an address replace the topology. Syntax derives structural views; topology lookup still determines existence, Agent-versus-Team kind, configured Team ingress, and execution config. `rootTeamRunId` remains necessary because the same canonical logical address can exist in multiple concurrent TeamRun snapshots. Exact AgentRun/task identities remain separately owned.

SR-008 retains that boundary with native target naming: `ResolvedTeamRecipient = {kind:"agent",address} | {kind:"agent_team",address,coordinatorAddress}`. Stable pre-existing machine codes containing `INGRESS` remain byte-compatible, but no target field/type uses that synonym.

The larger `TeamRunConfig`/metadata/event route system predates this shared collaboration boundary and is materially wider. Static inventory shows 78 production files mentioning `memberPath`, 128 mentioning `memberRouteKey`, and 34 mentioning `coordinatorMemberRouteKey`. Those fields participate in history lookup, status/event payloads, conversation segments, task identities, memory/token scopes, and current metadata. Removing them may be a valuable future topology normalization, but it requires separate behavior/persistence investigation. SR-006 makes them non-authoritative for collaboration without pretending to solve that broader system.

### 19. The comprehensive scope remains authorized, but SR-007's persisted normalization is superseded

After SR-006 was integrated, the user explicitly chose to perform the larger refactor now and accepted migration/API/frontend work when it produces a substantially cleaner codebase. The formerly deferred surfaces form one cross-project identity concern. During user review, however, the separated `MountedTeamTopology + TeamAgentLaunchProfile + TeamRunBindingSet` metadata was judged harder to understand than the original recursive execution tree. That feedback is consistent with the design principles: semantic responsibility can be separated in runtime code without spatially normalizing one cohesive JSON aggregate.

The current classification is:

1. **Logical placement:** one absolute `AgentTeamAddress` decided by definition mounting.
2. **Concrete execution:** kind-specific AgentRun/TeamRun/task run IDs; the same address can have multiple executions.
3. **Configured AgentTeam entry:** one direct `coordinatorAddress`; address syntax alone cannot derive which child is configured.
4. **Agent launch facts:** current runtime/model/tool/skill/workspace-root/application/platform fields, local to the Agent node they configure.
5. **Physical containment:** ancestor TeamRun-ID lineage inside memory storage; it is not logical topology.

Display breadcrumbs, basenames, parent Teams, relative recipient expressions, route selectors, URL components, and storage segments are derived values. Definition-local `memberName` remains authoring input before mounting, not mounted identity.

### 20. The original recursive member tree has useful locality but the wrong identity fields

The original `TeamRunConfig.memberTree` naturally answers which Agent or nested AgentTeam exists and keeps each Agent's launch/restoration facts next to that Agent. This is operationally valuable for inspection, mapping, backup, migration, and restore. Its problems are narrower and structural:

- `memberName`, `memberPath`, and `memberRouteKey` encode one placement three times;
- `coordinatorMemberRouteKey` creates a second coordinator route shape;
- generic `memberRunId` changes meaning by node kind and duplicates `childTeamRunId` for Team nodes;
- the root Team uses a wrapper shape different from nested Team nodes;
- persistent child execution copies/localizes the tree; and
- derived leaf/route maps are treated as state rather than rebuilt views.

SR-008 retains the recursive locality and makes the root a normal `agent_team` node at `/`. Agent nodes keep genuine Agent fields and `agentRunId`; AgentTeam nodes keep definition ID, `teamRunId`, `coordinatorAddress`, and `children`. Handoffs remain top-level edges. No separate persisted topology/profile/binding tables or derived index are required.

### 21. Current storage evidence supports local Agent facts, not speculative hoisting

The current metadata types persist `runtimeKind`, `platformAgentRunId`, `agentDefinitionId`, `llmModelIdentifier`, `llmConfig`, `autoExecuteTools`, `skillAccessMode`, `workspaceRootPath`, and `applicationExecutionContext` per Agent. The restore mapper reconstructs `workspaceId` from `workspaceRootPath`; it does not persist `workspaceId`. Storage derives `memoryDir`; it is not metadata.

The primary web launch surface supplies shared Team defaults and per-Agent runtime/model/tool/config overrides. That does not prove that all supported launch surfaces and historical records always have the same `workspaceRootPath`. The evidence-backed target therefore keeps existing genuine fields locally, does not invent a generic `agentProfile`, does not add `workspaceId` or `memoryDir`, and does not hoist workspace until a separate investigation proves a durable equality invariant.

### 22. Concrete execution location still needs one owner distinct from logical address

Conversation, Team communication, token usage, task identity, status/event projection, WebSocket commands, and frontend task keys independently encode a root Team run plus task-Team/member/task-Agent position. Collapsing those values to `memberAddress` would be incorrect because persistent and task instances may occupy the same logical placement concurrently.

One `TeamExecutionAddress` therefore carries:

```ts
type TeamExecutionAddress = Readonly<{
  rootTeamRunId: string;
  taskTeamRunIds: readonly string[];
  memberAddress: AgentTeamAddress;
  taskAgentRunId: string | null;
}>;
```

This is not a second logical address. `memberAddress` selects the logical node; the root/task run IDs select one concrete instantiation. Team execution owns contextual resolution through root metadata and task identities. Invocation IDs, task ledger IDs, event IDs, and provider handles remain with their operation owners.

### 23. Persisted identity cannot be simplified safely by a tolerant normal reader

Representative metadata contains independent path, route, name, coordinator-route, generic run, and kind-specific run facts. A tolerant reader could project them only by selecting one value when they disagree. That would silently rebind history and keep historical schema knowledge in normal runtime.

Framework-owned structured stores are classified separately:

- Team definitions: directly usable; authored local names mount into addresses.
- TeamRun metadata, communication/task records, token usage, and released external bindings: migration required. Unused application launch/run stores are rebuilt/reset directly to the target and are not predecessor inputs.
- Derived history indexes/caches: discard and rebuild.
- Physical Agent memory directories and stable context files: directly usable at their current paths.
- Opaque provider raw arguments: directly usable as untyped display/history payloads, never routing input.

The existing app-data migration capability supplies ordering, completion records, backups, atomic file writes, and retries. It must gain an explicit required/blocking outcome checked before service bootstrap/listen. Normal parsers support only schema v3 after successful conversion.

### 24. Public and frontend boundaries must change in the same cut

GraphQL, REST, WebSocket, project-owned application SDKs, external/application binding schemas, and production web state currently carry route/path fields. If only server internals changed, callers would reconstruct the removed authority at the next boundary.

Target transport fields are deliberately small: `memberAddress`, `coordinatorAddress` when an AgentTeam entry relation is exposed, kind-specific run IDs, and `TeamExecutionAddress` for concrete execution. Generated contracts, SDKs, server, and production web land atomically with no mixed-version support. The frontend puts one recursive `rootTeam` and its private derived address index behind `TeamTopologySnapshot`; `TeamExecutionState` privately keys persistent/task runtime state by serialized execution address so equal logical addresses do not collide.

### 25. Complete simplification removes parallel meaning, not useful locality or independent facts

The user's intuition that many fields are redundant is correct for logical placement. It does not imply that a canonical address replaces concrete runtime instance identity, the configured AgentTeam coordinator, Agent launch settings, or physical storage ownership. The final schema is simpler because each retained field answers one distinct question:

- `address` answers **where is this node in the mounted AgentTeam**;
- `agentRunId` / `teamRunId` answer **which persistent execution was allocated**;
- `coordinatorAddress` answers **which direct Agent coordinates this AgentTeam**;
- Agent-local launch fields answer **how this Agent is restored or launched**; and
- `ancestorTeamRunIds` answer **which concrete run directories contain memory**.

The four TeamRun questions—what mounted, how configured, which handoffs, and which run IDs—are a schema completeness test, not four persisted normalized sections. Runtime may derive address, run, coordinator, and direct-child indexes, but those are disposable views of the rooted aggregate.

### 26. Public tool naming should express domain meaning, while the type expresses resolution state

The implemented `send_message_to.recipient_name` name came from the flat roster, where the value really was a short name. Under hierarchical collaboration, values such as `./field_team` and `/research_team/field_team` are no longer names. Keeping the field would make the public contract lie about its data.

`recipient_path` describes the filesystem-like notation accurately, but overemphasizes representation and can be confused with physical filesystem paths or the legacy `memberPath` identity being removed. The operation itself sends or delegates to a logical recipient location, so `recipient_address` is the most natural public name.

The clean type progression is:

```text
recipient_address: RecipientAddressExpression
  -> strict /... or ./... parse and caller-relative resolution
  -> AgentTeamAddress
  -> exact rooted-tree lookup
  -> ResolvedTeamRecipient
```

This keeps the API natural without weakening type precision. Both recipient-oriented tools must change together. `recipient_name` and `recipient_path` must be rejected rather than translated, while exact `target_agent_run_id` remains a separate message-only selector. This is a current API/tool-manifest cut and does not require a TeamRun metadata transformation; raw historical provider payloads remain display-only.

### 27. Handoff retrieval is an action projection, not a service-status report

The current caller binding has already done the important authorization/scope work: `MemberTeamContextBuilder` stores only edges whose `from` equals the current canonical Agent address. Returning `member_address` and repeating `from` therefore adds no decision information. Returning `accepted`, a success code, generic count prose, and a `result` wrapper exposes the service/adapter protocol rather than the Agent's next action.

Each authored edge answers a durable graph question and correctly remains `{from,to,rules[]}`. The tool should not mutate that stored model. It projects each rule string to a separate decision row:

```text
{from, to, rules:[r1,r2]}
  -> {when:r1, recipient_address:to}
  -> {when:r2, recipient_address:to}
```

Stable edge order and rule order make the result deterministic. `recipient_address` is directly reusable by `send_message_to`; `when` tells the LLM how to evaluate the natural-language text. `{handoffs:[]}` truthfully means there is no configured outgoing action. No additional wrapper is needed for success.

The lifecycle instruction and tool availability must form one invariant. If every Team Agent must retrieve guidance before final completion or blocked termination, package-selected exposure is structurally invalid: the prompt can require a tool that is absent. Team runtime must therefore compose `get_handoff_rules` and `send_message_to` intrinsically after resolving package tools. Standalone non-Team Agents remain under normal configuration. Provider adapters and prompt timing remain thin variants of this one Team-owned protocol.

### 28. Live validation needs a test-owned environment spine, not production shortcuts

The user-selected fixture is valuable because it exercises a root coordinator, an AgentTeam recipient, a nested coordinator, and a nested peer. Its external source is not current-contract-ready: prose still tells `Teacher` to call the removed `{target:{kind,name}}` selector, and there is no handoff graph. Editing that package would violate scope and make test evidence mutate shared input. A temporary package containing a copied fixture plus a recorded current-contract overlay keeps the public `LOCAL_PATH` import path real while isolating compatibility text and deterministic handoff data.

Credential preparation has the same ownership rule. The local `.env` is input to the repository secret importer, not a file to source into logs or copy into fixtures. Importing it into a disposable database makes runtime credential resolution real without changing the user's normal database. One fresh TeamRun per runtime avoids cross-provider state/history confusion. Since the user requires all three runtimes, unavailable credentials, models, or processes are evidence of Blocked/Fail rather than a passing skip.

The live spine is additive to deterministic coverage. It proves provider tool exposure, prompt timing, model-visible handoff shape, delivery, task lifecycle, and restoration through real runtime seams; it does not replace exhaustive migration/API/frontend negative cases. Exact setup, matrix, assertions, evidence, and cleanup are normative in [nested-classroom-live-validation-contract.md](./nested-classroom-live-validation-contract.md).

### 29. Application SDK is a direct target contract, not a migration or compatibility subsystem

The project already distinguishes three independent version domains. Application manifest schema is V4, backend bundle envelope is V1, and iframe transport is V4. Those envelopes did not acquire a different shape in the canonical identity refactor. The breaking contracts are the backend definition and frontend SDK because their public launch/binding/target/event types contain the removed Team route/path identity. Their target version is therefore V5 while the unchanged envelope/transport versions remain stable and refer to the V5 SDK compatibility values.

The target admission spine reuses ordinary current-contract ownership rather than inventing negotiation. `parseApplicationManifest` requires `ui.frontendSdkContractVersion === "5"`; `parseApplicationBackendManifest` requires both SDK declarations equal `"5"`; and `ApplicationBackendDefinitionLoader` requires `definitionContractVersion === "5"` before exposure validation, lifecycle hooks, or handlers. All project-owned source, generated SDK output, vendor copies, importable packages, fixtures, and fresh test databases move together. A non-target package fails the exact parser/loader like any other invalid input. No old payload is translated, no incompatible worker is imported, and no version-specific compatibility, quarantine/upgrade, or reinstall workflow is added.

The user confirmed that the application framework has no supported users. Therefore `ApplicationPlatformStateStore.listExistingPlatformDatabasePaths()` is not a migration inventory for this ticket even though the capability exists. Pre-ticket application databases are unsupported/disposable; project-owned fixtures/databases are regenerated or reset directly in the current schema. `20260801...` contains no application bundle/database item, current repositories contain no predecessor decoder, and startup does not preserve an unused application database merely because it can be discovered.


### 30. Legacy TeamRun `memberName` was display data; the later migration ID is the executable canonical owner

The previous design made a false equivalence. Historical flat TeamRun metadata wrote `memberName` from the configured display/member label and independently normalized `memberRouteKey`; the store validated their types but never required equality. The stable flat-to-memberTree migration preserved that distinction. `Program Manager` plus `program_manager` is therefore not a contradiction and must not be “repaired” by overwriting the display value.

The structural contract is narrower and deterministic. Flat v1 has one direct structural `memberRouteKey`; it cannot encode nested topology and any nested route/hybrid nested-Team fields remain unsafe. The predecessor recursive tree has structural `memberRouteKey` and `memberPath`; after separator/trim normalization, those two must encode the same ordered segments, each node must be a direct child of its recursive parent, addresses must be unique, the coordinator route must resolve to one direct Agent, and generic/kind-specific Team run IDs must agree. `memberName` may differ arbitrarily as display text and is not compared to the address basename. The v3 target intentionally omits it; canonical display derives from the address basename plus retained role/description and other target-owned presentation.

Migration record lifecycle determines ownership. `runPending()` treats `SUCCEEDED` and `SUCCEEDED_WITH_WARNINGS` as terminal for a migration ID. Therefore `20260517_team_run_metadata_member_tree` remains the stable pending flat-to-predecessor record owner; no corrected design expects it to rerun after a prior success. The separately registered, later `20260801_team_canonical_identity` migration is the sole final-v3 owner. Both migration definitions reuse one pure migration-only flat decoder, so the canonical migration can consume a repaired residual flat item left under a terminal-warning record in memory and write only final v3. It is ticket-owned/unreleased and absent from the inspected operational records, so this covers all supported histories without inventing a third version or requiring a GraphQL retry after startup has been blocked:

```text
fresh flat -> pending 20260517 converts/preserves -> pending 20260801 derives from route/path -> v3
terminal 20260517 + predecessor tree -> 20260517 skipped -> pending 20260801 derives from route/path -> v3
terminal-warning 20260517 + residual safe flat -> 20260517 skipped -> 20260801 shared-decodes in memory -> v3
```

Historical interpretation remains migration-only. Normal v3 repositories never accept flat/memberTree input, and no alias, lazy conversion, or dual reader is introduced.

### 31. Target token conversion needs the pending canonical owner and one transactional store boundary

The old token migration is not an executable target owner. The production predecessor registered `20260703_token_usage_execution_address_backfill` as required on startup and serialized the legacy `{segments}` shape. `AppDataMigrationRunner.runPending()` treats both success statuses as terminal by ID. Current IR-014 source changes the converter under that same ID, so a normal predecessor start followed by upgrade yields zero executions of the corrected converter. A read-only operational snapshot confirms exactly that lifecycle: the old ID is `SUCCEEDED`, the ticket-owned `20260801_team_canonical_identity` record is absent, and roughly 139k Team token rows still carry `{segments}`.

The smallest coherent target is not another migration record or a second cleanup phase. `20260801_team_canonical_identity` is already independently pending, already owns the cross-store canonical identity aggregate, already runs after TeamRun/task conversion, and is already the sole exact-success pre-listen gate. It therefore composes one token canonical item that owns both semantic conversion and physical removal of every obsolete Team identity column/index. The old `20260703...` semantic definition and both narrow column-drop definitions are removed from the current registry; their durable records are neither deleted, reset, nor reinterpreted. Pre-existing token model/provider backfills are attempted before canonical contraction but are not canonical prerequisites because the established runner continues after their failure. This produces one target owner, transaction, and startup decision rather than parallel migration/cleanup/gate authorities.

The migration-local database boundary must also express the approved invariant directly:

```text
list rows + build strict task-Team index
  -> plan and validate every row without mutation
  -> immutable canonical update batch
  -> TokenUsageCanonicalIdentityMigrationStore.applyCanonicalTeamIdentityTransaction(...)
  -> one Prisma/SQLite transaction updates/verifies every targeted address,
     removes obsolete columns/old root index, creates/verifies canonical JSON-root index
  -> commit all, or throw and roll back all row/schema changes
```

A planner/index failure never calls the transaction method. A transaction failure reports zero migrated rows/columns, returns the enclosing `20260801...` result as `FAILED`, and leaves strict services/listen closed. Because Prisma migrations deploy first, target schema/client omission must not be paired with a physical pre-conversion drop migration; the migration-local raw store alone removes predecessor inputs after planning. After repair, the non-terminal canonical ID reruns; already-current file/database items skip, the token transaction commits, and only exact canonical success opens startup. No current token reader accepts `{segments}`, and no runtime fallback is introduced.

### 32. TeamRun source discrimination currently does not correlate payload type

`TeamRunEvent` stores `eventSourceType` beside an independent `TeamRunEventData` union, so a source can legally pair with the wrong payload. Task delegation erases four already-typed domain payloads to `unknown`; the mapper repairs this with casts. Agent/member-input receiver identity is also repeated between outer and inner structures. The correct boundary is one closed correlated event union with one receiver/execution authority per variant and a second correlated task subtype union.

### 33. The Team WebSocket has no single current contract owner

Server `ServerMessage` accepts `Record<string,unknown>`, Team egress consumes it, and the browser parser validates only a string `type` before casting. Frontend task DTOs then accept camel/snake aliases and arbitrary fields. API-F-015 proves that browser mocks can satisfy a field contract the real producer does not. A dedicated browser-safe workspace contract package is justified because it owns a real cross-process boundary: exact strict server/client DTOs, runtime schemas, and serialization. It owns transport spelling only; server domain remains topology/routing authority.

### 34. Frontend topology and execution answer different questions

The rooted frontend topology describes logical placement plus effective launch configuration; all persistent/task Agent and AgentTeam run bindings belong to concrete execution. Current `TeamRunNodeBase` mixes both, and `teamTaskExecutionTree` clones topology into task nodes with empty `agentRunId`/nested `teamRunId` placeholders. That makes invalid state representable and lets projection code mutate topology. Canonical metadata also does not own the active/inactive resume fact or dynamic Agent hydration, so a builder cannot truthfully invent a complete execution context from metadata alone. The target uses one all-or-nothing builder over a closed launch/open input: the single canonical disk aggregate, exact lifecycle from launch/resume, initial logical Agent focus, and one discriminated fresh/loaded/historical-unloaded identity-free Agent seed per metadata Agent. It produces run-ID-free immutable topology plus five valid execution variants: persistent Team, persistent Agent, task Agent, task AgentTeam root, and concrete Agent inside a task AgentTeam. Seeds supply workspace/dynamic state without repeating metadata-owned run/config/application facts. The root persistent Team derives its ID from its execution address rather than repeating it; only child persistent Team execution stores the distinct child TeamRun binding.

### 35. One frontend owner must encapsulate execution keys and lifecycle

Thirty production consumers touch `agentExecutionsByKey`; eleven parse serialized JSON keys. Streaming, task projections, GraphQL restore, history/navigation, focus, presentation, status/timeline, and terminal cleanup mutate mutually visible structures. The authoritative-boundary rule requires one `TeamExecutionState` aggregate. Its private index may serialize addresses, but callers pass typed `TeamExecutionAddress` and receive subject-specific immutable summaries, navigation rows, or Agent-context entries—not the internal concrete union/map. Each task execution needs only its task ID; one private task projection can supply active status/timeline and query-derived history instead of copying the snapshot into execution and another history collection. Live activation and restored durable records enter the same materialization transition; later result-submission/review signals request complete refresh rather than creating partial projections. The task store remains durable record authority.

### 36. Latest personal selection and canonical focus are complementary

The only base-merge conflict showed two independent UI facts: workspace selection answers which root TeamRun is open, while `focusedExecutionAddress` answers which execution inside it is focused. The correct current-row predicate requires both exact selected TeamRun and exact execution-address equality. Neither may replace the other; the `TeamExecutionState` owns focus while the workspace selection store owns selected subject.

### 37. A pre-launch Team draft is not a concrete Team execution

The supported launch UI currently holds editable configuration in `teamRunConfigStore`, copies it into a context, constructs `temp-team-*` as a TeamRun ID, synthesizes member run IDs, creates AgentContexts/conversation keys, and later parses/rebases those identities after server allocation. That would keep both a duplicate config and provisional execution inside the new “valid concrete” model and preserve a cross-index promotion transaction. The cleaner boundary is one deep-readonly `TeamLaunchDraft`: configuration, member-address focus, and pending input only, edited through one draft-store owner. The existing definition catalog remains the definition-topology authority and supplies a read-only derived view for draft rendering/address validation; the draft does not copy it. Workspace selection discriminates draft ID from real TeamRun ID. Successful launch builds `TeamTopologySnapshot`/`TeamExecutionState` from real server IDs and transfers the pending focus/input once; failure preserves the draft. No draft ID enters run, execution, stream, history, token, or conversation identity.

### 38. Generic standalone Agent input needs one Team ingress adapter, not a generic Team exception

The standalone Agent event subsystem remains broad and is used outside Team streaming, but the Team member bridge is already the semantic boundary that adds Team execution identity. Letting its generic payload enter `TeamRunEvent` would leave the same invalid discriminator/payload combinations inside the supposedly corrected domain. The bridge must instead verify the raw AgentRun ID, exhaustively map the raw event into a correlated `TeamAgentEvent`, and publish only that type plus a correlated execution binding. Persistent and task-Agent bindings carry only the exact address because their AgentRun identity is already metadata/address-owned. An Agent inside a task AgentTeam must retain the verified AgentRun ID once in its binding because `TeamExecutionAddress` deliberately omits that member-run fact and the frontend cannot materialize the concrete `task_team_agent` without it. The ID never remains in event details. The adapter also filters derived `INTER_AGENT_MESSAGE`/`TEAM_COMMUNICATION_MESSAGE` Team events because canonical `COMMUNICATION` plus `MEMBER_INPUT` already own communication projection and recipient transcript delivery.

### 39. Team approval tokens are dead metadata, not a contract to preserve

Repository source contains no server producer or consumer of `approval_token`. The frontend alone defines `ToolApprovalTokenPayload`, stores an `unknown`, sends it through `as any`, and tests create values that do not match the declared shape. Promoting this into the new strict protocol would turn an illusion into permanent API. Team approval/denial already has the necessary concrete identity in `invocation_id` plus required `execution_address`; retain only the invocation-to-address UI tracker and remove the token type/map/payload/casts/fixtures.

### 40. The bound Team stream already owns root scope; control messages should carry only changing facts

`/ws/agent-team/:teamRunId` resolves and binds one server session to one TeamRun before it registers egress and sends `CONNECTED`. Every lifecycle snapshot on that connection comes from the same bound run. The current `CONNECTED.team_id` and `TEAM_RUN_LIFECYCLE.team_run_id` therefore repeat scope rather than disambiguate it. The frontend does not consume the connected Team ID at all; it marks `isSubscribed` from low-level socket open, while the lifecycle handler compares the repeated ID against the context and ignores mismatches that the server binding already makes unreachable.

The clean boundary separates transport establishment from application readiness. Socket open means only that a WebSocket exists. Exact `CONNECTED {session_id}` means the server successfully bound that socket to the endpoint-selected TeamRun and is ready to stream its snapshot; `TeamStreamingService` privately owns that session/readiness fact. Exact `TEAM_RUN_LIFECYCLE {is_active}` then updates only the root persistent-Team execution of the already-bound context. Removing the repeated TeamRun IDs establishes one scope authority without weakening correlation, and strict surplus-field rejection prevents the old shapes from persisting as aliases.

### 41. Application producer address is concrete execution binding, not topology configuration

The recursive v3 disk aggregate deliberately keeps `ApplicationExecutionContext` beside the Agent because it is one inspectable/restoreable run snapshot. That persistence locality does not decide the frontend projection owner. V5 `ApplicationExecutionProducer` contains `executionAddress`, so retaining the whole context on `TeamTopologySnapshot` would hide concrete TeamRun/task identity inside a model described as run-ID-free and make the topology change when the same Agent placement is materialized for a task.

The context shape itself has one compile-time owner: V5 `@autobyteus/application-sdk-contracts` exports exact `ApplicationExecutionContext {applicationId,bindingId,producer}`, and the server application-domain model becomes an alias rather than a second declaration. Current TeamRun metadata/browser code accepts only object-shaped `Record<string,unknown>`; the target server metadata mapper and `TeamRunFrontendProjectionBuilder` must instead map every exact field, validate the producer through their existing canonical execution-address capabilities, reject missing/surplus/invalid values, and return the shared type. The SDK contract package does not gain a second logical-address resolver.

The same execution-binding invariant has two process-local enforcement boundaries, not two policy owners. On the server, `MixedAgentMemberHandle` already owns concrete AgentRun construction and already derives the handle's exact persistent/task execution address. Its application-context projection must validate that a persistent Agent's stored producer matches that address and, for a task Agent or Agent inside a task AgentTeam, clone only the stable application ID, binding ID, display name, and producer runtime kind while replacing the producer address before `AgentRunConfig` is constructed. This makes published artifacts and application event/stream attribution truthful at their source; a later consumer must not repair it.

On the browser, `TeamRunFrontendProjectionBuilder` separates the same mixed persisted field during its existing all-or-nothing projection. Stable logical/effective launch facts enter topology. The typed application context enters the paired concrete Agent execution and must carry that record's exact address. Persistent Agents validate the stored producer address; task Agent and task-Team-Agent records apply the same stable-assignment/exact-address rule. The disk schema stays cohesive, while server runtime attribution and frontend state each have one truthful concrete-execution authority.

### 42. Task delivery receiver and task execution root are distinct identities

`TaskDelegationRecord` intentionally records two different facts. `receiverAddress` is the endpoint that accepted delivery: the persistent/base Agent address for an Agent target, or the coordinator ingress inside a newly materialized task AgentTeam. Once activation succeeds, `taskRun.address` is the concrete execution root created for the delegated task: it adds the task Agent run ID for an Agent target, or identifies the task AgentTeam root rather than its coordinator for an AgentTeam target. Current `TaskDelegationEventPublisher` publishes `receiverAddress` as the outer task-event execution identity, and current browser task projection then reconstructs the task root from payload details. That makes delivery and execution compete as authorities and is the precise duplication SR-016 must remove.

The target keeps both facts only where their semantics exist. The durable task record retains `receiverAddress` as an operation-owned delivery fact. The three nonduplicated live task event variants publish the non-null `taskRun.address` as their one outer `executionAddress`, so no consumer synthesizes a concrete task identity. The GraphQL/task-record mapper requires that address and validates the separate delivery fact against the immutable topology: for an Agent target, removing `taskAgentRunId` from `taskRun.address` must equal `receiverAddress`; for an AgentTeam target, `receiverAddress` must be the configured coordinator ingress within the task Team represented by `taskRun.address`. It then emits one flat minimal `TeamTaskProjection` containing concrete execution address plus normalized task facts. Parentage is derived in the private execution graph from that address plus topology and is never copied into the projection. This is one authoritative identity per concern, not a fallback that guesses between address shapes.

### 43. Accepted task activation must precede runtime events from the new execution

The current activation spine starts/posts the task Agent or task AgentTeam before `TaskDelegationService` activates/persists the ledger record and publishes `TASK_DELEGATION_ACTIVATED`. `MixedAgentMemberHandle.postMessage()` synchronously emits the initializing status before it even ensures the AgentRun, and a task AgentTeam forwards child events as soon as its child run is subscribed. Therefore an exact task-Agent or task-Team-Agent event can reach the browser before the event/record that creates its parent task execution. Rejecting that real event would lose supported status/transcript output; materializing its missing parent from the child event would recreate synthetic inference; a frontend timeout buffer would move activation sequencing into transport.

The target fixes ordering at the producer authority. `TaskDelegationService` serializes only the short activation critical section per TeamRun. After fresh task IDs and exact `taskRun.address` are known but before starting/posting work, task delegation opens one bounded TeamRun event barrier for that exact task execution subtree. The prepared runtime accepts the work packet behind a closed execution gate: it may emit initialization events, but the model work and task-originated operations cannot run or create nested task state before parent activation. The TeamRun publication boundary queues only events whose Agent/member-input/communication/task identity belongs to the new subtree; unrelated Team events continue normally. If runtime start or durable activation persistence fails, the barrier and work gate are aborted and the fresh execution is terminated/settled, so no task activation or child event is published. On success, task delegation durably writes the privately staged active record, synchronously promotes the ledger/directories, publishes `TASK_DELEGATION_ACTIVATED` directly as the matching barrier-opening event, releases the queued events FIFO immediately afterward, and only then opens work execution. A count/byte overflow fails activation rather than dropping or partially publishing. One per-TeamRun activation sequencer prevents overlapping barrier leases without serializing the tasks themselves. This bounded sequencing mechanism owns no task state, address resolution, WebSocket retry, or frontend recovery and is removed when the activation attempt finishes.

### 44. Task event repetition and dual task read models are unnecessary

The durable `TaskDelegationRecord` already stores one complete task fact set: task ID/status, sender/receiver, content/references, concrete `taskRun {address,startedAt}`, ordered submission/review updates, and `createdAt`. `getTaskRecordUpdatedAt()` deterministically derives the monotonic update time, `deriveTaskLabel()` proves the label is a presentation projection of content/task ID, and `isTaskDelegationTerminalStatus()` proves terminal is a function of the finite status. The current publisher nevertheless repeats sender, label, description, references, target, delegator, arguments, execution, current/previous status, derived latest/pending/update IDs, and terminal flags on later events, and activation uses a new `Date()` rather than the record's exact start time. `TaskDelegationService` then publishes `TASK_DELEGATION_STATUS_UPDATED` only immediately after publishing the corresponding submitted/reviewed result, making two browser events for one transition. Keeping those fields/events in the new strict contract would preserve redundant authorities rather than merely type the old bag.

The tighter producer contract makes activation the only full base event because target ordering guarantees its active record is already durable. It publishes task ID, distinct sender, content/references, and the record's exact `createdAt` plus `taskRun.startedAt`; the event type implies `active`, and the initial projection derives `updatedAt = startedAt` just as the record does. Result-submission/review events carry only their event-specific IDs/decision/time and request the complete GraphQL record. `publishStatusUpdated` and `TASK_DELEGATION_STATUS_UPDATED` are removed because the service calls them only immediately after those same two result transitions. The frontend never creates a partial timeline from result events.

The same rule removes a second frontend duplication. A task Agent/task Team execution stores only its task ID. One private `TeamTaskProjection` per ID owns content/status/timeline and remains after terminal execution cleanup, so active views and history rows derive from the same value. Complete GraphQL responses are staged and merged monotonically because records are append-only: newer rows replace, older input preserves the current projection, equal-time disagreement rejects the whole candidate, and a known task omitted by a query that began before concurrent activation is retained rather than interpreted as deletion. Terminal subtree cleanup additionally requires a terminal candidate for every currently materialized descendant in that same response. This is the minimal truthful model: one durable store, one browser projection, and multiple derived views.

### 45. Synthetic task instances and parallel token identity columns are derived duplicates

The task record already owns `taskId`, delivery receiver, and—after activation—one exact concrete `taskRun.address`. Current `TaskAgentInstanceIdentity` adds `taskAgentInstanceId = "task_agent_" + taskId`, then copies the task Agent run ID, owning TeamRun ID, task ID, and a new creation time. `TaskTeamInstanceIdentity` does the same with `taskTeamInstanceId = "task_team_" + taskId`, task TeamRun ID, parent TeamRun ID, task ID, and another time. Member context, start requests, ledgers, directories, settlement, work packets, status, Team stream, tool routing, notifications, and tokens then clone or re-encode those objects. This is not independent domain meaning: task ID answers which task, `TeamExecutionAddress` answers which concrete execution, the Agent runtime owns the actual Agent run ID where it is not encoded by the address, and the task record owns time.

The target therefore has one derived in-memory discriminated view only:

```ts
type ActiveTaskExecutionBinding = Readonly<
  | { kind: "task_agent"; taskId: string; executionAddress: TeamExecutionAddress }
  | { kind: "task_team"; taskId: string; executionAddress: TeamExecutionAddress }
>;
```

It is reconstructed from the active task record and is never persisted. A task-bound member context retains nullable `taskId` only because no-argument `submit_task_result` needs that operation fact; its normal Team context already owns the exact execution address. Registries and settlement query by task ID; concrete message/status/runtime resolution queries by `TeamExecutionAddress`; standalone exact-Agent operations retain the actual `agentRunId`. Provider-visible delegation remains `{task_id,status[,message]}`. No generic `task_context`, synthetic instance alias, copied owner/parent/run/time bundle, or separate activation-result run-ID field replaces the deleted structures.

The token ledger has the same duplication in relational form. `execution_address_json` already contains root TeamRun, ordered task-Team chain, logical member, and optional task Agent run. `run_id` is the actual metered AgentRun, and `task_id` is the optional task-operation fact. Writable `root_team_run_id`, `member_agent_run_id`, `task_agent_instance_id`, and `task_agent_run_id` therefore restate that identity and can drift. Current root-Team lookup is a query concern, not a second identity authority. The target removes all redundant Team/path/route columns and the old root-column index, then adds `token_usage_ledger_events_execution_root_observed_at_idx` over `json_extract(execution_address_json,'$.rootTeamRunId'), observed_at`. Current repositories query through canonical JSON; only the migration-local planner/store reads predecessor columns.

Because Prisma migrations deploy before app-data migration, the implementation must not physically drop those input columns in a new Prisma migration. Historical migrations still create/preserve them on fresh and predecessor databases; target `schema.prisma` and generated clients simply omit them. Pre-existing token model/provider backfills are attempted first. The provider-name migration-local database deliberately reads `SELECT *`; classification receives only its semantic fields, while preservation sorts the discovered physical column names, excludes only `provider_name`, and compares every remaining value before/after. It therefore names no removable identity/display column and can retry after contraction. The independently pending `20260801...` token item then performs address writes, column/index replacement, and complete verification in one transactional SQLite boundary. This is a clean migration cut, not backward-compatibility code in current runtime.

### 46. Initial Team connection status is a supported projection source, but it is not a TeamRun event

`AgentTeamStreamHandler.connect()` binds a real TeamRun, emits `CONNECTED`, and immediately calls `sendInitialStatusSnapshot()`. `TeamRuntimeSnapshotService` obtains `TeamRun.getLeafAgentStatusSnapshots()` and currently maps each snapshot through `team-stream-agent-identity-payload.ts` into generic `ServerMessage`. The mixed manager includes offline unmaterialized persistent Agents and recursively includes active task Agents and Agents inside task AgentTeams. This is therefore a product-supported source for all three Agent execution kinds, including the task-Team Agent whose genuine AgentRun ID is owned by its `MixedAgentMemberContext`/fresh task-Team config.

The correct target is not to manufacture a `TeamRunEvent` merely to reuse the event mapper. A connection snapshot describes current state rather than a newly published domain event. The shared domain value is instead:

```ts
type TeamAgentStatusSnapshot = Readonly<{
  execution: TeamAgentExecutionBinding;
  details: TeamAgentStatusDetails;
  statusHint: AgentRunStatusHint;
}>;
```

One `createTeamAgentExecutionBinding({executionAddress,agentRunId})` constructor classifies and validates persistent Agent, task Agent, or task-Team Agent. The task-Agent form requires `executionAddress.taskAgentRunId === agentRunId` but does not repeat the ID; the task-Team form requires a non-empty task-Team chain and retains that genuine AgentRun ID once; the persistent form requires neither task selector and transports no run ID. One status-details constructor removes Agent name/runtime/Team/task/identity duplication. Persistent/task/task-Team handles and the config-backed offline path return only the immutable status snapshot. `TeamRuntimeSnapshotService` then calls the same exact Team Agent status projector used by the correlated live-event `AGENT_STATUS` arm, followed by the same strict serializer. `TeamRunLiveProjectionService` maps the snapshot to its own typed list/history DTO rather than reusing a generic stream payload or recreating duplicate identity.

### 47. Pre-run initializing/error feedback is a real event and can use the same snapshot without a generic builder

`MixedAgentMemberHandle.postMessage()` and `deliverInterMemberMessage()` publish initializing feedback before `ensureReady()`. That is supported behavior because Agent creation/restoration can take time or fail. The handle already has everything needed before materialization: a canonical `TeamExecutionAddress`, the allocated context `agentRunId`, and current status. For an Agent inside a task AgentTeam, the fresh child TeamRun/context owns the genuine member AgentRun ID; no projection-time lookup or persistent/local route guess is necessary.

The handle constructs the same `TeamAgentExecutionBinding` once and supplies it to `MemberCommandStatusOverlayStore`. The overlay stores only `TeamAgentStatusDetails` by a private canonical execution key, combines those details with the supplied binding into `TeamAgentStatusSnapshot`, and publishes through one narrowly named `createTeamAgentStatusEvent(snapshot)` domain constructor. That constructor produces the correlated `TeamRunEvent` Agent/`AGENT_STATUS` variant and is shared with no connection-snapshot path. When the first matching real correlated Agent status arrives, the store removes the overlay by the event's typed binding; status projection then follows the identical live event -> projector -> serializer -> parser -> frontend Agent-status transition. Display name, runtime kind, TeamRun ID, raw Agent ID, task instance, and repeated execution address are absent from status details.

### 48. The application migration mismatch is documentary, not a new rollout behavior

The user-authoritative rule, persisted-data table, AC-033, and SR-017 all agree that predecessor application bundle/database state is unsupported and project-owned state is directly rewritten or rebuilt. Only UC-019 retained older language saying application data was converted. Correcting UC-019 to name the actual released migration cohort—TeamRun/history/communication/task/token/external—closes DR-006 without changing migration code, adding compatibility, or weakening ordinary exact V5 target validation.

### 49. Segment type is a lifecycle projection, not a provider-content or Team-local fact

- Native evidence establishes one start-owned type and later ID-correlated deltas. Requiring every provider to repeat the type directly would make providers conform to a downstream DTO instead of preserving truthful source semantics.
- Team ingress is too late: the same canonical AgentRun event also feeds standalone WebSocket, application streaming, memory/history, and other listeners. A Team-local map would duplicate lifecycle and still leave those consumers inconsistent.
- Browser removal of content type is also wrong because a listener can join after the server start. Without a self-contained canonical content event, the browser would need another state/replay authority or would misclassify non-text segments.
- The first common serialized owner is `AgentRun`. A run-owned state machine can validate exact `(run,turn,segment)` identity, derive content type once, surface a non-terminal diagnostic, and clean state with existing turn/run lifecycle. Downstream repetition of type is then a read projection with one source, not a second authority.
- Current source also proves explicit provider-normalization seams for missing normalized starts: Claude text segment creation and Codex reasoning-block creation already own segment birth. They should emit start before content; unknown provider item kinds must not default to text.
- Root-cause classification: `Boundary Or Ownership Issue` plus `Shared Structure Looseness`; refactor required now.

### 50. The canonicalization barrier must include every current processor/listener

`getDefaultAgentRunEventPipeline()` runs transformers before processors, then lifecycle finalization, then listener dispatch. This makes the accepted first-transformer location the correct atomic cut, but it also means every later consumer must accept only canonical facts.

- `FileChangeEventProcessor` currently records a fresh run+invocation context on every matching start, so a repeated active start can erase accumulated write content. Content/end examine `segment_type` before consulting that context; target end intentionally has no type. The established context should instead record exact originating turn/invocation/source tool once, let content/end use the exact context, and release on tool/turn/run terminal. It remains file-operation state, not segment lifecycle.
- `RuntimeMemoryEventAccumulator` currently has `fallback-turn-*`, derived `turn:type` IDs, missing/unknown-type-to-text behavior, an `assistant` alias, ID-only segment storage, and missing-start content synthesis. Its supported transcript/history role can use exact canonical text/reasoning content plus a private compound identity while preserving tool/assistant trace ordering. Repository search found no independent run-history segment consumer.
- `CompactionRunOutputCollector` and `ImproverRunCompletionWatcher` accept ID/type/content aliases and can recover final text from `SEGMENT_END`. Their real subject is final-output aggregation: exact canonical text content plus existing `ASSISTANT_COMPLETE` priority and turn/idle completion are sufficient; end owns no text/type.
- External-channel direct parsing traverses provider/item aliases and treats missing end type as text; the Team parser discards error evidence. The supported reply path can accumulate exact text content and finalize on `TURN_COMPLETED`. Diagnostics must return before creating/deleting pending-turn state; exact terminal-turn error keeps the established deletion behavior.
- Standalone/Team coalescing and transport, application projection, and browser presentation are affected too. Coalescing is safe only when complete non-delta turn/segment/type matches. Application diagnostics must not become failure. Browser diagnostics may remain visible but must not terminalize open presentation state.
- Team communication/token processors and file/artifact relays are event-selective and do not parse segment payloads. They remain unchanged but need negative selection proof so a generic fallback is not introduced.

This is one clean source-to-canonical transition, not a compatibility period. The consumer-specific accumulators remain because they own different subjects; they may not read `AgentSegmentLifecycleState` or recreate source semantics.

### 51. Turnless post-ingress segment output is not a supported product state

The Product Reachability Rule changes the earlier SR-020 conclusion:

- AutoByteus `LlmStreamingResponseHandler` owns a required native turn before it creates `SegmentEvent`; `SegmentEvent` carries that same turn on start/content/end. The permissive empty-string coercion in `SegmentEventData` is a validation defect, not a supported caller contract.
- `ClaudeSession` allocates the turn before streaming and passes it as required projector input. Text/tool segment facts therefore have an exact session turn before server conversion.
- `CodexThread.activeTurnId` is the exact provider-session authority. Source inspection narrows production applicability and omission inheritance to four direct segment-producing native event names: `item/started`, `item/agentMessage/delta`, `item/completed`, and `item/reasoning/completed`. The current parser can read `params.turn_id`, `params.turnId`, `params.item.turn_id`, `params.item.turnId`, or `params.turn.id`; the target admission owner must inspect every present location rather than choose the first. Every present field on one of those four governed events must be a non-empty string equal to the current active turn. No active turn, a present invalid field, or disagreement is an ingress rejection. Other current native events do not enter this policy.
- The original `AgentRunErrorEvidence` union was deliberately asymmetric: `TURN_DIAGNOSTIC` is produced by normal recoverable LLM/tool/turn paths, while `TURN_TERMINAL` and `RUNTIME_GLOBAL` own distinct real terminal effects. `origin/personal` never had `RUNTIME_DIAGNOSTIC`; ticket commit `a0a1073cb` added it only for the new missing-turn transformer branch.
- A generic object, permissive parser, or synthetic test can mechanically omit `turn_id`, but none supplies the required independent product trigger and supported caller path after provider admission. MP-009 is therefore `Not Reachable` for the target runtime and cannot justify a fourth shared variant.

The proportional response is stricter at the real owner: missing, empty, inactive, or conflicting provider turn identity produces zero `AgentRunEvent`s, no browser/Team/standalone error, no lifecycle mutation, and only a sanitized internal provider-protocol record. Once an exact-turn source fact is admitted, an ordering/shape violation may reuse `TURN_DIAGNOSTIC`. Runtime/diagnostic remains invalid. The root cause is `Missing Invariant` at provider ingress, not a missing domain state.

### 52. Codex omission inheritance has one exact provider-local owner

ARCH-REV-015 / DR-010 is grounded by MP-010: ordinary Codex turns set `CodexThread.activeTurnId`, while direct item/reasoning notifications can omit a repeated turn field. The current converter has enough ownership context—the backend supplies `getLifecycleSnapshot()` derived from the same thread—but the current policy is fragmented:

- `isCodexItemEventName()` admits every `item/` prefix rather than one exact current set;
- direct source segments are actually created only by `ITEM_STARTED`, `ITEM_AGENT_MESSAGE_DELTA`, `ITEM_COMPLETED`, and `ITEM_REASONING_COMPLETED`;
- `CodexItemEventPayloadParser.resolveTurnId()` chooses the first non-empty candidate and therefore cannot detect a second disagreeing or present-invalid field;
- the reasoning normalizer may call its tracker with `turnId:null`, causing a start/content/end mini-lifecycle outside the active-turn tracker; and
- if target segment normalization begins returning rejection, the current generic `createEvent()` shape can substitute the raw payload unless that fallback is removed.

The target is one small provider-owned functional boundary, not another lifecycle subsystem. `resolveCodexSegmentTurnAdmission(nativeEventName, params, activeTurnId)` returns either an admitted exact turn/normalized params or one stable rejection reason. Its production-applicability and omission-inheritance set is exactly the four direct segment-producing names above. It examines all five current candidate locations; a present blank/non-string value rejects even when another field matches, multiple normalized values must agree, and the one agreed value must equal the current non-empty active turn. With no candidate, a governed event inherits that active turn. `ITEM_REASONING_COMPLETED` is admitted before the reasoning tracker so tracker state is exact-turn-owned. Every source-segment constructor requires the admitted result; invalid, inactive, or conflicting governed input cannot enter a raw/generic fallback. Rejection logs only runtime kind, run ID, native event name, and one of the stable reason categories (`CODEX_SEGMENT_TURN_EXPLICIT_INVALID`, `CODEX_SEGMENT_TURN_INACTIVE`, `CODEX_SEGMENT_TURN_CONFLICT`, `CODEX_SEGMENT_TURN_OMISSION_UNLISTED`). The last value remains only as the pure policy's internal misuse result; `CodexThread` does not invoke the policy for an unlisted native name, and no such production or synthetic provider case is required.

The exact allow set is intentionally smaller than all `item/*` events. Reasoning delta/summary-part events are intentionally non-emitting; plan, approval, file-output-log, raw-response, status, token, thread, and turn events own other facts. Reasoning close actions produced from `CodexReasoningBlockTracker` already contain the turn under which the block was opened and are not omission inheritance. This keeps one turn authority for every supported current segment-producing path. Later provider evolution is outside the current contract and must be designed from a concrete supported event when it exists.

### 53. The exact Codex policy must dominate the first per-thread native boundary

ARCH-REV-016 / DR-011 is grounded by two reachable premises, not a hypothetical defensive case. MP-011 follows a normal Codex Agent/Team turn through the client router into `CodexThread.handleAppServerNotification()`: before any backend listener/converter sees the event, `codex-thread-notification-handler.ts` can add a pending MCP call on `item/started`, remove it on `item/completed`, and emit `codex/local/mcpToolExecutionCompleted`. MP-012 follows the documented raw-debug operational path: `CodexThreadEventConverter.convert()` calls `logRawCodexThreadEventDetails()` before conversion, and `appendRuntimeRawEventLog()` can persist the complete event. Converter-local rejection therefore cannot truthfully promise zero mutation, zero derived event, or sanitized-only recording.

The narrow target correction uses the already-selected policy at the owner that has all required facts before any effect:

- `CodexThread.handleAppServerNotification()` is the first per-thread native boundary after routing and already owns the exact `activeTurnId`;
- the same module owns the exact four-name production-applicability/inheritance set and `resolveCodexSegmentTurnAdmission()` under the Codex thread/provider boundary, not in Team or AgentRun code. `CodexThread` calls it only for those four names; every other current item and non-item fact keeps its existing operation-owned handling without a second registry;
- a governed rejection calls only `logCodexSegmentTurnAdmissionRejection({runtimeKind,runId,nativeEventName,reasonCode})` and returns before `applyAppServerNotification()`; it cannot touch pending MCP, emit a local/original thread message, reach the backend/converter, mutate reasoning/lifecycle state, or call the raw-file sink;
- admission creates one readonly accepted native notification whose params are exactly `paramsWithExactTurn`; the notification handler reads that value for pending-MCP coordination and emits the original notification from it;
- locally derived messages are explicitly marked `local_derived`, while admitted native messages are marked `native_admitted`. `CodexThread` owns an unexported brand and private constructors for the listener-facing union; the handler/backend/converter may consume its opaque type but cannot structurally manufacture or cast it, and raw `CodexAppServerMessage` ends at the thread boundary;
- `CodexThreadEventConverter` owns no second admission rule. It converts/logs only a thread-emitted accepted/derived message. The existing raw JSONL/debug sink remains generic and unchanged, but rejected candidates never invoke it; and
- valid admitted MCP start/completion preserves add/remove/derived-completion ordering, valid non-MCP/reasoning conversion remains unchanged, and raw/debug capture remains available for admitted canonical/native and established local-derived messages.

This is a `Boundary Or Ownership Issue`, not a reason for another state machine. The accepted value crosses one path; the rejected value crosses none. No raw payload is retained merely to preserve pre-admission diagnostics, and no converter assertion is allowed to become a second parser/policy.

### 54. Active input has no common admission owner

`AgentRun.postUserMessage()` calls `applyCommandStart()`, but a `null` token does not stop backend invocation. The same exact run therefore receives incompatible policies: Codex owns a private input tail and active-turn steer selection; Claude rejects while active; AutoByteus relies on its runtime inbox; and `AgentRunCommandRegistry` independently rejects a different command and infers turn association from raw events. None of those locations owns the whole caller-to-provider-to-terminal spine.

The API-F-025 path is product-reachable and rules out a blocking “wait until provider start” result for next-turn-only runtimes: the replying Agent's tool call can wait on the original Agent whose active turn is itself waiting for collaboration. The single truthful common acceptance point is live AgentRun FIFO ownership. `accepted:true` therefore means one admitted ordered forwarding attempt, not provider completion. Codex's exact-turn append remains because a maintained live test proves it; Claude and AutoByteus wait centrally for canonical terminal and then start one later turn. One private per-entry observer replaces command raw-event inference. No queue is persisted and no collaboration retry or alternate route is introduced.

### 55. Latest Claude SDK improves provider queue control but does not provide exact active-turn append

The current official Agent SDK accepts a streaming `AsyncIterable<SDKUserMessage>` and exposes `priority:"now"|"next"|"later"`, `streamInput`, interrupt receipts, queued-message cancellation, and command lifecycle. These are not an exact active-turn API: the public input has no canonical turn ID or expected-turn precondition. The isolated current-version probe confirms that `now` interrupts/redirects the current work and `next` queues later work. Adopting either behind `append_to_active_turn` would lie; adopting `next` beside SR-026 would create a second FIFO. The clean target therefore retains one-string query turns and one AgentRun queue.

The breaking upgrade impact is bounded and production-reachable. Team-bound Claude Agents must have intrinsic Agent Tools MCP in turn 1, while `0.3.142+` permits background MCP readiness; the server-owned intrinsic MCP descriptor must set `alwaysLoad:true`. Removed V2 APIs and provider `TodoWrite` replacement do not affect the source because neither is consumed. Exact direct peers are required for package/type consistency. Existing `options.env`, `options.resume`, selected executable, event normalization, and session interrupt/close remain the established adapter boundary.

### 56. The supported one-string Claude interrupt is AbortController-owned

ARCH-REV-020 established a reachable supported interrupt path independent of the new streaming-input features: an explicit product interrupt reaches the active `ClaudeSession`, whose query was created with `Options.abortController`. The session clears/flushes pending approval state, calls `AbortController.abort()`, awaits the exact active turn execution, lets the established turn finalizer release its registered query/reference, clears active state, emits canonical `TURN_INTERRUPTED`, and only then exposes the terminal fact that permits AgentRun to drain waiting input. `Query.interrupt()` neither selects nor improves this one-string path and its installed control declaration belongs to the streaming-control surface that the product intentionally does not adopt. SR-028 therefore removes all normative interrupt-receipt/`Query.interrupt()` wording from the product spine while leaving unrelated query-control cleanup outside AgentRun/session interruption unchanged.

## Persisted Data Transition Evidence (When Applicable)

- Current stored subjects and representative shapes:
  - AgentTeam definitions use `team.md` plus `team-config.json` in shared, team-local, and application bundles. They retain authored local `memberName` segments and optional handoffs.
  - TeamRun history uses `<memoryDir>/agent_teams/<rootTeamRunId>/team_run_metadata.json`. Supported predecessor states include flat v1 `memberMetadata` with independent display `memberName` and structural `memberRouteKey`, and recursive `memberTree` with display name plus structural route/path, generic member run ID, Team run/coordinator route, genuine Agent launch settings, and handoffs.
  - Team communication and task delegation records persist `ConversationTargetAddress` and related logical/task path bundles.
  - Token usage uses the operational database. Supported predecessor rows persist legacy `{segments}` JSON under a terminal `20260703_token_usage_execution_address_backfill` record and parallel root/member/task-run/task-instance columns; target readers accept only exact `TeamExecutionAddress`, actual Agent `run_id`, optional task-operation `task_id`, and usage facts.
  - External-channel bindings use JSON and remain a released migration subject. Application platforms use databases for Team launch/run bindings, but the feature has no supported predecessor cohort; project-owned application databases are regenerated/reset and are not migration subjects.
  - Derived history indexes/caches reproduce projections from primary records.
  - Agent memory directories use root and ancestor TeamRun IDs; context-file locators use stable path segments derived from logical routes.
- Normal reader/writer behavior and why direct use is insufficient:
  - Definition readers can map missing handoffs to `[]`; no definition rewrite is needed.
  - TeamRun/history/task/binding readers explicitly normalize old structured identity. The target deletes those fields. Migration may discard display-only TeamRun `memberName`, but cannot choose between disagreeing structural route/path, coordinator, parent, or run representations. Current readers cannot safely own that historical distinction.
  - The released token database requires column/JSON conversion before target repositories can enforce clean constraints. Conversion must execute under a record absent from the predecessor state and must transact the complete planned row update plus obsolete-column/index contraction. Root lookup remains efficient through a JSON expression index rather than a parallel writable field. Application databases are instead reset/rebuilt because no supported predecessor data exists.
  - Derived indexes are safe to discard and rebuild from converted primary data.
- Required semantic invariants:
  - Every migrated structural route/path pair, local/source form, and parent relation must agree with one `AgentTeamAddress`. Historical TeamRun `memberName` is explicitly excluded from this equality because it was display-only.
  - Each `coordinatorAddress` must identify one direct Agent; generic/kind-specific legacy run IDs must agree before the generic value is removed.
  - Ordered task Team run-ID chains must preserve exact concrete run IDs and resolve through their minimal task identities to the correct nested topology scope; same-named or same-address persistent/task instances must not merge.
  - Content, references, timestamps, settings, handoffs, lifecycle state, token/cost facts, released external targets, and history reachability must survive. There is no preservation requirement for pre-ticket application bundle/database state.
- Physical-store and operational evidence:
  - Existing app-data migration infrastructure supports ordered registrations, completion records, backup, atomic file replacement, and rerun, but skips terminal records by ID. The registry orders `20260517_team_run_metadata_member_tree` before `20260801_team_canonical_identity`; the latter remains pending for supported predecessor installations. The same registry currently registers target token code under terminal historical `20260703...`, which is the ownership defect. Application physical discovery exists but is intentionally outside the target migration inventory because the feature has no supported predecessor state.
  - IR-010/IR-011 changed current server runtime to require exact `20260801_team_canonical_identity` success before services listen while preserving unrelated warning policy. SR-015 keeps that gate and makes its aggregate result include token transaction success.
  - Physical Prisma schema migrations still run first. They must preserve predecessor token inputs; target schema/client omission is separate from physical removal. Semantic token row conversion and physical Team-identity schema contraction belong together to the `20260801...` app-data owner through one migration-local raw Prisma/SQLite store; the old token/cleanup records and ordinary runtime repository own neither target conversion nor fallback.
  - Physical Agent memory relocation is unnecessary and operationally riskier than retaining its truthful run-ID lineage.
- Persisted-data decision by class:
  - AgentTeam definitions: `Directly Usable — No Migration`.
  - TeamRun metadata, communication/task records, token usage, and released external binding JSON: `Migration Required`.
  - Application platform launch/run-binding databases: `Discard or Rebuild`; project-owned fixtures are created directly in the target schema and no predecessor reader exists.
  - Application bundle code/artifacts: `Direct Target Replacement — No Migration`; project-owned outputs move atomically to V5 and non-target external inputs are unsupported.
  - Derived indexes/caches: `Discard or Rebuild`.
  - Agent memory directories, stable final context files, and opaque provider raw payloads: `Directly Usable — No Migration`.
  - AgentRun input FIFO, private sequence, and input lifecycle observers: `Not Affected`; deliberately non-persisted and empty on restore.
- Migration boundary: one pure migration-only decoder owns flat TeamRun interpretation and preserves the independent display label. Stable `20260517_team_run_metadata_member_tree` uses it for pending flat-to-predecessor replacement; pre-existing token model/provider backfills are attempted before schema contraction but are not canonical prerequisites; ticket-owned `20260801_team_canonical_identity` converts predecessor trees to v3, composes the decoder for residual flat input, and then composes the target token canonical migrator after TeamRun/task readiness. The token migrator plans all rows, and its store applies/verifies address updates, obsolete-column/index replacement, and final invariants in one transaction. Old `20260703...` and narrow cleanup history is untouched and unregistered as current authority. Exact `20260801...` success gates bootstrap/listen; current runtime/repositories contain no old-schema branches.
- Volume/rollout unknown: user-specific durable volume was not enumerated. The migration must stream/batch where a store requires it and report item identity/progress; lack of a fixed repository sample count is not permission to use a best-effort reader.
- Detailed schema, ordering, failure, and recovery contract: [team-run-canonical-identity-refactor.md](./team-run-canonical-identity-refactor.md) §§5, 10, and 12.

## Constraints / Dependencies / Compatibility Facts

- SR-028 is the current cumulative solution package. ARCH-REV-018 passed SR-024; SR-025 changed prompt copy only; ARCH-REV-019 passed SR-026's AgentRun-admission correction; ARCH-REV-020 accepted SR-027's package/capability/MCP boundaries but returned DR-013/DR-014. SR-028 corrects only the active one-string-query interrupt spine and authority navigation. Complete architecture re-review is required before implementation; focused/full source review must pass before API/E2E resumes.
- AgentRun input admission is non-persisted runtime coordination. The common state may read canonical turn lifecycle but may not duplicate active-turn authority. Providers expose explicit mechanics only; outer routers/command activation remain above AgentRun and cannot queue, retry, or select active behavior.
- Claude Agent SDK `0.3.231`, Anthropic SDK `0.116.0`, and MCP SDK `1.30.0` form one exact compatible package cut. Latest Claude streaming-input priority/queue/receipt mechanics are not adopted as an input owner. One-string `query()`/resume/full-env/selected-executable semantics stay; only the intrinsic Agent Tools MCP entry is required in turn 1 with `alwaysLoad:true`.
- Ticket 1 still excludes native AgentOrg, dynamic live topology reconciliation, automatic filesystem watching, cross-process Agent routing, and external pure-text package edits.
- Definition authoring remains local and reusable: each Team config references its direct members/nested Team definitions by local `memberName`; recursive mounting decides absolute addresses.
- TeamRun remains the native lifecycle owner for Agents and child TeamRuns. Rooted snapshot sharing does not flatten lifecycle or turn topology into a global service locator.
- AgentTeam remains coordinator-led and every `coordinatorAddress` must identify a configured direct Agent child.
- `target_agent_run_id`, task/run IDs, definition IDs, invocation IDs, and physical storage lineage retain their exact owners; canonical address replaces only parallel logical placement identity.
- Handoff edges remain sender guidance, not a routing/task-eligibility allowlist. Message and task share placement resolution, then separate into operation-owned behavior.
- Task delegation remains limited to direct members/child Teams of the caller's current TeamRun after root resolution.
- Server, generated transports, internal application SDKs, integrations, and production frontend must ship atomically; no mixed-version runtime support is designed. Backend-definition and frontend-SDK V5 are the sole current semantic contracts; application manifest V4, backend bundle envelope V1, and iframe V4 remain only as independent unchanged protocol versions.
- Application target validation reuses the application manifest parser, backend manifest parser, and backend definition loader. Project-owned source/build/vendor/importable artifacts and fresh fixtures/databases advance together; non-target external inputs are simply invalid, never edited, adapted, migrated, or given a special compatibility workflow.
- Current runtime parsers/writers support only the target schema after required migrations. Legacy readers live only in migration input modules/fixtures.
- `/ws/agent-team` server/client DTOs and runtime schemas have one browser-safe workspace package owner. This transport package must not import server topology/task policy or frontend stores. Team server and web ship atomically; no loose or alias contract remains.
- Segment source/canonical shapes are distinct within one current pipeline, not old/new compatibility shapes. Provider adapters emit only minimal source events; all consumers receive only canonical post-AgentRun events. No caller may bypass the lifecycle transformer while claiming supported runtime behavior.
- Same-type active start is a lifecycle-owner replay no-op. No processor/listener may receive it and therefore no downstream accumulator may own repeat-start reset/merge policy.
- Agent-bound errors are classified only by the original `AgentRunErrorEvidence` union: exact turn diagnostic, exact turn terminal, or runtime-global terminal; established unclassified errors remain explicit null evidence. Runtime/diagnostic is invalid. Provider turn-admission rejection is not an Agent-bound error and produces no Team/standalone/browser projection.
- CR-F-043 remains owned by `api_e2e_engineer`. No solution/architecture/implementation reviewer should inspect, move, or delete the journal or operational data; API/E2E must handle only its owned disposable target and repair its evidence before a new live run.
- Frontend rooted topology is immutable. Concrete execution identity/lifecycle/focus is owned by one aggregate; no consumer may access serialized execution keys or mutate topology to represent tasks.
- Team runtime intrinsically composes `get_handoff_rules` and `send_message_to` for Team-bound Agents after package tool resolution; provider adapters remain thin and preserve the operation-specific minimal rule result or send delivery envelope.
- Clean cut means no `recipient_name`/`recipient_path` alias, bare-name fallback, representative projection, root/local selector guessing, localized child topology, dual read/write, or compatibility map.
- Physical Agent memory directories and stable context-file paths do not move. Storage derives compatible path segments or uses concrete ancestor TeamRun IDs internally.
- Active Team definitions may be explicitly refreshed in the catalog, but an existing TeamRun remains immutable and restores from its rooted TeamRun snapshot.

## Open Unknowns / Risks

- The identity surface is large. Missing one producer/consumer would recreate a parallel authority even if the central types are clean. Implementation and coverage need a repository-wide legacy-field inventory with a narrow documented allowlist.
- Migration must distinguish semantic roles before validating equality: `memberName` is display-only, while route/path, parent relation, coordinator route, and generic/kind-specific run IDs are structural. It may derive address only from an agreeing structural pair and must never use fallback order.
- Existing metadata may first require the stable flat prerequisite, but terminal records are intentionally not rerun by `runPending()`. The separate pending canonical ID must own final v3 for freshly produced or previously recorded predecessor trees and for repaired residual flat items under terminal-warning history by composing the same migration-only decoder. Recovery cannot depend on the GraphQL manual-retry surface because the exact canonical startup gate may prevent that API from listening.
- IR-010/IR-011 scope exact-success startup blocking to the canonical identity migration. Token conversion must become a named item of that aggregate so the existing gate is truthful; unrelated established warning behavior must remain unchanged.
- Token volume is material: the read-only operational snapshot observed roughly 143.8k rows. Planning and transaction execution must be bounded and observable, update only rows needing conversion, and remain restartable/idempotent; a large batch is not permission for row-at-a-time commits.
- Application migration must not re-enter through implementation convenience. No application database belongs in the canonical inventory; project fixtures/databases are reset/rebuilt to target and current repositories reject predecessor shape.
- Same logical address can identify a persistent and multiple task executions. Frontend/API/runtime maps keyed only by `memberAddress` would collide; `TeamExecutionAddress` serialization must be canonical and consistently used.
- Task-Team nesting produces an ordered task Team run-ID chain. Conversion must use enclosing record/task structure and minimal task identities, not parse synthetic route strings heuristically or repeat logical Team addresses in the locator.
- Rooted tree sharing changes object construction and lifecycle seams. Lazy child creation/restore must select exact direct child nodes and their typed run facts without accidentally materializing siblings or losing private handle ownership.
- Task mapping still needs current-Team direct eligibility. The common resolver must not absorb task lifecycle/policy, and task code must not reconstruct a second address parser.
- AgentTeam coordinator remains configured data. It must not be guessed as the first Agent or inferred from the Team address; TeamRun-tree compilation and migration validate direct-child membership.
- Exact AgentRun routing already passes coverage. SR-026 changes only post-resolution input admission: pre-AgentRun recipient/run-not-found/task/tool errors remain operation-owned, while successful live-run acceptance becomes the common `accepted:true` responsibility boundary without replacing operation-owned success codes such as `DELIVERED`.
- A next-turn-only input must return after AgentRun FIFO admission rather than terminal/provider forwarding, or collaborating active turns can deadlock. That admission must not be misread as model completion; later provider failure settles the typed internal lifecycle once and never triggers Team retry or duplicate projection.
- Latest SDK streaming input can appear to solve active delivery but its priorities have different semantics: `now` interrupts and `next` queues without exact turn identity. Using it would either violate exact append or duplicate AgentRun FIFO/interrupt ownership. First-turn MCP background loading can also make mandatory tools unavailable unless the intrinsic server alone is marked `alwaysLoad:true`.
- Removing command-registry busy rejection can expose singular-in-flight assumptions in status projection, prepared-run cancellation, and restore activation. Those readers must support multiple records or choose a deterministic presentation row without acquiring ordering policy; activation remains a shared single-flight before the one AgentRun FIFO.
- AutoByteus, Codex, and Claude materialize tools/instructions at different seams. Coverage must prove that intrinsic Team exposure, canonical-name de-duplication, caller address, filesystem examples, “must” completion/blocked wording, and minimal result semantics remain equivalent.
- The `recipient_address` rename crosses AutoByteus schemas/parsers/manifests, Agent Tools MCP adapters/catalogs, provider instructions, task tooling, tests, and project documentation. A missed surface would expose two public names even if runtime resolution is singular.
- Physical memory storage must not be rewritten merely for cleaner names. Renaming the domain concept without preserving on-disk layout would create unacceptable data risk.
- Transport/SDK/frontend changes are atomic by design. No permanent alias or dual-shape response is allowed. Backend-definition/frontend SDK identity shapes advance to V5 and every project-owned consumer/artifact advances with them; there is no supported old application client cohort.
- Generated and vendored copies can drift from source constants if build outputs are not regenerated. Coverage must compare workspace SDK source/dist, both project application source manifests/definitions/scripts, UI vendor copies, and importable packages at V5.
- Application source scanning must prove that no predecessor decoder, database migration item, special V4 compatibility/quarantine branch, or stale project-generated artifact remains.
- Opaque provider payloads may contain text keys resembling legacy fields. The post-change scan must distinguish untyped historical payload from active structured identity instead of destructively rewriting it.
- Cumulative implementation exists through IR-045 and reviewed source HEAD `42e42a9471c251075af07c3e0805d43858246e67`, but CRR-086 supersedes the prior source gate for active input and API/E2E remains paused. SR-026 remains unimplemented authority despite ARCH-REV-019 Pass. SR-027 changes only user-authorized dependency manifests/lock plus solution artifacts and claims no corrected product implementation, completed product live result, cleanup, or delivery outcome.
- Strict wire admission can turn previously silent projection drift into visible protocol rejection. That is intentional; coverage must prove invalid input is mutation-free and real producer output is admitted.
- A Team-only lifecycle map is the wrong local fix: `TeamAgentEventAdapter` is recreated per event and is not the common owner; making it stateful would couple provider order to Team membership and leave standalone/application/history paths divergent.
- Provider adapters may lack an explicit normalized start today. The exact correction belongs to their existing segment-creation trackers/projectors, not to a guessed `text` default at common or browser boundaries.
- Segment lifecycle replay has no content event ID. Treating equal delta bytes as duplicates would corrupt valid repeated output; only start/end structural replay is idempotently handled.
- File change must not compensate for canonical end by restoring type. Its existing context must make segment-start initialization non-replacing and turn-aware; a later matching tool-start may enrich path/arguments only while preserving structural identity, accumulated content, and status. It remains independent of segment lifecycle state.
- Memory/history, compaction, skill-improvement, and external-channel fallbacks are current supported consumers, not dead code. A partial cut would leave source aliases/defaults or end-text recovery behind the canonical barrier and recreate competing authority.
- The removed runtime-diagnostic branch could survive in current source or transport even though no supported producer owns it. Source/contract scans must prove it absent; real turn diagnostic and terminal variants must retain their exact established effects.
- Listener reconnection can occur mid-segment. Canonical content must retain derived type so a new browser subscriber can truthfully create a local segment without a default; server run state remains independent of subscriber lifecycle.
- Initial connection/restore status is not a domain event. Reusing the correlated status wire projector must not tempt implementation to manufacture a fake `TeamRunEvent`, and the snapshot path must not retain its own binding parser or generic `ServerMessage` mapper.
- Pre-run initializing/error status exists before an AgentRun is materialized. The mixed handle/config owner must construct the binding from its allocated AgentRun ID and exact execution address; a lookup fallback or address-shape guess would recreate the authority gap. The first matching live status must replace the overlay exactly once.
- Adding a shared contract package could become an empty or wrong-domain abstraction if it owns routing/topology. Its allowed responsibility is exact Team transport DTO/schema/serialization only; standalone Agent streaming keeps its existing contract and no generic “all streaming” package is introduced.
- A single execution owner could become a coordination blob. Keep its public authority singular while splitting private pure transition and navigation-projection functions by concern; do not expose them as alternate mutation entrypoints.
- Putting execution state under `agentStreaming/` would incorrectly make transport look authoritative for launch/restore/history. The target `services/teamExecution/` capability owns topology/execution projection; streaming depends on its public boundary and the aggregate never imports WebSocket code.
- Task AgentTeam children may not yet have concrete AgentRun IDs when the root task record arrives. They must remain absent rather than be created with empty IDs; later exact Agent events/hydration materialize them.
- A complete append-only task query can begin before a concurrent activation and arrive after the activation event. Reconciliation must preserve the known newer task rather than treat response absence as deletion; equal-time contradictory rows must reject atomically, not pick one.
- Repeating full task base/derived fields on every strict event would simply standardize current duplication. Activation should carry the durable base once; later task events should trigger complete refresh and never create a partial lifecycle/timeline overlay.
- Consumer migration is broad. Compile-time removal of the raw map and an exact-path source allowlist must catch bypasses; a compatibility wrapper around old task projection modules is forbidden.
- The latest-base selection gate must not be lost: an execution row is current only when both selected root TeamRun and exact focused execution match.
- The local credential assignment and all three runtime dependencies are external live prerequisites. Downstream evidence must distinguish product failure, environment blockage, and provider unavailability without leaking values or manufacturing a Pass.

## Approval And Rework Context — SR-028

The user-approved product behavior basis and `ARCH-REV-019` cumulative SR-026 Pass remain unchanged. SR-025's exact prompt copy was implemented through IR-045 and introduced no architecture change. API-REV-039 then established the exact supported failure path, and CRR-086 superseded CRR-085 with `Fail — Design Impact` because provider-distributed active-input policy is broader than a Claude condition.

SR-026 establishes that:

1. every ordinary input caller still ends at the exact resolved live AgentRun; Team/global/application/external layers do not retry or bypass it;
2. AgentRun owns one non-persisted FIFO and returns `accepted:true` when it has accepted responsibility for one ordered, at-most-once forwarding attempt; AgentRun adds no success code, so operation owners retain established codes such as `DELIVERED`;
3. acceptance does not wait for provider completion, so an active next-turn-only recipient cannot deadlock the replying Agent's tool call;
4. AgentRun selects `append_to_active_turn` only for an exact identified active turn and a declared append capability; otherwise the head waits for canonical terminal and becomes `start_turn`;
5. Codex declares append support and maps explicit intent to `turn/steer`; Claude and AutoByteus declare it unsupported and receive a later explicit start; providers do not choose policy;
6. one private per-entry lifecycle observer tells the command bridge about forwarding, turn association, completion, interruption, failure, or cancellation; raw Agent events no longer serve as command association policy;
7. distinct commands may coexist in the registry, outer message replay identity remains, and activation single-flight remains provisioning-only;
8. interruption keeps undispatched input and drains after canonical terminal; accepted termination quiesces/cancels, rejected termination reopens original order; no accepted entry is retried or replayed;
9. the input state is not persisted, migrated, serialized, exposed as identity, or restored as an inbox; and
10. the exact API-F-025 Claude path plus AutoByteus next-turn and Codex same-turn controls must pass before API/E2E resumes its broader matrix.

ARCH-REV-019 passed SR-026 before the user requested latest-SDK investigation. SR-027 preserves all ten SR-026 invariants and adds that:

11. the exact target package set is Claude Agent SDK `0.3.231`, Anthropic SDK `0.116.0`, MCP SDK `1.30.0`, and existing compatible Zod 4;
12. official API/types and isolated live proof establish that Claude still cannot append to an exact identified active turn;
13. `priority:"now"` is interruption/redirect and `priority:"next"` is provider-owned later-turn queueing, so neither enters the AutoByteus backend command contract;
14. current one-string `query()`/resume/full-env/selected-executable behavior remains the only Claude input mechanism;
15. the intrinsic Agent Tools MCP server sets `alwaysLoad:true` so mandatory Team tools are ready in turn 1; and
16. no V2 adapter, provider queue, command-lifecycle owner, version compatibility branch, data migration, or unrelated SDK feature is introduced.

The cumulative package should return to `architecture_reviewer` for complete SR-028 review. Implementation, API/E2E, and delivery remain paused. The user-authorized manifest/lock upgrade and disposable isolated probes are the only non-document investigation changes; existing dirty SR-026 source/tests, operational data, protected stashes/backups, API-owned residue, and downstream state remain untouched.

### SR-028 correction after ARCH-REV-020

ARCH-REV-020 passed the exact dependency graph, latest-SDK capability conclusion, one AgentRun FIFO, Claude `activeTurnAppend:"unsupported"`, and intrinsic-only `alwaysLoad:true` decision. DR-013 identified one contradictory implementation prescription: the product uses one-string `query()` with `Options.abortController`, but DS-019D/CLAUDE-SDK-004 named the streaming-control `Query.interrupt()` method. DR-014 identified stale revision/live-contract navigation. SR-028 changes no requirement or product behavior. It makes the established AbortController/settlement/cleanup/canonical-event sequence authoritative, explicitly excludes Query.interrupt/receipt machinery from active product-turn interruption, and aligns current revision plus R-058/AC-053 navigation. Complete architecture re-review remains required before implementation resumes.
