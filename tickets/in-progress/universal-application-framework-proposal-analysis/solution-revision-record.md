# Solution Revision Record — Universal Application Dual-Host Foundation

The latest [requirements.md](requirements.md), [investigation-notes.md](investigation-notes.md), [design-spec.md](design-spec.md), and listed supplements are authoritative. This record is only the chronological solution-round and routing index.

## Revision Index

| Revision ID | Triggering Role / Report / Round | Finding IDs | Classification | Result |
| --- | --- | --- | --- | --- |
| SR-001 | Initial approved solution package / architecture review round 1 | N/A | `Initial Baseline` | Round 1 returned `Fail — Design Impact` |
| SR-002 | `architecture_reviewer` round 1 report plus user discussion refinements and latest-base refresh | AR-001–AR-004; SV-001–SV-007 | `Design Impact` | Revised package self-validated and routed for architecture re-review |
| SR-003 | `architecture_reviewer` round 2 / `ARCH-REV-002` | AR-001, AR-005, AR-006; SV-008 | `Design Impact` | Round-2 corrections self-validated and routed for architecture re-review |
| SR-004 | `code_reviewer` `CRR-010` after API/E2E round `API-REV-004`, plus clarified user contract | CR-006–CR-008; SV-009 | `Design Impact` | Package-default/readiness/prompt-authority corrections self-validated and ready for architecture re-review |
| SR-005 | `architecture_reviewer` round 4 / `ARCH-REV-004` | AR-007; SV-010 | `Design Impact` | Invalid/stale saved-override semantics corrected within DS-012 and ready for architecture re-review |
| SR-006 | `code_reviewer` `CRR-012` after IR-007 | CR-009, CR-012; SV-011 | `Design Impact` plus bounded policy correction | Selected-resource edit projection and recursive portable-field policy corrected; ready for architecture re-review |
| SR-007 | `code_reviewer` `CRR-015` after `API-REV-005` / IR-009 / CRR-014 | CR-013, APIE2E-F005, APIE2E-STANDALONE-MCP-001; SV-012 | `Design Impact` | **Withdrawn before architecture decision; superseded by CRR-016/SR-008 and not current authority** |
| SR-008 | `code_reviewer` corrective `CRR-016` | CR-013, APIE2E-F005, APIE2E-STANDALONE-MCP-001; SV-013 | `Local Fix` | Correct Agent Tools projection restored; only existing standalone route registration remains; routed to implementation |
| SR-009 | User MCP ownership and scope clarification after SR-008 | CR-013; SV-014 | `Clarification — No Design Or Source-Scope Change` | Studio MCP state/general gateway excluded from standalone; runtime-internal tooling explicitly outside this ticket |
| SR-010 | `code_reviewer` `CRR-020` after API/E2E `API-REV-007` / IR-011 / CRR-019 | CR-015, APIE2E-F007, APIE2E-STANDALONE-MCP-003; SV-015 | `Design Impact` | Application-runtime-scoped publication/session ownership and cycle-breaking lifecycle revised; ready for architecture re-review |
| SR-011 | `code_reviewer` `CRR-028` after functional/test passes through CRR-027 | CR-018; SV-016 | `Design Impact — Naming / Readability` | Behavior-neutral role vocabulary and exact clean-rename map self-validated; ready for architecture re-review |
| SR-012 | `code_reviewer` `CRR-031` after CRR-029 / API-REV-011 / CRR-030 passed | CR-019–CR-021; SV-017 | `Design Impact — Behavior-Neutral Architecture Simplification` | Narrow runtime boundary, late package reconciliation, and acyclic run/engine construction self-validated; ready for architecture re-review |
| SR-013 | `architecture_reviewer` `ARCH-REV-010` over SR-012 | AR-008, AR-009; SV-018 | `Design Impact — Bounded Lifecycle Correction` | Retains accepted runtime/package owners; restores worker ensure/restart and exact run-resource cleanup acyclically; ready for architecture re-review |
| SR-014 | `code_reviewer` eight-candidate design investigation request after CRR-033 / API-REV-012 / CRR-034 | BEH-011; REQ-011; AC-024; SV-019 | `Evidence-Backed Architecture Hardening` | Adopts only AFB-001–AFB-005 enforcement plus matching existing-doc updates; no production refactor; ready for architecture review |
| SR-015 | `architecture_reviewer` round 12 / `ARCH-REV-012` over SR-014 | AR-010, AR-011; MP-ARCH-012-001/002; SV-019 | `Design Impact — Bounded Checker Completeness Correction` | Adds exact fallback-by-omission obligations and truthful Vue/multi-project resolution; no production refactor; ready for architecture re-review |

## Revision Entries

### SR-001 — Initial dual-host foundation baseline

- Triggering role, report path, and round: Initial solution handoff followed by `architecture_reviewer` round 1 at [design-review-report.md](design-review-report.md).
- Triggering finding IDs: `N/A` for the baseline.
- Prior authoritative result: `N/A`.
- Current authoritative result: The initial package established the universal startup-provider/shared-runtime/two-composition direction; architecture review round 1 subsequently returned `Fail — Design Impact`.
- Why this baseline or revision entry is recorded: The initial handoff predated the mandatory revision record. This entry restores the required baseline without treating the missing prior record as a pass.
- Resolution: Initial authoritative requirements, investigation, critical analysis, and design were created in the dedicated task worktree.
- Approved behavior or requirement IDs affected: BEH-001–BEH-007; REQ-001–REQ-006; the then-current AC/use-case set.
- Canonical artifacts and sections updated: Current canonical paths are [requirements.md](requirements.md), [investigation-notes.md](investigation-notes.md), and [design-spec.md](design-spec.md); their latest contents supersede the initial round.
- Supplemental artifacts updated, added, or removed: [proposal-critical-analysis.md](proposal-critical-analysis.md) and retained proposal source.
- Downstream and architecture-review impact: Implementation was correctly blocked pending AR-001–AR-004.
- Next recipient or routing: Returned to `solution_designer`.
- Remaining gaps or risks: Exact named readiness, frontend migration inventory, composition-critical dependency conversion, and stable behavior traceability were unresolved in round 1.

### SR-002 — Review findings, native commands, and latest-base reconciliation

- Triggering role, report path, and round: `architecture_reviewer` round 1 [design-review-report.md](design-review-report.md), followed by user discussion refinements through 2026-07-27 and user-requested `origin/personal` refresh on 2026-07-29.
- Triggering finding IDs: AR-001–AR-004 and self-validation corrections SV-001–SV-007.
- Prior authoritative result: `Fail — Design Impact`.
- Current authoritative result: Revised solution package is requirements-approved, self-validated, aligned to `origin/personal` at `6caf809303294252c109420b238588f0c68aca6a`, and ready for architecture re-review. No pass is assumed before that review.
- Why this baseline or revision entry is recorded: It is the completed solution-design rework round after architecture findings and the user's final command/workflow decisions.
- Resolution:
  - Enumerated exact startup/readiness/recovery/stop ownership, including refreshed-base protected operational paths, Prisma, secret vault, provisioned Search tool, event pipeline, and cleanup.
  - Completed the clean-cut frontend/source/generated migration inventory and unversioned code-symbol plan.
  - Defined the exact composition-critical dependency graph, bounded singleton conversion, route seams, and disposal inventory.
  - Stabilized BEH-001–BEH-007 and added complete reachable UC/DS traceability.
  - Fixed the native application-folder contract to `dev`, `dev:studio`, `build`, `validate`, and `start`; added production DS-010 and rejected public mock fallback.
  - Revalidated eighteen reachable use cases and twenty-four scenarios against the canonical design principles.
- Approved behavior or requirement IDs affected: BEH-001–BEH-007; REQ-001–REQ-006; AC-001–AC-013; UC-001–UC-018; DS-001–DS-010.
- Canonical artifacts and sections updated: [requirements.md](requirements.md), [investigation-notes.md](investigation-notes.md), and [design-spec.md](design-spec.md), including behavior maps, named readiness, command/process boundaries, file mapping, sequence, risk, and guidance sections.
- Supplemental artifacts updated, added, or removed: Updated [proposal-critical-analysis.md](proposal-critical-analysis.md); updated [design-self-validation.md](design-self-validation.md); retained original proposal source; retained round-1 [design-review-report.md](design-review-report.md) as triggering evidence.
- Downstream and architecture-review impact: Architecture review should verify AR-001–AR-004 resolution plus the new DS-010/native-command and refreshed-base process-resource design. Implementation remains blocked until review passes.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Optimized standalone binary/container distribution, full offline dependency packaging, public-internet operation, marketplace isolation, and repository-wide singleton removal remain intentionally out of scope. Real execution must later verify worker-crash recovery and latest-base vault/Search/restart cleanup.

### SR-003 — Readiness consistency, maintained-project mappings, and fallback removal

- Triggering role, report path, and round: `architecture_reviewer` round 2 [design-review-report.md](design-review-report.md), indexed by [architecture-review-revision-record.md](architecture-review-revision-record.md) as `ARCH-REV-002`.
- Triggering finding IDs: AR-001, AR-005, AR-006, plus self-validation correction SV-008.
- Prior authoritative result: `Fail — Design Impact`.
- Current authoritative result: The cumulative package uses one exact refreshed-base readiness order and seven-group tool contract, makes all approved maintained-project commands executable through explicit devkit inputs, removes the contrary broad-server fallback from the approved supplement, and is ready for architecture re-review. No pass is assumed before that review.
- Why this revision entry is recorded: Round 2 verified AR-002–AR-004 resolved but found three bounded cross-artifact/production-path gaps that still blocked implementation.
- Resolution:
  - Aligned every lifecycle rule, sequence, graph/example, file row, guidance statement, and SV-C24 to `AppConfig/database location -> core migration -> protected DB/root-key/sidecar paths -> Prisma -> secret vault -> app-data migration -> remaining readiness`.
  - Replaced the stale six-group target row with exactly seven named P6 groups including provisioned Search.
  - Audited Brief Studio and Socratic's real layouts and selected one existing-owner path: identical checked-in `autobyteus-app.config.mjs` mappings for frontend/backend roots, entries, optional assets/agents, root teams, migrations, seven exposure booleans, and output.
  - Assigned icons to `frontend-src`, package-SDK imports to app entries, the five scripts directly to devkit, and clean deletion of custom builders plus source-root generated `ui`/`backend`/vendor mirrors.
  - Ran a disposable exact-config probe: both maintained apps packed and validated through the current devkit with icons, teams, migrations, and exposure manifests present; all probe/build/dependency artifacts were removed.
  - Revised the critical-analysis correction, target behavior, roadmap, and decision table so the current broad `buildApp()` composition is rejected as any standalone fallback or interim stage. Named underlying prerequisites remain reusable only inside the explicit selected-application composition.
- Approved behavior or requirement IDs affected: BEH-004–BEH-007; REQ-004–REQ-006; AC-001, AC-005, AC-006, AC-009–AC-013; UC-015, UC-016, UC-018; DS-005, DS-006, DS-010. This is execution/design precision for already-approved behavior, not a new product requirement.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) BEH-006/REQ-006/UC-015/AC-011 and constraints; [investigation-notes.md](investigation-notes.md) source log/behavior/design-health/open decisions/review guidance; [design-spec.md](design-spec.md) round-2 map, DS-006, lifecycle rules, exact mappings/removals, file maps, examples, and sequence.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) to remove the contradictory fallback; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-008. No new supplement was required.
- Downstream and architecture-review impact: Implementation remains blocked until architecture review verifies AR-001, AR-005, and AR-006 resolved. AR-002–AR-004 remain resolved and were not reopened.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: The config probe validates package-input feasibility only. Downstream execution must prove the new real `dev`/`dev:studio`/`start` sessions, worker recovery, origin/static behavior, graph isolation, and event-pipeline/vault/Prisma cleanup. Optimized independent distribution, marketplace isolation, and full offline dependency packaging remain out of scope.

### SR-004 — Self-contained package defaults, truthful run readiness, and graph-local team prompts

- Triggering role, report path, and round: `code_reviewer` implementation-source review `CRR-010` in [code-review-report.md](code-review-report.md), following API/E2E evidence round `API-REV-004` in [api-e2e-execution-coverage-report.md](api-e2e-execution-coverage-report.md), plus the user's clarified standalone product contract through 2026-07-29.
- Triggering finding IDs: CR-006, CR-007, CR-008, and self-validation correction SV-009.
- Prior authoritative result: Architecture review of SR-003 passed as `ARCH-REV-003`, implementation proceeded, and the later full implementation review returned `Fail — Design Impact`. API/E2E had proved that the real Studio Brief run succeeds through a saved override, while a clean standalone Brief run fails because its package leaf defaults omit `llmModelIdentifier`.
- Current authoritative result: The cumulative solution package now requires complete application-owned runtime/model defaults for every required standalone execution leaf, treats host overrides as optional non-mutating overlays, distinguishes package validity and host availability from process health, and carries graph-local team-definition authority through prompt construction. It is self-validated and ready for architecture re-review; no pass is assumed.
- Why this revision entry is recorded: CRR-010 exposed a false solution premise—“a bundled resource default is runnable”—and a composition-critical authority escape that affect requirements, lifecycle semantics, business launch, maintained package data, conformance, and documentation.
- Resolution:
  - Made `standalone.enabled` a source-only devkit declaration and required standalone-capable packages to resolve `runtimeKind` plus `llmModelIdentifier` for every required slot/effective leaf.
  - Defined pure package validation, reused by `build`/project-root `validate`/`dev`/`start`, that fails incomplete packages with exact slot/member/source diagnostics before an invalid package can be treated as runnable.
  - Fixed the then-provisional first Brief conformance baseline to application-owned `codex_app_server` / `gpt-5.6-sol` defaults for researcher and writer; this historical SR-004 selection is superseded by the user-confirmed Luna target recorded in SR-005. Secrets, endpoints, installed runtime/model availability, and credentials remain host-managed.
  - Replaced the nullable-profile/`READY` premise with one host-neutral `ApplicationLaunchConfigurationService` and exact aggregate outcomes: `RUNNABLE`, `INVALID_PACKAGE`, and `HOST_REQUIREMENT_MISSING`.
  - Defined deterministic package precedence, optional Studio/host overlay precedence, value provenance, atomic portable `llmConfig` handling, and the host runtime/model/credential validation ports.
  - Kept platform/process health separate from application-run readiness. Standalone refuses listen/static business UI when non-runnable; Studio may remain healthy and display diagnostics but uses the same authority before entry/run.
  - Reused current persisted configuration rows as optional overlays, added reset-by-delete semantics, and rejected package-default copying/seeding, package mutation, schema migration, silent fallback, and a mandatory standalone setup UI.
  - Made business launch consume only `requireRunnable(slot)` and removed the designed allowance for Brief hard-coded resource, request-model rescue, or SDK null-profile construction.
  - Specified the exact graph-local propagation from `createApplicationRunAuthorities` through mixed-team factory/manager/registries/handles to a `MemberTeamContextBuilder` constructed with the selected graph's `AgentTeamDefinitionService`.
  - Added package-default, host-requirement, Studio override/reset, business-boundary, nested-precedence, `llmConfig`, and final-prompt conformance cases.
- Approved behavior or requirement IDs affected: BEH-004–BEH-008; REQ-004–REQ-008; AC-005, AC-006, AC-011, AC-013–AC-017; UC-002, UC-004, UC-009, UC-012, UC-015, UC-018–UC-022; DS-003, DS-005, DS-006, DS-009–DS-013.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behavior/requirements/acceptance/use cases/constraints; [investigation-notes.md](investigation-notes.md) evidence, current gaps, configuration semantics, graph path, and decisions; [design-spec.md](design-spec.md) DS-011–DS-013, launch-configuration authority, readiness, lifecycle, business boundary, files/interfaces/removals/sequences; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) so the original proposal is assessed against self-contained package defaults and truthful readiness; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-009, 22 use cases, and 30 reachable scenarios. No new supplement is required.
- Downstream and architecture-review impact: Architecture review must verify CR-006/CR-007 are resolved by the package-baseline/effective-authority design rather than by a mandatory standalone setup surface, and verify CR-008's exact graph propagation. Implementation remains blocked until that review passes. After implementation-owned correction, the package must return through source review and API/E2E.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: API/E2E must run fresh-root standalone success on a host where the declared runtime/model/credential is actually available, assert explicit `HOST_REQUIREMENT_MISSING` elsewhere, prove incomplete-package rejection, exercise Studio override/reset without package mutation, and assert the package team instruction in the final prompt using distinct catalogs. Existing worker recovery and vault/Search/event-pipeline cleanup evidence also remains required. Optimized distribution, mandatory standalone override UI/CLI, authentication, public-internet operation, marketplace isolation, and repository-wide DI remain out of scope.

### SR-005 — Invalid saved host override without package blame or silent fallback

- Triggering role, report path, and round: `architecture_reviewer` round 4 in [design-review-report.md](design-review-report.md), indexed as `ARCH-REV-004` in [architecture-review-revision-record.md](architecture-review-revision-record.md), plus the user’s exact maintained-model confirmation before the SR-005 handoff.
- Triggering finding IDs: AR-007, self-validation correction SV-010, and the user-confirmed exact `gpt-5.6-luna` target.
- Prior authoritative result: `Fail — Design Impact`. Round 4 verified DS-011, DS-012’s package-baseline/valid-override/host-capability path, and DS-013, but found that SR-004 could not represent a valid package plus an unresolvable saved shared resource or stale saved member topology.
- Current authoritative result: DS-012 keeps the approved `RUNNABLE`, `INVALID_PACKAGE`, and `HOST_REQUIREMENT_MISSING` statuses while assigning invalid/stale saved overrides to host-local `HOST_REQUIREMENT_MISSING` with `HOST_OVERRIDE` issues. The package baseline and row remain visible, the affected effective configuration is null, launch is blocked, and only explicit replacement or Reset/delete permits package-default reevaluation. DS-011 now records the user-confirmed `codex_app_server` / `gpt-5.6-luna` default for Brief researcher, Brief writer, and the Socratic tutor. The package is self-validated and ready for architecture re-review; no pass is assumed.
- Why this revision entry is recorded: Studio supports both saving a shared team override and deleting that shared team. Current readers also detect stale member topology. That product-reachable host state must not be mislabeled as package corruption or bypassed.
- Resolution:
  - Removed duplicated configuration arrays from `ApplicationLaunchReadiness`; aggregate readiness now contains only the terminal status and typed issues, while per-slot views are the single configuration projection.
  - Defined exact evaluation precedence: package baseline -> saved override validity -> valid overlay/effective configuration -> host capability.
  - Reserved `INVALID_PACKAGE` for package-scoped defects. Defined `HOST_REQUIREMENT_MISSING` as valid package plus host-local blocker and discriminated `HOST_OVERRIDE` from `HOST_CAPABILITY`.
  - Added per-slot `savedOverrideState` (`ABSENT`, `VALID`, `INVALID`, `NOT_EVALUATED`), nullable `effectiveConfiguration`, scoped issues, and `canResetToPackageDefaults`.
  - For missing selected resource or stale member topology, preserved the valid package baseline, raw saved override, timestamp, resource/member identity, and issue; set the affected effective configuration null; rejected `requireRunnable`; forbade automatic row deletion/rewrite/member dropping/package fallback.
  - Specified Studio presentation, explicit replacement, and Reset DELETE behavior. The baseline is explanatory only while the higher-precedence invalid row exists.
  - Preserved direct-use/no-migration semantics for valid and invalid existing rows and added explicit table-driven/integration scenarios for shared-resource deletion and stale route/agent topology.
  - Replaced the provisional maintained-model target with the user-confirmed `codex_app_server` / `gpt-5.6-luna` baseline for Brief researcher, Brief writer, and the Socratic tutor. Recorded Brief’s incomplete current values and Socratic’s current Sol value as implementation deltas; a host without Luna must report `HOST_CAPABILITY` rather than substitute a model.
- Approved behavior or requirement IDs affected: BEH-004, BEH-006, BEH-007; REQ-004, REQ-006, REQ-007; AC-005, AC-012, AC-014–AC-016; UC-004, UC-015, UC-018–UC-021, UC-023; DS-011, DS-012.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behavior/use case/REQ-007/AC-014–AC-016/persistence/risks/approval; [investigation-notes.md](investigation-notes.md) ARCH-REV-004 evidence/current-state branch/model confirmation/decisions/review guidance; [design-spec.md](design-spec.md) AR-007 map, DS-011 maintained defaults, and DS-012 shapes/evaluation/example/UI/persistence/interfaces/sequence/risks/guidance; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the invalid-override proof behavior/exit criterion/decision and exact Luna proof target; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-010, UC-023, SV-C31, SV-C32, and the exact maintained-model cases. No new supplement is required.
- Downstream and architecture-review impact: Architecture should verify AR-007 is resolved without a fourth product status, schema migration, compatibility path, or fallback. CR-006/CR-007 then become fully resolved in design; CR-008 remains resolved in design and implementation-pending. Implementation remains blocked until review passes.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation/API-E2E must prove missing selected shared resource and stale member topology preserve the row/baseline, return exact `HOST_OVERRIDE` diagnostics, leave the affected effective configuration null, block entry/`requireRunnable`, and transition only after explicit replace/reset. It must also prove fresh-root runs with the exact Luna defaults where available and `HOST_CAPABILITY` failure without substitution where unavailable. Existing SR-004 prompt, worker-recovery, and cleanup evidence remains required.

### SR-006 — Authoritative selected-resource sparse editing and recursive portable package policy

- Triggering role, report path, and round: `code_reviewer` full implementation-source review `CRR-012` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), after implementation handoff `IR-007`.
- Triggering finding IDs: `CR-012` (`Design Impact`), the remaining bounded portion of `CR-009`, and self-validation correction `SV-011`.
- Prior authoritative result: SR-005 passed architecture as `ARCH-REV-005`; implementation continued through IR-006/IR-007. `CRR-012` returned `Fail — Design Impact`, score 88/100. CR-010 and CR-011 are resolved in source. CR-009 is partially resolved: exact token-count fields pass and some host fields fail, but nested password/authorization/access-token-value fields remain accepted.
- Current authoritative result: The revised design keeps `ApplicationLaunchConfigurationService` authoritative, exposes a distinct selected-resource pre-overlay baseline and no-write unsaved-selection preview, removes Studio’s post-overlay/wrong-resource inference, defines mixed-runtime sparse-edit semantics and PUT revalidation, and centralizes recursive portable-field validation. It is self-validated and ready for architecture re-review; no architecture, implementation, or API/E2E pass is assumed.
- Why this revision entry is recorded: Studio’s supported alternate-resource selector and sparse “inherit” fields make both first-save and later field-clearing paths product-reachable. The server already computes the required selected baseline, but the cross-boundary slot view omits it. Separately, real package probing proves nested secret-bearing fields can cross the package boundary. Both issues require the owning solution boundary to change before local implementation can resume.
- Resolution:
  - Defined four non-overlapping launch stages: manifest `packageBaseline`, current `selectedResourceBaseline`, sparse `savedOverride`, and post-overlay `effectiveConfiguration`. None can substitute for another.
  - Clean-cut renamed `ApplicationLaunchPackageBaselineBuilder` to `ApplicationLaunchResourceBaselineBuilder` because it resolves manifest, alternate bundled, and shared resources. No alias or compatibility wrapper remains.
  - Separated definition provenance (`PACKAGE_*_DEFAULT`, `SELECTED_RESOURCE_*_DEFAULT`) from host field provenance (`HOST_SLOT_OVERRIDE`, `HOST_MEMBER_OVERRIDE`); host provenance cannot appear in a definition baseline.
  - Added `selectedResourceBaseline` to each stored slot view. No row makes it equal the package baseline; a valid alternate row exposes its current pre-overlay definition baseline; deleted/unavailable selection exposes null; stale topology may expose the current baseline for diagnosis/explicit replacement while the row remains invalid and non-runnable.
  - Added closed `ApplicationLaunchSelectionPreview` and `previewSelectedResourceBaseline(applicationId, slotKey, ref)`/Studio POST boundary. It validates exact identity/source/kind and resolves through the same graph-local builder without store access/write, override overlay, host validation, readiness transition, or package fallback.
  - Specified Studio request identity/cancellation, save-pending/invalid gating, refresh after catalog/definition/save/reset changes, and PUT as final resource/topology concurrency authority. A changed resource/topology rejects and reloads; preview/baseline is never persisted.
  - Removed the Studio heuristic that uses manifest package or old effective state as alternate-resource inheritance. Clearing a host field persists absence and reveals the exact selected definition value.
  - Defined mixed-runtime editing: blank team-wide fields mean per-member inheritance; a common inherited runtime may drive bulk model choice; mixed inherited runtimes disable bulk model selection until an explicit common runtime is saved. No AutoByteus/previous-result fallback is allowed.
  - Added one `ApplicationPortableLaunchConfigPolicy` with closed runtime/root/pricing schemas and recursive object/array traversal. It preserves exact `max_tokens`, `token_limit`, `safety_margin_tokens`, and typed pricing fields, while rejecting password/passphrase, secrets/credentials, API/private keys, authorization/bearer, access/refresh/ID-token values, endpoints/base URLs/API bases/hosts, workspaces, and machine paths at any depth. Diagnostics carry exact paths/reasons but never values. No app-specific/compatibility exception is permitted.
  - Preserved current rows/schema as `Directly Usable — No Migration`; manifest/selected baselines and preview are computed only. Preserved complete Codex/Luna defaults, three readiness statuses, invalid-row/no-fallback behavior, standalone pre-listen gate, graph-local prompt authority, package immutability, no-auth scope, and all prior composition decisions.
  - Added SV-C33–SV-C35 for unsaved alternate preview/race, saved alternate field clearing/mixed runtime, and recursive portable positive/negative behavior. Recorded that API/E2E must replace the obsolete no-context auto-repair test after source review passes.
- Approved behavior or requirement IDs affected: BEH-004, BEH-006, BEH-007; REQ-004, REQ-006, REQ-007; AC-005, AC-006, AC-012, AC-014–AC-016; UC-002, UC-004, UC-009, UC-015, UC-018–UC-021, UC-023; DS-011 and DS-012. This is implementation precision for the already-approved sparse Studio override/no-secret package contract, not new product behavior.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) current/desired behavior, findings, UC-019/UC-020, REQ-007, AC-014–AC-016, constraints/persistence/risks; [investigation-notes.md](investigation-notes.md) CRR-012 evidence, current behavior/gaps, persistence, decisions, reviewer guidance; [design-spec.md](design-spec.md) CR-009/CR-012 map, DS-011 policy, DS-012 view/preview/provenance/edit/concurrency/mixed-runtime contract, files/interfaces/sequence/risks/guidance; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with selected-resource edit/policy target behavior, exit criteria, roadmap, decisions, and final recommendation; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-011 and SV-C33–SV-C35. No new supplement is required.
- Downstream and architecture-review impact: Architecture must verify the server boundary is now sufficient for both persisted and unsaved alternate-resource editing, the shared shapes do not overlap, no UI traversal/fallback/data migration was introduced, and the recursive policy is narrow enough to preserve valid tuning while rejecting actual host secrets. Implementation remains paused until architecture passes. After implementation, the full source review and API/E2E path must run again.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation/API-E2E must prove first-save alternate preview, saved-field clearing, mixed-runtime controls, stale response/PUT race behavior, recursive real-package policy, invalid-row/reset behavior, direct-use storage, exact definition/host provenance, and the real prompt/provider/events/artifacts journey. The validation host must have the declared Codex/Luna capability for the positive real run; otherwise it must prove `HOST_REQUIREMENT_MISSING` without substitution. Optimized distribution, optional standalone override UI/CLI, authentication, marketplace isolation, and repository-wide DI remain out of scope.

### SR-007 — Composition-owned internal Agent Tools MCP transport

> **Withdrawn / superseded:** `CRR-016` corrected the premise behind this revision before architecture approval. Architecture review was stopped, unapproved production drafts were removed, and none of the aggregate-runtime/ports/publication-bridge design below is current authority. See SR-008.

- Triggering role, report path, and round: `code_reviewer` focused API/E2E failure-origin review `CRR-015` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), after `API-REV-005` execution on implementation/source-review baseline IR-009/CRR-014.
- Triggering finding IDs: `CR-013` (`Design Impact`), `APIE2E-BRIEF-003` / `APIE2E-F005`, `APIE2E-STANDALONE-MCP-001`, and self-validation correction `SV-012`. `APIE2E-REPO-005` is retained separately as `Unclear` and does not drive this solution revision.
- Prior authoritative result: SR-006 passed architecture as `ARCH-REV-006`; IR-008/IR-009 implemented it and `CRR-014` passed source review. API-REV-005 proved complete package defaults, truthful readiness, binding/team creation, and Agent Tools session descriptor issuance in fresh standalone, then failed because standalone did not register the advertised session route.
- Current authoritative result: The revised design makes `/mcp/agent-tools/:sessionId` a required internal runtime callback in both compositions, distinct from Studio's optional external `/mcp/gateway`; one explicit `AgentToolsMcpRuntime` governs session issue, authorization, configured-tool dispatch, revocation, and close. It is self-validated and ready for architecture re-review; no architecture, implementation, or API/E2E pass is assumed.
- Why this revision entry is recorded: AC-005/AC-006 require the real tool-dependent team/artifact journey in both hosts. The current standalone run reaches Codex/Luna and receives a descriptor but the route returns generic 404, so no configured file/publication/message tools or artifacts occur. A one-line route fix would also implicitly select process-global session/catalog/dispatcher authorities outside the reviewed composition boundary.
- Resolution:
  - Added DS-014 from guarded backend launch through binding/team runtime, exact session capability issue, descriptor delivery, authenticated internal callback, configured tool execution, researcher-to-writer handoff, lifecycle events/artifacts, and revocation.
  - Classified `/mcp/agent-tools/:sessionId` as required internal application-run transport in Studio and standalone. It is absent from browser bootstrap and package contracts. External `/mcp/gateway` remains a separate optional Studio-only integration surface.
  - Defined one process/composition-scoped `AgentToolsMcpRuntime` hiding exactly one registry, catalog, executor, dispatcher/result mapper, and session service. Run provisioning/cleanup receives only its session port; Fastify registration receives only its matching HTTP port.
  - Removed composition-path global Agent Tools defaults from Codex/Claude bootstrap/session factories, AgentRunManager, mixed-member cleanup, route, registry/catalog/executor/dispatcher/session construction. No compatibility route or second tool system is retained.
  - Made catalog/tool adapter construction explicit. Added one fail-fast `DeferredPublishedArtifactPublicationPort`, bound exactly once to the graph-local publication service before P6A readiness, so configured publish calls cannot reach a global or wrong application graph.
  - Added P6A exact runtime readiness and changed S2 so both hosts seed the actual internal base URL immediately after listen and before recovery/business runs. One path builder is shared by descriptor/registrar/tests; wildcard bind normalizes to loopback and request Host is never reflected.
  - Preserved current cryptographic session ID/token, token-hash-only storage, timing-safe compare, missing-bearer 401, non-enumerating wrong/unknown/revoked 404, `cors:false`, loopback-origin gate, redacted descriptor/logging, immutable application/run/team/member/configured-tool context, and no database persistence.
  - Defined owner-based revocation and shutdown: stop new ingress/runs, stop workers/member handles and revoke their sessions, revoke remaining application sessions, close the aggregate Agent Tools runtime, then close event pipeline, vault, and Prisma. Sessions do not survive restart.
  - Extended AC-009/AC-010 exact route inventory and reserved `/mcp/agent-tools/*` before static fallback. Standalone still exposes no Studio/admin registries or external MCP gateway.
  - Added SV-C36–SV-C38 for the real configured-tool/handoff/artifact journey, capability/auth/revocation negatives, paired-port authority identity, external-gateway exclusion, base-URL ordering, deferred publication binding, and cleanup.
- Approved behavior or requirement IDs affected: BEH-004–BEH-006; REQ-004, REQ-005, REQ-007; AC-005, AC-006, AC-009, AC-010; UC-004, UC-009, UC-014, UC-017, UC-018; DS-004, DS-005, DS-014. This completes previously approved behavior rather than adding a new user feature or authentication subsystem.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behavior/REQ-004–REQ-005/AC-005–AC-006/AC-009–AC-010/use cases/constraints; [investigation-notes.md](investigation-notes.md) API-REV-005/CRR-015 evidence, source/composition trace, decisions, review guidance; [design-spec.md](design-spec.md) DS-014, lifecycle/route/security/composition/ports/files/removals/sequence/risks; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the internal-versus-external MCP correction, target behavior, MVP-EC-019, roadmap, and decision table; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-012 and SV-C36–SV-C38. No new supplement is required.
- Downstream and architecture-review impact: Architecture must verify DS-014 spans the full real business consequence, exact paired authority, route/public-surface distinction, explicit adapter/publication graph boundary, capability security/base URL, and close order without global fallback or external-gateway expansion. Implementation remains paused until architecture passes. After implementation, source review and API/E2E must rerun.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: API/E2E must rerun the exact standalone route and real Brief configured-tool/team/artifact cases, prove Studio and standalone paired-port identity/security/revocation/cleanup, and preserve prior affected regressions. `APIE2E-REPO-005` remains separately unattributed. Optimized distribution, user authentication, public-internet operation, marketplace isolation, persisted sessions, and repository-wide DI remain out of scope.

### SR-008 — Restore the correct Agent Tools projection and reroute CR-013 as a Local Fix

> **Scope clarified by SR-009:** runtime-internal file-tool behavior appears below only as historical failure-origin context. It is not an application-framework requirement, change, or acceptance target.

- Triggering role, report path, and round: `code_reviewer` corrective failure-origin result `CRR-016` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), superseding `CRR-015` before SR-007 architecture approval.
- Triggering finding IDs: `CR-013` (`Local Fix`), `APIE2E-BRIEF-003` / `APIE2E-F005`, `APIE2E-STANDALONE-MCP-001`, and self-validation correction `SV-013`. `APIE2E-REPO-005` remains separately `Unclear` and does not drive this revision.
- Prior authoritative result: SR-006 passed architecture as `ARCH-REV-006`. SR-007/SV-012 temporarily proposed a broad Agent Tools runtime refactor from the superseded CRR-015 premise, but architecture review was stopped before decision and the implementation engineer restored every affected production source path to HEAD.
- Current authoritative result: `ARCH-REV-006` remains the reviewed architecture. Codex/Claude native file tools remain native; the existing internal Agent Tools MCP gateway exposes eligible server-owned adapters and configured `ToolOrigin.MCP` tools; the external MCP gateway remains Studio-only. The only implementation defect is that standalone omits the existing `registerAgentToolsMcpRoutes(app)` call. No architecture re-review is required.
- Why this revision entry is recorded: CRR-016 verified that `buildDefaultAgentToolMcpAdapterProviders()` intentionally has no native file adapter and that `ConfiguredMcpAgentToolSourceResolver` forwards only MCP-origin tools. Therefore package `toolNames=[write_file,publish_artifacts,send_message_to]` cannot be treated as an MCP projection. The legitimate failure remains product-reachable because eligible server-owned publication/message tools target an advertised route that standalone has not registered.
- Resolution:
  - Withdrew the unapproved SR-007 aggregate `AgentToolsMcpRuntime`, paired ports, path builder, deferred publication bridge, readiness phase, and shutdown redesign.
  - Preserved the reviewed/implemented source state; the implementation engineer confirmed `git diff --name-only -- autobyteus-server-ts/src` is empty after cleanup.
  - Reclassified DS-014 as route parity and reachable-flow documentation over the existing Agent Tools subsystem, not a new authority.
  - Required standalone composition to import and await the existing `registerAgentToolsMcpRoutes(app)` before static/SPA fallback, matching Studio's internal-route registration.
  - Preserved current session descriptor, registry/catalog/dispatcher/adapters, bearer/auth behavior, base-URL behavior, revocation/cleanup, and external-gateway exclusion.
  - Explicitly retained runtime-native `write_file`; the internal gateway remains limited to eligible server-owned adapters such as `publish_artifacts` and `send_message_to` plus configured MCP-origin tools.
  - Corrected executable proof expectations to inspect the issued descriptor and `tools/list` rather than infer MCP tools from package `toolNames`.
  - Rejected native file adapters, a new runtime/ports layer, publication bridge, duplicate route, alias, fallback, persisted session path, user authentication, or external gateway exposure.
- Approved behavior or requirement IDs affected: BEH-004–BEH-006; REQ-004, REQ-005; AC-005, AC-006, AC-009, AC-010; UC-004, UC-009, UC-014, UC-017, UC-018; DS-004, DS-005, DS-014. This restores the correct implementation boundary for previously approved behavior; it adds no product feature or public surface.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behavior/findings/ACs and local-fix routing; [investigation-notes.md](investigation-notes.md) corrected source evidence/tool taxonomy/current-state decision; [design-spec.md](design-spec.md) bounded DS-014/composition/file/sequence/test map; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the native/server/configured-MCP distinction and bounded proof; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-013 and corrected SV-C36–SV-C38. SV-012 remains only as explicitly withdrawn chronology. No new supplement is required.
- Downstream and architecture-review impact: No architecture-review round is needed because CRR-016 classifies the defect as `Local Fix` and leaves `ARCH-REV-006` authoritative. Implementation may change only the standalone composition registration and implementation-owned focused coverage, then return through full source review and API/E2E. API/E2E owns correction of its durable gateway-tool expectations and the real rerun.
- Next recipient or routing: `implementation_engineer`.
- Remaining gaps or risks: Implementation must prove route registration precedes static fallback, missing bearer reaches 401 rather than generic 404, unknown/unavailable session remains 404, and external `/mcp/gateway` remains absent. API/E2E must then prove native file output separately from actual gateway `publish_artifacts`/`send_message_to`, team handoff, events, artifacts, and cleanup. `APIE2E-REPO-005` remains separately unattributed. Optimized distribution, user authentication, public-internet operation, marketplace isolation, persisted sessions, and repository-wide DI remain out of scope.

### SR-009 — Clarify MCP ownership and exclude runtime-internal tooling

- Triggering role, report path, and round: User design discussion after SR-008 and the bounded implementation commit `e8e06afdd`; supporting review authority remains `CRR-016` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md).
- Triggering finding IDs: `CR-013` remains the same bounded `Local Fix`; self-validation clarification `SV-014`. No new architecture finding or requirement is introduced.
- Prior authoritative result: SR-008 preserved `ARCH-REV-006`, corrected the over-broad `CRR-015` premise, and routed only standalone registration of the existing Agent Tools route to implementation. Its references to runtime file-tool behavior were failure-origin context, not an approved application-framework requirement.
- Current authoritative result: Both hosts register the existing run-scoped `/mcp/agent-tools/:sessionId` transport. General `/mcp/gateway` and Studio MCP Server Management remain Studio/platform integration surfaces and are not standalone application dependencies. A future application-owned MCP declaration/provisioning capability is separate work. Codex/Claude runtime-internal tooling is outside this ticket: it is not designed, modified, or tested here, so the branch simply retains the untouched `origin/personal` behavior.
- Why this revision entry is recorded: The user confirmed that applications need the platform-provided run-scoped Agent Tools callback, but do not inherit Studio-configured MCP servers or Studio's general external-client gateway. The user then clarified that runtime-internal Codex/Claude file tools must not become part of this ticket merely to explain the prior review error.
- Resolution:
  - Kept CR-013 strictly at the standalone composition boundary: mount the existing Agent Tools registrar before static fallback.
  - Defined the general gateway precisely as the Studio process surface that exposes current host-registered `ToolOrigin.MCP` tools to external MCP clients; it is not the per-run application callback and does not provision MCP servers.
  - Confirmed standalone does not expose the general gateway and does not copy or inherit Studio MCP configuration.
  - Deferred application-owned MCP declarations, host-secret binding, provisioning, lifecycle/readiness, and application-scoped registration to a separate design/ticket.
  - Removed runtime-internal file-tool behavior from application-framework requirements, acceptance criteria, implementation scope, and API/E2E proof. Historical SR-007/SR-008 text remains chronology only and is superseded by this scope statement.
- Approved behavior or requirement IDs affected: Clarifies BEH-004–BEH-006, REQ-004–REQ-005, AC-005, AC-006, AC-009, and AC-010 without changing their product behavior. DS-014 remains the same bounded route-parity map.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) scope/behavior/acceptance boundaries; [investigation-notes.md](investigation-notes.md) source evidence, MCP ownership, and current branch state; [design-spec.md](design-spec.md) terminology, DS-014 boundaries, file/change map, and validation ownership; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the exact general-gateway/application-MCP decision; updated evidence-only [design-self-validation.md](design-self-validation.md) with `SV-014`. No new supplement is required.
- Downstream and architecture-review impact: No architecture re-review is required because no architecture, public contract, source file, or source-scope decision changes. Commit `e8e06afdd` contains only the already-routed composition fix and remains subject to the normal implementation/source-review/API-E2E flow. Implementation and API/E2E must not add runtime-internal tool changes or assertions for this ticket.
- Next recipient or routing: `implementation_engineer`, as a clarification to the existing Local Fix handoff.
- Remaining gaps or risks: Implementation/source review must confirm the bounded registrar change and route-order/negative-surface behavior. API/E2E must inspect the real Agent Tools descriptor/`tools/list` and prove eligible `publish_artifacts`/`send_message_to`, handoff, events, artifacts, and cleanup. The future application-owned MCP provisioning capability remains intentionally unimplemented and must not be approximated by copying Studio state or exposing `/mcp/gateway`.

### SR-010 — Bind authenticated application sessions to graph-local publication

- Triggering role, report path, and round: `code_reviewer` focused failure-origin review `CRR-020` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), after `API-REV-007` executed IR-011/CRR-019.
- Triggering finding IDs: `CR-015` (`Design Impact`), `APIE2E-STANDALONE-MCP-003` / `APIE2E-F007`, and self-validation correction `SV-015`. `APIE2E-REPO-005` remains separately `Unclear`.
- Prior authoritative result: SR-009 preserved the user-confirmed route/general-gateway/native-tool boundary, and IR-010/IR-011 resolved route parity and graph-local Codex definition construction. CRR-019 passed that source. API-REV-007 then disproved SR-008’s narrower premise that the default publication provider family worked unchanged.
- Current authoritative result: The revised DS-014 defines one composition-owned Agent Tools process authority, one graph-scoped application session authority, one authenticated session-bound publication port, and one bind-once fail-closed construction-cycle seam. The package is self-validated and ready for architecture re-review; no architecture, implementation, or API/E2E pass is assumed.
- Why this revision entry is recorded: Both real Brief members authenticate, list actual `publish_artifacts`/`send_message_to`, and complete recipient-name handoff, but all five publication calls use the process-global publication service and reject the graph-local member as inactive. The graph already constructs the correct service; the missing design is its exact connection to session issue/route dispatch plus lifecycle ordering.
- Resolution:
  - Preserved the established route, capability token/hash, origin/content negotiation, descriptor/base URL, eligible tool projection, 401/404 behavior, successful recipient-name messaging, Studio-only external gateway, and provider-native tool scope.
  - Added `AgentToolsMcpProcessAuthority` as the one composition owner of the exact registry, catalog/provider family, executor, dispatcher, and route dependencies. Route registration and application scoped-session creation use narrow outputs from that same owner; neither resolves independent defaults. Non-application runs receive a separately explicit general-process session authority so they retain their existing process publication owner without application fallback.
  - Added `ApplicationAgentToolsSessionAuthority` over the process family. It attaches graph execution authorities to sessions, is injected into application Codex/Claude session creation, tracks only sessions it creates, blocks issue during close, and revokes its scope idempotently.
  - Added a narrow `PublishedArtifactPublicationPort` on the non-wire authenticated session. `PublishArtifactsMcpAdapterProvider` delegates only through that port and may not capture/discover `getPublishedArtifactPublicationService()` or `AgentRunManager.getInstance()`.
  - Added one `DeferredPublishedArtifactPublicationPort`: create before runtime factories/run manager, bind exactly once to the graph-local publication service after manager/relay construction, assert at P6A before readiness, reject pre-bind/rebind/post-close, and fail before any snapshot/journal/relay/projection mutation.
  - Defined stop/restart semantics: block new issue/runs, stop run/member handles through the scoped revoker, revoke remaining scope sessions, close the graph port, then close streaming/process authority/event/vault/Prisma owners. Restart creates a new scope; no session/port persists.
  - Inventoried maintained reachable adapters only: Brief researcher/writer and Socratic tutor use graph-sensitive `publish_artifacts`; Brief’s recipient-name `send_message_to` remains its proven session-member-context path. Browser/media/task/configured-MCP/provider-native paths receive no speculative refactor.
  - Added exact dependency, interface, file, removal, sequence, tradeoff, risk, and guidance maps. Rejected mutable singleton replacement, request-time graph lookup, graph-specific catalog merge, package branches, route aliases, persistence, compatibility fallback, external-gateway expansion, and broad SR-007 restoration.
  - Added SV-C39–SV-C42 plus revised SV-C03/C11/C15/C36–C38 for deliberately distinct global/graph managers, default-provider authenticated route dispatch, bind/close failures, scoped revocation/restart, maintained adapter reachability, and real Studio/standalone journal/projection.
- Approved behavior or requirement IDs affected: BEH-004–BEH-006; REQ-004, REQ-005, REQ-007; AC-005, AC-006, AC-009, AC-010, AC-016; UC-004, UC-008, UC-009, UC-014, UC-017, UC-018; DS-004, DS-005, DS-014. This completes already-approved AutoByteus publication behavior; it adds no new user feature, route, package field, MCP provisioning contract, or runtime-native tool behavior.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behaviors/findings/design health/recommendations/REQ-004–REQ-005/AC-005–AC-006/constraints/risks; [investigation-notes.md](investigation-notes.md) API-REV-007/CRR-020 evidence, source/cycle/adapter trace, current gaps, decisions, and re-review notes; [design-spec.md](design-spec.md) CR-015 map, DS-014, P6A/stop, exact graph, boundaries, files/removals/sequence/tradeoffs/risks/guidance; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the graph-publication gap, MVP-EC-020, roadmap/decision/final recommendation; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-015 and 42 reachable cases. No new supplement is required.
- Downstream and architecture-review impact: Architecture must verify the process authority is not a container, session issue and route dispatch share one family, publication selection occurs only through authenticated session authority, the deferred port is proportionate/fail-closed, lifecycle stop cannot leave sessions referencing a disposed graph, and the maintained adapter inventory does not over-expand scope. Implementation remains paused until architecture passes.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation and API/E2E must prove distinct-global/graph default-provider dispatch, pre-bind/rebind/post-close failure before mutation, scope-only revocation/restart, unchanged route/security/tool projection, real standalone and Studio publication/message/handoff/journal/projection, and cleanup. `APIE2E-REPO-005` remains separate and unattributed. Application-owned MCP provisioning, provider-native tools, optimized distribution, authentication, marketplace isolation, and repository-wide singleton removal remain out of scope.

### SR-011 — Familiar application-framework roles without behavior change

- Triggering role, report path, and round: `code_reviewer` fresh framework responsibility/naming review `CRR-028` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), after functional source/API-E2E/test review passed.
- Triggering finding IDs: `CR-018` (`Design Impact`) and self-validation correction `SV-016`.
- Prior authoritative result: SR-010 passed architecture through `ARCH-REV-008`; implementation reached `IR-015`; source behavior passed in `CRR-026`; real dual-host/package-parity coverage passed at 98.3% in `API-REV-010`; durable-test review passed in `CRR-027`. `CRR-028` then returned `Fail — Design Impact` for naming/readability only. No functional failure is open.
- Current authoritative result: The revised package defines one bounded familiar role vocabulary and an exact current-name/file to target-name/file map for the central application-framework spine. It preserves every passed instance, order, trigger, route, package, data, and lifecycle behavior; specifies clean removal of the mapped old names/files; and is ready for architecture re-review. No architecture approval or implementation completion is assumed.
- Why this revision entry is recorded: The current abstract suffixes overlap across an assembled server, live service set, process MCP subsystem, scoped session collection, run-manager lifetime owner, stop-only coordinator, concrete service groups, and bind-once proxies. The user independently could not infer what `createApplicationPlatformRuntimeGraph` or “creates agent/team execution” meant. The intended behavior is simpler: the host prepares application services/managers; application business demand starts new runs and established recovery restores only recorded runs.
- Resolution:
  - Defined `Server`, `Runtime`, `Manager`, `Supervisor`, `Coordinator`, `Service`, `Publisher`, `Handler`, `BindOnce*`, `Registry`, `Gateway`, `Store`, `Resolver`, `Builder`, and `Factory` by concrete responsibility and lifetime.
  - Reserved `composition` for top-level assembly activity/the `compositions/` folder and `dependency graph` for architecture relationships. Removed central `Authority`, `Graph`, and bind-once `Port` code names where they add no unique meaning.
  - Mapped `StudioServerComposition`/builder/file to `StudioServer`/`buildStudioServer`/`build-studio-server.ts`, and mapped the standalone builder/file to `buildStandaloneApplicationServer`/`build-standalone-application-server.ts`. The returned Studio handle exposes `fastify` and `applicationRuntime`; the Studio helper/result and fields/locals become `createStudioApplicationServices`, `studioServices`, `studioServer`, and `applicationRuntime`.
  - Mapped `ApplicationPlatformRuntimeGraph`/factory/files to `ApplicationPlatformRuntime`/`buildApplicationPlatformRuntime`/target files. Added the explicit invariant and structural proof that building it prepares managers/factories/services but creates no new agent/team run.
  - Mapped the process Agent Tools owner to `AgentToolsMcpRuntime`, the scoped collection implementation/interface/factory to `ScopedAgentToolMcpSessionManager` / `AgentToolMcpSessionManager` / `ApplicationAgentToolsSessionManagerFactory`, and session execution fields to `AgentToolMcpSessionExecutionCapabilities.executionCapabilities.publishedArtifactPublisher`. Exact helpers/handles become `createSessionManager`, `createApplicationSessionManager`, `generalProcessSessionManager`, `agentToolsSessionManager`, `assertExecutionCapabilitiesReady`, and `generalProcessPublisher`.
  - Mapped `GeneralProcessRunAuthority` to `GeneralProcessRunSupervisor`, `ApplicationRunShutdownAuthority` to `ApplicationRunShutdownCoordinator`, and its narrow dependencies to `ApplicationTeamRunStopper` / `ApplicationAgentRunStopper`; matching server/runtime locals and returned properties use the same supervisor/coordinator names.
  - Mapped publication and engine callback contracts/cycle breakers to `PublishedArtifactPublisher`, `BindOncePublishedArtifactPublisher`, `ApplicationEngineEventHandler`, and `BindOnceApplicationEngineEventHandler` with exact target files; their state machines and dependency inversion are unchanged.
  - Mapped orchestration/run/Studio API builders/results from `*Authorities` to `*Services`, including files and local variables.
  - Defined exact root-export impact: `autobyteus-server-ts` is private and repository scan finds no outside consumer, so the old export is removed without an alias. No wire, route, environment, manifest, database, package, or generated application contract changes.
  - Added a clean rename sequence, old-file/export/test removal, mapped test filenames, affected module/developer documentation plan, retired-name scan, compile/import checks, structural no-run-on-build proof, identity/lifecycle regressions, and proportionate dual-host validation.
  - Explicitly rejected an automatic repository-wide rename, compatibility aliases/deprecated wrappers, duplicate tests/files, runtime/tool redesign, provider-native tool change, external-gateway expansion, or data migration.
- Approved behavior or requirement IDs affected: Added/refined BEH-009, REQ-009, AC-018, UC-024, SV-C43, and SV-C44. The correction structurally preserves BEH-001–BEH-008, REQ-001–REQ-008, AC-001–AC-017, UC-004, UC-009, UC-014, and UC-018.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) goal/behavior/findings/design health/recommendations/scope/REQ-009/AC-018/constraints/risks/approval; [investigation-notes.md](investigation-notes.md) current baseline/source log/responsibility evidence/behavior map/design health/files/decisions/reroute; [design-spec.md](design-spec.md) current-state read/terminology/vocabulary/exact map/diagram/ownership/interfaces/removals/file maps/sequencing/docs/testing/risks/guidance; this record.
- Supplemental artifacts updated, added, or removed: Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) with the current functional pass, naming correction, runtime-versus-execution distinction, MVP-EC-021, roadmap/decision/final recommendation; updated evidence-only [design-self-validation.md](design-self-validation.md) with SV-016, UC-024, SV-C43/SV-C44, principles audit, and downstream evidence. No new supplement is required.
- Downstream and architecture-review impact: Architecture must verify the role vocabulary is small and coherent, each mapped target matches concrete responsibility/scope/lifecycle, no central consumer/file/export/test/doc is omitted, the private-export/no-alias decision is sound, runtime construction does not imply run creation, and the change remains behavior-neutral and bounded. Implementation remains paused until architecture passes; after implementation, full source review and proportionate API/E2E/test review resume.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation must preserve exact constructor instance identity and shutdown order during renames, prove no new run on runtime build, remove retired names outside historical ticket records, and keep affected docs consistent. A newly evidenced supported external consumer is Design Impact rather than permission to add an alias. The branch remains behind tracked `origin/personal`; delivery owns final refresh/integration. `APIE2E-REPO-005` remains separate and `Unclear`.

### SR-012 — Narrow runtime contracts and acyclic framework construction

- Triggering role, report path, and round: `code_reviewer` fresh design-principles audit `CRR-031` in [code-review-report.md](code-review-report.md), indexed by [code-review-revision-record.md](code-review-revision-record.md), after the SR-011 naming correction completed its full implementation and executable loop.
- Triggering finding IDs: `CR-019`, `CR-020`, `CR-021` (`Design Impact`) and self-validation correction `SV-017`.
- Prior authoritative result: SR-011 passed architecture, was implemented, and its source behavior passed at 97 in `CRR-029`; real dual-host behavior and exact 73/73 package parity passed at 98.9% in `API-REV-011`; proportional durable-test review passed in `CRR-030`. `CRR-031` then returned `Fail — Design Impact` for mixed outward access, temporal package construction, and two avoidable bind-once cycles. No product behavior failure is open.
- Current authoritative result: The revised package preserves the complete fixed executable baseline while defining four exact runtime projections, a package registry/command/reconciliation split with late dependency construction, and acyclic run/publication/session plus engine/event construction. Both bind-once proxies and the broad mixed engine host are removed cleanly. The package is self-validated and ready for architecture re-review; no architecture approval or implementation completion is assumed.
- Why this revision entry is recorded: Central application-framework code still exposes mixed abstraction levels and relies on temporal callbacks and bind-later objects even though the passed behavior can be assembled from stable state/control owners before later launch owners. These are boundary/ownership and construction-order design issues, not a runtime defect or a request for broader platform behavior.
- Resolution:
  - Reduced `ApplicationPlatformRuntime` from a 19-field internal service collection to exactly `lifecycle`, `rest`, `realtime`, and `hostManagement`. REST and WebSocket registrars receive lifecycle readiness plus their exact subject projection; Studio receives package-level catalog reconciliation only. Stores, availability internals, recovery, active runs, publication, sessions, engine, dispatch, and shutdown remain private.
  - Split package work into an early `ApplicationPackageRegistryService` for records/roots/snapshots, a late `ApplicationPackageCommandService` for import/reload/remove/validation/install/rollback, and one `ApplicationCatalogRefreshCoordinator` for the exact bundle -> runtime reconciliation/availability -> agent-definition -> team-definition sequence. Removed callbacks over later-assigned `bundleService`, `applicationRuntime`, and `definitionServices`.
  - Defined the successful command sequence and the failure/rollback sequence, including bounded best-effort post-rollback reconciliation and preservation of the original error. Existing GraphQL and package wire behavior remains unchanged through separate query and command contracts.
  - Added an early `ActiveAgentRunRegistry` that owns only the active-run map. The concrete application publication service is built from that registry, scoped Agent Tools sessions receive that publisher, and run managers/services are built afterward for launch/restore/terminate plus exact registry/session cleanup.
  - Removed `BindOncePublishedArtifactPublisher` without introducing a generic deferred container. Application sessions cannot discover process-global publication; supported general-process behavior is constructed only by named assembly factories.
  - Split the broad engine host into an early `ApplicationEngineController` for worker handles/status/listeners/invocation and a later `ApplicationEngineLauncher` for bundle/storage/process start, startup coalescing, capability wiring, ensure, and stop.
  - Added a closed `ApplicationExecutionEventDispatchQueue` that carries application-ID journal wakeups only. The durable journal remains authoritative; the late dispatcher retains claim/ack/fail/retry/backoff/resume/suspend/stop, and `ApplicationReentryService` owns stop/reload/reconcile/recover/resume.
  - Removed `BindOnceApplicationEngineEventHandler` and `ApplicationEngineHostService` without a service locator, generic event bus/container, compatibility alias, optional-field shared base, application global fallback, or dual path.
  - Defined the complete thirteen-step construction order, exact shutdown order with no bind-close phase, owner/forbidden-dependency map, add/modify/remove file inventory, rollback and refresh rules, refactor sequence, and no-data-migration decision.
  - Added UC-025, DS-015, SV-C45–SV-C51, and the complete design-principles audit. The complete `API-REV-011` source/API/E2E loop is the mandatory post-implementation characterization baseline.
- Approved behavior or requirement IDs affected: Added BEH-010, REQ-010, AC-019–AC-021, UC-025, DS-015, SV-017, and SV-C45–SV-C51. The correction structurally preserves BEH-001–BEH-009, REQ-001–REQ-009, AC-001–AC-018, UC-001–UC-024, every route/wire/package/database/default/override/provider/MCP/publication/handoff/projection/recovery/remount/restart/shutdown behavior, and the user-confirmed no-run-on-runtime-build invariant.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) behavior/requirements/acceptance/use-case/scope/constraints/risks; [investigation-notes.md](investigation-notes.md) fixed baseline, source evidence, ownership findings, supplement inventory, and reroute; [design-spec.md](design-spec.md) CRR-031 resolution map, DS-015, primary/return spines, runtime contracts, package refresh/rollback, construction graphs, dependencies, files/removals/sequence/shutdown/testing/risks; this record.
- Supplemental artifacts updated, added, or removed: Added intended-architecture supplement [application-framework-architecture-simplification.md](application-framework-architecture-simplification.md), which requires architecture approval and records the complete current/target spines, exact contracts, owners, forbidden dependencies, construction order, change inventory, rollback, shutdown, validation, and no-migration decision. Updated approved [proposal-critical-analysis.md](proposal-critical-analysis.md) and evidence-oriented [design-self-validation.md](design-self-validation.md).
- Downstream and architecture-review impact: Architecture must verify that the four projections are sufficient without boundary bypass, package state/commands/reconciliation have exact success and rollback semantics, both construction graphs are acyclic without empty indirection, and every passed behavior remains fixed. Implementation and delivery remain paused until architecture passes. After implementation, the full source review and complete API-REV-011-equivalent API/E2E/test review are mandatory.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation must preserve exact object identity, route and wire contracts, package refresh results, provider/session/run ownership, event-journal retry semantics, worker recovery/remount, and shutdown order while deleting the obsolete proxies/broad host. Structural checks must prevent projection bypass, temporal callbacks, generic container/event-bus replacements, application global fallbacks, and compatibility aliases. `APIE2E-REPO-005` remains separate and `Unclear`; delivery owns the final tracked-base refresh/integration after all gates pass.

### SR-013 — Preserve artifact-worker delivery and exact run-resource cleanup

- Triggering role, report path, and round: `architecture_reviewer` round 10 `ARCH-REV-010` in [design-review-report.md](design-review-report.md), indexed by [architecture-review-revision-record.md](architecture-review-revision-record.md), reviewing SR-012.
- Triggering finding IDs: `AR-008` / `MP-ARCH-010-001`, `AR-009` / `MP-ARCH-010-002`, and self-validation correction `SV-018` (`SV-C52`–`SV-C57`).
- Prior authoritative result: `Fail — Design Impact`. ARCH-REV-010 confirms CR-019 resolved through the four outward runtime projections and CR-020 resolved through package state/command/reconciliation ownership. CR-021 remains open only because SR-012's controller-only artifact relay omitted current worker ensure/restart and its later manager-callback cleanup could not preserve current session/observer cleanup acyclically.
- Current authoritative result: The cumulative package retains the accepted SR-012 outward/package design and replaces only those two incomplete lifecycle mechanics. A closed artifact-command queue and late delivery service preserve launcher ensure/restart before controller invoke. An early application MCP session scope, `AgentRunResourceManager`, and identity-checked `ActiveAgentRunRegistry` preserve exact session/file/artifact/memory cleanup without a reverse callback. The package is self-validated and ready for architecture re-review; no architecture, implementation, or executable pass is assumed.
- Why this revision entry is recorded: Both findings are source-grounded and product-reachable. A provider run can publish after its application worker exits, and inactive discovery/replacement/terminate/stop-all must remove the exact run and its resources. Removing the broad engine host and bind-once publisher cannot silently weaken those behaviors.
- Resolution:
  - Retained `ApplicationPlatformRuntime` as exactly `lifecycle`, `rest`, `realtime`, and `hostManagement`; retained package registry state/query, package command/rollback, and late ordered catalog refresh as distinct owners.
  - Added `ApplicationPublishedArtifactDeliveryQueue` for complete `{runId, applicationId, bindingId, revisionId, event}` commands, per-run FIFO, independent run lanes, one completion per accepted command, intake stop, and drain. It has no arbitrary topics, subscribers, handler registration, or durable-journal role.
  - Added `ApplicationPublishedArtifactDeliveryService`, constructed after the launcher/controller, which always performs `ApplicationEngineLauncher.ensureReady(applicationId)` before `ApplicationEngineController.invokeApplicationArtifactHandler` and settles the accepted command.
  - Preserved the current caller/failure policy: active-run event delivery remains fire-and-forget with logged failure; the no-active-run fallback remains awaited; delivery failure does not roll back an already persisted artifact snapshot/projection; shutdown drains accepted artifact commands while engine owners remain live.
  - Added early `ApplicationAgentToolMcpSessionScope` over the exact process session service/registry. It records/revokes application sessions by run/member/owner and can block/close, but it does not carry a publisher, choose tools, or depend on a run manager.
  - Added `AgentRunResourceManager.attach/release` to own run-session revocation and exact file-change, artifact-relay, and memory observer disposers. Resource ownership is removed before cleanup; all categories are attempted; errors aggregate; partial attachments roll back; repeated release is an at-most-once no-op.
  - Refined `ActiveAgentRunRegistry` to own only exact active identity with typed `register`, `getActiveRun`, `listActiveRuns`, and `removeIfCurrent({runId, expectedRun, reason})` results. Inactive discovery cleans before null; inactive replacement cleans before new attach; explicit terminate removes after accepted termination; stop-all snapshots exact objects and aggregates; stale/duplicate removal cannot delete a replacement.
  - Kept `AgentRunManager` responsible for create/restore/terminate/stop commands while removing its active/disposer maps. No registry-to-manager callback, process-global fallback, or generic cleanup event bus is permitted.
  - Refined construction to: early controller + closed queues; session scope + queue-backed relay; run resource manager + exact registry; concrete publication; later scoped issuer and run managers; orchestration/streaming; launcher; artifact delivery service and journal dispatcher; then gateway/reconciliation/lifecycle/four frozen projections. Runtime construction still starts no run.
  - Updated shutdown, ownership/forbidden-dependency, interface, file add/modify/remove, refactor sequence, risk, and validation maps. Both `BindOnce*` files and `ApplicationEngineHostService` remain clean removals; no deferred handler/container, reverse callback, service locator, compatibility alias, route/schema/data change, or migration is introduced.
  - Added UC-026/UC-027, AC-022/AC-023 traceability, MVP-EC-023/MVP-EC-024, and SV-C52–SV-C57 for worker-exit publication, queue semantics/drain, inactive discovery/replacement, partial attach, terminate/stop-all/stale identity, acyclic construction, and the full fixed baseline.
- Approved behavior or requirement IDs affected: Refined BEH-004, BEH-005, BEH-010, REQ-004, REQ-005, REQ-010, AC-021; added AC-022, AC-023, UC-026, UC-027, SV-018, and SV-C52–SV-C57. Every previously passed route, wire/package/database schema, Codex/Luna default, Studio override, provider/MCP/session behavior, publication/handoff/projection, recovery/remount/restart/shutdown outcome, and 73/73 package parity remains fixed.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) current behavior/findings/REQ-010/AC-021–AC-023/use cases/constraints/risks; [investigation-notes.md](investigation-notes.md) ARCH-REV-010/source traces/decision/routing; [design-spec.md](design-spec.md) round-10 resolution map, DS-015 exact contracts/spines/construction/lifecycle/files/tests; this record.
- Supplemental artifacts updated, added, or removed: Revised intended-behavior supplement [application-framework-architecture-simplification.md](application-framework-architecture-simplification.md); revised approved [proposal-critical-analysis.md](proposal-critical-analysis.md); revised evidence-oriented [design-self-validation.md](design-self-validation.md) through SV-018. No new supplement is required.
- Downstream and architecture-review impact: Architecture should verify AR-008/AR-009 resolution while preserving its accepted CR-019/CR-020 conclusions. It must confirm ensure-before-invoke, artifact queue semantics/drain, all exact run-removal origins, at-most-once/aggregate cleanup, the 14-step acyclic construction, and absence of broad/reverse/generic fallbacks. Implementation and delivery remain paused until architecture passes; the full source-review/API-REV-011-equivalent loop remains mandatory after implementation.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: Implementation proof must cover the real worker-exit-before-publication witness and every exact cleanup origin/partial-failure case before the complete dual-host characterization rerun. `APIE2E-REPO-005` remains separate and `Unclear`; optimized distribution, application-owned MCP provisioning, public-internet/authentication/marketplace work, and repository-wide cleanup remain out of scope.

### SR-014 — Enforce only the proven application-framework dependency boundaries

- Triggering role, report path, and round: `code_reviewer` design investigation request after the fully passed SR-013 loop (`CRR-033`, `API-REV-012`, `CRR-034`). This is an evidence/proportionality audit, not a reported runtime defect.
- Triggering finding IDs: No new downstream defect ID. Added `BEH-011`, `REQ-011`, `AC-024`, `UC-028`, `DS-016`, and self-validation `SV-019` / `SV-C58`–`SV-C62`.
- Prior authoritative result: SR-013 passed architecture in `ARCH-REV-011`, implementation in `IR-017`/`IR-018`, source review at 97 in `CRR-033`, real dual-host execution at 96.6% in `API-REV-012`, and durable-test review in `CRR-034`. The current four projections, package owners, acyclic run/session/publication and engine/delivery construction, worker restart, exact cleanup, and 73/73 package parity are fixed behavior.
- Current authoritative result: The eight-candidate audit finds no new production architecture defect. It adopts only one parser/resolver-backed architecture test with closed policies AFB-001–AFB-005 and a matching table/pointer in two existing server documents. It defers public API, shared host conformance, and new correlation behind named evidence gaps and rejects a generic lifecycle model, directory moves, and suffix churn. The package is self-validated and ready for architecture review; no implementation pass is presumed.
- Why this revision entry is recorded: Current source obeys the approved dependency directions, but TypeScript resolves representative forbidden imports and the standard suite has no durable enforcement. Existing docs explain runtime roles but omit an exact policy table linked to a checker. This is a missing enforceable invariant/documentation discoverability issue, not justification for another runtime refactor.
- Resolution:
  - Added [application-framework-hardening-evaluation.md](application-framework-hardening-evaluation.md) with source/probe evidence and a complete Adopt/Defer/Reject proof matrix for all eight candidates.
  - Defined AFB-001 for REST/WebSocket/standalone transport access through exact runtime contracts; AFB-002 for GraphQL/application presentation boundaries; AFB-003 for package/bundle ownership with one exact catalog-reconciliation exception; AFB-004 for exact forbidden application-path process run/session/publication callees with named general-process assembly exemption; and AFB-005 for maintained application SDK/local/declared-library boundaries.
  - Specified one `autobyteus-server-ts/tests/architecture/application-framework-boundaries.test.ts` owner using TypeScript parsing/resolution plus exact AFB-004 call-expression bindings. It validates current source and synthetic allowed/rejected fixtures and emits policy/importer/dependency/correction diagnostics.
  - Specified updates only to `autobyteus-server-ts/docs/modules/applications.md` and `autobyteus-server-ts/docs/ARCHITECTURE.md`; policy IDs/meanings must match the executable test, and no generated documentation system is added.
  - Preserved every production file and behavior. No route, wire/package/database schema, manifest/default/override, MCP/provider/tool, worker/run/session/artifact, UI, lifecycle, public export, generated artifact, or data migration changes.
  - Rejected service locator/DI container/event bus, `buildServer(mode)`, pass-through runtime facade, generic host harness/lifecycle framework, cosmetic move/rename, ambient correlation context, compatibility alias, singleton fallback, and production source edits merely to satisfy the checker.
- Approved behavior or requirement IDs affected: Adds BEH-011, REQ-011, AC-024, UC-028, DS-016, SV-019, and SV-C58–SV-C62 as a behavior-neutral contributor contract. BEH-001–BEH-010, REQ-001–REQ-010, AC-001–AC-023, UC-001–UC-027, and DS-001–DS-015 remain unchanged and already passed.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) current behavior/design health/scope/REQ-011/AC-024/coverage/approval; [investigation-notes.md](investigation-notes.md) baseline/probes/sources/decisions/routing; [design-spec.md](design-spec.md) current state, audit map, DS-016, ownership/dependency/file/docs/sequence/risk/guidance maps; this record.
- Supplemental artifacts updated, added, or removed: Added intended-behavior/evidence supplement [application-framework-hardening-evaluation.md](application-framework-hardening-evaluation.md); updated evidence-oriented [design-self-validation.md](design-self-validation.md) through SV-019. Retained proposal source, [proposal-critical-analysis.md](proposal-critical-analysis.md), and implemented [application-framework-architecture-simplification.md](application-framework-architecture-simplification.md) unchanged as relevant context.
- Downstream and architecture-review impact: Architecture must verify policy scope/exceptions/callee set, parser/resolver and fixture design, exact test/docs-only inventory, and preservation of the passed runtime baseline. Implementation and delivery remain paused. After architecture passes, implementation adds only the test and docs; source review and the complete API-REV-012-equivalent API/E2E/test loop remain required.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: A false-positive/negative checker is the main implementation risk; exact parsing/resolution, closed rules, positive/negative fixtures, actionable diagnostics, and the full behavior baseline mitigate it. Deferred candidates reopen only on their named evidence. `APIE2E-REPO-005` remains separate and `Unclear`; delivery owns final tracked-base refresh/integration.

### SR-015 — Close fallback-by-omission and heterogeneous-source checker gaps

- Triggering role, report path, and round: `architecture_reviewer` round 12 `ARCH-REV-012` in [design-review-report.md](design-review-report.md), indexed by [architecture-review-revision-record.md](architecture-review-revision-record.md), reviewing SR-014.
- Triggering finding IDs: `AR-010` / `MP-ARCH-012-001`, `AR-011` / `MP-ARCH-012-002`, and corrected self-validation `SV-019` / `SV-C58`–`SV-C62`.
- Prior authoritative result: `Fail — Design Impact`. ARCH-REV-012 accepts the evidence-based decision to adopt only one boundary checker and two existing-document updates, while finding that direct-callee matching misses compile-valid fallback-by-omission and that whole-file TypeScript parsing/resolution does not govern eleven Vue SFCs or the distinct server, web, application, and template project authorities.
- Current authoritative result: The revised package preserves the passed SR-013 production architecture and the SR-014 Adopt/Defer/Reject decision. DS-016 now defines exact application-side constructor/factory injection obligations, table-driven omission failures, direct SFC extraction through a declared test-only parser, deterministic per-project resolution profiles, owning-manifest rules, and aligned current-tree/fixture proof. The package is self-validated and ready for architecture re-review; no architecture or implementation pass is assumed.
- Why this revision entry is recorded: Both architecture findings are independently reachable contributor-contract failures. A contributor can omit an optional graph-scoped dependency and silently activate a reusable general-process default without calling a forbidden global in the governed file; a checker that parses a `.vue` file as TypeScript sees diagnostics and no trustworthy imports. The correction strengthens only the test contract and does not justify runtime source change.
- Resolution:
  - Retained AFB-001–AFB-005, their existing allowed/forbidden directions, and the exact named general-process assembly exemptions.
  - Extended AFB-004 from direct forbidden-callee detection to exact binding obligations for the current application construction root across four closed families: publication/resource, run, session/provider, and team/context.
  - Required object-based obligations to be explicit non-computed, non-null/non-undefined properties in an inline object literal; a spread cannot satisfy an obligation. Required positional arguments must be present and non-null/non-undefined. The checker resolves named aliases and namespace members to the exact constructor/factory export.
  - Added a current-tree occurrence assertion and table-driven synthetic negatives that remove every required property or positional input, with complete allowed fixtures for all four families. This detects omission without forbidding legitimate general-process defaults at their named assembly owners.
  - Added direct test dev dependency `@vue/compiler-sfc` `^3.5.28`. The checker extracts `<script>` and `<script setup>` and parses each by its declared language; SFC/compiler errors, unsupported script languages, or unresolved script sources fail closed. No generated `.nuxt` output or transitive-only dependency is an authority.
  - Defined deterministic importer profiles for server, Studio web, Brief backend/frontend, Socratic backend/frontend, and every discovered devkit template. Each profile owns its source root, config or explicit test-local JS profile, aliases, and package manifest.
  - Defined AFB-005 bare-library authority as the importing project's own `dependencies`, `devDependencies`, `peerDependencies`, or `optionalDependencies`, plus Node built-ins. Another project manifest cannot authorize the import. Expected governed local/alias/manifest-import specifiers fail as `UNRESOLVED_GOVERNED_IMPORT` rather than being skipped.
  - Refined the exact implementation inventory to add one architecture test; modify server `package.json`, root `pnpm-lock.yaml`, and two existing server docs; and add/modify/remove/rename no production source. No route, wire/package/database schema, runtime object, package byte, provider/tool/session behavior, UI behavior, migration, or generated package output changes.
- Approved behavior or requirement IDs affected: Refines only BEH-011, REQ-011, AC-024, UC-028, DS-016, and SV-019 / SV-C58–SV-C62. BEH-001–BEH-010, REQ-001–REQ-010, AC-001–AC-023, UC-001–UC-027, and DS-001–DS-015 remain the fixed passed behavior baseline.
- Canonical artifacts and sections updated: [requirements.md](requirements.md) contributor behavior/requirement/acceptance/constraint; [investigation-notes.md](investigation-notes.md) architecture findings, source/config probes, decision, and routing; [design-spec.md](design-spec.md) round-12 resolution map and corrected DS-016 source profiles, exact obligations, files, sequence, and validation; this record.
- Supplemental artifacts updated, added, or removed: Revised intended-behavior/evidence supplement [application-framework-hardening-evaluation.md](application-framework-hardening-evaluation.md) with H-013/H-014 and the corrected enforcement contract; revised evidence-oriented [design-self-validation.md](design-self-validation.md) through corrected SV-019. Retained [proposal-critical-analysis.md](proposal-critical-analysis.md), [application-framework-architecture-simplification.md](application-framework-architecture-simplification.md), and the proposal source unchanged as relevant context.
- Downstream and architecture-review impact: Architecture should verify exact omission-sensitive targets, truthful Vue extraction, deterministic project/manifest resolution, aligned fixtures/diagnostics, and the zero-production-behavior boundary. Implementation and delivery remain paused. After architecture passes, implementation owns the five-file test/dev-manifest/lock/docs delta, followed by source review and the full API-REV-012-equivalent executable/test-review loop.
- Next recipient or routing: `architecture_reviewer`.
- Remaining gaps or risks: The principal risk is a false-negative or false-positive architecture checker. Exact source/export binding, closed profiles, fail-closed resolution, complete omission fixtures, current-tree assertions, and actionable AFB diagnostics mitigate it. Deferred candidates reopen only on their recorded evidence gaps. `APIE2E-REPO-005` remains separate and `Unclear`; delivery retains final tracked-base refresh/integration ownership.
