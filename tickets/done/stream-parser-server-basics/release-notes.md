## What's New

- Added a **Streaming parser** card in Settings -> Server Settings -> Basics so operators can enable the XML streaming parser override without editing the raw Advanced settings key.

## Improvements

- Registered `AUTOBYTEUS_STREAM_PARSER` as a predefined, validated server setting with canonical values `xml`, `json`, `sentinel`, and `api_tool_call`.
- Refreshed parser override docs so the provider-native `api_tool_call` default and full parser value set are clear.
