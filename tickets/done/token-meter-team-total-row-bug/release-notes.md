# Release Notes

## Fixes

- Fixed the right-side Token tab `Team total` row for agent teams so partial live member updates cannot replace the ledger-backed team aggregate, and member focus changes no longer alter the aggregate total.
