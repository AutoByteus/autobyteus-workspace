# Actual API/browser evidence — API-REV-001
ORG-CATALOG-NAMES-20260916-001; not earlier tickets. Actual production Nuxt + built server/SQLite + admitted file sources. Browser controlled via CUA semantic Playwright surface, no API mutation substitutes or provider calls.

## Replay (only a new owned isolated root)
1. Read canonical coverage/report and web/server AGENTS/README. From worktree run pnpm -C autobyteus-server-ts build (generates SDK/Prisma prerequisites), prepare frontend dependencies normally.
2. Create private ignored .local/api-catalog-names/data and home; copy fixture/data into data. This fixture exactly matches initial22-file seed-manifest.json (both Orgs each4immediate members, shared and owned Agent/Team; distinct names versus roles; valid Team-local children). Do not copy user data/auth or previous test vault.
3. data/.env: APP_ENV=production, AUTOBYTEUS_SERVER_HOST=http://127.0.0.1:50981, DB_TYPE=sqlite, LOG_LEVEL=INFO, PRISMA_LOG_QUERIES=0, DISABLE_HTTP_REQUEST_LOGS=true. control.json at root starts {}. Verify50981–50984free first; never stop unknown owner.
4. Launch separately using python3 tickets/in-progress/readable-org-catalog-member-names/validation/api-live/launch.py backend|proxy|frontend. /rest/health200; Nuxt50983. launch script uses minimal env/isolated HOME and unused50984 local-model discovery.
5. Normal UI Agent Orgs opens names before any detail. Capture catalog-ready and icons-aria; initial backend exact queries six unique immediate refs, no Team child query. Shared catalogs exclude owned definitions.
6. Keep Org catalog open. Second actual browser tab Agent Teams -> Shared Delivery Team View Details -> Edit -> name Shared Delivery Team Renamed -> Save Changes. Original list remains old until Reload, then updated; parent Org revisions identical. No direct API authored mutation.
7. Supported transport edges only: control.json {"delayMs":12000} delays actual reference replies; Reload then filter Alpha->Beta before completion, verify readable role fallback then exact current owner. {"fail":true} emits503 only for these references, normal Org list still available; verify fallback/aria. Restore {} and Reload restores names. No definition response forged; edge evidence records actual backend replies and chosen fault separately.
8. Enter on View Details resolves full children. Run opens configuration (Space also verified after Vite modules warmed); select gpt-5.4-mini -> ready. DO NOT click Run Agent Org; no runtime needed.
9. Close only owned tabs/services, verify ports closed, clean generated untracked SDKdist if absent at intake. Retain synthetic logs/fixtures; never package a secret DB.

## Evidence / limits
catalog-entry-proof.json: initial query/no-mutation/history/hash proof. catalog-ready.txt/png and icons-aria.json: actual rendered names/icons/aria. ui-team-rename + catalog-before/after-reload + org-revision-before-after.json: actual rename/Reload. pending/unavailable/late-owner/read-recovery: network edges. normal-detail, run-ready-not-created, keyboard-run-alpha: navigation. final-proof.json: source9unchanged, post-rename22authored unchanged, initial-only Team.md deliberate delta; actual3EnsureProviderModelCatalog metadata operations during editor/model UI plus1explicit Team update. Zero Create/Send,3history indexesempty, native traces0. Initial list itself issued no mutation. No in-memory candidate count or exhaustive runtime certification claimed. Model discovery is not inference/startup of an Agent run.

All fault transport is temporary evidence, not a production change/durable test. Existing durable96/10andnarrow26/2independentlyPass. Wrong-owner/backend-binding/localized empty-role controls durable only; invalid authoring input not forced into real backend. Existing full-detail shared catalog freshness can show old shared name while catalog network-only refreshed; unchanged policy, not new list requirement. Initial Vite optimized module reload interrupted first navigation; settled retries passed. No Electron/provider acceptance needed for read-only scope. cleanup.json confirms owned cleanup.
