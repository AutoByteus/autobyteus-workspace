# Investigation — ORG-CATALOG-NAMES-20260916-001
New separate ticket, not reopening completed ORG-LOCAL-AGENT. User screenshots show owned Agent and Team badges rendering opaque IDs while shared Team badges show names. Both images read from conversation; no running-app reproduction claimed.
Bootstrap: fresh git fetch origin requirements/flat-agent-organization-model; isolated worktree /Users/normy/autobyteus_org/autobyteus-worktrees/readable-org-catalog-member-names; branch codex/readable-org-catalog-member-names; HEAD/base755831eb8fe185ee9a32f6a77ebabdf773350cc0; eventual same feature branch target, NOT personal. Initial git status clean. Existing base untracked SDKdist and unrelated history ticket untouched. No finalization/build/app operation authorized.
Read autobyteus-web/AGENTS.md: colocated tests, pnpm test:nuxt --run, no blanket staging. Investigation continuing.

## Confirmed source mechanism
- autobyteus-web/components/agentOrgs/AgentOrgExperience.vue49–51 catalog badges call teamById/agentById; lines241–250 helpers prefer detail references/public stores, then fall back to ID as name.
- CatalogMember/toCatalogOrg at198/255–258 retains kind/ref but drops memberName/refScope, losing an existing readable fallback and scope context in this projection.
- referencedMembers/watch323–338 explicitly uses empty members on org-list; exact references load only detail/edit context. Thus owned refs excluded from public catalogs predictably render raw IDs on ordinary list entry. This is source-confirmed display defect, not ownership labels intentionally added by design.
- agentOrgDefinitionStore.AgentOrgMember/GetAgentOrgDefinitions already carry memberName/refType/refScope. Exact queries return names with identity/scope/owner metadata. Shared loader in services/agentOrgDefinition/agentOrgDefinitionReferences.ts resolves selected graph and also traverses every child Team Agent; wholesale all-card reuse could perform unnecessary reads. Architecture must assess proportionate reuse without weakening checks or creating duplicate ownership policy. No target implementation selected before approval.
- Existing AgentOrgExperience.spec.ts baseline catalog test76+ contains shared-only mocked definitions; it does not cover owned chips missing from public arrays. Prior ticket's launch/owned-detail validation is not proof of this catalog path.

## Evidence inventory / limits
User-owned screenshots evidence/user-org-list.png and user-org-list-detail.png copied from provided paths, relate SCN001/REQ001. Factual current-state references, not normative redesign or new Product artifact. No new runtime reproduction, tests, private package inspection or production edits. Source basis fixed at bootstrap755831eb8. Files mentioned above read in isolated worktree. No claim that all list pages or detail views share this failure.

## SR-002 post-approval architecture investigation
User confirmed actual definition names, same as shared badges, explicitly BOTH Agent and Team inside Org. No intent to replace actual names with guessed ID fragments or permanently use aliases. Temporary fallback is pending/failed lookup only.
- Re-read AgentOrgExperience catalog49–54, projection198/255–258 and reloadOrgs363. Reload currently fetches only Org list; parent revision can remain unchanged when a referenced definition's name changes. A local completed-reload token must refresh label reads even when Org revision/membership is unchanged. Keep existing Run/go routes untouched.
- Existing shared reference service has one nested read function enforcing id/scope/owner and capturing unavailable IDs; full loader traverses child Team Agents for detail/launch readiness. Catalog labels need immediate members only, not every child Agent. Extract its private validated-read core in-place for two explicit operations; preserve full loader semantics rather than add a mode flag or skip its validation globally.
- Existing GetAgentOrgReferencedAgent/Team queries include name and ownership. No backend/schema/new endpoint needed. Exact network reads on list mount/explicit Reload resolve current names for both shared/owned references without seeding public arrays. Existing Apollo bound-client transport/cache remains authoritative.
- Existing windowNodeContextStore.bindingRevision identifies backend binding changes; use that existing signal for local snapshot invalidation, no new global lifecycle service. Component unmount/search removes card lifetime; ordinary watcher cleanup rejects late result. Readiness/launch owners unchanged.
- Existing en/zh-CN agentOrgs catalogs contain Agent/Team aria-label templates; type-only fallback can add two localized nouns if no existing suitable key. No internal ID in rendered/accessibility label; data-test/key IDs remain identity metadata, not user-facing copy.
- Design decision: one small member-chip component owns rendering/read lifetime per card; existing Experience remains catalog/filter/reload/navigation owner. Shared definition service remains exact-read/ownership authority. No parser/source/cache/runtime changes.
No source/test edits or new runtime reproduction. Source screenshots establish real reachability; tests and isolated browser check remain downstream work. Small/Low classification applies to bounded3production files plus at most2localization files and tests, not to screenshot inventory.
