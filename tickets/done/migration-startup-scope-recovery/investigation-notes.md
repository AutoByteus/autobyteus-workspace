# Investigation — MIGRATION-STARTUP-20260915-001
## Bootstrap / evidence provenance
Read incoming /Users/normy/autobyteus_org/autobyteus-worktrees/flat-agent-organization-model-base/.local/electron-build-20260915/migration-startup-recovery-handoff.md before actions. User-authorized existing-thread recovery request via delivery thread01a09f83-3902-7370-8984-55dba1238888, not terminal completion. Fresh git fetch and new worktree creation succeeded from origin/requirements/flat-agent-organization-model3f853c7626851cb5d89178965534401e9e4aa5e4 at /Users/normy/autobyteus_org/autobyteus-worktrees/migration-startup-scope-recovery, branch codex/migration-startup-scope-recovery. Dedicated ticket docs only; finalization target same unreleased feature base, NOT personal. Base generated SDK dist outputs left untouched. No source edits, test execution, live mutation or restart.

## INV-001 — Confirmed scan/conversion distinction and prior authority
Current AgentOrgContextFileLocatorTransition.inventory (src/app-data-migrations/migrations/agent-org-flat-team-families-v1/agent-org-context-file-locator-transition.ts) adds flat Team Agent directories, all existing Org directories, and ALL standalone Agent directories before listContextFileRecordSources. prepareAndCommit reads/transforms/digests each enumerated source during planning, rereads/transforms/hashes during commit, rereads and transforms for verification, even when unchanged. validateRoot adds per-root rereads. Current count542 counts migration dispositions, NOT total tracefiles visited or bytes read. Delivery metadata count6518/~6.58GB is overall inventory, not measured per-file I/O proof. Process CPU samples are corroboration only, not exact function attribution.
My previous yes/no explanation described conversion, not this broad locator scanning; it was incomplete. Reconcile with original archived flat-agent-organization-model design DS-043: sample166 locators (97nested,69flat),77files/~92.6MB; expressly justifies file-bounded transformation rather than all history, preserve unrelated bytes; selected saved locators whose owning Team converts require migration. Its instruction to inventory relevant roots does not independently prove ALL standalone/flat trace parsing is necessary/approved. Known typed URI transformer can rewrite locators pointing to converted roots even if held elsewhere, but actual supported outside-cohort reference reachability remains to investigate; no claim cross-run scanning is established product requirement. Must preserve real affected attachments and current retry state without blanket broad scan or silent link loss.

## INV-002 — Runtime failure count and missing-tree cohort
Read actual prior launch log and startup-probe/production-migration-result.log (incoming file list abbreviated location; corrected actual subdirectory). Read-only sqlite connection mode=ro + PRAGMA query_only selected ONLY app_data_migration_records (no secrets/other user content): family statusFAILED attempts2, started1789459995665 completed1789460130543 (~134.878s), summaryScanned542/migrated15/skipped519/failed8, same capped log path. Execution-tree predecessors:20260814_team_run_execution_tree_v1 SUCCEEDED_WITH_WARNINGS attempt1;20260824_team_run_execution_tree_v2 SUCCEEDED attempt1. No DB writes/ledger reset.
Metadata-only live directory listing independently found exactly8 current Team directories lacking team_run_execution_tree.json. Five classroom IDs match capped examples; remaining3: team_northstar-operating-company_6af4b5ba,team_software-engineering-team_359ede01,team_video-tutorial-creation-team_48955b3c. Six have only Agent-named subdirectories; Northstar/video also have old team_run_metadata.json and communication files/backups. No trace/conversation content read. This is current filesystem cohort consistent with count8, not proof all8 capped execution errors' exact hidden reasons nor corruption provenance. Do not delete or reconstruct from names. Predecessor team-run-execution-tree-v2-app-data-migration.ts:261–264 records ENOENT as SKIPPED_MISSING; family locator inventory and root loop currently classify missing tree as failure. Legacy/no-tree directories aren't proven supported conversion inputs.

## INV-003 — Backend versus shell startup
Actual production-launch.log: Electron timeout08:14:54.345Z at100s; family failure warn08:15:30.550Z; backend listening on0.0.0.0:29695 at08:15:53.084Z. Backend was NOT killed by timeout and did become listening later. Do not claim all downstream app acceptance completed.
server-runtime.ts:189+ runPending warns individual FAILED/RUNNING; then readiness indexes/other prerequisite checks. Team V2 not-clean warns and continues strict current admission. Database/secret-vault/readable-provider and thrown startup gates differ and can legitimately be fatal; don't make every failure nonfatal.
Electron baseServerManager.ts:33 maxStartupTime100000; waitForServerReady:429 removes ready/error listeners, clears health polling, sets settledStartupError and rejects. checkServerHealth:208+ only publishes ready if settledStartupGeneration!==generation; timeout settled that generation. ServerStatusManager.checkServerHealth returns early if manager.isRunning() false, so independent status polling doesn't repair same timed-out child readiness. This confirms a source-level permanent late-ready gap consistent with actual log, not merely a slow family migration result being fatal.
Live-state ps initially found71936/72543 sleeping. During investigation log acquired08:24:30 graceful-stop/SIGTERM entries; subsequent ps found neither PID. Designer sent no signals, shutdown or launch; external actor unestablished. User subsequently permits app launch, but existing evidence already proves failure; no new live retry performed to avoid reapplying partially complete migration. Any controlled rerun should use isolated faithful metadata/fixtures first; source fixes and live repair require approved requirements and design.

## INV-004 — Current risks / evidence limits
No source edits/tests/new live startup, no copying private DB or full traces. Completed14Org conversions and history update must be preserved; do not undo or reset because ledger overall failed. Existing correct flat tree skip doesn't prove its trace history was unscanned. Initial isolated RUST_LOG=warn issue is distinct from normal profile and must not be used as production cause. Exact larger performance complexity/changed-file costs require controlled instrumentation, not uncapped live trace scans. Need finish supported attachment-reference cohort evidence, startup progress/late-readiness intended behavior and safety-critical exceptions before authoritative architecture.

## Supplement Inventory
Incoming recovery handoff and startup-probe/{production-launch.log,production-migration-result.log,production-sample.txt,matrix.json} under base .local/electron-build-20260915 are Delivery-owned PRIVATE read-only diagnostic references, do not blanket stage/copy. Source docs from archived flat-agent-organization-model and completed follow-ups provide historical approval context, not authority to change behavior. New ticket owns current investigation and proposed requirements. No Product prototype/implementation/review/API result yet.


## INV-005 — Additional architecture investigation, 2026-09-15
All source reads in isolated worktree at 3f853c762; no test execution or source changes. Read server/web AGENTS.md. Read skill architecture standards, principles and template. Important new findings distinguish proposed design from confirmed behavior:

1. **There is a second full-trace scan, outside the family migration.** `autobyteus-server-ts/src/run-history/services/root-run-package-readiness-index.ts:199–244` builds structural Team/Org candidates, then repeatedly invokes `RootPackageContextFileValidation.validate` on every admitted Team and Org until no exclusions remain. `root-package-context-file-validation.ts:26–48` enumerates all member active/rotated/complete-archive traces and formal sidecars using `listContextFileRecordSources`, reads each and transforms in validation-only mode. This can reread unaffected histories even after a completed/skipped migration. It does not enumerate standalone Agent traces itself. Do not claim narrowing the family migration eliminates all startup trace scans.
2. **Removing this second scan changes availability policy, not just performance.** A missing/miscorrelated current attachment currently excludes the whole root and can transitively exclude referencing roots. Structural manifest, execution-tree and task/message consistency checks are separate (`inspectTeam`/`inspectOrg`, same file:245–303). `ContextFileReadService.getFinalFilePath` already resolves the authoritative owner and stats the exact file. `ContextFileOwnerResolver` enforces family/root/execution identity and the local-path resolver applies current locator parsing; these can remain the per-access boundary without admitting invalid structural packages. Proposed change: keep structural readiness; ordinary missing historic attachment fails access rather than hiding the whole valid run. User was asked specifically whether to include this additional behavior; approval pending, not inferred from prior migration-only approval.
3. **Current broad-scan test expectation is real but synthetic.** `tests/unit/app-data-migrations/agent-org-context-file-locator-transition.test.ts:49–99` directly writes a nested-root locator into standalone and flat-Team raw traces, then expects both rewritten. This is evidence of an existing test contract, NOT proof of actual historical user flow. It conflicts with explicit no-outside-cohort history-scan intent. Record and revise the expectation through approved scope; do not silently delete the whole test or claim every historical link can be preserved without inspecting it. Current `ContextFileFinalizationService` assigns attachment ownership to the final execution. Actual prior cross-cohort user-input reachability has not been proved/disproved. Within-candidate cross-member/task/archived references are supported and must remain covered. No automatic global reference repair is authorized.
4. **Cohort must govern all family phases.** Family `execute` currently invokes independent locator inventory, runtime enumeration, cleanup of Org roots with retired Team artifacts, then a global Org history rebuild. `migrateHistoryIndexes` validates every Org package, filters all Org index rows by discovered IDs and rewrites the Org index even for no conversions. Scope correction must include cleanup/history reconciliation, not merely remove standalone enumeration from locator code. Target-only partial conversion has existing evidence: retired Team artifacts and/or a retained Team index row for the exact Org root. Those are bounded recovery signals; arbitrary native Org roots are not a blanket repair cohort. Both-family collisions remain failures, not duplicate cleanup.
5. **Existing restart boundaries and dependency checks matter.** Locator transition uses per-file original/target digests, physical attachment-owner proof, committed-write outcome and strict reread. It records referenced candidate dependencies; current readiness also supplies final dependency exclusions. Removing global readiness must not accidentally publish partially converted candidates whose dependencies failed. Final architecture must explicitly retain candidate-local validation/commit completion and retirement ordering before current admission, including partial target retry. No generic new transaction/ledger/reset is presumed necessary.
6. **There is no migration progress protocol yet.** `AppDataMigrationRunner.runPending` awaits registered definitions and records lifecycle; `server-runtime.ts:189+` awaits all migration outcomes before current readiness and safe HTTP bootstrap. Existing child output parser only recognizes fixed platform-fatal records. `BaseServerManager` already uses a line-buffered process output forwarder with child/generation guards. A distinct bounded startup-phase signal can reuse that transport; ordinary log text is not an authoritative migration-active indicator. `ServerStatusSnapshot.message` exists, but `serverStore.updateServerStatus` only retains ERROR messages and `connectionMessage` is static while STARTING. Both propagation and rendering must be handled for truthful migration status.
7. **Existing timeouts have different purposes.** `BaseServerManager.maxStartupTime=100000` is a terminal startup deadline; health request/port release/shutdown limits are separate and must not be indiscriminately removed. `serverStore.waitForServerReady` has a local30s caller wait and no production callers found by repository search; its timeout returns false but does not set server ERROR. `isInitialStartup`30s affects presentation only. Runner15minute stale-ledger rule is not a migration execution deadline; inFlight prevents same-process duplicate execution. No claim it currently kills a long migration.

### User clarification and proposal status
User said “requirements are clear ... propose a reasonable deadline”, then corrected to **“reasonable design sorry for the typo”**. No schedule/ETA or replacement hard-time-limit request. Reaffirmed updating the unreleased existing migration directly; no new migration ID or definition converter. Core REQ001/003/004 intent confirmed. Additional startup readiness/attachment availability behavior above is a new approval question, not a reason to re-ask the already approved scan boundary.


## INV-006 — Approval resolution and architecture constraints
User directs Designer to resolve the presented boundary question reasonably, not seek another technical preference. SR005 captures delegated choice. REQ005 now included; INV005's pending wording is chronological, superseded by SR005. No additional intended-behavior decision remains pending.
Further source read: readiness `requiredOrgFiles` and `retiredOrgFiles` require all three current authorities and reject any retired Team execution/task/message authority. This existing manifest rule can withhold renamed-but-incomplete candidate targets without a new persistent progress schema. Existing cleanup removed the retired tree before sidecars and history publication followed all cleanup; target design deliberately retains the retired tree until locator/dependency validation and selected index reconciliation succeed. A target-only retry can identify these leftover artifacts without scanning unrelated histories. A prior completed target with a retained exact Team index row supports index-only reconciliation; no proof of conversion is inferred from display names or arbitrary Org directory existence.
`AgentOrgRunHistoryIndexStore` validates rows on read and atomically writes the complete index. Change the selection of rows merged, not the persisted format or store writer. Preserve all unselected rows; no global pruning. Existing index-level consistency errors may still prevent selected updates, and must leave staged target manifests unfinalized rather than pretending migration succeeded.
`BaseServerManager.startServer` currently checks isServerRunning, which remains false during startup. Retaining the pending wait requires same-owner in-flight coalescing, not using a delay warning to open another startup. Existing current-child/generation guards and terminal fatal/close behavior remain the basis; no new scheduler or process registry. Ordinary HTTP, port-release and explicit shutdown operation timeouts remain separate. Delayed startup warning remains nonterminal, so missing progress transport cannot itself create the old timeout/late-ready bug.
No executable result is inferred from these source inspections. Historical outside-cohort synthetic URL cases remain outside the explicitly approved migration scan; migrated candidate dependencies need exact validation before their last retired artifact is removed. Verification must exercise an interrupted referencing candidate as well as isolated independent candidates.


## INV-007 — Corrected change posture, SR006
User explicitly identifies the migration as unreleased first-migration development, not production migration upgrade/recovery. The real normal-profile attempt and partial modifications are diagnostic facts only, not authority to require compatibility with that feature attempt's output. Earlier INV005/006 suggestions about selecting arbitrary existing Org+Team-index overlaps as a recovery cohort are NOT adopted design authority. Direct source-to-target correctness is the design premise. Ordinary same-algorithm failure safety remains relevant, but no second migration, prior-feature artifact converter, blanket current-Org repair pass or ledger reset is approved. Current source and tests remain untouched; fresh isolated fixtures are required before considering any separately authorized normal-profile action.


## INV-008 — Local repair investigation and explicit startup-cause reconfirmation
Read-only recheck 2026-09-15: no listener on29695 or matching built-app process found at inspection. SQLite mode=ro/query_only confirms unchanged FAILED attempt2,542/15/519/8. Metadata inventory:527Team directories,14Org directories,519Team index rows,14Org index rows; no both-family IDs. All14Org roots have three current authorities, no retired Team authorities and one Org index row/no Team index row. Eight missing-tree directories have no current Team index rows. Invoked existing base compiled pure schema/state validators read-only:519Team packages and14Org packages structurally valid,8missing-tree Team dirs,0other structural failures. Not a fresh build, attachment/history/runtime validation or complete data-repair proof. No raw trace, secret key or credential content read; no DB/file repair, reset, quarantine or replay performed.
Earlier V2 migration log records SKIPPED_MISSING Count8 with same five classroom examples. Its details are also capped: this corroborates longstanding missing-tree exclusions, but does not establish the exact other three identities historically from that log. Current metadata identifies all8. Do not infer that14converted targets require rollback merely because aggregate migration status is FAILED.
Re-read actual production-launch.log and current source for the user's startup question:08:14:54.345Z Electron terminal100s timeout;08:15:30.550Z family FAILED8 warning;08:15:53.084Z external backend listener29695. Therefore observed shell startup failure was the timeout; eight items did not abort the backend, which later listened. This does not claim successful frontend readiness recovery or complete runtime acceptance. server-runtime.ts keeps per-history warning/strict-package admission separate from genuine fatal prerequisites. User explicitly affirms unusable individual histories should be excluded/reported, not prevent healthy application startup; this confirms REQ003 preserved isolation semantics, not universal suppression of migration failures.


## INV-009 — Timeout-first architecture investigation after sequencing approval
Re-read BaseServerManager lifecycle/tests, ServerStatusManager/tests, serverStore startup and health actions, ServerLoading and Electron application bootstrap. Existing Electron renderer health polls do not enforce the browser five-attempt connection limit: embedded initialization calls the IPC health path, whose `starting` result leaves startup pending. Existing local30s wait helper has no production call sites found and does not mutate status. M1 need not remove either unrelated timer.
BaseServerManager's health result is already authoritative only for matching child/generation and `{status:'ok'}`. Platform-fatal line buffering and process-close tests already exist. Normal Electron profile opens the window before async startup; E2E profile awaits server startup before opening its window. Actual delayed-loading UI validation therefore requires an isolated launch using the normal window-first lifecycle, not just the E2E profile that hides the window until ready.
ServerStatusSnapshot already carries optional message. ServerStatusManager stores messages, but serverStore currently ignores non-error messages. ServerLoading uses connectionMessage in initial loading and static copy for restarting; both must display nonterminal delay information without forcing ERROR. ServerMonitor reuses connectionMessage for STARTING. No need for new IPC channel, enum, migration table, backend progress protocol or renderer health authority.
Current BaseServerManager only checks isServerRunning before start, not an in-flight start. M1's longer pending lifetime must retain single-child startup ownership and settle/dispose observers on actual stop/error/ready. Test the ordinary start/ready/quit lifecycle and existing generation checks; no generic task scheduler or process registry is required. No tests or source changes executed during design.
