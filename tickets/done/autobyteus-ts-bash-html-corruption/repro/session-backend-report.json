{
  "commandLength": 1791,
  "expectedSha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
  "reports": [
    {
      "backend": "pty",
      "result": {
        "stdout": "mkdir -p j \ret-game && cat > jet-game/index.html <<'EOF_HTML'\r\n<!DOCTYPE html>\r\n<html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.> <!DOCTYPE html>\r\n<html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20> <html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, > <head>\r\n>   <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0>   <title>Jet Game</title>\r\n>   <style>\r\n>     body { margin: 0; background: #08111f; color: white; font-family: Arial, s \rans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fi>     canvas { display: block; width: 100vw; height: 100vh; }\r\n>   </style>\r\n> </head>\r\n> <body>\r\n>   <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lin>   <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - >     const canvas = document.getElementById('game');\r\n>     const ctx = canvas.getContext('2d');\r\n>     const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8,   t.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.fill();\r\n      ctx.fillStyle = 'white';       ctx.fil'Sc>     let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n>     function resize() { canvas.width = innerWidth; canvas.height = innerHeight \r; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8,   t.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.fill();\r\n      ctx.fillStyle = 'white';       ctx.fil'Score: ' + jet.score, 20, 30);\r\n      requestAnimationFrame(loop);\r\n    }\r\n    }\r\nrequestAnimationFrame(loop);\r\n  ctx.fil'Score: ' + jet.score, >     addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.max(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8,   t.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.fill();\r\n      ctx.fillStyle = 'white';       ctx.fil'Score: ' + jet.score, 20, 30);\r\n      requestAnimationFrame(loop);\r\n    }\r\n    }\r\nrequestAnimationFrame(loop);\r\n  ctx.fil'Score: ' + jet.score, 20, 30);\r\n.linmp.linmp.linmp.linmp.linmp.linmp>     addEventListener('keydown', e => keys.add(e.key));\r\n>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.m \rax(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n>       jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n>       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyl \re       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n>       ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.l \rineTo(jet.x - 8,   t.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ct \rx.fill();\r\n>       ctx.fillStyle = 'white';       ctx.fil'Score: ' + jet.score, 20, 30);\r\n>       requestAnimationFrame(loop);\r\n>     }\r\n>     }\r\n> requestAnimationFrame(loop);\r\n>   ctx.fil'Score: ' + jet.score, 20, 30);\r\n> .linmp.linmp.linmp.linmp.linmp.linmp.linmp.li)\r\n> print('exists', p.exists(), 'bytes', p.stat().st_size)\r\n> print(p.read_text()[:120])\r\n> PY\r\n> ",
        "stderr": "",
        "exitCode": null,
        "timedOut": true,
        "effectiveCwd": "/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/pty-workspace"
      },
      "file": {
        "exists": false,
        "error": "Error: ENOENT: no such file or directory, open '/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/pty-workspace/jet-game/index.html'"
      },
      "stdoutPreview": "mkdir -p j \ret-game && cat > jet-game/index.html <<'EOF_HTML'\r\n<!DOCTYPE html>\r\n<html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n   ",
      "stdoutTail": "nmp.linmp.linmp.linmp.linmp.linmp>     addEventListener('keydown', e => keys.add(e.key));\r\n>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet      jet.x = Math.m \rax(20, Math.min(canvas.width - 20, js.height - 20, jet.y + jet.vy));\r\n>       jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n>       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyl \re       ctx.fillStyle       c6e0ff'; ctx.beginPath();\r\n>       ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.l \rineTo(jet.x - 8,   t.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ct \rx.fill();\r\n>       ctx.fillStyle = 'white';       ctx.fil'Score: ' + jet.score, 20, 30);\r\n>       requestAnimationFrame(loop);\r\n>     }\r\n>     }\r\n> requestAnimationFrame(loop);\r\n>   ctx.fil'Score: ' + jet.score, 20, 30);\r\n> .linmp.linmp.linmp.linmp.linmp.linmp.linmp.li)\r\n> print('exists', p.exists(), 'bytes', p.stat().st_size)\r\n> print(p.read_text()[:120])\r\n> PY\r\n> "
    },
    {
      "backend": "direct",
      "result": {
        "stdout": "> > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > ~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace $ ",
        "stderr": "",
        "exitCode": null,
        "timedOut": false,
        "effectiveCwd": "/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace"
      },
      "file": {
        "exists": true,
        "bytes": 1564,
        "matchesExpected": true,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutPreview": "> > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > ~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace $ ",
      "stdoutTail": "> > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > ~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace $ "
    }
  ]
}