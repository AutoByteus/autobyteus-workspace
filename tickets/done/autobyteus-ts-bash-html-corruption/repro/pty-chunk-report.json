{
  "expectedSha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
  "reports": [
    {
      "chunkSize": 256,
      "delayMs": 2,
      "timedOut": false,
      "stdoutLen": 2080,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": "llRect(0, 0, canvas.width, canvas.height);\r\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.fill();\r\n      ctx.fillStyle = 'white'; ctx.fillText('Score: ' + jet.score, 20>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n>       jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\r\n>       jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n>       ctx.fillStyle = '#"
    },
    {
      "chunkSize": 128,
      "delayMs": 1,
      "timedOut": false,
      "stdoutLen": 1281,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": "     const ctx = canvas.getContext('2d');\r\n>     const keys = new Set();\r\n>     let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n>     function resize() { canvas.width = innerWidth; canvas.height = innerHeight \r; }\r\n>     addEventListener('resize', resize); resize();\r\n>     addEventListener('keydown', e => keys.add(e.key));\r\n>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n>       jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\r\n>       jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n>       ctx.fillStyle = '#"
    },
    {
      "chunkSize": 128,
      "delayMs": 2,
      "timedOut": false,
      "stdoutLen": 449,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": "mkdir -p \r jet-game && cat > jet-game/index.html <<'EOF_HTML'\r\n> <!DOCTYPE html>\r\n> <html lang=\"en\">\r\n> <head>\r\n>   <meta charset=\"utf-8\" />\r\n>   <title>Jet Game</title>\r\n>   <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n   >     body { margin: 0; background: #"
    },
    {
      "chunkSize": 64,
      "delayMs": 1,
      "timedOut": false,
      "stdoutLen": 2304,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": ">       ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.l \rineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ct \rx.fill();\r\n>       ctx.fillStyle = 'white'; ctx.fillText('Score: ' + jet.score, 20, 30);\r\n>       requestAnimationFrame(loop);\r\n>     }\r\n>     loop();\r\n>   </script>\r\n> </body>\r\n</html>\r\nEOF_HTML\r\npython3 - <<'PY'\r\nfrom pathlib import Path\r\np = Path('jet-game/index.html')\r\nprint('exists', p.exi> </html>\r\n> EOF_HTML\r\npython3 - <<'PY'\r\nfrom pathlib import Path\r\np = Path('jet-game/index.html')\r\nprint('exists', p.exists(), 'bytes', p.stat().st_size)\r\nprint(p.read_text()[:120])\r\nPY\r\n~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/\r/in-progress/autobyteus-ts-bash-html-corruption/repro/pty-chunk-64-1 $ "
    },
    {
      "chunkSize": 64,
      "delayMs": 2,
      "timedOut": false,
      "stdoutLen": 1281,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": "     const ctx = canvas.getContext('2d');\r\n>     const keys = new Set();\r\n>     let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n>     function resize() { canvas.width = innerWidth; canvas.height = innerHeight \r; }\r\n>     addEventListener('resize', resize); resize();\r\n>     addEventListener('keydown', e => keys.add(e.key));\r\n>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n>       jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\r\n>       jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\r\n>       ctx.fillStyle = '#"
    },
    {
      "chunkSize": 32,
      "delayMs": 1,
      "timedOut": false,
      "stdoutLen": 2665,
      "file": {
        "exists": true,
        "matchesExpected": true,
        "bytes": 1564,
        "sha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
        "excerpt": " 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.f"
      },
      "stdoutTail": "python3 - <<'PY'\r\nfrom pathlib import Path\r\np = Path('jet-game/index.html')\r\nprint('exists', p.exists(), 'bytes', p.stat().st_size)\r\nprint(p.read_text()[:120])\r\nPY\r\n~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/\r/in-progress/autobyteus-ts-bash-html-corruption/repro/pty-chunk-32-1 $ python3 - \r <<'PY'\r\n> from pathlib import Path\r\n> p = Path('jet-game/index.html')\r\n> print('exists', p.exists(), 'bytes', p.stat().st_size)\r\n> print(p.read_text()[:120])\r\n> PY\r\nexists True bytes 1564\r\n<!DOCTYPE html>\r\n<html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin\r\n~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/\r/in-progress/autobyteus-ts-bash-html-corruption/repro/pty-chunk-32-1 $ "
    }
  ]
}