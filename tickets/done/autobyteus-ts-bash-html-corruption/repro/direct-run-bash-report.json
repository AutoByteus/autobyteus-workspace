{
  "workspace": "/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace",
  "commandLength": 1791,
  "command": "mkdir -p jet-game && cat > jet-game/index.html <<'EOF_HTML'\n<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n  <meta charset=\"utf-8\" />\n  <title>Jet Game</title>\n  <style>\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\n    canvas { display: block; width: 100vw; height: 100vh; }\n  </style>\n</head>\n<body>\n  <canvas id=\"game\"></canvas>\n  <script>\n    const canvas = document.getElementById('game');\n    const ctx = canvas.getContext('2d');\n    const keys = new Set();\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\n    addEventListener('resize', resize); resize();\n    addEventListener('keydown', e => keys.add(e.key));\n    addEventListener('keyup', e => keys.delete(e.key));\n    function loop() {\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y + jet.vy));\n      jet.vx *= 0.94; jet.vy *= 0.94; jet.score += 1;\n      ctx.fillStyle = '#08111f'; ctx.fillRect(0, 0, canvas.width, canvas.height);\n      ctx.fillStyle = '#66e0ff'; ctx.beginPath();\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18, jet.y - 14); ctx.lineTo(jet.x - 8, jet.y); ctx.lineTo(jet.x - 18, jet.y + 14); ctx.closePath(); ctx.fill();\n      ctx.fillStyle = 'white'; ctx.fillText('Score: ' + jet.score, 20, 30);\n      requestAnimationFrame(loop);\n    }\n    loop();\n  </script>\n</body>\n</html>\nEOF_HTML\npython3 - <<'PY'\nfrom pathlib import Path\np = Path('jet-game/index.html')\nprint('exists', p.exists(), 'bytes', p.stat().st_size)\nprint(p.read_text()[:120])\nPY",
  "result": {
    "stdout": "mkdir - \rp jet-game && cat > jet-game/index.html <<'EOF_HTML'\r\n> <!DOCTYPE html>\r\n> <html lang=\"en\">\r\n<head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20,> <head>\r\n  <meta charset=\"utf-8\" />\r\n  <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y  >   <meta charset=\"utf-8\" />\r\n>   <title>Jet Game</title>\r\n  <style>\r\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     s>   <style>\r\n>     body { margin: 0; background: #08111f; color: white; font-family: Arial, s \rans-serif; }\r\n    canvas { display: block; width: 100vw; height: 100vh; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.f>     canvas { display: block; width: 100vw; height: 100vh; }\r\n>   </style>\r\n> </head>\r\n> <body>\r\n  <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); c>   <canvas id=\"game\"></canvas>\r\n  <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.move>   <script>\r\n    const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.moveTo(jet.t.x ->     const canvas = document.getElementById('game');\r\n    const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.moveTo(jet.t.x - 8, jet.y); ctx      ctx.moveTo(jet.et.y + 14); ctx.c>     const ctx = canvas.getContext('2d');\r\n    const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.moveTo(jet.t.x - 8, jet.y); ctx      ctx.moveTo(jet.et.y + 14); ctx.closePath(); ct      ctx.moveTo(jet.x + 26,>     const keys = new Set();\r\n    let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.moveTo(jet.t.x - 8, jet.y); ctx      ctx.moveTo(jet.et.y + 14); ctx.closePath(); ct      ctx.moveTo(jet.x + 26, jet.y);te';       ctx.moveTo>     let jet = { x: 100, y: 200, vx: 0, vy: 0, score: 0 };\r\n    function resize() { canvas.width = innerWidth; canvas.height = innerHeight; }\r\n    addEventListener('resize', resize); resize();\r\n    addEventListener('keydown', e => keys.add(e.key));\r\n    addEventListener('keyup', e => keys.delete(e.key));\r\n    function loop() {\r\n      jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n      jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n      jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n      jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n      jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n      ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.moveTo(jet.t.x - 8, jet.y); ctx      ctx.moveTo(jet.et.y + 14); ctx.closePath(); ct      ctx.moveTo(jet.x + 26, jet.y);te';       ctx.moveTo(jet.x + jet      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo>     function resize() { canvas.width = innerWidth; canvas.height = innerHeight \r; }\r\n>     addEventListener('resize', resize); resize();\r\n>     addEventListener('keydown', e => keys.add(e.key));\r\n>     addEventListener('keyup', e => keys.delete(e.key));\r\n>     function loop() {\r\n>       jet.vx += (keys.has('ArrowRight') - keys.has('ArrowLeft')) * 0.4;\r\n>       jet.vy += (keys.has('ArrowDown') - keys.has('ArrowUp')) * 0.4;\r\n>       jet.x = Math.max(20, Math.min(canvas.width - 20, jet.x + jet.vx));\r\n>       jet.y = Math.max(20, Math.min(canvas.height - 20, jet.y      jet.);\r\n>       jet.vx *= 0.94; jet.vy *= 0.94;     score += 1;\r\n>       ctx.fillStyle       ctx.fillStyle       ctx.fillStyle       ctx.fillStyl \re       ctx.fillStyle       ctx0ff'; ctx.beginPath();\r\n>       ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - 18     .y      ctx.mov \reTo(jet.t.x - 8, jet.y); ctx      ctx.moveTo(jet.et.y + 14); ctx.closePath(); ct \r      ctx.moveTo(jet.x + 26, jet.y);te';       ctx.moveTo(jet.x + jet      ctx.m \roveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - }\r\n>     loop();\r\n>   </script>\r\n> </body>\r\n> </html>\r\n> EOF_HTML\r\n~/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/\r/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace $ ",
    "stderr": "",
    "exitCode": 0,
    "timedOut": false,
    "effectiveCwd": "/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace"
  },
  "file": {
    "path": "/Users/normy/autobyteus_org/autobyteus-worktrees/autobyteus-ts-bash-html-corruption/tickets/in-progress/autobyteus-ts-bash-html-corruption/repro/direct-workspace/jet-game/index.html",
    "exists": true,
    "bytes": 1564,
    "sha256": "0a0119aaaa2a2a3c479079fc08b2a3d26601a8d87bd60702fe02bf2f6f85c880",
    "matchesExpected": false,
    "expectedSha256": "89137452ca99af727693af103de7b637a540860ee91dda164c2bbfe8ab3ebd99",
    "prefix": "<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n  <meta charset=\"utf-8\" />\n  <title>Jet Game</title>\n  <style>\n    body { margin: 0; background: #08111f; color: white; font-family: Arial, sans-serif; }\n    ca",
    "suffix": "t.et.y + 14); ctx.closePath(); ct      ctx.moveTo(jet.x + 26, jet.y);te';       ctx.moveTo(jet.x + jet      ctx.moveTo(jet.x + 26, jet.y); ctx.lineTo(jet.x - }\n    loop();\n  </script>\n</body>\n</html>\n"
  }
}