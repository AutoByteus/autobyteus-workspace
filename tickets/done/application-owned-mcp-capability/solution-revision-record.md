# Solution Revision Record

The current `requirements.md`, `investigation-notes.md`, `design-spec.md`, and approved supplement remain authoritative. This record indexes solution rounds only.

## Revision Index

| Revision ID | Triggering Role / Report / Round | Finding IDs | Classification | Result |
| --- | --- | --- | --- | --- |
| SR-001 | Solution designer initial baseline; user-approved requirements basis | N/A | `Initial Baseline` | Complete solution package ready for architecture review |
| SR-002 | User naming feedback and approval after the initial handoff was stopped | N/A | `Design Impact` | Capability port renamed to remove redundant “Runtime” terminology |
| SR-003 | Architecture reviewer `ARCH-REV-001` round 1; `design-review-report.md` | `ARCH-DI-001`, `ARCH-DI-002` | `Design Impact` | Native schema parity and the supported package catalog-transition spine corrected for re-review |
| SR-004 | Architecture reviewer `ARCH-REV-003` round 3; `design-review-report.md` | `ARCH-DI-001` | `Design Impact` | Native application adapter now preserves raw arguments until the common Ajv gateway; generic BaseTool behavior remains unchanged |
| SR-005 | Code reviewer `CRR-001` implementation source review; `code-review-report.md` | `CR-DI-001` | `Design Impact` | Complete platform/static adapter namespace is reserved from applications independently of configured-MCP precedence |
| SR-006 | API/E2E post-`API-REV-001` gap; `api-e2e-agent-ui-proof-gap.md`; user Option A clarification; delivery `DR-002` pause | N/A | `Requirement Gap` | Real model-backed Brief role calls and result use are carried through the existing read-only tool, publication/projection, and browser UI path |
| SR-007 | Code reviewer `CRR-005` failure-origin review of `API-REV-002`; `code-review-report.md` | `CR-DI-002`; reachable premise `CR-MP-001` | `Design Impact` | Maintained Codex/Luna workflow uses provider-native edit, relative publication, and complete research handoff instead of unavailable registry file tools or shell fallback |
| SR-008 | Code reviewer `CRR-007` failure-origin review of `API-REV-003`; `code-review-report.md` | Reopened `CR-DI-002`; reachable premise `CR-MP-002` | `Design Impact` | Maintained Luna prompts use built-in `apply_patch`; Codex `fileChange` and AutoByteus normalized `edit_file` remain separate execution-evidence layers |
| SR-009 | User business-prompt clarification plus delivery `DR-004` latest-base conflict report | `DR-004` Design Impact; no new product finding ID | `Design Impact` | Business prompts are operation-agnostic; application capability/routes compose with the finalized deterministic tokenless run-session/listener/stop-restore owners |
| SR-010 | User correction after API/E2E `API-REV-005` and code reviewer `CRR-012` | Reopened `CR-DI-002`; reachable premise `CR-MP-002`; old `AC-039` | `Requirement / Proof-Oracle Correction` | Remove the unrequested zero-shell rule; preserve the passing production architecture and accept any authorized runtime foundation operation, including shell |

## Revision Entries

### SR-001 — Initial application-owned agent-tool capability baseline

- Triggering role, report path, and round: Solution designer initial design round; no prior review report.
- Triggering finding IDs: `N/A`.
- Prior authoritative result: `N/A`.
- Current authoritative result: Design-ready requirements and approved intended-behavior basis have been mapped to an implementation-aware architecture in the dedicated ticket worktree.
- Why this baseline or revision entry is recorded: Establish the first reviewable solution package before architecture review.
- Resolution: Define a static manifest `agentTools` catalog and worker `agentToolHandlers`; one application-local catalog/route/gateway/call-lifecycle capability; sealed injection only into the application execution family; thin Agent Tools MCP and AutoByteus bound projections; exact binding/producer authorization; strict input/result/frame enforcement; lifecycle-aware reentry/removal/shutdown; clean manifest v5/backend definition v7 rebuild; and Brief Studio `get_brief_context` as the durable read-only proof.
- Approved behavior or requirement IDs affected: BEH-001–BEH-007; REQ-001–REQ-017; AC-001–AC-031.
- Canonical artifacts and sections updated:
  - `requirements.md` — approved Design-ready basis and resolved worker-failure posture.
  - `investigation-notes.md` — complete current-state/construction/identity/lifecycle/schema/sample evidence.
  - `design-spec.md` — complete initial architecture baseline.
  - `application-owned-mcp-intended-behavior.md` — approved intended-behavior authority.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` marked approved; no supplement removed.
- Downstream and architecture-review impact: Architecture review must decide whether the design is ready for implementation, especially the capability assembly, task-created Team identity authorization, native/MCP precedence, lifecycle-aware catalog replacement, and strict contract transition.
- Next recipient or routing: `/architecture_reviewer`.
- Remaining gaps or risks: No known requirement gap. Implementation risks are the construction cycle, catalog/reentry races, schema projection parity, safe error/result handling, and environment setup for the full runtime matrix.

### SR-002 — Rename the application tool capability port

- Triggering role, report path, and round: User feedback on the design terminology after stopping the initial architecture-review pass, followed by explicit approval to continue the naming update on 2026-08-27; no review report.
- Triggering finding IDs: `N/A`.
- Prior authoritative result: The newly proposed port and its construction assembly were named `ApplicationAgentToolRuntimeCapability` and `ApplicationAgentToolRuntimeCapabilityAssembly`.
- Current authoritative result: They are named `ApplicationAgentToolCapability` and `ApplicationAgentToolCapabilityAssembly`.
- Why this revision entry is recorded: “Runtime” described where the capability is projected, not what the boundary owns, and was redundant in the canonical type name.
- Resolution: Rename the port and assembly throughout the design while retaining the same application-execution-only scope, immutable route input, MCP/native projections, gateway delegation, and construction-cycle solution.
- Approved behavior or requirement IDs affected: None; this is naming-only and does not change BEH-001–BEH-007, REQ-001–REQ-017, or AC-001–AC-031.
- Canonical artifacts and sections updated: `design-spec.md` terminology, spines, ownership map, boundaries, interfaces, naming check, folder responsibilities, and change sequence; this revision record.
- Supplemental artifacts updated, added, or removed: None.
- Downstream and architecture-review impact: Future review and implementation must use the shorter canonical name and must not reintroduce the old `RuntimeCapability` types.
- Next recipient or routing: `/architecture_reviewer`; the user explicitly requested that the updated package be sent for additional review on 2026-08-27.
- Remaining gaps or risks: No new gap; existing implementation risks remain unchanged.

### SR-003 — Correct native schema parity and the real package catalog transition

- Triggering role, report path, and round: `/architecture_reviewer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/design-review-report.md`, `ARCH-REV-001`, round 1. Review history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/architecture-review-revision-record.md`.
- Triggering finding IDs: `ARCH-DI-001`, `ARCH-DI-002`.
- Prior authoritative result: `Fail — Design Impact`. The initial subset accepted nullable/length/closed-object semantics not faithfully represented by current native `ParameterSchema`/`BaseTool`, and DS-006 skipped the supported Settings/GraphQL/package-command path that mutates live bundle state.
- Current authoritative result: The application schema contract is narrowed to an exact current native mapper-round-trippable subset with a checked native projector, and every live package/application catalog change crosses one staged `ApplicationCatalogTransitionService` before mutation/commit.
- Why this revision entry is recorded: Both reachable issues protected already-approved runtime invariance and lifecycle behavior and required actionable design correction without changing product scope.
- Resolution:
  - `ARCH-DI-001`: reject defaults, nullable, length, `additionalProperties`, composition/reference, tuple/boolean-schema, and unknown keywords; retain only non-null object/array/primitive types, required fields, string enum-or-pattern, and numeric bounds. Add `ApplicationAgentToolNativeSchemaProjector` over the unchanged current mapper/model and require canonical `ParameterSchema.toJsonSchema()` round-trip equality before native materialization.
  - `ARCH-DI-002`: replace `ApplicationCatalogRefreshCoordinator` and the catalog-mutation portion of `ApplicationReentryService.reloadAndReenter` with `ApplicationCatalogTransitionService`; revise reentry into participant-only prepare/recover/quarantine operations; enter the transition from `ApplicationPackageCommandService` before registry/root/source mutation and from REST exact-app reentry; derive old package participants from the live snapshot; quiesce/drain/stop them; stage only the target package/application slice; synchronously commit prepared bundle/tool state; reconcile/recover or close/quarantine; and restore then re-stage actual rollback state. Unrelated application lanes and catalog slices remain untouched.
- Approved behavior or requirement IDs affected: BEH-003–BEH-005; REQ-003, REQ-005, REQ-010, REQ-013–REQ-015; AC-005, AC-010, AC-019, AC-021–AC-024. Their approved outcomes are unchanged.
- Canonical artifacts and sections updated:
  - `investigation-notes.md` — corrected native schema evidence; recorded the real Settings/GraphQL/package refresh path, affected-ID evidence, architecture findings, and revised decisions/risks.
  - `design-spec.md` — current-state read, behavior map, DS-002/DS-006/DS-011, ownership and dependency boundaries, interfaces, file/folder mapping, exact schema subset/projector, catalog operation matrix, removal plan, sequence, risks, and implementation guidance.
  - `solution-revision-record.md` — this `SR-003` entry.
- Supplemental artifacts updated, added, or removed: None; the approved intended-behavior supplement remains consistent and unchanged.
- Downstream and architecture-review impact: Re-review must verify exact native schema round-trip semantics and that package import/reload/removal plus REST reentry have no direct live bundle/tool mutation bypass. Implementation must remove the refresh coordinator and old reentry bundle-mutation entrypoint rather than retain wrappers; revised reentry remains only as an internal participant lifecycle owner.
- Next recipient or routing: `/architecture_reviewer` with the cumulative package, `design-review-report.md`, and `architecture-review-revision-record.md`.
- Remaining gaps or risks: No known requirement gap. Residual implementation risks are rollback restoration/quarantine, construction assembly sealing, Team descendant authorization, result/frame parity, and full environment-backed runtime/lifecycle coverage.

### SR-004 — Preserve raw native application-tool arguments at the adapter boundary

- Triggering role, report path, and round: `/architecture_reviewer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/design-review-report.md`, `ARCH-REV-003`, round 3. Review history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/architecture-review-revision-record.md`.
- Triggering finding IDs: narrowed `ARCH-DI-001`. `ARCH-DI-002` was accepted as resolved in `ARCH-REV-003` and is not reopened.
- Prior authoritative result: `Fail — Design Impact`. The mapper-round-trippable v5 schema subset was coherent, but the design still allowed inherited `BaseTool.prepareExecution` to coerce native integer/number/boolean strings, empty-string arrays, and nested values before the common gateway, while Claude/Codex MCP passed raw JSON.
- Current authoritative result: The native bound application adapter explicitly preserves raw arguments and the common strict Ajv gateway receives the same JSON meaning from AutoByteus, Claude, and Codex. The checked `ParameterSchema` remains the native advertised definition. Generic `BaseTool`, fundamental tools, configured tools, automatic exposure, and the accepted catalog-transition design remain unchanged.
- Why this revision entry is recorded: The remaining reachable mismatch violated the already-approved runtime-invariant invalid-input contract even though schema advertisement had become portable. The correction belongs at the one native adapter that introduced the transformation, not in the fundamental tool framework.
- Resolution:
  - Add `DS-012`, the native raw-preparation bounded spine, and revise DS-002/DS-003 so both projections enter `ApplicationAgentToolCapability` with the original JSON object.
  - Make `ApplicationAgentTool.prepareExecution` the explicit native seam: bind ordinary agent identity, check pre-start cancellation, resolve and locally normalize ordinary result mode, and return the same argument object. It must not call `super.prepareExecution`, coerce, default, clone, normalize, or validate with `ParameterSchema`; inherited `BaseTool.execute` retains its post-preparation abort and `_execute` behavior.
  - Keep `ApplicationAgentToolNativeSchemaProjector` and `getArgumentSchema()` as the exact native advertisement path; make the common gateway's strict Ajv validator the sole application-schema invocation authority.
  - Add explicit integer/number/boolean string, empty-string array, nested coercible value, correctly typed value, object-identity, agent-ID, cancellation, result-mode, worker-non-invocation, and native/MCP parity cases.
  - Forbid a global `BaseTool` change or application path through generic preparation; map the seam through ownership, interfaces, dependencies, file responsibilities, sequence, risks, tradeoffs, and implementation guidance.
- Approved behavior or requirement IDs affected: BEH-003, BEH-004; REQ-003, REQ-005, REQ-010; AC-010, AC-019. Their approved outcomes and the approved requirements/supplement are unchanged.
- Canonical artifacts and sections updated:
  - `investigation-notes.md` — `ARCH-REV-003` evidence, exact `BaseTool`/ToolPhase dispatch findings, relevant file responsibility, raw-argument decision, and reviewer notes.
  - `design-spec.md` — current-state read, intended change, behavior map, health assessment, terminology, DS-002/DS-003/DS-012, ownership and boundaries, dependencies/interfaces, file/folder mapping, concrete preparation/parity example, rejection log, sequence, tradeoffs, risks, and guidance/coverage.
  - `solution-revision-record.md` — this `SR-004` entry.
- Supplemental artifacts updated, added, or removed: None; the approved intended-behavior supplement remains consistent and unchanged.
- Downstream and architecture-review impact: Re-review must verify that native schema advertisement stays exact while native invocation bypasses only application-tool coercion/validation, preserves ordinary preparation/cancellation/result-mode behavior, and reaches the same Ajv boundary with raw JSON. Implementation must not change `autobyteus-ts/src/tools/base-tool.ts` or any fundamental/configured tool path.
- Next recipient or routing: `/architecture_reviewer` with the full cumulative package, `design-review-report.md`, and `architecture-review-revision-record.md`.
- Remaining gaps or risks: No known requirement gap. Residual implementation risks are accidentally re-entering generic preparation twice through `super`, incorrect adapter-local result-mode normalization, rollback restoration/quarantine, construction assembly sealing, Team descendant authorization, result/frame parity, and full environment-backed runtime/lifecycle coverage.

### SR-005 — Separate application static-name reservation from configured-MCP collision policy

- Triggering role, report path, and round: `/code_reviewer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-report.md`, `CRR-001`, implementation source review round 1. Review history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-revision-record.md`. Reviewed implementation basis: `IR-001` in `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/implementation-revision-record.md`.
- Triggering finding IDs: `CR-DI-001`.
- Prior authoritative result: `Fail — Design Impact`. The architecture-approved design and `IR-001` treated only static adapters with `configuredMcpCollisionPolicy: "protect_static_adapter"` as forbidden application names. Browser static adapters such as `open_tab` use `prefer_configured_mcp`, so readiness omitted them and MCP session composition allowed an application route to shadow them. The code reviewer confirmed the reachable contradiction with a focused temporary Vitest probe and removed the probe after recording evidence.
- Current authoritative result: Every registered `AgentToolMcpCatalog` static-adapter name is reserved from application declarations regardless of adapter availability or configured-MCP collision policy. The configured-MCP protected/prefer rule remains unchanged and applies only when configured MCP collides with a static adapter. The corrected design is ready for architecture re-review before source correction.
- Why this revision entry is recorded: Approved `REQ-009`/`AC-013` already distinguish platform/static ownership from host-configured MCP precedence. The prior design reused a narrower policy set with a different meaning, allowing application ownership to vary with an unrelated configured-MCP decision.
- Resolution:
  - Stretch DS-001 from static adapter registration through `AgentToolMcpCatalog` and a frozen `AgentToolsMcpHost.staticAdapterToolNames` snapshot into application readiness; compare every application declaration with the complete set, including inactive and `prefer_configured_mcp` adapters.
  - Make `AgentToolMcpCatalog.resolveRuntimeSessionToolExposure` hold separate registered, active, and configured-protected static views. An application route plus any registered static adapter fails defensively. Only a non-static application name may precede configured MCP. The existing no-application configured-MCP protected/prefer branch remains unchanged.
  - Rename/remove the public/application-facing `protectedStaticToolNames`, `listProtectedStaticToolNames()`, and ambiguous complete `listSupportedToolNames()` path in favor of exact `staticAdapterToolNames` / `listStaticAdapterToolNames()` naming; carry only the immutable name set through Studio/standalone composition to readiness. Do not expose adapter objects, availability, or configured policy outside MCP ownership and do not retain compatibility aliases.
  - Keep native composition dependent on successful readiness rather than MCP internals. Add explicit `open_tab`, protected-static, inactive-static, non-static application-over-configured, and configured-browser-precedence non-regression cases.
- Approved behavior or requirement IDs affected: BEH-002; REQ-004, REQ-009; AC-013. Their approved outcomes and the requirements/intended-behavior supplement are unchanged.
- Canonical artifacts and sections updated:
  - `investigation-notes.md` — CRR-001/probe evidence, collision-owner/source trace, corrected policy separation, file implications, status, and architecture-review notes.
  - `design-spec.md` — current-state read, intended change, BEH-002, health evidence/response, terminology, DS-001/DS-002, ownership/boundaries, dependencies/interfaces/naming, subsystem/file/folder mappings, collision matrix, rejection log, sequence, tradeoffs, risks, guidance, and coverage.
  - `solution-revision-record.md` — this `SR-005` entry.
- Supplemental artifacts updated, added, or removed: None; the approved `application-owned-mcp-intended-behavior.md` already states the complete platform/static collision rule and remains unchanged.
- Downstream and architecture-review impact: Architecture re-review must verify the full static-name production path and that the configured-MCP policy is neither widened nor reused. After a pass, `/implementation_engineer` must correct `IR-001`, append its own implementation revision, rerun focused build/check evidence, and return the cumulative package through `/code_reviewer`. API/E2E must remain blocked until source review passes.
- Next recipient or routing: `/architecture_reviewer` with requirements, investigation, design, approved supplement, all solution/design review history, `IR-001`, and `CRR-001` artifacts.
- Remaining gaps or risks: No requirement gap. Implementation-sensitive risks are accidentally treating every static adapter as configured-protected (breaking browser configured precedence), filtering the reserved application set by availability/request, retaining the misleading protected-subset API as an alias, or coupling native/readiness code directly to MCP adapter internals. Prior assembly, Team identity, result/frame, transition rollback, and full runtime coverage risks remain.

### SR-006 — Prove the maintained Brief Agent call through the existing publication/UI workflow

- Triggering role, report path, and round: `/api_e2e_engineer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-agent-ui-proof-gap.md`, post-`API-REV-001` requirement-gap reroute; confirmed by `/code_reviewer` and `/delivery_engineer` at delivery pause `DR-002`. The user's 2026-08-27 clarification authorizes changes to the Brief Studio demonstration application and selects the read-only Option A direction.
- Triggering finding IDs: `N/A` (user-approved Requirement Gap, not a defect in the already-passed direct-MCP scope).
- Prior authoritative result: `API-REV-001` and `CRR-003` passed the approved lower-level 33-file/234-test scope at 97.2% confidence, proving authenticated MCP through exact Team ownership, common gateway, real worker, actual handler, and SQLite. Delivery subsequently paused because that proof did not run a model-backed Brief Agent or open the UI.
- Current authoritative result: The amended requirements and design require the actual configured researcher and writer Agents to call `get_brief_context` exactly once as their role-local first tool action, reuse its exact marker in ordinary published artifacts, and produce the existing final publication/projection/UI outcome. The tool remains read-only; no platform invocation boundary, mutation tool, storage schema, GraphQL operation, UI workflow, or generic payload log is added. The package is ready for architecture re-review.
- Why this revision entry is recorded: Tool selection and a shared Team instruction do not prove that either model called or used the application tool. The maintained demonstration needs an observable model-local invariant and a causal identity join from real Agent call/result through the existing business write path to the browser.
- Resolution:
  - Add BEH-008/UC-008, REQ-018–REQ-020, and AC-032–AC-036. The user explicitly approves the maintained Brief application change and the read-only Option A boundary.
  - Make each researcher/writer `agent.md` the authoritative model-local sequence: one successful first call, fail closed on error/missing marker or cross-role brief mismatch, copy the canonical compact-JSON `Brief context:` line with fixed keys/order and JSON escaping, then continue the existing role-specific write/publish/handoff work. Keep `team.md` and `BriefRunLaunchService` aligned only as reinforcement; configs continue to own availability.
  - Keep `getBriefContext` binding-derived and read-only, but make its text result the canonical exact marker matching its structured fields. The handler never accepts routing identity from model arguments and never calls Brief write/projection/UI owners.
  - Add DS-013 from the supported browser launch through actual configured Team roles, read-only context, normal artifacts, reconciliation/notification, and the existing browser outcome. Add DS-014 joining paired access-controlled Agent run events to Team binding, artifact revision/projection, and DOM evidence by existing `agentRunId`, `applicationId`, `bindingId`, `memberAddress`, `toolCallId`, and `briefId` relationships.
  - Require the same selected brief to render `in_review`, the launch binding in Workflow diagnostics, the research artifact, exactly one `brief-studio/final-brief.md`, and the writer's exact marker. Prove a tool read alone changes no business/UI row; only `publish_artifacts -> BriefArtifactReconciliationService` causes the state change.
  - Preserve the direct MCP test as valid lower-level coverage. It cannot satisfy the new real-Agent/browser criteria, and a fake provider cannot substitute; unavailable real provider access is reported as blocked evidence.
- Approved behavior or requirement IDs affected: adds BEH-008, UC-008, REQ-018–REQ-020, and AC-032–AC-036; BEH-001–BEH-007, REQ-001–REQ-017, and AC-001–AC-031 remain unchanged.
- Canonical artifacts and sections updated:
  - `requirements.md` — amended approval, BEH-008/UC-008, in/out-of-scope guardrails, REQ-018–REQ-020, AC-032–AC-036, exact UI/correlation evidence, risks, and coverage intent.
  - `application-owned-mcp-intended-behavior.md` — approved Option A ownership, maintained role behavior, observability, UI outcome, and causal guardrails.
  - `investigation-notes.md` — API-REV-001 gap evidence, prompt/config/launch findings, handler/trace/binding/projection/UI production paths, design-health classification, and review guidance.
  - `design-spec.md` — BEH-008; DS-013/DS-014; role/handler/trace/projection/UI ownership and boundaries; exact marker and browser proof shape; dependencies, rejection log, file mapping, sequence, tradeoffs, risks, and implementation/test guidance.
  - `solution-revision-record.md` — this `SR-006` entry.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` amended as approved intended behavior. `api-e2e-agent-ui-proof-gap.md` remains the downstream triggering evidence artifact and is included in the cumulative re-review package.
- Downstream and architecture-review impact: Architecture review must verify that the role-local prompt boundary is explicit, the handler stays read-only, the existing publication/projection owner alone creates UI state, and evidence joins existing access-controlled authorities without a new payload log or proof aggregate. After a pass, `/implementation_engineer` should update only the maintained handler/prompt/launch text and proportionate coverage, append its implementation revision, and return through source review; API/E2E then executes the real provider/browser journey and reroutes any durable coverage edits through code review before delivery resumes.
- Next recipient or routing: `/architecture_reviewer` with the full cumulative package and the downstream gap/delivery-pause artifacts.
- Remaining gaps or risks: No known requirement gap. The expanded live proof depends on configured provider availability and model compliance; the design mitigates prompt ambiguity and makes compliance observable but must report provider unavailability truthfully. Evidence collection must wait for trace/projection convergence and join exact IDs rather than timestamps, titles, or log ordering.

### SR-007 — Correct the maintained Codex file-operation and cross-member data spine

- Triggering role, report path, and round: `/code_reviewer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-report.md`, `CRR-005` API/E2E failure-origin review of `/api_e2e_engineer` `API-REV-002`. Review history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-revision-record.md`; execution report: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-execution-coverage-report.md`.
- Triggering finding IDs: `CR-DI-002`; material production premise `CR-MP-001` (`Reachable`).
- Prior authoritative result: `API-REV-002` failed the renewed real-Agent/browser scope at 87.1% validation confidence. The actual shipped Codex/Luna researcher and writer each called `get_brief_context` first through the exact application/binding/member/worker path, but configured ordinary registry `read_file`/`write_file` were absent from Codex. Researcher used `run_bash` to publish a blocker, writer produced no final and later repeated context, and the supported UI rendered the same brief blocked. CRR-005 classified the missing provider-facing file-operation edge as Design Impact rather than fixture/environment or bounded implementation deviation.
- Current authoritative result: The approved read-only Option A outcome and maintained Codex/Luna runtime remain unchanged. The production spine now uses the existing Codex provider-native file-change capability normalized as `edit_file`, the existing workspace-relative `publish_artifacts` contract, and the existing Team message boundary carrying the complete bounded research body. Unavailable ordinary registry file names and absolute-path-return assumptions are removed from the maintained config/prompt contract; shell fallback is explicitly forbidden.
- Why this revision entry is recorded: SR-006 correctly established the application-call and UI causality contract but stopped its spine at configured file-name selection. API-REV-002 proved that selection is not Codex exposure. The correction must bind the maintained workflow to owners the shipped provider actually exposes without expanding generic provider/security behavior or evading the requested real runtime.
- Resolution:
  - Keep both roles and Team on `codex_app_server` / `gpt-5.6-luna`. Remove `read_file`/`write_file` from role `toolNames`; retain the actual routed application/collaboration/publication names. Do not list provider-native `edit_file` as an MCP/registry tool and do not modify Codex bootstrap, default adapter, configured-MCP, or dynamic-tool composition.
  - Researcher calls `get_brief_context` exactly once first, validates/copies its marker, uses provider-native `edit_file` without `run_bash` to create `brief-studio/research.md`, publishes that canonical relative path, and hands the writer the exact marker/path plus complete 200–500-word body.
  - Writer calls `get_brief_context` exactly once first after handoff, validates the matching brief, consumes the complete handoff without `read_file`, copies at least one complete non-marker research `Key findings` bullet verbatim into final `Key evidence`, uses provider-native `edit_file` without `run_bash` to create `brief-studio/final-brief.md`, and publishes that canonical relative path.
  - Reuse the publication service's exact member-workspace resolution and the existing application artifact relay/reconciliation/UI path. `get_brief_context` remains read-only; final publication remains the only cause of `in_review`.
  - If context, native edit, publication, or required handoff fails, report a truthful blocker and stop. No shell-created/fabricated normal artifact, runtime/model switch, direct-MCP/mock substitution, write-capable application tool, or new UI/API path is allowed.
  - Extend DS-013/DS-014 and evidence requirements to join application call/result, normalized native edit, Team handoff, member workspace path resolution, publication, binding/brief projection, and browser outcome, and to assert zero `run_bash`/`read_file`/`write_file` invocations.
- Approved behavior or requirement IDs affected: BEH-008 and UC-008 are technically corrected; REQ-018/REQ-020 and AC-032/AC-033 are refined; REQ-021 and AC-037–AC-039 are added. BEH-001–BEH-007, REQ-001–REQ-017/REQ-019, and AC-001–AC-031/AC-034–AC-036 retain their approved outcomes.
- Canonical artifacts and sections updated:
  - `requirements.md` — corrected BEH-008 current/desired/preserved path, investigation/recommendation/scope guardrails, REQ-018–REQ-021, AC-032–AC-039, coverage mapping, risks, and approval applicability.
  - `application-owned-mcp-intended-behavior.md` — maintained Codex owner/file/handoff/relative-publication contract, no-shell boundary, proof evidence, and technical-correction record.
  - `investigation-notes.md` — API-REV-002/CRR-005 source/runtime evidence, Codex composition and native edit proof, relative publication and Team handoff evidence, design-health classification, file responsibilities, decisions, risks, and reviewer guidance.
  - `design-spec.md` — current-state read, BEH-008, design health, terminology, DS-013/DS-014, owners/boundaries/dependencies/interfaces, removals/reuse/file mapping, concrete sequence/evidence, rejection log, change sequence, tradeoffs, risks, and implementation/API-E2E guidance.
  - `solution-revision-record.md` — this `SR-007` entry.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` remains the approved intended-behavior supplement and is aligned. `api-e2e-agent-ui-proof-gap.md` remains relevant triggering context; `api-e2e-evidence/api-rev-002/` remains retained production failure evidence. No supplement is removed.
- Downstream and architecture-review impact: `/architecture_reviewer` must verify the corrected spine reaches the actual Codex provider boundary, keeps provider-native edit distinct from configured/MCP routes, makes cross-member research data explicit, reuses relative publication safely per member workspace, and forbids shell/runtime/provider expansion while preserving read-only application/UI causality. After a pass, `/implementation_engineer` should make focused maintained config/prompt/team/launch/test corrections, append an implementation revision, and return through source review. API/E2E then reruns the same supported real browser journey; durable coverage changes return through proportional code review before delivery resumes.
- Next recipient or routing: `/architecture_reviewer` with the full cumulative package, CRR-005/API-REV-002 reports/revision records, the gap/delivery-pause artifacts, and retained provider/tool/workspace/browser evidence.
- Remaining gaps or risks: No requirement gap; the user already authorized free improvement of the demonstration under read-only Option A. Residual risks are model choice of shell despite instructions, native edit naming/event normalization, complete-handoff size/content drift, member-workspace/path correlation, and provider nondeterminism. These are bounded by explicit prompts, fail-closed behavior, exact source/integration coverage, retained event/message/path evidence, and a real browser rerun.

### SR-008 — Separate the Luna patch instruction from Codex and AutoByteus event names

- Triggering role, report path, and round: `/code_reviewer`, `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-report.md`, `CRR-007` focused failure-origin review of `/api_e2e_engineer` `API-REV-003`. Review history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-revision-record.md`; execution report: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-execution-coverage-report.md`; failure analysis: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-evidence/api-rev-003/runtime-native-edit-failure-analysis.md`.
- Triggering finding IDs: reopened `CR-DI-002`; material production premise `CR-MP-002` (`Reachable`).
- Prior authoritative result: `API-REV-003` failed the real configured Agent/browser scope at 88.6% validation confidence. Two independent supported UI launches used the shipped `codex_app_server` / `gpt-5.6-luna` roles. All four members called `get_brief_context` exactly once first at their role point, but both researchers interpreted the maintained “provider-native edit_file” instruction as unavailable and failed closed. No native file changes or publications occurred; writers received blockers; the same brief remained `not_started` with zero artifacts/revisions. Authentication, routing, worker, binding, Team messaging, database, read-only behavior, and browser surfaces were healthy. CRR-007 classified this repeatable result as Design Impact rather than fixture, environment, or bounded implementation error.
- Current authoritative result: The approved read-only Option A outcome, Codex/Luna configs, routed capability selection, complete handoff, relative publication, fail-closed behavior, and existing projection/UI causality remain unchanged. The production contract now distinguishes three interfaces: maintained model-facing text says Luna built-in `apply_patch`; Codex app-server execution emits `item/fileChange` / `file_change`; existing AutoByteus parser/converter records that activity as normalized `edit_file` lifecycle evidence. Neither `apply_patch` nor `edit_file` is added to `toolNames`, and normalized `edit_file` is never presented to Luna as the callable operation.
- Why this revision entry is recorded: SR-007 removed the ordinary registry-file dependency but treated the downstream normalized evidence label as the model-facing provider operation. An exact `codex-cli 0.150.1` app-server / `gpt-5.6-luna` diagnostic with `workspace-write`, approval `never`, no dynamic tools, and shell forbidden created the requested file only when the instruction named built-in `apply_patch`; the provider emitted `item/fileChange`, and current AutoByteus code normalized it to `edit_file`. Event normalization cannot establish or substitute for the model-facing vocabulary that causes the event.
- Resolution:
  - Refine BEH-008, REQ-020/REQ-021, and AC-032/AC-033/AC-037–AC-039 so the ordered maintained role instruction explicitly says built-in `apply_patch`, prohibits `run_bash`, and keeps `apply_patch`/`edit_file` out of role `toolNames`.
  - Retain Codex `item/fileChange` / `file_change` and the corresponding successful AutoByteus normalized `edit_file` lifecycle as distinct execution evidence under the same member `agentRunId`. Prompt/source checks prove the instruction text; provider/run evidence proves the effect. Neither layer substitutes for the other.
  - Update the intended-behavior supplement and DS-013/DS-014 through the complete spine: first application call -> built-in patch instruction -> provider file-change event -> platform normalization -> member-relative publication -> complete Team handoff -> final patch/publication -> existing reconciliation/notification/GraphQL/browser outcome.
  - Preserve current Codex/Luna configs, zero shell/ordinary-file calls, canonical relative publication, complete bounded research handoff, verbatim key-finding witness, read-only application tool, and failure behavior. A provider-reported failed patch blocks normal publication; a purported success without native/normalized evidence fails verification. No fabricated artifact is permitted.
  - Leave `CodexThreadBootstrapper`, provider composition, static/configured/application tool routing, event parser/converter semantics, application gateway/worker, publication/reconciliation, database, GraphQL, and UI contracts unchanged. Do not introduce a generic AutoByteus `apply_patch` API, alias, adapter, dynamic registration, mutation tool, runtime/model switch, or payload log.
  - If the optional live Codex integration is used downstream, correct its stale model-facing prompt to `apply_patch` and supply the current required `agentRunId` factory input; pin/record the exact model. Its prior skip/default model cannot satisfy the Luna proof. The same supported browser `Generate draft` journey remains mandatory.
- Approved behavior or requirement IDs affected: BEH-008, REQ-020, REQ-021, AC-032, AC-033, AC-035, AC-037–AC-039 are technically corrected; REQ-018, REQ-019, AC-034, and AC-036 retain their approved outcomes. BEH-001–BEH-007, REQ-001–REQ-017, and AC-001–AC-031 remain unaffected.
- Canonical artifacts and sections updated:
  - `requirements.md` — current/desired/preserved BEH-008, API-REV-003/CRR-007 finding, recommendation and guardrail, REQ-020/REQ-021, AC-032/AC-033/AC-037–AC-039, resolved direction, and approval applicability.
  - `application-owned-mcp-intended-behavior.md` — separate model instruction, provider protocol, and platform observability owners; maintained proof sequence/evidence; UI causal spine; approval record.
  - `investigation-notes.md` — API-REV-003 evidence, CRR-007 classification/diagnostic, source/coverage limitations, three-name boundary, file responsibilities, decisions, risks, and reviewer guidance.
  - `design-spec.md` — current-state read, intended change, BEH-008, health/terminology, DS-013/DS-014, ownership/boundaries/dependencies/interfaces, removal/reuse/file mapping, concrete sequence/proof, rejection log, change sequence, tradeoffs, risks, and implementation/API-E2E guidance.
  - `solution-revision-record.md` — this `SR-008` entry.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` remains the approved intended-behavior supplement and is aligned. `api-e2e-agent-ui-proof-gap.md` remains relevant user-scope context. `api-e2e-evidence/api-rev-003/` remains retained production-failure evidence. No supplement is removed.
- Downstream and architecture-review impact: `/architecture_reviewer` must verify the three interfaces are explicit and dependency direction is correct: role prompts depend on the provider's current built-in instruction; Codex owns protocol `fileChange`; AutoByteus owns normalized `edit_file` evidence; none becomes a configured/application/MCP route. After a pass, `/implementation_engineer` should make focused prompt/team/launch and proportionate coverage corrections, append an implementation revision, and return through source review. API/E2E must rerun the same real configured provider/Team/browser journey. Durable coverage changes return through proportional code review before delivery resumes.
- Next recipient or routing: `/architecture_reviewer` with the full cumulative package, CRR-007/API-REV-003 reports/revision records, gap/delivery-pause artifacts, and retained provider/member/database/browser evidence.
- Remaining gaps or risks: No requirement gap; the user already authorized changes to the maintained demonstration under read-only Option A. Residual risks are future provider vocabulary drift, model nondeterminism, stale optional live integration setup, protocol/normalized event correlation, complete-handoff drift, and workspace/path identity. They are bounded by fixed Codex/Luna config, exact prompt/source checks, exact-model provider diagnostics, retained native/normalized event evidence, fail-closed behavior, and the mandatory supported-browser rerun.

### SR-009 — Reconcile business-focused prompts and latest-base run-session ownership

- Triggering role, report path, and round: user clarification on 2026-08-28 after API-REV-004, followed by `/delivery_engineer` `DR-004` in `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/delivery-latest-base-conflict-report.md`. Related delivery history: `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/delivery-revision-record.md` and `delivery-integration-evidence.log`.
- Triggering finding IDs: `DR-004` Design Impact; no new product finding ID. DR-004 was triggered by the user-requested refresh to latest `origin/personal` `ebef77eb32bbeaefd4fccdb6998240264c82a3c1` and its finalized `agent-tools-mcp-session-resume` ticket.
- Prior authoritative result: SR-008/ARCH-REV-008/IR-005/CRR-009/API-REV-004 proved the application call, identity, worker, publication, handoff, and browser outcome, but SR-008 coupled maintained role prompts to one provider operation name. The protected delivery checkpoint `aaf7e076ed66c5daaf142f896230ad63085330c7` also implemented application routes/capabilities over the now-removed random bearer/main-listener session seam. A latest-base no-ff merge produced 12 semantic conflicts across the host/session/provider/kernel/docs/tests and was correctly aborted before Electron build.
- Current authoritative result: Maintained `agent.md`, `team.md`, and launch instructions own business calls, artifact content/path, publication, handoff, and truthful failure only; they name no foundation/provider operation. Runtime provisioning/model reasoning selects the operation, while provider-native events remain optional verifier evidence. The application capability now extends the finalized latest-base deterministic tokenless run-session design: one host-owned dedicated listener, scoped `activateForRun`/`deactivateForRun`, active-only state, fresh restore materialization, current application routes/fingerprints, and exact managed-run cleanup. Package/application drain remains orthogonal to whole-run session deactivation.
- Why this revision entry is recorded: Both corrections protect approved boundaries. The user's clarification keeps application role logic at the business level instead of turning provider mechanics into application API. DR-004 proves that mechanically selecting feature/base conflict hunks could either discard application authorization or regress established stop/restore and listener security semantics. A combined architecture is required before implementation resumes.
- Resolution:
  - Refine BEH-008, REQ-018–REQ-021, and AC-032–AC-039 so role configs select only routed business/collaboration/publication capabilities and role prompts require the exact first application call, marker, canonical artifacts, result-use witness, relative publication, complete handoff, and truthful failure without naming any runtime/provider foundation operation. Current Codex file-change/normalized events may corroborate execution but are not prompt/API/cross-runtime contracts.
  - Add BEH-009, UC-009, REQ-022/REQ-023, and AC-040–AC-044 for the finalized latest-base contract: deterministic non-secret route identity, headerless dedicated-loopback descriptor, active-only state, current restore rematerialization, exact-run deactivation, and orthogonal package/run lifecycles.
  - Preserve `AgentToolsMcpHost` as the sole listener/registry/catalog/dispatcher/scoped-authority process owner. Add only the complete static-name snapshot; expose no raw route dependency or local base. Remove the feature bearer/issuer/revoker/main-listener/global-default/partial-owner seam without compatibility wrappers.
  - Extend tight session base/execution capabilities with explicit nullable `applicationAgentTools`. General scopes pass `null`; application scope passes the sealed capability. `AgentToolMcpSessionService.activateForRun` builds current execution capabilities, resolves current application routes from current execution/sender/catalog state, freezes pure route/fingerprint data, and inserts one active record. Restore repeats this path at the same URL; no live state is persisted or reused.
  - Update `AgentProviderFactoryBuilder.createForExecution` and `ApplicationExecutionScopeKernelBuilder` so Codex/Claude receive `agentToolMcpRunSessions`, AutoByteus receives the optional application capability locally, and `AgentRunResourceManager` retains exact `deactivateForRun`. Do not restore issuer injection or make AutoByteus use local HTTP.
  - Keep `ApplicationCatalogTransitionService` / `ApplicationAgentToolCallLifecycle` independent of run-session liveness. Reload/removal drains only the affected application lane and leaves other routes active; gateway currentness/fingerprint/ownership checks govern an old application route. Exact managed run stop alone deactivates the session. Shutdown blocks activation/admission, drains/stops apps, finalizes runs/sessions/scopes, closes the capability, then closes the process listener.
  - Add focused integration/architecture coverage for latest-base topology, general/application injection, create/stop/restore current-route materialization, package drain without session teardown, changed/removed fingerprints, exact cleanup, listener order, and absence of legacy bearer/main-route/issuer APIs. Rerun the actual business-focused Brief browser journey; provider event evidence is optional corroboration, not prompt authority.
- Approved behavior or requirement IDs affected: refines BEH-003, BEH-005, BEH-008 and REQ-005/REQ-006/REQ-012–REQ-015/REQ-018–REQ-021 with unchanged product outcomes; adds BEH-009, UC-009, REQ-022, REQ-023, and AC-040–AC-044. BEH-001/BEH-002/BEH-004/BEH-006/BEH-007, REQ-001–REQ-004/REQ-007–REQ-011/REQ-016/REQ-017, and AC-001–AC-031 retain their approved outcomes.
- Canonical artifacts and sections updated:
  - `requirements.md` — business-focused prompt authority, latest-base current behavior, BEH-009/UC-009, REQ-022/REQ-023, AC-040–AC-044, lifecycle/listener constraints, coverage mapping, and approval record.
  - `application-owned-mcp-intended-behavior.md` — role/runtime ownership split, dedicated deterministic session lifecycle, package/run orthogonality, proof wording, and latest-base approval record.
  - `investigation-notes.md` — user clarification, API-REV-004 historical pass, DR-004 evidence, latest-base ticket/source/current-owner trace, feature/base comparison, lifecycle/interleaving decisions, files/tests, and reviewer guidance.
  - `design-spec.md` — current/intended state; BEH-008/BEH-009; DS-015–DS-017; host/session/provider/kernel/run-resource/application-transition ownership; exact interfaces/data shapes; operation-agnostic role flow; removal/rejection plan; concrete lifecycle matrix; sequence, tradeoffs, risks, and implementation/coverage guidance.
  - `solution-revision-record.md` — this `SR-009` entry.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` remains the approved intended-behavior supplement and is aligned. `delivery-latest-base-conflict-report.md` and the completed latest-base ticket under `origin/personal:tickets/done/agent-tools-mcp-session-resume/` remain triggering evidence/context, not new intended-behavior supplements. No supplement is removed.
- Downstream and architecture-review impact: `/architecture_reviewer` must review the full cumulative package against both triggers. A pass is required before `/implementation_engineer` resolves the source conflicts. Implementation must begin from latest-base session owners, preserve application authorization/gateway behavior, remove old session APIs cleanly, update business prompts/tests, and rerun the full source/API/E2E cycle. Delivery remains paused; after review and renewed API/E2E evidence it must integrate latest base again, build Electron, and request renewed user verification.
- Next recipient or routing: `/architecture_reviewer` with the cumulative core/supplement/review/implementation/code/API/delivery package and DR-004 report.
- Remaining gaps or risks: No unresolved requirement gap. Residual implementation risks are conflict-resolution regression toward the old session seam, capability injection into general scope, stale route reuse on restore, accidental package-to-session lifecycle coupling, listener shutdown ordering, model nondeterminism, and evidence drift. The design supplies explicit interfaces, forbidden paths, lifecycle matrix, file mapping, and focused executable cases for each.

### SR-010 — Remove the unrequested zero-shell proof rule

- Triggering role, report path, and round: explicit user correction on 2026-08-28 after `/api_e2e_engineer` `API-REV-005` in `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/api-e2e-execution-coverage-report.md` and `/code_reviewer` `CRR-012` in `/home/autobyteus/workspace/autobyteus-workspace-application-owned-mcp-capability/tickets/in-progress/application-owned-mcp-capability/code-review-report.md`. Review history: `api-e2e-revision-record.md` and `code-review-revision-record.md`.
- Triggering finding IDs: reopened `CR-DI-002`; material premise `CR-MP-002` (`Reachable`); sole failed acceptance criterion was prior `AC-039`.
- Prior authoritative result: SR-009/ARCH-REV-009/IR-006/IR-007/CRR-011 produced the current latest-base implementation. API-REV-005 passed the application-owned MCP call, deterministic tokenless session lifecycle, both real Codex/Luna first calls/results, exact artifacts, relative publications, complete handoff, application/binding/producer identity joins, read-only causality, same-brief reconciliation, and supported browser UI. It reported `Fail / 96.4%` only because prior AC-039 disqualified the correct shell-created artifacts. Both roles used their automatically supplied authorized shell and otherwise satisfied AC-032–AC-038 and AC-040–AC-044.
- Current authoritative result: The zero-shell condition is removed. The maintained business prompts remain operation-neutral, runtime provisioning remains unchanged, and the model may use any already-authorized automatically supplied foundation capability, including shell. Acceptance is based on the real artifact inside the exact member workspace, required path/content/marker, relative publication, complete handoff/result-use witness, exact application/binding/producer identity, and publication-caused UI outcome. Operation telemetry is optional diagnostics only. Fabricated, missing, outside-workspace, unauthorized, unpublished, or causally unjoined output still fails.
- Why this revision entry is recorded: The user explicitly stated that the zero-shell rule was never requested and ordered it removed. The solution process introduced and retained that condition while attempting to force a provider-specific file-operation proof. API-REV-005 demonstrates that this artificial oracle—not the product architecture or implementation—was the only blocker.
- Resolution:
  - Revise BEH-008, REQ-020/REQ-021, and AC-032/AC-033/AC-038/AC-039 so any authorized foundation operation, including shell, is valid and operation labels cannot decide pass/fail.
  - Keep `agent.md`, `team.md`, and launch instructions business-focused and free of prescribed foundation/provider operation names. Do not add a runtime capability restriction, provider policy, application-specific file adapter, or prompt choreography.
  - Preserve the exact passing SR-009/ARCH-REV-009 production architecture: application catalog/routes/gateway/worker, deterministic tokenless activation/restore/deactivation, application-lane/session orthogonality, read-only context tool, relative publication, reconciliation, and UI owners are unchanged.
  - Update DS-013/DS-014, proof ownership, file-impact boundary, rejection log, risks, guidance, and downstream coverage so the authoritative artifact/workspace/publication/handoff/identity/UI join remains mandatory while provider/normalized operation events are diagnostics only.
  - Preserve the three API-REV-005 durable test edits for later proportional code review. Correct stale proof-oracle/evidence wording, then reclassify or rerun the same real browser journey under corrected AC-039; do not substitute direct MCP or a mock.
- Approved behavior or requirement IDs affected: BEH-008, REQ-020, REQ-021, AC-032, AC-033, AC-038, and AC-039 are corrected by explicit user direction. BEH-001–BEH-007/BEH-009, REQ-001–REQ-019/REQ-022/REQ-023, and AC-001–AC-031/AC-034–AC-037/AC-040–AC-044 retain their established outcomes.
- Canonical artifacts and sections updated:
  - `requirements.md` — status, BEH-008, investigation basis, recommendation, supplement approval, REQ-020/REQ-021, AC-032/AC-033/AC-038/AC-039, maintained-proof decision, and approval record.
  - `application-owned-mcp-intended-behavior.md` — authorized foundation-operation ownership, proof boundary, representative journey, and approval record.
  - `investigation-notes.md` — API-REV-005/CRR-012 evidence, explicit user correction, superseded zero-shell history, current validation posture, and architecture-review guidance.
  - `design-spec.md` — current-state evidence, BEH-008, no-architecture-change health classification, DS-013/DS-014, operation-diagnostic boundary, forbidden shortcuts, file impact, sequence, tradeoffs, risks, and implementation/API-E2E guidance.
  - `solution-revision-record.md` — this `SR-010` entry.
- Supplemental artifacts updated, added, or removed: `application-owned-mcp-intended-behavior.md` remains the approved intended-behavior supplement and is aligned. `api-e2e-agent-ui-proof-gap.md`, API-REV-005 evidence, and CRR-012 remain triggering context/evidence; no supplement is removed.
- Downstream and architecture-review impact: `/architecture_reviewer` should confirm this is a requirements/proof correction with no production architecture change. After a pass, `/implementation_engineer` should confirm production source/prompts remain unchanged and avoid inventing a capability restriction. `/api_e2e_engineer` should update the proof oracle and reclassify or rerun the same real browser journey; the current durable coverage diff then returns through `/code_reviewer` for proportional review before delivery resumes.
- Next recipient or routing: `/architecture_reviewer` with the full cumulative package, CRR-012/API-REV-005 reports/revision records, and retained current-run evidence.
- Remaining gaps or risks: No architecture gap and no production behavior gap. The only residual risk is retaining the invalid zero-shell assertion in a downstream test/evidence synthesizer or, while removing it, weakening the real workspace artifact, publication, handoff, identity, authorization, read-only causality, or browser checks.
