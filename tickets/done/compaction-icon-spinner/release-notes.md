## Fixes
- Fixed the compaction sync icon so it spins while memory compaction is actively running.
- Kept queued, completed, and failed compaction states visually still so they do not look like active processing.
