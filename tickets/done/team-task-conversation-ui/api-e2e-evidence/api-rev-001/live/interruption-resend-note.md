# Interruption turn resend note

The browser submitted `LIVE_INTERRUPTION_HOLD` immediately after the accepted record and requested ordinary message became visible, while the Teacher's provider turn was still settling. That first UI send did not create a second task. A targeted temporary client therefore resent the identical text through the public Team WebSocket `SEND_MESSAGE` command to the same owned RootTeamRun and its discovered `/Teacher` AgentRun. The still-open main browser then observed the real second task as active, captured `live-interruption-active.png`, invoked the supported `terminateAgentTeamRun` mutation, observed the authoritative task become `interrupted` with an `interruption` update, and captured `live-interruption-terminal.png`. No record or persistence file was edited directly.
