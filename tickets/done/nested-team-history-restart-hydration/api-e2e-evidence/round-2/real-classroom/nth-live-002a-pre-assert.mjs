import fs from 'node:fs/promises';import path from 'node:path';import { chromium } from '../../../node_modules/.pnpm/playwright-core@1.58.2/node_modules/playwright-core/index.mjs';
const e=path.resolve('api-e2e-evidence/round-2/real-classroom'),runtime=(await fs.readFile(path.join(e,'runtime-root.txt'),'utf8')).trim(),base=`http://127.0.0.1:${(await fs.readFile(path.join(e,'frontend-port.txt'),'utf8')).trim()}`;const rootTeamRunId=(await fs.readdir(path.join(runtime,'memory/agent_teams'))).find(x=>x.startsWith('nested_classroom_test_team_')),runDir=path.join(runtime,'memory/agent_teams',rootTeamRunId),tree=JSON.parse(await fs.readFile(path.join(runDir,'team_run_execution_tree.json'),'utf8')),teacherId=tree.rootTeam.members.find(x=>x.address==='/Teacher').agentRunId,studentTeam=tree.rootTeam.members.find(x=>x.address==='/StudentStudyGroup'),studentId=studentTeam.members.find(x=>x.address.endsWith('/student_one')).agentRunId,request='NTH_LIVE_002A_TEAM_PRE_20260823',ack='NTH_LIVE_002A_TEAM_ACK_PRE_20260823',refPath=path.join(runtime,'temp_workspace/api-e2e-round2-references/team-address-reference.txt'),messages=JSON.parse(await fs.readFile(path.join(runDir,'team_communication_messages.json'),'utf8')).messages;
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}),p=await b.newPage({viewport:{width:1600,height:1100}});p.setDefaultTimeout(180000);const errors=[];p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});try{console.log('open');await p.goto(`${base}/workspace/`,{waitUntil:'domcontentloaded'});await p.locator('[data-test="workspace-row"] button').first().click();await p.locator('[data-test="workspace-team-definition-row-nested-classroom-test"]').click();const root=p.locator(`[data-test="workspace-team-row-${rootTeamRunId}"]`);await root.click();const center=p.locator('[data-test="workspace-center-pane"]'),disclosure=p.locator(`[data-test="workspace-team-member-disclosure"][data-team-run-id="${rootTeamRunId}"][data-member-address="/StudentStudyGroup"]`);if(await disclosure.getAttribute('aria-expanded')!=='true')await disclosure.click();console.log('student');const student=p.locator(`[data-test="workspace-team-member-${rootTeamRunId}-/StudentStudyGroup/student_one"]`);await student.click();await center.getByText(ack,{exact:false}).first().waitFor();const studentText=await center.innerText();console.log('teacher');await p.locator(`[data-test="workspace-team-member-${rootTeamRunId}-/Teacher"]`).click();await center.getByText(ack,{exact:false}).last().waitFor();const teacherText=await center.innerText(),right=p.locator('[data-test="workspace-right-panel"]');await right.locator('[data-tab-name="teamMembers"]').click();await right.getByText(request,{exact:false}).last().waitFor();await right.getByText(ack,{exact:false}).last().waitFor();const commText=await right.innerText(),shot=path.join(e,'nth-live-002a-pre-before-stop.png');await p.screenshot({path:shot,fullPage:true});console.log('terminate');const terminate=root.getByRole('button',{name:'Terminate team',exact:true});await terminate.click();await terminate.waitFor({state:'detached'});const result={scenario:'NTH-LIVE-002A',phase:'pre-restart',rootTeamRunId,teacherId,studentId,request,ack,refPath,uiTeacherContainsAck:teacherText.includes(ack),uiStudentContainsAck:studentText.includes(ack),uiCommunicationContainsRequest:commText.includes(request),uiCommunicationContainsAck:commText.includes(ack),messages,screenshotPath:shot,consoleErrors:errors,approvalNote:'Initial browser disconnected before the pending button became actionable; exact pending invocations were approved through a real browser WebSocket on the same canonical Team route.'};if(messages.length!==2||!result.uiTeacherContainsAck||!result.uiStudentContainsAck)throw new Error(JSON.stringify(result));await fs.writeFile(path.join(e,'nth-live-002a-pre-result.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));}finally{await b.close()}
