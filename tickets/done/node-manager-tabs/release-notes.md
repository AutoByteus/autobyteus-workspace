# Release Notes: Node Manager Tabs

## Improvements

- Settings → Nodes now opens on a Manage Nodes tab for actionable node registration, sync, and remote browser sharing controls.
- Docker launcher instructions now live in a separate Docker Guide tab so the static tutorial no longer dominates the node settings flow.
