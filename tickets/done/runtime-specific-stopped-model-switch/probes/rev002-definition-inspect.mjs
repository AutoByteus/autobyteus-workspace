import fs from 'node:fs/promises';import{createRequire}from'node:module';const require=createRequire(new URL('../../../../autobyteus-web/package.json',import.meta.url));const{chromium}=require('playwright-core');const dir=new URL('.',import.meta.url).pathname;const info=JSON.parse(await fs.readFile(dir+'live-stack-info.json'));const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const p=await b.newPage();await p.goto(info.frontendUrl+'/agents');const t=p.getByText('REV002 Saved Default Agent',{exact:true}).first();await t.waitFor({state:'visible'});console.log(await t.evaluate(e=>e.parentElement?.parentElement?.outerHTML.slice(0,4500)));await b.close();
