# Architecture Design Complete — SR-007 repeat-review handoff

## Result And Requested Next Action
- Stable package: `runtime-specific-stopped-model-switch`.
- Classification: **Architecture Design Complete** on approved revised requirements; `task_size=Medium`, `architectural_risk=High`. Independent architecture review is required before implementation. The user expressly requested “additional review.”
- Review request: independently review **SR-007 design against SR-006-approved requirements** under the team architecture-review skill and design principles. Scrutinize backend catalog ownership, saved exact-`default` settings continuity, self-contained run-option DTO/frontend display, catalog consumers, removals and persisted-data transition. Report findings to the correct owner using the reviewer’s own route; do not treat old ARCH-REV-001 as a pass for this revision.
- Handoff rule decision: the completed revised architecture package has architectural_risk=High and approved requirements, so the most specific returned rule selects only `/architecture_reviewer`. No implementation, test-code, deployment or delivery completion is claimed by this result.

## Original Request And Approval Basis
The user originally wanted stopped Agent/Team/Org runs to switch to any model supplied by Claude Agent SDK, Codex App Server or Antigravity CLI, including smaller/unknown-context models; AutoByteus retains its verified non-decreasing-capacity rule. The user explicitly approved that behavior at SR-002. During explicit verification of the ticket-built Electron app, the user saw a confusing “Saved / runtime-offered” group and a raw `default` Claude choice in addition to a listed concrete equivalent. Read-only backend/UI investigation found that the SDK `default` resolved to listed `opus`; the backend sent both and the frontend’s alias folding/fallback produced the odd row. The user then directed backend-side filtering, saying the “backend provide, frontend display,” and, after the exact REQ-008/AC-010–011 baseline was presented, explicitly asked for a principled revised design and additional review. That is the SR-006 approval reference. It is **not** approval of the current Electron build or final delivery verification.

Approved correction: Claude backend selection-facing provider catalog omits redundant `default` only when unambiguously equivalent to another listed concrete ID; retain it if no proven sibling. Preserve an old run’s exact saved `default` ID, current-value visibility, supported unchanged-model settings edit while raw SDK still reports it, and normal provider continuation. Do not silently convert it to `opus`, add an external context-size gate, hide aliases only in frontend, or change the AutoByteus rule. Fresh changed Save uses the backend offered set and exact target schema.

## Canonical Package Paths
- Approved requirements: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/requirements-doc.md` — SR-006 Approved; REQ-001–008, AC-001–011, SCN-001–007.
- Canonical investigation/evidence: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/investigation-notes.md` — E01–E22, AE-01–15.
- Revised design authority: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/design-spec.md` — SR-007.
- Cumulative history: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/solution-revision-record.md` — SR-001–007.
- Still-relevant evidence supplement: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/model-picker-verification-investigation.md` — read-only packaged UI/backend diagnosis, not normative UI/UX approval.
- Historical review, **old basis only**: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/design-review-report.md` (ARCH-REV-001 Pass for SR-003/SR-002), and `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/tickets/in-progress/runtime-specific-stopped-model-switch/architecture-review-revision-record.md`.
- Current SR-007 independent review report: **N/A — not yet applicable**. Product prototype/UI/UX spec: **N/A — not requested**. Behavior-defining supplements beyond approved requirements: **N/A**.

## Design And Evidence Summary
The Claude SDK adapter remains a faithful raw source. `ClaudeModelCatalog` owns offered-row normalization based on unambiguous resolved-model evidence and retains exact raw-current lookup from the same snapshot. `ModelCatalogService` is the runtime catalog boundary used by provider snapshots, launch consumers and `RunModelSelectionService`; the selection service does not bypass it to reach Claude SDK internals. For stopped-run options, server returns self-contained replacement descriptors plus an exact saved-current descriptor/schema (or null if raw row is unavailable). Changed Save accepts only currently offered IDs; unchanged Save resolves the persisted exact ID from fresh raw evidence. Agent/Team/Org stopped lifecycle and atomic writers, AutoByteus capacity gate, history/provider binding and normal restore remain unchanged. The Web existing-run picker displays these backend descriptors and saved current separately; frontend alias folding/aliasIds/mixed fallback are removed after caller audit. Provider snapshots are already normalized for launch pickers. This is a bounded refactor, not a new runtime or storage design.

Key evidence: E17–19 reproduced 11 backend Claude IDs plus saved historical ID in packaged UI, with `default`→`opus` alias relation; AE-10–15 traced SDK, catalog, selection, GraphQL and frontend schema/alias paths. A pure backend row filter would break existing saved-`default` settings because Save and UI schema currently both read only offered rows. The richer run-option/current descriptor closes that real supported gap and eliminates a second frontend catalog intersection. Representative persisted Agent/Team/Org files are directly usable without migration (AE-07); there is no stored-shape change or silent exact-ID rewrite.

## Constraints, Residual Risks, And Expected Review Output
- Review the actual target production spines DS-01–06, authoritative boundary/no-bypass, DTO tightness, file responsibility and clean-cut removal in the revised design. Specific issue to challenge: whether self-contained run-option descriptors are the proportionate way to preserve saved-current schema and avoid Web/server catalog drift.
- `listLlmModels` has launch/platform consumers; audit exact saved-`default` values without inventing a generic legacy wrapper. If a supported continuity conflict is found, classify and route it rather than assume the old SR-003 review covers it.
- Fresh Save remains catalog/schema/lifecycle authoritative; cached UI choices may become stale. Provider-native long-history continuation under smaller external models remains uncertain and must be bounded honestly downstream, not “fixed” with a restored platform context gate.
- Current Electron delivery remains in explicit user-verification hold; no final user acceptance, release, cleanup or `Delivery Completed` receipt is represented here.
- Worktree `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch`, branch `codex/runtime-specific-stopped-model-switch`; refreshed base `origin/personal` at `a2694ed453e353550d8b345fa82ef489634dcaf2`; finalization target `origin/personal` via Delivery Engineer.
- Expected output: independent architecture review report/revision record for SR-007, with Pass or traceable findings and the reviewer’s rule-based routing. Do not directly implement on the historical ARCH-REV-001 basis.

## Applied Handoff Rule
`get_handoff_rules` returned the High-risk Architecture Design Complete condition for `/architecture_reviewer`; the Small/Medium Low-risk direct implementation rule does not match because risk is High, and the delivery-receipt-gap rule does not match because no `Delivery Completed` receipt was received. Notify only `/architecture_reviewer` with this result file attached. Message acceptance remains to be confirmed by `send_message_to`.
