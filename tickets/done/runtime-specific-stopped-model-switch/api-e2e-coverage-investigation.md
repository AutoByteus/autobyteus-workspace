# API/E2E Coverage Investigation — Runtime-specific stopped-run model switching

## Investigation meta and authority
- Round 1; trigger: CRR-002 Pass after IR-002; latest investigation is this file. Prior API/E2E investigation/result/revision: N/A.
- Worktree: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch`.
- Approved requirements: `requirements-doc.md` SR-002; investigation: `investigation-notes.md`; solution record: `solution-revision-record.md`; design: `design-spec.md` SR-003; supplement: `architecture-design-result.md`; independent design review: `design-review-report.md`, `architecture-review-revision-record.md` ARCH-REV-001; implementation: `implementation-handoff.md`, `implementation-revision-record.md` IR-002; independent source review: `code-review-report.md`, `code-review-revision-record.md` CRR-002. These are all canonical files in this ticket directory. Current-state screenshot path is in the upstream investigation; Product prototype N/A. Delivery record N/A.
- API/E2E revision record: to be created at first complete result as API-REV-001. Ledger: `api-e2e-test-case-ledger.md`.
- Classification: **Medium / High**, Reviewed route; successful output to Code Reviewer for proportional review of changed durable test code.

## Requirement, design, and changed-boundary map
| IDs | Expected proof | Changed boundary / consequence |
| --- | --- | --- |
| REQ-001/002/007; AC-001/002/009; SCN-001–003 | Stopped external Agent/Team/Org list exact runtime-catalog IDs, including Claude `default` and explicit sibling, smaller/unknown targets; accurate copy; Save persists schema-valid target. | Server selection policy, GraphQL ID-only option shape, Web editor/picker/copy. |
| REQ-003; AC-003; SCN-004 | Native AutoByteus remains verified non-decreasing; settings-only bypasses capacity. | Native-capacity resolver and shared validator. |
| REQ-004/006; AC-004/005/008; SCN-002/003/005 | Fresh catalog/schema, lifecycle/ownership/archive guards, linked-scope-only atomic commit, visible failure/retry. | Agent/Team/Org lifecycle writers and GraphQL; Web reconciliation. |
| REQ-005; AC-006/007; SCN-006 | Existing records read directly, selected model takes effect on normal resume without Save-time history/binding reset; provider acceptance or visible rejection is distinguished. | Persisted metadata/tree, restore and external-provider boundary. |

Changed surfaces: backend domain **yes**; API/GraphQL **yes**; frontend/store **yes**; browser journey and web-equivalent Electron renderer **yes**; process/lifecycle/persisted direct-use **yes**; external provider **yes**. Authentication/permissions are preserved but matter for real-provider setup. Electron preload/window/packaging, workers/queues/distributed coordination **no material code change**; actual desktop execution is not selected because browser can exercise the changed renderer/HTTP behavior without touching a user's app.

Legacy/persisted-data decision: IR-002 reports no retained external-capacity compatibility path and **Directly Usable — No Migration**. Representative Agent metadata, Team V2 tree, Org V1 tree plus history snapshot must read via current GraphQL/restore; no conversion, fallback or discard. A discovered dual-path shim would be rerouted, not tested as approved behavior.

## Project execution discovery
| Path | Authority / commands and constraints |
| --- | --- |
| Root `README.md`, `package.json` | `pnpm dev` for built backend + Nuxt browser stack; `pnpm test:e2e` deterministic server E2E; `pnpm test:e2e:real:preflight` and `pnpm test:e2e:real` external capability checks. Dev state is isolated under this worktree's `.autobyteus/development`. |
| `autobyteus-server-ts/AGENTS.md`, `README.md`, `package.json` | Vitest `run --no-watch`; built-server E2E uses test-owned isolated SQLite/runtime and sanitizes inherited credentials. Real provider requires explicit capability and cannot be inferred from ordinary E2E. |
| `autobyteus-web/AGENTS.md`, `README.md`, `package.json` | Web tests `pnpm test:nuxt --run`; browser probe `pnpm test:e2e:existing-run-model-config -- --output-dir ...` owns temporary Nuxt route/process, Chromium and cleanup. |
| `test-support/live-e2e/test-runtime-bootstrap.mjs` and `run-live-e2e.mjs` | Isolated server start/stop, readiness, database and evidence handling; real preflight must report missing capability rather than simulate success. |

No server or data store already running in another worktree will be stopped or reused. Built-server E2E and browser probe own their runtimes and cleanup. Real-provider continuation requires a reachable configured external credential/session; no secret values will be recorded. Browser probe uses intercepted GraphQL and therefore proves renderer semantics, not a real backend contract; server process E2E and live probe must separately cover that gap.

## Existing durable coverage inventory and validity
| Path/scenario | Decision | Reason/action |
| --- | --- | --- |
| `autobyteus-server-ts/tests/unit/llm-management/run-model-selection-service.test.ts` | Still Valid | IR-001 external trio, native threshold, fresh catalog, schema and batch cases represent approved behavior; execute. |
| `autobyteus-server-ts/tests/unit/agent-team-execution/team-run-model-selection-save.test.ts` | Needs Update | First two cases assert the obsolete universal non-decreasing gate while fixture runtime is Codex; REQ-002 now permits decreasing external targets. Retarget those preservation assertions to AutoByteus with the current synchronous numeric native-capacity contract. Preserve the nine persistence/reconciliation cases on Codex. |
| `autobyteus-server-ts/tests/unit/agent-org-execution/agent-org-run-service-model-selection.test.ts` | Still Valid | Per-scope policy owner; execute. |
| `autobyteus-server-ts/tests/e2e/run-history/stopped-run-model-config-graphql.e2e.test.ts` | Needs Update | Real GraphQL/metadata/restore covers native Agent/Team but not external replacement or ID-only contract. Add deterministic GraphQL schema assertion for the narrowed option result/row. External catalog Save was exercised in the separate isolated full-stack browser environment; do not make CI depend on a local provider login. |
| `autobyteus-server-ts/tests/e2e/agent-org-runs/stopped-org-workspace-graphql.e2e.test.ts` | Still Valid; possible update | Real Org lifecycle/direct-use proof is native; execute, consider external extension only if deterministic. |
| `autobyteus-web/components/launch-config/__tests__/RuntimeModelConfigFields.spec.ts` and related form/store tests | Still Valid | Claude exact-ID picker, lag/error and copy assertions are useful mocked component evidence; execute. |
| `autobyteus-web/tests/e2e/existing-run-model-config-probe.mjs` + fixture | Needs Update | Existing Agent/Team browser journey uses current ID-only DTO but native-only options. Add a bounded external picker/copy case if feasible; run existing case suite. |
| Prior deleted external capacity-reader tests | Stale / Remove already completed by IR-001 | Obsolete universal capacity assertion contradicted by REQ-002; current service tests replace policy coverage. No new removal in this round. |

## Coverage actions before execution
- Add durable ID-only GraphQL option contract assertions in the existing built-server E2E suite. Real external catalog/Save is exercised by the isolated package-backed full-stack browser/API probe. Do not commit a CI test dependent on local Claude/Codex/Antigravity credentials.
- Update the two stale Team unit assertions described above before rerunning API-E2E-001; no implementation defect is inferred from their old expected outcome.
- Extend durable browser probe for external exact-ID selection and runtime-specific copy if its fixture can be adapted without creating a parallel UI path; retain existing native journey.
- No compatibility-only coverage or migration fixture to be added.
- Do not change production code from this stage. Any implementation failure returns to Code Reviewer for origin review.

## Planned cases and repository execution
Ledger is required for multi-case and long-running validation. Initialize before execution; case IDs are stable.
| Order / case | Command or entry | Proof |
| --- | --- | --- |
| 1 / API-E2E-001 | Focused server Vitest unit/integration suites | External trio/native policy, schema, scope batching/atomicity |
| 2 / API-E2E-002 | Focused Web Vitest suites | Exact-ID picker, copy, store and schema lag |
| 3 / API-E2E-003 | Built-server GraphQL Agent/Team/Org E2E | Real transport, lifecycle, readback/restart and direct-use |
| 4 / API-E2E-004 | Web existing-run browser probe | Renderer journey and Settings copy/picker |
| 5 / API-E2E-005 | Real-provider preflight then targeted external continuation if configured | Representative smaller-window resume/provider failure/history |
| 6 / API-E2E-006 | Contract/schema and broader affected suite/build | Removed numeric fields, no regression |

Post-repository scorecard, broader-validation gate and final coverage decisions will be filled after the planned repository checks; no result or confidence is inferred from IR-002/CRR-002. Broader validation is **Required** for browser/web-equivalent journey and real-provider boundary. The user explicitly requires the real frontend and backend to be started and suggests importing Daily Assistant, Classroom Simulation Team, and Software Development Department from `/Users/normy/autobyteus_org/autobyteus-agents`. After repository checks, use the worktree-owned full-stack development environment and browser to test those package-backed stopped-run Settings surfaces; keep imported data isolated under the worktree's development root and clean only owned state/processes. A mocked GraphQL browser probe is supplementary, not a substitute for this full-stack journey. `nuxi typecheck` toolchain failure remains a known environment risk; a build/test pass does not imply typecheck success.

## Repository execution and post-repository decision (API-REV-001)
The planned case order was refined without changing scope: the user-required real-stack browser journey replaced the mocked fixture as the main API-E2E-004 proof; the fixture probe remained a supplemental native renderer regression. The temporary provider journey and edge checks were run before the final fixture regression so the owned Nuxt process could be stopped before the fixture's separate Nuxt process started. The external fixture extension was attempted but reverted because its synthetic runtime-catalog response left the editor in an options-unavailable state even though the real backend journey succeeded. No production defect was inferred from that synthetic mismatch, and no invalid durable test was retained.

| Command / mode | Result | Boundary and evidence |
| --- | --- | --- |
| `pnpm -C autobyteus-server-ts exec vitest run tests/unit/llm-management/run-model-selection-service.test.ts tests/unit/agent-team-execution/team-run-model-selection-save.test.ts tests/unit/agent-org-execution/agent-org-run-service-model-selection.test.ts tests/unit/agent-org-execution/agent-org-run-config-options.test.ts tests/unit/run-history/services/studio-run-model-config-service.test.ts --no-watch` | Pass, 5 suites / 42 tests | Policy/schema/scope; `api-e2e-server-focused.log` |
| `pnpm -C autobyteus-web test:nuxt components/launch-config/__tests__/RuntimeModelConfigFields.spec.ts components/workspace/config/__tests__/AgentOrgRunConfigForm.spec.ts components/workspace/team/__tests__/TeamCanonicalPlus.spec.ts stores/__tests__/existingRunConfigStore.spec.ts --run` | Pass, 4 suites / 54 tests | Picker/store/copy; `api-e2e-web-focused.log` |
| `pnpm -C autobyteus-server-ts build` | Pass | Built backend, `api-e2e-server-build.log` |
| `pnpm -C autobyteus-server-ts exec vitest run tests/e2e/run-history/stopped-run-model-config-graphql.e2e.test.ts tests/e2e/agent-org-runs/stopped-org-workspace-graphql.e2e.test.ts --no-watch` | Pass, 2 files / 3 tests | Real GraphQL, SQLite, direct-use, ID-only schema assertion; `api-e2e-graphql.log` |
| `pnpm -C autobyteus-web test:e2e:existing-run-model-config -- --output-dir ../tickets/in-progress/runtime-specific-stopped-model-switch/probes/durable-browser` | Pass, 6 scenarios | Actual editor with deterministic GraphQL interception, native renderer regression; `api-e2e-durable-browser.log` |
| `pnpm -C autobyteus-web build` | Pass | Production Nuxt compile/prerender; `api-e2e-web-build.log` |

### Post-repository confidence scorecard
Scored after focused tests/built-server E2E but before the real package-backed browser/provider journey. The later deterministic browser/build rerun did not materially alter the decision to perform real-stack validation.
| Category | Score | Support | Remaining uncertainty / validation selected |
| --- | --- | --- | --- |
| Requirement and AC proof | 90% | Focused policy and UI assertions plus real built-server native E2E | External package-backed real Save and continuation absent; full stack |
| Changed-boundary directness | 90% | Real backend and actual components each tested | Crossed browser/GraphQL boundary not yet exercised; full stack |
| Cross-boundary realism / mock gap | 75% | Built GraphQL native; component/provider catalogs mocked | External catalog and frontend/backend coupling; full stack |
| Environment/config/identity/fixture fidelity | 90% | Isolated built-server SQLite and authentic app test setup | Imported real package and CLI identity not yet exercised; full stack |
| Failure/edge/lifecycle/recovery | 90% | Unit atomicity, schema, active/archive and native built-server cases | Real external live failures/history needed; API and provider probes |
| User-surface/browser/desktop-shell | 75% | Component tests and deterministic browser fixture | Real Settings routes/Network/API absent; Chromium against both services |
| Durable regression quality/relevance | 90% | 42 backend + 54 Web and ID-only GraphQL contract; stale Team tests corrected | No durable real-provider/browser-against-backend run (credential-dependent) |

- Overall post-repository confidence: **85.7%** (simple mean, 600/7), with two categories below 90%; no repository-only Pass. Every critical AC directly proven at this point: **No**.
- Broader-validation decision: **Required**. Expected gain: verify exact runtime catalog, package-backed stopped Agent/Team/Org Settings and atomic persistence through real browser/GraphQL; real provider continuation or visible rejection and history identity. Selected modes: isolated backend + Nuxt + Chromium, live GraphQL edge probes, external Codex/Claude sessions. This addresses the specific mock gap and explicit user instruction, not a generic browser ritual.
- Desktop decision: browser exercises changed Electron-web-equivalent renderer and HTTP boundary. No relevant preload/IPC/window/packaging code changed; actual desktop startup would risk the user's app without materially improving this validation.

## Broader-validation environment and final coverage decision
- First `pnpm dev` fixed-port attempt was started and owned by this run, then stopped when pre-existing development data in that worktree was discovered. Existing data was not altered. Validation moved to a unique test-owned runtime/SQLite path with dynamic ports and an owned Nuxt process; `probes/live-stack.mjs` controlled startup/stop/readiness and cleanup. This preserved the documented backend/Nuxt architecture while avoiding collision.
- Imported `/Users/normy/autobyteus_org/autobyteus-agents` through public GraphQL `importAgentPackage`; seeded Daily Assistant Agent, Classroom Simulation Team, and Software Development Department Org stopped runs through public GraphQL. Five initial stopped fixtures and two live provider runs were confined to the owned test runtime. No shared user application state was reset or reused.
- Live API and browser evidence: `probes/fullstack-catalog-import.json`, `fullstack-seed-evidence.json`, `fullstack-browser-evidence.json`, `team-browser-evidence.json`, `org-browser-evidence.json`, `fullstack-api-edge-evidence.json`, `fullstack-lifecycle-edge-evidence.json`; screenshots and logs in `probes/`.
- Provider evidence: real Codex Astra→Luna switch resumed on same run/thread and answered `PROBE_BETA` after `PROBE_ALPHA` with history visible. Real Claude `[1m]`→Haiku Save retained session/trace and history, but provider quota rejected both turns visibly. These results prove identity/history preservation and one real external continuation; they do **not** prove successful Claude continuation or a numeric smaller-window pair, because the live external catalogs reported null context capacity. The UI's policy correctly does not require numeric capacity for those runtimes.
- Final residuals: successful Claude and Antigravity provider continuation not executed; external model capacity ordering not independently verified; Electron shell-specific behavior not exercised (unaffected). The temporary package-backed real browser/provider harness is retained as evidence, not added as credential-dependent CI coverage. Existing component/unit/API tests are durable for the policy and option shape. No migration, compatibility branch, or obsolete coverage was introduced.
- Final confidence: **94.3%** (categories 95/95/95/95/95/95/90; mean 660/7). No category below 90%; all critical approved AC directly proven through a combination of repository and live evidence, including permitted provider rejection. The default 95% clean target is **not** met because no durable real backend/browser/provider integration suite can safely run in CI without local credentials; the selected targeted broader surface has already been executed. Outcome may be Pass with this explicit bounded residual, not a claim of universal provider success.

# API-REV-002 investigation — SR-006 / SR-009 / IR-003 / CRR-004 (2026-09-25)
This section supersedes API-REV-001's ID-only Claude-picking outcome for the newly approved REQ-008/AC-010–011. API-REV-001's 94.3% is historical, **not** a confidence score for IR-003. Trigger: CRR-004 independent source Pass; Medium/High reviewed route remains. Current authorities are revised `requirements-doc.md` SR-006, `design-spec.md` SR-009, `architecture-design-recovery-result.md`, ARCH-REV-003 in design review/revision files, `implementation-handoff.md` IR-003, `code-review-report.md` CRR-004; `model-picker-verification-investigation.md` is evidence-only. Prior `api-e2e-execution-coverage-report.md` and this investigation plus revision record were read. The five indexed local saved-default records are read-only and must not be changed.

## New behavior and boundary map
| IDs | Boundary and expected proof | Coverage decision |
| --- | --- | --- |
| REQ-008/AC-010 | Real Claude SDK default+proven sibling: backend provider snapshot and Agent/Team/Org stopped replacements omit only `default`; no-sibling retains it; Web renders exact server rows, not a local alias filter. | Unit catalog cases remain valid but mocked; **add/execute real backend GraphQL and browser probe**. |
| REQ-008/AC-011 | Exact saved `default` remains current descriptor, unchanged settings Save/restore, definition→Run/mobile, Application Setup/readiness; newly changed `default` not eligible when filtered. | Focused unit/components valid but mocked; **add/execute live API, package-backed browser and representative provider path**, isolated fixtures only. |
| REQ-002–007/AC-001–009 | External capacity-independent choices, native verified non-decreasing, fresh schema/catalog, lifecycle/atomic Team/Org, history/provider identity, accurate copy. | Rerun affected regression suites plus selected real API/browser/provider checks; prior result not inherited. |
| ARCH-REV-003 Codex CWD caution | Compare saved-workspace Codex `model/list` with process-CWD exact-current descriptor if possible. | Focused live probe, report actual observed catalog equality/difference; do not invent mismatch. |

Affected surfaces: backend Claude SDK catalog and ModelCatalogService, API/GraphQL provider snapshot/run-option/current-descriptor contracts, Web stopped picker and definition/mobile/Application setup, application readiness, persisted exact-current reader, real browser integration and external provider. No Electron shell/IPC, worker/queue or storage schema change. `Directly Usable — No Migration` remains; no compatibility-only fallback is approved.

## Coverage inventory and decisions before edits
- `autobyteus-server-ts/tests/unit/llm-management/{claude-model-catalog,model-catalog-service,run-model-selection-service}.test.ts`, `tests/unit/api/graphql/types/llm-provider.test.ts`, application host/launch tests: **Still Valid** for normalization, no-sibling, exact lookup and readiness; rerun.
- `autobyteus-web/components/launch-config/__tests__/RuntimeModelConfigFields.spec.ts`, Agent/Team/Application launch editors and store tests: **Still Valid** for current-only display/schema and no local re-offer; rerun.
- `autobyteus-server-ts/tests/e2e/run-history/stopped-run-model-config-graphql.e2e.test.ts`: **Needs Update**. API-REV-001's ID-only `RunModelOptionsObject`/row schema assertion is now obsolete under reviewed SR-009's descriptor contract. Replace those two expected field sets with current/replacement descriptor and exact-current query assertions. Preserve lifecycle E2E.
- `autobyteus-web/tests/e2e/existing-run-model-config-probe.mjs`: **Needs Update**. Its intercepted GraphQL option rows are ID-only, so its deterministic browser journey no longer represents SR-009. Update fixture DTO without introducing frontend alias policy; execute after live stack is stopped.
- Prior temporary full-stack scripts in `probes/`: **Use Temporary Executable Probe Only** after adapting to new response shape. They depend on local Claude/Codex/Antigravity CLI state and must not become credential-dependent CI tests.
- No stale durable test file removal planned. Obsolete assertion is the exact ID-only GraphQL field equality from API-REV-001, contradicted by SR-009 DTO; replacement is the new descriptor schema assertion. No general alias dedup or migration test should be added.

Project execution authority remains root README/package scripts, `autobyteus-server-ts/AGENTS.md` and Web AGENTS/README, `test-support/live-e2e/test-runtime-bootstrap.mjs`: built server then Nuxt, isolated SQLite/runtime and dynamic ports, Vitest `--no-watch`, Web `test:nuxt --run`, browser probe self-owned Nuxt, cleanup only owned resources. Local provider identity may be available but is not an entitlement; no secrets in evidence. The existing `probes/live-stack.mjs` is test-owned and can be reused with a new unique runtime; do not touch packaged Electron/local five saved-default records.

## Planned API-REV-002 cases
| Case | Proof and execution surface |
| --- | --- |
| API-E2E-007 | Focused backend/Web unit+component suites for new offered/current semantics and preserved rules. |
| API-E2E-008 | Built-server GraphQL schema/lifecycle E2E after updating obsolete descriptor assertion; provider snapshot/current descriptor contract. |
| API-E2E-009 | Isolated live backend GraphQL: Claude SDK alias proof, provider snapshot, stopped Agent/Team/Org options, exact saved-default same-model Save/restore, changed default rejection, Codex CWD comparison. |
| API-E2E-010 | Real Chromium on started backend+Nuxt with imported package: stopped Agent/Team/Org, definition→Run/mobile and Application Setup/readiness where safe/available; current-only default and no new offer. |
| API-E2E-011 | Representative real provider continuation/rejection/history; no universal acceptance claim. |
| API-E2E-012 | Deterministic browser regression, production builds, lifecycle/atomicity/cleanup and final confidence gate. |

Post-repository confidence and broader-validation gate will be scored after repository checks. Broader validation is provisionally **Required** because the new backend catalog normalization/current descriptor crosses GraphQL and multiple browser consumers; historical browser/provider evidence predates SR-009. API-REV-002 result is not inferred from CRR-004 or API-REV-001.

## API-REV-002 repository evidence and post-repository confidence
- Backend focused: 8 suites/69 tests plus 4 suites/32 tests passed (`api-e2e-rev002-server-focused.log`, `server-extra.log`). One initially mistyped model-catalog-service path matched no suite; the correct path passed in the extra run. Web focused: 7 suites/75 tests passed (`api-e2e-rev002-web-focused.log`). Server build passed; built GraphQL E2E 2 files/3 tests passed after new descriptor introspection (`server-build.log`, `graphql.log`). Deterministic browser fixture updated to richer DTO/current-only behavior; six scenarios passed and cleaned up (`durable-browser.log`). These are current IR-003 checks, not inherited API-REV-001.
- Before real full-stack browser, the scorecard was: requirement proof 90, changed-boundary directness 90, integration realism 75, environment fidelity 90, failure/lifecycle 90, user-surface 75, durable coverage 90 = **85.7%** (600/7). Critical AC-010/011 not yet fully directly proven; broader validation **Required**. Selected isolated backend+Nuxt+Chromium, real Claude SDK/API/provider, saved-default Agent/Team/Org, definition→Run and Codex CWD comparison. This was a material gap, not a generic browser check.
- After browser discovered F-API-002, targeted `RunConfigPanel.spec.ts` rerun failed 18/30 with the same runtime error. This changed the coverage decision: the existing RunConfigPanel suite was **Needs Update/Failing due implementation source**, not a valid Pass. The new accepted regression basis must include Agent definition→Run render after the source fix. No API/E2E source workaround is appropriate; this is a preliminary **Local Fix (implementation)** for Code Reviewer failure-origin review.

## API-REV-002 live evidence and failure-origin recommendation
- Isolated live stack readiness at dynamic ports, public package import and test-owned records: `probes/rev002-live-stack-owner.log`, `rev002-seed-evidence.json`. Claude SDK provider snapshot had 10 offered IDs without redundant `default`, with sibling `opus`; exact-current descriptor resolved `default`; stopped Agent/Team/Org options exposed exact current `default` separately and 10 offered descriptors. Same-model settings Saves for all three succeeded; changed `opus`→`default` Save returned `MODEL_UNAVAILABLE` with no write (`rev002-live-api-evidence.json`, `rev002-default-reoffer-rejection.json`).
- Real Chromium stopped Agent, Team, Org Settings showed exact saved default but did not re-offer it, and explicit `opus` persisted. The first Team fixture had root settings edited to high before root model switch, making members divergent; their remaining default was correct. A separate fully linked Classroom Simulation Team fixture updated all three scopes to opus (`rev002-browser-evidence.json`, `rev002-team-browser-evidence.json`).
- Real Claude exact-default active turn and post-stop same-model Save/normal continuation succeeded: `REV002_DEFAULT_ALPHA` and `REV002_DEFAULT_BETA`, same session ID, unchanged trace hash at Save, prior history visible. Codex process-CWD and saved workspace both yielded the same seven `model/list` IDs and matched GraphQL snapshot/current descriptor; no actual CWD catalog mismatch was found (`rev002-codex-cwd-evidence.json`).
- **F-API-002, AC-011/BEH-007/MP-001:** public `createAgentDefinition` saved exact Claude `default`; in real Chromium `Agents` → its own **Run** button, route `/workspace` rendered HTTP 500: `Cannot read properties of undefined (reading 'seedModelIdentifier')`. Source trace at `autobyteus-web/components/workspace/config/RunConfigPanel.vue:35` references `agentRunConfigStore.seedModelIdentifier`, but script setup declares only `runConfigStore`. Independent `RunConfigPanel.spec.ts` rerun reproduced 18 failures / 12 passes at that line. Expected: seeded current-only `default` visible with schema and no redundant offered row, and Run panel operable. Observed: panel crashes before picker. Preliminary classification **Local Fix, implementation-owned**; Code Reviewer must confirm origin. No requirement/design ambiguity evidenced. Do not infer Application/mobile outcomes from this crash; they are not tested this round.
- Final confidence after the critical failure: requirement proof 50, directness 95, cross-boundary realism 95, environment fidelity 95, failure/edge 90, user surface 50, durable relevance 75 = **78.6%** (550/7). Two categories below 90 and a critical scenario fails; outcome **Fail** regardless of mean. Broader validation was Required and performed. Default 95 target not met. Cleanup: owned stack/runtime/SQLite removed; shared user data and five indexed local records untouched. Preserve all evidence for focused failure-origin review and rerun.

# API-REV-003 investigation — IR-004 / CRR-006 re-entry (2026-09-25)
- Trigger: CRR-006 independent source Pass on IR-004 commit `6ff59d9e4`, bounded correction of F-API-002/CRR-005 F-002. SR-006 requirements, SR-009 design and ARCH-REV-003 remain authoritative. API-REV-002 **Fail/78.6%** is the prior result, not presumed resolved. Classification stays **Medium/High**, reviewed route.
- Prior failure resolution plan, first: rerun the exact formerly failing `RunConfigPanel.spec.ts` command; then start isolated backend+Nuxt and click the test-owned saved Claude exact-`default` Agent card's Run button in real Chromium. Require no HTTP 500, exact current ID/schema visibly loaded, no newly offered duplicate `default`, no silent rewrite. Source review/component Pass alone is insufficient.
- After F-API-002 retirement, continue the API-REV-002 Not Tested Application Agent/Team Setup/readiness and mobile browser surfaces if the actual app can be safely seeded and reached; otherwise record precise limits, not invented proof. Rerun representative stopped Agent/Team/Org normalized snapshot/option/current descriptor and same-model Save, Codex CWD comparison, provider identity/history only proportionately because IR-004 changes one Web binding and earlier live backend/provider evidence is current SR-009 basis. Verify no new browser regression, builds/fixture probe, and cleanup.
- Existing durable `RunConfigPanel.spec.ts` is **Needs Rerun**, now augmented by IR-004's exact-default branch; two API-REV-002 durable changes remain **Still Valid**, no further edits planned unless execution shows a test defect. No test-only compatibility case or migration. The five indexed local exact-default records remain read-only.
- Planned re-entry cases reuse IDs API-E2E-007–012: 007 focused Web/component regression; 008 GraphQL contract check if backend restarted; 009 live catalog/Codex CWD spot check; 010 F-API-002 Agent Run first, then Team/Org and Application/mobile UI; 011 provider identity/history only if material; 012 builds/browser fixture/cleanup/confidence. The ledger continues in place. Provisional broader validation **Required** specifically for the formerly failing real browser boundary. Post-repository and final scorecards will be recorded after checks, with API-REV-002 baseline and failure resolution explicit.

## API-REV-003 execution decision and changed coverage validity (latest)
- **Repository re-entry:** `pnpm -C autobyteus-web test:nuxt components/workspace/config/__tests__/RunConfigPanel.spec.ts --run` passed **31/31**, including IR-004's exact-default Agent definition→Run regression (`api-e2e-rev003-run-panel.log`). Post-repository scorecard for the new one-line source correction: requirement proof 90, directness 90, integration realism 75, environment fidelity 90, failure/lifecycle 90, browser/user surface 85, durable quality 90 = **87.1%** (610/7). Broader validation remained **Required**, specifically because F-API-002 was a prior real-browser failure that component-only proof could not retire.
- **Direct browser resolution of F-API-002:** isolated backend+Nuxt, public `/Users/normy/autobyteus_org/autobyteus-agents` import, test-owned saved Claude exact-`default` Agent definition. Real Chromium Agents card→Run opened `/workspace` without 500. Pinia seed/config stayed exact `default`; current descriptor requested it, UI showed canonical Claude label and `reasoning_effort=high`, and picker showed ten normalized new choices without redundant `default`. Existing stopped Agent/Team/Org seed API options in `rev003-seed-evidence.json` also retained exact default separately from normalized replacements. The prior failure is **resolved**, not inferred from CRR-006.
- **New Application Setup failure, F-API-003:** A stock Brief Studio import could not be used for setup because it became quarantined after catalog transition and setup REST returned 503. To avoid treating that fixture limitation as feature proof, an isolated copied package with optional Agent/Team slots and no default resource was imported through the public API. It was RUNNABLE, and actual Chromium Application Setup rendered; test-owned shared Agent and Team definitions with saved exact Claude `default` each appeared in selected-resource baselines. Agent-slot browser Save persisted `VALID` exact-default effective configuration, with REST readiness `RUNNABLE`. A fresh normal page reopen then rendered **Error 500/DataCloneError** at `autobyteus-web/utils/application/applicationLaunchProfile.ts:156`: `structuredClone(view.savedOverride.executionResourceRef)` attempts to clone Vue's reactive Proxy. This browser failure blocks saved Application Setup restoration and prevents completing the Team Save branch. The pre-save and REST evidence must not be misreported as a browser restore Pass. Existing Application components/API tests are now **Insufficient for this saved-override renderer boundary**; add a durable saved-override/Proxy regression after implementation correction. No API/E2E production source workaround is permitted.
- `/mobile` was reached in Chromium but presented the normal pairing/authentication gate; an authenticated mobile new-run journey was not safely available from this isolated browser fixture. Do not claim mobile launch proof. Earlier API-REV-002 real Claude ALPHA/BETA continuation, Codex CWD equality, stopped Agent/Team/Org browser Saves, and built backend GraphQL checks are still relevant to the unchanged backend/SR-009 basis, but were **not rerun** in API-REV-003. No numerical smaller-window guarantee is claimed. No new durable test edit was made in this round; the two API-REV-002 durable edits remain pending test-code review after a successful validation round.
- Final scorecard after the newly discovered real UI failure: requirement/design preservation proof 75, changed-boundary directness 95, cross-boundary realism 95, environment fidelity 95, failure/lifecycle 90, user/browser surface 50, durable regression quality 75 = **82.1%** (575/7). Categories below 90: requirement proof, browser surface, durable quality. **Fail** even though F-API-002 is retired; F-API-003 is a concrete implementation-origin recommendation for Code Reviewer independent failure-origin assessment. Broader validation was Required and executed. The owned backend/Nuxt/runtime/SQLite and this round's generated Brief Studio/devkit/frontend SDK `dist` artifacts were removed; the five indexed local saved-default records were not mutated. Scope remains Directly Usable — No Migration, with no compatibility-only coverage.

# API-REV-004 investigation — IR-005 / CRR-008 re-entry (2026-09-25)
- Trigger: CRR-008 independent source Pass on IR-005 commit `2c4699a14`. API-REV-003 **Fail/82.1%** and F-API-003 remain open until executable rerun. SR-006/SR-009/ARCH-REV-003 and Medium/High reviewed route unchanged. IR-005 changes only `autobyteus-web/utils/application/applicationLaunchProfile.ts` and adjacent parent Setup test; it replaces Vue Proxy clone of saved bundle/shared resource refs with flat projections and unwraps reactive JSON llmConfig before clone. No backend/catalog/provider/GraphQL or Run-panel source changed. Read current CRR-008/IR-005, prior API-REV-003 report/revision/ledger, and repository instructions before execution.
- Prior failure first: rerun the exact focused Application parent test with real slot editor; then recreate an owned isolated backend+Nuxt/Chromium Application fixture, save a test-owned exact Claude `default` Agent selection through the UI, reopen the page in a **new browser page** and assert no Error 500/DataCloneError, selected saved resource, exact default/current descriptor/config, VALID and RUNNABLE REST state. Repeat Team selection Save→fresh reopen with a test-owned exact-default Team, including non-null JSON config. No component-only pass retires F-API-003.
- Coverage validity: IR-005's `ApplicationLaunchSetupPanel.spec.ts` is **Needs Rerun**, now includes reactive saved refs and config; existing Agent/Team editor tests remain **Still Valid**; the two API-REV-002 durable API/browser DTO changes remain **Still Valid** and unchanged here. A temporary full-stack package fixture is appropriate because Application capability/import/live browser setup and local provider credentials are environment dependent; no new API/E2E durable test edit before observing results. If direct browser fails, preserve evidence and route, not source-patch in this role.
- Follow-on after successful restore: proportionate real stopped Agent/Team/Org Settings and exact current option spot checks, Agent definition→Run remains previously resolved but can be smoke-tested; mobile browser route/pairing link if safe; production Web build and deterministic browser fixture once owned Nuxt stops; backend/provid­er continuation/capacity evidence from API-REV-002 remains current SR-009-basis but cannot be claimed as fresh IR-005 execution. No five indexed local saved-default records may be mutated. Lifecycle/cleanup and no-migration/legacy constraints remain.
- Provisional broader-validation decision **Required** for the exact real-browser saved Vue Proxy failure. Initial post-repository scorecard awaits focused test; no source-review Pass is an execution result.

## API-REV-004 execution evidence and final coverage decision
- Focused repository re-entry before live browser: `pnpm -C autobyteus-web test:nuxt components/applications/__tests__/ApplicationLaunchSetupPanel.spec.ts components/applications/setup/__tests__/ApplicationAgentLaunchProfileEditor.spec.ts components/applications/setup/__tests__/ApplicationTeamLaunchProfileEditor.spec.ts --run` **Pass 13/13**. The parent/slot tests now exercise reactive saved Agent exact default with non-null config and Team Save→fresh mount, but this alone did not retire the prior real browser F-API-003. Post-repository scorecard before broader validation: requirement proof 90, directness 90, integration realism 75, environment fidelity 90, failure/lifecycle 90, browser/user surface 85, durable quality 90 = **87.1%** (610/7), with broader validation **Required** specifically for saved Setup fresh reopen.
- Started new owned isolated built backend + Nuxt, imported `/Users/normy/autobyteus_org/autobyteus-agents`, created stopped Daily Assistant Agent / Classroom Simulation Team / Software Development Department Org runs via public GraphQL. Fresh stopped Agent/Team/Org option responses preserved exact saved `default` current descriptor and ten normalized replacements. Rebuilt Brief Studio sample package only to copy it under owned temporary runtime; made a test-owned application manifest with optional Agent/Team shared slots and no defaults so it was genuinely `RUNNABLE`, then imported through public GraphQL. No user package/source manifest or five indexed local exact-default records was edited.
- **Direct F-API-003 resolution:** real Chromium Application list→Setup selected test-owned saved-exact-default Agent definition. Browser PUT Save returned 200, REST readback `VALID`, effective `claude_agent_sdk/default`, `RUNNABLE`; new browser page opened same app and displayed the saved Agent selection, exact default baseline, no Error 500/DataCloneError. Team definition with exact default then followed the same browser PUT 200/REST VALID/default/RUNNABLE/**new-page saved restore** sequence. The UI sent exact-current GraphQL descriptor queries for `default`; zero page errors. `probes/rev004-application-browser-evidence.json` and saved/reopened screenshots are direct evidence. The reactive Proxy defect is retired on the actual prior failing boundary.
- **Mobile browser follow-on:** enabled Phone Access only in the isolated backend. Server policy forbids loopback pairing URLs, so Chromium's host-resolver mapped a test-only trusted `.local` alias to owned `127.0.0.1` without a LAN listener; backend pairing used the documented acknowledged private-HTTP API. Real browser paired and loaded authenticated mobile catalog. Agent definition and Team definition Runs setup each requested exact-current `default`, retained exact ID in the stores, and rendered the canonical Claude label/schema. No mobile run was created because no workspace was intentionally selected. This is browser current-value/setup evidence with hostname-emulation limitation, **not** real phone-network or provider-launch proof. Pairing devices were revoked and Phone Access disabled before the owned DB was removed; no token/pairing code persisted in evidence.
- After stopping owned Nuxt, `pnpm -C autobyteus-web build` **Pass**, deterministic repository browser E2E **6/6 Pass**, and built-backend GraphQL E2E **3/3 Pass**. `git diff --check` Pass. Backend/catalog/provider source is unchanged by IR-005, so API-REV-002's real Claude default ALPHA/BETA same-session continuation, Codex workspace-CWD equality, same-model Settings and provider-failure/history evidence remain valid current SR-009-basis evidence but are not falsely labeled fresh API-REV-004 execution. Successful mobile Create Run, real phone/network pairing, Antigravity continuation and numerically verified smaller-window provider pair remain untested bounded scope, not approved universal guarantees.
- Final confidence category scores: requirement/AC proof 95, changed-boundary directness 95, cross-boundary integration realism 95, environment/config/identity/fixture fidelity 95, failure/edge/lifecycle/recovery 90, user/browser/desktop-shell confidence 95, durable regression relevance 95 = **94.3%** (660/7). No category <90, critical supported AC/MP-002 direct evidence present, but default 95 clean target not met due bounded provider/mobile real-device uncertainty. The targeted executable browser/API/build surface has already been run and cannot close provider-independent numeric capacity without an externally verifiable pair; do not inflate to 95. **Pass with explicit bounded residual**, broader validation Required and executed. API/E2E made no new durable test edit this round; API-REV-002's two durable descriptor/fixture updates remain pending proportional test-code review on this reviewed route. No compatibility-only code or migration was introduced. Owned stack/runtime/SQLite/build outputs cleaned, pre-existing shared dist untouched.
