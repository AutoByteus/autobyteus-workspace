# Handoff Summary — Runtime-specific stopped-run model switching

## Final Authoritative Package
- Ticket: `runtime-specific-stopped-model-switch`; `task_size=Medium`, `architectural_risk=High`, **reviewed route**.
- Requirements: SR-006 approved REQ-001–008 / AC-001–011; design: SR-009; independent architecture ARCH-REV-003 Pass; implementation IR-005; source CRR-008 Pass; API/E2E API-REV-004 **Pass/94.3%**; proportional durable-test review CRR-009 Pass/no findings. API-REV-002/003 failures F-API-002/003 are closed by later direct browser evidence, not silently removed from history.
- Ticket worktree/branch: `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch`, `codex/runtime-specific-stopped-model-switch`. Reviewed candidate checkpoint `5f1cad274`; latest tracked `origin/personal` `69006cc79a1c02afab2fad19f955a34846c9907e` was merged at `3010bb35b` after the user accepted the DR-003 test package. Only `autobyteus-web/generated/graphql.ts` overlapped; the resolution retains both current-model and newly integrated memory query types. No runtime-model source behavior changed in the new base.
- Post-integration checks: server build Pass; built stopped-run GraphQL 3/3; affected Web 26/26; Web production build Pass; base memory component 5/5; deterministic existing-run browser 6 scenarios Pass. Exact logs: `delivery-post-integration-{server-build,focused-tests,web-build,memory-spot,browser}.log`. The integrated model-selection handoff did not materially change; renewed manual verification was not required. `git diff --check` on delivery edits Pass. Separate memory feature had its own finalized validation.
- Docs sync: `docs-sync-report.md` Pass, now describing backend Claude offered/current split and safe Application restore. Prior DR-002 package built before SR-006/SR-009/IR-005 is **superseded**.

## Fresh Local Electron Test Package
- **DMG:** `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/autobyteus-web/electron-dist/AutoByteus_runtime-specific-stopped-model-switch_DR003_macos-arm64-1.4.81-test.dmg`
- **ZIP:** `/Users/normy/autobyteus_org/autobyteus-worktrees/runtime-specific-stopped-model-switch/autobyteus-web/electron-dist/AutoByteus_runtime-specific-stopped-model-switch_DR003_macos-arm64-1.4.81-test.zip`
- README-guided integrated-backend personal macOS ARM64 build from the current reviewed candidate completed with exit 0. Bundle `com.autobyteus.app`, version `1.4.81`, ARM64; backend present. DMG checksum, ZIP integrity, and isolated **packaged Playwright Electron first-window/backend-readiness smoke** passed with owned process/data-root cleanup. Evidence: `electron-build-rev005-macos-arm64.log`, `electron-build-verification-rev005-macos-arm64.log`, `electron-packaged-smoke-rev005-macos-arm64.log`.
- This is **ad-hoc signed, not Developer ID signed/notarized**, and was a local unmerged DR-003 test build, not the published `v1.4.81` despite sharing its internal version. macOS Gatekeeper may warn. It is the exact package the user accepted; the later base merge added a separately finalized memory feature and no changed model-selection source behavior.

## Suggested User Verification
1. In stopped external Agent, Team and Org Settings, confirm distinct current backend-offered models are selectable without a platform capacity gate. For Claude, a proven redundant `default` should not appear as a new option; an existing run saved as exact `default` must remain visible/configurable without ID rewrite. AutoByteus retains its verified non-decreasing capacity rule.
2. Save/reopen an Agent or Team model/settings edit; confirm intended scopes only, retained history/provider identity, and clear invalid/stale/lifecycle errors. Provider continuation may reject an oversized prior conversation visibly rather than resetting it.
3. If available, exercise definition→Run and Application Agent/Team Launch Setup with an exact saved Claude `default`; Save, leave the page, reopen in a new page and confirm the selected resource/model/config and `VALID`/`RUNNABLE` state persist without a blank/500 page. The authenticated mobile browser setup was checked, but real-device/mobile Create Run remains untested.

## Validation Limits And User Acceptance
- API-REV-004 confidence **94.3%**, below its default 95% clean target, with no category below 90%. Current direct proof includes focused Application 13/13, built GraphQL 3/3, deterministic browser 6/6, Web production build, real Chromium Application Agent/Team Save→new-page restore and paired-mobile browser setup. Earlier provider/current-browser evidence remains indexed, not all freshly rerun in API-REV-004.
- No numerically verified smaller-window external provider pair, real-device/mobile Create Run, CI-durable credentialed full-stack/provider suite, or universal provider success is claimed. Persisted-data decision: **Directly Usable — No Migration**; no automatic history rewrite/reset.
- Explicit user completion verification/acceptance of the **current DR-003 package**: **Received 2026-09-26** — “i already tested, it works. lets fianlize and release a new version”. This authorizes finalization and a new version, not a claim of universal provider success. The post-acceptance base refresh/integration was completed and checked above; no model-selector behavior material to this ticket changed, so no renewed verification is needed.
- Ticket was archived at `tickets/done/runtime-specific-stopped-model-switch` before final ticket commit `3005d420a`, which was pushed. Target merge `8b84f0afd` and release commit/tag `21fea8c77` / `v1.4.82` were pushed to remote `personal`; all four tag workflows completed successfully. The public release contains 17 GitHub assets; iOS archive/upload to App Store Connect and versioned multi-arch Docker image publication succeeded. The separate later `v1.4.83` release advanced `personal` while our workflows ran and contains `v1.4.82`; this report does not certify that later rollout or call `v1.4.82` latest. The main `personal` worktree remains occupied/dirty and was not altered; the clean finalization worktree holds final delivery evidence. See `release-deployment-report.md` and `release-v1.4.82-verification.log`.

## Final delivery and cleanup
- **Delivery Completed (DR-005)** for `task_size=Medium`, `architectural_risk=High`, reviewed route. User verification, integrated checks, docs sync, ticket/target finalization, `v1.4.82` publication and workflow verification passed. No migration is required. Release URL: https://github.com/AutoByteus/autobyteus-workspace/releases/tag/v1.4.82.
- The remote ticket branch was deleted after target inclusion/no-open-PR confirmation. The local ticket worktree/branch remain intentionally because they hold the exact user-accepted test DMG/ZIP and pre-existing user/build state; force cleanup would destroy those artifacts. The clean delivery worktree remains the durable local source for the final archived ticket while the main checkout is dirty/behind. This is safe-cleanup **Not required**, not a blocker.
- Validation remains bounded at API-REV-004 **94.3%** (below 95% default clean target); no numeric smaller-window external pair, real-device/mobile Create Run, CI-durable credentialed full-stack/provider suite or universal provider success. `v1.4.82` release success does not alter these limits.
