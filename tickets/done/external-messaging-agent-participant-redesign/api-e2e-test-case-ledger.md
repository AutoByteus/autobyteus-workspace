# API/E2E Test-Case Ledger — Remove External Messaging From The Main Product

## Ledger Meta

- Assigned task workspace / worktree: `/Users/normy/autobyteus_org/autobyteus-worktrees/external-messaging-agent-participant-redesign` (HEAD `e9bbb28ab`)
- Coverage investigation: `.../api-e2e-coverage-investigation.md`
- Execution coverage report: `.../api-e2e-execution-coverage-report.md`
- API/E2E revision record: `.../api-e2e-revision-record.md`
- Ledger scope and reason it is required: 18 independently meaningful cases (R-01…R-09, L-01…L-08, plus G-01 for gate findings), with long-running suites, a baseline build, a Docker build and live runtimes. Context-compression risk is high.
- Evidence folder: `.../api-e2e-evidence/` (`logs/`, `scripts/`, fixtures)
- Execution safety rule: every command runs through `api-e2e-evidence/scripts/cleanenv.sh` (scrubbed env), because the agent shell inherits the user's real `AUTOBYTEUS_DATA_DIR`/`AUTOBYTEUS_MEMORY_DIR`/`DATABASE_URL`.
- Last updated: 2026-09-24. Round 2 (API-REV-002, `40f769e0d`), events #25–#26.

## Planned Cases

| Case ID | Case / Journey | Requirement / AC IDs | Boundary / Execution Surface | Planned Command Or Entry Point | Planned Order | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | Contract package tests | DS-004 | Repository | `pnpm --filter …contracts test` | 1 | |
| R-02 | autobyteus-ts build + message/public-surface/MCP tests | REQ-101, REQ-116 | Repository | vitest | 2 | |
| R-03 | Server changed unit + architecture tests | REQ-114, AC-102, AC-119 | Repository | vitest | 3 | |
| R-04 | Server MCP/skills unit + integration | REQ-116, AC-118 | Repository | vitest | 4 | |
| R-05 | Server full unit/architecture + integration (regression vs baseline) | REQ-120 (suites pass) | Repository | vitest | 5 | Long-running |
| R-06 | Server build + related e2e | AC-114 support, REQ-116 | Repository | vitest e2e | 6 | |
| R-07 | Web guards + specs + full test:nuxt | AC-116, DS-004 | Repository | vitest (nuxt) | 7 | Long-running |
| R-08 | Frozen install, lockfile, actionlint, script checks, workflow diff | AC-117 | Repository/static | pnpm, actionlint, shellcheck | 8 | |
| R-09 | REQ-120 gate (content + path), residue, AC-121 inspection | AC-120, AC-121 | Static | scripted git grep | 9 | |
| L-01 | Baseline-produced legacy data | AC-114/103/119 setup | Baseline server | owned baseline worktree | 10 | Long-running (build) |
| L-02 | Upgrade start on legacy data | AC-114, AC-103, QR-104 | Lifecycle | candidate server | 11 | |
| L-03 | Old API surface probes | AC-102 | Live API | curl + GraphQL | 12 | |
| L-04 | Fault injection + retry | QR-105 | Lifecycle | chmod + restarts | 13 | |
| L-05 | Historical binding run viewable | AC-119 | GraphQL + browser | | 14 | |
| L-06 | Settings desktop width + deep link | AC-116 | Browser | | 15 | |
| L-07 | Live stdio MCP with env token across runtimes | AC-118 | Live API + runtimes | | 16 | |
| L-08 | Docker all-in-one decision probe | AC-117 | Docker | | 17 | Long-running |

## Execution Events

| Sequence | Case ID | Timestamp | Event | Command / Entry Point / Material Configuration | Expected Observable Result | Observed Result Or Checkpoint | Result | Evidence / Artifact Path | Next Action / Unresolved Issue |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | G-01 | 2026-09-24 | Checkpoint | `git ls-files autobyteus-server-ts/external-channel` (discovery) | No tracked messaging residue in the main product | Tracked `autobyteus-server-ts/external-channel/gateway-callback-outbox.json` (empty outbox, committed in `76bd9107d`) is still in the main product. The package root is the default app-data dir (`AppConfig` dataDir = appRootDir), so the new cleanup migration would delete this tracked file on a default-data-dir start. | (pending R-09) | `git ls-files` output | Confirm in R-09; decide classification |
| 1 | L-01 | 2026-09-24 | Started | `git worktree add --detach …/autobyteus-worktrees/emr-api-e2e-baseline 40b1783f4`; background `pnpm install --frozen-lockfile && pnpm -C autobyteus-server-ts build` (scrubbed env) | Baseline build ready | Running in background (PID 69213) | — | `logs/L-01-baseline-install-build.log` | Check later |
| 2 | R-01 | 2026-09-24 | Completed | `pnpm --filter @autobyteus/agent-presentation-contracts --filter @autobyteus/team-stream-contracts test` | Contract tests pass; rebuilt tracked `dist/` unchanged | 1 + 2 tests pass; `git status` shows no tracked change after the rebuild | Pass | `logs/R-01-contracts.log` | — |
| 3 | R-02 | 2026-09-24 | Completed | `autobyteus-ts: pnpm build`; `vitest run tests/unit/agent/message tests/integration/public-surface tests/unit/tools/mcp tests/integration/tools/mcp tests/unit/agent/shutdown-steps/mcp-server-cleanup-step.test.ts` | Build OK; message/public-surface/MCP tests pass | Build OK (`verify:runtime-deps OK`). 21/25 files, 107 tests pass. 4 files fail: `integration/tools/mcp/{stdio-managed-mcp-server,tool-registrar,http-managed-mcp-server,websocket-managed-mcp-server}`. They spawn toy MCP servers from `<autobyteus-ts>/../autobyteus_mcps/…`, which is absent in every checkout of this superrepo (worktree and main). The http/wss toys don't exist anywhere, so `spawn uv ENOENT` (missing cwd). A temp sibling symlink didn't help because the tests resolve inside the repo root. `autobyteus-ts/src/tools` and these tests are unchanged in the diff. | Pass (4 environment-dependent files pre-existing, not change-related) | `logs/R-02-*.log` | Live stdio MCP proof moved to L-07 |
| 4 | L-01 | 2026-09-24 | Checkpoint | Baseline install + server build | Baseline `dist/` ready | Frozen install + `build:full` completed (bootstrap smoke passed) | — | `logs/L-01-baseline-install-build.log` | Proceed to baseline legacy-data production |
| 5 | R-03 | 2026-09-24 | Completed | `server: vitest run tests/unit/app-data-migrations tests/unit/remote-access …/local-memory-run-view-projection-provider.test.ts tests/unit/server-runtime-app-data-migration-gate.test.ts tests/unit/services/agent-streaming tests/unit/standalone-application-host …/standalone-agent-run-lifecycle-service.test.ts tests/unit/config tests/architecture tests/unit/startup` | Changed tests pass; no new failures | 73/77 files, 566 tests pass. The changed files all pass: cleanup migration 8/8, route-policy 8, projection 9, migration gate 7, streaming, host, lifecycle, architecture. 4 unchanged files fail (23 tests): `agent-org-history-candidate-safety`, `agent-org-token-attribution-transition`, `token-usage-run-records-v1-{app-data-migration,source-token-decoding}`. The baseline worktree fails the **identical** 23 tests (sorted FAIL-line diff empty). | Pass (pre-existing failures identical on baseline) | `logs/R-03-server-changed-unit.log`, `logs/R-03-baseline-recheck.log` | — |
| 6 | R-04 | 2026-09-24 | Completed | `server: vitest run` MCP/skills set (`unit/mcp-server-management`, `unit/mcp-gateway`, `unit/agent-tools/mcp`, claude/codex MCP materializers + session MCP builder, `unit/skills`, `integration/{mcp-server-management,mcp-gateway,agent-tools/mcp,skills}`, `team-agent-tools-mcp-lifecycle`, `codex-mcp-tool-args-projection`) | MCP/skills unchanged → pass | 25 files / 172 tests pass, 5 skipped. 1 failure in `codex-mcp-tool-args-projection` (expects a `reasoning` trace first) fails **identically** on baseline. | Pass (pre-existing failure identical on baseline) | `logs/R-04-server-mcp-skills.log`, `logs/R-04-baseline-recheck.log` | — |
| 7 | R-05 | 2026-09-24 | Started | Background: `server: vitest run tests/unit tests/architecture` then `vitest run tests/integration` (scrubbed env) | No failures beyond the baseline set | Running (PID 76505) | — | `logs/R-05-*.log` | Check later; recheck failures on baseline |
| 8 | R-09 | 2026-09-24 | Completed | `scripts/req120-gate.sh <worktree>` (content + path + residue) and the AC-121 static script | Content and path gates clean apart from the allowed set; AC-121 facts hold | **Content gate:** only the 4 registry lines (allowed set 4) → clean. **Path gate:** 1 hit, tracked `autobyteus-server-ts/external-channel/gateway-callback-outbox.json` (G-01). **Residue:** only `LIVE_E2E_GATEWAY_*` (keep), the WeChat prototype (keep), DV-1 `onAcceptedExternalUserMessage` (confirmed keep). **AC-121:** `autobyteus-ts/src/external-channel` absent; gateway has 12 src + 6 test files; 134 `external-channel/` imports, all relative and resolving; no `autobyteus-ts` in gateway src/tests/package.json/vitest config; no main-product reference to the gateway package. | AC-121 Pass; AC-120 content Pass; path-level G-01 open | `logs/R-09-req120-gate.log`, `logs/R-09-ac121-static.log` | G-01 decision after R-05/R-06 (observe whether any suite deletes the tracked file) |
| 9 | L-01 | 2026-09-24 | Completed | Baseline `40b1783f4` server (`node dist/app.js --host 127.0.0.1 --port 18731 --data-dir /private/tmp/emr-e2e/legacy-src`, scrubbed env). Setup through baseline's own GraphQL: `saveProviderApiKey(OPENAI)`, `createAgentDefinition(telegram-helper)`, `createAgentTeamDefinition(classroom-relay)`, `upsertExternalChannelBinding` ×2 (TELEGRAM AGENT + TEAM), `saveManagedMessagingGatewayProviderConfig` (fake token); 3 loopback `POST /rest/api/channel-ingress/v1/messages` → 202; graceful SIGTERM. Seeded rows in both orphan tables; seeded gateway `versions/`, `runtime-data/`, `config/gateway.env` (fake token), `download/messaging-gateway/*.tgz`, `logs/messaging-gateway/gateway.log`; non-messaging siblings `extensions/voice-input/`, `download/other-download.bin`. Derived variant run `telegram_helper_legacymeta…0001` = a copy of the real agent run with `metadata.externalSource` injected on its 2 user traces. | Real legacy state: binding runs, DB orphan tables, all four roots, token files | Baseline-created DB has `channel_message_receipts` + `channel_delivery_events` (1 row each seeded). Binding-started runs: agent `telegram_helper_ceadcfac…` (2 turns, real `gpt-5.4-mini` replies "2+2 equals 4.", "3+3 equals 6.") and team `classroom_relay_9995…` (1 turn, "Seven."). `bindings.json` records both run IDs. **Observation:** baseline's native recorder (and the Codex/Claude `recordForwardedUserMessage`) never persisted `externalSource` in raw traces; real binding history is ordinary `user`/`assistant` traces. Pristine copy at `/private/tmp/emr-e2e/legacy-pristine`. | Pass | `logs/L-01-baseline-server.log`, `fixture-legacy-baseline-40b1783f4/` | L-02 on a copy |
| 10 | R-05 | 2026-09-24 | Completed | Candidate: `vitest run tests/unit tests/architecture` (518 files) and `vitest run tests/integration` (85 files). Then the 21 + 18 failing files rerun on the baseline worktree; normalized FAIL-line sets diffed. | No failure beyond baseline | Unit/arch: 497 pass, 21 files / 56 tests fail. Integration: 49 pass, 18 skipped, 18 files / 46 tests fail. **None of the failing files is changed by the diff.** Baseline fails the **identical** sets (56 unit + 47 integration FAIL lines; `diff` empty). The worktree stays clean after the suites; the tracked G-01 file is untouched by the suites. | Pass (no regression; pre-existing failures identical) | `logs/R-05-server-full-{unit,integration}.log`, `logs/R-05-baseline-{unit,integration}-recheck.log`, `logs/R-05-failing-tests-{unit,integration}.txt` | — |
| 11 | L-02 | 2026-09-24 | Completed | Rebuilt candidate at `e9bbb28ab` (`pnpm -C autobyteus-server-ts build`, pass; no `managed-capabilities` in dist). `cp -a legacy-pristine upgrade-a`; `node dist/app.js --host 0.0.0.0 --port 18741 --data-dir upgrade-a` (scrubbed env) | Roots deleted, tables dropped, server starts; no run created/restored/messaged; no gateway process; no token left; no backup | Health 200 after 4 s. Prisma applied `20260924120000_remove_external_channel_tables`. Both tables absent (only diff in the table list). Record `SUCCEEDED`, attempts 1, "Scanned 4; migrated 4; skipped 0; failed 0." (log shows the 4 items MIGRATED). All four roots absent. `extensions/voice-input`, `download/other-download.bin` and `logs/server.log` kept. All 11 removed files lie under the four roots. `grep FAKE-E2E-TOKEN` finds none (QR-104). No `app-data-migration-backups`. Run indexes byte-identical; raw-trace line counts identical (5/5/3); `listWorkspaceRunHistory` shows the 3 binding-started runs with `isActive=false`. Server has no child processes; no gateway process on the machine; the log has no channel/binding/gateway activity. | Pass (AC-114, AC-103, QR-104, REQ-102/103) | `logs/L-02-candidate-upgrade-start.log`, `L-02-upgrade/*`, `logs/L-02-run-history-after-upgrade.json` | Server kept running for L-03/L-05/L-06 |
| 12 | L-03 | 2026-09-24 | Completed | curl probes: loopback `127.0.0.1:18741`; non-loopback via LAN IP `192.168.2.158` (peer address verified as non-loopback with a throwaway listener; the Docker Desktop container path showed as `127.0.0.1`, so it was treated as loopback evidence only). Same probes against baseline on a scratch dir for contrast. GraphQL introspection dump. | Old ingress/delivery POSTs → 404 on loopback; remote requests get the default protected treatment; no messaging GraphQL types/fields | **Candidate:** loopback POST `/rest/api/channel-ingress/v1/messages` and `/v1/delivery-events` → **404** (`Route … not found`), identical to a never-existing path. Non-loopback without credentials → 404 (same as the unknown-path control). Non-loopback with an invalid mobile credential → **401 `REMOTE_ACCESS_AUTH_INVALID`**, identical to protected `/graphql` and the unknown-path control; public `/rest/health` → 200. **Baseline contrast:** the same non-loopback POST without auth → **202 accepted** (UNBOUND), and with an invalid mobile credential → **202** (the `EXTERNAL_SIGNATURE` allow-list bypassed auth). **Introspection:** 270 types / 95 query / 88 mutation fields; 0 hits for externalChannel / managedMessaging / messaging / channelBinding / gateway / provider names (baseline had `externalChannel*`, `managedMessagingGateway*`, `upsertExternalChannelBinding`, …). | Pass (AC-102) | `L-03-api/http-probes.txt`, `L-03-api/remote-probes-candidate{,-lan}.txt`, `L-03-api/remote-probes-baseline-contrast.txt`, `L-03-api/graphql-introspection-candidate.json` | — |
| 13 | L-05 | 2026-09-24 | Checkpoint | GraphQL `getRunProjection` (agent run + legacy-metadata variant) and `getTeamMemberRunProjection` (team member) on the upgraded candidate | Runs open; messages are ordinary user/assistant | All 3 projections return without errors. Conversations are `[user, assistant, user, assistant]` (agent + variant) and `[user, assistant]` (team member), each item keyed only `content/kind/media/role/ts`. Content equals the real baseline turns. `externalSource`/peer metadata never surfaces in the projection (variant included). | (GraphQL part Pass) | `L-05-history/projection-*.json` | Browser part next |
| 14 | L-06 | 2026-09-24 | Completed | Nuxt dev (`nuxi dev --port 18751`, `BACKEND_*` → :18741, scrubbed env) + temporary Playwright probe `scripts/settings-desktop-probe.mjs` at a **1440×900** viewport (agent browser tab is fixed at 450 px, so Playwright-core 1.58.2 with cached Chromium was used for desktop width) | No Messaging nav entry; `/settings?section=messaging` lands on a valid section; no errors | `/settings`: 12 nav entries in the left column (x=16, w=224) at an even 48 px pitch (73…553), no gap: Back to Workspace, API Keys, Token Statistics, Display, Language, Local Tools, MCP Servers, Application Packages, Agent Packages, Server Settings, Extensions, Updates. `innerWidth` 1440; no "messaging" text anywhere; heading "API Key Management". `/settings?section=messaging`: identical nav, **API Keys highlighted**, "API Key Management" shown, 0 page errors. The narrow tab (450 px) also shows no Messaging text. | Pass (AC-116, REQ-118) | `L-06-settings/settings-desktop-1440.png`, `L-06-settings/settings-deeplink-messaging-desktop-1440.png`, `L-06-settings/probe-output.json`, `logs/L-06-nuxt-dev.log` | — |
| 15 | L-05 | 2026-09-24 | Completed | Browser: agent tab (450 px) on `/workspace` → Open runs/history → `ws-legacy` → Telegram Helper (2) / Classroom Relay (1) → click each run; Playwright `scripts/history-desktop-probe.mjs` at 1440×900 (same journey). First probe run had a false positive (case-insensitive `TELEGRAM` matched the agent name "Telegram Helper"); the regex was tightened to real metadata tokens and the probe rerun. | Historical binding-started runs list and open; messages render as ordinary user/assistant | All 3 runs are listed under workspace `ws-legacy` and open, showing "Offline" (not restored). Agent run: You "Hello from Telegram. What is 2+2?…" → Telegram Helper "2+2 equals 4." → You "Thanks. And what is 3+3?" → "3+3 equals 6.". The variant (with injected `metadata.externalSource`) renders identically. Team run: You "Professor, name one prime number…" → professor "Seven.". No metadata tokens (`externalSource`, peer IDs, `BUSINESS_API`, chat title, message IDs) visible. 0 page errors. | Pass (AC-119, REQ-119) | `L-05-history/*-desktop-1440.png`, `L-05-history/*-narrow-tab.png`, `L-05-history/browser-probe-output.json`, `L-05-history/projection-*.json` | — |
| 16 | L-04 | 2026-09-24 | Completed | `cp -a legacy-pristine upgrade-fault`. Fault: `chflags uchg download/messaging-gateway/autobyteus-message-gateway-1.4.0.tgz` (verified `rm` → EPERM). `scripts/start-stop.sh` (candidate on :18761, health wait, record query, graceful stop) run 4× — #1 and #2 with the fault, then `chflags nouchg`, then #3, then #4. | FAILED record, startup continues; later start retries; success recorded once; no re-run after success | **#1:** health 200 in 4 s; record `FAILED`, attempts 1, "migrated 3; failed 1", error "1 external messaging data root could not be removed; the cleanup retries on the next start."; `[WARN] Studio app-data migration '20260924_remove_external_messaging_data' is FAILED…`; server listening; item detail `EPERM … unlink …tgz`; the other 3 roots removed. **#2 (fault kept):** health 200; `FAILED`, attempts 2, "skipped 3; failed 1". **#3 (fault removed):** health 200; `SUCCEEDED`, attempts 3, "migrated 1; skipped 3; failed 0"; all four roots absent. **#4:** `SUCCEEDED`, attempts still 3, no new migration log (3 logs total) → not re-run. Siblings kept throughout. | Pass (QR-105, AC-114 alternate) | `L-04-fault/start{1,2,3,4}.txt`, `L-04-fault/*.log`, `logs/L-04-start*.log` | — |
| 17 | L-07 | 2026-09-24 | Completed | Candidate :18741 (upgraded data dir). Temp stdio MCP server `scripts/token-echo-mcp-server.mjs` (workspace `@modelcontextprotocol/sdk`). Tool `verify_e2e_token(nonce)` returns `TOKEN_PRESENT/TOKEN_SHA8/NONCE` and logs each invocation. Steps: GraphQL `configureMcpServer` (STDIO, `env: {E2E_MCP_TOKEN: fake}`) → `discoverAndRegisterMcpServerTools` → `createAgentDefinition(toolNames:[verify_e2e_token])` → per runtime `createAgentRun` + `/ws/agent/<runId>` `SEND_MESSAGE` (`scripts/mcp-runtime-probe.mjs`). Models: native `gpt-5.4-mini` (OpenAI key from the upgraded vault), Codex `gpt-5.5` (logged-in CLI), Claude `haiku` (logged-in CLI). | Configure, discover and assign work; each runtime invokes the MCP tool, the env token reaches the server process, and the assistant returns the result | Configure saved the STDIO config with env. Discover: `success`, "registered 1 tools", `verify_e2e_token` (origin MCP). Agent definition shows `toolNames:[verify_e2e_token]`. **autobyteus:** TOOL_EXECUTION_STARTED/SUCCEEDED, result `TOKEN_PRESENT=true TOKEN_SHA8=abf32337 NONCE=N-autobyteus-86d4205a`, assistant echoed it, idle, 0 errors. **codex_app_server:** `mcpToolCall` completed with the same pattern for `N-codex_app_server-57e0a524`, 0 errors. **claude_agent_sdk:** `toolu_…` STARTED/SUCCEEDED for `N-claude_agent_sdk-779e30d5`, 0 errors. `invocations.log` shows 3 invocations; all MCP processes have ppid 97593 (the candidate server) and hash `abf32337` = sha256(fake token)[:8]. | Pass (AC-118, REQ-116) | `L-07-mcp/{configure,discover,agent-definition}.json`, `L-07-mcp/run-{autobyteus,codex,claude}.json`, `L-07-mcp/invocations.log` | — |
| 18 | R-06 | 2026-09-24 | Completed | Candidate server rebuilt (`build:full` pass). `vitest run tests/e2e` (75 files; live `RUN_*_E2E` suites skipped by default). Failing files rerun on baseline; FAIL-line sets diffed. The one extra candidate failure was then rerun in isolation, and as the file pair (provider-semantics + analytics) on both worktrees. | No failure beyond baseline; changed e2e (`skill-access-mode-graphql`, `custom-provider-readable-id-startup-migration`, runtime files) pass or skip as on baseline | 46 files pass, 15 skipped, 14 files / 40 tests fail. None of the failing files are changed, except `memory-sync-multiprocess`, which fails identically on baseline ("Team V2 cannot contain configured Team '/nested'"). Baseline fails 38 of the 40 identical lines. The 2 extra lines are `token-usage-analytics-graphql` (`TOKEN_USAGE_SAFE_INTEGER_EXCEEDED`). That file passes 5/5 in isolation on the candidate, and **baseline reproduces the same failure** when `token-usage-ledger-provider-semantics` (seeds `MAX_SAFE_INTEGER` rows) runs first. So it is a pre-existing order-dependent shared-test-DB leak exposed by the changed file order; token-usage src/tests are unchanged. | Pass (no regression; pre-existing failures, including one order-dependent) | `logs/R-06-server-e2e.log`, `logs/R-06-baseline-e2e-recheck.log`, `logs/R-06-candidate-token-usage-isolated.log`, `logs/R-06-pair-{baseline,candidate}.log`, `logs/R-06-failing-tests-e2e.txt` | — |
| 19 | R-07 | 2026-09-24 | Started | Web guards run; full `NUXT_TEST=true vitest run` + electron vitest in background (Nuxt dev stopped first) | Guards pass; no failures beyond baseline | Guards: `guard:web-boundary` Passed, `guard:localization-boundary` Passed, `audit:localization-literals` exit 0. Full suites running. | — | `logs/R-07-web-*.log` | Check later |
| 20 | R-08 | 2026-09-24 | Completed | (1) Fresh detached worktree `…/emr-api-e2e-frozen-install` at `e9bbb28ab`: `CI=true pnpm install --frozen-lockfile`. (2) `actionlint` on all workflows, baseline vs candidate. (3) `bash -n` + `shellcheck` baseline vs candidate on `desktop-release.sh`, `android-bootstrap-termux.sh`, `personal-docker.sh`. (4) Workflow/script diff review. (5) **Local release dry run:** isolated `git clone --local` (remote removed), branch reset to `e9bbb28ab`, `scripts/desktop-release.sh release 9.9.9 --release-notes <tmp> --no-push`. | Install works without the gateway; no new lint findings; gateway steps removed surgically; the release path runs without gateway steps | (1) Frozen install exit 0 in 12.9 s. The gateway gets no `node_modules`; 0 `message-gateway`/`whiskeysockets` entries in the lockfile, 0 `baileys` in the store (baseline lockfile had 1 gateway importer). 4 "Failed to create bin" warnings, identical on baseline. (2) actionlint: candidate 0 findings; baseline's only finding was in the deleted `release-messaging-gateway.yml`. (3) All `bash -n` OK; shellcheck 0/0, no new findings. (4) The desktop/android "Validate workspace package versions" steps keep the desktop check and lose only the `GATEWAY_VERSION` lines and the manifest-validation step (R-2). Termux filter and release-script `git add` list lose only gateway items. (5) Dry run exit 0: "Release prepared locally (not pushed)"; the commit changes only `.github/release-notes/release-notes.md` + `autobyteus-web/package.json`, 0 gateway/messaging files (base v1.4.76 commit also bumped the gateway package + release-manifest). Tag exists only in the isolated clone; the shared repo has no `v9.9.9`. `release:test` (GitHub dispatch) not run: needs a pushed ref. | Pass (AC-117 workspace/release portion) | `logs/R-08-frozen-install-clean-worktree.log`, `logs/R-08-ci-static.log`, `logs/R-08-release-dryrun.log` | Docker portion → L-08 |
| 21 | R-07 | 2026-09-24 | Completed | `NUXT_TEST=true vitest run` (497 files) + `vitest --config ./electron/vitest.config.ts run`; the 5 failing files rerun on baseline web (after `nuxi prepare`); FAIL lines and failure details compared | Guards pass; no failure beyond baseline; changed specs (`settings.spec.ts`, streaming specs, `memberInputMessageHandler`, zh-CN glossary, font-size audit) pass or fail identically | Nuxt: 490 pass, 2 skipped, 5 files / 5 tests fail. Electron: 33 pass, 2 skipped (151 tests). The failing set is **identical on baseline** (6 FAIL lines). This includes the edited `app-font-size-fixed-px-audit` case, where both sides list the **same 14 token-usage offenders**; the new `ProviderApiKeyEditor.vue` perimeter adds none. The guards (from sequence 19) pass. | Pass (no regression) | `logs/R-07-web-test-{nuxt,electron}.log`, `logs/R-07-baseline-web-recheck.log`, `logs/R-07-web-guard*.log` | — |
| 22 | G-01 | 2026-09-24 | Completed | Probe in the assigned worktree: temporary gitignored `autobyteus-server-ts/.env`; candidate `node dist/app.js --host 127.0.0.1 --port 18771` with **no `--data-dir`** (default app-data dir = server package root, per `AppConfig`: `dataDir = appRootDir`); record query; `git status`; then restore (`git checkout -- …outbox.json`; `.env`, probe DB/logs/backup/empty `llm/` and the R-06 `workspaces.json` by-product removed) | The main product tracks no messaging residue, and the new cleanup deletes no tracked repository file | "APP DATA DIRECTORY: …/autobyteus-server-ts". Record `SUCCEEDED`, "migrated 1; skipped 3". `git status` → ` D autobyteus-server-ts/external-channel/gateway-callback-outbox.json`. The tracked file (empty outbox JSON of the removed gateway-callback feature, committed accidentally in `76bd9107d` on 2026-04-13) is the only path-level REQ-120 hit (R-09 path gate). Every server start from the package root as data dir (the default without `--data-dir`; that dir's `.gitignore` already treats `/db/`, `/logs/`, `/download/`, `memory`, `/workspaces.json` as runtime data) deletes a tracked repository file and dirties the tree. Restored; tracked tree clean again. | **Fail** — preliminary `Local Fix` (implementation: `git rm autobyteus-server-ts/external-channel/gateway-callback-outbox.json`; no source change) | `G-01/default-data-dir-probe.txt`, `logs/G-01-default-data-dir-start.log`, `logs/R-09-req120-gate.log` [2] | Route per handoff rules |
| 23 | L-08 | 2026-09-24 | Checkpoint | Docker (`desktop-linux` docker-driver builder, context = clean `e9bbb28ab` worktree). The `# syntax=docker/dockerfile:1` line was removed in a temp copy because Docker Hub resolution hung (frontend image already local); semantics unchanged. (A) candidate `Dockerfile.allinone` as-is. (B) temp-patched copy adding only the missing `agent-presentation-contracts` + `collaboration-stream-contracts` COPY/filter/build/runtime-copy lines (`logs/L-08-temp-patch.diff`). | (A) fails at the known pre-existing step; (B) builds, and the container runs server + web without any gateway | (A) Failed at `[builder 18/21] RUN pnpm -C autobyteus-team-stream-contracts build` with `TS2307: Cannot find module '@autobyteus/agent-presentation-contracts'`, the step reported identical on baseline. (B) running. | (A) as expected | `logs/L-08-docker-asis.log` | Wait for (B); then baseline as-is confirmation |
| 24 | L-08 | 2026-09-24 | Completed | (B) temp-patched build → image `emr-api-e2e-allinone:candidate-temp-patched`; `docker run -p 127.0.0.1:18781:8000 -p 127.0.0.1:18782:3000`; `supervisorctl status`, file/process/port checks, host HTTP checks; container removed. (C) baseline `40b1783f4` `Dockerfile.allinone` as-is (same syntax-line removal) for independent confirmation. | Gateway-free packaging builds and runs once the pre-existing gap is patched; the as-is failure is identical on baseline | (B) All 24 builder/runtime steps pass (contracts, Prisma, server `build`, web `build:mobile-web`). Container: `autobyteus_server` RUNNING, `autobyteus_web` RUNNING. Supervisor programs = base-image set + those two; **no `message_gateway` program**. `/app/autobyteus-message-gateway`, `/usr/local/bin/allinone-start-gateway.sh` and `/app/memory` absent; no gateway/messaging process; listening only on 8000 and 3000 (no 8010). Host: health `ok`, web 200, old ingress POST 404. The cleanup migration ran on a fresh data dir. `chrome FATAL` comes from base-image `base.conf` (zygote sandbox "Operation not permitted" without extra container privileges) and is unrelated to the change. (C) Baseline fails at the **identical** step: `[builder 20/24] RUN pnpm -C autobyteus-team-stream-contracts build` → `TS2307 … '@autobyteus/agent-presentation-contracts'`. | Pass (AC-117 Docker portion, with the pre-existing gap recorded as out of scope) | `logs/L-08-docker-{asis,temp-patched,baseline-asis}.log`, `logs/L-08-temp-patch.diff`, `logs/L-08-container-probe.txt` | Pre-existing Dockerfile gap → separate ticket candidate |
| 25 | R-09 | 2026-09-24 | Completed | **Round 2 (API-REV-002)** on `40f769e0d` (IR-003 / CRR-004). Delta verified first: `git diff --name-status e9bbb28ab HEAD` = only `D autobyteus-server-ts/external-channel/gateway-callback-outbox.json`; added-file set = the 6 designed additions; 440 files +579/−32279. Then `scripts/req120-gate.sh <worktree>` plus a broader path scan (`messaging\|telegram\|discord\|whatsapp\|wecom\|wechat\|outbox\|external-channel\|channel-ingress`) | Content gate = only the allowed registry lines; path gate empty | Content gate: only `app-data-migration-registry.ts` L68–71 (allowed set 4). **Path gate [2]: empty.** Broader scan: only the allowed-set cleanup migration file + its unit test. Residue [3]–[5] unchanged known keeps (`LIVE_E2E_GATEWAY_*`, WeChat prototype, DV-1). | Pass (AC-120 content + path) | `logs/R-09-req120-gate-round2.log` | — |
| 26 | G-01 | 2026-09-24 | Completed | **Round 2 recheck** in the assigned worktree: temporary gitignored `autobyteus-server-ts/.env`; candidate `node dist/app.js --host 127.0.0.1 --port 18771`, **no `--data-dir`** (dist equals `40f769e0d`: `src`/`prisma` unchanged since `e9bbb28ab`); record + migration log; `git status`; graceful stop; probe artifacts removed against a pre-probe snapshot | No tracked deletion; binding root SKIPPED | "APP DATA DIRECTORY: …/autobyteus-server-ts"; health 200. Record `SUCCEEDED`, "Scanned 4; migrated 0; skipped 4; failed 0"; `bindingDataDir …/autobyteus-server-ts/external-channel` → `SKIPPED` "Not present." (all 4 roots SKIPPED). `git status` shows **no tracked change**. The package root, `db/`, `logs/` and `download/` match the pre-probe snapshot after cleanup. | **Pass** (prior failure G-01 resolved) | `G-01/default-data-dir-probe-round2.txt`, `logs/G-01-round2-default-data-dir-start.log` | — |

## Re-entry And Reconciliation

- Last durably recorded event: 26 (G-01 round-2 recheck Completed)
- Last completed case and result: G-01 — Pass (round 2, `40f769e0d`)
- Cases still running, interrupted, or not started: none
- Next case or recovery action: none. Round 2 complete.
- Interruption, context-compression, or rerun note:
  - Round 1 (events #0–#24, `e9bbb28ab`): all background runs (L-01 build, R-05, R-07, Docker builds) completed and were recorded.
  - Round 2 (events #25–#26, `40f769e0d`): rechecked only the prior failure G-01 and the R-09 gates, as agreed with `/code_reviewer` (CRR-004). The delta is a single deleted data file, so all other round-1 evidence remains valid.
- Reconciled into execution coverage report: `Yes`. `api-e2e-execution-coverage-report.md` → "Test-Case Ledger Reconciliation"
- Reconciliation note for any case missing a terminal result: none. Round 1: G-01 = Fail, all others Pass. Round 2: G-01 = Pass, R-09 = Pass.
- Cleanup: all owned servers, the Nuxt dev server, the browser tab, the Docker container/image, temp worktrees, the isolated clone, `/private/tmp/emr-e2e` and the probe artifacts in the assigned worktree were removed. The tracked file was restored. `git status` equals the pre-validation state. The round-2 probe artifacts were removed against a pre-probe snapshot, `/private/tmp/emr-e2e` was removed again, and the tree is clean at `40f769e0d`.
