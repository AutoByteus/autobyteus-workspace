# Docs Sync: Docker Guide command hotfix

Status: Pass

Updated:

- `autobyteus-web/docs/settings.md`
- `autobyteus-server-ts/README.md`
- `autobyteus-server-ts/docker/README.md`

Docs now describe `new-container`, `upgrade --all`, `destroy --all`, and `reset` instead of the removed `start`, `start --new`, and `update` commands.
