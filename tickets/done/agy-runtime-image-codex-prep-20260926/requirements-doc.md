# Requirements Document

## Document Status
- Status: **Approved — SR-024 server-only scope correction (DEC-007)**. The user explicitly excluded changing the separate `autobyteus-agents` project and directed current-project finalization. SR-021 image scope, exact-eight native names, and SR-013 missing/invalid-skill handling remain approved. Portable Codex skill packaging is withdrawn from this ticket.
- Current solution revision ID: SR-024 (user-approved cross-project de-scope; SR-023 remains the reviewed server design basis)
- Package identifier: agy-runtime-image-codex-prep-20260926
- Request / ticket: User request of 2026-09-26, CLI transcript and four attached screenshots
- Requirements owner: Solution Designer
- Date: 2026-09-26
- Approval state and reference: SR-005 native-tool baseline was approved by user on 2026-09-26 in reply to the explicit scope question (“Okay … we shouldn't block their own tools …”). The user's later instruction that a nonexistent skill should warn in the backend rather than block AGY startup prompted SR-009. The user explicitly approved the full revised behavior in reply “yesss” to the question spelling out warn-and-start, omit only genuinely missing skills, keep other skills, preserve unsafe-source/unrelated failures, and still make Codex's workflow skill portable. The user then replied “approve” to the explicit SR-013 question defining Codex-parity validation/warn-and-skip for missing or invalid content and separate safety failures (E-034). **SR-016 hook proposal was withdrawn; the user approved exact eight-name DEC-005 in E-048/SR-018.**
- Exact approved requirements baseline: SR-009 content, approved in SR-010 by user's 2026-09-26 “yesss” reply, remains historical authority for all unchanged behavior. After learning AutoByteus does validate configured `SKILL.md` for Codex App Server, the user directed AGY to follow the same validation approach (E-033). This changes BEH-002/REQ-004/AC-004/QR-001 from SR-012’s fatal invalid-candidate rule; the reconciled SR-013 requirements are **approved** in E-034.
- Behavior-defining supplements: N/A — no separate product supplement

## Approved SR-024 Server-Only Scope Correction — DEC-007
The user said the separate agent-package project was not in this ticket's scope, explicitly directed deletion of PR #14, and instructed finalization of the current project only (E-057). **This supersedes the portable Codex workflow-skill packaging portion of prior REQ-003/AC-003/DEC-002 and SR-023's package-copy design; it does not undo the server's AGY skill warning/omission behavior.** `autobyteus-agents` `main` remains unchanged. No package-local skill copy, broken-link repair, README edit, package PR/merge, or promise that the intended workflow skill is present belongs to this ticket. When the configured skill is absent, AutoByteus warns/omits it and otherwise healthy AGY Codex starts and can answer; it must not claim the skipped skill was loaded. If a valid skill is independently configured, existing materialization still applies. The source skill or package layout can be addressed separately only on a new request. Historical tests that selected the unmerged local package `a140474` prove that configuration, **not** ambient `autobyteus-agents/main`; they do not establish a server-only bundled-skill outcome. Real AGY 1.2.11 missing/invalid-skill first-turn evidence in API-REV-002 remains applicable to the reduced server contract. No new version/release is authorized.

## Approved SR-021 Scope Change — DEC-006
The user clarified in E-054 that the outcome sought is **AGY-native invocation**, not AutoByteus ownership of AGY's generated image bytes or storage location: “what we only make sure ... the tool can be called ... that's it.” This materially narrows the earlier REQ-002/AC-001 output promise. The user explicitly approved removal of the overscoped design in E-055; the following is the authoritative replacement:

- `BEH-003` / `REQ-002`: AutoByteus exposes the approved AGY-native `generate_image` and truthfully represents its native tool-call lifecycle. AGY owns image generation and storage. AutoByteus does **not** search AGY brain transcripts, verify/copy generated bytes into run memory, create an app-owned image path/Files entry, or promise an image preview/content endpoint. An ordinary AGY assistant reply may mention an AGY path, but AutoByteus does not parse that prose into an artifact contract.
- `AC-001` / `SCN-001`: In a real AutoByteus-launched AGY run, an image request leads to provider-provenance `tool_name=generate_image` (not `call_mcp_tool`) with the native tool lifecycle visible in the normal chat trace; provider completion is represented accurately. A provider-native image file may remain in AGY's own storage. Passing does **not** require AutoByteus to discover, copy, render, index, serve, or retain that file.
- `AC-002`: A native provider error or denial is shown as a safe failure, not a fabricated success. A native `DONE` step without an output path is **not** treated as an image failure solely because AutoByteus did not receive a path. Keep public/private redaction and the approved exact-eight/MCP/collaboration/skill policies unchanged.
- Remove the SR-019 transcript reconciliation, run-owned copy, `FILE_CHANGE`/Files/reopen and special finalization/input-gate promises from this ticket's target. Preserve ordinary provider result/turn handling and existing app-wide input semantics. IR-004's shared-admission/publication design impact is then not a reason to add shared contracts for this reduced scope; any independently reproducible generic bug would require its own ticket.

**Approval:** In E-055 the user explicitly said AGY knows where it stores the generated image, AutoByteus only needs to make the native tool available and callable, and directed removal of the overscoped image design. Thus no app image preview, path, copy, Files entry or content route is required; “display” refers to the normal agent/tool-call conversation, not a new artifact UI. This supersedes the earlier accessible-path commitment while retaining safe truthful tool-state presentation.

## Problem And Desired Outcome
The same user's standalone Antigravity CLI can invoke native GenerateImage, but an AutoByteus Software Development Department Solution Designer running through Antigravity CLI denies image capability. A separate AutoByteus Codex agent using that runtime fails during preparation with only a generic error. The user's repeated clarification makes the image target unambiguous: the tool supplied by AGY itself must be usable in AutoByteus-launched AGY runs. AutoByteus's MCP `generate_image` is not a substitute and its availability must not count as meeting native-image acceptance criteria. The desired outcome is native runtime capability parity within the approved exposure scope, with AGY-native invocation visible in normal app conversation while AGY owns the output. The standalone Codex agent must not be blocked by a missing configured skill: the server warns and omits it, then an otherwise healthy AGY run can answer. This ticket does not make the workflow skill available through a package change. User initially requested broad AGY-native default capabilities plus separately configured MCP, excluding AGY-native subagent/messaging. After live rejection of the 49-name profile, the user explicitly approved the tested exact eight-name task scope in E-048; optional additional native tools require a later decision. The CLI custom-agent contract documents an allowlist, not a subtractive denylist. Live AGY 1.2.11 validation now **refutes** SR-015's proposed 49-name allowlist: 25 names are not accepted as custom-agent components, and both first-turn scenarios fail. Future CLI tool inheritance and exact compatible exposure must be established rather than inferred from `init.tools`. Skill warning/skip policy remains in scope; skill packaging in the separate agent-package repository is excluded by SR-024.

**SR-013 approved change:** The user initially preferred raw copy-through (E-030), then explicitly made the AGY policy conditional on the actual Codex App Server behavior (E-033). Static source shows AutoByteus already parses configured Codex `SKILL.md` metadata and checks declared name before exposing a skill (E-032). The approved AGY rule is therefore the same content judgment: validate the configured skill using shared AutoByteus skill parsing/name rules; if the source is missing or its manifest is malformed, unreadable, or name-mismatched, emit a sanitized backend warning, omit that skill, and continue an otherwise healthy run with other valid skills. Do not copy invalid content through or make malformed content an AGY preparation hard failure. Keep filesystem containment and source/destination collision failures separate from content invalidity; they remain blocking safety failures, as in the approved SR-010 boundary and Codex materializer. If AGY rejects content that passes AutoByteus validation, report provider failure safely rather than promise startup. The existing Codex global-invalid edge may still throw (E-032); that is not a product behavior to deliberately replicate. The user approved this intended rule in E-034. Live symlink updates remain out of scope.

## Relevant Current And Desired Behavior
| Behavior ID | Kind | Related Scenario IDs | Evidence-Backed Current Behavior | Desired Behavior | Intentionally Preserved Behavior | Investigation Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| BEH-001 | User | SCN-001 | `origin/personal` hard-codes eight coding names without native image; unfinished task branch expanded to 49 names, 25 of which AGY 1.2.11 rejects on first turn. | New AutoByteus AGY runs expose the exact approved eight native names in REQ-001, including AGY-native `generate_image`, plus separately configured MCP; AGY-native subagent/messaging names remain excluded. | Run identity/MCP grants remain scoped; app-owned media stays distinct; existing capsules remain snapshots. | E-001–E-003, E-036, E-041–E-048 |
| BEH-002 | User | SCN-002, SCN-003 | Standalone Codex agent config references software-engineering-workflow-skill, which is absent from configured discovery roots; AGY capsule rejects unresolved binding; UI shows only generic preparation failure. | **SR-013/SR-024 approved:** AutoByteus validates configured AGY skills using the same `SKILL.md` metadata/name rules used for Codex. Missing, malformed, unreadable or name-mismatched skill content yields a sanitized backend warning and is omitted, without blocking an otherwise healthy AGY run. No package-bundling promise remains. | Skipped skills are not falsely reported as loaded; other usable skills remain available; unsafe source paths are not copied; source/destination safety failures and unrelated base-runtime failures remain blocking. | E-004–E-006, E-022/023, E-028–E-033 |
| BEH-003 | System | SCN-001 | AGY owns generated image storage; its native `generate_image` stream reports ACTIVE/DONE without a result path. Task-branch code incorrectly treats absent app-visible image path as failure and adds transcript lookup/copy. | AutoByteus presents the genuine native tool lifecycle and ordinary AGY reply accurately; AGY retains its own image storage. No app-owned image artifact, path, Files entry or preview is required. | App-owned media remains independent; safe native error/denial redaction remains. | E-001, E-037/038, E-045, E-054/055 |

## Stakeholders, Actors, And Outcomes
| Actor | Goal / Responsibility | Required Outcome | Constraint |
| --- | --- | --- | --- |
| User | Ask Solution Designer for native image generation and run standalone Codex | See AGY call its native image tool and receive its ordinary reply or a safe tool failure | AutoByteus does not promise to locate or serve AGY-owned image bytes |
| Solution Designer agent | Use only configured capabilities truthfully | Invoke image tool when granted; avoid false denial or false claim | Team-scoped tool grants |
| Codex agent | Start even when a configured workflow skill is absent | AutoByteus warns/omits an absent or semantically invalid skill, then an otherwise healthy AGY run can answer; a separately available valid skill may load | No claim that this ticket bundles or loads the absent skill |
| App operator | Maintain AGY availability and configured skills | Proposed: backend warning identifies absent/invalid run and skill; unsafe-source and AGY-origin failures remain distinguishable | No credential leakage |

## Scope Guardrail
### In-Scope Use Cases
- UC-001: A user uses an AGY-backed AutoByteus agent with the approved exact eight AGY-native names, including `generate_image`, plus that agent’s separately configured MCP tools.
- UC-002: A user starts the standalone Codex agent with the AGY runtime and gets a first response.
- UC-003: A configured skill is absent or semantically invalid, a source/destination is unsafe, AGY rejects otherwise validated skill content, or native AGY image capability is unavailable; approved skill policy warns/skips absent/invalid content while safety, provider-origin and native-image failures remain accurately and safely reported.

### Out Of Scope
- Adding non-default AGY tools, unrelated providers, or unconfigured MCP grants.
- Replacing or modifying AutoByteus's separate app-owned media provider, or assuming CLI credentials apply to it.
- Broad redesign of run creation, team routing, all skill discovery, the media catalog, or historic runs.
- Retrofitting already-created AGY capsules and tool sessions without starting a new run.

### Non-Goals
- A new image-generation UI workflow, image preview, AutoByteus image artifact/Files entry, or Product prototype.
- Changing AGY text model selection or auto-approve semantics.
- Changing output-directory policy or introducing a data migration.

### Preserved Behavior Boundary
Preserve BEH-001 to BEH-003 per-run identity and capsule isolation, app-owned MCP grants, and credential-safe errors. AGY-native subagent and messaging tools are excluded; AutoByteus team communication/delegation continues through configured MCP tools. **SR-013 approved:** AutoByteus semantically validates configured AGY `SKILL.md` using the same shared metadata/name rules as Codex. Missing, malformed, unreadable or name-mismatched skill content warns/skips without blocking an otherwise healthy run; other valid skills remain. It must not copy an invalid/unsafe path or claim a skipped skill loaded. Unsafe provenance/collision and unrelated base AGY startup failures remain failures, not content warnings. AGY may still reject otherwise validated content, in which case its failure is safely reported.

### Review Authority
Blocking design or implementation findings must cite these approved IDs once approved. SR-013 changed behavior was explicitly approved by the user in E-034; downstream design/review must use this baseline.

## Requirements
| ID | Requirement | Related Behaviors | Priority | Rationale / Source |
| --- | --- | --- | --- | --- |
| REQ-001 | New AGY-backed AutoByteus runs expose exactly these AGY-native custom-agent names on the validated installed CLI contract: `view_file`, `write_to_file`, `replace_file_content`, `grep_search`, `list_dir`, `find_by_name`, `run_command`, `generate_image`. They coexist with separately configured MCP tools; app-owned MCP `generate_image` is not native parity. An unsupported CLI/profile drift must fail visibly rather than silently remove or expand native grants. | BEH-001 | High | User E-041/E-048; live E-045–E-047 |
| REQ-002 | AutoByteus must permit and truthfully present AGY-native `generate_image` invocation and ordinary provider tool status/assistant response. AGY owns image storage; AutoByteus is not required to find, copy, index, serve, render or retain generated image bytes or a path. A native error/denial must not be mislabeled as success and public failure must remain credential-safe. A native DONE without a path is not failure by itself. | BEH-001, BEH-003 | High | User E-054/055 supersedes earlier output-path scope; E-037/045 native stream evidence |
| REQ-003 | The standalone Codex agent can start an AGY-backed first turn and answer when a configured workflow skill is absent; absence warns/omits rather than becoming a startup prerequisite. This ticket does not change the separate agent package or require the workflow skill to be bundled/present. | BEH-002 | High | User screenshot; E-022 missing-skill direction; E-057 server-only de-scope |
| REQ-004 | **SR-013 approved:** AGY configured skills use the same AutoByteus `SKILL.md` metadata/name validation applied for Codex App Server. An absent folder or malformed, unreadable or name-mismatched manifest produces a sanitized backend warning identifying run, skill and safe reason; only that skill is omitted and otherwise healthy AGY startup and other valid skills continue. A valid source is materialized for AGY; no skipped or merely materialized skill is claimed as loaded. Unsafe source provenance, protected destination collisions and unrelated base-runtime/capsule failures remain blocking safety/system failures with safe reporting; AGY-origin rejection of a validated skill remains a possible provider failure. | BEH-002 | High | User E-028/E-033; Codex comparison E-032; prior safety approval E-024 |
| REQ-005 | AutoByteus-configured MCP tools remain additive to native AGY tools and scoped to the agent/run's configured exposure. | BEH-001 | High | User clarification; E-008, E-012 |
| REQ-006 | AGY-native subagent spawning/definition/management and native inter-agent messaging are not exposed to AutoByteus-launched AGY agents; AutoByteus Team/Org collaboration remains through its configured MCP tools. | BEH-001 | High | User clarification; prior AGY design scope; E-013 |

## Acceptance Criteria
| ID | Related Requirements | Related Behavior / Scenario | Trigger | Observable Expected Outcome | Failure / Alternate | Verification Intent |
| --- | --- | --- | --- | --- | --- | --- |
| AC-001 | REQ-001, REQ-002 | BEH-001, SCN-001 | New AGY-backed Solution Designer asks for a dog image with AGY-native generation available | Provider-provenance `tool_name=generate_image` is actually invoked and reaches its reported terminal state; normal app chat shows the native tool-call lifecycle and ordinary AGY reply. AGY may store the image in its own location. | An AutoByteus MCP `call_mcp_tool` image alone does not pass; no app file/path/preview/Files requirement and no false claim when provider reports failure | Real AGY-backed app-run check of provider native call and visible tool lifecycle, not app artifact bytes |
| AC-002 | REQ-002 | BEH-003, SCN-001 | AGY-native image generation unavailable, denied or reports error | User sees an accurate safe tool failure/denial; no fabricated success or image claim | Native DONE lacking an output path is not failure; credentials and raw private diagnostics absent | Controlled native-tool lifecycle and redaction tests |
| AC-003 | REQ-003 | BEH-002, SCN-002 | New standalone Codex run using AGY with the configured workflow skill absent | Run warns/omits that skill, prepares and answers first prompt without claiming the skill loaded | No generic preparation failure solely from skill absence; no bundled-skill/content expectation | Real missing-skill first-turn check (API-REV-002); ambient package first-turn not established by package-branch tests |
| AC-004 | REQ-004 | BEH-002, SCN-003 | **SR-013 approved:** configured AGY skill absent, malformed/unreadable `SKILL.md`, name-mismatched metadata, valid, or unsafe source/destination | Absent/semantically invalid skill yields sanitized backend warning, no materialized skill, and AGY starts if otherwise healthy; other valid skills remain. A valid configured skill is materialized and available to AGY. | Unsafe provenance/collision remains a safely reported preparation failure; AGY may reject otherwise validated content; no false loaded-skill claim or raw stack/secret | Controlled missing/invalid/valid/safety tests plus first-turn provider check |
| AC-005 | REQ-001, REQ-003, REQ-005 | BEH-001, BEH-002 | New AGY standalone and Team/Org runs on supported CLI version | The exact eight approved native names are usable with separately configured MCP; no 49-name startup rejection; run identity/permissions and Codex startup remain correct | No unconfigured MCP grants or silent native-name expansion | Versioned runtime and first-turn/provider regression checks |
| AC-006 | REQ-006 | BEH-001, SCN-004 | New AGY-backed standalone or Team/Org run | Native AGY subagent and messaging calls are unavailable while configured AutoByteus MCP collaboration still works where appropriate | No untracked provider-native collaboration masquerades as AutoByteus Team activity | Selective-tool and Team/Org MCP checks |

## Relevant Scenarios And Journeys
| ID | Kind | Actor / Event | Goal | Supported Trigger | Starting Condition | Product-Level Sequence | Expected Outcome | Alternate / Error | Validity | Evidence | Related IDs |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SCN-001 | User | User asks AGY-backed agent to generate image | Invoke AGY-native image generation | AutoByteus AGY standalone/Team/Org chat | New AGY run | User asks; agent calls approved native `generate_image`; app shows tool lifecycle and ordinary AGY reply while configured MCP remains available; AGY owns image storage | Genuine native invocation/status, without app-owned image artifact obligation | Native failure gives accurate safe error | Supported Normal Scenario; SR-021/E-055 narrows output scope, SR-018 exact eight remains approved | User E-054/055, CLI/probe E-001/045–048 | REQ-001/002/005; AC-001/002/005 |
| SCN-002 | User | User starts Codex | Get assistant reply | Standalone agent chat | New AGY Codex run | User sends hello; run prepares; assistant responds | First reply | None beyond normal runtime failure | Supported Normal Scenario | Screenshot, metadata, log E-004/005 | REQ-003; AC-003 |
| SCN-003 | System | Configured skill absent or invalid | **SR-013 approved:** mirror Codex content validation with nonblocking per-skill disposition | Run preparation | One configured skill is absent, has malformed/unreadable/name-mismatched `SKILL.md`, or has an unsafe source/destination; base AGY runtime otherwise healthy | User sends first message; AutoByteus validates metadata/name as for Codex, warns/omits missing or invalid content, materializes valid sources for AGY | Missing/invalid content does not block; valid other skills remain available; agent can respond | Unsafe provenance/collision is a preparation failure; AGY may reject validated content; no false loaded-skill claim | Supported Explicit Edge Scenario: user directs Codex-parity judgment and earlier nonblocking invalid-content outcome | E-004/006, E-022/023, E-028–E-033 | REQ-004; AC-004 |
| SCN-004 | Contract | AutoByteus AGY tool policy | Keep provider collaboration separate from tracked Team/Org work | AGY run activation | Supported CLI and configured agent | Exact eight approved native names and selected MCP tools are exposed; native subagent/messaging names are excluded | No provider-native collaboration path in AutoByteus AGY runs | Unsupported CLI profile fails compatibly rather than silently changing grants | Supported Explicit Edge Scenario: user explicitly requests Claude-like exclusion | User E-013/E-048; E-045–E-047 | REQ-001/005/006; AC-005/006 |

## UI, Interaction, And Experience Requirements
- Applicable: Yes — existing chat tool lifecycle and error presentation only; no generated-file presentation requirement.
- Linked UI/UX or interaction supplement: N/A — not applicable; no new design requested.
- Prototype-specific fields: N/A — not applicable.
- Required states: native tool ACTIVE and terminal success/failure/denial, plus ordinary AGY reply. No app image path/preview/Files state. Missing/invalid-skill warning belongs in backend logs, not a blocking chat error.
- Unresolved product decisions: None. DEC-005 exact eight-name scope was approved in E-048; DEC-006 native-invocation-only scope in E-055; DEC-007 server-only package exclusion in E-057. SR-016 hook option remains withdrawn/not approved.

## Quality And Non-Functional Requirements
| ID | Related REQ/AC | Area | Constraint | Verification |
| --- | --- | --- | --- | --- |
| QR-001 | REQ-002/004, AC-002/004 | Security | **SR-013 approved:** No provider credentials, MCP session URL, private stack, raw provider stderr or raw skill-source exception in user-visible failures or backend warnings; source-path containment and safe materialization remain required; AutoByteus uses Codex-parity semantic skill validation, and provider-origin failures remain distinguishable and safely redacted. | Error/warning-path tests |
| QR-002 | REQ-001/003, AC-005 | Compatibility | Existing unaffected agent grants, AGY per-run isolation and historic run state unchanged. | Regression checks |

## Data Continuity And Acceptable Loss
- Persisted or external data affected: Existing agent runs and server skill handling; the separate agent package is unchanged and no user content/schema transition is intended.
- Preserve prior conversations/images and other agents' configuration.
- Acceptable loss/reset: New runs may be needed for changed tool grants; no deletion of old runs.
- Unknowns: Native AGY image invocation and visible lifecycle inside a live AutoByteus run remain to be verified. AGY-owned image bytes/location are not an AutoByteus acceptance condition.

## External Contracts And Dependencies
| Dependency | Required Behavior | Evidence | Risk |
| --- | --- | --- | --- |
| AGY CLI native GenerateImage | Must be available and actually callable in supported AutoByteus AGY runs | User CLI transcript and exact-eight probes | Full app invocation/lifecycle validation still open; no output projection obligation |
| AutoByteus generate_image | Separate app-owned tool; not a replacement for native AGY GenerateImage | Server media service/docs | Its provider/credential state is independent |
| Agent package repository | Existing package configuration is read but not changed by this ticket; an absent configured skill must not block otherwise healthy AGY startup | E-005, E-022, E-057 | Existing missing/broken skill may be skipped; availability is outside this ticket |

## Supplemental Artifacts
N/A — no behavior-defining supplement. Four user screenshots and CLI transcript are evidence referenced in investigation-notes.md.

## Assumptions And Open Decisions
| ID | Question / Assumption | Owner | Status |
| --- | --- | --- | --- |
| DEC-001 | Original broad-native preference with native subagent/messaging exclusion? | User | Original SR-005 approval remains historical. E-048/SR-018 narrows current implementation to exact eight names; other native tools, including schedule/ask_question, are no longer in this ticket’s required exposure. Native collaboration remains excluded. |
| DEC-002 | Historical combined portable-skill and warn/continue outcome | User | SR-010 historically approved both; **SR-024/E-057 supersedes only the portable package outcome**. Warn/omit and safe AGY startup remain approved. |
| DEC-003 | Should AGY configured skills use the same AutoByteus `SKILL.md` metadata/name validation as Codex, warning/skipping missing or invalid content instead of copying invalid content through or hard-failing it? | User | E-033 condition applies because E-032 establishes Codex does judge. Full reconciled SR-013 baseline, including safety/provider-failure boundary, approved by user “approve” in E-034. |
| ASM-001 | CLI custom-agent frontmatter can preserve supported non-collaboration native tools through a validated explicit list; exact supported names and MCP coexistence must be verified per supported version | Architecture investigation | **Refuted as used in SR-015:** 49-name list fails first turn on 25 unknown custom-agent components in real AGY 1.2.11. A supported replacement contract is required; `init.tools` is only inventory. E-036. |
| DEC-004 | If the CLI cannot hide native subagent/messaging tools without suppressing other AGY-native capabilities, may AutoByteus preserve the native tools and deny collaboration calls before execution, even though those names may remain visible to the model? | User | **Not approved; withdrawn from current proposal after E-041.** Do not treat hook denial as equivalent to current REQ-006/AC-006. |
| DEC-005 | Should the AGY custom-agent native-tool profile be exactly seven **actually exercised** coding tools plus native `generate_image` (eight total), omitting the uninvoked `multi_replace_file_content` and all other current 49-name extras unless a separate supported need is approved? | User | **Approved** by user’s explicit “approve” reply to the exact-eight question (E-048/SR-018). E-045–E-047 provide installed-CLI proof; future additional names require separate approval/validation. |
| DEC-006 | Is genuine AGY-native invocation and normal tool-call/status/assistant display sufficient, with AGY owning generated-image storage and no AutoByteus file/path/preview/Files obligation? | User | **Approved** by explicit E-055 instruction to remove the overscoped design after E-054 clarification. Supersedes the earlier REQ-002/AC-001 app-result-path outcome; safe native failure remains. |
| DEC-007 | Exclude the separate `autobyteus-agents` package modification and accept Codex warn/omit/start without a bundled-skill guarantee in this ticket? | User | **Approved** by explicit 2026-09-26 user direction to remove PR #14 and finalize the current project only (E-057). |

## Historical SR-016 Proposal — Not Approved Or Current
API-REV-001/CRR-004 (E-036/037) proves the SR-015 native profile fails before either supported first-turn scenario and that native image DONE lacks the assumed explicit output path. The image outcome **does not change**: REQ-002/AC-001 still require a genuine AGY-native image and accessible, verified result, never an MCP substitute or final-prose path scrape. Four already-produced AGY transcripts contain structured provider `media[]` URI records (E-038), a technical candidate for design after the approval boundary is resolved. Skill behavior REQ-003/004 and AC-003/004 stays as approved in SR-013.

The requested decision affects only the native collaboration boundary. **Current approved REQ-006/AC-006 remains “not exposed/unavailable.”** Proposed replacement: native AGY subagent/agent-messaging operations must not **execute** in AutoByteus AGY runs; AutoByteus Team/Org collaboration remains via configured MCP. Prefer provider-supported removal from model exposure when it preserves other required native tools. If the installed CLI offers no such compatible removal, a provider `PreToolUse` denial before any collaboration action is permitted even though names may be model-visible. Such attempted calls must be safely shown as denied, not silently performed, and must not be recorded as AutoByteus Team activity. All other supported native capabilities, especially native `generate_image`, and separately configured MCP must remain available; the provider profile must answer a first turn rather than fail because registry-only names were copied into custom-agent frontmatter. This proposed exception does **not** authorize the diagnostic five-name profile, a silent 25-name trim, an MCP-image fallback, or success based on `init.tools` alone. The exact tool composition and hook reliability remain executable design/validation gates, not assumed facts.

This option was not approved. The user then questioned the need for 49 names and directed a minimal wanted-name list (E-041). It was superseded by DEC-005, approved in E-048. A hook is not an authorized production fallback.

## Approved SR-018 Native-Tool Scope (originating in SR-017 proposal)
For the reported AGY image and Codex-startup cases, the user approved **exactly eight AGY-native custom-agent names**: `view_file`, `write_to_file`, `replace_file_content`, `grep_search`, `list_dir`, `find_by_name`, `run_command` (seven previous coding tools that were each actually invoked in isolated AGY 1.2.11 turns), plus `generate_image` (the native tool the user specifically requested). The earlier eighth coding name, `multi_replace_file_content`, was accepted by the constructor but did not invoke in four attempts, including three forced prompts where the model answered “unavailable”; it is omitted as unproven/duplicative rather than treated as a working capability. The other 40 names in the task branch's 49-name policy—including browser internals, permission, scheduling, and optional web tools—are also omitted. The list remains separate from agent-configured MCP; it exposes no native AGY subagent or messaging tools. Do not add `call_mcp_tool` to native frontmatter, because the separately scoped MCP session remains provider-managed. This changes the previously approved broad “all supported default non-collaboration native capabilities” promise in REQ-001/AC-005 to the **named requested native set** for this ticket; other AGY-native capabilities can be requested and validated in a later change. The exact-eight decision originally retained the prior image-path promise; **SR-021/E-055 later superseded that image-output promise**. **SR-024/E-057 further withdraws the separate package-bundling outcome**, retaining native invocation, safe failure, scoped MCP and server missing/invalid-skill handling.

This approved selection is supported by **bounded live provider proof**, not a claim of universal or full-app validation. E-045–E-047 show the exact eight-name profile starts, each seven coding tools executes in a disposable task, native `generate_image` creates real JPEGs under the relevant Low/High models, a separately configured dummy MCP tool executes, and a locally selected, now-unmerged Codex package skill was read on first turn. This last observation is historical configuration evidence, not a current package-main acceptance gate. Optional `search_web` and `read_url_content` were also invoked in a ten-name profile, but are not needed for these two reported cases; URL reading requires permission when auto-approve is off. AutoByteus Team/Org, native tool lifecycle/redaction, scoped MCP, Codex startup and future provider versions still require reviewed design and downstream API/E2E validation. E-055 removes Files projection and structured-media image-output work from this ticket.

## Traceability
| REQ | Use Cases | Behaviors | ACs | Scenarios |
| --- | --- | --- | --- | --- |
| REQ-001 | UC-001 | BEH-001 | AC-001, AC-005 | SCN-001 |
| REQ-005 | UC-001 | BEH-001 | AC-005 | SCN-001, SCN-004 |
| REQ-006 | UC-001 | BEH-001 | AC-006 | SCN-004 |
| REQ-002 | UC-001, UC-003 | BEH-001, BEH-003 | AC-001, AC-002 | SCN-001 |
| REQ-003 | UC-002 | BEH-002 | AC-003, AC-005 | SCN-002 |
| REQ-004 | UC-003 | BEH-002 | AC-004 | SCN-003 |

## Architecture Phase Input
- Approved scenario IDs: SCN-001 to SCN-004 under SR-005/SR-013 as revised by exact-eight SR-018/E-048 and native-invocation-only SR-021/E-055 approvals. SR-016’s hook alternate was not approved. SR-015’s native profile and image-output premise failed live validation. BEH-002/SCN-003/REQ-004/AC-004 skill behavior remains approved as SR-013/E-034.
- Preserve run-scoped identity/MCP grants, generated AGY capsule, best-effort skill intent and safe diagnostics; do not add native image output projection or app-owned media coupling.
- Design must verify AGY native tool-list semantics, actual native GenerateImage invocation and accurate safe lifecycle (not image bytes/path projection), security/team-tool exclusions, and non-blocking missing/invalid-skill warnings with safe provenance separation. **No package-repository portability work or validation gate remains in this ticket.**
- No target architecture is approved by this document.

## Readiness Check
- Current behavior evidence-backed: Yes.
- Desired and preserved behavior explicit: Yes — shared metadata/name validation; warning/omission for absent or semantically invalid skills; blocking safety/unrelated failures; AGY-origin failure remains possible.
- Scope/non-goals clear: Yes.
- Requirements/ACs testable and traceable: Yes.
- Scenarios supported/evidence-backed: Yes.
- UI/UX supplement: N/A.
- Material assumptions visible: Yes.
- Content ready for user approval: Yes; DEC-005 exact eight names were approved in E-048/SR-018, and DEC-006 AGY-owned image/invocation-only scope was explicitly approved in E-055/SR-021. MCP, collaboration-exclusion and skill outcomes are unchanged.
- User approval received: **Yes** for SR-018 exact eight names (E-048) and SR-021 native image scope reduction (E-055); SR-013 skill approval remains authority for unchanged portions.
- Approved basis ready for affected design: **Yes.** SR-023 design implements the narrowed outcome and safe terminal-result failure boundary; independent architecture re-review and executable validation remain required before implementation/delivery claims.
