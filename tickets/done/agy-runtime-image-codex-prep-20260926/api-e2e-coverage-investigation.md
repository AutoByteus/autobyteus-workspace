# API/E2E Coverage Investigation — current API-REV-004

## Current API-REV-004 investigation — user-requested running frontend/backend journey
- Trigger: user explicitly asked to start both backend and frontend, import the selected agents package and do a real run in the frontend. API-REV-003 Pass/95.0% covered real in-process backend and mounted frontend components but **not** the running browser journey; do not claim otherwise.
- Authority remains approved SR-023/E-048/E-055 and CRR-005 source Pass. User requests additional execution, not changed product behavior. Task size Medium / architectural risk High, reviewed route. API-REV-003 F-TEST-001 durable assertion fix remains pending proportional re-review; this round adds independent live-browser evidence.
- Changed/selected boundaries: local built server process with isolated app-data/SQLite and AGY CLI 1.2.11; local Nuxt dev process on non-conflicting port; browser renderer, GraphQL proxy, direct WebSocket stream, package import/selection, fresh AGY run and rendered native generate_image lifecycle/ordinary reply. No shell-specific Electron behavior, data migration, worker or production source change.
- Execution discovery: `autobyteus-server-ts/AGENTS.md`, `autobyteus-web/AGENTS.md`, root/server/web READMEs, both package scripts, `nuxt.config.ts`, `AgentPackagesManager.vue`, and existing real-app E2E. The normal paths are server build then `node dist/app.js --data-dir ... --host 127.0.0.1 --port ...`; Nuxt `dev` with BACKEND_NODE_BASE_URL and WS endpoints. `pnpm` is not installed but local Node binaries are available, so build shared TypeScript packages and server with installed `tsc`/asset script. Use unused 18080/13080 rather than colliding with existing localhost:3000; isolated app-data and fresh workspace. Import current local package `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-codex-skill-bundle-20260926` through UI. Do not touch running user apps or ambient data.
- Existing durable coverage: all seven API-REV-003 reviewed test/fixture paths remain **Still Valid** for their prior scope; no change requested to assertions and no additional durable test edit is warranted until the live journey reveals a gap. New browser journey is **Use Temporary Executable Probe Only** for deployment-like user verification of package import and rendering; capture process/browser/API evidence and record observed result. Browser result does not override prior real-provider evidence if its environment differs.
- Plan: initialize API-REV-004 case-ledger checkpoints; build/start owned services, verify readiness and package identity, navigate running UI with CUA, import package in Settings, launch a new AGY 1.2.11 run with model `gemini-3.8-flash-low`, send a native image prompt, observe one coherent tool card and normal reply. Correlate with backend events/history and capture screenshots/logs without exposing private credentials. If live run fails, classify exact boundary and route per skill. Stop/clean only owned server/frontend processes and isolated data after evidence; never stop existing localhost:3000 or the user's desktop app.
- Confidence gate: prior 95.0% remains historical. Current full-browser journey is **Required**, not yet executed; current result/score are pending observation. Browser evidence can close the previously explicit frontend realism gap or reveal a product/environment failure. No result inferred from the setup plan.
- Executed outcome: built and started isolated backend 18080 and Nuxt 13080. Initial backend attempt inherited production APP_ENV/DATABASE_URL and failed before readiness with locked database; explicitly overridden test.db process succeeded, verified by startup log and GraphQL 200. Chrome UI import confirmed selected current package, 8 shared/44 team-local agents and 12 teams; Featured Codex appeared with bundled workflow skill. Through the browser, selected AGY/Gemini 3.8 Flash (Low), sent native image prompt, observed one green generate_image card, Activity SUCCESS/DONE output:null, ordinary reply and Idle. Backend raw trace correlates same invocation and turn IDs; new-run frontmatter exactly eight native grants and imported skill bytes match source. A fresh second browser run read the skill via four native view_file calls and first reply identified Stage 0 Bootstrap + Draft Requirement. No app image preview/bytes/path is claimed or required. Evidence: `api-e2e-evidence/api-rev-004/browser-observation.md`, `real-run-trace-summary.json`, `real-run-projection.json`, `real-skill-first-turn.json`, isolated backend/frontend logs.
- Coverage validity after browser execution: all seven prior durable paths remain **Still Valid**; no new test source edit or removal. This browser journey is retained as a temporary direct executable check because the user requested immediate live verification. CRR-007 separately passed API-REV-003's proportional test review. API-REV-004 result **Pass/95.0%**; seven categories all 95%, broader validation **Required, executed and closed**. Other Team/Org/failure/safety cases carried, not rerun. Owned loopback services, isolated data and marked browser tab deliberately remain live for the user's requested inspection; cleanup ownership is documented. No Electron shell test.


## Current API-REV-003 investigation — CRR-006 Local Fix F-TEST-001
- Trigger: proportional changed-test-code review CRR-006 Fail — Local Fix F-TEST-001. API-REV-002 observed **Pass/95.0%** remains historical truth; actual JSON from that run showed matching invocation IDs. The reviewer identified a durable assertion gap, not a production-source failure. CRR-005 source Pass and SR-023/E-055 authority remain unchanged; task size Medium / architectural risk High, reviewed route.
- Re-read the current approved requirement/design package, IR-005, CRR-005, the CRR-006 test-review report, current investigation/report/revision/ledger and target real-app test. AC-001/SCN-001 demands one accurate native image tool-call lifecycle in normal chat. The frontend handler keys tool cards by invocation_id. Existing real-app test currently counts one STARTED and one SUCCEEDED but does not require nonempty/equal invocation_id or turn_id or event order. The last live JSON demonstrated matching IDs, but the durable assertion would miss divergence.
- Coverage validity decision before edit: the target `autobyteus-server-ts/tests/e2e/runtime/agy-native-image-app-chat.e2e.test.ts` is **Needs Update** for API-CASE-004/AC-001. All other six reviewed durable test/fixture paths are **Still Valid** for this finding; no product source, package, Files gate, fake-provider fixture or frontend logic should change. Replace no scenario and remove no valid coverage. Add focused IDs and ordering assertions to the existing real-app case, with turn-completion correlation; include turn_id/event index in bounded evidence JSON for audit.
- Execution plan: initialize API-REV-003 ledger re-entry, build only missing local shared TS artifacts needed by the existing documented Vitest setup, run the single opt-in real AGY app case (and optionally both cases in the same file if selection/setup requires), inspect JSON for ordered same-ID STARTED/SUCCEEDED/completion and ordinary reply, run `git diff --check`, remove only owned generated dist, update canonical investigation/report/revision/ledger. Do not infer provider behavior from init.tools or add image bytes/Files checks.
- Confidence gate: API-REV-002 broader real AGY, Team/Org, failure and UI evidence remains valid and need not be rerun for an assertion-only fix. The new critical evidence is a fresh real app invocation with durable correlation assertions. If it passes, final confidence can remain 95.0% with improved regression quality; if not, classify the observed failure rather than assuming the prior pass. Browser/Electron shell adds no evidence for this test-assertion boundary.
- Executed scoped revalidation: from autobyteus-server-ts, RUN_AGY_CAPABILITY_E2E=1 with isolated API-REV-003 evidence directory, Vitest selected only the native image app case: **1 passed / 1 unrelated Codex case skipped** (`api-e2e-evidence/api-rev-003/app-correlation.log`). Actual WebSocket indices: TURN_STARTED 5, native generate_image STARTED 6, SUCCEEDED 7, assistant SEGMENT_CONTENT 9, TURN_COMPLETED 12. STARTED/SUCCEEDED have the same nonempty invocation_id and turn_id; completion has the same turn_id. The new assertions passed on a real AGY 1.2.11 app run, not on a synthetic event. Bounded JSON now records event_index and turn_id (`app-native-image-chat.json`). Source build tsc and git diff --check passed; no product source changed. Initial root-workspace Vitest command failed during collection due aliases, then corrected to documented server-package cwd; no test ran under the mistaken command. Owned shared dist outputs cleaned.
- Final API-REV-003 scorecard: all seven categories **95%**, overall **95.0%** unchanged from API-REV-002. Requirement/directness/integration/environment/failure/user-surface prior evidence is carried, not rerun; durable regression quality is stronger because lifecycle correlation/order is now enforced. Broader validation for this narrow fix was **Required and executed** as a fresh real app boundary; browser/Electron not required. F-TEST-001 resolved locally; no new implementation or design finding. Return to Code Reviewer for proportional test re-review on reviewed Medium/High route.

## Current API-REV-002 investigation — CRR-005 / IR-005 / SR-023 (supersedes historical API-REV-001 plan)
- Trigger and authority: CRR-005 Pass after IR-005, ARCH-REV-008, SR-023. Approved user changes E-048 (exact eight 1.2.11 native names) and E-055 (native invocation/status/ordinary reply only, AGY owns image storage) supersede historical API-REV-001 AC-001/002. Approved SR-013 skill warn/skip/safety remains. Task size Medium / architectural risk High; reviewed route and Code Reviewer review after successful durable test change. Prior API-REV-001 was Fail/75.0%, F-API-001/002. No new result inferred.
- Read current requirements, investigation E-036–056, solution record SR-021/023, design SR-023, ARCH-REV-008, IR-005 and CRR-005. Current source and unchanged package worktrees are assigned. Legacy/compatibility: no 49-name fallback, no transcript/copy/Files image writer, no dual path. Persisted-data decision remains Directly Usable — No Migration: manifest v1 and generic projections remain readable; new grants only on new capsules.
- Changed boundaries: AGY native policy/capsule, external 1.2.11 model exposure, adapter/backend canonical tool and turn events, restricted diagnostics, WebSocket/chat presentation, scoped MCP Team/Org identity, package skill resolution/materialization. No frontend source or Electron shell change; browser is preferred for web-equivalent tool-card rendering if API/WebSocket checks cannot directly prove it. No worker/queue or schema change.
- Execution discovery unchanged: `autobyteus-server-ts/AGENTS.md`, server/root README/package scripts, Vitest; `tests/e2e/helpers/studio-runtime-test-server.ts` supplies isolated in-process Fastify/MCP runtime; `agy-team-inter-agent-roundtrip.e2e.test.ts` supplies real GraphQL/WebSocket Team/Org journeys gated by `RUN_AGY_E2E=1`; local AGY 1.2.11/model and package available. Use isolated temporary app data/workspaces, owned processes only, local installed test binaries if pnpm remains unavailable. Never use ambient credentials as proof of app media grants.

### Revised existing-coverage decisions before edits
| Path/scenario | Prior assertion | Current decision and authority | Action |
| --- | --- | --- | --- |
| `tests/integration/api/run-file-changes-api.integration.test.ts` added AGY converter→Files case | A successful AGY native image must produce verified file path and Files bytes | **Stale / Remove**: E-055 and SR-023 explicitly remove app-owned image path/Files acceptance; current converter intentionally succeeds pathless. Generic preexisting seeded absolute-image Files API test remains valid for other tools/providers. | Remove only added AGY-specific case/imports; retain valid V2 Team fixture/process-manager repair. No replacement at Files boundary because it is out of scope; new native lifecycle/API/WebSocket test replaces behavioral acceptance. |
| `tests/e2e/runtime/agy-native-image-codex-skill.e2e.test.ts` API-REV-001 native case | Full profile must expose 49 names, image bytes/path from converter | **Replace**: E-048 exact eight; E-055 pathless DONE success. | Refactor to exact-eight direct CLI/adapter supporting probe; remove old five-name diagnostics and file-byte gate. Add actual app-launched GraphQL/WebSocket case as primary proof. |
| Existing AGY policy/converter/backend/diagnostic/skill/capsule unit suites | Current exact-eight, pathless success, fixed-safe terminal failure, warn/skip and provenance | **Still Valid** for SR-023 if tests assert current contract; run. | Do not infer live provider/UI from mocks. |
| `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` | Real scoped Team/Org MCP routing | **Still Valid** under E-048/SR-023; run with installed AGY. | Verify provider `call_mcp_tool`, receipts, no native collaboration. |
| `tests/e2e/agent-definitions/agent-package-private-skills.e2e.test.ts` | Package-private skill discovery | **Still Valid**, but synthetic; run package actual first turn separately. | Preserve. |
- No other durable removals. Never retain a test whose only purpose protects the superseded app-image artifact obligation.

### Revised case plan and evidence gates
| ID | Current case | Acceptance criteria | Mode |
| --- | --- | --- | --- |
| API-CASE-001 | Exact-eight policy, unsupported-version, canonical converter/projection regression | 001/002/005/006 | focused unit |
| API-CASE-002 | Pathless native DONE→success, no fabricated FILE_CHANGE; generic Files regression | 001/002 | integration/API |
| API-CASE-003 | Bundled skill discovery, semantic invalid/missing warn/omit, unsafe provenance/collision, manifest restore | 003/004 | unit/package |
| API-CASE-004 | Real AutoByteus-launched standalone image: GraphQL create run→WebSocket send→provider-native `generate_image` ACTIVE/DONE, canonical tool card/ordinary reply; no MCP substitute | 001/005 | live API/WebSocket, browser if needed |
| API-CASE-005 | Genuine provider pathless DONE truthfulness and no app artifact promise | 001/002 | direct CLI/adapter supporting plus live app |
| API-CASE-006 | Actual bundled Codex skill and first AGY reply via normal package | 003/004 | live API/WebSocket |
| API-CASE-007 | Step ERROR/denial and terminal result ERROR safe public ACK/history/tool card, private bounded diagnostic; missing/invalid skill warnings vs unsafe hard failures | 002/004 | controlled API/E2E + browser as justified |
| API-CASE-008 | Scoped Team and Org MCP with exact native profile; provider-native collaboration excluded | 005/006 | real Team/Org GraphQL/WebSocket |
- Ledger remains the canonical `api-e2e-test-case-ledger.md`; API-REV-001 events are historical. Append API-REV-002 case events before each execution and checkpoint promptly.
- Post-repository-only scorecard before live execution: requirement 75, directness 75, integration 75, environment 90, failure/edge 90, user surface 75, durable coverage 95 = **82.1%**. Real provider, app transport and skill first turn were critical; broader validation **Required**. Selected real AGY and app GraphQL/WebSocket plus controlled process failure, Team/Org and Nuxt rendered-component suites. No desktop-shell change.
- Reroute triggers: genuine provider behavior contradicting current exact-eight/pathless lifecycle design → Design Impact; implementation or test-only defect → Local Fix preliminary classification through Code Reviewer. Preserve approved no-MCP-image-fallback boundary.

## Executed API-REV-002 investigation update and coverage decision
- Current acceptance map: AC-001 native image is **provider `tool_name=generate_image` ACTIVE→DONE**, matching canonical WebSocket tool-call invocation and ordinary reply; no app image bytes/path/Files gate. AC-002 pathless DONE is success; denial/terminal error remains safe. AC-003/004 selected Codex package/skill first turn and missing/invalid warn-skip plus unsafe fail. AC-005/006 exact eight frontmatter, separate scoped MCP Team/Org and native collaboration exclusion.
- API-CASE-001: 6 policy/converter/backend/projection files, **51 pass** (`api-e2e-evidence/api-rev-002/case001.log`). API-CASE-002: current generic Files API/projection only, **17 pass** across 3 files (`case002.log`). API-CASE-003: **82 pass/1 existing skip** across skill, capsule, package suites (`case003.log`), source build tsc pass; direct real AGY missing+semantic-invalid bindings warn/omit and `SUCCESS` first turn (`direct-native-final.log`, `real-skills-warn-skip.json`). Unsafe provenance, collision and source mutation remain fail-closed in unit tests.
- API-CASE-004/005: full in-process AutoByteus studio GraphQL create-run→WebSocket send with real AGY 1.2.11, model `gemini-3.8-flash-low`: actual generated custom-agent line is exactly `tools: [view_file, write_to_file, replace_file_content, grep_search, list_dir, find_by_name, run_command, generate_image]`, no native collaboration or `call_mcp_tool`. Genuine `generate_image` ACTIVE/DONE with matching invocation ID, canonical success `{provider_state:DONE,output:null}`, assistant segment and TURN_COMPLETED; no MCP image substitution. Direct CLI supporting test independently 3/3 pass after revision. Do **not** infer exposure from provider `init.tools` inventory (57 registry entries); actual invocation and selected frontmatter are the proof. (`app-integrated-final.log`, `app-native-image-chat.json`, `direct-native-final.log`.)
- API-CASE-006: isolated package root explicitly selected after ambient older `autobyteus-agents` package shadowed the new bundle; fresh definition sourceInfo equals supplied separate package `a140474`, bundled skill content exact, real app first AGY turn reads materialized skill and replies Stage 0 Bootstrap; **2/2 integrated app tests pass** (`app-integrated-final.log`, `app-codex-first-turn.json`). Ambient older checkout is untouched; Delivery must point actual deployment at the current bundle.
- API-CASE-007: controlled compatible CLI child through the real app yields native image TOOL_DENIED and terminal result ERROR (without tool); WebSocket ACK/tool card and GraphQL projection exclude secret/private path; private diagnostic exists, mode 0600 and bounded under 32 KiB; **2/2 pass** (`failure-transport-final.log`). Fixed-safe redaction is tested across process→adapter→API/WebSocket/history, not only a converter mock. Nuxt generic handler/presentation/component tests include AGY pathless native success/denial: 30/30 focused plus mounted-indicator rerun 14/14 pass after `nuxi prepare` (`frontend-tool-card.log`, `frontend-indicator.log`). No frontend product source or Electron shell changed; browser/desktop execution adds little over live backend events and mounted rendered component for this boundary.
- API-CASE-008: real Team scoped MCP `call_mcp_tool` send_message_to roundtrip and direct/nested Org attributed turns via GraphQL/WebSocket, **2/2 pass** (`team-org.log`). Exact eight frontmatter excludes native messaging/subagent; scoped MCP is separate and actually used.
- New durable files: `tests/e2e/runtime/agy-native-image-app-chat.e2e.test.ts`, `agy-failure-transport.e2e.test.ts`, `tests/fixtures/agy-failure-cli.mjs`; refactored API-REV-001 opt-in `agy-native-image-codex-skill.e2e.test.ts`; updated Nuxt handler and indicator tests. Removed only obsolete AGY-specific Files case/imports from existing API integration file; retained valid generic Files coverage and prior V2 fixture repair. Fake CLI is opt-in and not provider-exposure proof; real AGY app tests are separately opt-in.
- Confidence after broader validation: requirement **95**, changed-boundary directness **95**, cross-boundary realism **95**, environment/identity **95**, failure/edge **95**, user-surface **95**, durable coverage **95** → **95.0%** arithmetic mean. Residual: one installed AGY version/model/account and macOS machine; full browser/Electron shell not run because unchanged renderer maps the same canonical events and mounted AGY-specific tool card passed. No applicable category under 90; critical AC-001–006 directly supported. Broader decision **Required, executed and closed**; final recommendation **Pass** with proportional test-code review on reviewed Medium/High route.
- Static check note: `tsc -p tsconfig.build.json --noEmit` passed. Repository `tsc -p tsconfig.json --noEmit` includes tests outside configured `rootDir: src`, yielding baseline TS6059 pre-collection errors; overridden-root diagnostic also exposed preexisting missing `ws` type declaration and numerous unrelated test errors. Vitest is the documented test execution path; new test files have no further TS diagnostics under that override. `git diff --check` passed.

## Historical API-REV-001 investigation meta and authority
- Assigned worktrees: server `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-runtime-capabilities-20260926`; agent package `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-codex-skill-bundle-20260926`.
- Requirements Doc: `requirements-doc.md` (SR-013/E-034 approved); Investigation Notes: `investigation-notes.md` (E-001–035); Solution Revision Record: `solution-revision-record.md`; Design Spec: `design-spec.md` (SR-015); supplements: N/A.
- Independent review: `design-review-report.md` / `architecture-review-revision-record.md` (ARCH-REV-004 Pass); implementation: `implementation-handoff.md` / `implementation-revision-record.md` (IR-003); source review: `code-review-report.md` / `code-review-revision-record.md` (CRR-003 Pass). Delivery re-entry: N/A.
- Trigger: CRR-003 source Pass. Initial API/E2E investigation, no prior API/E2E result. Canonical latest investigation: this file. Ledger: `api-e2e-test-case-ledger.md`.
- Classification: Medium / High; Reviewed route. Successful route Code Reviewer for proportional test-code review if durable coverage changes; otherwise Not Applicable review record. API/E2E revision record created after completed result.

## Historical API-REV-001 requirement, design, and changed boundaries
- AC-001/002: real AGY-native `tool_name: generate_image`, not `call_mcp_tool`; successful regular image bytes/projected absolute path readable through Files/content; safe denial/failure/no false success. DS-002/003.
- AC-003/004: bundled Codex workflow skill in canonical package, valid skill loaded for AGY first reply; absent or semantic-invalid content warns/skips without false loaded claim, peers remain; unsafe provenance/collision/source mutation and unrelated base failures block safely. DS-004.
- AC-005/006: AGY 1.2.11 model-exposed non-collaboration native profile plus separately scoped MCP; no native subagent/messaging tool exposed; Team/Org collaboration remains AutoByteus MCP. DS-001.
- Changed surfaces: backend domain and external AGY process (yes); canonical events/transport/File content API (yes); package/configured skill filesystem and first-turn lifecycle (yes); Team/Org MCP identity (yes). No frontend source or Electron-shell change. Existing web tool card and Files renderer are affected observables; browser validation is preferred for web-equivalent surface if live API cannot directly prove them. No worker/queue or persistence schema change.
- Legacy/compatibility: no old eight-tool fallback and no native-to-MCP image fallback permitted. Approved persisted-data transition is Directly Usable — No Migration: v1 capsules and prior projections remain readable/unchanged. Existing restore regression will be run.

## Project execution discovery
| Path | Authority / instructions |
| --- | --- |
| `autobyteus-server-ts/AGENTS.md` | Vitest `pnpm -C autobyteus-server-ts exec vitest run <path> --no-watch`; integration suite same command `tests/integration`. |
| `autobyteus-server-ts/README.md` | Node 18+/pnpm, monorepo install, `.env` or isolated `--data-dir`, build then `node dist/app.js`; server host required, default SQLite, Settings owns credentials; do not use plaintext env as provider credentials. |
| root `package.json`, server `package.json` | `prepare:shared`, Prisma generate, source build/typecheck; `test:e2e:real` harness available. |
| `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` | Live AGY E2E gated by `RUN_AGY_E2E=1`, isolated app data, in-process Fastify/WebSocket. |
| `tests/unit/.../agy-production-live.test.ts`, `agy-mcp-team-live.test.ts` | Existing direct AGY live tests gated by `AGY_LIVE=1`, but use tiny artificial tool profiles and prior-ticket output paths; not sufficient for AC-001/003/005. |

- Local AGY 1.2.11 is installed; npm dependencies present. Live provider authentication/availability still to be determined from execution; never record credentials. Isolate temporary workspace/app data and stop only owned processes.
- Runtime components: Vitest in-process server, AGY child CLI, optional local MCP host, SQLite temp app-data, existing file-content API. Readiness: AGY `--version`, server listen/health, CLI init/result events. Cleanup: close Fastify/MCP/backend, remove owned temp dirs. No shared process/store reuse.
- Fixture/identity: canonical Codex package copied under isolated app-data definition root; dummy configured invalid/missing skills and marker-valid skill; temporary image bytes only for contract tests, never count as live provider proof.

## Existing coverage inventory and validity
| Path / scenario | Decision | Why / action |
| --- | --- | --- |
| `tests/unit/.../agy-native-tool-policy.test.ts`, `antigravity-cli-capability.test.ts` | Still Valid | Pins 1.2.11 names and unsupported-version failure; does not prove actual model exposure. Run. |
| `tests/unit/.../agy-stream-event-converter.test.ts`, diagnostic-sink test | Still Valid | Synthetic native/MCP provenance, safe failure, explicit file path; provider actual shape and downstream public views unproven. Run. |
| `tests/unit/agent-execution/events/file-change-event-processor.test.ts` | Still Valid | IR-003 AGY origin guard and non-AGY projection. Run. |
| `tests/integration/api/run-file-changes-api.integration.test.ts` | Needs Update | Existing seeded absolute PNG content proves API serving, but not converter→projection→API on same generated image. Add connected integration case. |
| `tests/unit/.../agy-run-capsule.test.ts`, `agy-configured-skill-materializer.test.ts`, `tests/unit/skills/services/skill-service.test.ts` | Still Valid | Missing/invalid/valid/safety and restore-v1 fixtures. Run; no obsolete invalid-hard-fail test remains. |
| `tests/unit/.../agy-production-live.test.ts`, `agy-mcp-team-live.test.ts` | Needs Update / selective use | Gated live tests use artificial profile and old output paths; build a focused current-profile validation rather than infer full parity. |
| `tests/e2e/runtime/agy-team-inter-agent-roundtrip.e2e.test.ts` | Still Valid | Real Team/Org transport journey, but native exclusion remains only profile assertion unless model-exposed queried. Run if provider available. |
| `tests/e2e/agent-definitions/agent-package-private-skills.e2e.test.ts` | Still Valid | Package discovery; does not establish live first reply. Run. |
- Stale/removal: none identified. No existing scenario should be deleted.

## Coverage changes and execution plan
- Add one narrow durable integration scenario to `tests/integration/api/run-file-changes-api.integration.test.ts` connecting actual AGY converter event→shared file projection→REST content, proving verified bytes/path and rejecting failed event; fixtures synthetic, not live provider evidence.
- Use temporary real-provider executable probe for 1.2.11 model-exposed profile, native image event/output, scoped MCP Team/Org, and bundled Codex first reply. A provider/account-dependent and potentially costly image generation probe should not run in default CI; retain evidence and consider opt-in durable promotion after schema is observed.
- Run focused unit tests, new integration, package validation, source typecheck, then selective live AGY probe; decide browser need after real transport/public-view evidence. Failure/redaction edge coverage remains durable unit plus live transport if feasible.
- Cases: API-CASE-001 repository policy/profile; 002 image event→Files API; 003 skill content/safety/restore and package; 004 real AGY native tool exposure/MCP; 005 real native image and Files; 006 real Codex first turn; 007 public/private failure redaction and browser/transport; 008 Team/Org collaboration and non-regression.

## Initial confidence and broader-validation gate
- Repository execution pending. No score or Pass inferred from source review. Post-repository mandatory seven-category scorecard will be recorded after checks.
- Broader validation provisionally Required: external provider schema, actual model-exposed profile, native image bytes, first reply and UI/transport failure safety cannot be proved by mocks. Selected modes: AGY CLI plus in-process Live API/WebSocket; browser if web-equivalent surface remains uncertain. Electron shell is not changed, so actual desktop launch is not planned.
- Reroute trigger: provider behavior contradicting SR-015 native tool/image design → Design Impact, not MCP fallback; source/test defect → preliminary Local Fix to Code Reviewer for origin review.

## Executed coverage update (authoritative API-REV-001)
The initial plan above was completed through the first decisive real-provider failures. This update supersedes provisional expectations; do not infer AC-001/003/005/006 from repository fixtures.

| Order | Exact command or check | Result | Evidence |
| --- | --- | --- | --- |
| 1 | `cd autobyteus-server-ts && ./node_modules/.bin/vitest run tests/unit/runtime-management/antigravity-cli-capability.test.ts tests/unit/agent-execution/backends/antigravity/agy-native-tool-policy.test.ts tests/unit/agent-execution/backends/antigravity/agy-stream-event-converter.test.ts tests/unit/agent-execution/events/file-change-event-processor.test.ts --no-watch` | Pass, 36 | `api-e2e-evidence/case001.log` |
| 2 | `cd autobyteus-server-ts && ./node_modules/.bin/vitest run tests/integration/api/run-file-changes-api.integration.test.ts --no-watch` | Pass, 6 after fixture repair | `api-e2e-evidence/case002.log` |
| 3 | `cd autobyteus-server-ts && ./node_modules/.bin/vitest run tests/unit/agent-execution/backends/antigravity/agy-configured-skill-materializer.test.ts tests/unit/agent-execution/backends/antigravity/agy-run-capsule.test.ts tests/unit/agent-execution/backends/antigravity/agy-native-image-diagnostic-sink.test.ts tests/unit/skills/services/skill-service.test.ts --no-watch` | Pass, 77; 1 existing skip | `api-e2e-evidence/case003.log` |
| 4 | `cd autobyteus-server-ts && ./node_modules/.bin/vitest run tests/e2e/agent-definitions/agent-package-private-skills.e2e.test.ts --no-watch`; `./node_modules/.bin/tsc -p tsconfig.build.json --noEmit`; bundled package link/file audit | Pass, 4; Pass; 32 files, zero symlink/missing relative references | `api-e2e-evidence/package-e2e.log`, `source-typecheck.log` |
| 5 | `RUN_AGY_CAPABILITY_E2E=1 vitest run tests/e2e/runtime/agy-native-image-codex-skill.e2e.test.ts --no-watch -t 'real AGY 1.2.11 native'` (from server) | Fail actual provider | `api-e2e-evidence/live-image.log`, `real-native-image.json` |
| 6 | Same opt-in file, `-t 'bundled Codex skill'` | Fail actual provider | `api-e2e-evidence/live-codex.log`, `real-codex-skill.json` |
| 7 | Same opt-in file, `-t 'narrow diagnostic profile'` | Fail adapter acceptance after genuine provider image | `api-e2e-evidence/diagnostic-image.log`, `diagnostic-native-image.json`, `diagnostic-native-image-file.json` |
| 8 | Same opt-in file, `-t 'narrow diagnostic native profile'` with `AGY_CODEX_PACKAGE_ROOT` and `AGY_CAPABILITY_EVIDENCE_DIR` | Pass isolation only; bundled skill read and first reply | `api-e2e-evidence/diagnostic-codex.log`, `diagnostic-codex-skill.json` |

The documented `pnpm` executable was unavailable in this shell; the installed local Vitest/tsc binaries were used. Shared workspace TS packages were built using their local `tsc -p tsconfig.build.json` binaries before execution; two new untracked build directories were cleaned after checks. Initial API suite failures exposed a stale test fixture: it seeded obsolete Team v1 metadata after the current V2 clean-cut. The test still represents valid historical Team Files behavior, so its fixture was updated to the current `testExecutionTree` and owned process manager setup, not removed. New durable image API scenario now connects AGY converter→file-change processor→GraphQL/REST exact image bytes, but its synthetic provider output is not evidence of the real provider schema. No compatibility-only coverage was retained.

### Post-repository confidence scorecard
| Category | Score | Support / limitation |
| --- | ---: | --- |
| Requirements and AC proof | 75% | Mocked AC-001/002/004/005 and package contract; real critical journeys pending. |
| Changed-boundary execution directness | 75% | Real API/processor exercised, AGY provider only simulated. |
| Cross-boundary integration realism/mock gap | 75% | API Files chain real but input synthetic; no provider stream. |
| Environment/configuration/identity/fixture fidelity | 75% | Isolated app-data and current CLI installed; model turn not run yet. |
| Failure/edge/lifecycle/recovery | 90% | Strong safe-failure, provenance, collision and restore unit checks; live failure pending. |
| User-surface/browser/desktop | 75% | GraphQL/REST bytes, no chat/Files UI or WebSocket/browser. |
| Durable regression quality/relevance | 90% | Focused boundary tests and current fixture; opt-in live test added. |
- Overall: **79.3%**, simple seven-category mean; critical ACs not directly proved, categories below 90. Broader validation **Required**, selected live AGY CLI plus API; browser conditional on a working provider turn.

### Real-provider discovery and coverage decision
- Installed CLI version **1.2.11**; `agy models` returned `gemini-3.8-flash-low`. Full production capsule's 49-name native policy gives a 57-name `init.tools` registry inventory but the first turn exits code 3/ERROR: **25 configured non-collaboration names are reported `unknown component: tool ... not found in registry`**. `init.tools` is not evidence of custom-agent model exposure. Both image and Codex full-profile runs fail before any tool call or reply. This directly fails AC-001/003/005 and prevents AC-006 live proof; separately scoped MCP cannot rescue it.
- A diagnostic-only five-name profile (not production fallback) successfully invoked provider-native `tool_name: generate_image`, state ACTIVE→DONE and turn SUCCESS. A genuine JPEG exists under the AGY brain directory. However the DONE step has `tool_info` fields `name` and `parameters` only; **no output/result/path field anywhere on the step**. The absolute image path appears in the model's final response prose/link. The SR-015 adapter's explicit-output-only rule correctly emits no successful image artifact; Files projection therefore cannot index this actual provider result. Four genuine diagnostic runs observed the same no-output shape. This directly fails AC-001 and is a **Design Impact** against DS-002's premise, not justification for unapproved generic prose scraping or MCP fallback.
- Diagnostic-only Codex under the five-name profile snapshots the actual bundled workflow skill, uses native `view_file` to read it, and replies with its Stage 0 title. Thus package/skill payload is viable, while production full-profile first turn remains failed.
- Real public/private image-failure WebSocket ACK/history/browser, safe native denial, actual production Team/Org MCP coexistence/exclusion, and true missing/invalid skill first turn are **Not Tested** after the upstream profile/image failures. Repository semantic/safety tests are valid but not equivalent to live proof. Do not interpret skipped later cases as Pass.

### Final investigation decision
- Proceeded to executable checks: Yes. Durable coverage added: opt-in AGY capability E2E test; updated: run-file-changes API integration/Team fixture; removed: none.
- Final broader-validation status: Required and partially executed; further browser/Team/Org journeys deferred until design and implementation recovery, not a permission/environment Blocked result.
- Final confidence: **75.0%** (see execution report), below gate; critical AC-001/003/005 not met.
- Reroute: **Yes — preliminary Design Impact**, specifically unsupported assumed custom-agent full profile and no explicit provider image output. Code Reviewer should confirm failure origin before Solution Designer recovery. No runtime source fix or fallback was made here.
