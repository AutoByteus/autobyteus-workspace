# User Verification Handoff — AGY native invocation and Codex startup (DR-005)

## State
- Ticket `agy-runtime-image-codex-prep-20260926`; `task_size=Medium`, `architectural_risk=High`, independent architecture/source/test-code review route.
- **User verification received.** The user stated on 2026-09-26: “the task is done. i tested. lets finalize. no need to release a new version”. This authorizes repository finalization only; no version, tag, release, publication or deployment. Package target `main` is protected and currently requires independent GitHub PR approval, so full delivery remains open.
- Finalization target from bootstrap: server workspace `origin/personal`; separate `autobyteus-agents` package `origin/main`. Server task branch integrated latest post-verification `origin/personal@cf005d377` as `cf0e0e7e`; package branch `a140474819b9cc7a3311ab52f39d74114a694655`.

## Approved outcome to verify
1. In a new AutoByteus AGY 1.2.11 run, the native `generate_image` invocation is displayed as one correlated STARTED→SUCCEEDED (or truthful failed/denied) tool lifecycle, with an ordinary assistant reply. This is **not** app MCP `generate_image`/`call_mcp_tool` and does not promise image bytes, path, Files, preview, or retention in AutoByteus.
2. Exactly eight native E-048 names are granted in new AGY capsules; configured MCP remains separately scoped and AGY-native collaboration/messaging is excluded. Other CLI versions fail compatibility validation rather than inheriting the 1.2.11 profile.
3. New AGY Codex first turn can read the bundled `software-engineering-workflow-skill` from the selected current agent package; missing or semantically invalid configured skills warn/omit, while unsafe source/collision/source mutation blocks safely.
4. Provider image and terminal failures show safe public status, keeping private details in bounded restricted diagnostics.

## Evidence and limits
- Approved requirements: `requirements-doc.md` SR-021/E-055; design: `design-spec.md` SR-023; history: `solution-revision-record.md`.
- ARCH-REV-008 Pass in `design-review-report.md`/`architecture-review-revision-record.md`; IR-005 source `4fe185502` and handoff `ca801db64` in `implementation-handoff.md`/`implementation-revision-record.md`; CRR-005 source Pass and CRR-007 test Pass in `code-review-report.md`, `api-e2e-test-review-report.md`, and `code-review-revision-record.md`.
- API-REV-004 Pass/95.0% in `api-e2e-execution-coverage-report.md` and `api-e2e-revision-record.md`: separately running worktree backend/Nuxt, real Chrome UI package import, selected `a140474` Codex skill source, one green native `generate_image` card with pathless DONE and ordinary reply, then fresh Codex first turn reading the skill. See `api-e2e-evidence/api-rev-004/browser-observation.md`, `real-run-trace-summary.json`, `real-run-projection.json`, and `real-skill-first-turn.json`. API-REV-003 focused real-app assertion pass and API-REV-002 Team/Org/failure/safety checks remain carried, not rerun in the browser. CRR-008 test-code review is Not Applicable because API-REV-004 changed no durable test/fixture; CRR-007 remains the seven-path Pass. No production source changed in API/E2E.
- Delivery first integrated `origin/personal@fc2a60527` via `ee0e2c313` after local safety checkpoint `98922d6a8`. Following acceptance, `origin/personal` had one unrelated ticket-doc commit `cf005d377`, merged as `cf0e0e7e`; four focused AGY suites reran with 36 pass/1 preexisting skip. This did not materially change the verified user-facing state, so renewed user verification was not required. `delivery-integration-check.md` records the initial 5 API integration/36 AGY unit passes; the final reintegration log is `delivery-final-reintegration-test.log`.
- Current separate package branch is `a140474`; its `origin/main@1b1a75e` base was already current. The real-app E2E and new browser UI import both selected that root and verified skill bytes/source identity. An ambient older `autobyteus-agents` root can shadow it; deployment must select the integrated current bundle and verify selected definition `sourceInfo`, not merely the server commit.
- Residual: observed AGY 1.2.11 and one model/account; provider/version drift and an untested installation remain rollout risks. No Electron shell or direct production deployment is claimed. API-REV-004 initially encountered an inherited `DATABASE_URL` lock before readiness, then explicitly isolated `APP_ENV=test` and SQLite in `/tmp`; no production-data mutation is claimed. Its backend/frontend loopback services at 127.0.0.1:18080/13080 were live at the API-REV-004 observation but no longer listen after the reported power-off; the written/browser evidence remains historical, not a current live endpoint.

## Local Electron test build and retest hold
The first DR-003 App was **unusable**: a user blank-screen report was reproduced, and the packaged HTML referenced missing development-only Vite assets. The earlier backend-readiness smoke did not catch renderer failure. Delivery cleaned Nuxt generated output and reran the full README macOS ARM64 build; the rebuilt packaged ASAR now references only present production entry assets. Exact new App/DMG/ZIP paths and checksum are in `delivery-electron-build-report.md`. This is an unsigned/unnotarized local artifact, not a release. At the user's request Delivery has not relaunched the rebuilt GUI; **please retest it**. The existing installed `/Applications/AutoByteus.app` and its data were not changed. Since that older installed app is currently running, use the ticket's isolated `launch-electron-manual-test.sh` to start the rebuilt executable directly; a generic `open AutoByteus.app` may activate the existing app instead. The current Codex package root remains the separate `a140474` worktree.

## Verification and next gates
The user explicitly tested and accepted the rebuilt Electron task, asking for **repository finalization only**. No release notes, version, tag or deployment are authorized. Server finalization is proceeding on the latest checked base. The separate package branch is pushed and [PR #14](https://github.com/AutoByteus/autobyteus-agents/pull/14) targets protected `main`; GitHub currently reports `REVIEW_REQUIRED`. Full terminal completion must wait for its required independent approval/merge. No migration or image-file cleanup is required.

## Authoritative delivery artifacts
`docs-sync-report.md`, `delivery-integration-check.md`, `delivery-electron-build-report.md`, `delivery-revision-record.md`, `release-deployment-report.md`, and `delivery-final-reintegration-test.log` in this ticket folder. All relative paths in this handoff resolve under `/Users/normy/autobyteus-org/autobyteus-task-worktrees/agy-runtime-capabilities-20260926/tickets/done/agy-runtime-image-codex-prep-20260926/`.
