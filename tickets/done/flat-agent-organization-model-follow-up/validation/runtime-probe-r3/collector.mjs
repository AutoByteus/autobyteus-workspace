import http from 'node:http';import fs from 'node:fs';import {fileURLToPath} from 'node:url';
const log=fileURLToPath(new URL('./browser-observations.jsonl',import.meta.url));
const server=http.createServer((req,res)=>{res.setHeader('Access-Control-Allow-Origin','http://127.0.0.1:50381');res.setHeader('Access-Control-Allow-Headers','content-type');if(req.method==='OPTIONS'){res.writeHead(204);res.end();return;}if(req.method!=='POST'||req.url!=='/observe'){res.writeHead(404);res.end();return;}let body='';req.on('data',c=>{body+=c;if(body.length>2000000)req.destroy();});req.on('end',()=>{try{const v=JSON.parse(body);fs.appendFileSync(log,JSON.stringify(v)+'\n');res.writeHead(204);res.end();}catch{res.writeHead(400);res.end();}});});server.listen(50382,'127.0.0.1',()=>console.log('OBSERVER READY 50382',process.pid));
