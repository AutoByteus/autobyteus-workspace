# Investigation Notes

## Investigation Status

- Bootstrap Status: `Complete`; dedicated branch/worktree created from freshly fetched `origin/personal`.
- Current Status: `Recursive Memory Compactor self-compaction and ownership investigation complete; ARCH-REV-006 passed SR-006; full REQ-001–REQ-017 basis approved; SR-008 supersedes SR-007's bare null-runner guard with a bounded memory-owned automatic-compaction configuration while preserving the approved response/storage, Unicode, retry, threshold, strategy, and commit contracts; package ready for architecture re-review`.
- Investigation Goal: determine why the Daily Assistant's compactions failed or repeated, distinguish model task confusion, invalid output, runner/provider failure, and trigger-planning defects, identify the minimum safe output and lifecycle contracts, and establish migration/refactor posture.
- Scope Classification (`Small`/`Medium`/`Large`): `Medium`.
- Scope Classification Rationale: the defect is bounded to memory compaction but crosses prompt composition, child-run execution, parsing, lifecycle reporting, atomic persistence, lineage compatibility, documentation, and executable coverage.
- Scope Summary: task/evidence framing, processed compactor prompt relevance, transient output schema, schema-aware parsing, one bounded correction, trigger-aligned planning, runner-versus-response failure ownership, provider-safe Unicode rendering, fail-closed user-initiated recovery, authoritative turn origin, queued non-user preservation, memory-owned automatic-compaction composition, non-recursive built-in compactor execution, safe failure, tool authority, stored-data compatibility, and current memory projection.
- Primary Questions Resolved:
  1. What did the compactor actually output? It continued the source CER/Vue task twice and emitted `run_bash` DSML; it did not output compacted memory.
  2. Did surrounding prose alone break parsing? No. Current code and probes accept prose/fences around a valid object.
  3. Should the six-array structure change? No in this task. Although downstream storage does not mechanically require six parallel arrays, the user explicitly chose to preserve the repeatedly tested original response section because it is clear and already reliable when the model selects the correct task.
  4. Should the compactor receive `write_file`? No for this fix; the existing compactor is intentionally tool-free, and generic workspace access would enlarge authority without fixing task confusion.
  5. Is migration required? No. The response is transient and maps into existing canonical typed memory.
  6. Why did changing 80% to 20% create three rapid successful compactions? The trigger moved to 123,148 tokens but planning still retained against a fixed 35% of the full input budget, so the first two successful operations remained above the trigger.
  7. Why did the later compaction fail twice after `continue`? Four child LLM requests failed before usable output; their generated error text was misclassified as invalid JSON, and the unchanged pending parent operation reran on the next turn.
  8. Why was the input budget 615,744? The selected `deepseek-v4-flash` metadata advertises a 1,000,000-token total context and a 384,000-token maximum output, no smaller input-only cap was known, and the runtime subtracted a 256-token safety margin: `1,000,000 - 384,000 - 256 = 615,744`.
  9. What should happen after a genuine compactor runner/provider failure? The user approved strict fail-closed behavior: report it, stop the current target-agent turn, retain the pending compaction, perform no automatic correction/provider retry, and let a later user-authored message such as `continue` explicitly initiate one new attempt before target-agent dispatch.
  10. What should happen if another agent or a system task sends input while that user-retry gate is active? It must not authorize a retry or be lost. Keep it unclaimed in the existing turn-start queue, allow the earliest USER entry behind it to authorize one attempt, and resume ordinary FIFO only after that user turn succeeds and settles. The initial attempt for a newly requested operation remains automatic.
  11. Why did the 2026-08-15 20% reproduction fail before inference? `ReadableValueRenderer.omitMiddle` split valid `🛡️` at a UTF-16 surrogate boundary while rendering a 2,000-character tool result. The derived child prompt contained lone U+D83D, so DeepSeek's HTTP layer rejected `messages[1].content`. Preserve raw traces and make only the provider-facing copy Unicode/control safe.
  12. Why did the successful retry show a much smaller task with two START/END pairs? The outer built-in Memory Compactor was provisioned like an ordinary agent and applied the global 20% policy to its own 176,655-token prompt. It launched a nested compactor over its single already-wrapped user task, whose 540,257 characters were shortened to 2,430 by per-value rendering. The correct outcome is a non-compactable leaf; the approved design reaches it by cleaning up the fragmented policy/runner composition rather than treating runner nullability as an accidental capability flag.
  13. How much compaction-policy/strategy architecture is needed? One current policy and one current strategy implementation are sufficient. `CompactionPolicy` answers whether an observed normal-agent prompt has `none`, `proactive`, or `hard_input_cap` pressure. The existing strategy registry and current `structured-json` strategy answer how to perform the operation. Agent creation selects one discriminated memory-owned configuration: disabled, or enabled with that policy and the current strategy runner. No additional policy subclasses, controller hierarchy, or chunking strategy is justified by current evidence.

## Request Context

The user reports that compaction is production-critical and asks why the Daily Assistant run failed. They suspect brittle JSON (“adjacent”) requirements, ask whether commentary breaks parsing, question whether highly structured output is necessary, and consider file writing as an alternative. They then identify the crucial behavioral clue: earlier compaction looked correct, while later compactor output was strange and appeared to misunderstand the conversation history instead of returning compacted memory. On clarification, the user rejected adding a hierarchy of XML tags: keep one history block and its rendered content, make only a few wording refinements in the otherwise preserved `agent.md` so it explicitly describes summarizing a target agent's conversation history, and add only a concise user-message identification plus a visible plain-text start/end boundary. After reviewing the exact generated prompt, the user found its overall section order acceptable and characterized the original `agent.md` as approximately 98–99% correct; no prohibitive instruction about continuing/performing actions, removal/reordering of common sections, or post-history task restatement is wanted. After comparing a proposed `items` contract with the original response section, the user explicitly chose the original six-array response instructions unchanged because their detailed field explanations and exact example are already clear and repeatedly tested. The user then approved renaming the sole generic outer tag to `<target_agent_conversation_history>` for an explicit target-agent boundary, without adding a second/nested wrapper.

User verification of the built Electron package later established two additional supported behaviors. First, changing the live ratio from 80% to 20% caused three successful operations in one parent turn before the prompt fell below the new threshold. Second, after the prompt legitimately grew back to the 20% threshold, two visible failures occurred: each parent execution launched an initial and correction child even though all four child LLM requests failed before usable assistant output.

After distinguishing a typed runner failure from invalid model output, the user chose a strict availability policy: compaction is important enough that a final compaction failure must stop the current target-agent turn even when the prompt is below the hard input limit. The user will send a later `continue` message after the provider recovers. That new message is an explicit retry signal, not permission for same-turn or background retries.

Architecture review round 3 then established that this policy needed an origin boundary, not only a pending-presence check. The user approved the simplest resolution: authoritative USER origin alone may retry; agent/system messages remain in the existing queue without starting a turn, invoking the provider, or being dropped. On successful user retry the user turn proceeds first and retained non-user work resumes afterward; failure leaves it queued. The agent runtime remains alive throughout, so this is a turn gate rather than agent shutdown.

On 2026-08-15 the user again changed a live 371,352-token parent prompt from 80% to 20%. Compaction correctly triggered, but the child request was rejected with HTTP 400 `unexpected end of hex escape`. Exact trace correlation proved a local deterministic defect rather than context overflow or provider downtime: middle omission split the shield emoji in a parent tool result, creating a lone surrogate only in the derived compactor prompt. The user approved a reasonable safety correction and agreed that provider-facing conversation history should be made safe while raw source traces remain unchanged. Valid multilingual text and valid emoji are retained; the design does not reduce content to ASCII.

After the Unicode-safe retry, the user inspected a new Memory Compactor task and found two START/END pairs plus unexpectedly little content. Exact run correlation showed that the intended outer compactor had already produced its response but then compacted itself because the server attached the normal runner and the global 20% trigger applied to its 176,655-token prompt. The nested child received the outer task as one trace, shortened it under the normal 2,000-character per-value renderer, and added its own wrapper. The user agreed that the built-in Memory Compactor must be treated specially as a non-compactable leaf rather than sharing the target-agent compaction policy. The original parent operation nevertheless committed successfully and the next actual parent observation was 73,102 tokens; no evidence indicates migration damage or canonical-memory loss.

The user then clarified the desired ownership rather than accepting a one-off null check as the final architecture. Memory owns compaction as a submodule. The current single `CompactionPolicy` answers whether automatic compaction should happen for a usage observation; the current pluggable strategy boundary answers how it is performed. Agent creation supplies the composition. Normal server agents receive the existing enabled default, while the Memory Compactor receives disabled automatic compaction and uses provider/model request capacity rather than the target agent's 20% trigger. The user explicitly asked for this refactor now but rejected overengineering: no speculative ratio/capacity policy classes, second strategy, new controller hierarchy, or multi-pass algorithm is in scope.

In the discussion immediately before this reproduction, the user articulated a broader product direction: conversation-history compaction may ultimately be a standalone unstructured continuation-summary capability, while episodic/semantic memory is distilled separately from raw/work traces. That separation is architecturally credible, but the user did not explicitly authorize replacing the currently implemented six-array contract in this Unicode-fix round. SR-005 therefore records the narrow proven input-safety defect and preserves the current approved implementation basis. A clean-cut compaction-versus-memory redesign should be handled as a separate requirement/design revision if the user explicitly activates it.

The supplied screenshot shows two distinct visible errors in sequence:

1. ordinary Daily Assistant call: `Tool 'write_file' not found or configured...`; and
2. compaction lifecycle: `Memory compaction failed before dispatch: Could not parse a valid JSON object from the compaction response.`

## Environment Discovery / Bootstrap Context

- Project Type (`Git`/`Non-Git`): `Git` super-repository.
- Task Workspace Root: `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness`.
- Task Artifact Folder: `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness`.
- Current Branch: `codex/compaction-response-robustness`.
- Current Worktree / Working Directory: `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness`.
- Bootstrap Base Branch: freshly fetched `origin/personal` at `54890a07f74e941a7a12b6daaa26364f4c927b72`.
- Remote Refresh Result: `git fetch --prune origin` succeeded on 2026-08-14; `origin/HEAD` resolves to `origin/personal`.
- Task Branch: `codex/compaction-response-robustness`, tracking `origin/personal`.
- Expected Base Branch (if known): `origin/personal`.
- Expected Finalization Target (if known): `personal`.
- Bootstrap Blockers: None.
- Notes For Downstream Agents: older compaction worktrees were stale and/or dirty and were intentionally not reused. The requirements basis and behavior-defining supplements were approved by the user on 2026-08-14/15. Implementation must use the exact prompt text from `memory-compactor-prompt-spec.md`, must not paraphrase it, and must apply the narrow derived-copy Unicode boundary from `compaction-unicode-safety-analysis.md` without mutating source traces.

## Supplemental Task Artifact Inventory

| Artifact Path | Purpose And Scope | Evidence, Context, Or Decision Captured | Core Artifact(s) Supported | Related Requirement / Acceptance-Criteria IDs (When Applicable) | Status | Approval Applicability / State | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/memory-compactor-prompt-spec.md` | Exact intended prompt wording | Full target-agent `agent.md`, detailed per-kind response semantics, complete valid JSON example, minimal final user-message separator shape, global sender-heading cleanup effect, and explicit preserved behavior | Requirements; design spec | REQ-001, REQ-002; AC-001–AC-003 | Approved wording authority | User approved 2026-08-14 | Implementation must apply without paraphrase |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/prompt-confusion-root-cause.md` | Final-prompt anatomy and causal analysis | `[User Requirement]` wrapping, recurrent continuation collision, tool-tail cue, generic Bash context, earlier-success comparison, prompt-structure principles | Requirements; investigation; later design spec | REQ-001; AC-001, AC-002 | Verified evidence/context | N/A | Use as root-cause authority in design |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/compaction-output-contract-decision.md` | Intended-behavior contract and option matrix | Preserved six-array response, task/evidence boundary, one repair, rejection of arbitrary Markdown/generic tools, persistence posture | Requirements; design spec | REQ-001–REQ-010; AC-001–AC-013 | Approved behavior authority | User approved 2026-08-14 | Carry unchanged into design |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/daily-assistant-compaction-failure.png` | Durable user-supplied UI evidence | Visible separate `write_file` and compaction parse errors | Requirements; investigation | REQ-006, REQ-009; AC-009, AC-012 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/memory-compactor-user-requirement-view.png` | Durable user-supplied compactor-run screenshot | Shows the visible `[User Requirement]` immediately followed by `<conversation_history>` and the recurrent “You are continuing...” content | Investigation; requirements; later design spec | REQ-001; AC-001 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/daily-assistant-server-log-excerpt.txt` | Focused server log excerpt around both failed attempts | Compaction threshold/operation, prompt, tool configuration, child IDs, failure metadata, repeated pending operation | Investigation; later design spec | REQ-001, REQ-005–REQ-009; AC-001, AC-008–AC-012 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/failed-compactor-final-system-prompt.md` | Exact final processed system prompt from the failed compactor run | Complete 5,466-character prompt actually configured on the model, including generated identity, workspace, Bash, and file-practice sections | Investigation; requirements; later design spec | REQ-001; AC-001 | Retained exact evidence | N/A | Compare against target processed prompt after implementation |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/failed-compactor-outputs.json` | Focused failed child-run traces | First/last prompt content and public assistant output; private reasoning excluded | Requirements; investigation; later design spec | REQ-001, REQ-003–REQ-007; AC-001, AC-002, AC-008–AC-010 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/successful-compactor-output-comparison.json` | Same-parent successful comparison | Earlier valid JSON outputs, input/output sizes, category counts, and lineage records | Requirements; investigation; later design spec | REQ-001, REQ-002, REQ-010; AC-001, AC-003, AC-013 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/parser-tolerance-probe.jsonl` | Executed current-parser probe | Exact/fenced/prose success; unrelated-first-object failure; both actual failures | Requirements; investigation; later design spec | REQ-003, REQ-004; AC-004–AC-007 | Retained evidence | N/A | Re-run/extend after implementation |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/repeated-compaction-runtime-analysis.md` | Trigger/selection runtime analysis | Exact 80%→20% threshold math, three-operation sequence, fixed 35% retention mismatch, forced-prefix fallback | Requirements; investigation; revised design | REQ-011, REQ-012; AC-014–AC-017 | Verified evidence/context | N/A | Incorporated into the revised design; architecture re-review pending |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/compactor-runner-failure-analysis.md` | Runner/repair/re-entry analysis | Four pre-response failures, missing token usage, error-text parsing, inapplicable response correction, same pending operation retry, and corrected USER-only re-entry direction | Requirements; investigation; revised design | REQ-013–REQ-015; AC-018–AC-023 | Verified evidence/context | N/A | Aligned with SR-004; architecture re-review pending |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/compaction-runtime-behavior-examples.md` | Intended runtime behavior examples | Concrete correct/incorrect outcomes for trigger alignment, later threshold crossing, response validation, pre-response runner failure, user-only retry, queued non-user preservation, Unicode-safe rendering, non-recursive leaf execution, unattainable target, and preserved default behavior | Requirements; revised design | REQ-011–REQ-017; AC-014–AC-029 | Approved intended behavior | User approved 2026-08-14/15 | Use as scenario authority; keep implementation mechanics in design |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/compaction-memory-shape-reassessment.md` | Response/storage reassessment | Current host-owned commit path, verified lineage episode/semantic counts, current projection contribution, four simplification options, and the resolved preserve-existing-contract decision | Requirements; revised design | REQ-002, REQ-007–REQ-010; AC-003, AC-007–AC-013 | Resolved decision context/evidence | Evidence N/A; user decision recorded 2026-08-14 | Carry preserved six-array/tool-free/no-migration boundary into design |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/repeated-compaction-at-20-percent.png` | User-visible repeated-success evidence | Three rapid completed/active compaction cards after the supported 80%→20% setting change | Requirements; investigation; revised design | REQ-011, REQ-012; AC-014–AC-017 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/repeated-compaction-server-log-excerpt.txt` | Corrected focused exact runtime excerpt | Operations `mssvv6cl_1`, `mssvwf3s_2`, `mssvxn2c_3`; prompt 249,416→243,153→242,812→8,755; compactor prompt lengths 17,849, 3,628, and 372,840; exact plan/result metadata | Investigation; revised design | REQ-011, REQ-012; AC-014–AC-017 | Retained evidence; corrected for AR-FIND-002 | N/A | Revalidated directly: three operation IDs, four prompt observations, and three child prompt lengths; SHA-256 `adc1c471f487ad1aee1ffe4e6176fdd70603218b0dadff39c09af439134148cf` |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compactor-provider-failure-and-repeat.png` | User-visible provider/runner failure evidence | Two later compaction failures after the legitimate threshold crossing plus Token Meter context | Requirements; investigation; revised design | REQ-013, REQ-014; AC-018–AC-021 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compactor-runner-failure-evidence.json` | Correlated parent/child failure summary | Four request-recovery-only children, no usable assistant trace/token usage, repeated pending operation, and retained child IDs | Investigation; revised design | REQ-013, REQ-014; AC-018–AC-021 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/compaction-unicode-safety-analysis.md` | Unicode request-rejection analysis and intended safety boundary | Exact valid-source-to-lone-surrogate causal chain, derived-copy sanitization/truncation boundary, direct coverage, and no-migration posture | Requirements; investigation; revised design | REQ-016; AC-024–AC-026 | Approved intended behavior | User approved reasonable safety fix 2026-08-15 | Implement without ASCII conversion or source mutation after architecture pass |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compaction-unicode-request-rejection.png` | User-visible Unicode request-rejection evidence | Correct `runner_error_completion`, exact DeepSeek HTTP 400, and 371,352-token parent prompt | Requirements; investigation; revised design | REQ-016; AC-024, AC-026 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compaction-unicode-request-rejection-log.txt` | Focused exact runtime excerpt | Correct trigger/target, child prompt length, one request rejection, typed failure propagation, and parent turn stop | Investigation; revised design | REQ-013, REQ-016; AC-018, AC-024–AC-026 | Retained evidence | N/A | Re-run exact source after implementation |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compaction-unicode-truncation-proof.json` | Machine-readable Unicode proof | Original valid `🛡️`, source trace identity, derived lone U+D83D, reconstructed request offset, and root-cause classification | Investigation; revised design | REQ-016; AC-024–AC-026 | Retained evidence | N/A | Use as deterministic fixture source |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/recursive-compaction-root-cause.md` | Recursive child-run analysis and intended leaf boundary | Exact parent/outer/nested lifecycle, shortened double-wrapper explanation, correctness impact, rejected alternatives, direct coverage, and no-migration posture | Requirements; investigation; revised design | REQ-017; AC-027–AC-029 | Approved intended behavior | User approved specialized leaf outcome 2026-08-15 | Implement after architecture pass; keep normal agent policy unchanged |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/recursive-memory-compactor-ui.png` | User-visible recursive-compactor evidence | Small nested task with two target-agent wrapper/separator pairs | Requirements; investigation; revised design | REQ-017; AC-027, AC-028 | Retained evidence | N/A | None |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/recursive-memory-compactor-server-log-excerpt.txt` | Focused exact parent/outer/nested execution excerpt | Daily Assistant operation, 176,655-token outer self-trigger, nested child, intended parent commit, and 73,102-token actual reset | Investigation; revised design | REQ-017; AC-027–AC-029 | Retained evidence | N/A | Re-run equivalent high-child-usage scenario after implementation |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/recursive-memory-compactor-proof.json` | Machine-readable recursive-run proof | Exact run/task/operation IDs, prompt sizes, wrapper counts, source hashes, and expected capability boundary | Investigation; revised design | REQ-017; AC-027–AC-029 | Retained evidence | N/A | Use as deterministic fixture/reference |

## Source Log

| Date | Source Type (`Code`/`Doc`/`Spec`/`Web`/`Repo`/`Issue`/`Command`/`Trace`/`Log`/`Data`/`Setup`/`Other`) | Exact Source / Query / Command | Why Consulted | Relevant Findings | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-14 | Setup | `git fetch --prune origin`; `git symbolic-ref refs/remotes/origin/HEAD`; `git worktree add -b codex/compaction-response-robustness ... origin/personal` | Establish current isolated baseline | Fetch succeeded; base `54890a07f`; dedicated worktree created | No |
| 2026-08-14 | Other | User request and supplied `ctx_a4fd37c468a1__image.png`, retained as `evidence/daily-assistant-compaction-failure.png` | Establish reported behavior | Two distinct UI errors; screenshot alone did not expose raw compactor output | No |
| 2026-08-14 | Log | `/Users/normy/.autobyteus/server-data/logs/server.log`, focused lines 2608960–2609460 retained in `evidence/daily-assistant-server-log-excerpt.txt` | Correlate screenshot to runs and lifecycle | Parent/operation/strategy/model IDs found; two failed child runs; no tools; same pending operation; exact parse error | No |
| 2026-08-14 | Log | First `Final processed system prompt` block extracted verbatim from `evidence/daily-assistant-server-log-excerpt.txt` into `evidence/failed-compactor-final-system-prompt.md`; verified against logged `Final length: 5466` | Give the user and downstream reviewers the exact complete prompt received by the failed compactor model | Extracted file is exactly 5,466 characters and includes all generated sections, not only `agent.md` | Compare after implementation |
| 2026-08-14 | Trace | `/Users/normy/.autobyteus/server-data/memory/agents/memory_compactor_506ea3c502f34687a3f6004a08364364/raw_traces_active.jsonl` and `.../memory_compactor_e1f5d8b1cc884f66a4d4c9918639ba30/raw_traces_active.jsonl` | Inspect actual failed model requests and public output | Both 71,043-character histories were labeled `[User Requirement]`; both outputs resumed CER/Vue and emitted DSML `run_bash`; no JSON object | No |
| 2026-08-14 | Trace | `/Users/normy/.autobyteus/server-data/memory/agents/memory_compactor_da3d2793f9244876b0c9bc81e95e5be9/raw_traces_active.jsonl` and `.../memory_compactor_d305bed6091947f1984e2159f285e789/raw_traces_active.jsonl` | Compare earlier same-run compactions | Earlier 50,686/73,669-character inputs produced valid 10,672/7,071-character structured summaries | No |
| 2026-08-14 | Data | `/Users/normy/.autobyteus/server-data/memory/agents/daily_assistant_2a39c68eb96443ada6f5af9f4f81acef/{compaction_lineage.jsonl,episodic.jsonl,semantic.jsonl,working_context_snapshot.json,raw_traces_*}` | Verify actual persistence and successful lineage | Two successful lineage records use prompt contract v2; failed operation produced no third record; canonical output is separate from child response | No |
| 2026-08-14 | Code | `autobyteus-ts/src/memory/compaction/{working-context-compaction-prompt-builder.ts,compaction-conversation-history-renderer.ts,agent-compaction-summarizer.ts,compaction-response-parser.ts}` | Trace model request and parse boundary | Task builder returns only rendered history; renderer excludes system/reasoning and ends at closing tag; parser takes first parseable object then applies rigid schema | No |
| 2026-08-14 | Code | `autobyteus-server-ts/src/built-in-agents/templates/memory-compactor/{agent.md,agent-config.json}` and `autobyteus-server-ts/src/agent-execution/compaction/{server-compaction-agent-runner.ts,compaction-run-output-collector.ts}` | Verify compactor contract/tool authority/output collection | Prompt demands exact six-array JSON; config has zero tools; runner uses `autoExecuteTools:false`; collector captures public assistant completion and fails tool approval | No |
| 2026-08-14 | Code | `autobyteus-ts/src/memory/compaction/{structured-json-compaction-strategy.ts,compaction-result-normalizer.ts,accepted-compaction-builder.ts,accepted-compaction-committer.ts,pending-compaction-executor.ts}`; `memory-manager-compaction-coordinator.ts` | Trace proposal/commit/failure ownership | Parser output is normalized and proposed; manager verifies baseline/lineage; host assigns identities; commit owns ordered store mutation; failure precedes commit | No |
| 2026-08-14 | Code | `autobyteus-ts/src/memory/{projection/compacted-memory-message-builder.ts,models/semantic-item.ts,lineage/compaction-lineage-record.ts}` | Determine which structure downstream really needs | Final Markdown headings are host-derived; semantic categories drive storage/order/salience; lineage reader accepts prompt versions 1/2 only today | No |
| 2026-08-14 | Doc | `autobyteus-ts/docs/agent_memory_design_nodejs.md:292-316`; `autobyteus-server-ts/docs/modules/agent_memory.md:98-119` | Confirm documented current contract | Docs intentionally specify operation message as only history block and exact structured JSON; prompt contract current version is 2 | Update with implementation |
| 2026-08-14 | Spec | `tickets/done/pluggable-memory-compaction-strategies/working-context-compaction-domain-contract.md` | Determine universal vs strategy-specific contract | Generic strategy abstraction does not universally require JSON; structured JSON belongs to the production strategy | No |
| 2026-08-14 | Issue | `tickets/done/simplify-native-tool-continuation/api-e2e-execution-coverage-report.md` and related handoff/review records | Check prior known provider behavior | A previous live DeepSeek compactor returned invalid JSON once, then unchanged rerun passed; risk was retained as stochasticity | No |
| 2026-08-14 | Probe | Current built `CompactionResponseParser` invoked against exact, prose-wrapped, fenced, unrelated-object-first, and two captured failed outputs; results retained in `evidence/parser-tolerance-probe.jsonl` | Verify parser rather than infer behavior | First three parse; unrelated object masks later valid object; both real outputs have no parseable compaction object | Extend after implementation |
| 2026-08-14 | Command | `rg -n`, `nl -ba ... | sed -n`, `jq`, targeted `find`, and Python JSON extraction scripts over the sources above | Locate owners and retain focused evidence | Produced reproducible paths/counts without altering runtime data | No |
| 2026-08-14 | Code | `autobyteus-server-ts/src/agent-customization/processors/prompt/user-input-context-building-processor.ts`; `autobyteus-ts/src/agent/{pipelines/agent-input-pipeline.ts,loop/tool-continuation-input-builder.ts,llm-request-assembler.ts}` | Trace the global sender-heading path and verify active sender/tool semantics | The shared processor alone injects generic headings; sender metadata still drives turn rules, text-only tool continuation returns no added LLM message, context/media continuation remains self-describing, and provider-native tool messages remain canonical | Preserve active metadata; remove only shared heading formatting |
| 2026-08-14 | Code | `autobyteus-ts/src/agent/compaction/compaction-runtime-reporter.ts`; `autobyteus-ts/src/agent/loop/llm-phase.ts`; `autobyteus-ts/src/memory/compaction/working-context-compaction-strategy.ts` | Trace terminal lifecycle and diagnostic propagation | `PendingCompactionExecutor` is the sole parent-operation terminal status owner; strategy diagnostics currently carry only one child metadata record | Keep repair below executor; exhausted repair error must carry both available child run IDs while reporter may continue enriching the terminal event with the last attempt |
| 2026-08-14 | Code/Test/Doc | `autobyteus-ts/tests/{unit/memory,integration/agent}`; `autobyteus-server-ts/tests/{unit,integration,e2e}`; `autobyteus-{ts,server-ts}/docs` targeted by current tags/version/prompt text | Map durable coverage and documentation impact | Exact template, prompt tag, parser, single-attempt, lifecycle, lineage-v2, tool-free runner, and four memory architecture docs have direct expectations | Update proportionately after implementation; preserve unrelated historical ticket evidence |
| 2026-08-14 | Log/Trace | `server.log` operations `mssvv6cl_1`, `mssvwf3s_2`, `mssvxn2c_3`; corrected `evidence/repeated-compaction-server-log-excerpt.txt`; parent lineage; `message-budget-strategy.ts`; `working-context-message-window-planner.ts` | Explain rapid successful compactions after 80%→20% and resolve AR-FIND-002 | Corrected excerpt contains every claimed operation and prompt value exactly once per budget observation; old `mssvuhbz_1`/249,139 run is absent; fixed 35% recent suffix exceeds 20% trigger; fallback collapses 425 units | Complete; align planner and post-success episode with active trigger |
| 2026-08-14 | Log/Trace/Data | `server.log` operation `msswp70b_4`; four child `raw_traces_active.jsonl`; token ledger query; `compaction-run-output-collector.ts`; `llm-phase.ts`; `agent-compaction-summarizer.ts` | Explain two later failed cards | All four children contain only user + `llm_request_recovery`, no assistant trace or token usage; error text parsed as JSON failure; same pending operation reran on user `continue` | Typed runner failure plus approved fail-closed/manual retry required |
| 2026-08-14 | Code/Log/Data | Parent and child `run_metadata.json`; `autobyteus-ts/src/agent/token-budget.ts`; `autobyteus-ts/src/llm/{models.ts,supported-model-definitions.ts}`; `.env`; detailed budget logs | Explain the 615,744-token input budget and 12.4%-versus-20% display | `deepseek-v4-flash`, no run config/active-context override, 1,000,000 total context, 384,000 reserved output, no separate input cap, 256 safety margin; Token Meter and compaction use different denominators | Preserve calculation; consider UI threshold clarification separately |
| 2026-08-14 | Web/Spec | Official DeepSeek `https://api-docs.deepseek.com/api/create-chat-completion/` and `https://api-docs.deepseek.com/quick_start/pricing/` | Verify whether subtracting output allowance from total context is provider-contract-correct | Official API states input plus generated tokens are limited by context length; current model page lists 1M context and maximum 384K output | Treat full-output reservation as conservative when no smaller requested output cap exists |
| 2026-08-14 | Code | `autobyteus-ts/src/agent/loop/llm-phase.ts`; all `resolveTokenBudget`/`inputBudget` call sites | Determine whether the calculated budget admits/rejects one newly arriving huge user message before dispatch | Budget is resolved and passed to compaction planning; no general pre-dispatch tokenizer/admission/chunking gate was found for an arbitrary new message | Keep as explicit residual risk/future scope unless user expands this ticket |
| 2026-08-14 | Data/Code | Parent `compaction_lineage.jsonl`, `episodic.jsonl`, `semantic.jsonl`, `working_context_snapshot.json`; `compacted-memory-message-builder.ts`; accepted builder/committer | Reassess whether episodic/semantic and JSON are necessary and whether the child appends files directly | Five heads contain episode/semantic counts 4/25, 3/21, 2/18, 2/18, 5/0; current projection is episode-only. Model JSON is transient; host assigns canonical fields and commits. | User decision required; direct child writes rejected as unsafe |
| 2026-08-14 | Other | User approval after `compaction-memory-shape-reassessment.md` discussion | Resolve the reopened transient-response and canonical-memory decision | User explicitly selected the earlier solution: preserve the six-array JSON response, episodic/semantic memory, tolerant parser, exact approved prompt, host-owned commit, and tool-free compactor; resume REQ-011–REQ-014 design | Revise design and route to architecture review |
| 2026-08-14 | Review/Code | `design-review-report.md` (`ARCH-REV-003`, `AR-FIND-003`, `MP-002`); `autobyteus-ts/src/agent/{loop/agent-turn-runner.ts,pipelines/agent-input-pipeline.ts,llm-request-assembler.ts,event-inbox/agent-event-inbox-entry.ts,event-inbox/agent-event-inbox.ts,event-inbox/agent-event-scheduler.ts,event-inbox/handlers/turn-start-inbox-event-handler.ts,runtime/agent-worker.ts}` | Trace supported USER versus non-user turn admission into retained compaction | `InterAgentMessageReceivedEvent` and `UserMessageReceivedEvent` share `turn_start`; input conversion happens only after turn start; request assembly executes from pending presence. The FIFO scheduler currently claims only lane head, so leaving an ineligible AGENT at the head without predicate-aware selection would block a later USER retry. Existing store can preserve the entry while a narrow matching-claim extension selects the user. | Add authoritative origin, pending attempt state, and origin-aware scheduler admission |
| 2026-08-14 | Code | `autobyteus-server-ts/src/agent-team-execution/services/{inter-agent-message-router.ts,inter-agent-message-runtime-builders.ts}`; `autobyteus-server-ts/src/agent-communication/services/{global-agent-run-message-router.ts,global-agent-run-message-runtime-builders.ts}`; task-delegation notification builders | Validate non-user production representations beyond `InterAgentMessageReceivedEvent` | Team/direct agent messages commonly reach AutoByteus as `UserMessageReceivedEvent` containing `SenderType.AGENT`; task notifications use `SenderType.SYSTEM`. Event class alone cannot authorize retry; turn-start origin must use the original sender type before conversion. | Cover direct event, AGENT carrier, and SYSTEM carrier |
| 2026-08-14 | Other | User response to `ARCH-REV-003` clarification | Resolve AR-FIND-003 intended behavior | User approved no autonomous retry while provider may be down, runtime remains alive, later user `continue` may retry, and delegated the simplest reasonable non-user preservation design | Record REQ-015/AC-022–AC-023 and revise design |
| 2026-08-15 | Log | `/Users/normy/.autobyteus/server-data/logs/server.log:2647250-2647420`; child `memory_compactor_c7436219af804d759223edf4f4feef85`; parent `daily_assistant_2a39c68eb96443ada6f5af9f4f81acef` | Determine whether the new 20% error was expected | Trigger math was correct; child input was 540,727 UTF-16 units; DeepSeek rejected `messages[1].content` before inference; host correctly classified one `runner_error_completion` and created no JSON correction child | Trace exact invalid character and owner |
| 2026-08-15 | Code | `autobyteus-ts/src/memory/presentation/readable-value-renderer.ts`; `compaction-conversation-history-renderer.ts`; `compaction-policy.ts`; `compaction-response-parser.ts` | Trace provider-facing truncation boundaries | Default `maxItemChars=2000`; `omitMiddle` and response clamp use UTF-16 `slice` without surrogate-boundary protection; final fallback slice can repeat the defect | Add shared safe middle/end primitives and final prompt invariant |
| 2026-08-15 | Data | Python/Node probes over the exact parent/child traces, retained as `evidence/compaction-unicode-truncation-proof.json` | Prove local defect rather than infer from provider text | Original parent tool result contains valid `icon: '🛡️'`; derived child prompt contains exactly one lone U+D83D at index 31,434; reconstructed serialization places `\ud83d` near the provider-reported request location | Use captured source in direct regression coverage |
| 2026-08-15 | Other | User discussion after exact diagnosis | Resolve intended sanitization scope | User approved a reasonable safety solution and wants conversation history made as safe as possible. Raw traces remain authoritative; derived provider text may normalize malformed Unicode/control content. | Record REQ-016/AC-024–AC-026 and revise design |
| 2026-08-15 | Review | `design-review-report.md`; `architecture-review-revision-record.md` (`ARCH-REV-006`) | Resolve SR-005 evidence/oracle findings | Corrected evidence checksum and per-value/per-clamp oracle passed; substantive Unicode design is approved. | Use SR-006 as passed baseline for recursive defect revision |
| 2026-08-15 | UI/Log/Trace | User screenshot retained as `evidence/recursive-memory-compactor-ui.png`; `/Users/normy/.autobyteus/server-data/logs/server.log` focused excerpt; outer run `memory_compactor_6d151dd0cc8441d8881a2d684f86b454`; nested run `memory_compactor_08242efd53204df8b27dd4782347bd17` | Explain small task with double separators | Parent created outer child over 540,726 processed characters; outer prompt usage 176,655 exceeded 20% trigger 123,148 and requested self-compaction; nested renderer reduced the outer single trace to 2,430 characters and added another wrapper. Intended parent result still committed. | Add REQ-017 and leaf design |
| 2026-08-15 | Code | `autobyteus-server-ts/src/agent-execution/backends/autobyteus/autobyteus-agent-run-backend-factory.ts`; `autobyteus-server-ts/src/agent-execution/compaction/server-compaction-agent-runner.ts`; `autobyteus-ts/src/agent/{context/agent-config.ts,factory/agent-factory.ts,loop/llm-phase.ts,token-budget.ts}`; `autobyteus-ts/src/memory/{memory-manager.ts,policies/compaction-policy.ts,compaction/default-working-context-compaction-strategy-registry.ts,compaction/working-context-compaction-strategy-resolver.ts}` | Locate recursive-capability and ownership boundaries | Server factory constructs a runner for every AutoByteus definition and currently types the factory as `CompactionAgentRunner | null`; several tests inject null to bypass the subsystem. `AgentConfig` carries that nullable runner; `AgentFactory` separately and unconditionally creates `new CompactionPolicy()`; `LlmPhase` resolves capacity, mutates the policy, creates the strategy resolver/executor from the top-level runner, and evaluates after every usable response. Direct core/test construction of `AgentConfig` and `MemoryManager` is widespread and commonly omits compaction dependencies. There is exactly one policy class and the default registry has exactly one `structured-json` strategy registration. `MemoryManager` already owns the policy and the internal coordinator. | Replace the split fields/construction with one discriminated memory-compaction configuration supplied at agent creation and owned by `MemoryManager`; make omitted direct-core construction explicitly disabled, require non-null runner composition for normal server definitions, and retain the one policy, existing coordinator, registry, and strategy rather than adding a hierarchy |
| 2026-08-15 | Data/Command | `evidence/recursive-memory-compactor-proof.json`; `evidence/recursive-memory-compactor-server-log-excerpt.txt`; SHA-256/wrapper-count probes over outer/nested raw traces; LF-counted extraction from `server.log` | Retain reproducible proof | Outer task has one START/END pair and SHA `3a28b7c...`; nested task has two pairs, length 2,430, and SHA `75e60a...`. The focused verbatim log excerpt contains the exact parent/outer/nested IDs and directly recomputes to SHA-256 `d357fa1188b4518cc65985f9de9bde22d6f8f7487caaf219bc6857ed79f77681`. | Use exact facts in coverage; do not mutate runtime files |
| 2026-08-15 | Log | Repeated Prisma errors in `/Users/normy/.autobyteus/server-data/logs/server.log` for missing `token_usage_ledger_events.member_name` | Determine whether migration destroyed compaction data | Independent token-usage schema/startup mismatch causes ledger persistence errors and log noise, but is not on the recursive compaction or canonical-memory path. | Route as separate issue; no migration in this ticket |
| 2026-08-15 | Other | User architecture clarification after the recursive-run diagnosis | Resolve whether to apply only a null-runner guard or refactor compaction ownership | User approved the bounded refactor: memory manager owns the compaction subsystem/configuration; current policy decides whether; current strategy decides how; agent creation supplies enabled/disabled composition; no additional policy classes, strategy implementations, controller hierarchy, or chunking | Supersede SR-007 with SR-008 and route the complete package for architecture review |


## Relevant Existing Behavior And Production Paths

| Behavior ID | Kind (`User`/`System`/`Operational`/`Contract`) | Current Supported Trigger Or Governing Contract | Current Production Path And Lifecycle | Meaningful Current Outcome / Invariants | Evidence |
| --- | --- | --- | --- | --- | --- |
| BEH-001 | System | Parent token budget crosses automatic compaction threshold before next dispatch | implemented worktree baseline: neutral input composition -> explicit target-agent operation message -> zero-tool child runner | The original wrong-task ambiguity is corrected by the approved v3 prompt; user verification retained this prompt while exposing unrelated planning and runner failures. | Initial failed traces/prompt evidence; `ed7f65a5d`; exact prompt tests; later child traces |
| BEH-002 | Contract | Built-in `structured-json` strategy receives public assistant text | implemented worktree baseline: collector output -> exact/fence/balanced candidates -> validate all against six arrays -> select one semantic fingerprint -> `CompactionResult` | Commentary/fences/harmless extras and an unrelated object are tolerated; missing arrays, no episode, wrong entry shape, or distinct valid objects fail. | Parser source/tests; parser probe; `ed7f65a5d` |
| BEH-003 | Operational | Child output or execution fails during pending compaction | implemented worktree baseline: usable parse failure -> one new correction child -> success or one parent failure; any terminal executor failure leaves the pending request set | Genuine invalid output now has bounded repair, but pre-response child errors are transported as assistant text and enter that repair incorrectly; the later parent turn reruns the same pending operation. | Summarizer/executor/coordinator source; initial implementation tests; newest child/parent traces |
| BEH-004 | System/Contract | Strategy returns a valid normalized proposal and baseline remains current | strategy normalize -> manager prepare -> `AcceptedCompactionBuilder` assigns IDs/timestamps/salience and builds lineage/finalized context -> output validator -> `AcceptedCompactionCommitter`: archive raw traces -> add rows -> append lineage -> install context -> write snapshot -> clear pending | Canonical commit is host-owned; at least one episode, unique raw IDs, baseline and lineage-head consistency, and complete replacement remain enforced. | Strategy/builder/committer/coordinator source; memory docs |
| BEH-005 | User/Operational | Normal assistant or child compactor emits a tool call | Parent tool dispatch checks configured instances; compactor agent config provides none; compactor runner disables auto-execution; collector treats approval as failure | Parent `write_file` was rejected, then parent used `run_bash` successfully. Compactor has no write capability. | Screenshot; server excerpt lines 28–37 and 102–112; raw parent trace; config/runner/collector source |
| BEH-006 | Contract | Current memory is loaded/projection built after a successful lineage append | lineage head selects exact episode/semantic bundle -> `CompactedMemoryMessageBuilder` groups categories -> fixed Markdown-like continuation prompt; current reader accepts prompt versions 1/2/3 and writer emits 3 | Model-authored six arrays are not persisted. Categories remain meaningful to storage/salience/Inspector/projection; representative latest head is valid with five episodes and zero semantic facts. | Parent memory files; projection/model/lineage source/tests; `ed7f65a5d` |
| BEH-007 | System | A supported settings change or later usage observation puts the parent prompt at/above the active proactive trigger | `resolveTokenBudget` -> `CompactionPolicy.shouldCompact` -> `requestCompaction` -> strategy construction with only `inputBudgetTokens` -> `EstimatedMessageBudgetStrategy` fixed 35% suffix -> planner -> commit -> next LLM budget evaluation | The trigger is correct, but the plan has no trigger target and success retains no episode awaiting an actual below-threshold usage observation. At 20%, two successful results remained above the trigger and a third used the all-fitting fallback to collapse context. | `repeated-compaction-runtime-analysis.md`; corrected focused log/screenshot; token-budget/policy/planner source; AR-FIND-001 |
| BEH-008 | Operational | Child LLM/provider/ingestion fails before any usable assistant output | `LlmPhase` restores request and returns `{isError:true, CompleteResponse(error text)}` -> response pipeline publishes assistant completion without the error bit -> collector accepts text -> parser reports JSON extraction -> summarizer launches correction | Runner failure is misclassified as model-authored invalid JSON; underlying cause is obscured and a second child is wasted. | `compactor-runner-failure-analysis.md`; four child traces; LLM phase/pipeline/payload/collector source |
| BEH-009 | Operational | A required compaction operation reaches a final failure | executor emits failure and throws while coordinator retains the pending request -> later user-authored request assembly executes the same operation before appending the new message | Canonical memory is safely unchanged. The user approved this as the fail-closed/manual-retry boundary; the defect is that runner failures currently masquerade as invalid JSON, launch a correction child, and lack an explicit no-autonomous-retry contract. | `compactor-runner-failure-analysis.md`; parent turns 0004/0005; executor/coordinator/assembler source; user clarification 2026-08-14 |
| BEH-010 | System | A normal USER, AGENT, or SYSTEM turn-start input is queued while a failed compaction remains pending | current inbox: all external entries share `turn_start`; idle scheduler claims FIFO head -> turn starts -> `AgentInputPipeline` converts direct inter-agent input or preserves `SenderType.AGENT`/`SYSTEM` carrier -> `LlmPhase` -> assembler executes any pending operation | Supported non-user input can autonomously retry the provider. Simply leaving it at FIFO head would also block a later user recovery signal. The approved target retains non-user entries in place while predicate-aware selection admits USER only. | `ARCH-REV-003` / `MP-002`; core inbox/turn/input/assembler files; server inter-agent/system builders; user approval 2026-08-14 |
| BEH-011 | Operational/Contract | Compaction history renders a long message/tool value whose middle boundary falls inside a supplementary-plane character | selected message units -> `CompactionConversationHistoryRenderer` -> `ReadableValueRenderer.omitMiddle` -> arbitrary UTF-16 `slice` -> operation prompt -> OpenAI-compatible request serialization | Source trace is valid, but the derived prompt may contain a lone surrogate and be rejected before inference. The actual runner error now remains truthful, but no model result can exist. | 2026-08-15 screenshot/log; exact parent/child traces; proof JSON; renderer/policy/parser source |
| BEH-012 | Operational/Contract | The parent launches the built-in Memory Compactor with a large but provider-admissible one-shot task while the global proactive ratio is low | `ServerCompactionAgentRunner` -> ordinary `AgentRunService.createAgentRun` -> server backend factory attaches another runner -> child `LlmPhase` observes 176,655 > 123,148 -> requests child-owned compaction -> nested built-in child | The one-shot worker recursively summarizes its own already-wrapped task; the nested input is small only because the outer single message is per-value truncated. This adds cost/latency/noise and can make an already-valid outer response fail if the nested operation fails. | Recursive UI/log/proof supplement; backend factory/runner/LLM phase/config source |

## Design Health Assessment Evidence

- Change posture (`Feature`/`Bug Fix`/`Behavior Change`/`Refactor`/`Cleanup`/`Performance`/`Larger Requirement`): `Bug Fix` with bounded behavior/contract refactor.
- Candidate root cause classification (`Local Implementation Defect`/`Missing Invariant`/`Boundary Or Ownership Issue`/`Duplicated Policy Or Coordination`/`File Placement Or Responsibility Drift`/`Shared Structure Looseness`/`Legacy Or Compatibility Pressure`/`No Design Issue Found`/`Unclear`): `Local Implementation Defect` in UTF-16-unsafe middle/end slicing; `Missing Invariant` at provider-facing text, the prompt boundary, post-compaction target, failed-pending authorization, and the one-shot compactor leaf boundary; `Duplicated Policy Or Coordination` between trigger and suffix retention; `Boundary Or Ownership Issue` where the child error bit is lost before collection, original turn origin is not carried to execution authorization, and server provisioning grants the compactor its own recursive capability; `Shared Structure Looseness` in the original parser candidates and current reduced pending-request shape; `Legacy Or Compatibility Pressure` in the removed sender-heading layer.
- Refactor posture evidence summary: the prompt/parser/attempt correction is already implemented. Additional local refactoring is needed across trigger observation, separate post-success episode state, pending planning budget/attempt state, assistant event transport, authoritative turn-start origin/admission, fail-closed failure disposition, and automatic-compaction composition. Reusing the existing turn-start queue, single policy, internal coordinator, strategy registry, and current strategy is simpler and safer than new recovery state, a persisted opt-out, policy/controller hierarchy, or second strategy. The bare nullable runner is removed because it fragments composition; one tight memory-owned disabled/enabled configuration replaces it. The accepted-compaction owner and persistence spine remain healthy and must be reused. Generic file-backed transport and recursive/chunked fallback are rejected for this task because they would regress authority or change the product contract.

| Evidence Source | Observation | Design Health Implication | Follow-Up Needed |
| --- | --- | --- | --- |
| Failed raw traces | Entire history is labeled `[User Requirement]` and ends after a tool result without first being identified as the target agent's history | The target-agent boundary is ambiguous; refine system identity and add a pre-history identification/separator | Target prompt design |
| Processed system prompt in server excerpt | The generated prompt includes the authored compactor instructions followed by common Workspace/Bash/File sections | User reviewed the complete prompt and considers the section order acceptable; the target correction should refine `agent.md`, not reorder common prompt sections | Preserve order; refine compactor-authored wording |
| Two identical-input failures | Model resumes CER/Vue and emits `run_bash` twice | This is repeatable wrong-task selection, not a one-character JSON typo | Add correction path and regression scenarios |
| Earlier same-parent successes | Same model previously emitted valid summaries | Failure depends on prompt/history/model interaction; not every compaction is mechanically broken | Keep evidence-specific root cause; do not blame tool tail alone |
| Parser probe | Prose/fences accepted; first unrelated object masks valid later candidate | Parser extraction and schema validation are incorrectly sequenced | Schema-aware candidate selection needed |
| User contract decision plus successful same-model runs | The original six-array response section is detailed, repeatedly exercised, and produced valid summaries when the model selected the compaction task | Avoid unrelated schema churn; preserve the complete response section verbatim | Exact prompt specification is authoritative |
| Committer/coordinator source | Existing proposal/accept/commit separation fences all canonical writes | Persistence boundary is healthy; bypassing it with generic file writes would regress ownership | Preserve unchanged |
| Historical live E2E | Prior DeepSeek invalid JSON was retained as stochastic risk | Invalid-output recovery is now justified by production recurrence | One bounded repair, not unbounded retry |
| Outer/nested compactor trace pair | One-shot outer child receives the same enabled automatic-compaction composition as a normal target agent and recursively creates the built-in definition after its response | Automatic-compaction ownership is fragmented and violates the specialized worker lifecycle; separator changes cannot fix it | Provision the built-in compactor with disabled memory compaction and let generic core consume the memory-owned configuration |

## Relevant Files / Components

| Path / Component | Current Responsibility | Finding / Observation | Design / Ownership Implication |
| --- | --- | --- | --- |
| `autobyteus-ts/src/memory/compaction/working-context-compaction-prompt-builder.ts` | Builds operation task prompt | Returns renderer output verbatim; no pre/post task framing | Natural owner for minimal explanation + plain-text boundary around the unchanged renderer output |
| `autobyteus-ts/src/memory/compaction/compaction-conversation-history-renderer.ts` | Canonically renders selected conversation/tool protocols and owns the sole outer history delimiter | Correctly omits system/private reasoning and escapes delimiter collisions; current tag is generic | Rename the owned delimiter and its collision escaping to `target_agent_conversation_history`; preserve inner rendering behavior |
| `autobyteus-ts/src/memory/presentation/readable-value-renderer.ts` | Redacts and head/tail-omits long visible values used in compaction/work-trace presentation | `omitMiddle` uses arbitrary UTF-16 code-unit boundaries and created the verified lone U+D83D | Retain redaction/marker ownership; delegate safe boundary and derived-copy normalization to one tight Unicode utility |
| `autobyteus-ts/src/memory/compaction/compaction-response-parser.ts` | Extracts/validates the six-array envelope and clamps accepted text | `clampText` uses unsafe `slice(0, limit)`; an accepted emoji-boundary clamp can place a lone surrogate into future context | Reuse the same safe end-truncation primitive; response schema/limits remain unchanged |
| `autobyteus-ts/src/memory/compaction/working-context-compaction-prompt-builder.ts` | Builds the completed first/correction child task prompt | Correct target-agent boundary, but no final well-formed-text invariant exists | Normalize/assert the derived completed prompt before runner launch without editing stored sources or approved wording |
| `autobyteus-server-ts/src/built-in-agents/templates/memory-compactor/agent.md` | Stable compactor task/schema instructions | Response section is already clear; only early target-agent/conversation-history wording needs refinement | Apply exact approved prompt file; preserve response section verbatim |
| Processed internal-agent prompt composition | Wraps `agent.md` in identity and appends shared environment/Bash/file practice | Full captured prompt is now reviewable; user accepts this overall order | Preserve generated composition for this correction |
| `autobyteus-ts/src/memory/compaction/agent-compaction-summarizer.ts` | Builds one task, runs one child, records metadata, parses output | Single-attempt owner; parser failure retains first child metadata only | Target owner for the fixed initial/new-child-correction state flow and exhausted-attempt diagnostics |
| `autobyteus-ts/src/memory/compaction/compaction-response-parser.ts` | Extracts and validates transient response | Preserved six-array schema; first parseable object wins | Keep six-array mapping; make candidate selection schema-aware and tolerant of harmless extras; keep text bounds |
| `autobyteus-ts/src/memory/compaction/compaction-result.ts` | Internal typed raw summary | Healthy consumer-facing shape with episode and category arrays | Reuse as host internal mapping target; model schema need not mirror it |
| `autobyteus-ts/src/memory/compaction/compaction-result-normalizer.ts` | Cleanup, dedupe, category ordering/salience | Category semantics are product-relevant and healthy | Preserve |
| `autobyteus-ts/src/memory/compaction/structured-json-compaction-strategy.ts` | Plans, summarizes, normalizes, reports diagnostics, builds proposal | Correct strategy owner; generic interface not universally JSON | Keep strategy boundary; name may be reconsidered only if misleading, without compatibility shim |
| `autobyteus-server-ts/src/agent-execution/compaction/server-compaction-agent-runner.ts` | Creates visible child, posts message, waits, returns public output/metadata, terminates | Zero-tool auto-execute false; one task per run | Preserve least authority; expose attempt metadata needed by summarizer |
| `autobyteus-server-ts/src/agent-execution/compaction/compaction-run-output-collector.ts` | Collects final public assistant text; fences terminal/error/tool approval | Private reasoning is not parsed; tool request is failure | Preserve output authority and no-tool behavior |
| `autobyteus-ts/src/memory/compaction/pending-compaction-executor.ts` | One operation lifecycle around propose/prepare/validate/commit | Emits failed only after strategy throws | Keep operation-level single terminal status; attempt diagnostics belong below it |
| `autobyteus-ts/src/memory/memory-manager-compaction-coordinator.ts` | Pending state, optimistic baseline/lineage checks, prepare/commit delegation | Pending clears only in successful commit | Preserve |
| `autobyteus-ts/src/memory/compaction/accepted-compaction-builder.ts` | Assigns host IDs/time, builds rows/lineage/context | Requires episode and complete candidate before commit | Preserve |
| `autobyteus-ts/src/memory/compaction/accepted-compaction-committer.ts` | Ordered canonical mutations and pending clear | Safe host-owned commit spine | Preserve; file-backed model must never bypass |
| `autobyteus-ts/src/agent/event-inbox/{agent-event-inbox-entry.ts,agent-event-inbox.ts,inbox-queue-store.ts,agent-event-scheduler.ts}` | Classifies, stores, and selects runtime turn-start entries | USER/AGENT/SYSTEM share one FIFO lane; current head-only claim cannot defer non-user while admitting user behind it | Stamp origin on entry; add matching claim and use the same admission predicate for claim and wait logic; keep one queue |
| `autobyteus-ts/src/agent/event-inbox/handlers/turn-start-inbox-event-handler.ts`; `src/agent/runtime/agent-worker.ts`; `src/agent/agent-turn.ts` | Starts one active turn from a claimed external trigger | Origin is not an explicit turn identity today | Carry the pre-conversion origin into the active turn and request assembly; do not infer it from converted text |
| `autobyteus-ts/src/agent/pipelines/agent-input-pipeline.ts`; `src/agent/llm-request-assembler.ts` | Converts input and executes pending compaction before appending the current message | Conversion can erase original event class; assembler uses pending presence only | Preserve origin separately and require typed execution authorization before pending execution |
| `autobyteus-server-ts` inter-agent/direct-message and task-notification builders | Create supported AGENT/SYSTEM carriers | Production non-user inputs are not represented by one event class | Preserve current content/metadata; classification is by original sender type at core inbox boundary |
| `autobyteus-ts/src/memory/projection/compacted-memory-message-builder.ts` | Builds final Markdown-like prompt memory | Headings are derived from typed data | No reason for model to author final Markdown |
| `autobyteus-ts/src/memory/models/semantic-item.ts` | Typed semantic categories, order, base salience | Categories are more than cosmetic headers | Preserve category discriminator |
| `autobyteus-ts/src/memory/lineage/compaction-lineage-record.ts` | Validates lineage schema/version | Supports prompt contract 1/current 2 | Extend direct acceptance to 3; no data rewrite |
| `autobyteus-server-ts/src/built-in-agents/templates/memory-compactor/agent-config.json` | Compactor tools/skills/processors | All lists empty | Preserve no-tools policy |
| `autobyteus-server-ts/src/agent-execution/backends/autobyteus/autobyteus-agent-run-backend-factory.ts` | Builds `AgentConfig` for every AutoByteus definition and currently creates the server compaction runner | No built-in identity exclusion exists, so `autobyteus-memory-compactor` receives the same recursive composition as normal agents; current nullable factory results can also blur required normal composition with deliberate disablement | Own definition-aware composition: disabled memory compaction and zero runner-factory calls for the canonical compactor; enabled configuration containing the existing policy/required runner for normal definitions; runner failure/null fails normal composition; create/restore share the same builder |
| `autobyteus-server-ts/src/built-in-agents/built-in-agent-registry.ts` | Owns canonical built-in agent definition IDs/registration | Provides the server-owned identity for `autobyteus-memory-compactor` | Reuse canonical identity; do not duplicate a string or leak it into core |
| `autobyteus-ts/src/agent/context/agent-config.ts` | Carries runtime composition into core agent creation | A bare top-level nullable runner is separated from the policy that `AgentFactory` creates later and permits invalid/ambiguous composition | Replace it cleanly with a non-null discriminated `memoryCompaction` configuration; `copy()` clones enabled policy scalars while retaining the runner identity, or preserves disabled mode |
| `autobyteus-ts/src/agent/factory/agent-factory.ts` | Creates runtime stores and `MemoryManager` | Unconditionally constructs `new CompactionPolicy()` regardless of the supplied runner or agent role | Stop inventing policy locally; pass `AgentConfig.memoryCompaction` into `MemoryManager` so agent creation is the composition boundary |
| `autobyteus-ts/src/memory/compaction/memory-compaction-configuration.ts` (new) | Tight runtime-only automatic-compaction composition | No existing type binds enablement, the current policy, and the current strategy runner without allowing enabled-without-runner | Define only `disabled` and `enabled { policy, runner }`; no policy hierarchy, generic dependency bag, persisted schema, or strategy taxonomy |
| `autobyteus-ts/src/memory/memory-manager.ts` | Memory boundary; currently owns `compactionPolicy` and the internal compaction coordinator | Policy is owned here, but enablement/runner are outside, so the LLM phase assembles the subsystem from fragmented sources | Own the full configuration and expose a focused read boundary; disabled observations return a no-work decision and cannot create pending/threshold state |
| `autobyteus-ts/src/memory/policies/compaction-policy.ts` | Single current pressure policy | `classifyPressure` already answers `none`, `proactive`, or `hard_input_cap`; no evidence requires alternative policy classes | Retain as the only current policy inside the enabled configuration |
| `autobyteus-ts/src/memory/compaction/{default-working-context-compaction-strategy-registry.ts,working-context-compaction-strategy-resolver.ts}` | Pluggable `how` boundary; default registry selects a strategy | Exactly one `structured-json` strategy is registered and it requires the current runner | Preserve registry/strategy; take the runner from the enabled memory configuration instead of `AgentConfig`; add no second strategy |
| `autobyteus-ts/src/agent/token-budget.ts` | Currently combines provider/model capacity arithmetic with compaction ratio/trigger arithmetic | Disabled compactor still needs provider request-capacity accounting, but must not inherit a 20% automatic-compaction threshold | Separate common request-capacity resolution from enabled-only compaction threshold derivation without changing the approved capacity or B/T/P arithmetic |
| `autobyteus-ts/src/agent/loop/llm-phase.ts` | Executes LLM request/response lifecycle and integrates automatic compaction | Currently reads manager policy and top-level runner separately, applies settings, constructs strategy/executor, and evaluates after every usable response | Resolve common provider request capacity for all runs; ask `MemoryManager` once for enabled/disabled automatic compaction; only enabled constructs strategy/executor, applies policy, executes pending work, and evaluates post-response |

## Runtime / Probe Findings

| Date | Method (`Repro`/`Trace`/`Probe`/`Script`/`Test`/`Setup`) | Exact Command / Method | Observation | Implication |
| --- | --- | --- | --- | --- |
| 2026-08-14 | Trace | Inspect failed child `raw_traces_active.jsonl`; retain focused output with Python/JQ | Run `506e...` output 1,149 chars and run `e1f5...` output 713 chars; both source-task continuation/tool DSML; neither JSON | Exact failure is wrong-task output |
| 2026-08-14 | Trace | Compare successful child traces and parent lineage | `da3d...`: input 50,686, output 10,672, four episodes; `d305...`: input 73,669, output 7,071, three episodes; both valid v2 lineage | Early success vs late confusion confirmed in same run/model |
| 2026-08-14 | Probe | Invoke current built parser on controlled strings and actual captured outputs | Exact/prose/fenced pass; unrelated-object-first fails `unsupported fields: note`; actual failures fail no valid JSON | Extra visible text is not root; candidate order is a separate robustness defect |
| 2026-08-14 | Trace | Inspect parent raw traces around `write_file` | `write_file` failed; model explicitly recognized absence and used `run_bash` heredoc; file creation succeeded | Parent tool error is separate and recovered |
| 2026-08-14 | Data | Compare parent memory files before/after failed operation via lineage head | Only two successful lineage records exist; pending operation `_3` did not append | Safe-failure commit fence worked |
| 2026-08-14 | Trace | Correlate 20% budget evaluations, plans, result summaries, and lineage after the setting change | Three successful operations in one parent turn; first two remain over threshold, third retains five units and reaches 8,755 tokens | Supported low ratio exposes inconsistent trigger/retention policy |
| 2026-08-14 | Trace/Data | Inspect four new child raw traces and query `token_usage_ledger_events` | Every child has `user` then `llm_request_recovery`; zero assistant traces and zero usage rows | Failure preceded usable model output; JSON-repair classification is wrong |
| 2026-08-14 | Trace | Compare parent turn 0004 failure and turn 0005 `continue` | Same operation ID, 208 selected units, five retained, and 694 raw traces executed twice | Retained pending operation traps the next message in unchanged retry |
| 2026-08-15 | Trace/Log | Correlate Daily Assistant `compaction_operation_msu088l2_1`, outer compactor `compaction_operation_msu09qwk_1`, and nested child run | Outer child prompt 176,655 crossed 20% trigger 123,148 and launched nested run after producing its response; nested task is 2,430 characters with two wrapper pairs | Built-in Memory Compactor must be a non-compactable leaf; 80% only masks the defect |
| 2026-08-15 | Trace/Data | Inspect parent terminal operation and next token-budget observation | Intended parent result committed 4,974 episode-summary characters and 33 semantic facts; next actual parent prompt 73,102 is below threshold | No canonical-memory destruction; leaf fix preserves parent accepted commit/reset |

## External / Public Source Findings

- Public API / spec / issue / upstream source: `N/A`; local production traces, current source, and repository docs fully establish the issue.
- Version / tag / commit / freshness: repository base `54890a07f` after fresh remote fetch on 2026-08-14.
- Relevant contract, behavior, or constraint learned: N/A.
- Why it matters: provider documentation is not needed to determine that the actual model output did not contain a compaction object; target behavior is provider-agnostic.

## Reproduction / Environment Setup

- Required services, mocks, emulators, or fixtures: No new service. Investigation used existing persisted local run data and current built parser output.
- Required config, feature flags, env vars, or accounts: None for trace/code inspection. The original run used configured DeepSeek `deepseek-v4-flash`.
- External repos, samples, or artifacts cloned/downloaded for investigation: None.
- Setup commands that materially affected the investigation: fresh fetch and dedicated worktree creation only; no runtime memory file was modified.
- Cleanup notes for temporary investigation-only setup: No process launched and no temporary service to clean. Evidence copies intentionally remain in the ticket folder.

## Findings From Code / Docs / Data / Logs

1. **The user's diagnosis is correct.** The later compactor did not merely format a summary incorrectly; it selected the source task. The first failed answer promised to rebuild the CER demo and read Vue files. The second again promised to read current views. Both emitted DSML `run_bash` calls.
2. **The final user-level instruction is ambiguous.** `WorkingContextCompactionPromptBuilder` returns only the rendered `<conversation_history>` block. Normal input processing labels that block `**[User Requirement]**`; there is no explicit compaction request after the close tag. The transport already sends it as a user-role message, so this literal Markdown label adds no role boundary for the model and inaccurately describes an internally generated compaction payload as a user requirement.
3. **The failed history creates a strong continuation cue.** It begins with the previous compacted-memory continuation prose, contains the active CER/Vue work, and ends immediately after a successful `run_bash` result containing `DESIGN.md`. A general agent would normally continue from precisely that point.
4. **Processed system context reinforces the wrong affordance.** Although the built-in agent definition correctly says to summarize, the final processed prompt also gives Working Environment, Bash Operating Practice, and File And Directory Practice instructions for `/Users/normy/eddie_project`; the compactor has zero tools. This irrelevant context makes “read the files next” look legitimate.
5. **Private reasoning did not corrupt parsing.** The collector uses public assistant completion/segments; private reasoning is stored separately and excluded from the retained evidence.
6. **Visible extra prose is already tolerated.** `parseObject` tries the trimmed response, JSON fences, and balanced objects. Controlled probes pass valid JSON wrapped in prose or fences.
7. **Candidate selection is nevertheless brittle.** It returns the first parseable object and only then validates the compaction schema. A preliminary unrelated object causes rejection even when a valid compaction object follows.
8. **The six arrays are a transient model boundary, not the persisted end product.** The host converts them to `CompactionResult`, normalizes categories, creates persistent rows/lineage/context, and renders prompt-ready Markdown. The user chose to preserve this tested model contract verbatim; its empty-array rule remains part of the accepted schema even though host-owned canonical storage is typed separately.
9. **Some structure remains necessary.** Categories control semantic storage type, ordering, salience, Memory Inspector grouping, lineage membership, and final prompt headings. Arbitrary Markdown would also accept the exact wrong-task prose/tool markup, converting a detected failure into silent memory corruption.
10. **The accepted-compaction architecture is healthy.** Proposal/accept/commit with baseline/lineage checks ensures invalid output does not archive traces or replace memory. The target should feed this boundary, not bypass it.
11. **`write_file` is unrelated.** It was requested by the parent Daily Assistant, rejected by parent tool configuration, and immediately replaced by a successful `run_bash` heredoc. The compactor child has no tools by definition and launch policy.
12. **Adding a generic tool is a larger unsafe redesign.** The runner disables tool auto-execution and collector fails tool approval. Because the child currently shares the parent workspace path, generic writes would expose source files to a model demonstrably confused by source-task instructions and would still require a staged protocol plus host validation/commit.
13. **The current one-shot failure is insufficient for critical compaction.** Previous live E2E already recorded a stochastic DeepSeek invalid-JSON attempt. This run repeated the same wrong-task behavior across two manual tries. One automatic corrective attempt is a proportionate recovery boundary; unbounded retry is not.
14. **Prompt correction is causally prior to schema/retry hardening.** The output schema only detected the wrong-task response; it did not cause it. Contract simplification and one repair remain independent hardening choices.
15. **No persisted-data migration is indicated.** The transient response schema remains unchanged, current canonical rows/snapshots/archives remain directly usable, and only new lineage prompt-contract metadata changes.
16. **`[User Requirement]` is inaccurate globally.** `UserInputContextBuildingProcessor` adds the label to every `SenderType.USER` input even though the message is already carried with the user role and may be a question, answer, correction, approval, or internal payload. It is also persisted into working context and therefore recurs inside later compaction histories.
17. **The generic non-user headings are also misplaced in the shared processor.** Native tool results are committed as canonical structured tool-result messages and rendered through the provider tool protocol. The `SenderType.TOOL` message is an internal continuation carrier; `AgentInputPipeline` returns `llmUserMessage: null` for text-only continuation, so `[Tool Execution Result]` normally never reaches the model. A context/media continuation may require an auxiliary user/media carrier, but `ToolContinuationInputBuilder` already renders self-describing completion text. Current inter-agent builders explicitly render sender name/id, and current system task renderers explicitly describe activation, submission, or revision. The recommended global policy is therefore no generic sender heading in `UserInputContextBuildingProcessor`, raw authored content when there is no readable context, and neutral `[Context]` / `[Message]` sections only when both share a payload. Sender metadata, provider roles, and origin-specific rendering remain unchanged.
18. **Obsolete formatting is removed rather than retained.** The user explicitly confirmed the design-principle posture that obsolete in-scope behavior must be cleaned up. The generic heading map, its sender-type formatting branch/parameter where no longer needed, and old heading assertions are removal work. They must not survive behind a feature flag, compatibility alias, dual rendering mode, or fallback. The active `SenderType` metadata model and internal `SenderType.TOOL` continuation carrier are not legacy: current runtime routing and media continuation still use them and they remain in scope to preserve.
19. **The original response section is preserved verbatim.** The user rejected the proposed `items` contract after comparing it with the original. The authored `agent.md` has been tested repeatedly and is approximately 99% correct; only small target-agent/conversation-history wording changes are approved. The exact prompt specification therefore retains every original response field explanation, the non-empty episode/empty-array rules, and the complete six-array JSON example without paraphrase. Parser tolerance for prose/fences/harmless extras remains defense in depth and does not change the requested model format.
20. **The sole history tag is made explicit, not multiplied.** The user approved replacing `<conversation_history>` with `<target_agent_conversation_history>` so the delimiter itself states whose history it contains. This supersedes the earlier instruction to retain the old tag, but not the prohibition on XML hierarchy: the target has exactly one renamed wrapper, unchanged rendered inner history, collision escaping for the new delimiter, and no old-tag alias or nested/dual wrapper.
21. **The 80%→20% trigger itself was correct, but the repeated success chain was not.** The ratio changes only the proactive trigger. Planning still uses a fixed 35% recent-suffix budget, which can be larger than the trigger and therefore violates the required post-compaction invariant at low ratios.
22. **The later trigger was a legitimate new crossing.** After the third successful compaction reached 8,755 tokens, hundreds of supported tool-loop calls grew the prompt to 123,520; the 20% threshold was 123,148. The new request was not an immediate continuation of the earlier three-operation chain.
23. **The later failures were not prompt confusion or malformed model JSON.** All four child attempts failed before a usable assistant response, rolled back the LLM request, and recorded no usage. No model-authored compaction output existed to parse.
24. **The new repair boundary misclassifies runner failures.** `LlmPhase` converts request failure into an error `CompleteResponse`; the collector accepts that as output because the diagnostic error event is not a canonical terminal failure; the summarizer then interprets the error prose as a JSON extraction failure and launches the correction child.
25. **Safe pending retention is now an approved fail-closed gate, but the retry boundary must be explicit.** After the first parent failure, the later user-authored `continue` re-executed the same operation and plan before parent dispatch. The user approved that user-initiated retry behavior even at 123,520 tokens below the 615,744 hard input budget. The defect is the meaningless response-correction child and lack of truthful failure typing—not the fact that required compaction blocks target-agent dispatch until a later explicit retry succeeds.
26. **The precise provider cause is currently unobservable.** Immediate zero-usage failure makes quota/rate-limit or another provider rejection plausible, especially after 68.81M gross input tokens, but the runner boundary discards the concrete LLM error message. That cause cannot be claimed without new evidence.
27. **The Token Meter and trigger use different denominators.** The screenshot shows 123,520 / 1,000,000 = 12.4%, while compaction correctly triggers at 20% of the 615,744 input budget. The computation is consistent, but current presentation makes the relationship non-obvious.
28. **Pending presence is not retry authorization.** `AgentTurnTrigger` supports direct user and inter-agent events, while server-managed team/direct delivery also posts `SenderType.AGENT` through the user-message carrier. All paths share one turn-start lane and reach request assembly. After final failure, the pending record therefore needs an attempt state, and the original entry needs an origin, before the platform can distinguish the automatic initial attempt from a later USER-authorized attempt.
29. **The existing queue is sufficient if selection becomes eligibility-aware.** Moving or copying non-user entries into a second deferred buffer would add lifecycle and shutdown state. Leaving an ineligible non-user at FIFO head with the current head-only claim would block `continue`. The proportionate design leaves entries in the ordinary queue, scans for the first eligible USER only while `awaiting_user_retry`, and otherwise preserves normal FIFO and existing shutdown drain.
30. **The 2026-08-15 trigger was correct and the failure was deterministic local input corruption.** At 371,352 tokens the 20% threshold was 123,148 and target 110,833, so requesting compaction was expected. No response JSON existed: the provider rejected the request body before inference.
31. **Middle omission split a valid shield emoji.** The parent trace contains `icon: '🛡️'`. Default 2,000-character omission retained the high surrogate and inserted the marker before the low surrogate, leaving one U+D83D in the 540,727-unit child input. This exactly explains DeepSeek's `unexpected end of hex escape` at `messages[1].content`.
32. **The new runner boundary behaved correctly.** The error remained `runner_error_completion`, one child ran, no response parser/correction child was invoked, the target turn stopped, and the runtime remained available. Retrying unchanged rendering would nevertheless deterministically fail again, so user retry is not a substitute for fixing provider-facing construction.
33. **Safe derived text is narrower than ASCII conversion.** Replacing/removing malformed surrogates and unsafe controls plus surrogate-safe slicing solves the transport invariant. Removing valid emoji or all non-ASCII content would damage languages, paths, code, and symbols without improving JSON safety. Raw traces must remain exact; only the provider-facing copy changes.
34. **The double wrapper is recursive runtime composition, not prompt confusion.** The outer child task contains one target-agent wrapper. The nested child wraps a shortened rendering of that whole outer task, creating exactly two pairs. The small input comes from the existing 2,000-character per-value bound, not missing parent history.
35. **Automatic-compaction composition is fragmented.** `ServerCompactionAgentRunner` intentionally uses normal run creation. The backend factory attaches a runner to every definition, `AgentConfig` carries it independently, `AgentFactory` always constructs a new policy, and `LlmPhase` combines both with global strategy resolution. The built-in compactor therefore enters the same post-response policy as its target agent even though no owner explicitly chose that behavior.
36. **The defect can turn success into failure.** The outer compactor had already produced a usable response, but core waits for immediate child-owned compaction to settle before completing the run. A nested failure can therefore reject the usable outer response at the parent boundary.
37. **A bounded memory-owned configuration is the proportionate boundary.** The server owns which definition is the built-in compactor and selects disabled at agent creation. `MemoryManager` owns whether automatic compaction is disabled or enabled with the existing policy/current strategy runner. Generic `LlmPhase` consumes that boundary and remains definition-agnostic. This removes recursion and invalid enabled-without-runner composition without a ratio hack, prompt instruction, persisted flag, whole-task clamp, new controller hierarchy, or data migration.
38. **The observed 176,655-token child task was provider-admissible.** It exceeded the 20% proactive trigger but remained far below the resolved 615,744 input budget. Proactive policy is not an input-capacity limit, so the leaf should send this one-shot task directly. General multi-pass handling for a truly oversized child is separate scope.
39. **The parent result and stored data survived this reproduction.** The intended Daily Assistant operation completed, pending cleanup occurred through the accepted commit, and the next actual observation was 73,102. Existing nested run archives are evidence and require neither deletion nor migration.
40. **The token-ledger Prisma error is independent.** Missing `token_usage_ledger_events.member_name` prevents some token-usage persistence and creates log noise, but it is not on the operation-prompt, child creation, response collection, or accepted-memory commit spine. It requires a separate ticket and does not justify migration here.
41. **The existing policy/strategy abstractions are already sufficient.** There is only one `CompactionPolicy`, and its pressure classification is the correct `whether` boundary. The default strategy registry has one `structured-json` registration, and the resolver is already the `how` boundary. The refactor should compose these existing pieces; adding ratio-policy/capacity-policy subclasses or a second strategy now would be speculative.
42. **Provider request capacity is not automatic-compaction policy.** `resolveTokenBudget` currently calculates both in one object, but the underlying provider/model context/input caps and output reserve remain relevant even for a disabled leaf. The 20% ratio must not apply to the leaf; ordinary request admission/capacity still does. A small split in calculation responsibilities is justified, not a new capacity subsystem.
43. **Defaults must differ by composition boundary, not by an ambiguous nullable runner.** Direct core/test construction frequently omits compaction dependencies, so the safe complete default there is explicit disabled configuration. Production server construction has enough definition context to choose deliberately: canonical Memory Compactor disabled; normal definitions enabled. A normal runner-factory exception or null is therefore a composition failure, not evidence that the normal agent should silently lose proactive/hard-cap protection.

## Persisted Data Transition Evidence (When Applicable)

- Current stored subject, location, representative shape, and approximate volume:
  - `/Users/normy/.autobyteus/server-data/memory/agents/daily_assistant_2a39c68eb96443ada6f5af9f4f81acef/episodic.jsonl`: typed `{id,ts,summary}` rows.
  - `semantic.jsonl`: typed `{id,ts,category,fact,salience}` rows.
  - `compaction_lineage.jsonl`: two schema-v1 records with prompt contract version 2 and exact output membership.
  - raw trace archives/manifest and `working_context_snapshot.json` remain separate canonical artifacts.
- Relevant code-model, serialization, semantic, or physical-store change: proposed change affects the transient model response, derived provider-facing rendering, new lineage execution metadata (`promptContractVersion: 3`), and runtime-only memory-compaction composition for each agent; no raw-trace, episodic, semantic, lineage-shape, snapshot, agent-definition storage, or database schema changes.
- Normal readers and writers, including unknown/extra-field behavior: current lineage normalizer accepts prompt versions 1 and 2 only; it must accept 3. Episodic/semantic/current-output readers are version-independent of the transient response.
- Representative direct-read or compatibility evidence: the two earlier compactions load as the current lineage chain and project through current memory; the failed attempt created no third lineage row.
- Required semantics and invariants preserved by direct use: `Yes` — existing IDs, timestamps, categories, facts, salience, lineage membership, archive origin, and current snapshot remain unchanged.
- Physical storage, privacy/security, disposal, rebuild, or operational constraints: runtime memory is user data and must not be rewritten for representational cleanliness. Generic compactor workspace writes would increase exposure and are excluded.
- Concrete benefit, cost, and risk of migration if it remains a candidate: no semantic benefit; rewrite cost and corruption risk are nonzero; migration rejected.
- Existing migration framework or lifecycle constraints, only if migration may be required: N/A; outcome is `Directly Usable — No Migration`.

## Constraints / Dependencies / Compatibility Facts

- The model may ignore or misunderstand instructions; syntax and minimum semantic shape require host enforcement.
- The compactor system prompt is persisted in a built-in agent definition but the final processed prompt also includes platform-generated context; both layers matter.
- The renderer's omission of private reasoning/system content and canonical tool-protocol formatting are valuable and should remain.
- One response-contract repair attempt must remain below the pending-operation lifecycle so a recovered usable-but-invalid response does not produce a stale visible failure card; runner failure must bypass this loop.
- Attempt metadata must survive parser failure, while runner/provider failure must preserve its original typed cause and must not be reduced to parser-stage metadata.
- The generic `WorkingContextCompactionStrategy` does not mandate JSON; response rules remain within the production strategy/summarizer boundary.
- Clean-cut transient contract replacement is possible because old child responses are not read after a process restart; no dual parser is justified.
- Existing immutable lineage versions 1/2 must remain directly accepted when version 3 becomes current.
- Unicode repair/sanitization applies only to derived compaction/provider text. Raw trace/tool values and canonical stores remain unchanged, and safe truncation must preserve configured bounds and omission semantics.
- The existing nullable `AgentConfig.compactionAgentRunner` is evidence of fragmented composition, not the target boundary. Replace it with the runtime-only discriminated memory-compaction configuration. Server provisioning owns built-in definition identity; core runtime remains definition-agnostic and depends only on the memory boundary's enabled/disabled configuration.
- Direct `AgentConfig` or `MemoryManager` construction with the new argument omitted resolves to disabled. Production `AgentFactory` always passes a complete configuration, and a normal server definition whose required runner cannot be constructed fails agent creation rather than falling back to disabled.
- Normal agents must keep proactive and hard-cap compaction. Only the built-in one-shot Memory Compactor is a leaf; a response-correction run is a parent-owned sibling leaf.
- Preserve the one existing `CompactionPolicy`, existing coordinator, existing pluggable strategy registry, and current `structured-json` strategy. Do not add policy subclasses, a compaction-controller layer, or a chunking strategy without new evidence and approval.

## Open Unknowns / Risks

- Exact model behavior cannot be guaranteed; a syntactically valid envelope can still contain an incomplete or inaccurate summary. This remains a compactor-quality risk beyond deterministic contract validation.
- Architecture design must refine the compactor-authored identity/instructions without reordering the generated common sections, and must preserve the current history renderer output unchanged inside the minimal boundary.
- Design resolution: the correction uses a new child run because the runner owns one create/post/collect/terminate lifecycle per call; this avoids inheriting the misdirected child conversation and yields distinct run IDs.
- One repair increases worst-case latency/token usage; final design should keep the budget fixed at two attempts and preserve interruption/timeout cleanup.
- A future file-backed transport may be useful for a demonstrated truncation/transport problem, but must be independently designed with isolated staging and least authority.
- The exact provider/response-ingestion error for the four newest child runs is unavailable because current retained events/traces preserve only generic request recovery. Quota/rate limiting is plausible but unproven.
- The intended REQ-014/REQ-015 outcome is strict and user-controlled. Every final compaction failure stops the current target-agent turn and leaves the pending request in `awaiting_user_retry`; no timer, provider retry, or automatic re-arm scheduler is needed. A distinct USER-origin turn executes the pending operation once before dispatch. AGENT/SYSTEM entries stay in the existing queue and resume after successful recovery. Proactive-versus-hard-cap pressure remains diagnostic, not a reason to bypass compaction.
- Compactor-model input-capacity preflight/chunking remains a separate future topic; the four newest attempts were only about 191k characters and no evidence establishes context overflow.
- The earlier four generic request-recovery failures remain causally unobservable, but the separate 2026-08-15 HTTP 400 is fully explained by the captured lone surrogate. Do not generalize the proven Unicode cause backward to those older children.
- The user's proposed future separation of unstructured conversation compaction from episodic/semantic work-trace distillation is not included in SR-005. It would change the current product contract and needs an explicit scope decision before implementation; do not mix it implicitly into the narrow Unicode correction.
- A future genuinely oversized compactor task still needs an independently approved admission/chunking strategy. Recursive self-compaction is not an acceptable fallback; this ticket retains truthful planning/pre-launch/runner failure and does not prebuild a hierarchy for that future case.
- The concurrently observed token-usage ledger schema mismatch remains unresolved and may reduce accounting evidence, but it is independent of compaction recursion and intentionally excluded from SR-008.

## Notes For Architecture Reviewer

ARCH-REV-006 passed SR-006 after the bounded checksum/oracle corrections. The later live retry established BEH-012 on the same production path: the server provisioned the outer built-in Memory Compactor with its own runner, so 176,655 tokens crossed the global 20% trigger and launched a nested child over the already-wrapped task. SR-007 described the behavioral leaf as a narrow null-runner guard. The user subsequently clarified and approved a bounded ownership refactor, so SR-008 supersedes that implementation boundary: one runtime-only discriminated configuration is selected at agent creation and owned by `MemoryManager`; disabled has no policy/runner, enabled contains the single existing policy and current strategy runner; omitted direct-core composition is disabled while normal server runner failure/null is a creation failure; the top-level runner and factory-created policy are removed; common provider request capacity remains independent; the existing registry/current strategy remain unchanged. Review SR-008 against that composition and the same leaf scenario. Normal-agent proactive/hard-cap behavior, initial/correction sibling ownership, exact approved prompt, six-array response, typed memory, Unicode rendering, fail-closed USER retry, accepted commit, and no-migration posture remain unchanged. The Prisma token-ledger schema error is explicitly separate and not part of this review.
