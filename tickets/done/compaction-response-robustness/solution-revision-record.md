# Solution Revision Record

The latest `requirements.md`, `investigation-notes.md`, `design-spec.md`, and still-relevant supplements are authoritative. This record is only the round/rationale index.

## Revision Index

| Revision ID | Triggering Role / Report / Round | Finding IDs | Classification | Result |
| --- | --- | --- | --- | --- |
| SR-001 | `solution_designer` initial solution baseline | N/A | `Initial Baseline` | Complete solution package ready for architecture review |
| SR-002 | User verification/runtime evidence after local Electron delivery | BEH-007–BEH-009; response/storage reassessment | `Requirement Gap` + `Design Impact` | Revised approved solution ready for architecture re-review |
| SR-003 | `ARCH-REV-002` plus user fail-closed clarification | AR-FIND-001, AR-FIND-002; REQ-014 clarification | `Design Impact` + supplemental evidence correction | Corrected approved solution ready for architecture re-review |
| SR-004 | `ARCH-REV-003` plus user non-user-disposition approval | AR-FIND-003, AR-FIND-004; BEH-010 | `Requirement Gap` + `Design Impact` | USER-origin authorization and same-queue non-user preservation ready for architecture re-review |
| SR-005 | User live 20% reproduction and Unicode-safety approval | BEH-011; REQ-016 | `Requirement Gap` + local `Design Impact` | Provider-safe derived text ready for architecture re-review |
| SR-006 | `ARCH-REV-005` bounded corrections | AR-FIND-002, AR-FIND-005 | `Design Impact` + evidence-metadata correction | Corrected Unicode oracle and revalidated evidence ready for architecture re-review |
| SR-007 | User live nested-compactor evidence after Unicode-safe retry | BEH-012; REQ-017 | `Requirement Gap` + local `Design Impact` | Approved non-compactable built-in leaf ready for architecture re-review |
| SR-008 | User ownership/modularity clarification after SR-007 handoff | BEH-012; REQ-017 ownership boundary | `Design Impact` + bounded refactor posture | Memory-owned automatic-compaction composition ready for architecture re-review |

## Revision Entries

### SR-001 — Prompt-boundary and bounded-repair baseline

- Triggering role, report path, and round: `solution_designer`; initial design round; report path N/A.
- Triggering finding IDs: N/A.
- Prior authoritative result: `N/A`.
- Current authoritative result: user-approved, design-ready behavior package plus implementation-ready architecture design.
- Why this baseline or revision entry is recorded: establish the first complete handoff after production-trace investigation, iterative user clarification, exact prompt approval, and architecture-level current-state read.
- Resolution: fix the target-agent task/evidence boundary; globally remove obsolete sender headings; preserve the exact original six-array response contract; select parser candidates by schema; add one new-child corrective attempt below the single parent lifecycle; preserve zero tools and accepted-compaction commit; write prompt contract 3 while directly reading 1/2/3 with no migration.
- Approved behavior or requirement IDs affected: BEH-001–BEH-006; REQ-001–REQ-010; AC-001–AC-013.
- Canonical artifacts and sections updated: `requirements.md` (`Refined`); `investigation-notes.md`; `design-spec.md` (complete mandatory design); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: approved `memory-compactor-prompt-spec.md`; evidence-backed `prompt-confusion-root-cause.md`; approved `compaction-output-contract-decision.md`; retained runtime prompt/output/parser/UI/log evidence.
- Downstream and architecture-review impact: architecture reviewer can judge exact prompt preservation, ownership of the bounded two-attempt flow, clean-cut removals, parser semantics, lifecycle/commit invariants, no-migration versioning, file/test/doc mapping, and residual risk without rediscovery.
- Next recipient or routing: `architecture_reviewer` for initial design review.
- Remaining gaps or risks: model factual quality remains probabilistic; one repair adds bounded latency/token cost; no first-attempt runner/transport retry; architecture review decision pending.

### SR-002 — Trigger-aligned planning and typed failure lifecycle

- Triggering role, report path, and round: user verification of the delivered local Electron build; runtime evidence in `repeated-compaction-runtime-analysis.md`, `compactor-runner-failure-analysis.md`, and `compaction-memory-shape-reassessment.md`; second solution round.
- Triggering finding IDs: BEH-007 (trigger/planner mismatch), BEH-008 (pre-response runner failure misclassified as invalid JSON), BEH-009 (unchanged failed proactive operation blocks the next message), and the reopened response/storage decision.
- Prior authoritative result: `ARCH-REV-001 Pass`; REQ-001–REQ-010 baseline implemented, code-reviewed, API/E2E-validated, documented, and packaged for user verification.
- Current authoritative result: REQ-001–REQ-014 and all intended-behavior supplements are user-approved; the revised design is implementation-ready pending architecture re-review.
- Why this revision entry is recorded: user verification proved that the correct v3 prompt/schema implementation still had independent production defects in low-ratio planning, child error transport, and proactive failure re-entry. The user then considered simplifying memory output and explicitly reconfirmed the original six-array, episodic/semantic, host-owned, tool-free contract.
- Resolution: carry one immutable trigger-time planning budget through the pending operation; cap the existing 35% quality preference by a complete-prompt target with 10%/256-token headroom and target-scaled summary reserve; validate the finalized estimate before commit; propagate `is_error` through the assistant event/collector boundary; reserve response correction for non-error invalid output; and use a coordinator-owned pending-state union that retains failed proactive operations non-executably, permits one linked retry after a fresh usage observation, suppresses a second failure until reset, and preserves executable hard-cap blocking.
- Approved behavior or requirement IDs affected: BEH-007–BEH-009; REQ-011–REQ-014; AC-014–AC-021. BEH-001–BEH-006 and REQ-001–REQ-010 are explicitly preserved.
- Canonical artifacts and sections updated: `requirements.md` (`Refined` and approval resolved); `investigation-notes.md` (current implemented baseline plus BEH-007–BEH-009 evidence); `design-spec.md` (complete revised architecture); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: added approved `compaction-runtime-behavior-examples.md`; added evidence-backed `repeated-compaction-runtime-analysis.md` and `compactor-runner-failure-analysis.md`; added and resolved `compaction-memory-shape-reassessment.md`; retained all original prompt/contract/evidence supplements unchanged.
- Downstream and architecture-review impact: prior implementation remains the current code baseline, but implementation rework must not begin until architecture review validates the new planning formula, dynamic strategy budget, typed assistant error event, pending-state/gate ownership, precommit postcondition, failure diagnostics, and no-migration/no-prompt-change constraints.
- Next recipient or routing: `architecture_reviewer` for revision round 2 with the prior design review report and complete cumulative solution package.
- Remaining gaps or risks: provider tokenization remains estimated; one oversized newly arriving message has no general admission/chunking gate; process restart resets runtime-only suppression; deterministic validation cannot prove factual summary quality; architecture re-review pending.

### SR-003 — Actual-observation threshold episode and fail-closed manual retry

- Triggering role, report path, and round: `architecture_reviewer`; `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/design-review-report.md`; `ARCH-REV-002`, round 2; plus the user's 2026-08-14 clarification that a final compaction failure must stop the current target-agent turn and be retried only after a later user message.
- Triggering finding IDs: `AR-FIND-001` (accepted estimate incorrectly substituted for an actual below-threshold observation) and `AR-FIND-002` (incorrect focused log excerpt and incomplete canonical evidence inventory); REQ-014 failure-policy clarification.
- Prior authoritative result: `ARCH-REV-002 Fail — Design Impact` for SR-002. The reviewed B/T/P formula, reserve, dynamic strategy budget, typed `is_error` path, runner-versus-response boundary, accepted validation, prompt/schema/tool preservation, and no-migration posture passed; the post-success episode and evidence package did not.
- Current authoritative result: REQ-001–REQ-014 remain approved; REQ-014/AC-020/AC-021 now express strict fail-closed/manual retry; the design has a separate coordinator-owned post-success threshold episode and the corrected evidence package is ready for architecture re-review.
- Why this revision entry is recorded: accepted commit must clear pending, but a local estimate below `T` cannot satisfy the approved requirement for a fresh provider usage observation below `T`. Separately, the user chose correctness over availability: runner/provider or other final compaction failure must not allow target-agent dispatch or create an autonomous retry. The prior retained excerpt also represented the wrong run.
- Resolution: accepted commit's existing final hook clears pending and installs runtime-only `awaiting_below_observation`; first fresh same-key usage below `T` rearms, while first at/above `T` emits one inadequate-reduction diagnostic and suppresses another proactive operation until actual-below or budget-key reset, with hard-cap override. Final failure retains one executable pending gate, stops the current target-agent turn, and schedules no retry; a later user message invokes one new pre-dispatch attempt. Replaced `evidence/repeated-compaction-server-log-excerpt.txt` with the exact three-operation sequence and added all four SR-002 evidence artifacts to the canonical inventory.
- Approved behavior or requirement IDs affected: BEH-007, BEH-009; REQ-012, REQ-014; AC-016, AC-020, AC-021. REQ-013 typed runner failure and REQ-005 usable-output correction remain unchanged and disjoint.
- Canonical artifacts and sections updated: `requirements.md` (fail-closed/manual retry plus actual-observation AC-016); `investigation-notes.md` (decision, corrected evidence inventory/source log, current behavior); `design-spec.md` (separate post-success episode, manual pending retry, owner/interface/file/coverage mapping); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: `compaction-runtime-behavior-examples.md` aligned to actual-observation and fail-closed behavior; corrected `evidence/repeated-compaction-server-log-excerpt.txt` (current directly recomputed SHA-256 `adc1c471f487ad1aee1ffe4e6176fdd70603218b0dadff39c09af439134148cf`; stale earlier checksum assertion corrected by SR-006); inventoried `evidence/repeated-compaction-at-20-percent.png`, `evidence/compactor-provider-failure-and-repeat.png`, and `evidence/compactor-runner-failure-evidence.json`. No prompt/schema supplement changed.
- Downstream and architecture-review impact: architecture re-review must resolve AR-FIND-001/002 against the new separate episode state, exact commit-hook transition, first fresh same-key observation outcomes, direct coverage, strict failure gate, and corrected durable evidence. Implementation must not use SR-002's deferred/suppressed failure-retry states.
- Next recipient or routing: `architecture_reviewer` for revision round 3 with `ARCH-REV-002` and the complete cumulative package.
- Remaining gaps or risks: provider tokenization remains estimated; runtime-only post-success suppression can reset on restart and permit one additional proactive operation; one oversized newly arriving message lacks a general admission/chunking gate; deterministic shape validation cannot prove factual summary quality; architecture re-review pending.

### SR-004 — Authoritative USER retry and same-queue non-user preservation

- Triggering role, report path, and round: `architecture_reviewer`; `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/design-review-report.md`; `ARCH-REV-003`, round 3; plus the user's 2026-08-14 approval that provider-time failures must not cause autonomous retries and that the designer should choose the simplest reasonable preservation design.
- Triggering finding IDs: `AR-FIND-003` (supported non-user turns could execute failed pending compaction because no origin/authorization boundary existed) and `AR-FIND-004` (runner-failure supplement still prescribed superseded SR-002 re-arm behavior).
- Prior authoritative result: `ARCH-REV-003 Fail — Requirement Gap`. `AR-FIND-001` and `AR-FIND-002` were resolved; the B/T/P formula, actual-observation threshold episode, typed runner-versus-response path, accepted commit, and no-migration posture passed.
- Current authoritative result: REQ-001–REQ-015 and all intended-behavior supplements are user-approved. BEH-010, REQ-015, AC-022, and AC-023 define the supported USER/AGENT/SYSTEM outcome; the revised design is ready for architecture re-review.
- Why this revision entry is recorded: both direct `InterAgentMessageReceivedEvent` and production `SenderType.AGENT`/`SYSTEM` carriers reach the same turn-start/request path. Pending presence therefore cannot authorize execution. Dropping/consuming non-user inputs would lose supported work, while leaving one at a head-only FIFO would block the user's recovery message.
- Resolution: stamp `TurnStartOrigin` on the ordinary inbox entry before input conversion; carry it through `AgentTurn`; distinguish `initial_attempt_ready`, `attempt_in_progress`, and `awaiting_user_retry`; authorize the initial attempt for any origin but a post-failure attempt only for a distinct USER-origin turn. Extend the existing queue with first-matching claim and a narrow read-only `CompactionRetryTurnAdmissionPolicy`, leaving non-user entries unclaimed while admitting the earliest USER behind them. Success runs the user turn then resumes ordinary FIFO; failure retains both pending gate and queued non-user entries. No second queue, retry scheduler, persistence, or migration is introduced.
- Approved behavior or requirement IDs affected: BEH-009, BEH-010; REQ-014, REQ-015; AC-020–AC-023. BEH-001–BEH-008 and their prompt/schema/planning/runner/commit outcomes remain unchanged.
- Canonical artifacts and sections updated: `requirements.md` (BEH-010, REQ-015, AC-022/023, approval and coverage); `investigation-notes.md` (MP-002 paths, server carrier evidence, files, design-health classification); `design-spec.md` (DS-006, origin/attempt/queue owners, interfaces, files, recovery, examples, coverage); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: `compaction-runtime-behavior-examples.md` gained the approved USER-versus-non-user queue scenario; `compactor-runner-failure-analysis.md` retained its evidence but replaced the stale SR-002 normative direction with strict fail-closed/USER-only recovery. No prompt, response-contract, storage, or evidence file changed.
- Downstream and architecture-review impact: architecture re-review should resolve AR-FIND-003 against the explicit origin-stamped entry, attempt authorization state, first-eligible same-queue selection, failure/success order, shutdown behavior, and direct USER/AGENT/SYSTEM coverage; AR-FIND-004 should resolve against the aligned supplement. Implementation rework remains blocked until that review passes.
- Next recipient or routing: `architecture_reviewer` for revision round 4 with `ARCH-REV-003`, the cumulative solution package, and all still-relevant supplements/evidence.
- Remaining gaps or risks: provider tokenization remains estimated; runtime-only threshold state can reset on restart; ordinary queued turn-start inputs remain non-persistent and follow existing shutdown drain; one oversized new input lacks general admission/chunking; deterministic validation cannot prove factual summary quality; architecture re-review pending.

### SR-005 — Provider-safe Unicode compaction text

- Triggering role, report path, and round: user live verification of the Electron build on 2026-08-15; screenshot retained at `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/compaction-unicode-request-rejection.png`; exact log/trace correlation in `compaction-unicode-safety-analysis.md`; fifth solution round after `ARCH-REV-004` passed SR-004.
- Triggering finding IDs: BEH-011 / REQ-016. A valid source tool result containing `icon: '🛡️'` was middle-omitted at a 2,000-unit UTF-16 boundary, creating lone U+D83D in the child task. DeepSeek rejected `messages[1].content` with HTTP 400 before inference.
- Prior authoritative result: `ARCH-REV-004 Pass` confirmed SR-004 resolved `AR-FIND-003`/`AR-FIND-004`; `AR-FIND-001`/`AR-FIND-002` remained resolved. The implemented/revised runner path correctly classified the new provider rejection as one `runner_error_completion`, launched no response-repair child, stopped the target-agent turn, and kept the runtime available.
- Current authoritative result: REQ-001–REQ-016 and all intended-behavior supplements are user-approved. The current design adds a narrow provider-safe derived-text boundary while leaving threshold/origin/retry/prompt/schema/commit decisions unchanged.
- Why this revision entry is recorded: the 20% trigger and post-compaction target were correct, but deterministic local UTF-16 slicing corrupted valid source text before the provider could parse the request. Repeating `continue` with the unchanged build would regenerate the same malformed prompt, so truthful fail-closed handling alone is insufficient.
- Resolution: preserve raw traces/tool payloads/canonical memory exactly; add one pure `ProviderSafeCompactionText` utility under memory presentation; make `ReadableValueRenderer` middle omission and `CompactionResponseParser` end clamp surrogate-safe; normalize lone surrogates, line endings, and non-useful controls only in derived text; preserve LF/TAB and valid multilingual/code/path/symbol/emoji content; finalize/assert the complete task prompt before child launch. A local invariant failure produces zero child/provider calls and follows the same fail-closed pending path. No ASCII conversion, emoji-specific subsystem, global provider middleware, retry, persistence rewrite, or migration is introduced.
- Approved behavior or requirement IDs affected: BEH-011; REQ-016; AC-024–AC-026; UC-011. REQ-001–REQ-015 and AC-001–AC-023 remain unchanged except cross-references to the new safety boundary.
- Canonical artifacts and sections updated: `requirements.md` (BEH-011, REQ-016, AC-024–AC-026, UC-011, approval/coverage); `investigation-notes.md` (exact runtime/source/code proof, inventory, file map, findings); `design-spec.md` (DS-007, owner/interface/file/sequence/invariant/coverage mapping); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: added approved `compaction-unicode-safety-analysis.md`; added `evidence/compaction-unicode-request-rejection.png`, `evidence/compaction-unicode-request-rejection-log.txt`, and `evidence/compaction-unicode-truncation-proof.json`; updated approved `compaction-runtime-behavior-examples.md` with the exact Unicode-boundary scenario. No prompt/schema/storage evidence was removed.
- Downstream and architecture-review impact: SR-004 is already architecture-approved. The next architecture review should assess only SR-005's narrow source-versus-derived ownership, shared Unicode utility, local final prompt guard, typed pre-launch failure, no-migration posture, and exact direct fixture coverage while confirming the SR-004 behavior remains unchanged. Implementation rework remains blocked until the cumulative SR-005 package passes.
- Next recipient or routing: `architecture_reviewer` for the next re-review with SR-005, prior review artifacts, and the complete cumulative package.
- Remaining gaps or risks: malformed old/external source becomes U+FFFD only in the provider-facing copy; provider tokenization remains estimated; runtime-only threshold state can reset on restart; ordinary queued turn-start inputs remain non-persistent; one oversized new input lacks general admission/chunking; deterministic validation cannot prove factual summary quality; architecture re-review pending.

### SR-006 — Per-value Unicode bounds and corrected evidence identity

- Triggering role, report path, and round: `architecture_reviewer`; `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/design-review-report.md`; `ARCH-REV-005`, round 5.
- Triggering finding IDs: `AR-FIND-002` reopened for a stale checksum assertion and `AR-FIND-005` for an undefined whole-prompt `plannedRenderedLimit` oracle.
- Prior authoritative result: `ARCH-REV-005 Fail — Design Impact`. The reviewer accepted BEH-011 production reachability and reproduction, source-versus-derived ownership, the pure memory-presentation utility, surrogate-safe middle/end clamping, the completed-prompt guard, typed zero-launch fail-closed handling, accepted-text clamp reuse, direct fixtures, and Directly Usable — No Migration. `AR-FIND-001`, `AR-FIND-003`, and `AR-FIND-004` remained resolved.
- Current authoritative result: the same user-approved REQ-001–REQ-016 behavior is preserved. The stale evidence identity is corrected and directly revalidated, and the design now distinguishes complete-prompt well-formed/control safety from configured per-value/per-accepted-text length limits.
- Why this revision entry is recorded: a checksum is evidence metadata and must match the retained bytes. Separately, a whole-prompt character limit was never defined or approved and could be misread as authority to truncate a valid 540,727-unit child prompt independently of B/T/P planning.
- Resolution: retain `evidence/repeated-compaction-server-log-excerpt.txt` byte-for-byte and correct its SHA-256 to `adc1c471f487ad1aee1ffe4e6176fdd70603218b0dadff39c09af439134148cf`; revalidate the three operation IDs, four prompt observations, and three child prompt lengths. Remove `taskPrompt.length <= plannedRenderedLimit`; require the complete prompt to be well formed/control-safe, require each rendered value or accepted-text clamp to remain within its own configured limit after safe-boundary adjustment, and explicitly introduce no whole-task character clamp. Complete-prompt budget control remains exclusively in the approved B/T/P token path.
- Approved behavior or requirement IDs affected: none. BEH-011, REQ-016, and AC-024–AC-026 are clarified without semantic change; REQ-001–REQ-015 remain unchanged.
- Canonical artifacts and sections updated: `investigation-notes.md` (status, evidence inventory, reviewer routing); `design-spec.md` (Unicode example, exact invariants, executable coverage); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: `repeated-compaction-runtime-analysis.md` checksum and exact sequence metadata corrected. The evidence excerpt itself and all other supplements/evidence remain unchanged.
- Downstream and architecture-review impact: architecture re-review should resolve `AR-FIND-002` against the directly recomputed identity/sequence and `AR-FIND-005` against the per-value boundary plus explicit absence of whole-task character truncation. No implementation work may begin until the cumulative SR-006 package passes.
- Next recipient or routing: `architecture_reviewer` for round 6 with `ARCH-REV-005` and the complete cumulative package.
- Remaining gaps or risks: unchanged from SR-005; provider tokenization remains estimated, and deterministic Unicode/shape validation cannot prove model-summary factual quality. No data migration is required.

### SR-007 — Non-recursive built-in Memory Compactor leaf

- Triggering role, report path, and round: user live verification of the Unicode-safe Electron build on 2026-08-15; screenshot retained at `/Users/normy/autobyteus_org/autobyteus-worktrees/compaction-response-robustness/tickets/in-progress/compaction-response-robustness/evidence/recursive-memory-compactor-ui.png`; exact outer/nested run correlation in `recursive-compaction-root-cause.md`; seventh solution round after `ARCH-REV-006` passed SR-006.
- Triggering finding IDs: BEH-012 / REQ-017. The intended Memory Compactor child produced its response at 176,655 prompt tokens, inherited the global 20% trigger of 123,148, and launched a nested Memory Compactor over its own 540,257-character already-wrapped task. Per-value rendering shortened that task to 2,430 characters, producing the observed small task with two START/END pairs.
- Prior authoritative result: `ARCH-REV-006 Pass` resolved `AR-FIND-002` and `AR-FIND-005`; SR-006's Unicode-safe derived-copy boundary and all earlier threshold, runner, retry-origin, prompt/schema, accepted-commit, and no-migration decisions passed.
- Current authoritative result: REQ-001–REQ-017 and all intended-behavior supplements are user-approved. The current design makes the canonical built-in Memory Compactor a non-compactable leaf while preserving normal-agent compaction and the existing parent-owned initial/correction sibling lifecycle.
- Why this revision entry is recorded: recursive self-compaction is not harmless UI duplication. It adds cost, latency, run-history noise, and a nested task whose meaning is wrong; because the outer child does not complete until immediate self-compaction settles, a nested failure can reject an already-usable outer response. The 80% setting only masks the defect, and the 20% setting is supported.
- Resolution: use the canonical `MEMORY_COMPACTOR_AGENT_DEFINITION_ID` in `AutoByteusAgentRunBackendFactory`; do not invoke `compactionAgentRunnerFactory` for that definition and pass `AgentConfig.compactionAgentRunner=null` through both create and restore. In generic `LlmPhase`, treat runner absence as no automatic-compaction capability: construct/pass no executor, skip post-response evaluation and immediate execution, and return the original LLM result. Normal definitions retain the current runner. A usable invalid output may create one separate correction sibling, also a leaf. A genuinely oversized child fails through existing planning/pre-launch or typed runner handling; it is not recursively summarized. No ratio hack, prompt change, whole-task clamp, persisted flag, historical deletion, or migration is introduced.
- Approved behavior or requirement IDs affected: BEH-012; REQ-017; AC-027–AC-029; UC-012. REQ-001–REQ-016 and AC-001–AC-026 remain unchanged except cross-references to the new capability boundary.
- Canonical artifacts and sections updated: `requirements.md` (BEH-012, REQ-017, AC-027–AC-029, UC-012, approval/coverage); `investigation-notes.md` (exact parent/outer/nested evidence, inventory, source/component map, separate Prisma observation); `design-spec.md` (DS-008, provisioning/core owners, interfaces, files, sequence, invariants, coverage); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: added approved `recursive-compaction-root-cause.md`; added `evidence/recursive-memory-compactor-ui.png`, the LF-line-counted focused `evidence/recursive-memory-compactor-server-log-excerpt.txt` (direct SHA-256 `d357fa1188b4518cc65985f9de9bde22d6f8f7487caaf219bc6857ed79f77681`), and `evidence/recursive-memory-compactor-proof.json`; updated approved `compaction-runtime-behavior-examples.md` with the high-usage leaf scenario. Prompt/schema/storage supplements remain unchanged.
- Downstream and architecture-review impact: architecture re-review should assess only the narrow server definition-aware provisioning guard, generic core null-capability enforcement, direct versus genuinely oversized handling, sibling correction semantics, parent commit/reset regression, exact executable coverage, and no-migration posture. Existing implementation work must not resume until SR-007 passes.
- Next recipient or routing: `architecture_reviewer` for round 7 with `ARCH-REV-006`, the cumulative solution package, and all still-relevant supplements/evidence.
- Remaining gaps or risks: a genuinely oversized one-shot child has no general chunking solution and fails truthfully; provider tokenization remains estimated; the independent token-ledger database mismatch remains out of scope; deterministic Unicode/shape validation cannot prove factual summary quality. No data migration is required.

### SR-008 — Memory-owned automatic-compaction composition

- Triggering role, report path, and round: user architecture clarification after the SR-007 architecture-review handoff; eighth solution round; runtime/source evidence remains `recursive-compaction-root-cause.md` plus the three recursive evidence artifacts.
- Triggering finding IDs: BEH-012 / REQ-017 design ownership refinement. No new runtime failure is introduced; the user explicitly superseded the narrow null-runner implementation boundary while preserving its leaf outcome.
- Prior authoritative result: SR-007 was awaiting architecture review and proposed server null-runner provisioning plus a generic LLM-phase presence guard. It correctly removed recursion but left automatic-compaction composition split between top-level `AgentConfig.compactionAgentRunner`, unconditional `AgentFactory` policy construction, `MemoryManager`, and `LlmPhase` strategy assembly.
- Current authoritative result: REQ-001–REQ-017 and all intended-behavior supplements remain user-approved. SR-008 is the current authority: agent creation supplies one runtime-only discriminated memory-compaction configuration; `MemoryManager` owns it; disabled has no policy/runner; enabled contains the single existing `CompactionPolicy` and current strategy runner; omitted direct-core construction is explicitly disabled, while missing runner composition for a normal server agent fails creation; provider request capacity remains separate; the existing coordinator, registry, and sole `structured-json` strategy remain.
- Why this revision entry is recorded: the user asked for clean domain ownership and modularity now, while explicitly rejecting overengineering. A bare null runner is an accidental binary marker when policy creation and strategy composition remain elsewhere; multiple new policy classes/controllers/strategies would be equally disproportionate.
- Resolution: add one tight configuration type under memory compaction; replace the top-level runner field; default omitted direct `AgentConfig`/`MemoryManager` construction to the complete disabled variant; stop `AgentFactory` from creating a second policy; install/consume configuration through `MemoryManager`; split common provider/model request-capacity arithmetic from enabled-only ratio/trigger derivation without changing formulas; select disabled/no runner-factory call for the canonical Memory Compactor and enabled/current policy+required runner for normal server agents, failing composition rather than silently disabling when that runner cannot be constructed; keep `LlmPhase` as the thin definition-agnostic lifecycle integration. Do not add policy subclasses, a controller hierarchy, a second strategy, chunking, persistence, or migration.
- Approved behavior or requirement IDs affected: BEH-012; REQ-017; AC-027–AC-029; UC-012. REQ-001–REQ-016, AC-001–AC-026, and the actual non-recursive leaf outcome remain unchanged.
- Canonical artifacts and sections updated: `requirements.md` (BEH-012, design-health/refactor posture, REQ-017, AC-027–AC-029, constraints, approval); `investigation-notes.md` (source evidence for the fragmented owners, single policy/strategy finding, component map, reviewer note); `design-spec.md` (DS-008, configuration/capacity shape, owners/interfaces/files/removals/sequence/tradeoffs/invariants/coverage); `solution-revision-record.md`.
- Supplemental artifacts updated, added, or removed: revised approved `recursive-compaction-root-cause.md` and `compaction-runtime-behavior-examples.md` to the memory-owned configuration boundary. The prompt/schema/storage/Unicode supplements and all evidence bytes remain unchanged; no new evidence file is required.
- Downstream and architecture-review impact: SR-007 is superseded before implementation. Architecture review should assess the bounded refactor rather than a null-runner patch: exact disabled/enabled shape, direct-core disabled defaults, normal-server fail-fast runner composition, policy copy/ownership, `AgentFactory` removal, `MemoryManager` boundary, capacity/threshold split, server composition, definition-agnostic LLM integration, preservation of the one current policy/strategy, executable coverage, and no-migration posture. Implementation remains blocked until pass.
- Next recipient or routing: `architecture_reviewer` for SR-008 re-review with `ARCH-REV-006`, the complete cumulative package, and all still-relevant evidence.
- Remaining gaps or risks: a genuinely oversized one-shot child still has no chunking strategy and fails truthfully; provider tokenization remains estimated; mutable policy instances must not be shared across runtimes; independent token-ledger database mismatch remains out of scope; deterministic validation cannot prove factual summary quality. No data migration is required.
