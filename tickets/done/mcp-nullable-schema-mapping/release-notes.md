## Fixes
- Fixed configured MCP tools with optional array inputs so fields such as video input images are exposed as arrays instead of strings.
- Fixed configured MCP tools with optional object settings so generation configuration fields keep their object shape.
