# Actual API/browser evidence — API-REV-001
Owned production TS server + normal Nuxt app; no synthetic page or successful response replay. Small/Low current history-publication ticket only.

## Replay/setup
From worktree: `pnpm --dir autobyteus-server-ts build`; focused frontend commands in canonical investigation/report. Place retained synthetic `fixtures/{agents,agent-teams,agent-orgs}` under a **fresh owned** `.local/api-history-latency/data`; never replace user data. launch.py uses minimal environment/isolated HOME and loopback51181/51182/51183, discovery51184 unused. Official built `dist/secret-management/cli/import-local-environment-secrets.js --source <explicitly authorized .env> --database-url file:<owned data>/db/production.db --dry-run`, then ordinary confirmation import only to owned empty database. This run imported9 new/0 replacement secrets; normal fresh DB initialization, not user-data migration. No secret values attached. Existing ignored private runtime data remains local and must not be published.

Run `python3 validation/api-live/launch.py backend`, then proxy and frontend from ticket-relative correct paths. Each records exact PID/command in ignored owned runtime. Browser startup is localhost51183; UI locale zh-CN. Create one Agent, one Team, one Org via normal catalog Run and Send the short distinct LATENCY messages; three actual gpt-5.4-mini replies, then sidebar Stop. Files in fixtures are authored setup only, not fabricated runtime history.

## Timing/fault controls
proxy.mjs forwards actual backend bytes. Set owned runtime control.json `hold:[operation names]` to delay release, or `fail:[operation names]` for explicit503 network fault; `{}` restores normal. It never manufactures a successful result. Log request/backend_ready/released independently. Hold scoped GetWorkspaceRunHistory as well during disclosure to prevent a second query from masking missing first publication. Actual browser controls only; no API mutation/store injection used as acceptance.
- normal focused startup: timing-summary.json and focused-normal-startup-*; 1ms backend each, <=172/171ms response-to-observed group DOM. Polling upper bounds, not paint timing or universal SLA. Initial catalog page intentionally collapses workspace: normal-startup-timing.json false group markers do NOT measure rendering delay. No baseline browser speedup ratio claimed.
- org-first/workspace-first: both real history orders with definition/avatar catalogs unresolved; exact hold sets and DOM/screenshots/transport.
- active-hydration: actual Team resume response held ~6.4s; Org selected Idle with reply while global history loading; Team member projection requests occurred AFTER release (not independently held). Team/Agent normal reconnect then exact conversations recovered; provider call count stayed3.
- quiet failure: each family's ordinary5-second polling response returned503 independently; rows/selection/draft/history stayed; normal next success cleared error without reload. Mounted never-used leaf remained Offline/empty.
- preservation-proof, stopped-data-before/after:16 unchanged persistent files; final-integrity:6 authored files and all4 input manifest files unchanged; actual API active flags false,3 total deliberate provider requests.

## Limits and cleanup
Attachment picker setFiles was rejected by browser extension; native fallback stopped upon user browser-control interruption. No attachment upload completed and no attachment-specific success claimed. User allowed continuing without it. Chrome build/version not queried; normal viewport screenshot1512x862, zh-CN. DOM-only evaluate lacks navigator, so version collection not used as evidence. No Electron or production-volume latency claim.
cleanup.json records exact owned six-process tree stopped,3 ports closed,owned tab closed,2 intake-absent generated SDK dist outputs removed; no user services/actions. API did not modify production/durable tests. Logs/captures retained here, private DB/key remain ignored, uncommitted. Source typecheck/full-suite limitations in canonical report.
