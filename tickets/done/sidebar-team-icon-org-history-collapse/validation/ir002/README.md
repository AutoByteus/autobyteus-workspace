# IR-002 implementation-local validation — 2026-09-16

Authority SR-006 / DS-REV-002; Medium / Low. This is not API/E2E acceptance. Repository HEAD remains 75a42f18b3cf8555bef2496b03679c41575ad915. `implementation-manifest.json` pins tested cumulative source; `ir001-preservation.json` confirms all 19 files in the IR-001 manifest remain byte-identical. No source/backend edits from an independent reviewer claimed.

## Executable checks
Run from worktree root unless stated otherwise.

- `pnpm -C autobyteus-web test:nuxt components/agentOrgs/__tests__ components/common/__tests__/ConfirmationModal.spec.ts stores/__tests__/agentOrgDefinitionStore.spec.ts stores/__tests__/agentOrgDefinitionCache.spec.ts components/workspace/history/__tests__/WorkspaceAgentOrgDisclosure.spec.ts components/workspace/history/__tests__/WorkspaceHistoryWorkspaceSection.spec.ts composables/__tests__/useRunHistoryAvatarState.spec.ts composables/__tests__/useWorkspaceHistoryTreeState.spec.ts stores/__tests__/agentOrgHistoryApollo.spec.ts --run`
  - **122 passed / 13 files**, `focused-final.log`. Includes 29 new IR-002 tests: 12 authoring-actions, 4 avatar/editor, 11 real Apollo cache and 2 common-modal cases. Existing hidden-field/exact-owned authoring, original sidebar/disclosure/tree/history tests also pass.
  - First run failures were test selector/expected-label assumptions (new file input precedes name input; existing name selector made semantic), and event inspection after wrapper unmount; corrected durable tests, no assertions removed to mask behavior. Initial logs retained, not final acceptance.
- `pnpm -C autobyteus-web test:nuxt components/workspace/history/__tests__/WorkspaceAgentRunsTreePanel.spec.ts components/workspace/history/__tests__/WorkspaceAgentRunsTreePanel.regressions.spec.ts components/workspace/history/__tests__/WorkspaceAgentOrgActivityPublication.spec.ts --run`
  - **54 passed / 18 failed / 16 errors**. `broader-recheck.log`; `baseline-comparison.txt` confirms exact failed test identities equal the original HEAD control logs preserved from IR-001. Missing selection mock methods/old nested-Team expectations and stop-phase expectation remain outside this ticket. New catalog integration case is among passes. No all-suite-green claim.
- `pnpm --package=vue-tsc@3.2.4 --package=typescript@5.9.3 dlx vue-tsc --noEmit -p autobyteus-web/tsconfig.json`
  - **Exit 2**, `typecheck-final.log` (563 lines with core/SDK builds prepared). Existing project dependency/type errors remain. New real-cache test also imports `@apollo/client/core` and shares the existing unresolved type-module diagnostic; executable Vite resolution works. No diagnostic remains in changed IR-002 production files. Original readonly hierarchy-prop diagnostics/attachment fixture issue unchanged. Not a clean typecheck. Dependency preparation reduces log size compared to IR-001, not proof of fixes.
- Isolated server service/provider/domain/transaction tests, **3 passed**, `server-local-final.log`:
  1. omitted avatar stays; replacement/empty-string normalization persists; hidden fields/revision conflicts preserved;
  2. exact package removal includes physical owned descendant fixture files while sibling shared/history/asset markers remain byte-identical; duplicate returns false;
  3. external-read-only edit/delete rejected, package bytes unchanged.
  These filesystem marker assertions are not full valid shared/local catalog/run acceptance; downstream real fixture coverage remains required.
  Replay without unrelated Prisma global reset: build `pnpm -C autobyteus-application-sdk-contracts build` and `pnpm -C autobyteus-ts build`; `pnpm -C autobyteus-server-ts exec prisma generate` (client generation only, no DB reset); copy this folder's `vitest.org-avatar-local.config.ts` to server root; run `pnpm -C autobyteus-server-ts exec vitest run --config vitest.org-avatar-local.config.ts`; remove that owned temporary config afterward. Durable test lives in normal server tests directory. First attempts failed to collect due missing built SDK/Prisma client; setup logs retained. SDK untracked dist cleaned after checks; normal ignored core/client dependency builds remain. No application server/provider launched.
- `git diff --check`: pass. Production nonempty line maximum430, all deltas below220. `source-size-check.json` and manifests retain specifics.

## Rendered feedback loop
Used actual AgentOrgExperience, Avatar/Editor, ConfirmationModal, HandoffManager, localization, CSS, Vue Router, and real FileUpload Pinia store in an owned Vite browser renderer. `renderer-preview/` records replay fixture. Definition store/REST/Apollo are explicit local doubles; upload double receives selected File/FormData and returns a blob URL. No backend/user server, real upload endpoint, catalog filesystem, provider or user data was touched. Native file-picker UI/keyboard activation and wide desktop viewport are NOT acceptance here.

Inspected 400×738 CSS viewport at 2x pixel ratio: optional empty image form; selected PNG pending status + disabled Save; loaded64px fixture preview; Save→catalog/detail image; remove→Save→initials; safe named Delete warning in English/Chinese; visible Cancel focus; pending duplicate/cancel disabled; read-only rejection stays modal; Cancel; subsequent successful disposable deletion routes list and modal disappears after existing300ms transition. Stable English/Chinese/error screenshots inspected directly; no in-scope visual defect observed. Native keyboard/focus-trap policy unchanged. Fixture-only unwrapped event log caused horizontal overflow; styled wrapping in recorded fixture (not production) and confirmed dialog/product width400. A screenshot captured during transition was discarded rather than treated as stable UI.

Evidence: `form-empty.png`, `form-avatar.png`, `catalog-avatar.png`, `delete-error.png`, `delete-zh.png`. Catalog screenshot predates fixture event-log wrap, so its log overflows; product cards and controls do not. These are rendered inspection evidence, not downstream functional acceptance.

Replay: copy `renderer-preview` into web `.local/org-authoring-preview`; from web run `node ../node_modules/.pnpm/node_modules/vite/bin/vite.js --config .local/org-authoring-preview/vite.config.mjs`; browse `http://127.0.0.1:4318/#/agent-orgs?view=org-edit&id=disposable`. Owned tab6467d9 closed, Vite PID48064 stopped, port4318 verified free, active fixture directory removed. Initial path/alias/restart setup difficulties fixed before inspection. Do not connect fixture to a real user backend.
