# Investigation — SIDEBAR-ORG-20260916-001
## Bootstrap
2026-09-16. Fresh remote fetch succeeded: origin/requirements/flat-agent-organization-model75a42f18b3cf8555bef2496b03679c41575ad915 (newer than completed timeout ticket). Dedicated worktree /Users/normy/autobyteus_org/autobyteus-worktrees/sidebar-team-icon-org-history-collapse; branch codex/sidebar-team-icon-org-history-collapse. Eventual same feature base, NOT personal. Prior completed tickets not reopened. Base untracked SDK outputs and org-history-resume-offline-analysis notes preserved untouched. Worktree creation completed successfully before task documents.
## User evidence
Three provided screenshots viewed in conversation: top-level workspace Team groups show SE/AW initials while Org group has building icon; mounted Teams already show people/group icons; user reports Org RUN history row chevron cannot collapse while nested Team chevrons can. Screenshots alone show the appearance, not an executed click failure. No app launch, provider work, source edits or test execution yet.

## Source investigation — 2026-09-16
Pinned HEAD 75a42f18b3cf8555bef2496b03679c41575ad915 verified in isolated branch. Read autobyteus-web/AGENTS.md. Commands were bounded `rg`, `sed`, `git status --short`, `git rev-parse HEAD`, and `git branch --show-current`; no production edits or runtime tests. One read attempt used skill directory rather than worktree and returned file-not-found; corrected immediately, no evidence inferred from that failure.

### E-001 — Team group artwork
`autobyteus-web/components/workspace/history/WorkspaceHistoryWorkspaceSection.vue:186–222` renders Team definition groups. The header uses supplied Team image when available, otherwise initials in a round background. Team activity dot, count, name and definition disclosure are separate. This matches screenshot SE/AW. `WorkspaceAgentOrgHistoryCollection.vue:7–16` uses a generic building icon for Org definition groups. Mounted Teams already use `heroicons:user-group-20-solid` in that collection around lines87–91. Agent initials are a separate render path and not implicated. Proposed uniform Team-group icon policy includes groups with custom images at THIS surface only; explicitly present this for user approval, do not alter avatar data/catalog/detail UI.

### E-002 — Org run disclosure and open conflated
`WorkspaceAgentOrgHistoryCollection.vue:21–33` puts chevron inside the run-open button (`@click="openRun(run)"`). At lines161–164, openRun toggles only if `!run.isActive || !isRunExpanded(run.rootRunId)` and always calls onOpenAgentOrgRun. Thus for active+expanded it never calls collapse; this is a source-confirmed cause consistent with reported live Org failure, not a recorded runtime reproduction. For inactive runs it can toggle but still opens/navigates, so a pure disclosure contract is missing for both states.
`WorkspaceAgentRunsTreePanel.vue:409–411` routes that action through `useWorkspaceHistorySubjectActions.ts`; its execute open action performs inspection and navigation, with selection intent, not just disclosure. Do not use run-open as a disclosure primitive.
`useWorkspaceHistoryTreeState.ts:333–339` already owns exact-root expansion and toggle independently of run activity. `359–430` reveals ancestors on changed selection identity once; topology refresh alone does not repeatedly force expansion after a reveal already applied. Keep that genuine navigation reveal behavior while ensuring disclosure alone does not trigger navigation or selection changes. No need to hypothesize backend/lifecycle fault.

### E-003 — Coverage boundaries
`WorkspaceHistoryWorkspaceSection.spec.ts:230–263` covers mounted Team action, stop and locale preservation but does not exercise Org ROOT disclosure. Its default fixture always returns true for root expansion, so merely adding a toggle-spy assertion would not prove hide/re-expand behavior. `useWorkspaceHistoryTreeState.spec.ts:332–371` covers Org ancestry reveal across selection/catalog registration, not user's collapse with retained selection. Downstream regression must combine actual root disclosure interaction with reactive expansion, and verify unchanged selected descendant and no open/inspect/stop side effects, plus history refresh durability. Real browser click coverage still required downstream; none executed here.

## Supplement inventory
- evidence/sidebar-team-org-headers.png: user supplied appearance evidence for REQ-001; reference, not Product-owned prototype.
- evidence/org-run-expanded.png and evidence/mounted-team-expanded.png: user supplied hierarchy appearance for REQ-002/003; not proof of clicked behavior.
All copied only from the three explicitly attached images. No private DB, credentials or runtime data copied.

## Status / unknowns
Requirements SR-001 Ready for Approval. Need user's approval of presented scope including consistent Team icon at group headers. No authoritative architecture yet; design/size/risk classification follows approval. No source/test edit, app launch, provider operation, commit/push/merge, or finalization. Other tickets remain separate. Initial dummy Product message was connectivity-only and not a Product Design request.

## E-004 / SR-002 — User clarifies avatar-first policy
User asks current behavior and explicitly requests avatar when present, Team icon otherwise, same principle for Org. Re-read current Team and Org header templates: Team already has img/showTeamAvatar/error handler then initials; Org unconditionally renders building icon. Thus current behavior is NOT symmetric. SR-001 proposed hiding supplied avatars is withdrawn, not approved. Update REQ-001/AC-001 and SCN-001 to avatar-first for both headers, keeping Agent avatars and other surfaces unchanged. Org avatar data path remains to investigate; no implementation claim. Broken-image fallback uses respective icon rather than broken image. Complete requirements approval pending; no design/source changes.

## E-005 — Architecture investigation: existing Org avatar capability
At pinned base, `autobyteus-server-ts/src/agent-org-definition/domain/agent-org-definition.ts:37,50,62` declares optional input, stores string/null. `providers/agent-org-definition-config.ts:137–154` normalizes absent read to null and serializes the known field. `providers/file-agent-org-definition-provider.ts:72–87` loads it from org-config.json; GraphQL `src/api/graphql/types/agent-org-definition.ts:34,45,57,91` exposes avatarUrl and inputs. Frontend `graphql/queries/agentOrgDefinitionQueries.ts:1–10` already requests it; `stores/agentOrgDefinitionStore.ts:15–25,41–59` retains it, provides reactive definitions/byId and bound-backend-aware fetchAll. No schema, server or authoring changes needed for supplied avatars. Evidence is source reading, not live image success. Existing Org editor creation uses null and does not expose a new avatar editing surface; this task concerns consuming supplied definition avatars in sidebar, not inventing an uploader.
`utils/runTreeProjection.ts:390–404` history group contains definition identity/name/runs but no avatar. History may outlive its definition. Keep history separate: derive visual metadata by exact definition ID from existing definition store; missing catalog must not suppress historical groups.

## E-006 — Existing frontend owners and design feasibility
`composables/useRunHistoryAvatarState.ts` owns avatar lookup/failure state; Team URL map is computed from definition IDs, broken-image keys include URL, loading completion resets errors. `WorkspaceAgentRunsTreePanel.vue:160–208,382–394,427–439` wires Agent/Team stores and bindings and fetches their catalog on mount, but not Org definition catalog. Extend the same owner/bindings with Org methods, pass them via `WorkspaceHistoryWorkspaceSection.vue:309–315` to Org collection. No fetch per Org row; no history DTO avatar field/copy.
`workspaceHistorySectionContracts.ts:49–74` is the internal renderer binding contract; extend locally with exact Org definition ID getters/error callbacks. Existing tree state already toggles root expansion and preserves nested flags; changing whole shared selection/watch mechanism is unnecessary. Title opening should expand only when collapsed, then keep existing subject-action navigation; never toggle closed on a title click. Chevron is a separate sibling native button using existing toggle with stop propagation; no nested buttons/no open command. Existing selected-ancestry watcher reveals genuine identity change once; no new watcher required.
Existing localized Org labels are in `localization/messages/en/workspace.ts` and `zh-CN/workspace.ts`. Read attempts at nonexistent useWorkspaceHistoryAvatars.ts and locales/ were corrected to actual useRunHistoryAvatarState.ts and localization/messages; no behavior inferred from missing paths. No avatar-specific composable test file found: add focused coverage rather than claiming existing regression protection.

## Approval and current status
USER-APPROVAL-20260916-001 expressly authorizes design on clarified avatar-first requirements and conditional Org avatar support. E-005 resolves condition: persisted/API support exists; only sidebar integration missing. SR-003 approved basis replaces earlier pending status; earlier entries remain chronology. Design DS-001 complete, classified Medium/Low due bounded multiple frontend wiring files, not runtime/schema/ownership rewrite. No app launch, durable tests, production edit or implementation success claimed. User-provided images remain reference evidence; actual interaction and avatar load validation delegated downstream.

## E-007 — Org/Team authoring UI comparison
Current source `components/agentTeams/AgentTeamDefinitionForm.vue:11–46,412–455` supports image picker, uploadFile, preview, remove (empty-string draft), loading/error and saves URL; accepted image types JPEG/PNG/GIF/WebP. Shared `stores/fileUploadStore.ts` uploads /upload-file returning fileUrl. Org `components/agentOrgs/AgentOrgExperience.vue:108–153,327–357` lacks avatar form; create hardcodes null, update visibleInput omits avatar. Org catalog/detail use initials at lines38/73, so sidebar-only support is not authoring parity. User explicitly requested adding this now. No tests/uploads performed by Designer.

## E-008 — Existing Org delete authority and exact scope
Team detail uses AgentDeleteConfirmDialog and deletion store action (`AgentTeamDetail.vue:33,66–74,140–144`). Org detail has only Run/Edit. OrgDefinitionStore.remove exists and checks GraphQL errors/boolean then filters definitions; its query is cache-first, so cache refresh coherence needs investigation in design, not assume filtering Pinia is sufficient. Existing Org GraphQL delete → Studio-bound AgentOrgDefinitionService.deleteDefinition → FileAgentOrgDefinitionProvider.delete (139–145). Host composition supplies that real service/provider, confirmed in compositions/host-definition-services.ts:39 onward and api/graphql/studio-application-api-services.ts:89.
Provider validates source writable by runtime; external_read_only and implementation_repository rejected by definition-source-descriptor.ts:43 onward. It invokes DefinitionPackageTransaction.remove:120–127: locked recursive rm of exact Org package directory. Thus package-owned children inside orgDir/agents and orgDir/agent-teams are removed too; verified agent-org-owned-definition-source-index.ts:43–77. Separately shared references and runtime history directories are not traversed. No active-run stop or reference safety guard seen in this delete path; do NOT invent a current guard or claim safe future restoration without definitions. UI confirmation must name package-owned scope. Tests of deletion must use disposable owned fixtures only, not user's live Software Development Department or imported package.

## E-009 — Removal encoding and preservation detail
AgentOrgDefinitionService.updateDefinition uses `v.avatarUrl ?? existing.avatarUrl`; null means preserve there, but empty string becomes null in domain constructor. Existing Team UI removal sends empty string. Revised design must explicitly choose existing supported clear encoding and preserve omitted unchanged fields rather than casually sending null and claiming it clears. No schema change decided/needed yet. Org existing tests asserting update has no avatarUrl must become intent-sensitive (unchanged vs explicitly edited), not blindly deleted. Upload-file is separate from definition save; cancellation may leave uploaded asset, same existing policy, no new garbage-collection scope.

## SR-004 coordination / evidence inventory
User asks to add avatar authoring and detail Delete in same ticket; Team detail screenshot copied to evidence/team-detail-delete-reference.png. Read prior handoff before recovery. Existing Implementation run implementation_engineer_f84b5074541a47fea830604d1bcb77c3 successfully received coordination via send_message_to: preserve current sidebar code/tests; no new authoring work/complete-ticket forwarding until revised basis; bounded existing work can continue. Not a claim of acknowledged hold or new assignment. Incoming many dirty frontend source/tests belong to Implementation and remain untouched. Designer only revises ticket docs/evidence. Scope SR-004 Ready for Approval, design Needs Revision; no new classification or forward handoff yet. Read-only source exploration encountered nonexistent guessed paths and corrected through rg; no runtime evidence inferred.

### SR-004 coordination acknowledgment
Implementation Engineer acknowledged hold in received message 2026-09-16. Read implementation-handoff.md and implementation-revision-record.md including Scope-change hold. IR-001 original sidebar implementation/tests/evidence retained, no commits or new authoring/Delete work. Original scope had already been delivered to API existing run api_e2e_engineer_450b25f17f2245fe89b9c52cd17038ae before Designer notice. Implementation states it is notifying that execution not to forward expanded ticket as complete; API acknowledgment/delivery of that new hold is not yet independently confirmed here. No duplicate API assignment sent by Designer. Reported original focused passes and baseline failures remain IR-001 evidence, not acceptance of expanded scope. Refined SR-004 requirements approval remains pending, no design resumption or new handoff from this acknowledgment.

## E-010 — Original-personal nested Team definition deletion
User explicitly asked source comparison before completing revised design. Read pinned origin/personal5645b49d6 (Sep11) using git show, no fetch/checkout/runtime action. Full source lines, limits and comparison in personal-team-deletion-comparison.md. Old detail-confirm → store/API → service/provider removes selected Team package directory recursively; no member-reference graph deletion. Local nested Team directories therefore removed, shared referenced definitions stay. Old path does not Stop runs/delete history. Current Org package provider already matches this ownership principle, with explicit source restrictions; no need to copy old nested traversal. Old Apollo callback success limitation noted, not inherited as new design authority. Supplement evidence-only for REQ-005/AC-007/008; user approval not required for historical facts.
USER-APPROVAL-20260916-002 received before this question: user “yesss. so i think the requirement is clear now” after shared/local-folder boundary confirmation. Expanded requirements Approved; revised architecture still pending, implementation hold on new scope remains.

## E-011 — Revised architecture integration evidence
Re-read current AgentOrgExperience hydrate/save and agentOrgDefinitionStore create/update/remove. Existing form metadata omissions intentionally preserve hidden fields and cached GraphQL __typename members are projected by store. Existing tests AgentOrgExperience.spec.ts / AgentOrgExperienceApolloEdit.spec.ts / AgentOrgOwnedAuthoring.spec.ts / stores/__tests__/agentOrgDefinitionStore.spec.ts require unchanged-avatar omission; preserve that assertion for unchanged avatar and add explicit edited/cleared cases. Update avatar with empty string for removal: service `v.avatarUrl ?? existing.avatarUrl` accepts empty string, domain normalizes it to null, canonical parser accepts null. Sending null on update would preserve old image; no backend semantic change necessary.
Existing mutations return full definition fields; normal Apollo InMemoryCache supports normalized updates (plugins/30.apollo.client.ts). Org store currently only edits Pinia list, not cached collection membership on create/delete. Extend that store after successful mutation result to update existing GetAgentOrgDefinitions cache membership, rather than invalidating history or copying old unconditional Team cache eviction. Real Apollo test fixture pattern already exists in components/agentOrgs/__tests__ and test-support/agentOrgApolloFixture.ts.
`components/common/ConfirmationModal.vue` supplies shared named title/danger variant/localized Cancel but uses message v-html and lacks pending control. Use a small additive default message slot and pending prop to reuse its shell; Org passes name/scope/error via safe text interpolation, disables actions during one in-flight delete. Existing callers without slot/pending retain behavior. Do not pass unescaped Org names to message HTML. No global dialog rewrite needed.
Org create/edit avatar upload should be an owned small component using existing fileUploadStore; key its lifetime by current draft (view/Org ID), suppress late completed upload after disposal; parent owns draft string/changed intent and prevents Save while upload pending. Multiple distinct state owners are not needed. Org catalog/detail/editor image reuse can be a local presentation component carrying only image failure and initials, not a new shared framework or definition fetcher. Existing sidebar icon fallback remains independent artwork at its own surface.
Read implementation-handoff/revision scope hold: IR-001 original sidebar code/tests are already present and preserved. Reported 71 focused+1 panel pass and broader 18 baseline failures/16 errors remain bounded historical self-validation; no acceptance inferred. API was already notified by Implementation for original scope; revise via same existing workflow, no duplicate API assignment by Designer. No new tests/runtime/upload/delete executed by Designer.

## Current supplement inventory additions
- personal-team-deletion-comparison.md: evidence-only historical pinned source, REQ-005/AC-007/008; current and historical folder boundary, no runtime equivalence claim.
- evidence/team-detail-delete-reference.png: user supplied Team detail reference for new Org detail Delete, not a Product spec or actual delete.
- design-spec.ds001-historical.md: immutable prior sidebar technical basis for IR-001 lineage; superseded, not current approval authority.
- implementation-handoff.md, implementation-revision-record.md and validation/: Implementation-owned IR-001 evidence plus scope hold; read-only Designer inputs. Remain cumulative downstream references, not designer acceptance results.

## SR-006 current status
Expanded requirements explicitly approved and reconfirmed by user. DS-REV-002 complete, Medium/Low based on same existing authoring/store/API/persistence boundaries, no schema or deletion semantic change. New UI exposes existing definition operation; irreversible package boundary documented and tested downstream, not hidden. Route lookup after full package persistence. Scope-change hold only lifts for new work upon revised authoritative handoff, not upon these notes alone. Finalization/user-data mutation never authorized by requirements approval.
