# Antigravity CLI Runtime Redesign — Solution Revision Record

## Revision Index

| Revision ID | Phase | Trigger | Prior status | Current status | Result |
| --- | --- | --- | --- | --- | --- |
| SR-001 | Requirements/Evidence | User's 2026-09-24 redesign request and fresh-base investigation | N/A | Draft, no approval | New worktree and evidence-backed draft requirements; interaction decision pending. |
| SR-002 | Evidence | User's 2026-09-24 first-message identity/CLI improvement clarification | Draft | Draft, no approval | Distinguished PTY input delivery from instruction authority/model adherence; confirmed current CLI flags. |
| SR-003 | Evidence/requirements refinement | User's 2026-09-24 request for enough AGY experiments to support later design | Draft | Draft, no approval | Bounded positive/negative live CLI matrix; corrected early custom-agent overclaim, clarified fail-closed binding and soft-denial acceptance, kept UX and continuity decisions open. |
| SR-004 | Requirements conversation/design direction | User asked for the proper design based on experiments | Draft | Draft, no approval | Recorded a non-authoritative staged transport recommendation and launch-scope decision; no design spec or implementation route. |
| SR-005 | Evidence clarification | User asked whether AutoByteus `agent.md` can be copied directly and whether that was tested | Draft | Draft, no approval | Tested one actual repository agent file verbatim with name-match/mismatch and an adapted control; clarified body reuse versus general raw-copy integration. |
| SR-006 | Evidence/requirements preference | User asked if responses can arrive without node-pty and render/store normal frontend/raw traces | Draft | Draft, no approval | Verified ordinary-pipe stream events and current recording spine; found provider trace-fidelity limit; revised recommendation toward structured chat conditional on permission/trace approval. |
| SR-007 | Approval capture/readiness | User said “i approve the ticket” and asked for confidence | Draft | Ready for Approval; direction approved, full baseline unconfirmed | Recorded high-level approval and proposed exact behavioral interpretation; did not treat unspecified permission/trace/continuity decisions as automatically approved. |
| SR-008 | Evidence/requirements clarification | User explained routine AutoByteus `autoExecuteTools=true` usage and asked whether the AGY concern was merely missing approve/deny UI | Ready for Approval; proposed scoped-only policy | Ready for Approval; auto-approved AGY path supported by bounded test, disabled path unconfirmed | Corrected permission interpretation: AGY's all-tools flag executed a harmless command via structured stream; proposed mapping to per-run auto-approve while leaving global-default/toggle changes and disabled-path behavior unapproved. |
| SR-009 | Evidence clarification | User asked whether non-auto-approved headless AGY even emits JSON identifying a tool needing approval and requested experiments | Ready for Approval | Ready for Approval | Live stream shows attempted tool and post-denial error JSON, not a pending/resumable approval request; disabled-path decision remains open. |
| SR-010 | Requirement decision/evidence | User accepts lack of headless per-tool approval and explicitly directs AGY-specific auto-execute true by default | Ready for Approval; permission decision partly open | Ready for Approval; default-on permission portion approved, explicit-false and other baseline points open | Added AGY-specific default-on requirement/AC without changing other runtimes; inspected current frontend default/inheritance paths. |
| SR-011 | Requirement refinement/design direction | User reiterates AGY should become auto-execute true when selected and says not to make explicit-off an independent design concern | Ready for Approval; default-on approved, false UX queried | Ready for Approval; permission/default behavior resolved, remaining trace/continuity/team baseline open | Clarified selection-time scope across new standalone/team/member drafts; explicit off follows ordinary AGY policy with visible denial, no manual approval feature. |
| SR-012 | Requirement clarification/evidence | User asks what old-run import and AGY-exposed-only traces mean, says old unmerged AGY work is unused and removable | Ready for Approval; old-run import proposed out of scope, trace pending | Ready for Approval; no legacy AutoByteus AGY run import confirmed, trace fidelity still pending | Separated unused historical worktrees from product run data; proposed exact NDJSON archive plus normalized trace while disclosing omitted tool payloads. |
| SR-013 | Requirement correction | User states no AutoByteus AGY history could exist because old worktrees were never implemented/integrated; asks not to overthink | Ready for Approval; hypothetical no-import decision lingered | Ready for Approval; legacy migration removed as N/A | Removed invented historical-run scenario from active requirements/baseline; kept only factual worktree evidence and future-run restore. |
| SR-014 | Requirement direction | User clarified normalized AutoByteus raw traces | Ready for Approval | Ready for Approval | Separate provider NDJSON archive no longer proposed. |
| SR-015 | Evidence | User requested exact AGY tool JSON captures | Ready for Approval | Ready for Approval | Captured read/write/command/denial/multiturn provider stream evidence. |
| SR-016 | Requirements approval | User confirmed complete baseline | Ready for Approval | Approved | Day-one standalone/team/org AGY behavior authorized. |
| SR-017 | Architecture design | Completed initial design | Design N/A | Ready for independent review | Large/High design package submitted. |
| SR-018 | Design recovery | ARCH-REV-001 DR-001 | Review Fail | Needs Revision | Reproduced task-root failure; investigated capsule/skill alternatives. |
| SR-019 | Design correction | User chose minimal workspace stanza | Needs Revision | Architecture Design Complete | DR-001 correction; ARCH-REV-002 later Pass. |
| SR-020 | Requirement gap | IR-001 green tool-card request | Approved neutral mapping | Ready for Approval on narrow delta | Historical proposed display compromise, superseded by SR-021. |
| SR-021 | Requirements/design correction | User-approved DONE-to-success after command matrix | Ready for Approval | Approved; design reviewed | ARCH-REV-003 Pass, downstream implementation/validation proceeded. |
| SR-022 | Evidence clarification | User's packaged-Electron MCP schema/config observation | User-verification hold | Hold unchanged | Verified scoped MCP delivery; no new design or approval. |
| SR-023 | Design impact | User's large AGY Org launch stall screenshot and live browser/backend reproduction | Integrated user-verification hold | Architecture Design Complete; re-review required | Batch Org validation and bounded async AGY discovery; approved behavior unchanged. |
| SR-024 | Design impact | User's 1.4.80 Electron first-turn Org-member error and packaged AGY materializer repro | ARCH-REV-004/SR-023 and IR-008/API-REV-008 user-verification hold | Architecture Design Complete; independent review required | Safe team-package file-link snapshot in configured skills; approved behavior unchanged. |

## SR-001 — Fresh-base investigation and draft requirements

- Prior authoritative result: N/A for this new package. The old June ticket is historical evidence, not imported as an approved baseline.
- Current authorities: `requirements-doc.md` Draft; `investigation-notes.md` In progress; design spec N/A — not yet approved for design.
- Affected IDs: SCN-001–005, BEH-001–005, REQ-001–007, AC-001–006, DEC-001–003.
- Intended behavior changed: Not yet authoritative. The UI/permission choice remains open.
- Approval impact: Explicit user approval required before final design. User's request authorized investigation and workspace creation only.
- Evidence: CLI `1.2.9` supports custom main agents and structured conversation IDs; today's shared identity composer exceeds old AGY's first-app-message identity injection.
- Design/review/routing impact: No implementation-ready route. Design will be built against the new base after user approval, refreshed again if the base advances.
- Handoff rule result: `get_handoff_rules` returned only completed-architecture and delivery-receipt conditions; none matches this Draft approval hold. No recipient was notified.
- Remaining gaps: DEC-001–003; bounded validation of selected CLI mode and exact restore; final scenario/AC refinement.
- Next action: Present findings, obtain the user's interaction/data decision, then complete approval-ready requirements and architecture.

## SR-002 — First-message identity clarification

- Phase: Evidence clarification; triggering user question asks whether first-message identity actually works and whether AGY improved.
- Prior/current requirements status: Draft / Draft; design N/A — not yet approved.
- Finding: Old unit and integration tests prove formatting and PTY delivery of identity-like text on the first *app-mediated* input, not model adherence or high-priority system authority. Human direct terminal input bypasses that path. The local CLI moved from `1.2.9` to `1.2.10` during investigation; its newer custom-agent and structured-ID flags remain available.
- Affected IDs: BEH-002, BEH-003, REQ-002, REQ-003, DEC-001. Intended behavior unchanged; no new approval required for this evidence clarification.
- Next action: Explain the evidence/limits to user; continue requirements decision and selected-mode verification.

## SR-003 — AGY capability experiments and corrected draft

- Phase/trigger: Evidence clarification and draft requirements refinement after the user's request for comprehensive AGY experiments.
- Prior/current status: Requirements Draft / Draft; design N/A — not yet approved. Current evidence supplement: `agy-cli-experiment-report.md`; adjacent scripts and sanitized outputs are reproducibility records.
- Evidence: Installed AGY `1.2.10` custom main agent honored system identity with `--new-project`, while no-new-project accepted `--agent` in `init` but did not load local identity. First-user-message identity was remembered in a simple turn but overwritten by later user direction; a custom-agent system identity resisted that bounded conflict. Stream `init` reported CLI ID early, exact resume worked, nonexistent exact ID silently created a new ID, hook captured ID only on tool invocation, concurrent isolated primary folders kept identity separate, and headless permission denial returned exit 0 / overall SUCCESS while tool step errored. Native TUI response test was inconclusive; see full report.
- Factual correction: SR-001's initial CLI `1.2.9` no-turn `init.agent` observation is not proof that the custom file was loaded. The original entry remains historical; `investigation-notes.md` now records the correction explicitly.
- Affected IDs: BEH-002–005, REQ-002–005, REQ-007–008, AC-002–003, AC-006–007, DEC-001–004. REQ-008 / AC-007 make the already-proposed coherent approval UX observable; their intended behavior remains **proposed**, not approved. No authoritative design was revised.
- Approval impact: No previous approval exists. The user must still approve the completed intended-behavior baseline, including terminal/chat, permission, historical-run, and identity-evolution decisions, before authoritative architecture design.
- Design/review/routing impact: Non-authoritative candidate directions were sharpened; no architecture-complete or implementation handoff is valid. Independent review artifacts N/A — not applicable yet.
- Handoff rule result: `get_handoff_rules` offered only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. None matched this Draft approval hold; no recipient was notified.
- Remaining gaps: DEC-001–004 and selected-mode validation of PTY capture/approvals or headless permission policy; project/MCP lifecycle and fork behavior remain untested.
- Next action: Report the evidence and its limits to the user, obtain behavior decisions, then move the requirements to Ready for Approval and seek explicit approval before design.

## SR-004 — Recommended direction, pending behavior choice

- Trigger: User asked what the proper design should be based on experiments.
- Prior/current status: Draft / Draft; design spec N/A — not yet approved. No prior user approval is carried forward.
- Recommendation recorded in `design-direction-proposal.md`: shared per-run custom main-agent identity and isolated AGY project, separate AutoByteus/provider IDs with fail-closed exact restore, transport-specific terminal versus structured semantics. Prefer staged native terminal for direct human use because it retains interactive approvals; require a distinct, explicitly scoped structured permission contract for automated team/chat turns. Do not copy the old PTY turn-complete-on-enqueue mechanism.
- Affected IDs: DEC-001–005, REQ-002–005, REQ-007–008, AC-002–004, AC-006–007. New DEC-005 asks whether standalone-first staging is acceptable or team/chat must be day one. Intended behavior is still proposed; no authoritative requirement was approved or changed.
- Approval/design impact: User selection and approval required before design. Native-TUI ID capture and structured permission behavior remain release-gating technical uncertainties for their respective routes.
- Routing: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap routes; none matched. No downstream recipient notified. Independent review artifacts N/A — not applicable.

## SR-005 — Actual AutoByteus `agent.md` compatibility check

- Trigger: User asked whether existing AutoByteus agent Markdown can be copied directly into AGY and whether this was tested. Prior tests used synthetic AGY agent files, not a byte-for-byte AutoByteus source file.
- Prior/current status: Draft / Draft; design spec N/A — not yet approved.
- Evidence: New bounded `agy-cli-autobyteus-md-compat-probe.py` copied the repository's built-in Memory Compactor source file without editing it. With `--new-project --agent 'Memory Compactor'` and a matching folder name, AGY returned the source file's required JSON object despite a conflicting user prompt. With the same raw file but `--agent memory-compactor`/slug folder, AGY silently returned the user's plain response. A transformed frontmatter control with matching slug plus `mainAgent: true` also honored the source body. See sanitized JSON result and experiment report.
- Interpretation: The source body and frontmatter can be directly compatible in at least one case; **not** a guarantee that arbitrary AutoByteus definitions can be raw-copied safely. Name matching, external team/collaboration identity, configured skills/tools, and backend-specific instructions require deliberate mapping. The previous proposed generator remains the recommendation, but should reuse authored content rather than rewriting it unnecessarily.
- Affected IDs: BEH-002, REQ-002, AC-002. Intended behavior unchanged and unapproved; no new approval effect beyond existing Draft status.
- Routing/design impact: Evidence-only correction to the non-authoritative direction; `get_handoff_rules` returned no matching route while requirements remain Draft, so no recipient was notified. No architecture handoff or independent review yet. Remaining gaps: more representative private/team agent compatibility and selected-transport contract tests.

## SR-006 — Structured response/front-end/raw-trace clarification

- Trigger: User asked whether AGY responses still require `node-pty`, how the frontend would display them, and whether normal raw traces can be stored; the user expressed that following the existing structured pattern would be preferable if feasible.
- Prior/current status: Requirements Draft / Draft; design spec N/A — not yet approved. The preference is not approval of headless permission policy or trace completeness limits.
- Evidence: Earlier successful AGY model probes already used ordinary subprocess pipes and `stream-json`, not PTY. New `agy-cli-stream-trace-probe.py` confirmed live `init`, `user_input`, tool step, assistant text deltas and terminal `result`. Its `view_file` output field exposed only `2 lines, 17 bytes`, not the marker contents or a full tool result. Current `AgentRunBackend` → `AgentRunEvent` → `AgentRunMemoryRecorder` / `RuntimeMemoryEventAccumulator` → existing web projection supports normalized external-runtime traces and chat history.
- Affected IDs: BEH-004, BEH-006, REQ-007–009, AC-006–008, DEC-001/003/005/006. REQ-009 / AC-008 are proposed, not approved. Intended behavior requires explicit user decision on trace fidelity and permissions before design.
- Recommendation impact: `design-direction-proposal.md` now conditionally favors stream-json/no-PTY to satisfy the newly expressed frontend/trace preference; the prior terminal-first preference remains the alternative if native interactive approvals are mandatory. No authoritative design, review or implementation route is claimed.
- Remaining gaps: user approval of scoped headless permission behavior and whether exposed-event trace suffices; implementable mapping and replay validation; full tool payload availability cannot be assumed.
- Handoff rule result: `get_handoff_rules` returned no matching route while requirements remain Draft. No recipient was notified.

## SR-007 — Ticket-direction approval and confidence boundary

- Trigger: User explicitly said “niceee. i approve the ticket” and asked whether the integration will work.
- Prior/current status: Draft / Ready for Approval. The user's statement approves the **structured AGY ticket direction in principle** after being told about event-trace and headless-permission caveats, but it does not unambiguously specify every behavior-defining choice. Requirements are not yet Approved; design spec N/A.
- Evidence/confidence: Live tests support custom-agent identity, project discovery, multi-turn stream, exact ID binding and restore, exposed tool/assistant events, two-run isolation and current frontend/trace projection compatibility. They do not prove production integration or full traces. Headless command soft-denial was reproduced even with `--sandbox` alone; a usable scoped policy and replay mapping need implementation-time validation.
- Affected IDs: DEC-001–006, REQ-001–009, AC-001–008. `requirements-doc.md` now contains a five-point proposed approval baseline covering structured chat/team, exposed-event trace fidelity, scoped/visible permission denial, no legacy unmerged-run import and identity snapshot continuity.
- Approval impact: Ask user to confirm this exact baseline or correct any part. Do not mark Approved or start authoritative architecture until it is explicit.
- Design/routing impact: No Architecture Design Complete result or downstream handoff. Current worktree/base and historical worktrees unchanged beyond ticket artifacts. Independent review artifacts N/A — not applicable.
- Handoff rule result: `get_handoff_rules` returned no matching route for Ready for Approval; no recipient was notified.

## SR-008 — Existing auto-approve expectation and AGY control

- Trigger: The user clarified they normally run AutoByteus agents with `autoExecuteTools=true`, do not manually approve calls, and may later want always-approve to become the default. They asked whether the AGY limitation is only inability to show approve/deny.
- Prior/current status: Ready for Approval / Ready for Approval; authoritative design spec N/A — not yet approved for design. The earlier scoped-only/no-bypass proposed baseline was inconsistent with the user's normal enabled-path workflow and has been corrected, not treated as approved history.
- Evidence: Current Codex backend uses no-approval policy plus effective full access for per-run `autoExecuteTools=true`; Claude auto-approves callbacks without provider `bypassPermissions`. An AGY 1.2.10 disposable-workspace probe with `--sandbox --dangerously-skip-permissions` reported `permission_mode=always-proceed` and executed exact harmless `printf AUTOAPPROVE-PROBE` through `run_command` with visible tool ACTIVE/DONE events. Without the flag, the same class of command had soft-denied. The published AGY docs call the flag an all-tool grant. No user-global settings changed.
- Affected IDs: BEH-004, REQ-007–008, AC-006–007, DEC-003 and proposed approval baseline point 3. User's stated usage supports investigating the enabled path; it is not explicit approval of AGY's broad flag, changing other runtimes' default, removing the toggle, or a disabled-path UX.
- Approval impact: Confirm AGY enabled-path mapping and how `autoExecuteTools=false` should behave in structured chat before marking the complete requirements Approved. Trace/continuity baseline points still need exact confirmation.
- Design/review/routing impact: `design-direction-proposal.md` updated only as a non-authoritative candidate. No implementation-ready route. Independent review artifacts N/A — not applicable. Current worktree/base/finalization context unchanged.
- Remaining gaps: Disabled-path behavior, security/trust scope of AGY's all-tools flag, complete user approval, production mapping/E2E verification and trace-fidelity acceptance.
- Handoff rule result: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. None matches this Ready-for-Approval hold; no recipient was notified.

## SR-009 — Headless denied-tool stream shape

- Trigger: User asked whether AGY outputs JSON about the tool needing approval, then explicitly requested experiments.
- Prior/current status: Requirements Ready for Approval / Ready for Approval; design spec N/A — not yet approved. No intended-behavior change; AC-007 wording sharpened to distinguish denial from actionable approval.
- Evidence: In a disposable AGY 1.2.10 new-project headless run without the bypass flag, a harmless command produced `init.permission_mode=request-review`, a `run_command` ACTIVE event with exact `CommandLine`, then `run_command` ERROR with `tool_info.error.type=TOOL_ERROR` and permission-denial text. No `approval_request`-style event or request ID appeared in the NDJSON. Final result was SUCCESS with empty response; exit was 0; stderr said headless could not prompt and auto-denied. The sanitized stream shape and script are adjacent to `agy-cli-experiment-report.md`.
- Affected IDs: REQ-007–008, AC-006–007, DEC-003. Approval basis unchanged: the user has not selected the disabled-path UX, and the full intended-behavior baseline is not yet approved.
- Interpretation: AGY's stream exposes **a tool attempt and denial**, not a Codex App Server-style pending approval that AutoByteus could accept. The current docs also say headless stream `control_request`/`control_response` input is unsupported. One sampled shell command does not prove every policy/tool path.
- Design/review/routing impact: No authoritative design or implementation-ready handoff. Existing structured enabled-path direction remains; false-path contract remains open. Independent review artifacts N/A — not applicable.
- Remaining gaps: User decision on disabled-path behavior, exact baseline approval, production E2E validation.
- Handoff rule result: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. None matches the Ready-for-Approval evidence clarification; no recipient was notified.

## SR-010 — AGY-specific default-on approval decision

- Trigger/approval reference: The user's 2026-09-24 message explicitly accepts that current headless AGY cannot provide the Codex-style per-tool Approve/Deny frontend flow, says that is acceptable for this runtime, and directs `autoExecuteTools=true` as the AGY default to enable skip-permissions behavior.
- Prior/current status: Ready for Approval / Ready for Approval. This is explicit approval of **the AGY default-on/no-manual-approval portion**, not a complete requirements approval. Authoritative design spec N/A — not yet approved.
- Evidence: SR-009 stream probe shows attempted/denied tool JSON but no pending request; AGY official headless docs reject control-response messages and document the all-tool skip flag. SR-008 autoapprove probe shows `always-proceed` and successful harmless command. Current `useDefinitionLaunchDefaults.ts` initializes standalone/team root `autoExecuteTools=false`; team hierarchy and saved seeds can inherit/override explicit choices. Runtime-specific default true cannot be achieved by a global default flip without affecting other runtimes.
- Affected IDs: BEH-004, REQ-007–010, AC-006–009, DEC-003, proposed approval baseline point 3. New REQ-010/AC-009 make the AGY-only default and create/restore mapping observable. Exact disabled-run behavior remains open; do not infer removal of the toggle or a default change for Codex/Claude/AutoByteus.
- Design/review/routing impact: Non-authoritative direction updated; architecture must address runtime-aware standalone/team/member draft defaults and preservation of explicit saved values. No implementation-ready handoff. Independent review artifacts N/A — not applicable.
- Remaining gaps: Explicit `false` UX and remaining trace/history/identity/team baseline confirmation before full requirements approval and authoritative design. Current worktree/base/finalization context unchanged.
- Handoff rule result: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. Neither applies to this partial requirements approval; no recipient was notified.

## SR-011 — Runtime-selection default, not an explicit-off feature

- Trigger/approval reference: User's 2026-09-24 follow-up says “when user selects this runtime ... by default ... true” and that users do not explicitly turn it off; directs the design not to center the rare false case. This clarifies the earlier SR-010 AGY-only permission approval.
- Prior/current status: Ready for Approval / Ready for Approval. AGY permission/default behavior is resolved for intended usage, but trace, old-run continuity, identity snapshot and day-one team scope still lack full baseline approval. Authoritative design spec N/A — not yet approved.
- Affected IDs: BEH-004, REQ-007–010, AC-006–009, DEC-003 and proposed baseline point 3. The setting is selected in **new editable standalone/team/member drafts**, not made globally true for all providers or forcibly rewritten on existing runs. Explicit false remains technically possible because the existing control is not removed; it uses ordinary headless policy with visible denial and no false promise of manual approval.
- New current-code evidence: Desktop `AgentRunConfigForm.updateRuntimeKind` directly changes the draft; mobile uses `agentRunConfigStore.updateAgentConfig`; team root/member uses config edits and overrides; org config has a separate store action. None currently applies runtime-specific auto-execute defaults. `design-direction-proposal.md` now suggests a shared runtime-aware draft transition and backend mapping without claiming an authoritative design.
- Approval/design/routing impact: No need to query the user again about an unsupported AGY manual-approval UX. Still obtain one concise approval of the remaining trace/continuity/team intended-behavior points before authoring `design-spec.md` and classifying route. Independent review artifacts N/A — not applicable; existing worktree/base/finalization target unchanged.
- Remaining gaps: DEC-002, DEC-004–006 and complete requirements approval; implementation/E2E not begun.
- Handoff rule result: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. Neither applies to this Ready-for-Approval refinement; no recipient was notified.

## SR-012 — Unused historical work and trace-fidelity explanation

- Trigger: User questions “old unmerged AGY runs need no import,” says the old AGY work is not used at all and may be removable, and asks what “traces contain what AGY exposes (not hidden details)” means.
- Prior/current status: Requirements Ready for Approval / Ready for Approval; design spec N/A. User confirms no legacy AutoByteus AGY run import. Trace-fidelity decision remains open until the user understands/accepts the concrete limit.
- Evidence: Current `origin/personal` base does not register AGY. Historical AGY worktrees remain, but the refresh has 92 modified/untracked paths and the original has an untracked ticket; another `agy-runtime-support` worktree is dirty. Do not equate user willingness to remove unused work with approval to discard those files now. The existing `view_file` stream probe emitted only `output: "2 lines, 17 bytes"` for a successful read although the agent's response used the marker content; neither provider NDJSON nor run-local log included the full file contents. AGY stream docs describe exposed event/tool fields, not a complete internal transcript.
- Affected IDs: BEH-006, REQ-009, AC-008, DEC-002/006, proposed approval baseline points 2 and 4. DEC-002 is resolved out of scope. DEC-006 now asks specifically whether exact provider NDJSON plus normalized AutoByteus trace suffices despite omitted internal tool payloads/reasoning. This is a proposed behavior supplement, not yet approved.
- Design/routing impact: `design-direction-proposal.md` now proposes retaining exact received NDJSON as a separate raw provider trace plus normalized history, with retention/redaction to be designed. Historical dirty worktree cleanup belongs to later safe finalization, not this requirements clarification. No authoritative design or implementation handoff; independent review artifacts N/A — not applicable.
- Remaining gaps: User acceptance of trace fidelity, identity snapshot continuity and day-one team scope. No import of old AGY AutoByteus runs is required.
- Handoff rule result: `get_handoff_rules` returned only Architecture Design Complete and Delivery Receipt Evidence Gap conditions. Neither applies to this Ready-for-Approval clarification; no recipient was notified.

## SR-013 — Remove nonexistent legacy-run scenario

- Trigger: User explicitly corrected the Solution Designer's phrasing: there is no AGY AutoByteus run history from the old worktrees because the integration was never in use. The repeated hypothetical import question was overthinking and should stop.
- Prior/current status: Requirements Ready for Approval / Ready for Approval; design spec N/A. No behavior change to actual supported product scenarios.
- Factual correction: The old git worktrees contain unmerged implementation drafts, not product-run history. `origin/personal` does not register AGY. SR-012's “no import” conclusion remains true, but it was framed as a decision about hypothetical records. Current `requirements-doc.md` removes DEC-002 and baseline point 4 rather than continuing to ask about nonexistent data. Future exact restore applies to runs created by the new integration only.
- Affected IDs: SCN-003, REQ-003–004 wording context and removed DEC-002 from active decisions; other stable IDs remain unchanged. Historical SR-012 text is preserved for audit but superseded on this point.
- Approval/design/routing impact: No renewed approval needed for eliminating an unsupported scenario. Remaining approval topics are trace fidelity, identity continuity and day-one team scope. No authoritative design or handoff yet; independent review artifacts N/A — not applicable.

## SR-014 — Reuse AutoByteus's normalized raw-trace pipeline

- Trigger/user reference: The user corrected the Solution Designer's “exact AGY event stream”/separate archive framing and specified that the backend should convert AGY output into **our raw trace**, like the Codex App Server runtime.
- Prior/current status: Requirements Ready for Approval / Ready for Approval; authoritative design spec N/A — not yet approved for design. This is a user clarification and removal of an unrequested requirement, not implementation authorization.
- Evidence: Current Codex backend converts app-server notifications to `AgentRunEvent`. `AgentRunResourceManager` attaches `AgentRunMemoryRecorder`; `RuntimeMemoryEventAccumulator` and `ExternalRuntimeMemoryWriter` write normalized raw-trace items. The external-runtime kind gate must recognize AGY. The previous AGY `view_file` probe exposed only a size summary; normalized traces cannot contain absent provider payloads.
- Affected IDs/artifacts: BEH-006, REQ-009, AC-008, DEC-006 and proposed approval baseline point 2 in `requirements-doc.md`; `investigation-notes.md`, `design-direction-proposal.md`, and `investigation-result.md`. The earlier SR-012 separate exact-provider-NDJSON archive proposal is superseded; historical entries remain evidence of the correction, not active requirements.
- Approval/design/routing impact: The normal AutoByteus raw-trace pattern is user-clarified. No separate verbatim AGY NDJSON archive is required; the AGY stream is transport input to a canonical event adapter. Remaining complete-baseline confirmation concerns identity snapshot continuity and day-one team scope. No authoritative design, independent review, implementation, or handoff yet; review artifacts N/A — not applicable.

## SR-015 — Capture and inspect verbatim AGY tool-event JSON

- Trigger/user reference: The user requests actual experiments showing AGY JSON when tools are called, with the output stored to files and inspected rather than relying on prior summarized probes.
- Prior/current status: Requirements Ready for Approval / Ready for Approval. This is new evidence and a correction to confidence about exposed tool outcomes, not a new feature request or complete-baseline approval. Authoritative design spec N/A; review artifacts N/A.
- Evidence: `agy-tool-event-capture.py`, `agy-multiturn-tool-event-capture.py`, and `agy-command-failure-event-capture.py` ran locally with AGY CLI 1.2.10 in disposable projects. Verbatim stdout NDJSON and stderr files reside in `agy-tool-event-capture/`; `agy-tool-event-capture-analysis.md` documents line-by-line inspection. Cases: `view_file` ACTIVE/DONE with only size-summary output; allowed `run_command` ACTIVE/DONE with marker output; denied command ACTIVE/ERROR with `tool_info.error`, `result.denied_actions`, overall SUCCESS/exit 0; `write_to_file` ACTIVE/DONE with path but no written bytes, independently verified file content; two tool turns with one init/two results; and an executed command exiting 7 reported as tool DONE without machine-readable exit status. An initial malformed stdin shape was preserved as a negative control and corrected per official documentation.
- Affected IDs/artifacts: BEH-004/006, REQ-009, AC-008; `investigation-notes.md`, `requirements-doc.md` (wording precision only), `agy-cli-experiment-report.md`, `design-direction-proposal.md`, and `investigation-result.md`. No new behavior-defining supplement or change to user-approved normalized-trace direction. This probe-only raw evidence is not a production AGY archive requirement.
- Design/routing impact: The future adapter can use emitted lifecycle and tool fields, but must record known user input itself, avoid assistant text duplication, and not invent omitted tool content or underlying command exit status. Actual AutoByteus conversion, raw-trace persistence and frontend replay remain unimplemented/unvalidated. Identity snapshot continuity and day-one team scope are the remaining complete-baseline approval topics. No architecture handoff applies while requirements remain Ready for Approval.

## SR-016 — Complete requirements baseline approved

- Trigger/user reference: After the structured direction approval (“niceee. i approve the ticket”), AGY-only default-on/no-headless-manual-approval decisions and normalized AutoByteus raw-trace clarification, the user reviewed the verbatim AGY tool JSON captures and asked: “So do you think now everything is clear? If yes, then we can start with the tickets now. You can continue, right?” (2026-09-24). This is the go-ahead for the fully stated baseline in `requirements-doc.md`, not proof of integration success.
- Prior/current status: Ready for Approval / **Approved**. Authoritative design was N/A before this round. Day-one standalone plus team/org member scope and run-start identity retention on exact resume are included in the confirmed complete baseline; no old AGY product-run migration exists.
- Affected IDs: SCN-001–004, BEH-001–006, REQ-001–010, AC-001–009, DEC-001/003–006. The latest approved point 2 is existing normalized raw traces, not the historical SR-012 provider-NDJSON archive proposal.
- Artifact impact: `requirements-doc.md` status and approved baseline; `investigation-notes.md` retains supporting experiments. Approval permits architecture design; it does not authorize shortcutting independent review, implementation, or validation. Review artifacts N/A — not yet applicable.
- Remaining gaps: Technical design and production-path validation only; intended-behavior decision gaps none on this baseline.

## SR-017 — Architecture design completed for independent review

- Trigger: SR-016 approval and additional current-branch architecture investigation in `investigation-notes.md`.
- Prior/current status: Requirements Approved / Approved unchanged; design N/A / **Ready**. No new intended behavior. `design-spec.md` is the authoritative technical package against SR-016.
- Affected IDs: All approved SCN-001–005, BEH-001–006, REQ-001–010 and AC-001–009; no requirement/AC wording changed in this design round.
- Evidence/design impact: Current provider factory/activation/restore, shared identity, MCP session, raw-trace projection, model/availability and standalone/team/org draft owners were mapped. The design uses a per-run durable AGY project capsule, composed custom main-agent identity, run-scoped MCP config, persistent stream-JSON pipes, exact early ID binding and fail-closed resume, canonical event conversion, existing recorder/Event Monitor, and AGY-only new-draft permission default. No PTY, first-user identity bootstrap, global settings edit, old-branch mechanical merge, or separate provider archive.
- Classification: `task_size=Large`, `architectural_risk=High`: a new runtime backend spanning execution, security, identity, persistence, model availability, team/org collaboration and frontend defaulting. Independent review is applicable; review artifact N/A — pending review. Implementation and API/E2E validation have not begun.
- Remaining risks: Actual AGY-to-AutoByteus MCP invocation, custom-agent/project discovery under supported versions, crash/interrupt and concurrent write behavior, and full end-to-end trace/frontend replay need downstream checks. These are explicitly gated in `design-spec.md`.
- Handoff rule result (SR-017): `get_handoff_rules` matched Architecture Design Complete + Large/High → `/architecture_reviewer`; direct implementation and delivery-gap conditions did not match. Review pass/fail remains pending.

## SR-018 — Review-finding recovery and provider controls; design not yet re-ready

- Trigger: ARCH-REV-001 Fail/DR-001 plus the user's request to critically test the reviewer, their model-directed workspace clarification, and their skill/project-folder questions.
- Prior/current status: Requirements Approved / Approved unchanged so far; SR-017 design Ready / **Needs Revision**; review Fail remains open. No implementation route.
- Affected IDs: SCN-001/004, BEH-002/005, REQ-002/005, AC-002/004; `skillAccessMode` parity is a newly identified design/possibly requirement gap not silently added to approved scope.
- Evidence: `design-review-report.md` shows a normal relative write landed in capsule with capsule-primary + `--add-dir`; source inspection confirms shared composer omits workspace path. New disposable AGY 1.2.10 controls show explicit generated working-environment instruction plus allowed tools can direct file and shell writes to the real workspace, and real-primary + added capsule can load custom identity and write in real. Skill controls show capsule skills load, but added-workspace skills load even with `skills: []`/slash-expansion disabled. Raw captures and scripts are indexed in `investigation-notes.md`.
- Approval/design/routing impact: The reviewer correctly failed the submitted design's missing task-root contract, but did not prove the capsule approach impossible. The user accepts prompt-directed dynamic targeting; a corrected design must make the selected workspace explicit and verify it without claiming static enforcement. Run-owned capsule remains preferable to user-project `.agents` mutation for concurrent run isolation. Skill policy requires further decision/technical design. No renewed approval is asserted for a weakened skill contract. Classification remains Large/High if/when design is ready again; independent review must be repeated on a revised basis.
- Remaining gaps: exact corrected AGY task-root/config/MCP shape and skill-access contract, then design revision/re-review; actual backend/E2E validation remains downstream.

## SR-019 — Minimal workspace guidance and complete DR-001 architecture correction

- Trigger: ARCH-REV-001/DR-001 recovery; user's explicit preference for a simple native-like `agent.md` workspace statement, followed by “cool. now its clear. you can continue now” and clarification that AGY-native subagents are outside this ticket.
- Prior/current status: Requirements **Approved SR-016 / Approved unchanged**; design **Needs Revision / Ready for independent re-review**. The earlier ARCH-REV-001 Fail is not converted into a pass; review must evaluate this revised basis. No implementation handoff has occurred.
- Affected IDs: SCN-001/002/003/004, BEH-002/003/005, REQ-002/003/005 and AC-002/003/004; configured-skill mapping is a technical compatibility detail under the existing run config, not a new approved strict skill ban. No acceptance criterion or user-visible permission/trace scope was weakened.
- Evidence: `agy-minimal-workspace-agent-probe.py` and raw/summary show the exact two-line main-agent workspace stanza directed ordinary file/shell outputs to the selected real workspace while `init.cwd` was capsule. Reviewer’s unguided relative-write control remains the negative comparison. Disposable capsule MCP server was called through AGY; one same-name collision control preferred capsule config. Tool-frontmatter controls rejected `*`/several auxiliary names and accepted the named eight coding tools. Current Codex/Claude skill materializer code plus AGY skill discovery controls show `NONE` suppresses AutoByteus-configured skill exposure, not provider-owned workspace/global discovery.
- Design change: `design-spec.md` now assigns capsule ownership of generated identity/MCP/configured skills, saves the selected real-workspace path, appends only the short working-environment stanza to the generated custom **main** agent, uses `--add-dir` for access, restores with the saved path, and gates verification on real file/shell targets and actual scoped AutoByteus MCP calls. It explicitly rejects deterministic task-root enforcement, exclusive AGY MCP/skill availability, unverified frontmatter tool names, and AGY subagent work.
- Classification and remaining risks: `task_size=Large`, `architectural_risk=High` unchanged; model-directed path choice remains nondeterministic, provider versions may drift, real AutoByteus MCP/team attribution and full product E2E remain downstream validation gates. Product Design artifact N/A — not requested. Implementation/code review/API-E2E/delivery artifacts N/A — not yet applicable. Historical review report is retained as prior Fail evidence.
- Handoff rule decision: `get_handoff_rules` matched Architecture Design Complete + Large/High → `/architecture_reviewer`; direct implementation and delivery-gap conditions did not match. This is a revised-basis re-review, not implementation authorization. Message delivery confirmation is recorded after `send_message_to`.
- Informational review receipt: `ARCH-REV-002` reports **Pass** on SR-016/SR-019, with prior DR-001 resolved at architecture level. The canonical `design-review-report.md` and `architecture-review-revision-record.md` contain the reviewed-basis evidence. The reviewer confirmed its primary package handoff to `/implementation_engineer`; Solution Designer does not forward it again or reopen this design.

## SR-020 — IR-001 AGY command-completion color proposal; approval pending

- Trigger: Implementation Engineer IR-001 `implementation-handoff.md` after the user's 2026-09-24 screenshot/feedback asks an AGY `run_command` completion to look green like other runtimes. The screenshot also shows a separate segment-lifecycle error, locally fixed by the engineer but not yet freshly live-validated.
- Prior/current status: SR-016 requirements **Approved** / **Ready for Approval for the narrow proposed SR-020 delta**; SR-019 design **ARCH-REV-002 Pass** / **Needs Revision pending user status decision**. Unaffected approved baseline remains intact. The current implementation is a partial checkpoint, not accepted or ready for Code Review/API-E2E.
- Affected IDs: BEH-004/006, REQ-008/009, AC-007/008; DS-003 and tool-state presentation mapping. Other tool types and non-AGY runtimes are unchanged by the proposal.
- Evidence: `agy-tool-event-capture/run_command_nonzero.stdout.jsonl` records `sh -c 'exit 7'` as AGY tool `DONE` and top-level `SUCCESS` without a structured exit code or error. IR-001 source maps such `DONE` to `TOOL_EXECUTION_COMPLETED`/`completed_unverified`; the screenshot displays the neutral badge. The separate `AGENT_SEGMENT_LIFECYCLE_INVALID` fix and local checks remain implementation-owned.
- Proposed user decision: Green **“Completed”** for AGY `run_command` terminal `DONE`, with “Exit status not reported” detail, **not** a green “Succeeded” claim; preserve neutral canonical/raw-trace state and red denied/error. Explicitly acknowledge a nonzero command may also show green completion because AGY does not expose its exit status in the observed event.
- Approval/design/routing impact: No behavior change is authoritative yet. Ask user for explicit approval of this exact visual/semantic compromise or retain the previously approved neutral treatment. Then revise affected requirements and design and repeat applicable review/handoff; do not authorize the engineer to map unknown outcomes to success. Product Design supplement N/A — not requested. Code Review/API-E2E/delivery artifacts N/A — not yet applicable.

## SR-021 — User-approved AGY DONE-to-success mapping after command outcome matrix

- Trigger/approval reference: User corrected their voice transcription, requested further experiments and explicitly directed: “if they really do not have return result, then we simply map done to success thats it.” This followed earlier discussion of nonzero shell commands potentially appearing as AGY DONE. Fresh AGY CLI 1.2.10 outcome controls confirmed the condition for exit 0, exit 8 and command-not-found/exit 127, while a permission denial emitted tool ERROR.
- Prior/current status: SR-020 proposed requirement delta **Ready for Approval / Approved SR-021**; SR-019 design **Needs Revision / Architecture Design Complete SR-021**, pending independent re-review. ARCH-REV-002 Pass applies to the prior neutral design, not this changed mapping. IR-001 remains partial/unaccepted implementation; no Code Review/API-E2E/delivery sign-off.
- Affected IDs: BEH-006, REQ-009 and new REQ-011, AC-008 and new AC-010; REQ-008/AC-007 denial protection remains. SCN-001/002/003/005. No change to non-AGY runtimes, permissions default, identity, workspace, MCP or trace storage format.
- Evidence: `agy-command-outcome-matrix-probe/summary.json` with four raw stdout/stderr captures, plus previous `agy-tool-event-capture/run_command_nonzero.stdout.jsonl`. Exit 0/8/not-found all had exact `run_command` ACTIVE→DONE without structured exit-status field and top-level SUCCESS; denial had tool ERROR/`denied_actions` even with top-level SUCCESS. The denial probe's outer process timed out after a result; no execution was inferred. The abandoned output-producing exit-8 probe timed out without a conclusion.
- Approved intended behavior: For AGY tools, `DONE` without explicit error maps to AutoByteus canonical success/green as a **provider-step** convention. This knowingly includes nonzero shell exits that AGY does not structurally report. `ERROR` or explicit tool error remains failed/denied regardless of overall result; preserve available output/error and source state, never fabricate exit code zero. SR-020's green-completion/neutral-data proposal is superseded.
- Design/review impact: `requirements-doc.md` and `design-spec.md` now agree on the new mapping. The earlier AGY-only `TOOL_EXECUTION_COMPLETED`/completed-unverified path is unnecessary if no independent producer remains; design directs clean-cut removal with a local development-trace audit, not a production migration. Task size **Large**, architectural risk **High** remain unchanged. Repeat independent architecture review before dependent implementation resumes; no duplicate handoff to Code Review. Product Design N/A — not requested.
- Remaining risks: Provider versions/tools beyond the bounded controls may differ; actual AutoByteus live/reloaded green/red and raw-trace behavior, separate segment-lifecycle fix, and full downstream gates remain implementation/API-E2E work.
- Handoff rule decision: `get_handoff_rules` matched Architecture Design Complete + Large/High → `/architecture_reviewer`; direct implementation and delivery-gap rules did not match. Re-review of SR-021 must precede affected implementation resumption; message delivery is confirmed separately by `send_message_to`.
- Interim ARCH-REV-003 coherence correction (no verdict yet): Reviewer noted that the canonical `investigation-notes.md` supplemental inventory omitted the SR-021 command-outcome script/raw/summary and IR-001 triggering reports, and that its SR-016/017 table could be mistaken for the current neutral-event design. The inventory now indexes those artifacts with owner, purpose/scope, status and approval applicability; the table and SR-020 section explicitly mark neutral mapping as historical/superseded by SR-021. This is an evidence/documentation correction only: no changed intended behavior, renewed approval, new design decision or review outcome is inferred. Independent review continues on the same SR-021 basis.
- Informational review receipt: `ARCH-REV-003` **Pass** on approved SR-021; prior DR-001 resolution remains valid. Canonical report: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/design-review-report.md`; cumulative reviewer record: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/architecture-review-revision-record.md`. The reviewer confirmed its primary reviewed-package handoff to `/implementation_engineer` to resume IR-001. This is architecture approval only, not source, live UI, API/E2E or delivery acceptance. Solution Designer does not forward the package again.

## SR-022 — Packaged Electron MCP behavior verified during user hold (evidence-only)

- Trigger: The user's 2026-09-25 packaged-Electron Classroom Simulation Team screenshots showed AGY `view_file` reading `~/.gemini/antigravity-cli/mcp/autobyteus_agent_tools/*.json` before `call_mcp_tool`; the user asked how MCP works and invited experiments against the Electron-started backend. The earlier preparation failure and user-requested removal of the single legacy workspace `.agents/mcp_config.json` are part of the same user-verification context.
- Prior/current status: Requirements **Approved SR-021 / unchanged**; architecture **ARCH-REV-003 Pass / unchanged**; integrated delivery **awaiting explicit user verification / unchanged**. This is not a new architecture handoff, product E2E certification or delivery acceptance.
- Affected IDs and evidence: REQ-003/AC-003 MCP collaboration and the existing capsule-isolation rationale; no scenario, behavior, requirement or AC changes. Read-only inspection of the user's actual Team run showed two distinct member capsule MCP connection configs, no global MCP server entry, AGY-home JSON containing tool names/descriptions/parameter schemas but no endpoint/credentials, normalized `view_file` then `call_mcp_tool` events, exact `DELIVERED` results both professor→student and student→professor, and two durable team communication records. Earlier failure reproduced `AGY_MCP_NAME_COLLISION` from the stale user-workspace config; after the user's requested targeted removal, the new Team run worked. The live data is not copied into the ticket because it contains scoped endpoints and conversation contents.
- Artifact impact: `investigation-notes.md` §SR-022 and supplemental inventory hold the full evidence/limits. `design-spec.md` only clarifies that **AutoByteus** does not author global AGY connection config while **AGY itself** may write shared home tool-schema metadata. The observed home-file purpose and cache lifecycle are not provider-documented in the inspected official MCP page; schema interpretation remains an inference from file shape, source and event sequence.
- Approval/review/routing impact: No changed intended behavior or technical design, so no renewed user approval or independent architecture re-review. The user has not accepted final delivery; the old generic preparation error and exact collision remain user-verification feedback for later disposition. The current package is not Terminal.
- Remaining limits: This single AGY 1.2.10 packaged-app Team run does not prove home-schema cache isolation, cleanup, all MCP tool/server behavior or future versions. Do not expose full run-scoped URLs or assume provider-global filesystem immutability.

## SR-023 — Large AGY Org launch/health design recovery

- Trigger: User's 2026-09-25 packaged-Electron screenshot and request to reproduce the AutoByteus Org stuck at **Starting Agent Org…**, with a sidebar health timeout. Source context is Delivery Engineer's unsigned integrated test build during explicit user-verification hold; no final acceptance or release occurred.
- Prior/current status: Requirements **Approved SR-021 / unchanged**; design **ARCH-REV-003 Pass on SR-021 / Architecture Design Complete SR-023, independent re-review required**; delivery **user-verification hold / hold unchanged**. ARCH-REV-003 is not authority for this new technical correction. Implementation, source review and API/E2E reports remain historical passes on their actual earlier basis, not prospective validation of SR-023.
- Affected IDs: SCN-002, BEH-001 and REQ-001/AC-001 availability and supported team/org member launch; DS-001 availability path and new DS-005 Org preflight. No changed intended behavior, permission, identity, MCP, traces, storage shape or other-runtime contract; renewed user approval N/A. Product Design N/A — not requested.
- Evidence: The original user Org run later reported active with 18 configured AGY placements. Independent temporary browser frontend against the **packaged Electron backend** visibly stayed on Starting Agent Org for tens of seconds, then reached an active separate 18-placement test run; that test run was stopped in browser, history kept, no member prompt sent. Source inspection found sequential Org `validate` per placement and synchronous `agy --version`/`--help`/`models` via `spawnSync`; `agy models` took ~1.2–1.3 s locally. A concurrent read-only catalog query (1.381 s) delayed `/rest/health` to 1.259 s from ~0.002 s baseline, directly proving event-loop starvation from this command path. Exact user screenshot request overlap is unproven; permanent AGY hang is **not** claimed. Full evidence and limits are in `investigation-notes.md` §SR-023.
- Design correction: `design-spec.md` DS-005 directs Org creation to validate root/Team/Agent placements in one `validateMany` call, preserving address-specific errors and sharing fresh catalog evidence only within the operation. AGY capability/model discovery becomes bounded nonblocking child I/O, awaited by catalog, availability/GraphQL/application callers and backend factory so no parallel synchronous request path remains. Dynamic model/auth/version/flag validation and non-AGY behavior remain. No process-global model cache or migration. `task_size=Large`, `architectural_risk=High` remain because the correction crosses Org orchestration, model/runtime availability interfaces and process-I/O ownership, not because of the number of members.
- Review/routing/verification: Repeat independent architecture review before affected implementation. Then implementation/local checks and applicable independent source/API-E2E/Delivery gates must rerun for the changed basis. Full 18-placement browser→real-backend Org launch with concurrent health and finite failed-probe behavior is a new acceptance gate; the earlier small direct/nested Org tests do not cover it. The original user Org remains active and must not be altered by the test; stopped reproduction run is historical normal data.
- Handoff rule decision: `get_handoff_rules` matched Architecture Design Complete + Large/High → `/architecture_reviewer`; no direct implementation or delivery receipt-gap route applies. Send confirmation is recorded in `solution-org-launch-recovery-handoff.md`.
- Remaining risks: CLI duration/auth variability, async availability interface fanout, and browser error-state behavior need downstream validation. SR-022's separate legacy MCP collision/generic error remains distinct feedback, not the cause of this stall.
- In-progress SR-023 reviewer clarification (no verdict yet): Architecture Reviewer identified that `RunModelSelectionService.validateWithEvidence` currently catches every catalog exception as reasonless `model_unavailable`, which Org launch renders as “The selected model is unavailable.” That would erase a timeout/auth/parse reason despite REQ-001/AC-001. Source inspection confirmed the GraphQL mutation, web store and Org config panel already carry and display an addressed service error and reset the spinner; the missing contract is catalog→validator. `investigation-notes.md` and DS-005 now specify a typed **safe** AGY probe diagnostic propagated through catalog and optional `model_unavailable.catalogDiagnostic`, then addressed Org error → existing GraphQL message → browser alert. A successful catalog lacking the selected slug retains the ordinary missing-model message; unexpected AGY catalog failures get a generic safe AGY discovery-failed diagnostic while non-AGY errors retain their existing generic result. Raw stderr/paths/secrets are not user-visible. This refines the same technical SR-023 correction; approved intent, Large/High classification and independent-review route remain unchanged. Downstream tests must distinguish these failure cases and verify finite visible error/redaction.
- In-progress SR-023 inventory correction (no verdict yet): Reviewer observed the canonical supplemental inventory still described only ARCH-REV-001/002 and IR-001, omitting ARCH-REV-003 and completed pre-SR-023 source/API-E2E/Delivery evidence. `investigation-notes.md` inventory and the design's supplemental table now identify ARCH-REV-003 Pass on SR-021, CRR-009 implementation-source Pass, API-REV-006 Pass / 96%, CRR-012 changed-test-code Pass, and Delivery Engineer's unsigned Electron artifact/user-verification hold. None is promoted to SR-023 review/implementation/validation authority; no new approval or reviewer verdict is inferred.
- Informational review receipt: **ARCH-REV-004 Pass** independently reviewed SR-023 (Large/High) on unchanged approved SR-016/SR-021 behavior. Authoritative report: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/design-review-report.md`; cumulative reviewer history: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/architecture-review-revision-record.md`. The reviewer confirmed its **primary reviewed-package handoff to `/implementation_engineer`**. This is architecture approval only; prior source/API-E2E passes do not cover SR-023 code, and Delivery remains on explicit user-verification hold. Solution Designer does not forward the package again.


## SR-024 — Packaged Electron Org member configured-skill preparation failure

- Trigger: User's 2026-09-25 1.4.80 Electron screenshot `ctx_6d33e7893c9e__image.png` after sending a harmless first prompt to `/software_engineering_team/solution_designer`. The Org tree is active; the **member**, not Org configuration creation, failed to prepare.
- Prior/current status: SR-023 **ARCH-REV-004 Pass** with IR-008/CRR-013 source, API-REV-008 execution and CRR-015 test-code Pass; Delivery's replacement Electron package remains in explicit user-verification hold. Current **SR-024 Architecture Design Complete, independent review required**; those prior passes do not cover SR-024. No acceptance, final merge/release or Terminal result is inferred.
- Affected IDs: SCN-002, BEH-002 and the configured-skill aspect of DS-001, REQ-002/AC-002. BEH-005/REQ-005/AC-004 run-capsule isolation remains preserved. No change to AGY permission, trace, model, MCP, selected-workspace or other-runtime behavior.
- Evidence/uncertainty: The normal UI's exact error is `Failed to prepare agent run 'solution_designer_ad7d806883d744e688bc6227ad24362c'.` The team-local agent config binds `solution-designer`, whose skill source has two file symlinks to regular files under the same team-owned `shared/` folder. The **packaged** AGY materializer deterministically threw `AGY_SKILL_SOURCE_SYMLINK` on that exact source in a disposable reproduction. `AgentRunManager` wraps this cause in the generic UI error; the production exception cause was not logged, so root-cause attribution is high-confidence, not a directly captured live cause. `agy models` listed the selected slug, selected workspace existed, and the prior user-owned MCP collision file was absent.
- Approval impact: **No renewed user approval**; the approved intent is that configured skills are available for selected agents and generated config stays isolated. The missing trust/copy boundary is a technical design issue, not a new product scenario or broadened link capability outside package ownership. Product Design N/A — not requested.
- Design/review impact: `design-spec.md` adds DS-006: permit only existing regular-file symlinks whose canonical targets remain in the authoritative owning team/agent/skill source root; copy resolved bytes as regular files into the run capsule; reject escaping/dangling/cyclic/directory links and collisions. Keep the source and selected workspace untouched and restore from the immutable snapshot. The cumulative `task_size=Large` and `architectural_risk=High` remain; the narrow SR-024 delta itself is a local AGY leaf change with a material filesystem-security boundary. Independent architecture re-review precedes implementation; prior CRR/API-E2E/Delivery evidence is historical for this new failure.
- Verification gate: A new isolated full AutoByteus Org with the actual Solution Designer configured skill must admit its first AGY turn, produce provider init/binding and visible reply, and load both linked shared skill files from the capsule. Focused negative link-escape, dangling, directory-link/cycle, collision, `NONE` and restore-snapshot checks; source, selected workspace and global AGY config unchanged. Repeat packaged Electron user verification; merely showing an active Org tree is insufficient.
- Handoff rule decision: `get_handoff_rules` matched Architecture Design Complete + Large/High → `/architecture_reviewer`; direct implementation and delivery-receipt-gap rules did not match. Send confirmation is recorded separately in `solution-org-member-skill-recovery-handoff.md`/tool result.

- SR-024 in-review coherence clarification (no verdict yet): Architecture Reviewer requested exact handling of the actual team-local **agent-private** skill and provenance lost by `ConfiguredAgentSkillBinding`. `design-spec.md` DS-006 now explicitly extends the existing resolver's winning resolved binding with `agent_private`/`team_shared`/`global` provenance and trusted root. Team-local private/team-shared use the owning team root; global fallback cannot borrow it. `requirements-doc.md` now identifies current design recovery as SR-024 while retaining Approved SR-021 behavior. The reviewed basis and intended behavior are unchanged; this is not a new review round or approval request.
- Informational review receipt: **ARCH-REV-005 Pass** independently reviewed SR-024/DS-006 on the unchanged approved SR-016/SR-021 behavior basis, including the agent-private-under-team provenance clarification. Canonical report: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/design-review-report.md`; cumulative reviewer record: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924/tickets/in-progress/antigravity-cli-runtime-redesign-20260924/architecture-review-revision-record.md`. The reviewer confirmed its primary reviewed-package handoff to `/implementation_engineer`. This is architecture approval only; live cause was unlogged and SR-024 member first-turn, source review, API/E2E and rebuilt Electron user verification remain downstream. Solution Designer does not duplicate the implementation handoff.
- Post-ARCH-REV-005 evidence-only clarification: Compared Codex/Claude `WorkspaceSkillMaterializer` with AGY. The shared materializer links whole skill directories into `.codex/.claude` workspace paths and intentionally reflects source edits, with guarded collision/refcount/cleanup; existing Codex E2E covers a linked sibling `shared/` file. AGY instead requires immutable per-run capsule bytes for exact restore. This strengthens the SR-024 causal/design rationale but changes no approved behavior or DS-006 decision, and does not trigger a duplicate review/handoff.
