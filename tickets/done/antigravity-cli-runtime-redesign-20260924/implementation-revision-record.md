# Implementation Revision Record

Current code and `implementation-handoff.md` are authoritative; this record indexes implementation rounds.

## Revision Index

| Revision ID | Triggering Role / Report / Round | Finding IDs | Classification | Related Revision IDs | Result |
| --- | --- | --- | --- | --- | --- |
| IR-001 | Architecture Reviewer ARCH-REV-002 Pass; initial implementation; user visual feedback 2026-09-24 | User green-completion request; `AGENT_SEGMENT_LIFECYCLE_INVALID` | Requirement Gap | SR-016, SR-019; ARCH-REV-001/002; CRR/API-REV/DR N/A | Partial implementation with local segment fix; product status decision required before downstream handoff. |
| IR-002 | Architecture Reviewer `design-review-report.md` ARCH-REV-003 Pass; rework after SR-021 | REQ-011/AC-010 green AGY DONE; IR-001 lifecycle recheck | Local Fix | SR-021; ARCH-REV-003; CRR/API-REV/DR N/A | Neutral path removed; approved green mapping and local checks complete; independent source review next. |
| IR-003 | Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-001 Fail — Local Fix of IR-002 | CR-001, CR-002 | Local Fix | SR-016/019/021; ARCH-REV-003; CRR-001; API-REV/DR N/A | Org Team/Agent AGY permission draft corrections, focused checks and rendered editor check complete; return for independent source re-review. |
| IR-004 | Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-003 API/E2E failure-origin review of API-REV-001 | API-F-001, CR-003 | Local Fix | SR-016/019/021; ARCH-REV-003; CRR-001/002/003; API-REV-001; DR N/A | Current Team/Org run-tree AGY admission corrected with focused regressions; return for source re-review, then AGY-02 rerun. |
| IR-005 | Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-005 API/E2E failure-origin review of API-REV-002 | API-F-002, CR-004 | Local Fix | SR-016/019/021; ARCH-REV-003; CRR-001–005; API-REV-001/002; DR N/A | Native Org stream DTO AGY admission corrected with contract and handler snapshot regressions; return for source re-review, then AGY-05 rerun. |
| IR-006 | Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-007 API/E2E failure-origin review of API-REV-003 | API-F-003, CR-005 | Local Fix | SR-016/019/021; ARCH-REV-003; CRR-001–007; API-REV-001/002/003; DR N/A | Repeated full GraphQL schema build metadata corruption reproduced and prevented; distinct-ID Org projection/trace-page regressions pass; return for source re-review, then real Org public API rerun. |
| IR-007 | Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-008 re-review of IR-006 | CR-006; CR-005 attribution withdrawn | Local Fix | SR-016/019/021; ARCH-REV-003; CRR-001–008; API-REV-001/002/003; DR N/A | Unsupported production schema cache removed; single-build public Org query check retained; return for source re-review before API/E2E fixture correction and clean Org public API rerun. |
| IR-008 | Architecture Reviewer ARCH-REV-004 Pass on SR-023 DS-005 technical correction | 18-placement Org serial synchronous discovery, health starvation, safe failure reason | Design Impact correction | SR-016/019/021/023; ARCH-REV-004; CRR-009/API-REV-006/CRR-012 prior basis; DR-001 hold | Org creation uses one complete-placement batch; AGY CLI discovery becomes bounded async with typed safe diagnostics; local checks pass; renewed source/API-E2E review required. |
| IR-009 | Architecture Reviewer ARCH-REV-005 Pass on SR-024 DS-006 technical correction | Team-local Solution Designer linked configured skill fails pre-init with same-build `AGY_SKILL_SOURCE_SYMLINK` reproduction | Design Impact correction | SR-016/021/023/024; ARCH-REV-005; IR-008/CRR-013/API-REV-008/CRR-015 prior basis; DR-005 user hold | Resolver records winning source/trusted root; AGY snapshots only in-bound regular-file link targets into capsule; local actual-link and safety checks pass; renewed source/API-E2E and Electron user verification required. |

## Revision Entries

### IR-001 — AGY runtime initial implementation and status-semantic gap

- Triggering role/report/round: Architecture Reviewer `design-review-report.md` ARCH-REV-002 initial implementation; user screenshot in implementation round.
- Triggering finding IDs: user request for green command completion; local canonical lifecycle diagnostic.
- Classification: **Requirement Gap**.
- Prior authoritative result: N/A.
- Current authoritative result: current worktree source and `implementation-handoff.md`; not accepted or downstream-review-ready.
- Related solution revisions: SR-016, SR-019.
- Related architecture-review revisions: ARCH-REV-001, ARCH-REV-002.
- Related code/API/delivery revisions: N/A.
- Why recorded: initial implementation baseline and direct user feedback conflict with approved neutral AGY `DONE` semantics.
- Behaviors/requirements affected: BEH-006 / REQ-009/AC-008 and BEH-004 / REQ-008/AC-007; separate local canonical bug in BEH-006.
- Implementation delta: AGY backend/capsule/stream/runtime registration, run binding/restore, MCP/configured skills, canonical neutral trace, UI draft policy and presentation; removed extra `segment_type` from AGY `SEGMENT_CONTENT` and added canonical transformer regression. Split team launch edit code to satisfy source size guardrail.
- Changed areas: server AGY/runtime/manager/MCP/event/memory/stream areas; shared contracts; web launch, stream, hydration and tool components; focused tests. See current handoff.
- Local validation: focused server/web tests and server build typecheck passed; live scoped implementation probes passed; fresh live UI check after segment fix not done. No API/E2E sign-off.
- Next routing: Solution Designer for revised intended status behavior and user approval; then resume implementation as directed.
- Remaining risks: AGY underlying command exit is absent from `DONE`; green could misrepresent a nonzero command. Provider stall and fresh UI lifecycle path require further checks.

### IR-002 — Approved AGY tool completion and canonical replay

- Triggering role/report/round: Architecture Reviewer `design-review-report.md` ARCH-REV-003 Pass on user-approved SR-021; second implementation round.
- Triggering finding IDs: REQ-011/AC-010 green AGY command completion; separate `AGENT_SEGMENT_LIFECYCLE_INVALID` from IR-001.
- Classification: **Local Fix / ready for source review**. Prior result: IR-001 **Requirement Gap**. Current result: code and `implementation-handoff.md` are authoritative; no downstream acceptance claim.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR **N/A**; API-REV **N/A**; DR **N/A**.
- Why recorded: approved intended behavior replaces the disputed IR-001 neutral AGY `DONE` mapping while preserving all SR-019 integration gates.
- Affected behavior/requirements: BEH-006 / REQ-009/011 / AC-008/010; BEH-004 denial priority; non-AGY preservation REQ-006.
- Implementation delta: AGY `DONE` without explicit error emits canonical success and retains provider state/output without a fabricated shell exit; ERROR or explicit error wins and retains exposed output in trace. Removed the sole-producer neutral canonical event, DTO, trace, hydration and UI branches. AGY denial raw-trace marker restores denied status without changing other-runtime errors. Existing segment-content contract fix remains and was rechecked live.
- Changed areas: AGY stream converter; server event/stream/memory/replay; presentation and team contracts/dist; web streaming, hydration and tool components; focused tests.
- Local validation: server 57 focused tests passed (5 opt-in live tests skipped), TypeScript/build passed; web 59 focused tests passed; live and reloaded exit0/exit8/not-found cards green with trace output retained and no lifecycle diagnostic. Explicit-off live denial non-green; pre-refinement reload non-green FAILED. New denial replay-label path has passing writer/projection/hydration tests, but a second post-refinement browser run was prevented by browser connection loss. Earlier SR-019 local production probes remain applicable and are referenced in the handoff.
- Local trace audit: one IR-001 development trace, `daily_assistant_b421bebb5fa045c1b737d1565a45a43b` sequence 3, contains neutral `completed_unverified`; no released-data migration added.
- Classification continuity and next routing: **Large / High** confirmed; request independent Code Review under handoff rules.
- Remaining limitations: green is AGY provider-step completion, not shell exit-zero proof; model-directed workspace targeting and provider-version variation remain; final denied label needs independent live/reload verification.

### IR-003 — Org Team/Agent AGY launch permission correction

- Triggering role/report/round: Code Reviewer `code-review-report.md` / `code-review-revision-record.md`, CRR-001 initial full source review of IR-002 at `d0ec1fc07`.
- Triggering findings: CR-001 Org Team AGY selection omitted the default-on policy; CR-002 Org Agent later explicit-off selection used the wrong prior override and was forced on. Classification: **Local Fix**.
- Prior authoritative result: IR-002 source-ready, but CRR-001 **Fail — Local Fix**. Current result: current code and `implementation-handoff.md` are authoritative; bounded correction ready for source re-review, not Code Review or API/E2E acceptance.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR-001; API-REV **N/A**; DR **N/A**.
- Why recorded: both supported Org launch paths affect BEH-004 / REQ-007/008/010 / AC-009 and could launch AGY with the wrong auto-execute flag. A direct UI check also showed synchronous runtime/model/config edits could overwrite the policy before store props reconciled; this is the same CR-001 path, fixed locally rather than an architecture change.
- Implementation delta and locations: `autobyteus-web/stores/agentOrgRunConfigStore.ts` now applies the shared new-runtime policy to Team overrides and uses the exact prior Agent override for Agent transitions. `autobyteus-web/components/workspace/config/TeamScopeConfigEditor.vue` applies the same policy to its pending Team/Agent edit before the synchronous reset events. Focused store/effective-form, component emission and mounted Org panel regressions cover new AGY selection and later deliberate off; no new launch model or compatibility branch.
- Focused validation: nine web test files, **79/79 passed**, including adjacent non-AGY Team launch/form checks; `git diff --check` and source-size guardrail passed. Fresh browser preview showed Org Team AGY select-on, explicit-off, and direct Agent AGY select-on, explicit-off retained after collapse/reopen, with root off and clean layout. This is implementation-scoped validation only; the editor check did not launch an Org.
- Classification continuity: **Large / High** confirmed from the completed design; no changed requirement or design decision. Return to independent Code Reviewer under handoff rules.
- Remaining limitations/downstream gate: fresh final-code denied live/reload label and full AGY acceptance still require API/E2E. The user specifically requested Team test parity with Codex/Claude. Current `agy-mcp-team-live.test.ts` uses stub message delivery and is not the real GraphQL/WebSocket inter-agent roundtrip; API/E2E must author and execute the AGY equivalent before claiming that gate.

### IR-004 — Admit AGY through current Team/Org execution-tree validation

- Triggering role/report/round: Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-003 focused failure-origin review of API/E2E `api-e2e-execution-coverage-report.md` API-REV-001, AGY-02 real Team test and `/tmp/agy-api-02-team-final.log`.
- Triggering findings: API-F-001 / CR-003, `rootTeam.defaultLaunchConfiguration.runtimeKind is unsupported` before member activation. Classification: **Local Fix** for the approved normal SCN-002 / BEH-002 / REQ-002 / AC-002 path.
- Prior authoritative result: IR-003 passed CRR-002 source review, but API-REV-001 **Fail / 74%** and CRR-003 confirmed an implementation-owned current-schema omission. Current result: current code and `implementation-handoff.md` are authoritative; bounded correction ready for source re-review, not API/E2E acceptance.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR-001/002/003; API-REV-001; DR **N/A**.
- Why recorded: current Team and Org run-tree validators share `validateLaunchConfiguration`; its literal three-runtime admission predated AGY registration and blocked real Team launch. No requirement or design decision changed.
- Implementation delta: `autobyteus-server-ts/src/run-history/store/run-execution-tree-shared-record-schemas.ts` validates against supported `RuntimeKind` enum values instead of a stale literal list. New `tests/unit/run-history/current-execution-tree-runtime-admission.test.ts` covers production Team builder/current reload validator, Org root/direct Agent/nested Team/member admission, and unknown-kind rejection. No change to provider permission, trace, or transport behavior.
- Migration-only assessment: `released-team-run-v2-schema.ts` imports the shared validator; its historical record shape and transition mechanics are unchanged. The current enum extension also admits AGY if such a shape is encountered, but no released AGY predecessor population exists and no migration is required or added. Separate V1 historical validator is untouched.
- Focused validation: Team/Org admission plus existing current Team package **6/6 passed**; flat-family migration-only suite **16/16 passed**; production server TypeScript config passed after shared-package preparation; `git diff --check` and source-size guardrail passed. A broader historical-candidate safety suite separately failed **8/11** because its old-table token fixture lacks the unrelated new `claude_sdk_usage_state_json` column; this is not presented as passing. Generic all-tests TypeScript config has existing rootDir/out-of-tree errors; the production build config passed. Frontend render check **Not Applicable** because IR-004 changes backend validation only.
- Classification continuity: **Large / High** confirmed from reviewed design and current implementation. Return for independent Code Reviewer source re-review under handoff rules.
- Remaining gate: AGY-02 real GraphQL/WebSocket Team member roundtrip has not been rerun after this fix; direct Org launch and Team continuation remain unproven. API/E2E should rerun AGY-02 first, then Org and restore/continuation. API-REV-001 did confirm final denial DENIED live/reloaded and DONE green controls, but API/E2E did not pass.

### IR-005 — Admit AGY in the native Org stream snapshot contract

- Triggering role/report/round: Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-005 focused failure-origin review of API/E2E `api-e2e-execution-coverage-report.md` API-REV-002, AGY-05 direct/nested real Org test and `/tmp/agy-api-r2-org-final.log`.
- Triggering findings: API-F-002 / CR-004. Org GraphQL launch succeeded, but `AgentOrgStreamHandler` sent CONNECTED then `AGENT_ORG_STREAM_UNAVAILABLE` because `agent-org-execution-dtos.ts` excluded `antigravity_cli` from root/direct/nested launch-config values. Classification: **Local Fix** for SCN-002 / BEH-006 / REQ-002/009 / AC-002/008.
- Prior authoritative result: IR-004 passed CRR-004 source review; API-REV-002 passed real Team scoped ping→pong and exact Team restore/continuation, but overall **Fail / 81%** due Org stream. Current result: current code and `implementation-handoff.md` are authoritative; bounded contract correction ready for source re-review, not Org API/E2E acceptance.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR-001/002/003/004/005; API-REV-001/002; DR **N/A**.
- Why recorded: the return-event contract's runtime value set was stale after AGY became a supported Org runtime. The approved user workflow is unambiguous; no requirement or design decision changed.
- Implementation delta: `autobyteus-collaboration-stream-contracts/src/agent-org-execution-dtos.ts` adds AGY to the current native Org launch-config enum. Rebuilt checked-in `dist` from source. `tests/root-execution-view-dtos.test.mjs` covers a typed native AGY Org snapshot across root/direct/nested Team/member, unknown-kind rejection at each scope and preservation of the three existing runtimes. `autobyteus-server-ts/tests/unit/services/agent-streaming/agent-org-stream-handler.test.ts` verifies the actual handler emits CONNECTED, typed ROOT_EXECUTION_VIEW_SNAPSHOT and ROOT_LIFECYCLE for those AGY scopes rather than ERROR.
- Focused validation: collaboration contract build/test **8/8 passed**; focused Org stream handler **11/11 passed**; server production TypeScript check passed after shared-package build; `git diff --check` and source-size guardrail passed. No API/E2E Org rerun was performed in this implementation role. Frontend render check **Not Applicable** because current DTO and handler-test changes have no rendered frontend delta.
- Persisted transition: **Directly Usable — No Migration** remains. This is current stream validation, not stored-schema change or a historical reader fallback.
- Classification continuity: **Large / High** confirmed from reviewed design and implementation. Return to independent Code Reviewer source re-review under handoff rules.
- Remaining gate: API/E2E must rerun AGY-05 direct/nested real Org GraphQL/WebSocket first, then full Team+Org and non-AGY regression. Team real delivery and restore passed API-REV-002, but Org member command, scoped delivery and trace remain unproven after this fix. No API/E2E or delivery acceptance.

### IR-006 — Reuse the public GraphQL schema to preserve Org member argument binding

- Triggering role/report/round: Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-007 focused failure-origin review of API/E2E `api-e2e-execution-coverage-report.md` API-REV-003, AGY-05 real Org test, `api-e2e-round3-live-evidence.json` and `/tmp/agy-api-r3-{full-final,org-projection-spy,org-http-diag}.log`.
- Triggering findings: API-F-003 / CR-005. Real direct/nested AGY Org turns and scoped director→worker delivery passed, but public GraphQL projection reached the service with shifted identities and blocked member view/reopen. Classification: **Local Fix** for SCN-002 / BEH-006 / REQ-009 / AC-008.
- Prior authoritative result: IR-005 passed CRR-006 source review; API-REV-003 confirmed real Org execution and delivery, but overall **Fail** on public projection; CRR-007 classified the failure as implementation-owned. Current result: current code and `implementation-handoff.md` are authoritative; bounded correction ready for source re-review, not API/E2E acceptance.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR-001–007; API-REV-001/002/003; DR **N/A**.
- Diagnosis: a full-schema GraphQL unit query with distinct `org-123`, `/director`, `agent-456` calls `getProjection` correctly after one `buildGraphqlSchema()`, but a second build in the same process reproduces the reported `(org-123, org-123, /director)` shift. TypeGraphQL 2.0.0-rc.3's global `MetadataStorage.initCache()` appends the same parameter entries on each build; resolver metadata retains the array reference. The AGY live test built once for the studio server and again for its direct GraphQL client, so its HTTP diagnostic was also affected by the second build. This is a shared schema-lifecycle binding defect, not an AGY-specific service or argument typo.
- Implementation delta: `autobyteus-server-ts/src/api/graphql/schema.ts` caches and returns the process's initial full schema promise, avoiding a second metadata build and protecting both server and direct clients. `tests/unit/api/graphql/types/agent-org-run-config.test.ts` asserts schema identity and executes exact distinct-ID public Org projection and adjacent trace-page queries including the cursor. No alternate projection path, GraphQL argument rename, migration or provider behavior change.
- Focused validation: reproduction failed before the source fix with the exact API-F-003 shifted tuple; after the fix, focused Org/Team/definition-catalog GraphQL suites **9/9 passed** and production server `tsc -p tsconfig.build.json --noEmit` passed after shared preparation. `git diff --check` and source-size guardrail passed (changed schema source: 89 effective non-empty lines, 9-line delta). Generic `tsconfig.json` noEmit remains blocked by pre-existing test-outside-rootDir TS6059 errors and is not claimed as passing. No implementation-owned live HTTP/AGY E2E rerun was performed.
- Frontend/render: **Not Applicable**; this round changes only server schema construction and unit regression. Persistence: **Directly Usable — No Migration** unchanged; no stored data delta.
- Classification continuity: **Large / High** confirmed from reviewed design and shared API binding scope. Return to independent Code Reviewer for bounded source re-review under handoff rules.
- Remaining gate: API/E2E must rerun AGY-05 public Org projection/trace-page and reload with exact direct/nested member identities first, then full real Team+Org and non-AGY checks. Real Org scoped delivery alone is not public view acceptance; no API/E2E or delivery sign-off.

### IR-007 — Remove unsupported schema cache after fixture-origin correction

- Triggering role/report/round: Code Reviewer `code-review-report.md` / `code-review-revision-record.md` CRR-008 bounded source re-review of IR-006 at `e53844214`, following API-REV-003.
- Triggering finding: CR-006 — IR-006's process-wide schema memoization defended against an E2E fixture-only second full build without a supported production in-process rebuild contract. CRR-008 withdraws CR-005's production-defect attribution; the approved SCN-002 / REQ-009 / AC-008 Org view goal remains real and clean-server behavior is unproven.
- Prior authoritative result: IR-006 presented the cache as a public-boundary correction, but CRR-008 failed that source change and demonstrated that normal production `registerGraphql()` builds once per server child process. Current result: current code and `implementation-handoff.md` are authoritative; unsupported cache removed, bounded source cleanup ready for re-review, not API/E2E acceptance.
- Related revisions: SR-016/019/021; ARCH-REV-001/002/003; CRR-001–008; API-REV-001/002/003; DR **N/A**.
- Implementation delta: restored `autobyteus-server-ts/src/api/graphql/schema.ts` to its pre-IR-006 production behavior (no `schemaPromise` or altered lifecycle). Removed the second-build/cache-identity assertion from `tests/unit/api/graphql/types/agent-org-run-config.test.ts`; retained one full-schema GraphQL query checking exact distinct Org root/member/run identities and adjacent trace-page cursor binding. No AGY-only path, schema migration or alternate projection owner.
- Focused validation: Org, Team and definition-catalog GraphQL unit suites **9/9 passed**; production server `tsc -p tsconfig.build.json --noEmit` passed after `prepare:shared`; `git diff --check` passed. This validates source cleanup and the one-build argument contract, not an actual HTTP Org projection or persisted reload. No implementation-owned E2E rerun was performed.
- Frontend/render: **Not Applicable**; server-only cleanup and unit test. Persistence: **Directly Usable — No Migration** unchanged. Classification remains **Large / High** from the reviewed design.
- Remaining gate: after source re-review, API/E2E must correct the fixture to query the running server's public HTTP GraphQL endpoint without building another full schema, rerun AGY-05 direct/nested Org projection/trace and reload, then full real Team+Org/non-AGY validation. A new clean-server failure would require fresh focused origin review; no production projection failure or success is inferred from API-REV-003.

### IR-008 — SR-023 Org launch preflight and nonblocking AGY discovery

- Triggering role/report/round: Architecture Reviewer `design-review-report.md` / `architecture-review-revision-record.md` ARCH-REV-004 Pass on Solution Designer SR-023 / design-spec DS-005. Triggering user-verification evidence and technical investigation are indexed in `solution-org-launch-recovery-handoff.md` and `investigation-notes.md` §SR-023.
- Triggering finding/decision: a real 18-placement AGY Org eventually activated, but current Org creation performed serial per-placement model validation and synchronous AGY version/help/models subprocess discovery; a controlled catalog/health overlap starved `/rest/health`. SR-023 is a **technical correction**, not a new intended-behavior or UX contract. The original screenshot alone does not prove a permanent hang, and idle members do not require provider conversation IDs.
- Prior authoritative result: IR-007 source passed CRR-009; API-REV-006 passed 96% and CRR-012 passed changed-test review for the earlier source; Delivery DR-001 remains on explicit user-verification hold. These passes are prior-basis evidence, not validation of SR-023. Current result: current code and `implementation-handoff.md` are authoritative; corrected code is ready for renewed independent source review, not API/E2E or delivery acceptance.
- Related revisions: SR-016/019/021/023; ARCH-REV-001–004; CRR-009 source and CRR-012 test prior-basis passes (earlier CRR history retained); API-REV-006 prior-basis pass; DR-001 hold. Requirement approval remains SR-016 plus user-approved SR-021; no renewed approval was needed for DS-005.
- Behaviors/requirements: BEH-001 / REQ-001 / AC-001 and approved SCN-002 Team/Org AGY member scope. BEH-002–006 and SR-021 trace/permission/identity/MCP/skill contracts remain unchanged.
- Implementation delta: `AgentOrgRunService.validateCompleteConfiguration` collects root, Team and Agent placements in order, checks addressed workspace fields, calls the existing `RunModelSelectionService.validateMany` once, requires one result per input and maps the first failed result to its address. The shared validator already coalesces fresh catalog evidence per runtime/workspace within one operation; its `model_unavailable` result now optionally carries a safe AGY `catalogDiagnostic` on catalog failure. Unexpected AGY catalog errors receive generic safe discovery failure; valid catalogs missing the selected slug retain the distinct existing model-unavailable message. No validation is skipped and no process-global model cache is added.
- Discovery delta: `antigravity-cli-capability.ts` replaces AGY-only `spawnSync` version/help/models calls with bounded `spawn` child I/O, timeouts, separate output caps, process kill and stable user-safe diagnostic codes/messages. Raw stderr, paths, command strings and credentials are not forwarded. `AntigravityModelCatalog`, AGY availability, GraphQL availability, application host validation and AGY backend factory await this owner. Other-runtime provider implementations retain their existing behavior. Application AGY catalog catch also sanitizes unexpected errors.
- Focused validation: seven server unit suites **79/79 passed**; one discovery for equivalent 18 placements; separate workspace and runtime contexts; first addressed timeout and incomplete-batch errors; unexpected AGY error versus valid missing slug; compatible/missing/unsupported/invalid/failed/timeout CLI controls; local concurrent `/rest/health` response while fake AGY models discovery was pending; async availability/application safety and existing Team/model regressions. Server production TypeScript check and full build/bootstrap smoke passed; `git diff --check` and source size/delta guardrails passed. A broad test-inclusive TypeScript invocation remains blocked by pre-existing out-of-rootDir and unrelated fixture errors; not claimed as passing. No real packaged/browser or API/E2E validation was run in this implementation role.
- Frontend/render: **Not Applicable** to the changed server source; no frontend code or UI contract changed. Existing GraphQL result `message`, web store launch-finally and `role="alert"` are the intended path, but a new rendered slow/failure/missing-slug check remains an API/E2E gate.
- Data transition: **Directly Usable — No Migration**; no persisted schema/identity/trace change. Classification continuity: **Large / High** confirmed by reviewed DS-005 across Org orchestration, shared validation, child-process boundary, availability/application callers and browser error return path; no new design gap.
- Remaining gate: renewed independent source review, then API/E2E full 1-root/3-Team/14-Agent browser→real packaged/equivalent backend launch, active persisted tree, responsive concurrent `/rest/health`, finite sanitized addressed failure alert and valid-catalog missing-slug countercase, plus affected Team/Org/non-AGY regression. Delivery/user-verification hold remains; no release, merge, cleanup or user acceptance.

### IR-009 — SR-024 team-local configured-skill link snapshot

- Triggering role/report/round: Architecture Reviewer `design-review-report.md` / `architecture-review-revision-record.md` ARCH-REV-005 Pass on Solution Designer SR-024 / design-spec DS-006. Triggering screenshot, read-only active Org/member state and same-build packaged materializer reproduction are indexed in `solution-org-member-skill-recovery-handoff.md` and `investigation-notes.md` §SR-024.
- Finding/decision: the user's active 1.4.80 Org's Solution Designer member failed on its first prompt before AGY init/provider ID. Its private skill under the owning team has two legitimate file links into that team's `shared/`, but the packaged materializer's blanket `rejectSymlinks` deterministically throws `AGY_SKILL_SOURCE_SYMLINK`. The live exception cause was not logged, so attribution is high-confidence reproduction, not a captured production stack. No user Org data was altered.
- Prior authoritative result: IR-008 source passed CRR-013; API-REV-008 passed 96% on SR-023 and CRR-015 passed changed-test review; Delivery DR-005 built a 1.4.80 user-test package but remains on explicit user-verification hold. Those passes did not exercise lazy first-turn activation of this linked-skill member. Current result: current code and `implementation-handoff.md` are authoritative; SR-024 source correction is ready for renewed independent source review, not post-SR-024 API/E2E or delivery acceptance.
- Related revisions: SR-016/SR-021 approved behavior, SR-023/024 technical corrections; ARCH-REV-001–005; CRR-013/015 and API-REV-008 prior-basis passes; DR-005 hold. DR-001 earlier design finding resolved; no current design finding. Behavior/requirements: SCN-002 / BEH-002 / REQ-002 / AC-002 with BEH-005 / REQ-005 / AC-004 capsule isolation and SCN-005 / REQ-006 non-AGY preservation.
- Implementation delta: `ConfiguredAgentSkillBinding` now requires a small winning-source descriptor. The existing `ConfiguredAgentSkillResolver` records agent-private/team-shared/global origin and canonical source/trusted roots at resolution time; team-local private and shared use their owning team root, standalone private uses agent root, global fallback uses only its own skill root. AGY's sole materializer replaces blanket link rejection plus generic `fs.cp` with a checked snapshot: canonical/layout/segment-safe containment, no directory-link traversal, regular-file and in-bound file-link bytes copied into new private capsule files, case-folded name checks, bounded file reads, source identity/revalidation during and after copy, and candidate cleanup on failure. Owner executable mode is retained without group/world access. `NONE` returns before source inspection. Existing successful capsule manifest/restore logic stays unchanged; no source re-resolution on restore and no migration.
- Focused validation: five unit suites **87/87 passed**: shared resolver precedence/provenance, actual mounted Solution Designer's both linked shared files, synthetic team-private/team-shared/standalone/global positive and negative paths, `NONE`, unsafe out-of-root/dangling/cyclic/directory links, source-layout/provenance and name collisions, changed-during-copy cleanup, source/workspace isolation, owner executable mode and exact restore after source edit, plus Codex/Claude bootstrapper regressions. Production server TypeScript check and full build/bootstrap smoke passed; `git diff --check` and source-size/delta guardrails passed. Broad test-inclusive TypeScript has pre-existing out-of-rootDir and unrelated fixture errors and is not claimed as passing. These are implementation checks, not a real disposable Org member/provider E2E.
- Frontend/render: **Not Applicable** to server-only source; no UI source/contract change. Persisted transition: **Directly Usable — No Migration**; earlier capsules remain immutable and the failed member had no provider binding/capsule. Classification: **Large / High** confirmed by DS-006 shared binding and filesystem trust boundary; no Design Impact beyond the reviewed correction.
- Remaining gate: renewed independent source review, then API/E2E disposable full Org first prompt to the actual linked-skill member with AGY init/provider ID and visible answer, capsule/source/workspace/global safety and representative non-AGY checks. Delivery must rebuild Electron and obtain explicit user verification. Do not mutate the user's normal Org or claim the prior active 18-placement tree covered this lazy activation; no release/finalization/cleanup.
