# Solution Designer Result — SR-022 Live AGY MCP Observation

- Package: `antigravity-cli-runtime-redesign-20260924`; result: **Evidence-only clarification during user verification**. No new architecture-ready package, requirement change, approval request, or finalized delivery receipt.
- Original request/goals: Redesign the old unmerged AGY CLI backend on a fresh `origin/personal` worktree, preserve current agent identity and collaboration, use structured streaming and normalized AutoByteus traces, and explain/verify actual AGY behavior. The user now asks why a classroom AGY member read MCP JSON under AGY's home and invites experiments using the running Electron build/backend.
- Workspace: `/Users/normy/autobyteus_org/autobyteus-worktrees/antigravity-cli-runtime-redesign-20260924`; branch `codex/antigravity-cli-runtime-redesign-20260924`; initial base `origin/personal@40b1783f40c072b577ad9d0c5d8fe4f5418c6c38`; Delivery Engineer locally integrated later `origin/personal@fdbd07124f0eaaa1310379c1ca8e2449f97b0dcf` by `c9c8373e1dc02ca44c285cd96286dacf7708208c`; finalization target `origin/personal`, still pending user acceptance.
- Authority/approval: `requirements-doc.md` Approved SR-016/SR-021; `design-spec.md` SR-021 reviewed by ARCH-REV-003 Pass. Large task / High architectural risk. Product Design supplement N/A; independent architecture review report already exists. Source review, API/E2E and delivery evidence are owned by their specialists. Delivery handoff remains in `handoff-summary.md`, **awaiting explicit user verification**, not released or Terminal.
- User-run evidence: Actual packaged-Electron Classroom Simulation Team `classroom_simulation_team_e94d6692953445ba9b7887a9d421fb08` has two distinct run-owned capsule `.agents/mcp_config.json` files containing scoped local `serverUrl` values, while global `~/.gemini/config/mcp_config.json` has no MCP servers. `~/.gemini/antigravity-cli/mcp/autobyteus_agent_tools/{send_message_to,get_handoff_rules,delegate_task}.json` contains only name/description/parameter schemas, not endpoint or authorization settings. The professor and student raw traces each show home-schema `view_file` followed by actual `call_mcp_tool`; both directions returned `accepted:true`, `code:DELIVERED`, with exact member targets, and two attributed team communication records persisted. This is a read-only inspection of the user's existing run, not a new frontend process or a synthetic E2E claim. Do not copy private message bodies or scoped URLs from normal user data into the ticket.
- Earlier user-verification issue: A stale `.agents/mcp_config.json` in the selected default workspace used the reserved `autobyteus_agent_tools` name and reproduced `AGY_MCP_NAME_COLLISION`; frontend showed only generic prepare-run failure. At the user's explicit request, that single legacy `.agents` folder was removed after checking contents. A later real Classroom Team run worked. No production source changed.
- Interpretation and limits: AutoByteus's run-scoped endpoint config is distinct from apparent AGY-managed shared home tool-schema metadata. The schema-file role is an inference from file shape, source and trace; [official AGY MCP docs](https://www.antigravity.google/docs/mcp) document global and workspace `mcp_config.json` server definitions but do not specify the observed home per-tool JSON lifecycle. Do not claim provider global filesystem immutability, cache isolation/cleanup, all-version parity, or zero collision risk. The observed MCP collaboration path worked for this AGY 1.2.10 run.
- Artifact changes: `investigation-notes.md` §SR-022 and inventory; `solution-revision-record.md` §SR-022; `design-spec.md` non-normative clarification that AutoByteus does not author global config but AGY may create home metadata. Requirements, approved mapping and architecture remain unchanged. `handoff-summary.md` and delivery artifacts are not edited by Solution Designer.
- Open risk / expected action: User may continue Electron verification and should explicitly report acceptance or a new failure to Delivery Engineer's gate. The old generic collision error can be evaluated as feedback if the user requests a change. No renewed requirement approval or re-review is indicated by this observation alone.
- Handoff-rule result: `get_handoff_rules` returned Architecture Design Complete→review, direct low-risk implementation, and Delivery Completed receipt-gap routes. None matches this evidence-only user-verification clarification: SR-021 design is already reviewed, no new architecture package is offered, and no Delivery Completed receipt was returned. No `send_message_to` handoff is made; return findings to the user.

## Follow-up — exact home JSON content requested (2026-09-25)

The user requested the contents and reason for AGY's home files. Read-only inspection of `~/.gemini/antigravity-cli/mcp/autobyteus_agent_tools/` found exactly three JSON files: `get_handoff_rules.json` (303 bytes), `send_message_to.json` (2305 bytes), and `delegate_task.json` (1682 bytes), each created/modified at 2026-09-25 05:33:03 Europe/Berlin. Each top level is precisely `name`, `description`, `parameters`; none contains `serverUrl`, localhost, or Authorization. The user-facing reply may quote the entire JSON because it is only MCP tool description/schema, not a scoped endpoint or secret. `delegate_task.json` appearing in discovery does not mean the classroom run invoked delegation, nor does it add AGY-native subagent scope to this ticket. The provider's reason for choosing the shared user-home path and exact cache lifecycle are not documented in the inspected official MCP docs; presenting it as discoverable schema staging is an evidence-based inference, not a verified provider guarantee. No new requirements/design/review route is indicated.
