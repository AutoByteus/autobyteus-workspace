# API/E2E Execution Coverage Report — AGY runtime

## Latest authoritative result and route
**API-REV-009 — Pass / 96%** for SR-024 / DS-006 / IR-009 source `42ec93de082dde28a26f421cbc3e64dece4a19c7`, independently source-reviewed **CRR-016 Pass**. Classification **Large / High, reviewed**. This is API/E2E validation of the checked-source backend/browser equivalent, **not** acceptance of the stale 1.4.80 Electron package, the user's normal Org, a release, or final Delivery. New durable browser/process test requires proportional Code Reviewer test-code review. Prior API-REV-008 Pass/96% was pre-SR-024 and is not substituted for this result.

## Authorities and boundary
Approved `requirements-doc.md` SR-021 REQ-002/AC-002 and SCN-002, `investigation-notes.md` SR-024, `solution-revision-record.md`, `design-spec.md` DS-006, `investigation-result.md`, `solution-org-member-skill-recovery-handoff.md`, `solution-org-launch-recovery-handoff.md`, `design-review-report.md`/`architecture-review-revision-record.md` ARCH-REV-005, `implementation-handoff.md`/`implementation-revision-record.md` IR-009, `code-review-report.md`/`code-review-revision-record.md` CRR-016, prior API/E2E investigation/report/revision/ledger, test-review report and Delivery hold were read. Paths are under this ticket except source/test files. Legacy removal check: no old materializer retained; persisted data **Directly Usable — No Migration**; restore uses the existing run-start capsule. No backward-compatibility shim was added.

Changed boundary: configured private skill resolver provenance and trusted owning-Team root → checked AGY capsule snapshot of two in-bound file links → lazy first member activation inside a real full Org → AGY provider identity and browser answer → actual backend process restart and exact continuation. Older 18-placement browser launch alone did not prompt the member. Electron shell packaging was not changed by API/E2E and is Delivery-owned.

## Executed cases and evidence
| Case | Result | Direct evidence |
| --- | --- | --- |
| **AGY-16 actual Solution Designer Org first turn** | **Pass** | New opt-in `autobyteus-web/tests/e2e/agy-actual-solution-designer-skill-org-probe.mjs` ran against freshly built server, real AGY and headless Chrome/Nuxt. It bound the actual read-only `/Users/normy/autobyteus_org/autobyteus-agents` package to isolated app-data/SQLite/workspace. Disposable Org contained 1 root, 3 *actual package* Teams and 14 Agents; exact configured member `/software_engineering_team/solution_designer` (`solution-designer` definition) was focused in the browser. First harmless browser prompt yielded visible exact answer, UI returned **Idle**, public projection matched run/address, and real AGY provider ID `0784e353-e6d3-42b4-921b-a641e9ad57a3` was bound. Both `design-examples.md` and `design-principles.md` were ordinary non-symlink capsule files with exact SHA-256 equal to their read-only Team-shared source links; selected workspace remained empty. `api-e2e-round9-actual-solution-designer-org/evidence.json`, `solution-designer-first-answer.png`, backend A log, `/tmp/agy-api-r9-actual-org-final-code.log`. |
| **AGY-16/17 same actual member restore** | **Pass, bounded shutdown observation** | Backend OS A PID 86146 stopped and distinct B PID 86240 started on the **same isolated root**. Browser reloaded/focused the same member; old prompt+answer marker was visibly present before the next send, old and new markers each appeared twice after the real AGY continuation, same member/provider IDs and both capsule hashes persisted. B returned to Idle. A exited **1** on `SIGTERM` with generic `General process run supervisor close failed` despite completed turn; this anomaly is preserved in `backend.log` and **not called clean shutdown/fixed**. B restoration and continuation passed. `solution-designer-after-restart.png`, `backend-after-restart.log`, evidence JSON. |
| **AGY-17 negative links, provenance, NONE and immutable snapshot** | **Pass 40/40** | Five focused unit suites: AGY actual mounted-skill/synthetic private-team-global links, out-of-root/dangling/directory/cyclic rejection/candidate cleanup, source changed during copy, collision, NONE, executable mode and exact source-edited restore; AGY capsule and Codex/Claude/shared workspace skill materializers. `/tmp/agy-api-r9-skill-regression.log`. |
| **AGY-18 Team/Org transport regression** | **Pass 2/2** | Real opt-in AGY GraphQL/WebSocket Team scoped send to real member and direct/nested Org delivery/attribution, public projection, quiescent stop/restore. `/tmp/agy-api-r9-team-org.log`. |
| **AGY-18 package/API and non-AGY representative checks** | **Pass 25/25, bounded** | Current package-private/team-shared skill GraphQL E2E 4/4, AGY availability 10/10, Org model-selection 11/11; Codex/Claude/shared materializer unit tests above. `/tmp/agy-api-r9-api-nonagy-regression.log`. |

### Stale older Codex/Claude provider-real fixture — not a product failure
Selected `agent-runtime-graphql.e2e.test.ts` skill cases were attempted with `RUN_CODEX_E2E=1 RUN_CLAUDE_E2E=1`. All three stopped **before provider startup**: the old inline-schema fixture lacks current Studio application API service wiring (`/tmp/agy-api-r9-codex-claude-live.log`). A temporary E2E-only setup probe, reverted completely, progressed to the generic E2E Studio service's intentional `createAgentRun` unimplemented result (`/tmp/agy-api-r9-codex-claude-live-fixed.log`). No production source or durable fixture change from that probe remains, no provider-run pass or product defect is inferred. This legacy suite is `Needs Update` separately; current focused Codex/Claude materializer unit and package GraphQL checks passed. Earlier real non-AGY provider evidence remains prior-basis only.

## Environment, commands and cleanup
Instructions: `autobyteus-server-ts/AGENTS.md`, `autobyteus-web/AGENTS.md`, server/web README and package scripts. Commands: `pnpm -C autobyteus-server-ts prepare:shared` and `pnpm -C autobyteus-server-ts build` (**Pass**, `/tmp/agy-api-r9-prepare.log`, `/tmp/agy-api-r9-build.log`); `RUN_AGY_E2E=1 node autobyteus-web/tests/e2e/agy-actual-solution-designer-skill-org-probe.mjs` (**Pass final**, log above); five-file Vitest `--no-watch` **40/40**; real AGY Team/Org Vitest **2/2**; three-file API/unit Vitest **25/25**. `node --check` new script and `git diff --check` passed. The canonical case ledger records exact surface, expected/observed, fixture retries and paths per case.

macOS, local authenticated AGY CLI, headless Chrome 1440×1000, dynamic loopback backend/Nuxt ports, isolated migrated SQLite/app-data/workspace; actual mounted package used read-only in both backend startups. Owned processes, Chrome and disposable data were removed; evidence/logs/screenshots retained. User's normal Org, normal data, source package, selected normal workspace, global AGY settings and packaged Electron were not modified. Existing untracked shared SDK `dist` belongs to prior work and was not removed/staged. No release/finalization/deployment or explicit user verification performed.

## Confidence and outcome
Repository-only confidence before the actual-member browser/process boundary: **80%** (critical lazy first-turn proof missing). Broader validation **Required and executed**. Final seven categories: requirement/AC proof **95%**; changed-boundary directness **100%**; cross-boundary realism/mock gap **95%**; environment/config/identity fidelity **95%**; failure/edge/lifecycle/recovery **90%** (exit-1 shutdown anomaly); user-surface/browser **100%**; durable regression coverage **95%** (new opt-in test plus focused negatives; old unrelated Codex/Claude live fixture stale). Mean **96% rounded**, no category below 90%; all critical SR-024 API/E2E gates passed. Residuals: backend-A abnormal exit despite successful B restore; old non-AGY provider-real fixture unexecutable before provider; other AGY versions and rebuilt Electron shell/user verification not proven here. Preserve these, not silently convert to passes.

**Durable test added:** `autobyteus-web/tests/e2e/agy-actual-solution-designer-skill-org-probe.mjs`. **Updated/removed:** none. **Production source changed by API/E2E:** none. Preliminary outcome **Pass**, with the two bounded observations above for Code Reviewer. Reviewed route: proportional changed-test-code review is **Required**; no direct handoff to Delivery until that review and Delivery's own rebuild/user-verification gates.

## CRR-017 / TR-002 reporting-only correction
The AGY-16 provider ID above is the value in the **final committed** `api-e2e-round9-actual-solution-designer-org/evidence.json` for both `firstTurn.providerRunId` and `restore.providerRunId`: `0784e353-e6d3-42b4-921b-a641e9ad57a3`. The prior report accidentally retained `dd5dd0bc-56fe-4d41-bb7b-0eab2d9b2f18` from an earlier passing run before the final changed-test-code rerun; that earlier run is historical and not the linked final evidence. This corrects report provenance only: **no test, production source, execution evidence, 96% confidence score or exit-1 caveat changed**, and no new validation was inferred. CRR-017 found the durable test itself proportionate; its TR-002 report mismatch returns for bounded review.
