import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';import {startBuiltTestServer} from '../../../../../test-support/live-e2e/test-runtime-bootstrap.mjs';
const here=path.dirname(fileURLToPath(import.meta.url));const root=path.resolve(here,'../../../../..');
const server=await startBuiltTestServer({port:50254,runtimeRoot:path.join(root,'autobyteus-server-ts/tests/.tmp/activity-retain-api001'),databaseUrlOverride:'file:'+path.join(root,'autobyteus-server-ts/db/activity-retain-api001.db'),environment:{NODE_OPTIONS:'--import='+path.join(here,'telemetry.mjs'),AORG_TELEMETRY_PATH:path.join(here,'telemetry.json')}});
await fs.writeFile(path.join(here,'server-info.json'),JSON.stringify({pid:server.child.pid,serverUrl:server.serverUrl,runtimeRoot:server.runtimeRoot,database:server.database.databaseUrl}));console.log('READY',server.child.pid,server.serverUrl);
while(true){await new Promise(r=>setTimeout(r,500));const c=await fs.readFile(path.join(here,'control.json'),'utf8').then(JSON.parse).catch(()=>null);if(c?.action==='stop'){await server.stop();await fs.writeFile(path.join(here,'server.log'),server.output());await fs.unlink(path.join(here,'control.json'));break;}await fs.writeFile(path.join(here,'server.log'),server.output());}
