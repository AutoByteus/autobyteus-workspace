# Investigation — ACTIVITY-RETAIN-20260914-001
## Bootstrap
New isolated worktree /Users/normy/autobyteus_org/autobyteus-worktrees/retain-activity-after-termination, branch codex/retain-activity-after-termination. Explicit base origin/requirements/flat-agent-organization-model freshly fetched at c208f33dcc3a8a56563a2f132f3de65862e68cab; eventual merge target same unreleased feature branch, NOT personal. Fresh origin/personal comparison 5645b49d6f51faa60bd3545bc8e3f0e7e3f96793. Clean status verified after worktree creation. Previous follow-up remains archived/completed; this is a separate ticket.
## User evidence
Two supplied screenshots: Team lead selected, live Idle shows Activity 2 Events (System instructions and successful send_message_to); Offline screenshot retains same conversation including successful tool segment but right Activity is 0 Events / No activity history yet. User says Terminate caused disappearance and later Send restores it. Screenshots establish visible contrast, not exact running commit/provider/termination call or all placement parity. No user server/conversation actions performed.
## Constraints
Read-only source investigation and task-owned documentation only until requirements approval/design. Preserve previous lazy readiness, retained restart statuses, manual task decision correctness, identities, messages, attachments and history. No backend rename, schema policy, migration, reset, release, deployment or global event-journal redesign requested.

## INV-001 — Exact Team path
Source base c208f33dc. WorkspaceAgentRunsTreePanel.vue:246–247 and RunningAgentsPanel.vue:225 invoke agentTeamRunStore.terminateTeamRun. Store:200–218 awaits backend success, disconnects stream, sets root inactive, applies Offline cleanup and explicitly calls clearActivities for EVERY retained member (:211). agentActivityStore.ts:374 deletes the run entry and increments its revision. ActivityFeed.vue:74–81 reads the selected Agent run ID and getActivities, not an active-only filter. Conversation is a separate context, explaining why completed tool segments remain in the center. Later Send on an inactive root (store:251–265) restores/hydrates and commits Team history activities, explaining reported reappearance without proving user's raw trace deletion. This is a confirmed source mechanism matching supplied screenshots; no independent browser reproduction yet.
## INV-002 — Parity and separation
Individual Agent terminateRun (agentRunStore.ts:370–415) disconnects and applies Offline cleanup without clearing Activity. Common agentRuntimeStatusState.ts:57–65 changes status/submissionPending only, no Activity deletion. Org stopAndInspect (agentOrgContextsStore.ts:248–264) retires stream, performs backend stop, marks historical, then readInspection; AgentOrgExecutionContext.setActive(false):202–209 does Offline cleanup without clearing Activity. Org hydration stageAgentOrgExecutionContext/applyProjection explicitly builds/replaces projected activities for direct and mounted contexts. Thus the same explicit destructive call is Team-specific in inspected production code; not proof that every individual/Org runtime journey is unaffected. Org projection availability/replacement, retained focus and stop failures require relevant validation. A scan of production clearActivities calls found only Team termination; fixture/test calls excluded.
## INV-003 — Personal comparison
Fresh origin/personal 5645b49d6 (Sep11) contains the identical explicit Team clear at agentTeamRunStore.ts:211. Read-only Aug30 d1a399a5919cf9b6040050d5699caeb0cd1e6633 also contains it (:202). Initial git log -S exact statement located its current textual form at 57ab99fcc (Aug11). INV-005 below corrects any implication that this introduced the clearing behavior: the parent already clears, and the Feb26 embedded-source snapshot also does. This is source history, not a runtime bisect. User's remembered older working experience is retained; exact deployed version/settings/timing unknown. Do not assert the flat-Org refactor or previous child fix introduced this deletion. No personal code copied or branch changed.
## INV-004 — Existing coverage limitation
agentTeamRunStore.spec.ts:423–445 tests termination once, disconnect, Offline/submission cleanup and history status. Activity store is mocked (:119–123); test does not assert retained completed/system Activity or render the post-stop panel. This explains a source-visible missing assertion, not proof of all historical test coverage. No tests executed in this investigation. Prior ticket/API passed other lifecycle scenarios; do not convert that into coverage of this precise visible Activity transition.
## Evidence / supplement inventory
- evidence/team-live.png and evidence/team-terminated.png: exact copies of user attachments, inspected visually in conversation, evidence only, no normative pixel redesign or Product approval.
- bootstrap-handoff.md: bootstrap and requirements-phase receipt, not implementation authority.
- Prior completed ticket at base tickets/done/flat-agent-organization-model-follow-up: reference for preserved laziness/restart/manual decision design, not the current ticket or approval.
## Unknowns and next verification
Actual user's app commit/runtime and precise Stop entry not captured; current Team source sufficient to define the ordinary supported scenario. Validate real frontend live→Terminate→retained Activity→later Send for Team; parity for standalone Agent and Org direct/mounted. Include inactive read-only focus/reopen, rejected stop, genuine empty histories and correct non-actionable stopped controls. Do not use automatic activation, arbitrary API substitutes, server-data edits or history recreation to manufacture success.

## INV-005 — End-to-end personal comparison and why Org diverged (SR-002)
User explicitly requests original nested-Team comparison and correct design rationale; no approval of SR-001 inferred. Read-only git show/grep/diff of pinned origin/personal5645b49d6, current c208f33dc, Aug30d1a399a59, 57ab99fcc/parent and b1c89884e. Personal WorkspaceAgentRunsTreePanel injects terminateTeamRun into useWorkspaceHistoryMutations.onTerminateTeam; wrapper awaits result and toasts failures, no post-success content hydration. Store clears Activity after success while retaining context/selection. History refresh rebuilds sidebar then reconcileDiscoveredActiveRuns marks retained inactive roots/leaves offline; this inactive branch does not repopulate Activity. Active context getter is selection-based, not liveness-based; ActivityFeed consumes the same per-run Activity store. Send on inactive root explicitly hydrates and commits activities. No compensating post-stop Activity reload found in this inspected ordinary path. Therefore source predicts the same disappearance on this personal snapshot, stronger evidence than isolated clear call, but no historical runtime execution or reconstruction of user's actual installed build.
Direct diff personal→current agentTeamRunStore has submission/attachment changes, NOT termination changes. Clearing predates Org transition: Aug11 commit rewrites existing cleanup; Feb26 b1c89884e embedded module snapshot already clears Team Activity during shutdown. No claim of first historical introduction beyond that snapshot.
Org divergence is traceable to 61f633abe (Sep12, fix(agent-org): separate inspection continuation and retained stop), which introduced stopAndInspect. Original feature ticket archived implementation-revision-record.md IR-046 explicitly records rationale: previously mixed restore-on-select/Stop-to-config behavior replaced by observational inspection, separate deliberate continuation, retained exact conversation/drafts, strict staged activity publication, stale-generation protection and failed-read content retention. Current source retains this path; later IR-0028bc62ce5f adds restart recovery, not the original stop-and-inspect mechanism. Existing feature design DS-037/047 additionally covers delayed physical history reads overwriting confirmed stopped state. These are historical design/implementation explanations, not newly performed validation.
Design assessment (recommendation only): common invariants, separate container addressing. Stop controls runtime ownership; retained per-Agent presentation/history remains inspectable; current interaction eligibility separate from historical tool records. Clear retained Activity is unjustified on success-stop, but clear/dispose can be valid for actual discard/remove. Full reinspection can reconcile final persisted outcomes and container/task structure, but is not intrinsically required because Org is nested. Don't add a Team-wide post-stop fetch just to recreate what was deleted; don't strip Org observation/stale-response safeguards merely for code symmetry. Architecture phase after approval must assess minimal owner correction and preserve read-only controls without global abstraction or history-policy rewrite.
Supplement personal-stop-comparison.md captures request, exact comparison evidence, uncertainty and recommendation; no behavior-defining supplement or new approval. No source/test/runtime mutation.

## AINV-001 — Post-approval architecture investigation
SR-003 explicitly approves SR-001 outcomes; no changes to requirements. Source HEAD c208f33dc, new worktree has only ticket docs/evidence changes. agentActivityStore.getActivities is a per-run presentation read; its recent-window policy already bounds retained entries. Removing the Team stop clear retains existing map content, not a new cache or persisted record. The only production useAgentActivityStore import/use in agentTeamRunStore is stop cleanup, so remove that dependency with the call. applyOfflineOrTerminalCleanup affects currentStatus/submissionPending only. TeamStreamingService.disconnect:137+ unbinds listeners, nulls context/root, clears approvalTracker and drains pending commands; agentTeamRunStore.disconnectTeamStream deletes the service. postToolExecutionApproval returns with no service and never restores one. ToolActivityItem is details/status display only (expand args/logs/result), not command authority. ToolCallIndicator approval controls belong to the retained conversation, not the Activity delta; handlers call existing command owner without optimistic status mutation. Current configured Team workspace access can remain 'live' as a capability label after stop, and a stale conversation button may still look enabled; no new runtime dispatch is authorized because the stream owner is absent. Do not claim universal disabled-button UX; scope safety means no executable stale decision/no automatic provider activation. If acceptance uncovers actual post-stop stale execution, return Design Impact rather than introducing global capability machinery silently.
Backend unchanged path: api/graphql/types/agent-team-run.ts:206→TeamRunService.terminateTeamRun:215→manager termination; successful response gates frontend cleanup. Org context retains separate stopAndInspect strict staged read with stale-response/identity guards; standalone Agent cleanup retains Activity. No persistence/model/schema/read/write change is needed for a retained-client presentation correction. Historical full reload can rebuild source-limited Activity under existing policy; this change does not promise indefinite history retention/all raw events or introduce a post-stop replay.
Real renderer/lifecycle tests should use actual Activity store and retained contexts, not the existing mock-only activity fixture. Existing cancellation, failure, duplicate-stop and restore behavior must remain. No test execution/browser/provider operations by Designer.
