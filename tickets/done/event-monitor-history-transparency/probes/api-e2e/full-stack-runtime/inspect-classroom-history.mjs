import {createRequire} from 'node:module'; const require=createRequire(new URL('../../../../../../autobyteus-web/package.json',import.meta.url)); const {chromium}=require('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage({viewport:{width:1280,height:900}}); await p.route(/^http:\/\/10\./,r=>r.fulfill({status:204,body:''})); await p.goto('http://127.0.0.1:54588/agents'); await p.waitForTimeout(4000); await p.getByRole('button',{name:'workspace',exact:true}).click(); await p.waitForTimeout(700); const g=p.getByRole('button',{name:/Classroom Simulation Team/}).first(); console.log('group',await g.count(),await g.innerText()); await g.click(); await p.waitForTimeout(700); console.log((await p.locator('body').innerText()).slice(0,3000)); console.log(JSON.stringify((await p.locator('button').evaluateAll(bs=>bs.map((b,i)=>({i,text:b.innerText,aria:b.getAttribute('aria-label'),title:b.getAttribute('title'),expanded:b.getAttribute('aria-expanded')})).filter(x=>x.i<35))),null,2)); await p.screenshot({path:new URL('classroom-history-expanded.png',import.meta.url).pathname,fullPage:true}); await b.close();
