# Solution Handoff — SR-014 Claude SDK Known-Context Token Meter Correction

- **Result:** `Architecture Design Complete` on **Approved SR-014** for the live Claude SDK known-context Token Meter defect; package `new-models-gpt6-opus55`. SR-014 is a bounded correction layered on SR-012/SR-013's approved/reviewed monetary design. **ARCH-REV-008 Pass** and its primary handoff to `/implementation_engineer` covered only SR-012/SR-013 on Approved SR-011, **not** SR-014. Historical sequence: ARCH-REV-007 Pass and initial handoff had already completed **before** the user's broader-probing/hold request; reviewer then held affected monetary implementation, and ARCH-REV-008's renewed handoff superseded that temporary hold. Neither prior Pass is a source/API-E2E/delivery conformance claim for this fix.
- **Classification:** cumulative `task_size=Large`, `architectural_risk=High`. The SDK result/event, selected-model cumulative fold, configured pricing snapshot, persistent quality, GraphQL/stream and Token Meter cross owners; wrong attribution or cache pricing would misstate money.
- **Workspace:** `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55`, branch `requirements/new-models-gpt6-opus55`; original base `origin/personal@467c1bc12d439ee79243d124402c2f65f25c3cd2`; finalization target `origin/personal`. Earlier Delivery Engineer integrated a later `origin/personal` into this worktree but did not finalize/release or obtain user acceptance.

## Canonical package

Ticket root: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55`.

- Approved requirements SR-014 (SR-011 monetary basis retained): `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/requirements-doc.md`
- Evidence I-01–I-45: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/investigation-notes.md`
- Current SR-014 context correction on SR-012 monetary design, with historical SR-003–010 sections: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/design-spec.md`
- Cumulative SR-001–014 index: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/solution-revision-record.md`
- This handoff: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/solution-handoff.md`
- Independent review history: same root `design-review-report.md`, `architecture-review-revision-record.md`. ARCH-REV-006 Pass reviewed SR-010/SR-009. ARCH-REV-007 Pass reviewed SR-012/SR-011 **before the hold request and SR-013's expanded probes** and sent its primary handoff to `/implementation_engineer`; reviewer then temporarily held affected monetary work. ARCH-REV-008 Pass re-reviewed SR-012 on Approved SR-011 **with** SR-013/I-42–I-44 and sent a renewed implementation handoff, superseding the hold. Prior ARCH-REV-005/003 Passes remain basis-limited.
- Specialist history: same root `implementation-handoff.md`, `implementation-revision-record.md`, `code-review-report.md`, `code-review-revision-record.md`, `api-e2e-coverage-investigation.md`, `api-e2e-test-case-ledger.md`, `api-e2e-execution-coverage-report.md`, `api-e2e-revision-record.md`, `api-e2e-test-review-report.md`, `delivery-revision-record.md`, `handoff-summary.md`, `docs-sync-report.md`, `release-deployment-report.md`. Previous source/API/E2E passes apply only to earlier scope; new monetary scope still requires source review, API/E2E and delivery gates.
- Key new sanitized evidence: `evidence/solution-designer-claude-sdk-cache-split-live-probe.test.ts`/`.log`, `evidence/solution-designer-claude-sdk-full-usage-shape-live-probe.test.ts`/`.log`, `evidence/solution-designer-claude-sdk-tool-usage-shape-live-probe.test.ts`/`.log`, and the failed guard follow-up `evidence/solution-designer-claude-sdk-tool-usage-guarded-live-probe.test.ts`/`.log`, all at ticket root. User images and other probes are indexed in investigation. Product UI/UX supplement: **N/A — not requested**; existing UI needs only an explanatory cost note, not a new product experience.

## User authority and facts

Original request: latest local `personal`, exact GPT-6 Sol/Luna and direct Anthropic Opus 5.5 support, both Anthropic SDK upgrades, no-key direct API/Autobyteus tests and locally authenticated Codex checks. Direct Claude uses `@anthropic-ai/sdk` Messages API; separate Claude Agent SDK discovers models dynamically and uses local auth. Earlier original work passed scoped checks but was not finalized. A real Electron Token Meter showed selected Opus variant `claude-opus-5-5[1m]` as `Price missing / model_not_found`.

Safe probes proved the existing first-map-key bug, selected `opus[1m]` result with both Haiku and Opus per-model records, cumulative create→resume semantics and SDK `supportedModels()` `value:'opus[1m]' → resolvedModel:'claude-opus-5-5[1m]'`. User Approved SR-009: **show only the selected Opus model's token statistics, never Haiku or Multiple models**. SR-010 designed selected-only identity/checkpoints with SDK `costUSD` and passed ARCH-REV-006.

User then chose to retain Autobyteus' pre-existing **own configured-price calculation** rather than SDK `costUSD`, because the dollar display is an estimate, not subscription billing. The user explicitly asked to update design and delegated choosing a reasonable cache-write estimate. New I-41 one-turn subscription-backed, cost-limited, sanitized probe confirms selected Opus per-model aggregate cache creation 7140, with top-level main-loop `cache_creation` 5m=0/1h=7140; all main-loop token dimensions matched selected Opus for that turn. SDK type says top-level `usage` is per-turn/main-loop-only and `modelUsage` is cumulative/per-model/query-pipeline; equality is **not universal**. No response text, key, signature or direct paid Anthropic API call was logged. The previous partial-only cache proposal and tentative SDK-dollar preference are history, not current behavior.

**User-requested evidence expansion (SR-013, evidence-only):** I-42 live create→resume showed terminal top-level per-turn cache splits matched selected Opus per-model **deltas**, not cumulative raw totals, in both turns; unchanged Haiku remained in the model map. I-43 live tool-bearing turn emitted two assistant frames (`tool_use`, then `text`) whose output usage **did not sum** to the terminal result; the terminal result's split and selected Opus per-model totals did reconcile, alongside an unselected Haiku row. This reinforces result-based pricing and forbids summing streamed assistant-frame usage. The tool probe printed an SDK warning that bare `allowedTools` shadowed its `canUseTool` callback; I-44 follow-up guard assertion failed. Do **not** claim that command-level callback gating was verified, and do not reuse that harness for live tool probes without a verified PreToolUse hook. Only sanitized numeric/model/block-type metadata was logged; no secret, response text or signature. These facts strengthen SR-012's exact reconciliation gate and do not change Approved SR-011 behavior or its 1h fallback.

## Current SR-012 design and review focus

Retain SR-010's actual-turn selected alias→SDK-resolved-raw→exact per-model entry match, selected-only token deltas, all-source private token checkpoints and idempotent transactional fold. Capture a **numeric-only** top-level per-turn cache-write 5m/1h split. Use it for selected model pricing **only if** its 5m+1h sum and all top-level input/output/read/write counts equal the selected per-model delta. Otherwise, for positive selected cache writes, use the configured **1-hour** write rate as a visibly marked approximation; missing trusted rate remains partial. No write means no TTL assumption. User delegation and the real 1h-matching probe justify this bounded approximation, not a claim of actual TTL on all turns.

Resolve exact SDK-provided selected `canonicalModel` (for observed Opus, `claude-opus-5-5`) to Autobyteus' configured Anthropic rate at observation time, then use the **existing TokenPriceConfigProvider/TokenCostCalculator** after selected-delta reconciliation. Do not strip `[1m]`, add a duplicate catalog row, price Haiku, use SDK `costUSD`/whole total as price, infer TTL from SDK dollars, or show a subscription charge. Persist selected component costs/rate snapshot and a durable approximation flag; expose a narrow run-summary boolean through GraphQL/stream so Token Meter can label `Configured API-equivalent estimate — not a subscription charge` and, when needed, explain the assumed 1h cache-write rate. Historical rows are read unchanged, not repriced. Existing nullable checkpoint SQL migration and startup current-schema readiness gate remain; no new SQL migration for the price-source change.

**Reviewer focus:** (1) SR-011 approval and exact selected-model identity; (2) top-level/per-model **all-dimension reconciliation**, fallback and zero/missing-rate cases under I-41–I-43, without treating observed equality as universal; (3) terminal result versus non-final assistant-frame usage, canonical assistant versus raw result ID; (4) configured price lookup/cost calculator after transactional delta, no SDK USD or unselected-model monetary leakage; (5) durable/visible approximation quality through run/team/GraphQL/UI; (6) checkpoint, migration/readiness, historical continuity and no duplicate price owner/alias catalog. Full path/file map, sequence, removals, tests and risk rationale are in the SR-012 design extension. Implementation Engineer has uncommitted shared-worktree WIP based on SR-010 and was told to hold affected money display; this handoff is **not** permission to bypass renewed review.

## Route

`get_handoff_rules` returned the matching `Architecture Design Complete` condition for `task_size=Large` or `architectural_risk=High`; the exact sole recipient is **`/architecture_reviewer`**. The Small/Medium+Low direct implementation and Delivery Receipt Evidence Gap conditions do not match. Send this same absolute-path cumulative package with `send_message_to`. Do not directly substitute implementation. Reviewer owns onward handoff on Pass; source/API/E2E/delivery remain required.

After the user's further-probing request, `get_handoff_rules` was rechecked for the evidence-enhanced, otherwise unchanged SR-012 design (SR-013 evidence-only). It again selected **only `/architecture_reviewer`**; Solution Designer sent the updated package and did not directly forward to implementation. **ARCH-REV-008 Pass** is the resulting new review round and its renewed primary implementation handoff supersedes the temporary hold. ARCH-REV-007 remains the prior completed Pass issued before the hold, not a Pass of I-42–I-44. No further Solution Designer routing is required on this informational receipt.

## SR-014 current correction and requested next gate

The user tested the current local Linux Electron executable in Delivery's isolated profile and explicitly requested repair after the Solution Designer diagnosed the Claude SDK context display. The first Opus/Claude SDK `hello` screenshot showed `22,135 · context limit unavailable`; a separate Codex/Luna run on the same Token Meter UI showed `10,836 / 258,400` and `4.2%`. A read-only check of the current test DB confirmed the Claude record already stored prompt=22,135 and valid capacity=1,000,000 but null percentage; Codex stored prompt=10,836, capacity=258,400 and percent≈4.1935. The Claude result adapter extracts selected `contextWindow` but omits the percentage field; the panel requires it to enter its known-capacity branch. The 2 standard + 22,133 cache-write = 22,135 gross input breakdown is correct, and the user explicitly did **not** request changing its established wording. Exact evidence, source paths and screenshot references are in I-45, without raw prompt/secret content.

Approved SCN-010/BEH-010/REQ-011/AC-015 requires a positive SDK-reported selected context limit to display alongside the latest full prompt and a derived percentage; the observed case should show `22,135 / 1,000,000 ≈ 2.21%`. Missing/invalid/zero capacity stays unavailable; no model-name-based 1M guess. Existing stored records with valid prompt/capacity and null percent should display correctly without SQL rewrite. Codex and all selected-model token/cache/cost/price behavior remain unchanged. User approval is the explicit “I think we should fix it” response to that precise diagnosis. Product supplement: N/A — existing UI layout/copy is retained.

DS-018 design: compute validated percentage in `claude-session-token-usage.ts` for new events, then derive a missing percentage only from the same latest stored record's prompt/capacity in `token-usage-run-aggregate.ts` for historical reads. Existing event/fold, GraphQL/stream and Vue paths consume the same fields; no new DTO, frontend production edit, migration/backfill, price/catalog owner, or deployment config. Unit, stored-record, GraphQL/stream, Vue and packaged Electron validation cases are specified in the design. SR-014 alone is Small/Low, but the cumulative still-in-delivery package remains `task_size=Large`, `architectural_risk=High`; ARCH-REV-008 is basis-limited and renewed configured review is requested. Source/API-E2E/delivery gates and explicit user verification are pending. The current Electron executable is still defective; do not claim a fix is already implemented.

Use `get_handoff_rules` for this newly completed SR-014 result and notify only the matching exact recipient with this same absolute-path handoff file attached. Prior ARCH-REV-008 routing is history, not a substitute for this revision's route.

**SR-014 route decision:** `get_handoff_rules` returned the Large-or-High Architecture Design Complete rule as the single match for the cumulative package. Exact recipient: `/architecture_reviewer`. The Small/Medium+Low direct route and Delivery Receipt Evidence Gap rule do not match. This handoff requests renewed independent review of only the new SR-014 context correction against the cumulative approved package, with ARCH-REV-008 retained as prior monetary basis; reviewer owns any onward implementation handoff on Pass.
