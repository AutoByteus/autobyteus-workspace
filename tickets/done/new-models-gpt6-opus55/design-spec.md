# Design Specification — GPT-6 Sol/Luna, Claude Opus 5.5, and Claude SDK usage tracking

## Solution And Approval Basis

- Package/revision: `new-models-gpt6-opus55` / **SR-012**. SR-003–005 remain reviewed historical design for original model work. SR-007/008 are historical design for the former SR-006 whole-SDK mixed-accounting behavior. SR-010 is reviewed selected-model-only architecture on SR-009. The **SR-012 extension at the end** supersedes SR-010's SDK-dollar monetary source and adds configured-price cache-write handling under Approved SR-011; selected-model identity/token ownership from SR-010 remains.
- Requirements: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55/tickets/in-progress/new-models-gpt6-opus55/requirements-doc.md`, **Approved SR-011** (original SR-002, SR-009 selected-model-only Token Meter, and SR-011 configured-price estimate with delegated reasonable cache-write approximation). Exact user approvals are quoted there. No behavior-defining supplement; screenshots and sanitized probes are evidence only.
- Investigation: same directory `investigation-notes.md`, evidence **I-01–I-44**. I-42/43 are further live return-shape probes requested by the user; I-44 records a probe permission-guard limitation. ARCH-REV-006 **Pass** reviewed SR-010 on Approved SR-009. ARCH-REV-007 **Pass and initial handoff** reviewed SR-012 on Approved SR-011 **before the later hold request and I-42–I-44**; reviewer temporarily held affected implementation after the user requested more probing. **ARCH-REV-008 Pass** re-reviewed this evidence-expanded package and confirmed renewed primary implementation handoff, superseding that hold. No Product UI/UX spec applies.
- Workspace: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55`, branch `requirements/new-models-gpt6-opus55`, base `origin/personal@467c1bc12d439ee79243d124402c2f65f25c3cd2`, finalization target `origin/personal`.
- Current authority boundary (2026-09-23): SR-005/ARCH-REV-003 remains reviewed for original SR-002. ARCH-REV-006 passed SR-010 selected-model-only identity/token design on Approved SR-009. Approved SR-011 changes SDK dollar-source behavior to Autobyteus configured pricing. ARCH-REV-007 passed the initial SR-012 design **before** the hold; ARCH-REV-008 passed SR-012 with SR-013/I-42–I-44 evidence and renewed onward implementation routing. Prior source/API passes are basis-limited; no claim of new-scope validation.

## Current-State Read

At the pre-implementation baseline, Autobyteus direct API models came from `supported-model-definitions.ts`; the server exposed that factory catalog and used its pricing for new usage snapshots. OpenAI already used Responses and had Astra/long-context patterns. Anthropic direct requests used `@anthropic-ai/sdk` **Messages API** (`client.messages.create`), not Claude Agent SDK, but its stream reduced signed thinking to display text and tool-use deltas; `LlmPhase`/`MemoryManager` and the prompt renderer lost native signatures. SR-004 resolved the direct signed replay, normal-turn and compaction design gaps (ARCH-F-001/002), and IR-001 implemented those reviewed paths. Codex and Claude Agent SDK catalogs remain independently discovered at runtime (I-08/I-10). Both Anthropic SDKs were upgraded to exact stable pins in IR-001. This historical baseline is retained to explain the cumulative design; the only new SR-005 source change is catalog structure.

**Post-implementation source-structure finding:** IR-001 implemented the approved rows and reviewed runtime paths; CRR-001 confirmed those behavior traces but found that changed `supported-model-definitions.ts` is 538 effective nonempty lines versus a `>500` changed-source hard limit (I-21). The file combines local catalog pricing construction, Anthropic-specific schemas/rows and other providers. Existing `qwen-supported-model-definitions.ts` demonstrates provider-specific row grouping while the one exported `supportedModelDefinitions` aggregate remains LLMFactory's authority (I-22). This SR-005 delta moves Anthropic-owned payload and genuinely shared pricing construction; it does **not** reopen Opus 5.5 runtime, persistence or SDK design.

## Task Size And Architectural Risk (Mandatory)

- **Task size: Large.** The cumulative approved solution still crosses provider stream, shared response/agent, memory/compaction and SDK runtime boundaries. The **new SR-005 delta alone** is a small/low-risk catalog file extraction, not a fresh broad refactor; the cumulative package and re-review route remain Large.
- **Architectural risk: High.** Cumulative signed-provider/persistence/SDK risk remains and was independently confirmed by ARCH-REV-002. The new catalog split changes no runtime owner, public API, persistence or request semantics; its local risk is preserving exact order/metadata/prices and avoiding import cycles. Classification is not downgraded merely because this repair is bounded.
- Escalate on any new schema migration, public API change, SDK permission/event semantic break, live provider behavior inconsistent with documented payloads, or evidence that old snapshots cannot be consumed directly. Return Design Impact/Requirement Gap as appropriate; do not silently broaden scope.

## Architecture Investigation Evidence

| Evidence | Observation → decision | Remaining uncertainty |
| --- | --- | --- |
| I-01/I-04/I-05 | Exact GPT-6 IDs, metadata, tier pricing and established Responses adapter → add Sol/Luna rows and focused payload tests, reuse Astra path. | Live API entitlement without keys. |
| I-02/I-06/I-13–I-15 | Opus 5.5 requires adaptive thinking and signed block replay; current renderer loses it → provider-specific request policy plus ordered native assistant-turn preservation at the tool-cycle boundary. | No-key contract tests cannot prove live 200/400 behavior. |
| I-07/I-16 | Existing catalog pricing and immutable historical snapshots → use existing pricing shape, no historical repricing. | Production record volume not inspected. |
| I-08/I-10/I-17 | Codex/Claude SDK discover models dynamically → preserve discovery; validate local Codex IDs/turns rather than static Codex entries. | Local Codex account's advertised models. |
| I-11/I-12/I-17 | Exact current pins and no-key Claude test seams → refresh both pins after re-query, adapt only compilation/behavior deltas, run focused regressions. | Latest version may advance before implementation. |
| I-18–I-20; ARCH-F-001 | Official provider contract permits removal of **all** thinking blocks, but not a middle gap; dynamic tools/system/media make indefinite prefix preservation broader than approved tool-cycle need → actively end signed retention at the next independent turn, never reintroduce old blocks. | No direct-key enforcement test. |
| I-18/I-19; ARCH-F-002 | Client-authored keep-tail summary invalidates retained signed blocks; current compaction is reachable even before tool continuation → defer compaction inside active signed tool cycle and strip stale thinking at accepted client compaction before retained tail is sent. | No direct-key enforcement test. |
| I-21/I-22; CR-F-001 | Changed catalog file is 538 effective lines, above source-review 500 limit; Anthropic schemas/rows and shared pricing helper can be extracted while one aggregate remains authoritative → bounded provider-owned split and focused equivalence tests. | Implementation must verify effective-line counts and zero duplicate IDs/import cycles. |

## Intended Change

Add exact provider API model definitions and Standard pricing, make the existing Anthropic Messages path valid for Opus 5.5 including signed thinking/tool continuation, refresh both Anthropic SDK packages, and validate dynamic Codex GPT-6 with the local account. Preserve other provider IDs, model defaults, historical snapshots and SDK/runtime journeys. No new selectors/routes or static Codex/Claude SDK catalog entries. **SR-005 technical correction:** keep these exact catalog semantics but move Anthropic schemas/rows into a provider-owned module and shared pricing construction into one small helper so the changed aggregator stays below the source-review hard limit.

## Relevant Behavior And Production-Path Map (Mandatory)

| Behavior | Kind / requirement / trigger | Current evidence → approved outcome | Target path / spine |
| --- | --- | --- | --- |
| BEH-001 | User/System; REQ-001, AC-001/002; direct model selection | I-04/05: Astra present, Sol/Luna absent → exact Sol/Luna metadata and valid Responses tool/usage | Selector → catalog/factory → agent LLM → Responses; DS-001 |
| BEH-002 | User/System; REQ-002, AC-003/004; direct Opus 5.5 multi-turn/tool continuation | I-06/I-13–20: baseline row absent, signed thinking lost; SR-004/IR-001 provides valid active replay; CR-F-001 requires structural-only catalog extraction | Selector → one aggregate catalog (Anthropic provider sublist) → Anthropic Messages → stream → memory → turn-boundary/compaction validity owner → renderer → Messages; DS-002/003/008 |
| BEH-003 | System; REQ-003, AC-005; usage observed | I-07/I-16: price factory/snapshot → documented Standard rates/tier/cache, no fabricated trust | Usage → catalog price → calculator → snapshot; DS-004 |
| BEH-004 | User/Operational; REQ-004, AC-006; Codex advertised selection | I-08/10: dynamic model/list → same discovered ID in thread/turn and local live smoke | Codex discovery → selector → thread/turn; DS-005 |
| BEH-005 | Operational; REQ-005/006, AC-007/008; validation | I-03/09/17: no API keys, Codex auth present → deterministic tests, live Codex attempt, honest outcome | Test harness → provider mocks / Codex local; DS-006 |
| BEH-006 | Operational/System; REQ-007, AC-009/010; SDK maintenance | I-11/12: both pins stale → stable exact pins and preserved Agent SDK/direct API journeys | manifests/lock → Claude SDK client/session; DS-007 |

## Relevant Supplemental Task Artifacts

None authoritative. User screenshots are input evidence (absolute paths in investigation notes); no behavior-defining supplement or Product artifact.

## Task Design Health Assessment (Mandatory)

- Change posture: Feature + provider contract bug fix + dependency maintenance.
- Current design issue: **Yes**, for signed-thinking tool replay and lifecycle. Root cause: **Boundary Or Ownership Issue / Shared Structure Looseness**. The Anthropic adapter knows complete native blocks but reduces them before the agent/memory/renderer boundary; first design omitted the approved normal-turn and compaction lifecycle. Adding only a model row or immediate-turn replay would leave the supported agent path broken (ARCH-F-001/002).
- Refactor needed now: **Yes, bounded**. Carry one typed provider-native assistant turn as a response-level unit rather than scattering thinking onto individual tool calls. Memory owns a single immutable signed-retention window and one atomic stale-thinking invalidation operation used at independent-turn and accepted-compaction boundaries. The Anthropic adapter/renderer own capture/replay; generic response transport remains narrow. Do not refactor unrelated providers or Claude Agent SDK internals.
- SR-005 additional design-health finding: **File Placement Or Responsibility Drift** in the static catalog. CR-F-001 found the changed aggregate at 538 effective nonempty lines, over the established 500 limit. A bounded provider-owned extraction is required now: one Anthropic definitions module contains its rows/schemas, one genuinely shared pricing constructor serves it and the remaining aggregate, and `supported-model-definitions.ts` remains the sole authoritative assembled registry. This is not a second catalog owner and does not change factory behavior.
- Residual risk: no-key mocks cannot prove provider acceptance; local Codex entitlement may differ from screenshot; latest SDK may introduce an uncovered runtime change. These are validation disclosures, not reasons to assert success.

## Terminology

- **Native assistant turn:** The ordered, complete `text`/`thinking`/`redacted_thinking`/`tool_use` content blocks from one Anthropic Messages assistant response, including thinking signatures or redacted data. It is distinct from display `reasoning_content` and per-call tool context.
- **Tool continuation:** Sending that assistant turn back with matching user `tool_result` blocks; signed thinking blocks must be byte-for-byte unchanged.
- **Active signed-retention window:** Consecutive Anthropic tool-result continuations within one active agent tool cycle. On the first independent later turn, all prior `thinking`/`redacted_thinking` blocks are removed atomically from replayable native turns and never restored. This is an allowed all-block reset, not a middle gap. Text, tool-use/results and display reasoning remain.

## Legacy Removal Policy (Mandatory)

Policy: **No backward compatibility; remove legacy code paths.** Replace the current lossy Anthropic tool-bearing assistant reconstruction as the path for newly captured Opus 5.5 native turns. Do not keep a second Opus 5.5 strip/rebuild branch. Existing generic rendering for historical messages without native blocks and existing other Anthropic models remains a supported data path, not a new compatibility shim. An all-block reset retains native text/tool-use blocks but removes signed reasoning; it does not synthesize signatures or use a beta drop fallback. Do not introduce aliases, static Codex fallbacks or duplicate SDK wrappers.

## Persisted Data / State Transition Decision (Mandatory When Persisted Data May Be Affected)

- Stored subjects: run config model ID (string), immutable token-usage price snapshots, working-context snapshot v5 assistant messages with optional metadata/native tool-call context; disk snapshot files through `RunMemoryFileStore`. Approximate production volume not observed. Representative **code shape**, serializer and validators inspected (I-15/I-16); no private production records were opened.
- Delta: new catalog identifiers/prices; optional typed `provider_native_assistant_turn` metadata on newly captured Anthropic assistant tool turns. Existing generic `Message.metadata` is serialized/deserialized unchanged and may be absent. Native turn includes sensitive reasoning/signature material already represented in reasoning memory, so keep it within working-context persistence; no segment event/log/GraphQL projection of the raw block payload. At an independent-turn reset or accepted client compaction, a **new current snapshot** stores retained native turns without old thinking/redacted blocks; raw historical traces and display reasoning are not rewritten.
- Invariants: old model IDs still resolve; historical price snapshots remain fixed; old snapshots without this optional key still parse and finalize. Within an active tool cycle, signed native blocks round-trip exactly and tool results match. Before a later independent turn or after a client-authored summary, **all** earlier replayable signed blocks are removed together and never reintroduced; retained text/tool-use/results/provenance remain and no orphaned tool result arises. Never strip the current tool-use assistant thinking before its immediate tool-result continuation; defer compaction until the tool cycle has settled. Snapshot v5's version-agnostic metadata reader supports absence or stripped form of the key.
- Decision: **Directly Usable — No Migration.** Neither storage envelope nor required old-field semantics change; `metadata` is an optional object and serializer already preserves it. No bulk I/O, downtime or data rewrite is justified. Current runtime may atomically replace its working-context snapshot at the explicit turn/compaction boundary; that is normal state progression, not historical migration. Test old-snapshot read, signed snapshot resume, all-block reset, new round-trip, compaction and rollback/retry behavior. If implementation finds a normal reader that drops or corrupts this key, return Design Impact rather than adding ad-hoc dual reads.
- SR-005 catalog extraction introduces **no additional persisted-data shape or state transition**. The same exact model IDs, prices and metadata reach the same readers; historical snapshots remain untouched. Verify catalog equivalence rather than inventing a migration.
- `Migration Plan`: N/A. Existing records lack Opus 5.5 signed turns because the model was not registered; they need no synthetic signatures. Existing generic tool turns remain directly readable.

## Data-Flow Spine Inventory

| Spine | Scope | Behaviors | Start → End | Owner |
| --- | --- | --- | --- | --- |
| DS-001 | Primary end-to-end | BEH-001 | Model selection → Responses reply/usage | Direct API factory + OpenAI adapter |
| DS-002 | Primary end-to-end | BEH-002 | Opus selection → Anthropic reply/tool continuation | Anthropic adapter + agent loop/memory |
| DS-003 | Return-event | BEH-002 | Anthropic stream events → working context → next Messages request | Anthropic assembler/renderer across memory transport |
| DS-004 | Bounded local | BEH-003 | Usage observation → price snapshot | Token pricing owner |
| DS-005 | Primary end-to-end | BEH-004 | Codex model/list → local thread/turn | Codex catalog/runtime |
| DS-006 | Operational | BEH-005 | Tests → evidence report | Implementation/API-E2E roles |
| DS-007 | Operational | BEH-006 | Registry stable check → pinned SDK → Claude runtime | Dependency manifest + Claude SDK integration |
| DS-008 | Bounded local / lifecycle | BEH-002 | New independent turn or accepted client compaction → signed-history validity reset → next Anthropic request | MemoryManager + AcceptedCompactionBuilder |
| DS-009 | Bounded local / catalog assembly | BEH-001/002/003 | Provider-owned static row modules → sole aggregate list → LLMFactory/price lookup | Static model catalog |

## Primary Execution Spine(s)

- DS-001: Existing selector → server model catalog → `LLMFactory`/Sol-Luna definition → `OpenAILLM`/`OpenAIResponsesLLM` → Responses API → agent stream/usage.
- DS-002/003: Existing selector → factory/Opus 5.5 definition → `AnthropicLLM` Messages API → ordered native block assembler + tool delta stream → `LlmPhase` response aggregation → `MemoryManager` assistant/tool batch → working-context snapshot → `AnthropicPromptRenderer` native turn + tool results → immediate next `messages.create` within the active tool cycle. At a later independent turn, DS-008 runs before rendering.
- DS-008: New independent agent turn → memory-owned all-block signed reset (persisted before recovery checkpoint) → optional accepted keep-tail compaction with retained stale blocks stripped → append new input → renderer. Pending compaction is **deferred** during an active tool continuation; no client summary is inserted between assistant tool-use and its required signed continuation.
- DS-005: Codex `model/list` → normalizer/catalog/GraphQL selector → configured run → `thread/start` → `turn/start` → local authenticated completion.
- DS-007: exact dependency pins/lockfile → Claude SDK client/query and session/backend adapters → existing model/event/tool/interrupt behavior.
- DS-009: `anthropic-supported-model-definitions.ts` (rows/schemas) + existing other provider rows/Qwen sublist + one `supported-model-pricing.ts` constructor → ordered `supportedModelDefinitions` aggregate → unchanged LLMFactory lookup and server price projection. No second runtime registry or alias.

## Spine Narratives (Mandatory)

DS-001 adds catalog payload only; existing Responses request and usage handling remain the governing path. DS-002/003 captures complete ordered assistant content for a tool-bearing response and carries it once through the response/memory path; the renderer sends it unchanged with matching results during the active tool cycle. DS-008 closes that cycle at the next independent turn: it removes **every** earlier signed thinking/redacted block from replayable Anthropic turns in one persisted operation before the new request, even when a plain assistant reply occurred between tool cycles. This consciously trades earlier reasoning continuity for a valid non-beta Messages request under the provider's allowed all-block-removal rule; it does not lose text, tool-use/results or display reasoning. Accepted client-side compaction independently applies the same reset to its retained tail, and execution is deferred while a tool continuation needs the signed blocks. DS-004 uses current catalog price fields and calculator tier logic for new usage snapshots, without repricing old ones. DS-005 has no static model substitution; validation observes and invokes each local advertised GPT-6 ID. DS-006/007 separate no-key contracts from real local Codex evidence and check SDK compatibility with the existing runtime test seams. **DS-009 is SR-005's only new spine**: provider-owned static row payload is combined once by the existing aggregate, then the same LLMFactory/pricing readers consume it; no new runtime path.

## Spine Actors / Main-Line Nodes

Model catalog/factory; provider adapter; agent LLM phase; memory manager/working context; Anthropic prompt renderer; pricing calculator; Codex App Server client/thread; Claude SDK client/session. Existing selector/GraphQL is a thin presentation entry, not a new owner.

## Ownership Map

Catalog owns exact static API definitions, config schema, metadata and price. Its **one authoritative exported aggregate** owns row ordering and all LLMFactory reads; the Anthropic provider module owns only its row/schema declarations, and a shared pricing constructor owns the common catalog metadata wrapper. Anthropic adapter owns request restrictions and raw stream assembly; it must not persist state directly. `LlmPhase` owns response aggregation and identifies whether the next request is an immediate tool continuation or a new independent turn; it does not edit Anthropic blocks. `MemoryManager` owns durable assistant/tool protocol, snapshot and **atomic all-block reset** of stale provider-native reasoning at turn boundary. `AcceptedCompactionBuilder` owns the same reset for retained messages before committing a client-authored summary; `PendingCompactionExecutor`/assembler defer that compaction during an active tool continuation. Renderer owns translating retained native turn/tool results into Messages payload and must never reconstitute stripped thinking from display reasoning. Codex/Claude SDK clients own dynamic discovery and session transport. Pricing owner owns trust/tier calculation.

## Thin Entry Facades / Public Wrappers (If Applicable)

Existing GraphQL/model catalog and LLM factory are entry boundaries only for this change. Do not add a route or expose native thinking/signatures through these facades.

## Removal / Decommission Plan (Mandatory)

| Item | Replacement / action | Scope |
| --- | --- | --- |
| Lossy reconstruction of newly captured Anthropic signed tool turns from `reasoning_content`/tool calls | Exact ordered native assistant turn in Anthropic renderer | In this change |
| Silent pass-through of Opus 5.5 forced `tool_choice` and unsupported explicit thinking modes | Opus 5.5 request preflight validation | In this change |
| Indefinite replay of signed blocks after a new independent turn or client-authored summary | One memory-owned, persistent all-block reset at these explicit validity boundaries | In this change |
| Client keep-tail compaction inside an active signed tool continuation | Defer pending compaction until the cycle settles; preserve exact immediate tool replay | In this change |
| Anthropic rows/schemas and shared pricing constructor embedded in an over-limit aggregate file | Move Anthropic payload to `anthropic-supported-model-definitions.ts`; move shared constructor to `supported-model-pricing.ts`; replace inline Anthropic block with one ordered spread | In SR-005 rework |
| Old exact Anthropic SDK pins/lock entries | Re-resolved stable exact versions | In this change |
| Generic historical tool-call rendering with no native turn | Retain: required for directly usable historical data/other supported models | N/A, not obsolete |

## Return Or Event Spine(s) (If Applicable)

Anthropic streaming `content_block_start/delta/stop` and `message_delta/stop` are assembled in provider order. Continue emitting text/reasoning/tool deltas for existing UX/tool execution. Emit a single completion chunk only after all content blocks/signatures are complete, with usage and optional native turn. Interruption must not persist an incomplete signed native turn; existing partial display/error handling remains. A new independent turn resets all prior replayable native thinking before request assembly; an immediate tool continuation does not. The reset persists before the request recovery checkpoint so a failed request cannot restore removed blocks. Codex/Claude SDK event flows remain unchanged except adaptations required by exact upgraded SDK types.

## Bounded Local / Internal Spines (If Applicable)

- Anthropic request preflight: model policy → provider-extra/kwargs merge → reject Opus 5.5 forced choice (`any`/`tool`) and explicit disabled/manual-budget thinking; omit unsupported sampling → `messages.create`. Do not silently pretend an explicit invalid user setting worked. Existing models retain their current policy.
- Anthropic native block capture: initialize indexed block from `content_block_start`; append `text_delta`, `thinking_delta`, `signature_delta`, `input_json_delta` to the corresponding block; retain `redacted_thinking`; finalize at `content_block_stop`; validate complete ordered blocks against tool IDs before attaching to terminal response. Fail closed on incomplete signature/JSON rather than replaying a lossy approximation.
- Tool replay: read typed native turn from assistant message; validate model/provider block shape and tool-use IDs match `ToolCallPayload`; emit blocks unmodified (deep-clone only) then user tool results. Do not derive thinking from display reasoning or mutate its signature.
- Turn-boundary lifecycle: determine independent turn versus immediate tool continuation from the agent-turn/tool-protocol state, not merely a UI sender label; on the former, use one typed all-block reset over **all** retained Anthropic native turns in working context and persist it before rendering. Reset removes `thinking` and `redacted_thinking` blocks, not text/tool-use blocks, executable tool intents/results, display reasoning or provenance. It must not reinsert old thinking on retry, rollback, snapshot restore or later renderer calls. A plain non-tool assistant reply does not create a signed block under this deliberately turn-scoped strategy; no later signed turn can be separated from an earlier retained one by that reply because prior signed blocks are gone before the later independent turn.
- Compaction lifecycle: if a pending automatic compaction is reached during an immediate tool-result continuation, **defer** it without marking the pending request failed or consumed. On the next eligible independent turn, run reset before compaction; at the accepted-compaction boundary, apply the same reset to every retained assistant native turn before finalizing summary + keep-tail. Do not compact between a tool-use assistant response and its signed tool-result continuation. Validator asserts no pre-summary signed block survives, tool-use/results remain paired, and new post-summary signed blocks may be captured normally.

## Off-Spine Concerns Around The Spine

Pricing cache/tier policy stays in catalog/pricing owner. Snapshot validation/compaction and signed-retention reset stay in memory owner; LlmPhase supplies only the lifecycle fact (continuation vs independent) and the compaction executor honors it. Provider key resolution stays in existing secret resolver; mocks use fake client, no key inspection. Telemetry emits existing text/reasoning/usage events only, not raw native blocks. SDK package/peer resolution stays in manifests/lockfile, not runtime version branching.

## Ownership Boundaries

Provider-native assembly and validation belong to `autobyteus-ts/src/llm/api` plus a JSON-safe Anthropic native-turn structure under `llm/utils`; shared response types provide only an optional transport slot. Working context persists the typed payload and owns removal of its signed blocks only at explicit turn/compaction validity boundaries; it never edits a surviving signed block. Renderer is the only consumer that turns it back into Anthropic request blocks. Claude Agent SDK session path is separate from direct Messages path; it must not consume the static Opus row.

## Boundary Encapsulation Map

Caller → `LLMFactory` → provider adapter; agent loop → `MemoryManager` (not snapshot store directly); provider adapter → shared response transport → agent loop/memory → provider renderer. Request assembler invokes the memory boundary before an independent turn and consults the existing pending-compaction owner; accepted compaction transforms retained messages through the same typed reset helper. Server model catalog → factory; Claude backend → `ClaudeSdkClient`; Codex backend → App Server client. No caller above these owners reaches into their private storage/stream internals.

## Dependency Rules

No Anthropic SDK resource types in generic memory/response types; define a tight JSON-safe provider-native turn type/validator and pure `withoutThinkingBlocks` transform in `llm/utils`, with assembly in Anthropic API code. Both independent-turn and compaction paths call that single pure transform via the memory owner; no provider-SDK import in memory. No duplicated signatures or redundant parallel block arrays. Do not put provider request policy in catalog or generic agent loop. No dynamic runtime model IDs hard-coded into static API definitions. No unverified fallback to another model on Codex failure.

SR-005 catalog dependency rule: `supported-model-definitions.ts` may import `anthropic-supported-model-definitions.ts` and `supported-model-pricing.ts`, and the Anthropic module may import the pricing helper, but **the provider module and pricing helper must never import the aggregate**. `LLMFactory` and server readers continue to import only the aggregate. Do not duplicate a `pricing` wrapper or create a second independently queried model registry. Preserve original order and all exact row values; source extraction is not a pricing correction.

## Interface Boundary Mapping

| Boundary | Subject / identity | Contract |
| --- | --- | --- |
| Catalog/factory | Exact API model ID string | One definition per new ID, provider/class/schema/metadata/pricing. |
| Anthropic request policy | Exact selected model ID + explicit params | Valid Messages payload or clear local error. |
| Stream → response → memory | One assistant response/tool batch | Optional immutable ordered native assistant turn, distinct from display text and per-call context; retained signed blocks last only through active tool cycle. |
| Working context → renderer | Assistant message with matching tool-call IDs | Replay complete signed blocks then matching results; reject malformed new native payload. |
| Independent turn / compaction → memory | Working-context message sequence + lifecycle classification | Remove all stale native thinking/redacted blocks atomically, persist before rendering/summary commit; retain text/tool protocol. |
| Codex catalog/runtime | Discovered model ID and advertised effort/tier | Propagate same ID without alias. |
| SDK client | Agent SDK query/session ID/model descriptor | Preserve existing auth/discovery/session/event/tool contracts. |
| Static catalog aggregate → LLMFactory | Exact API model ID string | One ordered public list, including an Anthropic provider-owned sublist at the existing position; no consumer imports the sublist as an alternate registry. |

## Interface Boundary Check

No mixed agent/team ID or generic provider selector is introduced. Existing model ID remains an exact string at its owning catalog/runtime boundary. New response field is provider-discriminated, not a bag of unrelated optional provider attributes.

## Main Domain Subject Naming Check

Use `AnthropicAssistantTurn` (or equivalent) for the complete signed assistant response; do not call it `ToolCallContext`, because multiple ordered blocks belong to the turn, not one tool call. Keep `ToolCallPayload` for executable intents and `reasoning_content` for display text.

## Existing Capability / Subsystem Reuse Check

Reuse current factory, Responses adapter, pricing calculator, memory snapshot/finalizer, compaction planner/builder/executor, Codex catalog/runtime and Claude SDK client. No new model registry, API endpoint, migration owner or UI component. Introduce only a small Anthropic-native turn assembler if keeping it in `anthropic-llm.ts` would overload request/stream duties; share one JSON-safe native-turn shape/reset helper across provider, renderer and memory. For CR-F-001, reuse the existing Qwen provider-list pattern, not a new registry abstraction.

## Subsystem / Capability-Area Allocation

- `autobyteus-ts/llm`: static rows, Anthropic request/stream adapter and prompt renderer; shared response transport.
- SR-005 static catalog subdivision within `autobyteus-ts/llm`: aggregate retains sole public registry ownership; Anthropic definition file owns related rows/schemas; pricing helper owns only the reusable catalog wrapper.
- `autobyteus-ts/agent` + `memory`: pass/persist native turn at established aggregation boundary; classify tool continuation vs independent turn; atomically reset signed history at independent-turn and accepted-compaction boundaries; defer compaction during active tool continuation; validate snapshots.
- `autobyteus-server-ts/token-usage`: existing price projection and tests, no new calculator.
- `autobyteus-server-ts/runtime-management/claude` and Claude backend: only SDK compatibility adaptations.
- Codex runtime: no product code unless a live/discovery test proves a real defect.

## Draft File Responsibility Mapping

SR-004 initial mapping modified `supported-model-definitions.ts`, `anthropic-llm.ts`, `response-types.ts`, `llm-phase.ts`, `llm-request-assembler.ts`, `memory-manager.ts`, `anthropic-prompt-renderer.ts`, compaction files, manifests/lockfile and tests. **SR-005 rework inventory is deliberately smaller:** modify only the aggregate and focused catalog tests; add Anthropic provider definitions and shared pricing-helper modules. Do not reopen the signed-turn lifecycle source without a separately evidenced finding.

## Reusable Owned Structures Check

The same complete native turn crosses three boundaries; define one narrow `provider-native-assistant-turn` type with Anthropic variant, ordered JSON-safe blocks, validator and pure all-block removal. It is response-level, not repeated on each tool call. The assembler is Anthropic-specific, not a generic provider framework. The reset helper operates on one typed turn; memory maps it over the complete retained message sequence, avoiding duplicated policy.

SR-005 catalog reuse is narrower: extract only the already-shared USD/source/effective-date `pricing` constructor because both the aggregate and Anthropic provider rows need identical defaults. Anthropic schemas are provider-owned, not promoted to generic shared configuration.

## Shared Structure / Data Model Tightness Check

Keep display `reasoning` separate from replayable signed `thinking`; never regenerate signature from text. All-block removal changes only the replayable native turn; display reasoning remains historical evidence and must never be turned back into provider thinking. Existing per-call `nativeToolCallContext` still records tool-use evidence but is not the authoritative full turn for Opus 5.5. Avoid storing duplicate native turns on both each call and message. No broad optional fields for other providers.

The SR-005 provider sublist contains only Anthropic rows and its three relevant schemas; it is not a second mixed-provider catalog. The pricing helper carries **no** model IDs, rate table or provider-specific tier rule. One aggregate owns global order and uniqueness.

## Final File Responsibility Mapping

| Change | File(s) | Final responsibility |
| --- | --- | --- |
| Modify (SR-005 correction) | `autobyteus-ts/src/llm/supported-model-definitions.ts` | Sole ordered export `supportedModelDefinitions`; preserve other provider rows and spread Anthropic sublist once at its original position. Import shared pricing wrapper; remove moved inline Anthropic schemas/rows and local wrapper. Target **well below 500 effective lines**, not an exactly-500 brittle result. |
| Add (SR-005) | `autobyteus-ts/src/llm/anthropic-supported-model-definitions.ts` | All existing Anthropic provider rows plus `claudeSchema`, `claudeAdaptiveThinkingSchema`, `claudeOpus55Schema`; exact IDs, order, dates, metadata, config/prices unchanged. Export one typed provider row array only; no factory/registry lookup. |
| Add (SR-005) | `autobyteus-ts/src/llm/supported-model-pricing.ts` | Single shared `pricing` constructor with the existing USD/source/effective-date defaults and override semantics; imported by aggregate and Anthropic sublist. No provider-specific rate policy or duplicate wrapper. |
| Add | `autobyteus-ts/src/llm/api/anthropic-assistant-turn-assembler.ts` (or equivalent Anthropic-owned file) | Ordered stream block assembly and completion validation; no memory mutation. |
| Add | `autobyteus-ts/src/llm/utils/provider-native-assistant-turn.ts` | Tight JSON-safe Anthropic turn type/validator and pure all-block thinking removal; no SDK import or generic provider machinery. |
| Modify | `autobyteus-ts/src/llm/api/anthropic-llm.ts` | Opus 5.5 request preflight and stream capture/terminal emission. |
| Modify | `autobyteus-ts/src/llm/utils/response-types.ts`, `autobyteus-ts/src/agent/loop/llm-phase.ts` | Optional provider-discriminated transport of one completed native turn. |
| Modify | `autobyteus-ts/src/memory/memory-manager.ts`, `autobyteus-ts/src/llm/prompt-renderers/anthropic-prompt-renderer.ts` | Persist once on tool-bearing assistant message; expose memory-owned atomic signed reset; replay exact surviving blocks with results. |
| Modify | `autobyteus-ts/src/agent/llm-request-assembler.ts`, `autobyteus-ts/src/agent/loop/llm-phase.ts` | Classify independent request vs immediate tool continuation; reset before new-turn render/recovery checkpoint; defer compaction for continuation. |
| Modify | `autobyteus-ts/src/memory/compaction/accepted-compaction-builder.ts`, `pending-compaction-executor.ts`, `working-context-compaction-output-validator.ts` | Strip stale signed blocks from all retained turns before client summary commit; keep pending compaction unconsumed during tool continuation; validate no pre-summary thinking survives and tool protocol remains complete. |
| Modify | `autobyteus-ts/package.json`, `autobyteus-server-ts/package.json`, `pnpm-lock.yaml` | Exact current stable Anthropic SDK pins/resolution. |
| Modify only on proven break | `autobyteus-server-ts/src/runtime-management/claude/**`, `src/agent-execution/backends/claude/**` | Adapt SDK type/event/session/tool differences while preserving behavior. |
| Add/modify tests | Existing `autobyteus-ts/tests/unit/llm/**`, `tests/unit/memory/**`, `autobyteus-server-ts/tests/unit/runtime-management/claude/**`, `tests/unit/agent-execution/backends/claude/**`, pricing/Codex test locations | No-key contracts, snapshot continuity, SDK regression, live Codex evidence. |

## Applied Patterns (If Any)

Existing adapter, factory and repository patterns only. Anthropic block assembler is an adapter-internal state machine keyed by content-block index; no new generic pattern layer.

## Target Subsystem / Folder / File Mapping

Paths above keep files under existing owners. Stream assembly remains under `llm/api`, while the JSON-safe native-turn shape/reset helper lives in `llm/utils` because response transport, memory compaction and renderer share it without importing SDK types. Do not create a general `common` folder. Compaction changes stay in existing memory files rather than a new subsystem.

SR-005 places provider catalog payload beside existing `qwen-supported-model-definitions.ts` under `llm`, not under `llm/api` (runtime adapter) or a new registry folder. The small `supported-model-pricing.ts` belongs beside catalog definitions because its USD/source/date wrapper is shared by the aggregate and Anthropic sublist. This placement preserves the flat existing catalog layout while removing the over-limit monolith.

## Folder Boundary Check

No new top-level subsystem. `llm/api` owns provider contract, `llm/prompt-renderers` owns outbound provider format, `memory` owns persistence and compaction validity, server Claude SDK area owns dynamic runtime. The small shared response field is transport only; no Anthropic API imports in generic folders. The native-turn helper in `llm/utils` is deliberately provider-discriminated and JSON-safe, not a vague shared policy container.

The SR-005 split follows provider subject depth: one provider row/schema file and one genuinely cross-provider pricing constructor. It does not arbitrarily split each model into a file or introduce a new folder. `supported-model-definitions.ts` remains the only assembled catalog and the sole import used by `LLMFactory`; import-direction tests/build should catch a cycle.

## Concrete Examples / Shape Guidance (Mandatory When Needed)

Good immediate tool cycle: `[{type:'thinking',thinking:'…',signature:'signed…'}, {type:'text',text:'…'}, {type:'tool_use',id:'toolu_1',name:'search',input:{q:'x'}}]` is stored once and replayed unchanged as assistant, followed by `{type:'tool_result',tool_use_id:'toolu_1',content:'…'}` as user. Do not surface actual signatures in test logs; fixtures use synthetic values.

Good multi-turn sequence for ARCH-F-001: turn A has signed tool call A/result A; before independent turn B, memory removes **all** turn-A thinking blocks but keeps A text/tool protocol. Turn B returns a plain assistant text answer, with no native signed block to replay. Before independent turn C, no earlier signed block exists; C's signed tool call C/result C is replayed intact. Thus there is no retained A → missing B → C signed-block gap. Bad: keep A signed block, omit B's thinking, then send C's signed block.

Good keep-tail compaction for ARCH-F-002: original prefix + retained `assistant[{thinking:S1,text:T1,tool_use:U1}], user[{tool_result:R1}]` becomes compacted-summary user message + retained `assistant[{text:T1,tool_use:U1}], user[{tool_result:R1}]`; S1 is removed in the committed current snapshot and never restored. A new post-summary tool cycle gets a fresh signed block. If U1 has just been produced and R1 needs immediate continuation, defer compaction instead and first replay S1 unchanged with R1. Bad: client summary plus retained S1 bound to the old prefix.

Good SR-005 catalog shape: `supportedModelDefinitions = [ ...existing earlier rows, ...anthropicSupportedModelDefinitions, ...existing later rows ]` with provider rows declared once in the Anthropic module, while both modules call the same `pricing` wrapper from `supported-model-pricing.ts`. Bad: two exported full catalog arrays or importing `supported-model-definitions.ts` from the provider module, which creates a circular/competing authority.

## Backward-Compatibility Rejection Log (Mandatory)

Reject model aliases, Codex static fallbacks, silent Opus 5.5 invalid-setting coercion, beta `drop_block`/retry as a hidden compaction fallback, and an old/new snapshot migration branch. The optional metadata field is directly absent on old data, not a parallel schema. Retain existing generic rendering for historical assistant tool turns because those are still valid supported records; new signed Opus 5.5 turns must use the exact native path during their active cycle. Once all signed blocks are intentionally removed, renderer must not re-create them from display text. SR-005 also rejects parallel catalog exports, a circular pricing import or duplicate Anthropic rows kept inline as a transition.

## Derived Layering (If Useful)

N/A — spine/owner boundaries above are clearer than a new layer diagram.

## Change / Refactor Sequence

1. Recheck official docs and npm stable dist-tags at implementation time; **first** pin exact both Anthropic packages and update lockfile, install in the isolated worktree, inspect the updated SDK Messages stream/content-block types and Claude Agent SDK query/session/tool contracts, then compile and run focused pre-adapter tests to surface concrete adaptations. Do not accept prerelease/floating pins or infer that a package upgrade alone repairs application-level thinking replay.
2. Add catalog metadata, schemas and Standard rates for Sol/Luna/Opus 5.5; unit-test exact IDs, default/effort, tier/cache and older rows.
3. Implement Opus 5.5 request preflight and ordered native block assembler; preserve existing send/stream usage and tool delta behavior. Test valid/invalid payloads with fake Messages client.
4. Add optional response transport; thread through `LlmPhase` and `MemoryManager` into one typed assistant message payload. Render exact signed blocks plus matching results **only inside the active tool cycle**; reject malformed/incomplete payload rather than silently stripping current-cycle thinking. Test multiple tool calls/continuations, interruption and resume.
5. Implement one JSON-safe `withoutThinkingBlocks` helper and memory-owned atomic reset at independent-turn boundary **before** recovery snapshot; classify by tool-cycle/turn state, not merely sender. Add compaction deferral for active continuation and accepted-compaction retained-tail reset/validator. Test A signed tool → B plain → C signed tool, retry/restart non-reintroduction, protected tool protocol, automatic pending-compaction timing, summary + retained signed turn, post-summary fresh thinking, old/new snapshot round-trips.
6. Adapt Agent SDK integration only to actual build/test findings; run focused no-key Claude direct/Agent SDK and Autobyteus-runtime regressions, then broader build/tests.
7. API/E2E Engineer observes local Codex `model/list`, attempts a minimal authenticated turn for every advertised Astra/Sol/Luna, and reports each exact outcome. Direct provider live tests remain Not Run without keys; no fake success. Delivery owns docs/final verification/finalization.

**SR-005 recovery sequence (overrides only catalog source allocation above):** (a) from IR-001 commit `704e2108e`, move the existing Anthropic schemas and all Anthropic rows verbatim into a typed provider-definition module; (b) move the reusable `pricing` wrapper unchanged into a catalog-pricing helper and import it from aggregate/provider module; (c) replace the exact contiguous Anthropic block at its original aggregate position with one spread; (d) build and test exact catalog order/identity, Sol/Luna/Opus metadata, reasoning schemas, Standard rates, tier/cache and pricing trust, plus old rows; (e) audit every changed implementation-source file with the reviewer effective-line rule, keeping aggregate safely below 500 and each new file below 500, with no `>220` changed-source delta; (f) send reworked implementation through independent source review before API/E2E. No direct-provider/Codex validation bypass is authorized by this structural correction.

## Key Tradeoffs

One typed native turn in working-context metadata avoids a new snapshot schema/migration and keeps generic memory mostly provider-agnostic, at cost of retaining signed thinking inside private context storage during the active tool cycle. The revised **turn-scoped** retention deliberately discards earlier replayable reasoning before the next independent turn, as the official contract permits, rather than implementing indefinite append-only binding across dynamic tool/system/media changes. This does not discard display reasoning or text/tool history and is consistent with the pre-change product, which never preserved native signed thinking across turns. Client-authored compaction strips stale thinking, not tool protocol; active-cycle compaction is deferred instead of silently weakening the immediate replay contract. No beta header/new provider feature. A scoped shared response field is preferable to hiding native data in each tool delta. No static Codex changes because account discovery is authoritative.

## Risks

- Provider may reject a payload despite doc-based mocks: live API keys unavailable; report unverified status.
- Native block assembly can miss `signature_delta`/redacted or emit before block completion: fail-closed tests for fragmented/multi-tool streams and message-stop ordering.
- A lifecycle classifier could mistakenly reset during a tool continuation or retain thinking into a new turn: explicit turn/tool-protocol state, persisted reset before recovery checkpoint and request-sequence tests. If the implementation cannot prove the classifier from existing state, return Design Impact rather than guessing.
- Client compaction may commit a summary with stale signed blocks or consume its pending request before tool continuation: defer without status transition, reset all retained blocks at accepted boundary and validate the finalized request shape.
- Persisted sensitive thinking/signature may leak via generic metadata projections: test/inspect outward serialization and redact/omit native field from events/logs; keep only private snapshot use.
- SDK 0.3.280/0.128.0 or later stable may change query/session/event/permission semantics: exact pin and focused regressions, no silent old-version retention.
- Local Codex may not advertise all three GPT-6 IDs despite screenshot from another host; report per-model blocker rather than substitute.
- SR-005 extraction could change row order, pricing defaults, config-schema references or introduce a catalog import cycle; compare catalog projections before/after the move, compile, assert exact rates/IDs and source effective-line counts. Do not “fix” unrelated catalog content while moving it.

## Guidance For Implementation

Honor approved AC-001–010 and no-key constraint. Treat surviving signed provider-native assistant content as immutable; validate complete block order and tool IDs before persist/replay. Use the documented all-block removal rule at explicit validity boundaries only; never drop a current tool-turn thinking block before its matching tool-result continuation, never remove one middle block and keep later blocks, and never reintroduce removed blocks on retry/resume. **For CR-F-001, change only the catalog file responsibilities described in SR-005 and retain the already reviewed runtime implementation unless a new evidenced finding requires more.** Do not run direct live API tests or print secrets. The user supplied `$HOME/.autobyteus/server-data/.env` as a possible later credential source; do not read/print it here, and carry it to API/E2E only after source-review pass for secret-safe validation. Follow repository `AGENTS.md` test/commit rules. If lifecycle classification cannot be proven or a required stored-data transition emerges, return a classified finding to Solution Designer rather than changing approved behavior. Renewed independent architecture review of SR-005 precedes implementation rework; renewed independent source review precedes API/E2E.

## SR-007 extension — Claude Agent SDK Token Meter (current design)

### Authority, evidence and design-health decision

This extension realizes **approved SR-006 SCN-007–009 / BEH-007–009 / REQ-008–010 / AC-011–014**. It does not change direct Messages API pricing/turns, Codex, dynamic Claude SDK model discovery, or the SR-005 catalog split. The original section above describing SDK accounting/display as out of scope is superseded for this extension. API-REV-004 is a diagnostic Requirement Gap, not a failed test of old AC-001–010. Earlier ARCH-REV-003, source and API passes do not cover SR-007.

Experiments I-27–I-31 are the decision basis. Explicit `opus[1m]` selection produced Haiku and `claude-opus-5-5[1m]` entries; no top-level result model. Changing map order alone changed our emitted single model. A real create→resume query proved `modelUsage` is cumulative across turns, may retain unchanged models with absent optional metadata, while top-level `usage` is latest main-loop usage. Current event→price→fold→summary reproduced the user's wrong Haiku/`model_not_found` Token Meter. The SDK does not disclose why Haiku ran, so the design must not infer it. Its per-model `costUSD` is an estimate, not a subscription invoice. This is an **event/accounting ownership defect**, not a missing static `claude-opus-5-5[1m]` catalog row or frontend string bug. A UI-only rename, stripping `[1m]`, pricing the first model, or summing every cumulative result would each preserve a false ledger.

The proportionate refactor is one Claude-SDK-specific result envelope and one transactional per-model cumulative fold, reusing the existing token-usage ledger/projections. No parallel full accounting system, new model registry, or generic provider abstraction. Keep implementation modules within source-review size limits; separate parsing, delta/state and projection responsibilities instead of growing the existing large fold or catalog file.

### Behavior-to-spine map and lifecycle

| Spine | Approved behavior | Production start → end | Boundary/owner |
| --- | --- | --- | --- |
| DS-010 | BEH-007/008 | Claude SDK terminal `result` → one sanitized `TOKEN_USAGE_UPDATED` observation | Claude session adapter parses **all** `modelUsage` entries; selected alias is request context only. |
| DS-011 | BEH-007/008 | Observation → enrichment → per-run transactional fold → authoritative contribution | Token-usage domain validates source; fold owns cumulative USD/token checkpoints, dedupe, status and identity. Pricing policy owner has an SDK-reported-total branch; static catalog is not consulted for SDK variants. |
| DS-012 | BEH-007/008/009 | Run record/analytics → GraphQL and stream summaries → Token Meter | One result is one ledger/report contribution; run summary retains per-model detail and labeled SDK-estimated USD. Existing direct-API presentation stays unchanged. |
| DS-013 | BEH-009 | Existing SQL records → current reader/summary | Additive nullable state; old rows remain readable and are not rewritten/repriced. |

**Observation contract.** Emit one result-level event keyed by run/session/turn and SDK result UUID when supplied (otherwise stable run/session/turn identity), never by model-map order or token amounts. Carry the actual `ClaudeProviderSessionLifecycle.buildNextQueryBinding()` `create|resume` kind into the result observation; do not infer fresh session from first local event. Carry a bounded, typed, numeric-only `claude_sdk_model_usage` array sorted by raw ID: `rawModelId`, optional SDK-provided `canonicalModel`, `provider`, `costBasis` (`list|managed|unknown`), nonnegative safe-integer input/output/cache-read/cache-create totals, finite nonnegative `costUSD`, and source session/result identity. Preserve raw IDs exactly; the canonical ID is a display/provenance field, not an alias rewrite or catalog key. Reject duplicate raw IDs, malformed/oversized maps and impossible numbers with explicit quality status, not truncation or first-key fallback. Do not persist SDK response text, signatures, secrets or the unsanitized terminal result in this usage event. Top-level `usage` remains **only** latest main-loop context/prompt information; it is not added to per-model totals or assigned to one model. If `modelUsage` is absent, preserve a truthful unpriced/unknown observation rather than fabricating per-model cost from top-level usage.

**Transactional cumulative fold.** Per run and SDK session, use `(provider, rawModelId)` as a source-series key; store last cumulative numeric tokens, USD and last known canonical/basis metadata. On a proven new zero-origin session, the first reported cumulative values are its initial contribution. On resume with an existing checkpoint, subtract each field from its own preceding value; unchanged entries contribute zero even if optional canonical/basis disappear. Optional metadata is sticky for the same raw series **only as a previously observed SDK fact**, never inferred from `[1m]`; changed metadata updates provenance without inventing usage. A new model appearing in a known session contributes its first cumulative values. If a pre-change run resumes without a usable checkpoint, baseline the first result at zero new contribution and mark that interval unaccounted/partial; do not add possibly historical cumulative values or revise old rows. A new session/explicit reset gets a new series only when the lifecycle proves a fresh zero origin; otherwise baseline conservatively. A regressed field, non-finite cost, missing basis on a positive-cost delta with no prior known basis, or inconsistent model/SDK total **never** yields negative or falsely complete cost: suppress/rebaseline the affected series and flag partial/missing. Maintain the other valid model deltas where possible. Deduplicate by stable observation ID **inside** the same transaction as checkpoint and run-total update, so retry/restart cannot count twice. Replayed identical cumulative values yield zero additional usage. SDK total-cost reconciliation is a sanity check against the sum of reported per-model cumulative costs with currency tolerance; it is not an additional charge. Do not assume map iteration order or selected alias determines actual usage.

**Accounting and display semantics.** For each valid positive model delta, gross input is SDK base input plus cache-read plus cache-create, output is SDK output; total and USD are sums of per-model deltas. Preserve cache components without inventing 5m/1h splits or unit prices. Keep `latest_prompt_tokens`/context from top-level main-loop `usage` separately. One canonical model may be the primary label only when the contributing result has one actual model; use SDK `canonicalModel` if present, else raw ID. With two or more actual reported models, primary label is localized **Multiple models**, and detail rows show canonical label, exact raw ID, token components, SDK cost estimate and basis for each. Unchanged cumulative entries may remain visible as historical per-model totals, but do not turn a new zero-delta result into new Haiku usage. An aggregate/team summary with more than one distinct model also presents mixed identity, not an arbitrary latest/first model. No fake model identifier `Multiple models` is passed into catalog/pricing; use nullable machine ID plus separate display state and structured details.

Extend the cost/provenance contract with an explicit `sdk_reported_estimate` source/basis and SDK-specific complete/partial status, distinct from `api_price_estimate` and `local_no_api_bill`. `list` and `managed` basis remain visible in model detail; `unknown`/invalid is not trusted. A complete SDK result may set **total** estimated USD and currency USD only; per-component USD/unit-price fields remain null. Mixed old missing and new SDK-estimated contributions show the known estimated amount as **partial**, not complete or zero; old row values remain recorded. The UI must say **SDK-estimated API-equivalent usage cost — not a subscription bill** (with `managed` basis distinctly labeled) and must not show direct-API Standard unit rates for an SDK result. Retain direct Messages API `claude-opus-5-5` static catalog row and exact pricing for direct requests; do not add a static `[1m]` row or change Claude SDK dynamic discovery.

### Owners, persistence and files

| Change | File owner / likely files | Responsibility |
| --- | --- | --- |
| Replace first-key collapse | `autobyteus-server-ts/src/agent-execution/backends/claude/session/claude-session-token-usage.ts`, `claude-session.ts`, `claude-provider-session-lifecycle.ts` (read-only contract); small adjacent SDK result parser | One sanitized, deterministic all-model envelope; pass actual create/resume binding into result, top-level usage context only, stable result identity. Remove `resolveModelUsageRecord` first-key fallback and misleading `claude_usage_model_usage_mismatch` comparison between main-loop and pipeline totals. |
| Extend transport | `src/agent-execution/domain/agent-run-token-usage.ts`, Claude session event converter, `src/token-usage/transformers/token-usage-event-enrichment-transformer.ts` (actual names/paths per repository) | Strict optional SDK array and source/basis/status fields; direct/Codex event shapes unchanged. Validate, never use unsanitized `raw_event_json` for cost authority. |
| Add one delta owner | Small `src/token-usage/projections/claude-sdk-model-usage-reconciler.ts`, called from `token-usage-run-fold.ts`; `token-usage-run-record-state.ts`; `token-cost-calculator.ts`/policy resolver | Per-model session checkpoints, dedupe and USD delta; existing fold remains sole transaction authority and applies one authoritative aggregate contribution. SDK branch bypasses static catalog but preserves common ledger totals. |
| Persist state additively | `src/token-usage/domain/token-usage-run-record.ts`, SQL codec/repository, `autobyteus-server-ts/prisma/schema.prisma` and migration | Nullable `claude_sdk_usage_state_json` on `TokenUsageRunRecord` containing versioned per-session/per-raw checkpoints and accumulated per-model detail/provenance. Old null means no checkpoint, not zero. No bulk backfill. Existing `snapshot_series_state_json` stays for Codex/generic token snapshots; do not overload its bounded token-only state with USD/provenance. Bound and validate state size; on capacity exhaustion mark subsequent estimates partial rather than silently evicting a live cost checkpoint. |
| Project/report | `src/token-usage/projections/token-usage-run-aggregate.ts`, pricing/analytics summary and contribution, GraphQL `token-usage-stats.ts` and stream payload, web Token Meter types/`TokenUsageMeterPanel.vue`/locale copy | One report per unique SDK result, one aggregate daily facet (not duplicated per model), structured per-model detail on run summary, mixed label and SDK estimate disclaimer. Daily model filtering cannot safely attribute a mixed aggregate to a single model; retain an explicit mixed/unattributed aggregate facet rather than Haiku or Opus full attribution. Any future per-model daily drill-down requires separate balanced facets, not this change. |
| Tests | Claude session parser tests; pricing/fold/projection tests; SQLite run-store migration/read tests; GraphQL/stream tests; `TokenUsageMeterPanel.spec.ts` | Exact AC-011–014 and regression coverage below. |

The SQL column is an **additive current-ledger migration**: nullable field with no default data rewrite, deployment migration before new writer, old rows decode with null SDK state. Historical snapshots, price decisions and identities are immutable. A legacy run receiving a new SDK result must not pretend its prior cumulative source is zero. Test both a clean new run and an old record with null state. Querying old records remains direct; no dual catalog/pricing registry or historical backfill. Approximate real record volume remains unknown, but the additive nullable operation does not require scanning/repricing rows. Verify actual SQLite migration and rollback/read behavior during implementation.

### Sequencing, verification and risks

1. Introduce validated numeric-only SDK result shape and stable ID; add no-key mixed-order and malformed/unknown-cost fixtures before replacing the adapter. Keep selected model solely as request metadata. Confirm one event per result and no response/secret leakage.
2. Add nullable versioned state/migration and isolated reconciler; integrate it with the existing transactional fold and SDK cost-policy branch. Prove clean first result, create→resume cumulative delta, unchanged Haiku with missing optional metadata, a new model, duplicate result, reset, regressed fields, partial cost, total reconciliation, old-null-state continuation and SafeInt/size limits. In a fixture matching I-30, the second result adds approximately **$0.0057032**, not the whole **$0.1417102**; unchanged Haiku adds zero.
3. Project per-model details and complete/partial source labels through run summary, analytics, GraphQL/stream and Vue. Test `Multiple models` regardless of input key order, exact raw/canonical detail, no invented unit rate, known partial amount on old+new runs, and unchanged direct Opus/Codex behavior. Ensure one result does not inflate report count/daily totals by number of models.
4. After independent architecture/source review, API/E2E validates no-key event→SQLite→GraphQL/Token Meter and one **cost-limited** locally authenticated SDK create→resume (if auth remains available), observing model list/result without assuming internal routing. Browser/web-equivalent Token Meter validation is preferred; no direct paid Anthropic API call or provider key output. Delivery repeats explicit user verification; prior delivery smoke is not final acceptance.

**Risk and mitigation:** An SDK query can invoke internal models and omit optional metadata on an unchanged resumed entry; per-raw checkpoints with sticky observed metadata and zero-delta handling prevent wrong attribution. First result on a legacy resume and unknown reset origin cannot be priced confidently, so baseline/partial is deliberate. Floating-point SDK cost differences need a documented epsilon and finite-number guard; never force a discrepancy to zero or back-solve rates. Persisted state can grow with long sessions; bound it and flag degraded tracking instead of evicting active checkpoints silently. Run/team analytics and frontend need a separate display identity so `Multiple models` is never treated as a catalog model. Source review must check the new shared-contract changes and file-size limits. No provider invoice is available; all USD claims stay labeled estimates.

**Classification:** cumulative `task_size=Large`, `architectural_risk=High`. SR-007 adds a cross-session/cumulative persisted monetary ledger, shared event/GraphQL contract, daily projection and Token Meter semantics; a wrong boundary can double-charge, misattribute model usage or silently reprice old records. This is the escalation trigger for **renewed independent architecture review** on approved SR-006, then implementation, source review, API/E2E and delivery under configured rules. Prior ARCH-REV-003 Pass remains valid only for SR-005/approved SR-002.

## SR-008 recovery — ARCH-F-003/004 (current design)

ARCH-REV-004 found two **Design Impact** omissions in SR-007, not an approved-behavior change. Approved SR-006/AC-011–014 and all other SR-007 choices remain fixed. This section completes the normal product path and the physical-schema transition; it supersedes any SR-007 suggestion that a null machine ID or a Prisma migration alone suffices. I-33/I-34 and the ARCH-REV-004 report are the evidence. Classification stays **Large/High** and renewed independent review is required before implementation.

### One primary product spine and identity ownership (ARCH-F-003)

**DS-014 — BEH-007/008 primary end-to-end spine:** user selects SDK alias `opus[1m]` → `ClaudeSession` opens a create/resume query → SDK terminal result reports raw/canonical per-model `modelUsage` → Claude SDK parser emits one sanitized result observation → `claude-session-event-converter.ts` emits `TOKEN_USAGE_UPDATED` → `createTokenUsageUpdatedPayload` validates a typed SDK identity kind → `TokenUsageEventEnrichmentTransformer` calls `TokenUsageContextEnricher` → component/delta/cost enrichment → transactional run fold/reconciler → run record and one analytics facet → GraphQL/stream summary → Token Meter primary label, model details and SDK estimate. The configured `opus[1m]` stays **request context only**; the SDK's actual per-model entries govern identity and accounting. DS-010–012 are subordinate segments of this spine, not alternate routes.

Use an explicit validated `sdk_model_attribution` discriminator on **new** Claude SDK result observations: `single`, `multiple`, or `unknown`. It is accepted only with `runtime_kind=claude_agent_sdk`, `ingestion_kind=claude_sdk_result`, and the sanitized per-model envelope. Parser derives `single` from one valid actual model, `multiple` from two or more, `unknown` from no trustworthy model set. For `single`, `model_identifier` is the SDK-provided canonical ID when present, otherwise the exact raw ID; the selected alias is never a substitute. For `multiple` and `unknown`, machine `model_identifier=null`. A separate display attribution state and per-model details flow to the run summary; Vue localizes `Multiple models` only for `multiple`, and `Unknown` for `unknown`. Neither text is a catalog model identifier. New SDK observations with a missing/invalid discriminator are treated as **unknown/partial**, not eligible for selected-config fallback or trusted cost. The field is a small SDK-specific transport contract, not a general identity framework. Historical rows are read as recorded and are not replayed through the new parser.

`TokenUsageContextEnricher` currently applies `payload.model_identifier ?? config.llmModelIdentifier` unconditionally. Change that precise fallback: preserve the run-config fallback for existing non-SDK event types, but **pass through** intentional `null` for validated SDK `multiple`/`unknown`; pass through the actual SDK `single` identity. Do not normalize `model_value` or later summary projections back to the selected alias for `multiple`/`unknown`. Keep `config.llmModelIdentifier` in run configuration for selected/requested-model context only. Validate attribution in `createTokenUsageUpdatedPayload` and recheck it at the context boundary; no caller may spoof a generic event into the SDK null pass-through. Existing non-SDK events retain their prior fallback semantics. Remove the conflicting selected-config fallback **for new Claude SDK result observations**, not for all token-usage events.

Concrete mixed example: selected `opus[1m]`; SDK entries `claude-haiku-4-5-20251001` and `claude-opus-5-5[1m]`, latter with canonical `claude-opus-5-5`. The new observation carries `sdk_model_attribution='multiple'`, `model_identifier=null`, sorted per-model details. After parser, context enrichment, pricing and fold, machine ID is **still null**; run/GraphQL/stream display state is `multiple`; Token Meter says **Multiple models** and details show both exact raw IDs/canonical labels and their own token/USD deltas. Bad: context enrichment writes `opus[1m]` (selected alias), or a projection writes a synthetic `Multiple models` model ID. A single Opus entry instead passes `claude-opus-5-5` as actual canonical ID and retains raw `[1m]` in detail. No static price lookup is applied to either SDK case.

**File/owner corrections:** modify `autobyteus-server-ts/src/agent-execution/domain/agent-run-token-usage.ts` parser/type, `src/agent-execution/backends/claude/events/claude-session-event-converter.ts` if needed for the new discriminator, `src/agent-execution/events/processors/token-usage/token-usage-context-enricher.ts` (the omitted owner), and its orchestrator `token-usage-event-enrichment-transformer.ts`; check `src/token-usage/projections/token-usage-run-record-state.ts`, run-summary/GraphQL/stream serializers and Vue for null/mixed pass-through. Tests must run the **actual event→context enricher→fold→run summary→GraphQL/stream→Token Meter** path for mixed, single and unknown SDK result and prove configured alias does not overwrite null. Include non-SDK fallback regression and reversed map order. Do not rely on a unit test of the SDK parser alone.

### Physical SQL migration and startup completion gate (ARCH-F-004)

**DS-015 — BEH-008/009 startup/state spine:** updated app starts → existing `runMigrations()` applies an additive Prisma SQL migration adding nullable `claude_sdk_usage_state_json` → Prisma initializes using the new generated schema/client → `assertTokenUsageCurrentSchema()` checks the physical `token_usage_run_records` column (and existing invariants) → current-schema capability is set before run admission → existing app-data/history migration readiness proceeds separately → new/old Claude SDK runs read/fold/write via the codec/repository → Token Meter. Both `server-runtime.ts` and `standalone-application-host/start-standalone-application-host.ts` use this order. The new column belongs in the fixed `RUN_COLUMNS` set in `src/startup/token-usage-current-schema-readiness.ts`; this is the **completion proof** for the required physical field, not merely the Prisma model declaration. `TokenUsageRunStore.recordObservation` must never reach a new writer when current-schema readiness failed.

Create an additive Prisma migration under `autobyteus-server-ts/prisma/migrations/` for the nullable column and update `prisma/schema.prisma`, generated client usage, domain record and SQL codec. No default, old-row scan, historical backfill or legacy dual-read/write branch. Existing rows decode null as **no SDK checkpoint** and preserve their recorded identity/cost; a resumed old run baselines its first new cumulative result as partial per SR-007. A successfully migrated but legacy app-data-degraded state is not the same as missing current schema: do not weaken the current-schema assertion to permit the new writer. Existing history readiness may still be degraded under its own policy only after the current physical schema passes.

If `runMigrations()` fails or is interrupted, existing startup treats it as fatal; if the column is absent or assertion fails after migration, set `CRITICAL_CURRENT_SCHEMA_FAILURE` and stop startup before admitting run observations. Do **not** catch this to continue with a no-checkpoint writer or silently create the column at query time. After correcting a migration/package/database issue, restart through the **existing** migration runner and re-assert the column before accepting SDK writes. An interrupted migration's actual Prisma state must be inspected by the existing runner/operator path; do not assume an arbitrary partial schema is safe or invent automatic rollback. Migration completion evidence is: migration deploy reports success, physical column assertion succeeds, old null row read succeeds, and a new SDK checkpoint round-trip succeeds in test. Record errors without exposing database secrets.

**File/owner corrections:** in addition to SR-007 Prisma/codec mapping, include `autobyteus-server-ts/src/startup/token-usage-current-schema-readiness.ts` as the authoritative fixed-column gate, with `src/startup/migrations.ts`, `src/server-runtime.ts`, `src/standalone-application-host/start-standalone-application-host.ts`, `src/token-usage/providers/token-usage-migration-readiness.ts` and `src/token-usage/providers/token-usage-run-store.ts` as sequencing/admission contracts (change only where tests expose a need). Validate a fresh database, an existing database with old nullable rows, server and standalone startup order, missing-column assertion, failed/interrupted migration no-admission, corrected-restart success, old-null codec read, new checkpoint round-trip and unchanged historical amounts/IDs. A SQL migration check alone is insufficient; verify the startup gate explicitly.

### Revised change sequence and health check

First add the SDK identity discriminator and tests that demonstrate a mixed null survives the real context-enrichment boundary; then integrate the per-model fold/projection and full event-to-UI test. Add the nullable migration **with** `RUN_COLUMNS` update and generated-client/codec mapping in one implementation increment, and test both startup paths before enabling the new writer. This is a bounded correction to two existing owners, not a new subsystem. It removes only the SDK-conflicting selected-alias fallback; non-SDK enrichment and historical data remain supported. The invariant at the end of each step is: no mixed result masquerades as one catalog model, and no new checkpoint writer starts without verified physical storage.

## SR-010 extension — selected-model-only Claude SDK Token Meter (current design)

### Approval, evidence, boundary and classification

The user explicitly corrected intended behavior in **Approved SR-009**: when an agent/team selects Opus in the Claude Agent SDK runtime, the Token Meter should use **only Opus statistics**, not Haiku statistics, even when the SDK result reports both. The user rejected an Opus headline with Haiku shown separately. This supersedes the SR-007/008 **Multiple models** headline, whole-SDK token/USD aggregate, visible Haiku detail and mixed/unattributed analytics facet for the selected-Opus run. It does **not** assert Haiku had zero usage or permit assigning Haiku's usage/cost to Opus. Original direct Messages API, Codex, historical rows and SDK dynamic discovery are unchanged. ARCH-REV-005 passed the former SR-006 behavior only; SR-010 is new and unreviewed.

I-35 adds decisive, safe SDK metadata evidence: installed `supportedModels()` returned `value:'opus[1m]'` with `resolvedModel:'claude-opus-5-5[1m]'`, exactly the raw Opus `modelUsage` key observed in I-28/I-30. This mapping is supplied by the **same SDK**, not guessed by stripping `[1m]` or reading the direct-API price catalog. Current `claude-sdk-model-normalizer.ts` drops `resolvedModel`; this is the matching root cause/target. No billable SDK turn was made for the metadata probe. Other model lists may differ, so no universal alias claim.

**Task size remains Large; architectural risk remains High.** The cumulative result still spans SDK selection/result, numeric event, transactional cumulative USD/token fold, SQL/readiness, GraphQL/analytics and Token Meter. The new policy narrows displayed totals but makes exact selection→raw matching and exclusion of other source deltas critical. A wrong match could show Haiku as Opus or under/overstate selected usage. Renewed independent architecture review is required before implementation of SR-009 behavior.

### Primary spine and decision contract

**DS-016 — BEH-007/008 selected-model primary spine:** user/agent config selects SDK catalog value `opus[1m]` → `ClaudeSdkClient` uses that exact value for query and captures the SDK's own `supportedModels()` `value→resolvedModel` binding for that turn → terminal SDK result provides cumulative `modelUsage` keyed by raw wire IDs → sanitized SDK result event carries selected alias, uniquely resolved raw ID, all source checkpoints and selected-match quality → payload parser/context enrichment preserve selected-vs-reported semantics → transactional reconciler advances **all raw source checkpoints** but contributes **only the selected raw model's** token and `costUSD` delta → run record, one selected-model daily facet, GraphQL/stream → Token Meter shows selected model and its own token/estimated USD. This supersedes DS-014's `single|multiple` cardinality as a display/accounting decision; it remains possible for the source envelope to contain multiple models, but the meter is selected-only.

**Selection binding owner.** Extend the existing Claude SDK model descriptor normalizer to retain the SDK's optional `resolvedModel` without changing the selected `value`; expose a narrow internal lookup from the same dynamic SDK catalog/control surface used by `ClaudeSdkClient.listModels()`. At query start, bind the **actual selected value passed to `startQueryTurn`** to a unique, nonempty SDK `resolvedModel`. Carry that binding with the turn/result; do not derive it from a display name, the first map key, the direct Anthropic catalog, or suffix manipulation. In the observed case `opus[1m] → claude-opus-5-5[1m]`. Require the resolved raw ID to match exactly one `modelUsage` key before producing trusted selected statistics. A missing, conflicting or stale resolution, absent selected entry, or unsupported SDK response produces selected-model **usage/cost missing or partial** with an explicit quality reason; never substitute Haiku, count whole-SDK totals or silently reinterpret `[1m]`. Preserve the configured alias as **selected/request metadata** and the selected raw/canonical IDs as **reported identity**. The SDK `ModelInfo.resolvedModel` is an alias→wire-ID relation, whereas a result entry's `canonicalModel` is a display/provenance field; do not conflate them.

**Result/event shape.** Keep SR-007's bounded numeric-only sorted `modelUsage` envelope, session ID, actual create/resume binding, stable result identity and no response text/signatures/secrets. Add `selectedModelValue`, `selectedResolvedRawModelId` and `selectedMatchState` (`matched|missing|ambiguous`) as a validated SDK-specific transport subject. `selectedMatchState` is derived from SDK metadata plus exact source-map lookup, not the count of map entries. `model_identifier` is the selected entry's SDK-provided `canonicalModel` when valid, otherwise its exact raw key, **only when matched**. On missing/ambiguous, the machine reported-model ID stays null and the UI may state the configured **Selected: opus[1m]** separately from unknown reported usage; it must not present the alias as proven actual usage. `TokenUsageContextEnricher` must pass through this intentional SDK null and the matched selected identity rather than replacing it with config fallback; other-runtime behavior stays unchanged. `Multiple models` is no longer the selected-run meter label. Keep nonselected entries in private source-checkpoint state only; do not project their tokens/cost/model details to this Token Meter or its daily totals.

**Token/cost authority.** SDK `modelUsage` is cumulative per raw model across resumed turns (I-30), so the transactional fold updates checkpoints for **every reported raw ID** to the current SDK cumulative numeric fields and `costUSD`, but adds to the run ledger only the matched selected raw ID's nonnegative delta for the current turn. This prevents internal Haiku usage from entering Opus totals and prevents an old hidden Haiku cumulative amount from being counted if Haiku is later selected. For a fresh proven create session, selected initial cumulative is its first contribution; for a known resume, subtract its prior raw checkpoint; for a legacy resume without checkpoint or a regression, baseline conservatively and flag selected statistics partial/missing. Deduplicate result ID and checkpoint/ledger write in one transaction. If the selected model changes between turns, use each turn's bound selected value and the corresponding pre-result raw checkpoint; never reallocate prior turns. For selected Opus, display its own base/cache/output token deltas and its own `costUSD` delta when finite and `costBasis=list|managed`; unknown basis/invalid cost remains unpriced. Top-level `usage` is main-loop-only context information and is **not** added to modelUsage totals or used as a substitute when selected raw mapping fails. The SDK `total_cost_usd` includes other models, so it is **not** a target that selected-only cost must equal; a smaller selected-model estimate is expected, not a quality failure. No static direct-API price row, fabricated component rates, or subscription-billing claim.

**Projection semantics.** The Token Meter's primary model is the selected model's reported canonical ID (`claude-opus-5-5` in the observed case), with its exact raw `claude-opus-5-5[1m]` in details. The amount label must say **Selected-model SDK-estimated usage cost — not a subscription bill or whole-SDK total**. Gross input/cache/output/output totals, amount, report count and daily facet are derived from selected deltas only; one unique SDK result contributes at most one report/facet. Other SDK models are neither shown as secondary meter rows nor included in its run/team totals. If a team aggregate contains **different models selected by different member runs**, its summary may be mixed across those selected models; that is distinct from showing one run's unselected internal models. Preserve a separate selected-model identity/quality status through GraphQL/stream/Vue. Previously stored mixed or price-missing rows stay readable **as recorded**; no reprice/relabel/backfill. A run spanning old and new policy has an explicit partial/mixed historical-quality status rather than pretending its old whole-SDK portion is selected-only.

### Ownership, removal and persisted-state decision

| Owner/path | SR-010 responsibility |
| --- | --- |
| `autobyteus-server-ts/src/runtime-management/claude/client/claude-sdk-model-normalizer.ts`, `claude-sdk-client.ts` and Claude session launch (`claude-session.ts`) | Preserve SDK `resolvedModel` from supported-model metadata and bind actual selected value to unique raw wire ID at turn start. Keep SDK model catalog dynamic; no static `[1m]` row. If lookup is absent/ambiguous, carry an explicit unresolved state rather than inventing a match. |
| `src/agent-execution/backends/claude/session/claude-sdk-result-usage-parser.ts`, `claude-session-token-usage.ts`, `events/claude-session-event-converter.ts`, `src/agent-execution/domain/claude-sdk-usage.ts` and `agent-run-token-usage.ts` | Preserve all numeric source rows privately, validated selected binding/match, selected reported identity and quality; remove source-map-cardinality `single|multiple` as the user-facing selected-model decision. No first-key or selected-alias-as-actual fallback. |
| `src/agent-execution/events/processors/token-usage/token-usage-context-enricher.ts` and enrichment transformer | Respect matched selected identity and unresolved intentional null; preserve other-runtime config fallback. Prevent cost enrichment from applying direct catalog to SDK raw IDs. |
| `src/token-usage/projections/claude-sdk-model-usage-reconciler.ts`, run fold/record-state, pricing summary and analytics contribution | Maintain all raw checkpoints; admit only selected raw delta to money/token ledger and analytics. Drop former whole-SDK sum and SDK-total reconciliation as ledger authority; mark old+new policy histories partial. Keep component rates unavailable. |
| Existing nullable `claude_sdk_usage_state_json` design; Prisma schema/migration/codec; `startup/token-usage-current-schema-readiness.ts`; both server/standalone startup | Retain SR-008 additive versioned state and startup column assertion. Internal raw checkpoints are not frontend-visible. Old null rows have no checkpoint, never a synthetic zero; migration failure blocks admission, corrected restart uses existing runner. No extra schema field/backfill required by the selected-only policy. |
| GraphQL/stream summary and `autobyteus-web/components/workspace/usage/TokenUsageMeterPanel.vue` plus locale copy | Show only selected model stats/cost, exact raw selected ID detail and truthful selected-estimate disclaimer/quality. Remove former `Multiple models` headline/Haiku detail for one selected-Opus SDK run; no synthetic catalog model. |

**Design-health/removal:** The root defect remains conflating request selection, actual per-model SDK usage and direct price identity. SR-010 retains one sanitized envelope and one authoritative fold but removes the prior requirement to sum every SDK model and show a mixed aggregate for a selected run. The new narrow SDK `resolvedModel` capture is required because the existing normalizer discards a field the SDK supplies. Do not build a second provider registry or a heuristic alias table. The physical SQL-state transition remains the reviewed additive migration plus fixed startup gate; SR-009 does not justify a new historical rewrite. No new top-level subsystem or generic model-identity framework is warranted.

### Sequence, tests and residual risk

1. Freeze SR-008 whole-SDK mixed display/accounting work pending review. Add no-key SDK-model metadata fixtures showing `opus[1m]→claude-opus-5-5[1m]`, default alias, missing/duplicate/stale resolution and different model variants; assert no suffix stripping, direct-catalog lookup or first-key selection. A metadata-only locally authenticated `supportedModels()` observation (I-35) may be repeated cost-free and secret-safe.
2. Thread the actual selected turn value/resolved raw key through sanitized result→payload→context enrichment; test selected Opus with reversed Haiku/Opus map order and exact raw/canonical identity, no Haiku UI detail, plus unresolved selected mapping producing missing/partial rather than Haiku. Retain non-SDK enrichment regressions.
3. Reconcile all raw cumulative source checkpoints but contribute selected raw delta only; test create→resume values from I-30 (Haiku unchanged and **never counted**, Opus second-turn USD delta ≈$0.0057032), two models both advancing but only selected added, selected-model switch, duplicate/retry, regression/reset, unknown basis, absent selected row and old-null resume. SDK total greater than selected amount must **not** flag mismatch. Test one report/facet per unique result and no hidden model leakage to GraphQL/Token Meter.
4. Keep SR-008 Prisma migration, fixed `RUN_COLUMNS`, server/standalone readiness and old-row tests. Then run actual event→fold→SQLite→GraphQL/stream→Token Meter selected-only journey and direct/Codex regressions. API/E2E may run one tiny locally authenticated SDK Opus create→resume after source-review pass, no direct paid provider call or secret logging. Delivery still needs explicit user verification; old scoped passes are not current-scope passes.

**Residual risk:** `supportedModels()` metadata may change by account/turn or be unavailable; fail closed and show missing/partial, never a guessed alternate. SDK costUSD remains an estimate and selected-only amount intentionally excludes other SDK-reported usage, so the UI must not call it total SDK cost or bill. A selection change and resumed cumulative result require all-source checkpoints even though only one source is displayed. Architecture review must confirm this ownership and the lack of hidden-other-model leakage before implementation resumes the affected behavior.

## SR-012 — Configured-price Claude SDK monetary design on Approved SR-011

### Decision and evidence boundary

This extension **replaces only SR-010's SDK `costUSD` pricing authority, component-null/no-unit-rate behavior and corresponding display copy**. SR-010's selected `value→resolvedModel→raw modelUsage` binding, all-raw cumulative token checkpoints, selected-only deltas, hidden unselected models, idempotency, nullable checkpoint migration/readiness, and old-record protection remain. The user chose the original Autobyteus self-calculation policy and delegated a reasonable cache-write estimate (requirements SR-011). The real I-41 probe proves a useful distinction: selected Opus per-model `cacheCreationInputTokens=7140`, while the same result's top-level main-loop `usage.cache_creation` reports 5m=0/1h=7140 and all four main-loop token dimensions equal selected Opus. I-42 confirms the same equality after cumulative create→resume delta, and I-43 observes it for a tool-bearing turn even though an unselected Haiku row is also reported. Crucially, I-43's streamed assistant-frame output counts **did not sum to terminal output**; the installed SDK explicitly calls those usage fields non-final. Therefore do **not** accumulate assistant-frame usage or use its canonical model ID as a raw result-map join key. SDK types say terminal top-level equality is not guaranteed when subagents/sidechains or other calls contribute; do not infer a universal top-level-to-selected identity from these probes. I-44 records the tool probe's permission-callback limitation, not a pricing contradiction. Anthropic's documented 5m/1h rates remain separate; the Opus row has both trusted rates. SDK dollar amounts remain private diagnostic input at most, never pricing output, cache-tier inference or an invoice.

**Design health/root cause:** The original TokenCostCalculator/TokenPriceConfigProvider already owns configured estimation and immutable price snapshots. SR-010's newly developed bypass that wrote SDK USD directly into the fold is the wrong ownership for Approved SR-011. Refactor **now** by restoring that one pricing owner after selected-model delta reconciliation, not by implementing another SDK-specific dollar calculator. The unavoidable SDK adapter is a small typed main-loop usage/split capture and an attribution gate. No new price catalog row for `[1m]`, generic cache-write rate, pricing database, or second public meter is warranted. The cumulative delta remains Large/High because the event/fold, persisted quality, GraphQL/stream and UI contracts are touched; this bounded monetary revision does not reduce the cumulative classification.

### DS-017 primary spine and ownership

`ClaudeSdkClient` runs actual selected catalog value in its active create/resume query (SR-010 reviewer guard) → sanitized terminal result parser carries cumulative per-model counts and **per-turn main-loop** `input_tokens`, `output_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`, and optional `cache_creation.ephemeral_5m_input_tokens`/`ephemeral_1h_input_tokens` as nonnegative safe integers → one SDK result event/context retains selected raw binding and this numeric-only main-loop shape → transactional selected-model reconciler obtains the selected **per-result delta** while advancing private all-model checkpoints → cache-tier attribution verifies (a) top-level 5m+1h equals top-level aggregate cache creation, and (b) top-level input/output/cache-read/cache-create counts **all equal** the selected per-model delta. Only then assign selected 5m/1h tokens from top-level. Otherwise, if selected cache-create delta is positive, assign it to **1h estimated bucket** and add durable quality flag `claude_sdk_cache_write_1h_assumed`; if zero, no TTL assumption. Never use top-level main-loop token counts as the selected billed token source. Then `TokenPriceConfigProvider` resolves **exact selected canonical model** under the configured Anthropic provider at `observed_at`, `TokenCostCalculator.applyPolicy` prices standard/read/5m/1h/output from that policy, and the transactional fold writes token totals, configured rate snapshot, component costs, total, quality and analytics. GraphQL/stream surfaces the run summary; Token Meter displays canonical selected model, configured API-equivalent estimate and any approximation note. Haiku remains absent from public totals and details.

**Fail-closed cases:** No unique alias→raw match means no selected contribution or catalog fallback. Missing/invalid selected `canonicalModel` leaves raw ID displayable but price missing; do not strip `[1m]` or treat raw as direct catalog alias. Unsupported SDK provider/processing tier, missing trusted configured rate, unsafe token count, or missing selected delta yields existing missing/partial semantics. A valid SDK token row remains usable even if SDK `costUSD`/`costBasis` is absent, unknown or differs; those fields cannot veto or determine configured pricing. A malformed split or mismatch cannot contribute top-level tokens and uses the marked 1h fallback only for positive **selected** cache-create tokens. If the configured 1h rate is absent/untrusted, the calculator's existing `partial_price_missing` and missing dimension apply; do not substitute a generic/zero/SDK cost. No SDK whole-total equality check. Zero cache writes have no fallback flag. Identical results are idempotently suppressed; reset/resume/legacy baseline stays conservative as in SR-010, and cost is never calculated for an unadmitted selected delta.

**Approximation semantics:** The 1h fallback is a *rate assumption*, not an observed TTL. It is reasonable for the observed Opus SDK shape (I-28/I-41) and is the higher of Opus's two configured write rates, but may overestimate a 5m or mixed-TTL turn. The total remains an `api_price_estimate`, with the configured price policy snapshot and one persistent approximation marker. The visible label is `Configured API-equivalent estimate — not a subscription charge`; when any contributing SDK event used the fallback, show `Some Claude SDK cache writes were estimated at the configured 1-hour rate because their duration could not be attributed.` No `sdk_reported_estimate` label or component-free SDK dollar total may leak into new records. Do not render an assumed 1h bucket as an unqualified observed 1h measurement; the run calculation detail must carry the note. Prior records retain their own stored source/status/amount without repricing.

### File responsibilities and contract cut

| Owner/path | SR-012 responsibility and boundary |
| --- | --- |
| `autobyteus-server-ts/src/agent-execution/backends/claude/session/claude-sdk-result-usage-parser.ts`, `claude-session-token-usage.ts`, `agent-execution/domain/claude-sdk-usage.ts` | Capture/validate bounded numeric top-level per-turn cache split alongside cumulative per-model data and selected binding. Keep raw result text, secrets and signatures out of usage payload. SDK USD may be dropped from new public usage contract or retained private/diagnostic only; invalid/missing SDK USD must not discard otherwise valid token counts. |
| `agent-execution/domain/agent-run-token-usage.ts`, `events/processors/token-usage/token-usage-event-enrichment-transformer.ts`, `token-usage/services/token-usage-run-accumulator.ts` | Preserve typed source through SDK bypass until transaction; before transaction resolve configured policy only for a uniquely matched selected canonical ID and supported Anthropic provider, never the raw suffix/first map key. Pass policy into fold, which revalidates match. Continue normal configured-pricing branch for direct APIs and Codex untouched. |
| `token-usage/projections/claude-sdk-model-usage-reconciler.ts`, `token-usage-run-fold.ts` | Own private all-model cumulative checkpoints and selected delta; exact split reconciliation or marked 1h bucket; invoke **existing** TokenCostCalculator on selected authoritative delta inside fold, not SDK-dollar direct assignment. Remove WIP SDK-cost checkpoint/public selectedDetails cost as monetary authority. No double pricing. Preserve dedup, reset and legacy baselining. |
| `token-usage/pricing/token-price-config-provider.ts`, `token-cost-calculator.ts`, `autobyteus-ts/src/llm/anthropic-supported-model-definitions.ts` | Reuse exact configured canonical rate lookup and existing component/tier calculator. Do not add `[1m]` row, generic write rate or SDK-specific price table. If observed cache split is valid, price both actual-duration buckets; if fallback, place selected aggregate in 1h estimated bucket with quality marker. Catalog remains one authority. |
| `token-usage/projections/token-usage-run-aggregate.ts`, `agent-execution/domain/agent-run-token-usage.ts`, `api/graphql/types/token-usage-stats.ts`, shared application SDK contracts, `autobyteus-web/graphql/queries/token_usage_meter_queries.ts`, generated GraphQL/types and `autobyteus-web/components/workspace/usage/TokenUsageMeterPanel.vue` plus locales | Expose a narrow run-summary `hasCacheWriteRateAssumption` boolean derived from durable quality flags, including team/member aggregation, without exposing raw SDK source or Haiku. Display approximation and not-subscription copy only when applicable; existing generic direct-API labels/rows stay unchanged. Generated files follow repository generation tooling. |
| Existing `claude_sdk_usage_state_json` nullable Prisma migration, SQL codec, startup readiness assertion | Keep reviewed SR-008/SR-010 migration/readiness order. The column stores selected/source **token** checkpoints; no further SQL migration is required for this price-source revision. Old null/readable history remains; no backfill. |

**Clean cut/removals:** Delete or cease calling new WIP paths that mark `pricing_status='sdk_reported_estimate'`, `cost_basis='sdk_reported_estimate'`, `pricing_source='claude_agent_sdk_result'` and put selected `costUSD` into `estimated_api_total_cost`. Remove unused SDK price-status enum only if no historical reader uses it; otherwise retain read compatibility but never emit it for new SDK observations. Do not expose persisted private `selectedDetails.costUSD` as configured cost. Keep SDK `total_cost_usd` entirely outside new configured-cost aggregation. No behavior-defining Product supplement; existing UI placement is sufficient for a small explanatory note.

### Sequence, persisted-data transition and verification

1. Retain SR-010 identity/session/sanitization and SQL/readiness work; add validated **terminal result** main-loop cache split to the event contract. Test absent/null, malformed, 5m+1h mismatch, safe-integer limits and secret-safe parsing. Confirm SDK result totals are cumulative per model but top-level usage is per-turn; never subtract top-level from prior top-level. Explicitly test that multiple streamed assistant frames with non-final usage (I-43) are **not summed or substituted** for terminal per-model/output counts. The assistant message `model` can be canonical while result `modelUsage` key is raw suffixed; keep the SDK-selected `resolvedModel` exact-match bridge.
2. Complete selected token checkpoint/delta fold. Resolve canonical configured price **after** match validation, snapshot it at observation time, assign exact split only when all token dimensions reconcile, otherwise flag and use 1h rate for selected positive cache writes. Price through the existing TokenCostCalculator. Test live I-41 fixture: Opus 2 input/63 output/9617 read/7140 create, top-level split 0/7140, Haiku entry present → Opus-only configured components and no assumption flag; alter top-level counts → selected tokens unchanged, 1h assumption flag and configured estimate; alter SDK USD → no cost change. Test 5m+1h mixed exact split, no-write case, missing canonical/rate, duplicate/cumulative/resume/reset/selection change, and top-level-only auxiliary mismatch.
3. Persist only selected priced contribution with private all-source checkpoints. Verify old/null checkpoint rows and price snapshots read unchanged; no monetary backfill. The planned nullable SQL migration must pass current-schema readiness before normal server/standalone startup and after restart. WIP JSON shape is not a released persisted contract; implement one versioned decoder/transition for local/test state if necessary, never silently reinterpret SDK-dollar fields as configured dollar totals.
4. Propagate configured price snapshot, component/unit rates and approximation boolean through run/team/analytics/GraphQL/stream/UI. Check one selected Opus meter journey with 1h exact split, one fallback approximation note and one missing-price state. Ensure Haiku never contributes, no raw source leaks, and direct Messages/Codex pricing regression tests remain. Review source before API/E2E, then validate new AC-011–013 and preserved AC-014.

**Classification:** cumulative `task_size=Large`, `architectural_risk=High`: changed shared event and run-summary contracts, transactional monetary fold, persisted quality and web projection cross owners; approximation/selection mistakes would misstate money. SR-012 delta alone is Medium/High but does not justify downgrading the full package. Residual risk is that main-loop split may mismatch selected per-model use in complex turns; the fallback is explicitly marked and uses configured rates, not fabricated SDK data. ARCH-REV-006 remains Pass only for SR-010/SR-009; renewed independent architecture review is required for SR-012/Approved SR-011 before monetary implementation and later source/API/E2E/delivery gates.

## SR-014 — Known Claude SDK context-window display correction on Approved SR-014

**Decision/evidence:** I-45 is a supported live Electron defect, not a cache-accounting discrepancy. The selected Claude SDK result supplied a positive `modelUsage[selectedRaw].contextWindow=1,000,000` and `latest_prompt_tokens=22,135`; the persisted run record holds both, but `context_window_usage_percent` is null. The Claude event builder omits that field, unlike the Codex adapter. The existing Vue panel requires all three values before it renders known capacity. The same UI showed Codex's `10,836 / 258,400 ≈ 4.2%`, isolating the defect to Claude event/projection rather than a universal frontend failure. User explicitly approved fixing the known-capacity display; input-breakdown wording and arithmetic are out of scope.

**DS-018 / BEH-010 primary spine:** user selects Claude SDK Opus and sends a message → active-turn selection binding and terminal SDK result identify the exact selected raw `modelUsage` row → `claude-session-token-usage.ts` validates its `contextWindow` and computes latest full prompt input and `100 × latestPromptTokens / contextWindow` when both are safe and capacity is positive → existing token event/parser/fold persists the three context fields without changing selected-model token or cost admission → `buildTokenUsageRunSummaryFromRecords` projects the newest observation; if a prior record has valid prompt/capacity but a null percentage, derive the same percentage **in read projection only** → existing GraphQL/stream summary and Vue store carry it unchanged → existing `TokenUsageMeterPanel.vue` known-capacity branch renders `22,135 / 1,000,000` and approximately `2.21%`. For a genuinely missing/zero/unsafe window, leave capacity/percentage null and preserve `context limit unavailable`. No model-name-derived 1M assumption and no first-key/Haiku fallback.

**Owners and structure:** `autobyteus-server-ts/src/agent-execution/backends/claude/session/claude-session-token-usage.ts` owns selected-result numeric extraction and computes the percentage just as `backends/codex/thread/codex-thread-token-usage.ts` already does; use a safe integer check on the sum of input+cache-read+cache-creation before claiming a prompt total. `autobyteus-server-ts/src/token-usage/projections/token-usage-run-aggregate.ts` owns a bounded null-percentage fallback from the **same latest record's** prompt and positive capacity. Preserve an already stored finite percentage; do not combine fields from different records or runs. This two-point repair is deliberate: new observations store a complete result, while existing null-percentage rows display correctly without rewriting them. A tiny shared pure function is optional only if it reduces duplication without reversing dependency direction; do not build a new context service. `TokenUsageMeterPanel.vue`, DTO/schema, GraphQL field names, selected-model reconciliation, pricing and SQL schema need **no production changes**.

**Design health / transition:** Root cause is a missing derived Claude event field plus a UI gate that correctly refuses to show a percentage-less capacity. Refactor needed now is the bounded producer/projection completion, not a frontend workaround that invents a denominator or a broader meter refactor. Persisted rows are **directly usable — no migration**: existing columns already contain prompt and capacity; normal read projection can derive the missing percentage. No backfill, repricing, cache-count change, storage rewrite or deployment configuration change. Codex event behavior remains untouched. If SDK omits/invalidates `contextWindow`, no percentage can be derived and the truthful unavailable state remains.

**Sequence/tests:** (1) Add a Claude event-builder unit fixture with selected raw entry/contextWindow=1,000,000 and top-level input=2, cache-write=22,133, read=0; assert prompt=22,135, capacity=1,000,000, percent=2.2135, selected canonical identity and unchanged token/cost source. Test missing/zero/unsafe capacity and unsafe prompt sum. (2) Add run-summary projection tests for an old stored record with valid prompt/capacity and null percent, an already populated Codex percentage, a newer observation with truly unavailable capacity, and two-record latest-selection correctness; no DB mutation. (3) Extend the existing Claude SDK GraphQL/stream→Token Meter case to show the known-capacity branch and verify the screenshot's `22,135 / 1,000,000 ≈ 2.21%` shape; retain zero-capacity unavailable and Codex 4.2% regression. (4) Rebuild the Electron app only after source/API-E2E review and ask the user to verify on a new or rehydrated Claude run; the current executable is not fixed by documentation alone.

**Classification/review:** SR-014 delta is `Small`/`Low`: two existing owners and focused tests, no new contract/schema/monetary owner. The *cumulative still-in-delivery solution package* remains `task_size=Large`, `architectural_risk=High` because SR-012's selected-model monetary path and its downstream gates are not terminal. Request the configured independent review of this revised cumulative design before affected implementation; ARCH-REV-008 covered SR-012/SR-013 only, not I-45/SR-014. No renewed approval beyond the user's explicit fix request is needed. Residual uncertainty is only whether the current packaged runtime always supplies a valid `contextWindow`; the observed run did, and missing cases remain unavailable.
