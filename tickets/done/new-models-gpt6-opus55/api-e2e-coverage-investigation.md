# API/E2E Coverage Investigation — API-REV-001 plan

## Investigation meta and basis
Assigned worktree: `/home/autobyteus/workspace/.codex/worktrees/new-models-gpt6-opus55`. Trigger: CRR-002 Pass on IR-002 commit `42ba8549b`. Read the complete cumulative package in this directory: `requirements-doc.md` (approved SR-002), `investigation-notes.md`, `solution-revision-record.md`, `design-spec.md` (SR-005 retaining SR-004), `solution-handoff.md`, `design-review-report.md`, `architecture-review-revision-record.md` (ARCH-REV-003 Pass), `implementation-handoff.md`, `implementation-revision-record.md`, `code-review-report.md`, `code-review-revision-record.md`. Supplements: N/A; screenshots are input evidence only. Prior API/E2E investigation/result/revision: N/A. This is the first round. Task size **Large**, architectural risk **High**, reviewed input and successful output route through Code Reviewer; proportional test-code review required only if durable tests change.

## Requirements and changed boundaries
- AC-001/002: exact Sol/Luna direct catalog, Astra regression, OpenAI Responses send/stream and no shorthand alias. Domain/backend catalog and external transport adapter; no changed frontend or browser code.
- AC-003/004: Opus 5.5 catalog/request preflight, signed assistant-tool replay, independent-turn reset, recovery and compaction. Changed provider transport, agent lifecycle and persisted optional metadata; no changed desktop shell.
- AC-005: Standard price, cache, long-context full-request tier, trusted versus partial/missing, existing snapshots. Domain/backend plus server pricing persistence.
- AC-006/007: dynamic Codex discovery/GraphQL and real locally authenticated per-advertised-model app-server turns. External integration and identity/account entitlement; direct API live runs are separate from no-key contract proof.
- AC-008: existing catalog IDs and historical snapshots direct-use without migration. No version-specific compatibility mechanism is approved; general historical generic turns and v5 absent metadata are valid current readers.
- AC-009/010: exact stable Anthropic SDK pins/lockfile, build and Claude SDK model discovery, session, tools/permissions, streaming/usage and interrupts; live Claude attempt only if local auth is safely detected.
- Not affected: UI components, browser integration, Electron shell, worker/queue/distributed coordination. Browser validation would not prove changed backend/provider or SDK boundaries and is not selected.

## Project execution discovery
`README.md`: pnpm workspace setup. `package.json`: workspace e2e scripts. `autobyteus-server-ts/AGENTS.md`: `vitest run ... --no-watch`, narrow then integration. `autobyteus-ts/package.json`: build via `pnpm --filter autobyteus-ts build`; tests via direct vitest. `autobyteus-server-ts/package.json`: `prepare:shared`, Prisma generation, build/typecheck and vitest. Existing `node_modules` and built workspace artifacts are present; do not use another process/data store. Live Codex tests are opt-in with `RUN_CODEX_E2E=1`; `codex` binary and local auth-file presence were noted upstream but live entitlement is unknown. No server process or persistent fixture is required for focused no-key suites. Codex test will use app-server client isolated temporary workspace and close owned process; no shared server/process stopped. Secret candidate `$HOME/.autobyteus/server-data/.env` will be checked only by existence and known-key nonempty booleans, never printed/persisted. Direct-provider live run only if safe usable key and a focused non-destructive path exist.

## Existing durable coverage inventory and decisions
| Path / scenario | AC | Decision | Action |
| --- | --- | --- | --- |
| `autobyteus-ts/tests/unit/llm/supported-model-definitions.test.ts` | 001,003,005,008 | Still Valid | Rerun exact rows/schema/prices/older IDs. |
| `autobyteus-ts/tests/unit/llm/api/provider-native-request-payloads.test.ts` | 002,003,004,007 | Still Valid | Rerun Responses and Anthropic no-key payloads. |
| `autobyteus-ts/tests/unit/llm/api/anthropic-llm.test.ts`, `anthropic-signed-tool-continuation.test.ts` | 003,004 | Still Valid | Rerun simple/stream/signed replay. |
| `autobyteus-ts/tests/unit/agent/{loop,events}/`, `tests/unit/memory/` relevant signed-turn files | 004,008 | Still Valid | Rerun reset/recovery/compaction/notifier/snapshot. |
| `autobyteus-server-ts/tests/e2e/token-usage/` | 005,008 | Still Valid | Rerun server price/snapshot scenarios. |
| `autobyteus-server-ts/tests/unit/runtime-management/claude/client/claude-sdk-client.test.ts`, Claude backend/manager tests | 009,010 | Still Valid | Rerun SDK regressions. |
| `autobyteus-server-ts/tests/integration/services/codex-model-catalog.integration.test.ts` | 006 | Still Valid but incomplete for exact new-model turn | Rerun live discovery/GraphQL; add or use focused live turn probe per advertised model. |
| `autobyteus-server-ts/tests/integration/runtime-execution/codex-app-server/thread/codex-thread.integration.test.ts` | 006 | Still Valid but selects older Mini | Use as pattern for focused per-model live probe; do not call this model-specific proof. |
No stale coverage initially identified; none to remove. Existing source test additions are implementation-owned and will be independently rerun. Potential durable addition: a gated per-advertised GPT-6 Codex turn test if current test seams permit deterministic isolation; otherwise use temporary live probe and explain why not durable. Coverage decisions will be updated before edits.

### Coverage-validity update after live execution
The existing opt-in `autobyteus-server-ts/tests/integration/runtime-management/claude/client/claude-sdk-client.integration.test.ts` is **Needs Update**: its `startQueryTurn` calls omit current required `sessionBinding` (and system prompt) and its resume case uses removed `sessionId` input. The first live attempt failed in the test harness before any Claude query with `Cannot read properties of undefined (reading 'kind')` at `buildQueryOptions`. Current production `ClaudeSession` and unit tests supply `sessionBinding`. Update this one durable live integration file to the current contract, then rerun only its single short live model/query case; do not run the entire potentially costly live suite. The separate Codex live thread test also calls old `submitInput`; this is **Needs Update**, but the locally scoped temporary per-model probe was corrected to current `startInput` and establishes task-specific live proof without broad editing of unrelated older Codex live cases. No test removal is justified.

`autobyteus-server-ts/tests/unit/token-usage/pricing/token-price-config-provider.test.ts` is **Needs Update** for AC-005: it has prior Astra and older Anthropic rates, but no exact Sol/Luna/Opus 5.5 assertion at the server pricing-consumer boundary. Add narrow parameterized new-model policy tests here, including the long-context tier for Sol/Luna. Shared catalog tests alone do not establish the server policy resolution for these IDs.

## Persisted-data basis
Approved `Directly Usable — No Migration` in SR-004 and implementation handoff. Exercise v5 absent native key and native/reset round-trip via current reader, plus historical price snapshot immutability via server tests. No migration or compatibility-only test will be added.

## Execution plan and ledger
Ledger is required for multiple independent cases; initialize before first run at `api-e2e-test-case-ledger.md`.
| Case | Scope | AC | Surface |
| --- | --- | --- | --- |
| API-C01 | catalog/OpenAI/Anthropic direct no-key contracts | 001–004,007,008 | shared vitest |
| API-C02 | signed active turn/reset/compaction/recovery/serialization | 004,008 | shared vitest |
| API-C03 | server pricing/snapshot and SDK regression/build | 005,008–010 | server/shared build and vitest |
| API-C04 | Codex model discovery/GraphQL/capability parity | 006,007 | live app-server integration |
| API-C05 | each locally advertised Astra/Sol/Luna minimal real turn | 006,007 | live app-server via Autobyteus path |
| API-C06 | secret-safe direct-provider and Claude-auth availability, conditional live attempts | 007,010 | local environment/external |

## Confidence gate (pending execution)
Post-repository scorecard will be completed after C01–C03. C04–C06 are **Required** broader validation, not replaced by mocked passes. Live Codex can materially close the account/transport gap; browser cannot. Clean Pass requires every critical AC directly proven, overall >=95%, every applicable category >=90%. If advertised Codex models cannot complete, report model-specific outcome and classify a genuine blocker versus implementation defect based on evidence. Direct-provider live success will never be inferred from no-key tests.

## Investigation decision
Proceed: Yes. Durable changes: undecided pending C01–C03 inventory evidence; no removals planned. No upstream ambiguity currently requires reroute. Temporary probe is acceptable only for local entitlement-sensitive smoke and will not be confused with durable contract coverage.

## Post-repository update (after API-C01–C03)
C01: 4 shared files/64 tests passed. C02: 7 lifecycle files/43 tests passed. C03: shared build passed; initial server run 6 suites/85 assertions passed with one import/setup failure because generated `@autobyteus/application-sdk-contracts/dist` was absent. Documented `prepare:shared` built it; targeted manager and SQLite/GraphQL price rerun passed 10/10. Prisma generate plus server build-target `tsc --noEmit` passed. Missing build artifact was environment setup, not an implementation assertion failure. Existing tests remain valid; no durable change selected yet.

| Confidence category | Post-repository score | Evidence and remaining gap |
| --- | ---: | --- |
| Requirement/AC proof | 75% | Direct deterministic AC-001–005/008–010 evidence; AC-006 live turns pending. |
| Changed-boundary directness | 75% | In-process adapters, memory, server GraphQL and build executed; live provider/account boundaries pending. |
| Cross-boundary realism/mock gap | 50% | Provider responses and Claude SDK mocked; real Codex not yet exercised. |
| Environment/identity fidelity | 50% | Isolated workspace/SQLite realistic; credential/account discovery pending. |
| Failure/lifecycle/recovery | 90% | Signed reset, recovery, compaction and missing-price paths exercised; live failures pending. |
| User-surface/browser/shell | N/A | No changed UI/browser/shell behavior; browser would not exercise provider requests. |
| Durable regression quality | 90% | Focused relevant unit/GraphQL suites; per-model live Codex test not yet durable. |

Overall post-repository confidence: **72%**, arithmetic average of six applicable categories (430/6=71.7%, rounded). Critical AC directly proven: No, AC-006 pending. Applicable categories below 90%: requirement, directness, realism, environment. Clean target unmet. Broader validation **Required**: live Codex app-server catalog/GraphQL and per-advertised-model thread/turn, plus safe credential availability check and conditional direct/Claude live attempts. This is the most direct mode for the remaining gap. Browser and actual desktop execution are not selected; backend/transport change has no browser or shell-specific path.

## Final investigation reconciliation — API-REV-001
- Coverage validity updated before edits: Claude Agent SDK opt-in integration file **Needs Update** for required session binding; server pricing-provider file **Needs Update** for exact new-model server consumer rates. Both were updated narrowly and executed. Old Codex opt-in thread test's removed `submitInput` is a pre-existing **Needs Update** outside this task-specific per-model probe; temporary current-path probe replaces it for this round's AC-006 evidence, but the old suite remains a maintenance residual.
- Durable paths updated: `autobyteus-server-ts/tests/integration/runtime-management/claude/client/claude-sdk-client.integration.test.ts`; `autobyteus-server-ts/tests/unit/token-usage/pricing/token-price-config-provider.test.ts`. Added/removed durable paths: none. No stale test deleted. Temporary Codex probe retained only under ticket `evidence/api-e2e/` and removed from repository test path after execution.
- C04 live Codex catalog/GraphQL parity passed. C05 account advertised all three exact GPT-6 IDs; Autobyteus CodexThreadManager app-server `startInput` turns completed for Astra/Sol/Luna. C06 secret-safe check found a nonempty Anthropic API key but no OpenAI API key; one small direct Anthropic SDK Opus 5.5 call passed, and one Claude subscription-backed Agent SDK query/model-discovery/session-history case passed after current-contract test repair. No further paid Claude cases ran. Direct Anthropic SDK result is separate from no-key Autobyteus path and does not prove a live signed tool cycle.
- AC-009 pins independently matched registry stable tags `0.3.280` and `0.128.0` on 2026-09-23. Build-target TypeScript passed. An attempted general `tsc -p tsconfig.json --noEmit` is not an authoritative build check because the repository config includes tests outside its `rootDir=src`; it failed with TS6059 unrelated to changed code.
- Final broader-validation decision: **Required and completed** via live app-server, one direct Anthropic SDK call and one Claude Agent SDK query. Browser/desktop Not Required; neither reaches the changed provider/backend boundary. The user's cost constraint led to skipping three additional live Claude integration cases; no inference of live tools/permissions/interrupts is made from the single query.
- Final category confidence: requirement/AC 95%; changed-boundary directness 95%; cross-boundary realism/mock gap 90% (OpenAI no key and signed Anthropic tool cycle mocked by approved contract); environment/configuration/identity/fixture fidelity 100% **for approved validation scope** (real local Codex, Claude subscription, key availability assessed; OpenAI live explicitly not required); failure/lifecycle/recovery 95%; user-surface/browser/shell N/A (unchanged); durable regression quality 95%. Arithmetic mean of six applicable categories = **95%**. Every critical AC has its required form of proof; no applicable category below 90%. Residual risks: no live OpenAI entitlement proof and no live full Autobyteus Anthropic signed tool cycle, both beyond approved no-key/cost-limited contract evidence; old opt-in Codex thread test API drift does not negate the new task-specific live probe.
- Investigation decision: validation **Pass**, pending proportional review of the two changed durable test paths. No requirement/design reroute or implementation defect identified. See `api-e2e-execution-coverage-report.md` for authoritative round result and `api-e2e-revision-record.md` for baseline.

## API-REV-002 extension investigation — requested real AnthropicLLM tool continuation
Trigger: Implementation Engineer relayed the user's explicit request for a live product `AnthropicLLM` integration test using the candidate `$HOME/.autobyteus/server-data/.env` credential, while limiting Anthropic spending. Prior authoritative API-REV-001 Pass/95% is read from the canonical execution report and revision record; it is **not** inferred. The prior direct Anthropic SDK Opus 5.5 request succeeded, but did not invoke `AnthropicLLM` or replay a signed tool turn. This is a real changed-boundary evidence gap for AC-003/004. All other C01–C06 evidence remains current; no source change or requirement/design revision is indicated.

Coverage validity: existing no-key `anthropic-llm.test.ts` and `anthropic-signed-tool-continuation.test.ts` remain **Still Valid** but mocked; their assertions cannot establish provider acceptance of the actual product adapter's signed replay. Add **temporary executable probe only**, not durable paid CI coverage, using current `AnthropicLLM` instance, the existing `SecretValue` resolver interface, `streamMessages` for a single Opus 5.5 tool-use turn, then one `sendMessages` continuation constructed via the production `AnthropicPromptRenderer` message types. At most two paid API requests; no retry loop. Preserve native block order/signatures only in memory; print no key, thinking, signature, tool input or response text. The candidate `.env` is parsed in memory; no `pnpm secrets:import` because import mutates persistent vault state and is unnecessary for a focused adapter probe. If credential missing/unusable, record precise model/access blocker and do not invent success. New case **API-C07**; append to canonical ledger before execution. Expected proof: streamed `providerNativeAssistantTurn` contains an actual signed thinking/tool-use block and completed usage; continuation with exact native assistant/tool-result replay receives a non-error real Messages response with usage. If model declines tool under `auto`, report partial live evidence, not success. No durable coverage edits are planned; API-REV-001's two durable test changes remain pending proportional review.
C07 first live request produced an actual Opus 5.5 native tool-use turn and usage, but no signed thinking block was exposed on the trivial task. The probe's required-signature assertion was too strict for this observable provider output and stopped before tool continuation. This is not evidence of a product defect. Update the temporary probe to require the real tool/native replay and record whether signed thinking was present without inventing it; rerun the two-request cycle at most once. If second call succeeds without a signature, mark live tool continuation Pass but signed replay specifically still proven only by deterministic no-key tests. The first request incurred a small paid call; no broad/retry loop.

### Final API-REV-002 decision
API-C07 **Pass for live product `AnthropicLLM` tool continuation** after one cost-limited rerun: current adapter streamed one Opus 5.5 native tool-use block and usage, and exact native assistant-turn/tool-result replay through `sendMessages` was accepted with nonempty content and usage. The provider emitted **no signed thinking block** on either trivial tool request, so live signed replay is **Not Tested**, not a claimed success; deterministic no-key signed replay/reset/compaction tests remain authoritative for that material. The initial overstrict probe assertion did not indicate a product failure. Three small product API calls total in this extension; the key was loaded in memory only, not imported, printed or persisted. No durable test/source edits were added in API-REV-002. Final confidence: requirement 95%, changed-boundary directness 95%, cross-boundary realism 95% (improved from 90 by real product tool cycle; signed thinking/OpenAI 200 residual), environment 100% for approved scope, failure/lifecycle 95%, UI/browser/shell N/A, durable regression 95%; mean **96%** rounded. Latest result remains **Pass**, superseding API-REV-001's 95% baseline. Reviewed Large/High route still requires Code Reviewer proportional review of the two API-REV-001 durable test updates; no new test file needs proportional review.

## API-REV-003 extension investigation — complex game task to elicit signed thinking
Trigger: user requested a more complex live Opus 5.5 test resembling a small Mario-style single-file HTML game with multiple tool calls, specifically to try to obtain signed thinking absent from API-C07. Prior API-REV-002 authoritative result **Pass / 96%** was read from the canonical report/revision record. The provider's official [Opus 5.5 changes](https://platform.claude.com/docs/en/models/opus-5-5/whats-new-opus-5-5) and [adaptive thinking guide](https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking) state adaptive thinking may omit a thinking block on easy tasks, while `output_config.effort` controls depth and `xhigh` strongly encourages/always uses deeper thinking; signed blocks must be replayed unchanged with tool turns. Thus C07's absence of a thinking block was not a defect. This extension needs no requirement/design/source change or durable paid CI test. Existing C01–C07 remain valid; add temporary **API-C08**.

Execution plan: use actual product `AnthropicLLM` at exact `claude-opus-5-5`, constant system prompt/tool definitions across turns, `thinking: {type:'adaptive'}` and `output_config:{effort:'xhigh'}` (provider-supported). A test-owned temporary directory contains only `index.html`. Expose bounded `write_html` and `inspect_html` tools; the former writes only that file with size cap and instructs inspection, the latter returns compact structural checks (HTML/canvas/script/keyboard/game loop) and no content. Prompt for a tiny single-file Mario-style platform game; loop at most **four** real Messages API requests, `max_tokens` 4096 each, no automatic retries. Preserve each actual native assistant block array (including any signed thinking) via `ANTHROPIC_ASSISTANT_TURN_KEY`, append exact tool results, and send through `AnthropicLLM.streamMessages` for the next request. Capture only booleans/block types/tool counts/usage counts, never the key, thinking text, signature, HTML content or response text in logs. Parse candidate env key in memory, no vault import; close and clean only owned temporary state. A valid result must show a real signed thinking block before tool use, a following real request accepting that exact signed native turn, and at least two tool invocations. If model/tool behavior does not produce that within cap, report partial proof and stop rather than inflating cost. Browser visual/game-quality validation is outside this provider-protocol probe; the game is only a realistic task fixture.

### Final API-REV-003 investigation decision
API-C08 **Pass**. Using real product `AnthropicLLM` on Opus 5.5 with xhigh adaptive thinking and a complex one-file platform-game task, the first streamed response contained a **signed thinking block** and two native `tool_use` blocks (`write_html`, `inspect_html`). The test-owned tools ran sequentially, producing one 4,259-byte HTML file and six positive structural checks. The second real request replayed the exact native assistant block array and tool results with constant system/tools, and the provider accepted it and completed with usage. Therefore the live signed active-tool-turn replay gap from API-REV-002 is closed for this bounded path. Only two of four permitted requests were needed; usage reported 3,733 input and 4,211 output tokens. Logs contain no secret/signature/thinking/HTML/response text. This does not prove browser gameplay quality, independent-turn reset or compaction live; those remain deterministic no-key tests. No implementation source or durable coverage changed in this round. Final confidence: requirement/AC proof 98%, changed-boundary directness 98%, cross-boundary integration realism 95% (OpenAI direct 200 remains untested by approved no-key plan), environment/identity fidelity 100% for approved scope, failure/lifecycle/recovery 95%, user-surface/browser/shell N/A, durable regression quality 95%; arithmetic mean **97%** rounded. Latest API/E2E result **Pass**, prior API-REV-002 Pass/96% preserved in revision record. Reviewed Large/High route remains to Code Reviewer for an explicit API-REV-003 no-durable-delta review/route decision. CRR-003 already passed the two API-REV-001 durable updates and CRR-004 marked API-REV-002 Not Applicable; no new test-code change needs structural review.

## API-REV-004 investigation — user-reported Electron Claude SDK price missing (2026-09-23)
Trigger: user screenshot of current Delivery Electron build shows `claude_agent_sdk`, latest model `claude-opus-5-5[1m]`, three usage reports, `Price missing`, `model_not_found`; adjacent Codex `gpt-6-sol` shows a complete estimate. Prior API-REV-003 result/confidence is Pass/97% for its approved AC-001–010 scope; this new screenshot is not overridden by that result. User asks why and requests SDK-returned-model verification.

Boundary: Claude SDK terminal `modelUsage` / usage event -> server `TokenPriceConfigProvider` -> shared strict catalog lookup -> persisted Token Meter/renderer. This newly observed web-equivalent desktop path was not covered by the previous direct-API Opus price or generic Claude SDK smoke. Existing `claude-session-token-usage.test.ts` and `token-price-config-provider.test.ts` are Still Valid for their current assertions but **Needs Update** for a cross-boundary suffixed SDK identifier scenario if behavior is approved. The user screenshot is real Electron evidence; no shell-specific feature is implicated. No existing tests are stale or should be removed.

Source evidence before execution: `resolveModelUsageRecord` accepts SDK `modelUsage` object key and `buildClaudeTokenUsageEvent` emits it as `model_identifier`/`model_value` when no explicit result model exists; `TokenPriceConfigProvider.resolvePolicy` forwards that exact ID; shared `findModel` uses exact equality against catalog `claude-opus-5-5`. The screenshot's `model_not_found` is consistent with that path. It does not alone establish which raw SDK field supplied the suffix, whether `[1m]` is always identical in billing to the base API model, or whether subscription-backed Claude SDK should display API-price estimates. Approved REQ-003/AC-005 specifically covers new **direct-API** Standard prices; no approved SDK-variant pricing policy is explicit. Preserve truthful missing-price semantics until Solution Designer decides intended behavior.

Plan and case IDs initialized before execution: API-C09 deterministic SDK-shaped result -> event -> real pricing resolver (suffixed versus exact ID); API-C10 one small subscription-backed live Claude SDK Opus turn or model listing, logging only model IDs/usage shape, no prompts/response text/secrets, if local auth remains usable. No new paid Anthropic direct API call. Temporary probes only; no production or durable test edits before upstream behavior approval. Browser not selected because it would exercise display of the same backend missing policy and user already supplied direct Electron screenshot. If mismatch confirmed, classify and route with clear current-scope versus newly requested behavior distinction.

### API-REV-004 diagnostic outcome and upstream decision
- API-C09 no-key real pricing resolver: 1/1 probe passed. SDK-shaped terminal `modelUsage` with `claude-opus-5-5[1m]` emitted the suffixed model and resolved `missing/model_not_found`; exact `claude-opus-5-5` resolved `trusted`. This reproduces the screenshot's status and explains why Codex exact `gpt-6-sol` is priced. It does not establish that a suffix should automatically inherit API Standard pricing.
- API-C10 actual locally authenticated SDK discovery listed `opus[1m]` among five advertised IDs. One tiny subscription-backed `opus[1m]` turn returned no top-level model, but `modelUsage` contained **two** keys: `claude-haiku-4-5-20251001` and `claude-opus-5-5[1m]`. The current usage event selected the first (Haiku) for that run because `resolveModelUsageRecord` chooses first object entry when `input.model=opus[1m]` is not a key. The user's screen showing Opus variant is another actual result, but raw field/order for that specific session was not inspected. Model attribution may be wrong for mixed-model SDK turns, independently of price mapping.
- Existing coverage decision: both current Claude usage and price-policy tests remain valid but do not cover variant/mixed-model SDK usage. **Escalate**, rather than add durable expected-price assertions or change production lookup, because approved SR-005 explicitly keeps Claude Agent SDK catalog independent and says it must not consume the static Opus row. REQ-003/AC-005 price rates are for direct API; subscription-backed SDK meter semantics, variant-rate equivalence, mixed-model allocation, and historical snapshot policy are not approved. Do not silently strip `[1m]`, price Haiku usage as Opus, or rewrite stored snapshots.
- Preliminary classification: **Design Impact / Requirement Gap**, not an implementation failure against AC-001–010. The current user's Token Meter is visibly incomplete; the prior API-REV-003 Pass is not a claim of correctness for this newly identified SDK variant. Ask Solution Designer to investigate and obtain approval for expected Claude SDK cost presentation and mixed-model accounting, then design a bounded implementation/test package. Browser/Electron rerun is unnecessary to establish cause because the user provided an actual Electron screenshot, and both real SDK and pricing resolver boundaries were executed. Follow-up validation should add durable mixed-key and suffixed-ID tests, plus a renderer/API journey after the approved change.

## API-REV-005 investigation — CRR-007 Pass, current SR-011/SR-012 Claude SDK accounting
Trigger: CRR-007 renewed source Pass for IR-004 `c5f31df45`, Approved SR-011 requirements, SR-012 design with SR-013 evidence, ARCH-REV-008. Historical API-REV-001–003 Pass/97% is old AC-001–010 scope; API-REV-004 is a diagnostic Design Impact/Not Scored, not a successful new-scope baseline. Re-read requirements, investigation/revision history, reviewed design/review record, implementation handoff/revision, source review/revision, and existing API/E2E records. Cumulative classification Large/High, reviewed route.

Current acceptance mapping: AC-011 selected `opus[1m]` -> exact SDK raw `claude-opus-5-5[1m]` / SDK canonical `claude-opus-5-5`, configured Anthropic price, raw detail, API-equivalent/not-subscription label, price-missing fail-close. AC-012 mixed Haiku+Opus/map order selected-only usage and hidden unselected model; no first-key fallback. AC-013 cumulative/create-resume/reset/retry/selection switch, all-model private checkpoints, exact 5m/1h only on four-dimension reconciliation, otherwise flagged 1h approximation/partial. AC-014 nullable SQL migration, startup readiness, old-null/historical price snapshots, no backfill. Preserve AC-001–010 direct/Codex/SDK regression scope. I-44 command guard remains unverified and must not be claimed. IR-004 duplicate-decoder assertion is inaccurate; do not claim it. No released interim duplicate persisted state established, so no coverage expectation for speculative repair.

Changed boundaries: active Claude SDK external query metadata and terminal result; sanitizer/typed event/context; transactional token fold/pricing; nullable SQLite migration/startup admission; GraphQL/stream contracts; Nuxt renderer Token Meter. Electron shell itself unchanged; browser-equivalent UI preferred, not user's running Electron process. Project instructions: root README/pnpm workspace; `autobyteus-server-ts/AGENTS.md` narrow `vitest run --no-watch`; `autobyteus-web/AGENTS.md` recommends `test:nuxt --run`; web README documents separate backend/Nuxt proxy via `BACKEND_NODE_BASE_URL`, packaged Electron loopback but do not use user's process. Existing generated worktree contracts/dist from other rounds must not be deleted. Isolate SQLite and browser test state, stop only self-owned services. `corepack pnpm` available; bare pnpm not on PATH.

Coverage inventory: current `claude-sdk-model-usage-reconciler.test.ts`, session-token-usage, selected binding/client, accumulator, price provider/calculator, startup readiness, SQL repository/old-null, GraphQL token usage E2Es, team stream transport and web Token Meter/store component tests are **Still Valid** for their assertions; check focused current-scope suites independently. AC-011–014 cross-boundary integration is incomplete: existing focused tests do not alone prove SDK terminal -> SQL -> GraphQL/stream -> browser Token Meter. **Add Durable Coverage** for deterministic selected mixed-model end-to-end if a supported test seam permits; **Use Temporary Executable Probe Only** for entitlement-sensitive tiny live SDK identity/usage and browser journey if test harness is too environment-specific. No stale coverage/removal currently identified. An existing old Codex opt-in test still uses outdated `submitInput`; preserve prior actual per-model probe evidence rather than mislabel that test as current success. User's screenshot proves old UI behavior only, not this post-IR-004 build.

Cases initialized before execution in canonical ledger: C11 focused server selected SDK parser/fold/pricing and old direct regression; C12 real isolated SQLite migration/startup/read/write/history; C13 GraphQL/stream and web component/store, plus narrow direct/Codex regressions; C14 tiny live SDK selected Opus turn with secret-safe IDs only; C15 browser-equivalent Token Meter journey on current build if safely runnable, otherwise report exact blocker and DOM contract evidence. Confidence scorecard and broader-validation decision pending repository checks. One tiny subscription-backed turn at most, no bulk Anthropic direct API calls. Exact live success cannot be inferred from mocked tests.

### API-REV-005 coverage update after C11–C13 repository execution
Eight focused server suites 70/70 passed; six SQL/startup suites 29/29 passed; server full build passed; isolated built-server restart/GraphQL 1/1 passed. Web Token Meter/store 21/21 passed; server GraphQL/stream 9/9 passed. Gap confirmed: existing suites had no single selected SDK terminal result→real configured policy→SQLite→GraphQL test. Added durable `autobyteus-server-ts/tests/e2e/token-usage/claude-sdk-selected-model-graphql.e2e.test.ts`, 1/1 passed. It uses mixed Haiku+Opus, exact raw/canonical, trusted configured rates rather than SDK USD, two cumulative turns, exact then mismatched/flagged 1h cache, idempotency, SQL private checkpoint and public no-Haiku summary. No durable tests removed or replaced. Existing tests remain Still Valid.

Provisional post-repository confidence: AC proof 90%; changed boundary directness 90%; realism/mock gap 75% (real SDK current build pending); environment/identity/fixture fidelity 90% (isolated SQL/live auth pending); failure/lifecycle/recovery 90%; browser/user surface 75% (rendered Vue component only, no browser); durable regression relevance 95%. Mean 86%; below Pass gate. Broader validation **Required**: one tiny live SDK selected query and browser-equivalent meter journey, not bulk paid tests or user Electron process.

### API-REV-005 final evidence and broader-validation gate
C14 one tiny actual locally authenticated CLI-default Claude Agent SDK `opus[1m]` turn passed: active query uniquely resolved raw `claude-opus-5-5[1m]`; terminal result contained Haiku and Opus; production sanitizer selected canonical Opus; actual SQL accumulator produced `estimated` from `autobyteus_model_catalog` with no assumption flag because the main-loop split reconciled. No direct API key loaded/imported, no response text or secret logged, no tool/command safety claim. C15 browser-equivalent current Nuxt settings statistics probe passed all nine journeys using its owned isolated backend/DB/Chromium. The browser fixture is generic settings statistics, **not** selected Claude workspace meter. Selected meter itself is covered by rendered Vue component assertions, strict stream DTO→store assertion, and new real-pricing/SQL/GraphQL E2E; a single full browser journey combining live selected SDK and meter was not run. Browser probe stopped its own processes and removed temp DB. `guard:web-boundary`, direct shared contracts 30/30, GPT SQL/GraphQL 6/6 and opt-in live Codex catalog/GraphQL 1/1 pass. Current per-model Codex turns were not rerun; API-C05 historical real Astra/Sol/Luna result remains earlier-scope evidence, and IR-003/004 did not edit Codex turn transport.

Durable coverage final: added server selected SDK result→configured price→SQL→GraphQL E2E (1/1 final); updated web stream DTO→store test (10/10 final). First two web test attempts failed due incorrect new test fixture/API names, then were corrected before final run; not product failures. No durable removals. Temporary live SDK source/log only in ticket evidence. `git diff --check` passes. No frontend whole-project typecheck was claimed; prior IR-003 attempts hit host memory limits and this round used targeted Nuxt tests, web guard and real browser dev build.

Final confidence scorecard (seven applicable categories, arithmetic mean 95.1% → **95%**): requirement/AC proof **98%** (AC-011–014 direct mixed/cumulative/missing/SQL evidence; no combined live browser); changed-boundary directness **98%** (actual active SDK query through selected binding/event/configured SQL plus new GraphQL test); cross-boundary realism/mock gap **95%** (real SDK, SQLite, GraphQL, current browser, but separately exercised selected UI); environment/configuration/identity/fixture fidelity **95%** (CLI-default live auth, isolated SQL/browser services, no key import); failure/edge/lifecycle/recovery **95%** (reset/retry/legacy/missing gate/approximation suite and restart); user-surface/browser/Electron shell **90%** (selected rendered Vue/stream DTO and generic real browser, but no current selected full-browser or shell run); durable regression relevance **95%** (two narrow new/updated tests across API and stream mapper, plus old suites). No category below 90%, no failing critical AC in approved form. Broader validation **Required and completed** via live SDK and isolated browser. Residual is bounded: actual user Electron build verification remains Delivery/user gate; command-level guard I-44 unverified; no selected live-SDK-to-browser single journey or whole-web typecheck. These are not silently claimed. Current API/E2E outcome **Pass / 95%** for reviewed SR-011/SR-012 after CRR-007, pending proportional durable test-code review.

## API-REV-006 investigation — CRR-009 Pass, Approved SR-014/AC-015 context meter correction
Trigger: CRR-009 renewed source Pass on IR-005 `f04c4389c` under Approved SR-014, DS-018/ARCH-REV-009; cumulative Large/High. Prior API-REV-005 Pass/95%, CRR-008/DR-007 are earlier AC-011–014 scope; this is the first current AC-015 validation. Re-read cumulative requirements/history, design/review, implementation/review and existing API/E2E artifacts. Source delta is limited to Claude result event prompt/capacity/percent and same-latest-record historical run-summary projection; no changed SQL schema, price fold, DTO/UI code or Codex transport.

AC-015 boundary and planned evidence: selected SDK terminal `modelUsage` raw row with safe positive `contextWindow` + complete main-loop input/cache read/cache create → numeric event fields; SQL current record; GraphQL and stream DTO; Nuxt Token Meter known-capacity card/progress. Existing null percentage with same-record valid prompt/capacity must derive only on read, without SQL mutation. Missing/zero/unsafe capacity remains unavailable; no model-name 1M assumption, cross-record stitching, Haiku-first lookup or Codex recalc. Preserve selected Opus pricing/token totals and Codex 10,836/258,400≈4.2% stored path. No raw payload/secret exposure.

Coverage inventory: `claude-session-token-usage.test.ts` (current 5+ new cases), `claude-sdk-context-summary.test.ts` (new source-owned same-record/unsafe/Codex), API-REV-005 durable selected SDK event→SQL→GraphQL E2E, web `TokenUsageMeterPanel.spec.ts`, web store stream mapper, generic token browser probe and direct/Codex tests are **Still Valid**. The source-owned tests cover math but the durable API E2E and rendered selected Claude meter lack exact 22,135/1M assertion: **Update Durable Coverage** in existing API E2E and web component/store test rather than introducing a second parallel harness. No stale test/removal identified. Project commands and environment follow prior investigation: `corepack pnpm`, server `vitest run --no-watch`, web `test:nuxt --run`; isolated SQLite and browser, no user Electron interference. One tiny CLI-auth SDK query may improve current live result-shape evidence; no bulk paid API suite, no key import.

Initialize ledger before execution: C16 focused event/read projection and new API SQL/GraphQL known/historical null/unknown; C17 web stream mapper/Token Meter rendered 2.21% and unavailable; C18 one tiny live SDK selected Opus prompt/capacity/percent; C19 Codex/pricing regressions and current generic browser or targeted selected browser if safe. Browser preferable for web-equivalent behavior, but a generic browser run does not prove selected context; record that distinction. Post-repository scorecard and broader-validation decision pending.

### API-REV-006 repository and broader-validation reconciliation
C16 server current-scope 3 files/11 tests passed: existing source event/read tests plus API-owned selected event→SQLite→GraphQL current and old-null historical record case. Current event and public summary show 22,135/1,000,000/2.2135%; SQL percent was set null, GraphQL still derived 2.2135% without mutation or cost change. Source tests also cover missing/zero/unsafe capacity, same-record lineage and stored Codex 4.2% preservation. C17 web 2 files/23 tests passed after durable updates: strict stream DTO/store carries all three fields; Token Meter rendered component shows 22,135 / 1,000,000, visible `2.2%` at its unchanged one-decimal precision, exact bar width 2.2135%, no unavailable branch, price unchanged. `2.2%` is the existing display rounding of the authoritative 2.2135%; not a change to the percentage math or label precision. C18 one tiny actual CLI-default subscription SDK Opus query passed: selected raw ID `claude-opus-5-5[1m]`, Haiku+Opus map, prompt 16,619, selected capacity 1,000,000, event and stored summary 1.6619%; configured price still estimated. First temporary probe syntax typo stopped before any SDK call, corrected. No secret/prompt response logged or env key imported. C19 current server build, 28/28 Codex/catalog/GPT regression assertions, web guard and isolated Chromium generic statistics nine journeys passed. Browser process/DB cleanup confirmed; current user's Electron was not touched. No whole-web typecheck or I-44 command guard claimed.

Final confidence (seven applicable categories, arithmetic mean **95%**): requirement/AC proof 98% (exact current/historical/unknown/Codex), changed-boundary directness 98% (real SDK→event→SQL and deterministic SQL→GraphQL/DTO/UI), cross-boundary realism/mock gap 95% (real provider and browser; selected full-browser not one journey), environment/identity/fixture fidelity 95% (CLI-default auth, isolated SQL/browser), failure/edge/lifecycle 95% (old null, unsafe/unknown, no mutation, Codex preserved), user-surface/browser/shell 90% (selected rendered component and generic real browser; current Electron package still requires rebuild/user verification), durable regression quality 95% (three focused updated paths). Mean 95.1% rounded 95%, none below 90%. Broader validation **Required and completed** via one bounded live query and isolated browser. The selected live SDK-to-current Electron visual journey is an explicit Delivery/user gate, not silently counted. Current API/E2E result **Pass / 95%** on AC-015 plus retained AC-011–014. No durable removals. C16/17 tests are ready for proportional Code Reviewer test-code review under cumulative Large/High route.
