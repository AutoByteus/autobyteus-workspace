# Investigation — TEAM-PACKAGE-READ-20260915-001
## Bootstrap
New isolated workspace /Users/normy/autobyteus_org/autobyteus-worktrees/tolerant-flat-team-package-reading, branch codex/tolerant-flat-team-package-reading. Fresh origin/requirements/flat-agent-organization-model at c95ef93f8c9042c2174b814c205f00173b816004; eventual same unreleased feature-base target, NOT personal. Worktree creation completed, no previous finalized ticket reopened.
External package source /Users/normy/autobyteus_org/autobyteus-agents at f5534cf5eda505ade3dc5493001991c4970c823b; existing dirty .codex/skills/software-engineering-workflow-skill left untouched. Read external AGENTS.md and server AGENTS.md. Inspection only; no external definition rewrites, user-server import/reload/runtime/reset, tests or implementation performed.
## Intake / prior evidence
User explicitly wants parse→extract consumed fields→validate required values/references, not exact input-key equality. Extra refType:'agent' should not reject a flat package. Prior read-only inventory found12 of14 top-level configs flat,2 containing nested Teams; all14 missing defaultLaunchConfig. Current exact Team parser rejects missing top-level key before extra member refType. Previous explanation saved outside archived repositories; this new ticket now owns canonical investigation. Need determine topology validation behavior when unused refType is omitted, including kind collisions, without silently treating Team refs as Agents.

## INV-001 — Input shape versus usable definition
Current Team parser requires exact top-level fields and exact memberName/ref/refScope member fields; missing defaultLaunchConfig in all14 current configs fails before refType. Loader/admission both invoke parser. Individual Agent config reader already projects known optional fields through normalizeAgentConfigRecord; this proves an existing selective-reading example, not authorization for blanket global parser changes. Shared handoff normalizer also checks exact entry keys; any future tolerant Team read must define consumed-field coverage without loosening runtime/protocol/Org contracts indiscriminately.
## INV-002 — Actual supplied flat/nested inventory
package-inventory.json records exact paths/SHA256 and direct reference path existence. Of14 top-level Teams,12 have only Agent references and all their expected scoped agent.md files exist. Not complete markdown/config/handoff/model validation or runtime import proof. Two nested packages: software-development-department has department_head Agent plus shared product-design-prototyping-team/software-engineering-team; northstar-operating-company has9 direct Agents plus6 team_local child Teams engineering-org/product-org/marketing-org/revenue-org/operations-org/finance-people-org. All8 nested reference targets exist as Team directories and not as Agent directories at the expected scopes. No agent-orgs directory supplied; no auto Org conversion requested. Deeper nested package inventory not needed to establish these top-level families are not flat.
## INV-003 — What ignoring refType would actually do
FlatTeamDefinitionResolver.resolve maps every canonical member to exact scoped Agent ID, then lookup.getAgentById; DefinitionAdmissionService.scan supplies getFreshAgentDefinitionById and runs assertValidFlatTeamDefinition. Missing Agent triggers COLLABORATION_TARGET_NOT_FOUND, mapped to DEFINITION_REFERENCE_UNRESOLVED/unavailable per definition. TeamRunService launch/validation requires admission.requireAvailable, preventing runtime launch of unavailable Teams. Resolver does not search Team namespace, expand members or convert Org. Handoff compiler also requires one direct Agent segment for Team-local endpoints; department nested handoff endpoints cannot silently preserve nested behavior.
Thus after correcting parse shape/default mismatch, supplied nested packages still fail actual Agent-reference validation, without needing to reject the mere presence of refType. Raw parse into a Team-shaped object is not proof it is an available executable Team. If some hypothetical other package has an Agent and a Team with identical ref IDs, ignoring refType means the scoped Agent namespace governs; the ignored field alone cannot establish rejection. No such matching Agent path exists in these8 actual nested references. Do not claim ignoring metadata universally preserves its old discriminator meaning, invent collision policy or nested support.
## INV-004 — Package registration is distinct from individual admission
AgentPackageService.importLocalPathPackage validates directory root, records/adds package root and refreshes catalogs; root summary validates agents/agent-teams/applications directories, not each Team's semantic topology. FileAgentTeamDefinitionProvider.getAll skips recognized malformed definitions; admission reports unavailable items independently. Therefore user's 'import blocked' can mean package registered but Teams absent/unavailable. No exact user's UI error or server invocation captured. Future fix must preserve valid sibling availability in mixed packages, not require all nested parents to become valid or fabricate Org definitions. Package root containing no Org definitions should create no Org.
## Current instruction / outcome
User interrupted to request analysis first. Worktree/ticket bootstrap already completed, only task-local docs/inventory written. Requirements stay Draft; no target architecture/implementation or runtime test executed. Findings answer nested-package question; next scoped requirements refinement/design must respect approval workflow and user's analysis-first instruction. No external package mutation. Supporting source locations: agent-team-definition/providers/agent-team-definition-config.ts; services/flat-team-definition-resolver.ts; utils/scoped-team-member-resolution.ts; collaboration-definition-admission/services/definition-admission-service.ts; agent-collaboration/definition/collaboration-handoff-compiler.ts; agent-team-execution/services/team-run-service.ts; agent-packages/services/agent-package-service.ts and utils/package-root-summary.ts.

## INV-005 — Personal defaultLaunchConfig absence confirmed
User clarified typo means explicit null, and requests original-personal comparison. Read-only git show origin/personal5645b49d6: agent-team-definition/providers/team-definition-config.ts declares defaultLaunchConfig optional; normalizeTeamConfigRecord calls normalizeDefaultLaunchConfig(value?.defaultLaunchConfig). launch-preferences/default-launch-config.ts returns null for absent/non-object value. File provider readTeamFromSourcePaths normalizes then uses config.defaultLaunchConfig ?? null. Thus normal external Team read in this pinned personal revision accepts absence; serializer may emit null but reader does not require it. Current flat parser exact root keys requires property, a stricter new read contract. User's intended absence/null equivalence is explicit; present malformed data validation still to specify, not automatic reuse of older permissive invalid-value normalization. Analysis only, no application edits/tests.

## INV-006 — Northstar visibility after dropping unused discriminator
User asks whether Northstar would disappear from list. Inspected API agent-team-definition.ts:agentTeamDefinitions filters admission.scan to available Team definitions; agentTeamDefinition(id) returns null when admission fails. Team launch requires available admission. Thus after syntax/default optionality correction, Northstar's six missing scoped Agent targets make it unavailable and omitted from normal Team catalog, not partially loaded with only nine Agents. definitionAdmissionDiagnostics query exists for unavailable reasons (no claim of verified user-facing diagnostic UI). Removing refType alone on unchanged parser still leaves absent defaultLaunchConfig failure. No physical package deletion, automatic Org generation or promotion of child Teams inferred. Source-level prediction, not UI execution.

## INV-007 — Architecture investigation after scope approval (2026-09-15)
Read-only commands in isolated W at unchanged c95ef93f8: rg -n 'parseAgentTeamDefinitionConfig' autobyteus-server-ts/src; cat codec/legacy selector; sed provider/admission/application/migration ranges; inspect handoff normalizer and test filenames. No executable tests/runtime/actions performed.
- providers/agent-team-definition-config.ts:43–147 owns object/value validation and strict canonical key sets; buildAgentTeamDefinitionConfig:149+ emits canonical five root fields. parseDefaultLaunchConfig requires three supported child fields when object supplied; llmConfig is an intentionally open provider dictionary.
- providers/file-agent-team-definition-provider.ts:81–84 validatePackage is **write-transaction** validation, not ordinary external import read. readDefinition:88–117 is normal read from discovered sources. content:124+ serializes canonical output. Thus only readDefinition should switch to a tolerant read boundary; write callback stays strict.
- collaboration-definition-admission/services/definition-admission-service.ts:116–137 predecodes Team raw JSON before calling fresh provider; must use same tolerant Team reader or still rejects before provider. scan retains real scoped Agent validation. Org branch stays unchanged.
- application-bundles/providers/file-application-bundle-provider.ts:318 uses codec to read application-owned Team resources before local Agent checks. It is a normal read consumer too, not canonical-output assertion; same input projection avoids competing Team read contracts. Application schema/manifest/ownership validation unchanged.
- app-data-migrations/legacy/collaboration-definition-authoring-transition.ts:18–22 selectTeamAuthoringCandidate strips known version, invokes STRICT parser, returns RAW candidate (not normalized result). migrations/agent-org-flat-team-families-v1/...:156–183 tries this first and continues as current; only on rejection inspects legacy refType. Broadly relaxing shared parser would let old nested superset pass as current candidate and bypass established migration classification. This is a source-demonstrated coupling risk, not observed runtime damage. New ticket does not run/change migration or add conversion acceptance.
- collaboration-definition-authoring-shape-app-data-migration.ts:50 and flat-team migration write/verify sites use strict parser as canonical verification. Preserve these callers and parser semantics.
- agent-collaboration/domain/collaboration-handoff.ts: normalizeCollaborationHandoffs accepts null/undefined as empty; entry requires exactly from,to,rules. Input Team projection may drop unused handoff-entry metadata before it; do NOT relax shared normalizer for Org/runtime. Existing malformed-handoff error typing/catch behavior is not silently redesigned.
- Relevant durable seams present: tests/unit/collaboration-definition-admission/{definition-authoring,definition-admission-service}.test.ts; tests/unit/application-bundles/file-application-bundle-provider.test.ts; tests/unit/app-data-migrations/collaboration-definition-authoring-shape.test.ts; tests/e2e/agent-team-definitions/agent-team-definitions-graphql.e2e.test.ts. No result inferred from file presence.

## INV-008 — Design decision evidence / uncertainty boundaries
One version-agnostic Team input projection can select only known keys at root/member/handoff/default-config levels and then reuse the existing canonical validator. Only absent root defaultLaunchConfig gets null; no other required fields fabricated. llmConfig contents remain opaque/preserved, not key-filtered. The normal read has one policy for all Team source placements; no special refType/schemaVersion/legacy fallback. Strict canonical writer/migration validation remains a different responsibility, not a second external-reader mode.
Representative external flat references have expected scoped Agent files; actual full markdown/handoff/default semantics and frontend registration remain unexecuted here. Nested parents remain unavailable by real target validation, not mere ignored refType. No claim every personal revision behaved identically: comparison pins personal5645b49d6. No new conversion acceptance: the prior draft's converted-Org scenario is withdrawn by SR-004.

## Canonical Supplemental Inventory / Current State
package-inventory.json — Solution Designer-owned read-only paths/hashes/reference existence; scope14 top-level Teams, REQ-001/003; current evidence only, no behavior-defining approval. bootstrap-handoff.md — historical SR-001/002 analysis-only result, superseded for current status by requirements SR-004 and design/handoff SR-005. Earlier external analysis receipt is provenance, not competing authority. No Product/independent review/implementation/API artifacts yet. Above initial 'Current instruction / outcome' paragraph describes the historical analysis-first round; current requirements are Approved and architecture proceeds on SR-004.

## INV-009 — User requests removing feature definition migration (2026-09-15)
Rechecked source, no runtime action. app-data-migrations/app-data-migration-registry.ts imports/instantiates BOTH AgentOrgFlatTeamFamiliesV1AppDataMigration and CollaborationDefinitionAuthoringShapeAppDataMigration (lines46–49). Thus these are registered startup paths, not merely unused migration files. The family migration execute:141–148 calls migrateDefinitions and cleanupDefinitionTargets before independent locator/runtime/Org-run-cleanup/history methods. migrateDefinitions scans getAgentTeamsDir, rewrites flat config, or writes Org config/md, moves nested parent to getAgentOrgsDir and deletes parent Team authoring files. cleanupDefinitionTargets:340+ also rewrites Org/owned-Team config and deletes retired authoring files. The separate authoring-shape migration is definition-only and registered; it normalizes version/refScope and writes/rereads owned definition files. These are distinct from runtime execution trees.
Runtime tree conversion helper constructs definition IDs/names from stored tree fields; initial search found no file-definition read in runtime-tree-target or context-file-locator-transition. This is preliminary dependency evidence, NOT proof all continuation paths are safe when definitions are not converted. History-first-message migration explicitly depends on the family migration ID, so deleting the entire family migration would conflate runtime and authoring responsibilities and break a registered prerequisite. Preserve runtime/history migration while removing definition-only logic; exact safe removal surface and downstream continuation contracts still require design investigation. No automatic reversal of already-migrated packages, data reset or external maintainer edits authorized.
User explicitly changes scope: remove this automatic authoring conversion because feature is unreleased and maintainers own package changes. This supersedes DS-001's decision to leave definition migrations unchanged; their existence no longer justifies preserving that coupling. Strict ordinary writer checks are a separate remaining consumer to reassess. Read-only command with incorrectly guessed services/app-data-migration-registry.ts failed; corrected actual registry path inspected successfully. No missing-source claim, no implementation/test results.

## INV-010 — Removal boundary and surviving runtime dependencies
2026-09-15 read-only architecture investigation after user reaffirmed software owns history, maintainers own definitions, and directed “Now let's work on the sticky now.” W/branch unchanged, HEAD c95ef93f8, only ticket docs untracked. No application edits or executable/runtime tests.
Commands: rg migration helper/import/ID usages across src and tests; inspect registry, runner, family migration remainder, Org run manager and actual execution-scope builder. Incorrect guessed service/factory filenames returned no file; actual app-data-migration-runner.ts and agent-org-execution-scope-builder.ts discovered and inspected instead.
- app-data-migrations/app-data-migration-registry.ts:46–49 registers family migration then authoring-only migration. Remove only authoring-only registration; retain family migration ID/order because agent-org-history-first-message-summary-v1 prerequisite references it. No other production reference to authoring-only migration ID was found.
- app-data-migration-runner.ts:48–78 enumerates registered definitions for statuses/pending work and skips successful records. runMigration:82 rejects unknown IDs. Thus removing authoring registration leaves old ledger entries inert under current runner; no deleting ledger records, no reset or forced rerun justified. Existing family ID must stay stable, preserving prior successful runtime migration record. Failed family attempts retry only remaining runtime work under current runner; no definition inspection needed.
- Family migration removable regions: migrateDefinitions, planOrgDefinition, cleanupDefinitionTargets; LegacyMember/LegacyTeamConfig/OwnedTeamDefinitionPlan/OrgDefinitionPlan; record/exact/string/legacyConfig/teamTarget/orgTarget; definition-only atomicText/readJson/writeDefinition/verifyDefinition; definition parse/inventory/ID-builder/crypto/deepEqual imports and definition-only report variants. CLEANED_CURRENT_ORG is also used by runtime cleanup and MUST stay. writeJson still serves runtime writes; remove its default file='definition' and require existing explicit runtime tags. config.getBaseUrl remains used by context locator transition, so do not indiscriminately delete config injection.
- legacy/collaboration-definition-authoring-transition.ts and legacy/owned-definition-package-inventory.ts have only the two removed definition migration consumers in production. Remove them after consumer deletion, not the ordinary DefinitionPackageTransaction subsystem imported by inventory. Existing authoring transaction recovery/write safeguards remain independent.
- Family runtime path retains locator prepare/validate, migrateRuntimeRoots, cleanupOrgTargets, migrateHistoryIndexes, validateCompleteOrgRunPackage, atomic writer and result counters. Validates actual execution tree/task/communication/locator packages, not authored definition files. agent-org-runtime-tree-target.ts obtains definition IDs/names from persisted tree snapshots. This is source evidence of removal separation, not live migration proof.
- AgentOrgRunManager.restore:91–106 loads/repairs persisted Org state then materializes with activationMode restore. AgentOrgExecutionScopeBuilder:60–66 calls resolveFreshInstruction only for fresh mode; restore uses null. Its only orgDefinitions/teamDefinitions lookups at175/180 serve fresh instruction. Thus inspected root restore doesn't require synthesizing a current Org definition first. Fresh launch still requires current available authored definition; not guaranteed for invalid nested packages. No broad claim about every provider/configuration continuation; retain real validation obligation.
- tests/unit/app-data-migrations/agent-org-flat-team-families-v1-app-data-migration.test.ts combines definition tests133–290 with runtime cohorts291+: flat zero-write, organization-like runtime conversion/history, predecessor V1/V2, unknown runtime member rejection, sidecar mismatch, family collision and runtime cleanup. Delete obsolete definition expectations, retain runtime cases and fixture helpers they actually use. authoring-shape.test.ts contains registry/runner coverage amid removed conversion tests; replace relevant registration/non-mutation coverage rather than indiscriminately losing startup preservation checks.

## INV-011 — Current design rationale after removal
Strict parser no longer needs to protect definition migration consumers because those are deleted. Surviving canonical writer builder and transactional validatePackage still assert output completeness: retaining a small normal input projection beside that validator is justified by these current responsibilities, not backward compatibility. Four read-policy production files unchanged from prior design scope; add bounded deletion in migration owner/registry and delete three migration-only source files. Existing runtime persistence migration and definition ownership behavior now explicitly in scope, so revised classification must account for them, not carry forward Small/Low automatically. No new migration, schema, ledger reset, definition converter or provider activation introduced.
