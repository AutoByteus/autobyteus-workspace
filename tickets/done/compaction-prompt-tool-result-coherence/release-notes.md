## Improvements
- Improved memory compaction so tool calls and their results stay paired in summaries, including clearer call identifiers for tool results.
- Refined compacted conversation wording to read like natural resume context instead of an internal transcript dump.

## Fixes
- Fixed built-in Memory Compactor prompt seeding so newly installed definitions use the updated context-summary language while preserving user-edited compactor instructions.
