# Navigation investigation — scope and attribution

Authority: RER-002 / AD-REV-001 CD-003, SV-012–016 / ARCH-REV-001 AR-PREM-003. New follow-up ticket only.

## Demonstrated writer: late explicit selection (SCN-004)
`AppLeftPanel -> WorkspaceAgentRunsTreePanel -> useWorkspaceHistorySelectionActions -> runHistoryStore.selectTreeRun -> inspectTeamMemberForStore -> inspectMountedTeamMember -> ensureAuthoritativeTeamMemberProjection` is a real selecting path. Previously the held projection could commit conversation/activity and focus, then select the prior Team and emit `run-selected`; AppLeftPanel's normal shell handler pushed bare `/workspace`. It was not enough to test the parent after the lower commit.

The durable composition uses actual installed Apollo/Link and document, real Pinia, hydration/focus, memory router and AppLeftPanel's real shell listener. Only the child test harness buttons replace its row rendering; selecting promises are not mocked. HEAD production source with the new regression fails because the Org query vanishes after the held Team result completes (`before-selection.log`). Fixed-source mounted success/error, cold Org reversal, same-target attempt ownership/current failure/retry, two real execution-link keys, and route leave/Back/Forward cases exercise the approved guard. Lower Agent/cold-Team/recovery tests additionally exercise publication boundaries with controlled hydration dependencies.

## Publication-only preservation, not original-cause closure (SCN-003)
Source inspected: `AgentOrgStreamingService.handleMessage -> AgentOrgExecutionContext.applyEvent` applies exact status/input events to the addressed Agent. Snapshot publication goes through `agentOrgContextsStore.publish -> adoptLocalContexts`, preserving the matched local AgentContext and explicit selection. History projection changes rows, not navigation intent. `AgentOrgWorkspaceView` watches exact route/context and mirrors active/history mode within the same Org. `activeContextStore` derives the Org target from route identity. None of these data publishers begins a selection intent. No global publication suppression was added.

The durable test passes real streamed snapshot/status/input through the existing context/store publisher while recording selection actions, route, exact selected object, prior Team focus and unsent draft. The browser fixture additionally renders the actual history components, AppLeftPanel, adaptive workspace and Org surface, observes three exact status publications at each of 1502 and 390 pixels, and checks a held prior-Team row selection followed by a deliberate Org row choice before releasing the response. Exact center, updated row, same unsent draft and no stale shell navigation survive. Direct/mounted same-root deliberate selection remains effective. HTML, screenshots, selection/publication/route trace and actual chip popup are retained.

Browser fixture uses a controlled Apollo/socket transport, a scoped memory router mirrored into the temporary fixture page query for the app's route-derived facade, fixture-only sidebar framing and supplied data. It is NOT normal provider task creation or independent live acceptance. Early fixture composition required correcting RouterView depth, responsive-layout injection, route-derived facade initialization and attachment timing after stream staging; these were harness changes, not production fixes. Final runs have no pageerror. The initial screenshot files were iterative discovery outputs; only named 1502/390 final outputs support current claims.

**Original publication-only incident cause remains unassigned.** These preservation checks and the reproduced SCN-004 writer neither establish nor repair-by-attribution the original SCN-003 incident. A normal instrumented live task/member ordering, current root/member IDs and repeated publication/explicit leave-return checks remain required under AC-003/004. A newly demonstrated writer needing different ownership must return Design Impact, not be folded into this guard's causal claim. Explicit cross-root unmount/disposal is not claimed to retain an Org draft; the tested retained draft is the still-selected same Org and same-root direct/mounted return.
