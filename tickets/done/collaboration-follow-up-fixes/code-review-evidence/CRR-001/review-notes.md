# Independent review evidence — COLLAB-FOLLOWUP-001 / CRR-001

New ticket only. Source 5710fdd5347bb1b3c464775dd9e32470c88a2ef5; artifact HEAD 270d0d72ec8b2feec2b4699b1687f5caa8707108. Reviewed against RER-002, AD-REV-001 and ARCH-REV-001. Source/test edits: none.

## Forward path witnesses

- SCN-001/007: normal Team/Org Run enters existing managers and `team-root-materializer.ts:93–151` / `agent-org-execution-scope-builder.ts:118–193`. Fresh deferral is selected at callers, not globally in the factory. All configured identities and initial root package remain. `agent-org-root-agent-execution-registry.ts:54–85` separates a registered handle from optional provider activation; `agent-org-team-execution-directory.ts:41–75` publishes the mounted scope after durability. `flat-team-execution-factory.ts:94–115` retains eager default/task/restore behavior. No status-only workaround or recursive configured Team restoration.
- SCN-002: Org human input enters `AgentOrgRun.executeAgentCommandWithExecutionKind`; inter-Agent input enters `deliverLogicalMessage`/`deliverExactAgentMessage`, authorizes initiating identity, resolves exact receiver, uses private published membership and shared communication engine. `agent-org-run.ts:439–455` retains root/address/ID/live-index/published-host checks, adding no public authorization relaxation. Unstarted direct receiver can reach `reserveAgentInput` -> registry -> `ConfiguredAgentExecutionHandle.reserveInput/ensureReady`; planner prepares, binding callback commits tree before candidate publication. Task creation still eagerly prepares/seals/durably publishes before releasing work. Root operation gate and existing termination scope drain/fence the same handles.
- SCN-004: sidebar/history/draft/running/link ingress -> `agentSelectionStore.beginSelectionIntent` -> existing open/inspection owners -> candidate/projection/activity/focus checks -> selection/config/history updates -> guarded outward shell event. The exact same intent is propagated, not newly acquired by each lower call. Team recovery checks at `agentTeamRunStore.replaceFailedTeamStream`'s existing pre-context-commit callback, not only after its asynchronous replacement. Old loading/error cleanup owns the installed attempt; synchronous invalidation precedes next loading installation. Existing identity/activity/stream/fresh-read guards remain.
- SCN-004 route lifecycle: `useWorkspaceRouteSelection` watches actual AgentRun execution-link key; processes a newer link while prior selection awaits; route leave/semantic update invalidates; same-identity active/history mode is excluded. Only a current captured link signature is stripped. Background `openAgentRun`/`openTeamRun` with `selectRun:false` does not acquire selection intent. Org selecting reads carry the guard to staging/publication and retain generation checks.
- SCN-003: `AgentOrgStreamingService` -> exact context apply/staged publication -> `agentOrgContextsStore.publish`/`adoptLocalContexts` -> history rows; `AgentOrgWorkspaceView` derives route target and same-root phase mode. Background publication does not acquire user intent. This is a preservation audit, NOT a causal explanation or acceptance closure of the original publication-only incident. The separate pending explicit Team selection is independently supported SCN-004.
- SCN-005/006: native standalone composer -> `agentRunStore` local append -> PrepareAgentRun -> temporary-ID promotion -> upload finalization -> shared submission finalizer -> normal stream Send -> actual UserMessage computed chip -> existing separate-link opener. `localUserSubmission.ts:60–105` now retains the same reactive UserMessage it appends; final attachment replacement invalidates the actual computed chip without forced remount/reload or a second input. The server/opener/file storage and original persisted association are unchanged.

All 24 production files were reviewed along their relevant callers, current source and diff; 15 changed tests reviewed proportionately. `source-size-audit.json` is the complete file/hash/nonempty/delta inventory. Tests are not subject to production size thresholds.

## Independent executable evidence

`checks.py` and `command-results.json` preserve exact commands, cwd, timestamps and exits. Normal SDK prerequisites built (3/3 exit0); only those initially absent outputs are eligible for cleanup. Server cohort 11 files/56 tests; web cohort 19 files/167 tests: 30 disjoint files/223 tests. The repeated implementation cohort is not additional coverage. Server production TypeScript, web/localization boundary guards and committed source/test whitespace pass. No browser/backend/provider/native/API journey launched by this reviewer.

New durable tests directly exercise actual root builders/factories/package writers/shared readiness with controlled provider candidates; real installed Apollo/Pinia/lower hydration/focus/AppLeftPanel and memory router; actual UserMessage/opener and same reactive context through promotion. Additional unit tests mock their lower boundary deliberately, and do not substitute for these composition tests. Team/Org first-message variants exercise shared helper targets, not native submitter integration. Same-target independent-completion case explicitly changes only the test client's default deduplication context; this does not claim a production Apollo policy change. Current first-submit and selection assertions were inspected, not accepted merely because green.

## Owner evidence / diagnostic limits

- Read baseline red receipts/logs and exact restoration evidence: 2 eager-readiness failures, 1 actual chip-click draft URL, 1 actual late-Team shell navigation. No reviewer baseline swap/replay. Owner original setup/callback/typing failures remain in the cumulative index.
- Read completed owner web production build evidence; no new reviewer web build. Repository-wide pinned Vue typecheck remains FAIL exit2, 386 diagnostics versus 394 baseline. Independently parsed file/code/message/multiplicity of completed logs: 131 production diagnostics each, no new/removed production diagnostic. `typecheck-verification.json`. Line positions alone omitted. This is no-new-production-diagnostics evidence, NOT a passing repository-wide typecheck. Original raw artifact whitespace failure is not erased.
- Two exploratory read-only path lookups found no root AGENTS.md / no guessed workspace mobile-actions path; actual applicable web/server AGENTS.md were read. No test or product failure attributed to these lookup misses.
- No old AORG report/result changed. Old known issues and finalization are provenance only; these three issues are required anew here. No source ownership, task size, confidence or suite volume inherited.

## Direct visual inspection

Reviewer directly viewed all 12 named final owner PNGs (six views at each of 1502×950 and 390×950): unused-direct, unused-mounted, publication, pending-team, retained, final-chip. Exact direct/mounted center labels display gray Offline when unused, addressed lead becomes green Idle, peers remain gray and draft text remains on retained mounted view. Pending narrow navigation shows the Team loading row while Org remains selected; retained view removes that pending label. The final text chip is visible without remount. The matching owner browser script/JSON records actual popup final URL and known controlled bytes; the screenshot alone does not prove HTTP access.

Fixture framing, supplied history/status/attachment data, controlled socket/Apollo/file transport and scoped mirrored memory router are explicit. Some intermediary named screenshots have delayed/missing network-loaded icons; no icon-fidelity verdict. Narrow pending/retained images show the fixture navigation surface, not simultaneous full center. These are not a normal fresh hosted Codex Team/Org or native DeepSeek first-Send run. Original publication-only cause remains unassigned. No cross-root disposed Org draft retention claim.

## Downstream obligations (not waived)

1. Genuine fresh Codex Team AND full Org: correlate root availability, exact configured identities/null bindings, unused runtime non-start, gray Offline, initially unfocused Org, first human/inter-Agent work and preserved task/restore/Stop behavior with ordinary UI/runtime evidence.
2. Original publication-only journey: prior Team then selected Org/task/member and unsent draft, ordinary repeated runtime publications, exact selection/conversation and navigation-writer instrumentation, deliberate leave/return. AC-003 is not closed by controlled SCN-004 or one non-reproduction. A materially different evidenced writer needing new ownership returns Design Impact.
3. Native AutoByteus/DeepSeek V4 Flash: new standalone text upload -> first Send -> real reply -> ACTUAL sent-chip Open immediately -> reopen same input/file/bytes, plus targeted narrow checks. A direct final-URL request or controlled popup cannot replace this. Missing native availability is Not Tested, not Pass or user-vault absence.

No automatic replay of old enormous suite; no new providers/media/native-shell coverage. All existing limitations and current no-migration decision retained.
