import fs from'node:fs/promises';import assert from'node:assert/strict';import{chromium,env,write,exists,eventually,snapshot,checkpoint,dir}from'./lib.mjs';
await eventually(async()=>{const r=await fetch(env.server+'/rest/health');return r.ok},'owned backend health',120000);
await eventually(async()=>{const r=await fetch(env.renderer);return r.ok},'owned renderer HTTP',120000);
const b=await chromium.connectOverCDP(env.cdp);const p=await b.contexts()[0].newPage();await p.setViewportSize({width:1502,height:844});await p.goto(env.renderer,{waitUntil:'domcontentloaded',timeout:120000});await p.evaluate(()=>window.name='COLLAB-API01-main');await p.getByRole('button',{name:'Agents',exact:true}).waitFor({timeout:120000});await snapshot(p,'bootstrap');await write('bootstrap.json',{at:new Date().toISOString(),url:p.url(),body:await p.locator('body').innerText(),ownedContext:true});await checkpoint('SETUP','Completed','Owned backend and real frontend ready; no Run/Send yet','bootstrap.json');await b.close();console.log('Browser user surface ready');
