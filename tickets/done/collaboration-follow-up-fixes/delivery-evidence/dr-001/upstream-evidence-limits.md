# Binding upstream evidence limits — DR-001

Authority: API-REV-001 execution report/case ledger and CRR-001/002. These are
preserved, not rescored or rewritten by Delivery. The copied cumulative index
resolves all 465 incoming references, including old AORG read-only context.

- Current API Pass95.0, all 11 groups, independent30 files/223 tests; counts
  overlap owner/reviewer and are not additive. No historical enormous-suite
  renewal. CRR-002 N/A means zero API-owned durable changes, not no test coverage.
- Current AC003/004 normal publication and explicit navigation Pass does not
  assign the original publication writer or prove CD-003 repaired that original
  incident. AR-PREM-003/SV-015 cause remains UNASSIGNED, no timing-immunity promise.
- Whole Vue typecheck remains FAIL exit2, 131 unchanged production diagnostics.
  No diagnostic suppression or whole-check green. Upstream web production build
  is not an independently rerun API or Delivery build.
- Original nonzero and setup/oracle receipts remain actual observations:
  immediate Team accessible-label initializing settles on same root; echoed
  prompt predicate; unscoped configuration selectors; configured-address URL
  oracle; responsive sidebar observer; explicit history re-selection after bare
  workspace reload; restored-reply punctuation. No replay of accepted inputs.
- Two 390px sidebar locator timeouts remain narrow DOM measurement gaps. Exact
  context/draft/router/wire continued; final row required restoring width. No
  continuous narrow sidebar coverage or cross-root disposed-draft claim.
- Native AutoByteus/DeepSeek is the provider runtime in real Chromium, not an
  Electron-shell launch. Actual immediate chip popup200/original bytes is proved;
  no new media/provider expansion or native image/shell/AppImage outcome follows.
- User declined redundant open_tab repetition. Initial capability inquiry was
  not product acceptance. Initial broad external read-only discovery was stopped
  by its exact owned PID; not a claim it never happened.
- API cleanup used owned normal Stops, credential cleanup via the existing vault,
  checkpoint/VACUUM and a verified sanitized archive. Shared8000/9222 and original
  credential were preserved. Own POSIX root plus three initially absent SDKdist
  and serverdist outputs removed. Preexisting core dist was rebuilt/retained and
  is explicitly NOT byte-identical. API protected8,824 hashes/224 incoming refs;
  Reviewer further manifests and current Delivery hashes remain separately scoped.
- Current TeamV2/OrgV1 directly usable without migration; attachment storage not
  affected. Existing Restore/task preparation may initialize workers and remains
  distinct from fresh-unused configured launch.
- Old AORG remains DONE/read-only, including accepted API38Fail85.6 and all native
  wait/outer-envelope, timing, association, archive/PAX, image-exclusion and data
  inventory limits recorded there. Old IR049 Architecture-owned actual-installation
  BEFORE CUTOVER decision is not waived by current source validation. No reset,
  replay, new migration, backfill, rollout, installation, release or native build
  authorization is inferred.
- No current product finding or new source/architecture failure is manufactured
  from these accepted evidence limits. Ordinary Delivery user verification and
  finalization remain pending, not already satisfied by the upstream handoff.
