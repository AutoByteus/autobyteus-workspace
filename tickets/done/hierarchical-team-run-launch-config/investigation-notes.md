# Hierarchical TeamRun Launch Configuration — Investigation Notes

## Investigation Status

- Bootstrap Status: Complete
- Current Status: SR-013 passed `ARCH-REV-005`; IR-011 implemented its distinct-capability/per-field classifier architecture. CRR-018 then inferred a multiline free-text requirement without tracing a producer, and IR-012 added CR/LF-specific source/tests. API-REV-009 reached that state only by inventing `ordinary_prompt`/`multiline_prompt`, mutating page-only Pinia catalog state, and injecting arbitrary GraphQLJSON into an isolated V2 fixture. CRR-021 rescinds CR-013/MP-CR-010, and API-REV-010 is Pass / 98% for real user paths. ARCH-REV-006 validated SR-014's intended producer-bounded cleanup but failed on AR-001 because the mandatory decommission table omitted the cleanup and the final hard block still covered arbitrary persisted values. SR-015 adds the four removal/retargeting rows and qualifies every relevant absolute design statement while keeping the runtime classifier generic and provenance-free. Architecture re-review is pending; implementation remains blocked.
- Investigation Goal: Trace every current TeamRun launch-configuration surface and establish the hierarchy, persistence, restoration, and UI requirements for treating nested teams as configuration scopes.
- Scope Classification: `Large`
- Scope Classification Rationale: The change crosses frontend types/store/UI/resolution, multiple launch surfaces, GraphQL, topology planning, persisted execution-tree schema, restore, and historical inspection.
- Scope Summary: Hierarchical TeamRun launch configuration; root global plus inherited/custom nested-team scopes plus Agent overrides. No live topology mutation.
- Resolved Design Outcomes:
  1. The immutable Team draft/store owns configuration intent and every exact-address Team workspace authoring/operation state; one topology-aware store transition reconciles both before the launch owner performs any registration, while the pure hierarchy resolver retains precedence/readiness projection and UI consumes derived views only.
  2. The backend accepts a complete Team/Agent hierarchy while one service root-config entrypoint expands root-only surfaces without coordinator inference; the topology planner completes exact validation before it allocates the root, nested-Team, or Agent identities, and application binding consumes the returned root identity.
  3. Runtime, persistence, stream transport, restore, and normal history become V2-only with required configured-Team defaults.
  4. One registered V1-to-V2 migration performs the approved direct-coordinator copy using existing atomic writer/runner/retry facilities and bounded diagnostics.

## Request Context

While bootstrapping Dynamic AgentTeam Runtime, the user observed that the current frontend gives only the root AgentTeam a global launch configuration. Nested teams are displayed as groups and their individual Agent descendants can be overridden, but the nested team itself cannot define a shared configuration. The user elevated this as a higher-priority prerequisite because every team should behave as a unit and a future dynamically added Agent should inherit from its containing TeamRun.

During final Electron verification, the user confirmed that hierarchical runtime/model authoring is correct but rejected the implemented root presentation as unnecessarily noisy. The selected Team definition and the visible root controls already provide the needed context and effective values. The user specifically identified “Root Team defaults”, its duplicate badge, `/`, and the expanded effective summary as non-useful appearance rather than product functionality.

## Environment Discovery / Bootstrap Context

- Project Type: `Git` super-repository
- Task Workspace Root: `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config`
- Task Artifact Folder: `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config/tickets/in-progress/hierarchical-team-run-launch-config`
- Current Branch: `codex/hierarchical-team-run-launch-config`
- Current Worktree / Working Directory: `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config`
- Bootstrap Base Branch: `origin/personal`
- Remote Refresh Result: Recovery reconstructed the ticket over recorded base `52b4be02ea793f2071fe5a63a94664ab25196433`. Delivery later refreshed and integrated `origin/personal@87b1b584592be95b1c8ee076f1d0ab3986a13f18` at merge commit `1f1fb12160c809b41bd4b716dc2fa4f920631f6d`; DR-003 then produced the local Electron verification build. Finalization remains held for user verification.
- Task Branch: `codex/hierarchical-team-run-launch-config`
- Expected Base Branch: `personal`
- Expected Finalization Target: `personal`
- Bootstrap Blockers: None
- Notes For Downstream Agents: This ticket is the prerequisite for the Dynamic AgentTeam Runtime work; that later requirements basis must consume the approved nearest-containing-TeamRun default rather than a root-only assumption.

## Supplemental Task Artifact Inventory

| Artifact Path | Purpose And Scope | Evidence, Context, Or Decision Captured | Core Artifact(s) Supported | Related Requirement / Acceptance-Criteria IDs | Status | Approval Applicability / State | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config/tickets/in-progress/hierarchical-team-run-launch-config/ui-ux-spec.md` | Origin-personal visual baseline, additive nested-Team journeys, editable/stored parity, disclosure states, wireframes, responsive/accessibility rules, and explicit removals | Target workspace-form presentation | Requirements and design | R-001–R-010, R-038–R-044; AC-001–AC-008, AC-031–AC-038 | Refined | User-approved through SR-011/SR-012 on 2026-08-25 | Include in architecture and every later presentation-related handoff. |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config/tickets/in-progress/hierarchical-team-run-launch-config/hierarchical-launch-configuration-behavior.md` | Intended behavior, hierarchy examples, UI states, editable/stored inspection, field participation, launch-surface semantics, and historical transition | Target user/system behavior | Requirements and design | R-001–R-044; AC-001–AC-038 | Refined | Functional behavior approved on 2026-08-24; editable and stored UI presentation approved on 2026-08-25 | Keep aligned and include in every downstream handoff. |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config/tickets/in-progress/hierarchical-team-run-launch-config/team-execution-tree-v2-contract.md` | Exact TypeScript structure, materialization rules, and realistic JSON example for TeamRun execution-tree V2 | Design/interface contract | Requirements and design | R-021–R-031, R-035, R-037; AC-016–AC-023, AC-030 | Reconstructed / confirmed | User-approved semantics; semantic equivalence confirmed by `ARCH-REV-001` because the original blob was unavailable | Keep synchronized and include in every later handoff. |
| `/Users/normy/autobyteus_org/autobyteus-worktrees/hierarchical-team-run-launch-config/tickets/in-progress/hierarchical-team-run-launch-config/recovery-audit.md` | Archive integrity, base reconstruction, hash verification, exact missing paths, and safe workflow restart | Recovery evidence and context | Investigation and downstream routing | N/A | Current | Evidence only / approval N/A | Include until implementation recovery and replacement stage artifacts are complete. |

## Source Log

| Date | Source Type | Exact Source / Query / Command | Why Consulted | Relevant Findings | Follow-Up Needed |
| --- | --- | --- | --- | --- | --- |
| 2026-08-24 | Command | `git fetch --prune origin`; `git worktree add --track -b codex/hierarchical-team-run-launch-config /home/ryan-ai/SSD/autobyteus_org_workspace/autobyteus-workspace-superrepo-hierarchical-team-run-launch-config origin/codex/hierarchical-team-run-launch-config`; `git merge-base HEAD origin/personal`; `git rev-list --left-right --count HEAD...origin/personal` | Resume the existing remote ticket in a current isolated tracking worktree | Ticket HEAD `274567367...` has current `origin/personal` `c5b87df4d...` as its parent/merge-base; branch is one ticket commit ahead and zero behind | No |
| 2026-08-24 | Command | `git fetch --prune origin`; `git rebase --autostash origin/personal`; `git rev-list --left-right --count HEAD...origin/personal`; `git rev-parse HEAD` | Refresh again immediately before approved design work | `origin/personal` advanced to `52b4be02...`; rebase succeeded; current ticket HEAD `030743de...` is one commit ahead and zero behind the refreshed base | No |
| 2026-08-24 | Code | `autobyteus-web/types/agent/TeamRunConfig.ts` | Inspect authoring state | One root global config plus flat `memberOverrides`; no team-scope override subject | Yes |
| 2026-08-24 | Code | `autobyteus-web/components/workspace/config/TeamRunConfigForm.vue` | Inspect root UI | Root runtime/model/workspace/auto-execute controls render once, outside recursive member tree | Yes |
| 2026-08-24 | Code | `autobyteus-web/components/workspace/config/MemberOverrideTree.vue` | Inspect nested UI | Nested team renders only a group label; the same root globals are passed recursively to Agent items | Yes |
| 2026-08-24 | Code | `autobyteus-web/utils/teamDefinitionMembers.ts` | Inspect hierarchy identity | Recursive tree already contains canonical addresses and team/Agent kinds; suitable subject identity exists | No |
| 2026-08-24 | Code | `autobyteus-web/composables/useDefinitionLaunchDefaults.ts` | Inspect template seeding | `buildTeamRunTemplate` uses only selected root definition defaults; nested definition defaults are not projected | Yes |
| 2026-08-24 | Code | `autobyteus-web/stores/teamRunConfigStore.ts` | Inspect edits/invariants | Store edits root fields or exact Agent overrides; root changes prune some inherited model configs | Yes |
| 2026-08-24 | Code | `autobyteus-web/utils/teamRunMemberConfigBuilder.ts` | Trace effective resolution | Each leaf resolves from root globals plus its exact override; no nearest-team resolution | Yes |
| 2026-08-24 | Code | `autobyteus-server-ts/src/agent-team-execution/services/team-definition-topology-planner.ts` | Trace backend launch planning | API must provide complete settings for every leaf Agent; team nodes receive no launch default | Yes |
| 2026-08-24 | Code | `autobyteus-server-ts/src/agent-team-execution/domain/team-run-config.ts` | Inspect compiled runtime topology | Agent nodes contain complete settings; Team nodes contain topology/identity only | Yes |
| 2026-08-24 | Code | `autobyteus-server-ts/src/agent-team-execution/domain/team-run-execution-tree.ts` | Inspect persisted tree | Configured Agent nodes persist complete launch snapshots; configured Team nodes do not persist effective defaults | Yes |
| 2026-08-24 | Code | `autobyteus-web/utils/teamRunLaunchReadiness.ts`; `autobyteus-web/composables/useTeamRunRuntimeCatalogSync.ts` | Trace readiness/catalog coverage | Root plus exact Agent override runtime/model combinations are validated/loaded; there is no Team-scope traversal or subject-kind validation | No |
| 2026-08-24 | Code | `autobyteus-web/components/mobile/MobileRunSetup.vue`; `autobyteus-web/composables/mobile/useMobileRunSetupController.ts` | Inventory mobile authoring | Mobile exposes only root Team workspace/runtime/model/config/auto-execute and uses the shared launch draft/store; it has no member or Team-scope editor | No |
| 2026-08-24 | Code | `autobyteus-web/components/applications/setup/ApplicationTeamLaunchProfileEditor.vue`; `autobyteus-web/utils/application/applicationLaunchProfile.ts` | Inventory application profile authoring | Application setup stores root defaults plus complete leaf-profile identity and exact Agent runtime/model overrides; it has no nested-Team profile subject | No |
| 2026-08-24 | Code | `autobyteus-application-sdk-contracts/src/execution-resources.ts`; `autobyteus-application-sdk-contracts/src/index.ts`; `autobyteus-application-backend-sdk/src/launch-profile.ts` | Trace application launch contract | Configured profiles expand to root defaults plus per-leaf complete launch records; public Team launch supports root preset or complete `memberConfigs`, but no explicit Team-scope policy/default contract | Design must extend one current contract, not infer a default from a leaf |
| 2026-08-24 | Code | `autobyteus-server-ts/src/external-channel/domain/models.ts`; `.../runtime/channel-binding-run-launcher.ts`; `.../providers/file-channel-binding-provider.ts` | Trace external-channel launch | External Team binding owns one complete root preset; backend expands it uniformly to every leaf | No |
| 2026-08-24 | Code | `autobyteus-server-ts/src/api/graphql/types/agent-team-run.ts`; `.../services/team-run-service.ts` | Trace API/service entry | GraphQL and service accept only complete per-Agent member configs; preset expansion also flattens directly to Agents | Design team-policy input boundary |
| 2026-08-24 | Code | `autobyteus-server-ts/src/agent-team-execution/services/team-run-execution-tree-builder.ts`; `autobyteus-server-ts/src/run-history/store/team-run-execution-tree-schema.ts`; `.../team-run-execution-tree-store.ts` | Trace write/read/restore schema | V1 writer/restorer maps Agent launch facts only; exact-key validation rejects added Team fields and current store accepts only schemaVersion 1 | Migration required |
| 2026-08-24 | Code | `autobyteus-server-ts/src/run-history/services/team-run-state-package-loader.ts`; `.../team-run-v1-package-catalog.ts`; `.../services/agent-team-run-manager.ts` | Trace startup admission and restore | Package catalog validates/repairs V1 before admission; manager rebuilds runtime config solely from admitted execution tree | New migration must run before current-schema catalog/readers |
| 2026-08-24 | Code | `autobyteus-team-stream-contracts/src/team-execution-view-dtos.ts`; `autobyteus-server-ts/src/services/agent-streaming/team-execution-view-projector.ts`; `autobyteus-web/services/teamExecution/teamExecutionContextFactory.ts` | Trace live/history projection | Wire DTO omits Team defaults; frontend reconstructs a synthetic root baseline from the root coordinator and diffs all Agents against it | New-format projection reads stored Team defaults; coordinator reconstruction is isolated to migration |
| 2026-08-24 | Code | `autobyteus-server-ts/docs/design/production_data_migration_conventions.md`; `autobyteus-server-ts/src/app-data-migrations/app-data-migration-registry.ts` | Determine transition rules | Current runtime must be forward-only; legacy interpretation belongs in a registered startup migration with ordinary retry | No |
| 2026-08-24 | Data/Command | `jq` over `autobyteus-server-ts/tests/fixtures/app-data-migrations/team-run-execution-tree-v1/*/team_run_execution_tree.json`; direct-coordinator invariant in `team-run-execution-tree-schema.ts` | Inspect representative current persisted shape and fallback availability | Fixtures contain `/`, `/architecture`, `/qa`, and `/qa/automation` Team scopes and heterogeneous Agent snapshots but no Team default field; every configured Team has a persisted direct coordinator address and complete coordinator launch snapshot | No |
| 2026-08-24 | User clarification | Nested subteams should expose their global configuration and inherit the parent when untouched; `skillAccessMode` remains root-inherited as it works now | Lock the scoped-field boundary | Nested scopes expose runtime, model/model configuration, workspace, and auto-execute; skill access is not a nested override | No |
| 2026-08-24 | User clarification | Historical Team defaults should be derived from the direct coordinator because users normally leave the coordinator on the global configuration | Lock the V1 reconstruction policy | V1 migration copies each Team's persisted direct coordinator launch snapshot into that Team's reconstructed default; the accepted risk is a historical coordinator-specific override | Design migration and focused fixture coverage |
| 2026-08-24 | User approval | Complete `requirements.md` and `hierarchical-launch-configuration-behavior.md` decision bundle | Authorize solution design | The user approved the clarified requirements, requested migration-convention/latest-example study, and required personal review before architecture review | Complete design package; do not hand off yet |
| 2026-08-24 | Design authority | `.codex/skills/solution-designer/design-principles.md`; `.codex/skills/solution-designer/templates/design-spec-template.md`; `.codex/skills/solution-designer/references/design-examples.md` | Apply the mandated architecture workflow | Target design must be spine-led, owner-specific, semantically tight, clean-cut, and explicit about migration/removal | No |
| 2026-08-24 | Migration convention | `autobyteus-server-ts/docs/design/production_data_migration_conventions.md`; `autobyteus-server-ts/README.md` (`Production data migrations`) | Govern the V1-to-current transition | Use a deterministic known-source-to-fixed-target transform, one writer/stable-attempt assumptions, forward-only current runtime, before/after validation, truthful capability-scoped failure, bounded diagnostics, and ordinary runner retry; no speculative journal/backup/state machine | No |
| 2026-08-24 | Command | `git log --all --date=iso-strict --format=... -- tickets/done/*migration*/design-spec.md tickets/done/nested-team-history-restart-hydration/design-spec.md` | Identify the latest two completed migration-design rounds | Latest were nested Team history/restart repair archived 2026-08-24 and app-data migration summary redesign finalized 2026-08-21; the prior token-ledger migration was 2026-07-03 | Read both latest designs and their implementations |
| 2026-08-24 | Latest migration design and implementation | `tickets/done/nested-team-history-restart-hydration/design-spec.md`; `autobyteus-server-ts/src/app-data-migrations/migrations/team-agent-memory-layout-app-data-migration.ts` | Learn the latest filesystem-migration simplification | One cohesive definition owns a closed disposition table and a small deterministic loop; reuse the runner/ledger/manual Retry; cap sorted examples at five per reason; avoid a generic planner/relocator, backup, journal, `fsync` protocol, or mechanical-failure matrix | Apply the same proportionate shape to the execution-tree rewrite |
| 2026-08-24 | Latest migration design and implementation | `tickets/done/app-data-migration-summary-log-redesign/design-spec.md`; `autobyteus-server-ts/prisma/migrations/20260820090000_redesign_app_data_migration_summary/migration.sql` | Learn the latest database-migration simplification | Keep legacy interpretation in one durable migration boundary, validate before mutation, use the existing atomic boundary, leave current runtime on one shape, and retain the migration for direct/skip upgrades | Apply the same one-boundary/current-only rule; use the existing atomic file writer rather than SQL |
| 2026-08-24 | Code | `autobyteus-server-ts/src/app-data-migrations/migrations/team-run-migration-state-classifier.ts`; `.../team-run-execution-tree-v1/*`; `.../team-agent-memory-layout-app-data-migration.ts`; `.../run-history/store/team-run-file-commit-writer.ts` | Resolve sequencing and legacy-boundary mechanics | The V1 promotion and memory-layout migration currently depend on current-schema V1 types; the new V2 migration must be registered after both, while exact V1 types/validators/builders used by those historical migrations move behind a migration-owned boundary. The shared writer already distinguishes pre-rename failure from post-rename finalization uncertainty | Design V1 isolation and V2 final-state reread explicitly |
| 2026-08-24 | User design review | Naming review of the complete launch value and execution-tree node types | Remove misleading or nonparallel names before architecture review | The approved shared value name is `AgentLaunchConfiguration`: an Agent stores it as its actual `launchConfiguration`, while a Team stores the same value as `defaultLaunchConfiguration`. `TeamRunLaunchConfiguration` and `*Snapshot` are rejected. The target execution-tree type family uses parallel `ConfiguredExecutionNode`, `ConfiguredAgentExecutionNode`, `ConfiguredTeamExecutionNode`, and `RootConfiguredTeamExecutionNode` names | Update design and revision record; continue user review hold |
| 2026-08-24 | User design review | Request for a dedicated contract with a realistic materialized Team execution-tree example | Make the proposed V2 persistence shape directly reviewable | Added `team-execution-tree-v2-contract.md` with exact types, materialization rules, a root/nested/sibling JSON example, explicit V2 runtime-kind values, V1 conversion rules, and excluded shapes | User review/approval of the proposed contract |
| 2026-08-24 | User design review | Whether `TeamRunExecutionTreeFileV2` and materialized `schemaVersion: 2` are unnecessarily duplicative | Resolve compile-time versus persisted version naming | The user chose to retain `V2` in the file-payload type name. `schemaVersion: 2` also remains as the persisted discriminator used by migration/storage classification | Record the intentional dual version markers; continue contract review |
| 2026-08-24 | User approval | Completed personal review of the design package, concrete V2 contract, and forward-only migration failure semantics | Release the package for architecture review | The user approved the solution package and explicitly authorized architecture-review handoff. Normal application code remains V2-only; migration failure is capability-scoped, truthfully recorded as failed, and leaves the affected TeamRun excluded with Retry available | Submit the cumulative package to `/architecture_reviewer` |
| 2026-08-24 | Recovery archive / command | `/Users/normy/Downloads/hierarchical-team-run-launch-config-recovered-delta-20260824.zip`; `unzip -t`; SHA-256; recovered `changed-map.tsv`, `deleted-paths.txt`, `missing-paths.txt`, and `preserved-index` | Restore the failed-disk implementation snapshot without assuming completeness | Archive passed integrity; 138/150 changed files restored with exact blob matches; four recorded deletions applied; twelve expected blobs unavailable | See `recovery-audit.md` |
| 2026-08-24 | Git recovery | `git fetch --prune origin`; replay bootstrap patch onto recorded `52b4be02...`; compare recovered index with `GIT_INDEX_FILE=... git diff-index --cached --name-status HEAD` | Recover the correct pre-implementation base | Reconstructed baseline `c915a0653` yields exactly 154 intended delta paths: 123 modified, 27 added, 4 deleted | Keep latest-base integration for delivery stage |
| 2026-08-24 | Recovery reconstruction | Recovered requirements/design/SR-004–SR-006 plus V2 domain/schema/migration source and tests | Recreate the missing approved V2 contract without inventing behavior | `team-execution-tree-v2-contract.md` now expresses the approved exact types, materialization, V1 conversion, and excluded shapes, but does not claim unavailable blob identity | Architecture reviewer confirmed semantic equivalence in `ARCH-REV-001` |
| 2026-08-24 | Architecture review | `design-review-report.md`; `architecture-review-revision-record.md` (`ARCH-REV-001`) | Resolve recovery uncertainty before implementation | Fresh review passed and confirmed the reconstructed V2 contract's semantic equivalence; later integration findings do not reopen that persistence contract | Retain as prior baseline; SR-008 requires a new architecture round |
| 2026-08-24 | Integrated source / frontend | `autobyteus-web/components/workspace/config/RunConfigPanel.vue`; `stores/teamRunConfigStore.ts`; `types/agent/TeamLaunchDraft.ts`; `components/workspace/config/TeamRunConfigForm.vue`; `utils/teamRunLaunchReadiness.ts`; `stores/agentTeamRunStore.ts` | Reproduce CR-008 against the actual delivery-integrated controlled-workspace path | `RunConfigPanel` owns `teamWorkspaceSelections` outside the draft, the store separately owns a global address loading map and config/topology repair, and the panel registers active New paths before `agentTeamRunStore.launchDraft` reaches reconciliation. Stable `team-draft:<draftId>` context intentionally preserves the parallel map on same-draft edits. | Consolidate Team workspace intent/status/replacement/reconciliation in the draft store; keep panel presentation-only |
| 2026-08-24 | Integrated source / backend | `autobyteus-server-ts/src/agent-team-execution/services/team-run-service.ts`; `.../team-definition-topology-planner.ts`; `.../application-orchestration/services/application-run-binding-launch-service.ts`; `.../domain/team-run-id.ts` | Reproduce CR-009 against full and root-only/application create paths | `TeamRunService` resolves/allocates the root ID before planner validation; application launch calls the public allocator even earlier. The planner allocates nested Team and Agent IDs after validation, so only root ownership/order is split. | Move configured root/nested identity allocation behind planner validation; remove public preallocation/input override |
| 2026-08-24 | Governing integrated behavior | `tickets/done/remote-node-new-workspace-team-run-visibility/requirements.md`; `design-spec.md`; `ui-ux-spec.md` | Confirm same-draft workspace selection stability and context-reset behavior integrated at delivery | One controlled selection per active Team scope, active/inactive New-path buffer stability within the same draft, actual-context reset, and one-click create-before-launch are established preserved contracts; they do not require a new requirement decision | Bind those states to the hierarchical draft/topology lifecycle without a broad config watcher |
| 2026-08-24 | Code review / probes | `code-review-report.md` CRR-010; `code-review-revision-record.md`; `implementation-evidence/code-reviewer-topology-workspace-probe-crr-010.txt`; `implementation-evidence/code-reviewer-root-identity-order-probe-crr-010.txt`; `implementation-evidence/code-reviewer-web-workspace-focused-crr-010.txt`; `implementation-evidence/code-reviewer-full-architecture-static-crr-010.txt` | Validate reachability, failure origin, and proportional response | MP-CR-006 and MP-CR-007 are Reachable. Reviewer proof shows stale removed-Team workspace completion throws before repair and invalid create consumes one root allocation before planner rejection. IR-006 separately passes 7 files / 106 tests for the earlier one-click source defect. | Design correction, architecture review, implementation, complete source review, then exact API-E2E-014 rerun |
| 2026-08-25 | Integrated delivery/review records | `architecture-review-revision-record.md` ARCH-REV-002; `implementation-revision-record.md` IR-008; `code-review-revision-record.md` CRR-012/CRR-014; `api-e2e-revision-record.md` API-REV-007; `delivery-revision-record.md` DR-002/DR-003 | Establish the actual state before accepting hands-on user feedback | CR-008/CR-009 were designed, implemented, source-reviewed, API/E2E-validated, proportionally re-reviewed, integrated with current base, and packaged for local Electron verification | Preserve those passed owners; route only the new UI simplification through design/review/implementation |
| 2026-08-25 | User Electron review / screenshot | `/Users/normy/.autobyteus/server-data/memory/agent_teams/software_engineering_team_f52e9abb9087479e94dd677300ebc829/solution_designer_c1cf88985b7f4756866d21a1165599c0/context_files/ctx_96307e1728d3__image.png`; copied evidence `solution-evidence/dr003-root-presentation-user-review-20260825.png`; user feedback in the active task | Determine whether the root address, scope chrome, and effective summary add user value | The selected Team definition and visible controls already communicate root context/effective values. The root title/badge, `/`, divider, and summary are redundant. The user subsequently rejected the effective summary altogether, including for nested Teams. | Preserve SR-009 as the initial feedback record and supersede its retained-summary proposal in SR-010 |
| 2026-08-25 | Current source | `autobyteus-web/components/workspace/config/TeamRunConfigForm.vue`; `TeamScopeConfigEditor.vue`; `RuntimeModelConfigFields.vue` | Trace the exact visual output in the screenshot | `TeamRunConfigForm` already renders Team Definition immediately before the root editor. `TeamScopeConfigEditor` renders root-only title/`/`, a duplicate badge, divider, and unconditional expanded effective summary before the reusable controls. No store/runtime data dependency requires those elements. | Remove only redundant render branches/messages; preserve edits, derived values, state, and launch wiring |
| 2026-08-25 | User direction | Active task messages following SR-009 discussion | Establish whether a dedicated UI/UX artifact and personal-branch comparison are required | The user requested inspection of the current original personal UI and a dedicated UI/UX specification, emphasized minimal visual change, and stated that the effective summary is noise rather than useful collapsed-state content. | Create `ui-ux-spec.md`; hold architecture/implementation until explicit approval |
| 2026-08-25 | Git/source comparison | `git show origin/personal:autobyteus-web/components/workspace/config/TeamRunConfigForm.vue`; `git show origin/personal:autobyteus-web/components/workspace/config/MemberOverrideTree.vue`; compare shared checkout `8d6b06b8...` with `origin/personal@87b1b584...` for relevant UI paths | Establish the actual pre-feature form structure rather than infer it from the integrated implementation | Relevant workspace-config paths have no material change between the available personal checkout and current remote. Original root sequence is Team Definition -> Runtime -> Default LLM Model (Global) -> optional model configuration -> Workspace Directory -> Auto approve tools -> Team Members Override -> Run Team. Original nested Team groups already provide name, TEAM marker, canonical address, indentation, and child Agent controls. | Use this structure as the mandatory visual baseline; add nested global controls inside the existing group |
| 2026-08-25 | Live browser render | Temporary Nuxt dev server on `http://localhost:3002` against live local backend; `/workspace`; selected `Nested Classroom Test Team`; evidence `solution-evidence/origin-personal-root-launch-form-20260825.png` and `solution-evidence/origin-personal-nested-team-group-20260825.png` | Verify actual layout, spacing, grouping, and disclosure behavior in the original personal UI | The live render confirms there is no root wrapper/title/badge/address/summary. `Team Members Override (N)` is an existing progressive-disclosure boundary, and nested Teams already have a recognizable group presentation that can be minimally extended. The browser tab and temporary Nuxt process were closed after capture; no personal-worktree source was modified. | Materialize exact journeys/states/wireframes in `ui-ux-spec.md` |
| 2026-08-25 | User approval | Active task confirmation following review of `ui-ux-spec.md` | Resolve the UI/UX approval hold | The user confirmed that the launch form should remain almost identical to the original, add only the necessary global configuration for each nested Team, and omit slash/effective-summary noise because the controls already show configured values. | Record SR-011 and submit the cumulative package for architecture re-review |
| 2026-08-25 | User Electron review | `solution-evidence/user-existing-team-run-settings-entry-20260825.png`; `solution-evidence/user-stored-team-config-cards-20260825.png`; active-task feedback | Determine whether selected existing TeamRun Settings preserves the approved configuration-form language | The user identified that the stored root/team/Agent key/value cards look completely different from the launch configuration form and required the post-configuration view to remain substantially the same form with controls locked. | Hold SR-011 architecture review and investigate origin/personal/current paths |
| 2026-08-25 | Live browser reproduction | Integrated Nuxt on `http://localhost:3003` with `BACKEND_NODE_BASE_URL=http://127.0.0.1:29695`, using the user-running Electron server; Temp Workspace -> Nested Classroom Test Team -> `hello` -> Teacher -> Settings; `solution-evidence/integrated-stored-team-config-live-20260825.png` | Reproduce the exact selected-run settings path outside the packaged renderer | Live view matches the user's screenshot: “Stored root Team defaults,” `/`, key/value fields, separate stored-member heading, and address-heavy Team/Agent cards. Browser tab/dev server were closed after capture. | Treat as USER-UX-003 requirement gap/design impact |
| 2026-08-25 | Origin/current source and test comparison | `git show origin/personal:.../RunConfigPanel.vue`; `TeamRunConfigForm.vue`; focused specs; current `RunConfigPanel.vue`, `StoredTeamRunConfigForm.vue`, `StoredTeamRunConfigTree.vue`, `StoredLaunchConfigurationCard.vue`; `solution-evidence/stored-team-settings-origin-personal-source-audit-20260825.txt` | Establish whether the different stored UI pre-existed | `origin/personal` routes selected TeamRuns through `TeamRunConfigForm` with `readOnly=true` and explicitly tests disabled controls/operable disclosures/no mutation. The three `Stored*` components do not exist there and were introduced by this ticket. | Restore one form presentation while retaining stored-view truth |
| 2026-08-25 | Origin-personal compatibility probe | Personal Nuxt on `http://localhost:3002` against current Electron server | Attempt a direct old-UI live render of the same V2 run | Workspace/run loaded, but selecting the run correctly failed the old strict parser because origin/personal expects schema V1 and the current server emits V2. Source/focused tests remain authoritative for old presentation. Browser tab/dev server were closed. | No product blocker; record limitation truthfully |
| 2026-08-25 | Current view/factory source | `TeamRunConfig.ts`; `teamExecutionContextFactory.ts`; `TeamRunConfigForm.vue`; `TeamScopeConfigEditor.vue`; `TeamMemberConfigTree.vue`; `MemberOverrideItem.vue` | Determine how to achieve visual reuse without corrupting stored truth | Immutable `TeamRunConfigurationView` already carries persisted Team/Agent topology and effective configs. Shared presentation must consume it directly; stored Agent values cannot be recomputed from null override intent, and stored workspace state cannot come from the editable draft store. | Design one presentation owner with editable/stored adapters and exact-value fallbacks |
| 2026-08-25 | User confirmation | Active task continuation after analysis began | Approve the corrected product rule | The user explicitly confirmed that how the Team is configured should be how it is later viewed; existing Settings should use the same UI with some controls locked, not a completely different form. | Record SR-012 and resume architecture review with revised package |
| 2026-08-25 | Architecture/implementation records | `design-review-report.md` / `architecture-review-revision-record.md` (`ARCH-REV-004`); `implementation-handoff.md` / `implementation-revision-record.md` (`IR-010`) | Establish the implemented SR-012 boundary before reacting to review | Architecture required a discriminated editable/stored capability and exact stored values. IR-010 restored one visual form and deleted the stored cards, but its common `TeamScopeFormModel` still required authoring-owned fields for both modes. | Treat the architecture intent as sound but make the scope boundary structurally explicit in SR-013 |
| 2026-08-25 | Code review | `code-review-report.md` (`CRR-017`, CR-011/CR-012, MP-CR-009); `code-review-revision-record.md`; `implementation-evidence/code-reviewer-static-audit-crr-017.txt`; `implementation-evidence/code-reviewer-stored-schema-drift-crr-017.txt` | Determine failure origin and reachability | Review failed 8.9/10 despite 11 files / 134 tests and a green Nuxt build. CR-011 is Design Impact; CR-012 is a bounded local defect within the reroute. MP-CR-009 is Reachable under BEH-010/R-044/AC-038. | Revise solution package and return through architecture review; no API/E2E/delivery |
| 2026-08-25 | Integrated source | `autobyteus-web/types/agent/TeamRunFormModel.ts`; `services/teamExecution/storedTeamRunFormModel.ts`; `utils/editableTeamRunFormModel.ts`; `TeamScopeConfigEditor.vue`; `MemberOverrideItem.vue` | Inspect the actual shared-form capability boundary | One common `TeamScopeFormModel` imports `TeamWorkspaceOperationState` from `TeamLaunchDraft` and mandates editable `workspaceSelection`, `override`, `workspaceOperation`, and catalog state. Stored projection fabricates `override: null`, Existing/New selection, and idle operation/catalog objects. Agent nodes are nominally split but the shared Agent component still exposes broad optional mode-specific props. | Define distinct scope/node capabilities and require components to narrow, not consume sentinels |
| 2026-08-25 | Integrated source/probe | `ModelConfigAdvanced.vue`; `RuntimeModelConfigFields.vue`; `MemberOverrideItem.vue`; `HistoricalModelConfigFallback.vue`; reviewer schema-drift evidence | Trace exact-history loss under partial schema drift | Whole-schema absence activates the fallback, but a present partial schema iterates only current keys. `resolveEffectiveConfigValue` maps stale enum values to the Default option; removed persisted keys are never rendered. | Add one pure per-field/per-value historical representability projector and direct root/nested/Agent coverage |
| 2026-08-25 | API/E2E correction and user challenge | `api-e2e-revision-record.md` API-REV-009; `api-e2e-evidence/api-rev-009-user-reachability-correction.md`; user screenshot showing `NESTED ordinary API-REV-009` | Determine whether the displayed “Ordinary prompt” is a designed/current product setting and whether isolated-CR layout can block delivery | `ordinary_prompt` and `multiline_prompt` were temporary API/E2E names. The owned page's Pinia catalog was mutated after load, arbitrary keys were placed in an isolated V2/GraphQLJSON fixture, the real Codex Luna catalog emits no free-text property, and no normal current UI can create those fields. | Do not treat synthetic fields as product UI or current-path acceptance evidence; record API-E2E-F-003 as non-blocking robustness residual and return scope through architecture confirmation |
| 2026-08-25 | Corrected downstream authority | `code-review-revision-record.md` CRR-021; `api-e2e-revision-record.md` API-REV-010; `api-e2e-evidence/api-rev-010-real-user-scope-resolution.md` | Establish the authoritative result after the user applied the production-reachability principle | CR-013/MP-CR-010 are rescinded/not reachable; CRR-020 is void; API-E2E-F-003 is Out Of Scope; real current-user paths pass at 98% with no rerun or product delta | Revise solution authority, retain real-path evidence, and do not authorize synthetic CR implementation/rerun |
| 2026-08-25 | Commit/source delta audit | `git show 003413b05`; `historicalModelConfigFields.ts`; `HistoricalModelConfigFallback.vue`; `historicalModelConfigFields.spec.ts`; `StoredTeamScopeHistoricalFields.spec.ts`; `MemberOverrideItem.spec.ts` | Decide whether already-committed IR-012 should remain as robustness and whether retained tests obey the same reachability rule | IR-012 consists only of CR/LF text-input classification, `whitespace-pre-wrap`, and synthetic ordinary/multiline fixtures. It has no named current/released producer. IR-011 is separable and production-grounded, but some retained tests use invented `removed_parameter`/`legacy_shape` names where real `reasoning_effort`/`service_tier` paths can express the same mechanics. | Reverse IR-012 source/tests and ground the retained three test suites in real emitted field names; do not reopen IR-011 or add isolated-CR mechanics |
| 2026-08-25 | Production schema source | `autobyteus-server-ts/src/agent-execution/backends/codex/codex-app-server-model-normalizer.ts`; stored TeamRun Settings path | Trace a real producer for the retained stale-enum classifier | Codex model configuration is emitted dynamically from live `model/list` reasoning metadata; users select emitted `reasoning_effort` values, runs persist them, and later Settings compares them with the then-current catalog. No equivalent current/released free-text producer exists. | Retain IR-011's per-field/value enum/schema-drift behavior; exclude speculative CR/LF behavior |
| 2026-08-25 | Architecture review | `design-review-report.md`; `architecture-review-revision-record.md` ARCH-REV-006 / AR-001 | Check SR-014 design coherence before implementation | Reviewer validated the reachability classification and cleanup direction, but the mandatory Removal / Decommission Plan omitted the four cleanup/fixture-retargeting items and the hard block still said “any explicit persisted” value | Add explicit decommission rows, producer-qualify blocking exactness, audit other absolute instructions, append SR-015, and return for re-review |
| 2026-08-25 | SR-015 artifact validation | `git diff --check`; local Markdown-link resolver over all six changed solution artifacts; `rg` audit of absolute persisted-value/exactness instructions | Verify AR-001 is closed without adding runtime provenance or reopening excluded behavior | Diff check passed; 6 Markdown files had 0 missing local links; the decommission plan names all four cleanup/retargeting actions; hard block, dependency/interface/file/sequence/guidance statements consistently limit blocking exactness to named supported current/released producer paths while the classifier remains generic/provenance-free | Return the cumulative SR-015 package for architecture re-review; implementation remains blocked |

## Relevant Existing Behavior And Production Paths

| Behavior ID | Kind | Current Supported Trigger Or Governing Contract | Current Production Path And Lifecycle | Meaningful Current Outcome / Invariants | Evidence |
| --- | --- | --- | --- | --- | --- |
| BEH-001 | User | Select AgentTeam and configure new TeamRun | Team definition -> root template -> immutable draft -> root + recursive Team form | Functionally complete; SR-010 restores the original personal-branch root presentation without hierarchy wrapper/title/badge, `/`, divider, or summary | Origin-personal render; user Electron screenshot; current components |
| BEH-002 | User | Expand or edit a nested Team scope | Definition tree -> `TeamMemberConfigTree` -> `TeamScopeConfigEditor` -> exact-address store edit | Existing nested Team groups gain default-collapsed global controls plus inherited/customized state and conditional Reset; no effective summary is rendered | Origin-personal nested-group render; integrated components/store; `ui-ux-spec.md` |
| BEH-003 | Contract | Resolve readiness or launch payload | Draft config + canonical member tree -> `teamRunLaunchHierarchy` -> complete Team/Agent records | Root -> nearest Team -> exact Agent resolution is one pure policy path | Resolver/readiness/payload source and tests |
| BEH-004 | Contract | Edit/lock/repair/launch a draft, including a same-draft topology change | Draft store owns Team workspace state; launch owner reconciles/plans/registers/commits/revalidates/admit/creates | CR-008 is resolved; topology-aware zero-side-effect repair and same-draft state preservation passed review/coverage | SR-008, IR-008, CRR-012, API-REV-007 |
| BEH-005 | System | Create, persist, and restore TeamRun | GraphQL -> `TeamRunService` -> planner validation -> configured identity allocation -> runtime/V2 persistence/restore | CR-009 is resolved; exact validation precedes configured root/nested Team and Agent allocation | SR-008, IR-008, CRR-012, API-REV-007 |
| BEH-006 | Contract | Seed config from AgentTeam definition | `buildTeamRunTemplate(rootDefinition)` -> draft | Only the selected root definition seeds defaults; embedded definitions inherit | Integrated default builder/tests |
| BEH-007 | User / Operational | Upgrade or inspect stored TeamRun | registered V1-to-V2 migration -> V2-only catalog/projector -> read-only view | Coordinator reconstruction is migration-only and complete Team defaults are stored/projected; live DR-003 startup later reported 514 migrated, 8 missing-file skips, 0 failures | API-REV-006/007; live migration ledger/log audit on 2026-08-25 |
| BEH-008 | System | Mobile, application, external channel, or backend preset launches a team | root-only adapter -> `createTeamRunFromRootConfig` -> common validated planner -> returned root identity | Root-only semantic expansion and returned-root application binding are current and passed | IR-008, CRR-012, API-REV-007 |
| BEH-009 | User | Load, recover, lock, or navigate hierarchical controls | draft/address-scoped controls/readiness/repair/locking -> UI | Address-scoped loading/error/repair/accessibility behavior is implemented and passed; SR-010 changes only presentation and removes duplicated summary output | CRR-012; API-REV-007; user Electron review; `ui-ux-spec.md` |
| BEH-010 | User | Select existing TeamRun/member and open Settings | supported stored V2 tree -> immutable `TeamRunConfigurationView` -> stored form projection -> shared `TeamRunConfigForm`/tree/controls | Visual parity, distinct stored capabilities, and production-grounded dynamic enum/schema drift are implemented. API-REV-010 passes real current paths. Target cleanup removes only unsupported CR/LF behavior and synthetic fixture names. | SR-013; IR-011; CRR-021; API-REV-010; SR-014 |

## Design Health Assessment Evidence

- Current change posture: `Requirement Applicability Clarification + Speculative Complexity Removal` within the already-complete hierarchical behavior change; no product-scope expansion.
- Current root cause classification: `Synthetic Reachability Overclassification`. The earlier visual/capability defects are resolved; CR/LF work was added because review reasoned backward from generic JSON/schema mechanisms without a producer.
- Refactor posture: `Bounded frontend simplification needed now`. Preserve the shared form, IR-011 capability models/classifier, and every functional owner; delete only IR-012's dead CR/LF behavior and ground retained tests in actual emitted fields.
- Reuse rule: reuse one form hierarchy and actual field presentation only for supported paths. Generic capability does not create a product obligation; every material fixture must name the independent production producer it reproduces.

| Evidence Source | Current Observation | Design Health Implication | Follow-Up Needed |
| --- | --- | --- | --- |
| User Electron screenshot and feedback | “Root Team defaults” appears as both heading and badge; `/` exposes an internal identity; the effective summary repeats visible runtime/model/workspace controls | Confirmed user-facing redundancy, not a missing feature | Remove all four root-only outputs |
| `TeamRunConfigForm.vue` | Team Definition is rendered immediately before the root editor | Root context already exists without a second heading | Proceed directly to root controls |
| Original personal live render plus user direction | The prior root form was already compact, while the effective summary was introduced only by the hierarchy implementation | A summary is not required for either root or nested editing and would add a new scan surface the user did not request | Render no effective summary; use disclosure state and actual controls |
| Current store/resolver/launch/server owners | SR-008 behavior passed architecture, source, API/E2E, proportional, integration, and build gates | No ownership or data-model redesign is justified | Preserve all commands, effective values, readiness, persistence, and migration behavior |
| Origin/personal `RunConfigPanel` + focused specs | Existing selected TeamRun went to `TeamRunConfigForm` with `readOnly=true`; controls disabled, disclosures worked, events did not mutate | User expectation is a preserved baseline, not a new feature invented after delivery | Restore visual parity using the stronger V2 stored view |
| Current `StoredTeamRunConfigForm` / tree / card | New root card, `/`, key/value grid, stored-member heading, and address-heavy cards replace the form | Three parallel presentation components duplicate field/hierarchy policy and caused the visible regression | Remove them after shared-form stored mode is covered |
| `TeamRunConfigurationView` and context factory | Stored snapshot already owns exact immutable topology/effective values independently of current definitions | Visual reuse must not mean converting history into editable intent or using current definition topology | Make the stored view the direct read-only presentation input |
| `TeamRunFormModel.ts` / stored projector | Common Team scope imports draft-owned operation state and stored projection fabricates override/workspace/idle sentinels | Type shape contradicts the approved authority boundary even when runtime mutation is suppressed | Split `EditableTeamScopeFormModel` and `StoredTeamScopeFormModel`; share display vocabulary only |
| Production Codex schema path | live `model/list` emits selectable `reasoning_effort` values and optional `service_tier=fast`; TeamRuns persist the user's emitted selection; later catalog metadata may change | Establishes an independent user-reachable stale-enum/removed-field path for IR-011 | Retain classifier behavior and use these real field names in tests |
| API-REV-009 synthetic prompt probe | API/E2E invented `ordinary_prompt`/`multiline_prompt`, mutated Pinia catalog state, and injected arbitrary V2 JSON | Does not establish product reachability; CR/LF code/test pressure is invalid | Remove IR-012 behavior and do not rerun the synthetic browser case |

## Relevant Files / Components

| Path / Component | Current Responsibility | SR-013 Correction | Must Remain Unchanged |
| --- | --- | --- | --- |
| `autobyteus-web/components/workspace/config/RunConfigPanel.vue` | Chooses editable draft or immutable stored-view adapter, then one Team form | Keep the branch; pass the discriminated model only | Selection ownership, no stored Run action, and back-to-events behavior |
| `autobyteus-web/types/agent/TeamRunFormModel.ts` plus editable/stored model modules | Current union contains a common kitchen-sink Team scope | Define neutral shared display fields plus distinct `EditableTeamScopeFormModel` / `StoredTeamScopeFormModel` and corresponding nodes; stored modules import no `TeamLaunchDraft` or workspace-authoring types | One top-level discriminated form identity |
| `storedTeamRunFormModel.ts`; `editableTeamRunFormModel.ts` | Project immutable history or editable draft into form inputs | Stored projector emits stored effective values/workspace display only; remove fake selection, override, operation, and catalog objects. Editable projector retains real authoring capabilities | Current-definition independence and exact stored topology/order |
| `TeamRunConfigForm.vue`; `TeamMemberConfigTree.vue`; `TeamScopeConfigEditor.vue`; `MemberOverrideItem.vue`; `WorkspaceSelector.vue` | Singular form/tree/Team/Agent/workspace visual presentation | Accept/narrow discriminated subject capabilities; never default missing stored authoring props to idle/null sentinels | Approved appearance, editable events, stored immutability, disclosure/accessibility |
| `RuntimeModelConfigFields.vue`; `ModelConfigAdvanced.vue`; `HistoricalModelConfigFallback.vue`; pure representability utility/type | Runtime/model/model-config presentation | Retain IR-011's actual emitted enum/schema-drift classification; remove IR-012 CR/LF predicate/whitespace styling and ground tests in `reasoning_effort`/`service_tier` | Editable schema/default behavior and compact shared form styling |
| Deleted `StoredTeamRunConfigForm.vue`; `StoredTeamRunConfigTree.vue`; `StoredLaunchConfigurationCard.vue` | No longer present in IR-010 source | Keep deleted; do not restore as fallback | N/A |
| `teamExecutionContextFactory.ts`; `TeamRunConfigurationView` | Build immutable stored hierarchy/effective configs | No semantic change; remain the stored authority | Stored values, freeze/immutability, workspace hydration, V2 semantics |
| `autobyteus-web/localization/messages/en/workspace.ts`; `zh-CN/workspace.ts` | Scope UI strings | Remove root-only strings if no remaining call site; retain nested state/reset/error strings | Localization completeness |
| Focused `RunConfigPanel` / `TeamRunConfigForm` / tree/field tests | Rendered editable/stored behavior | Assert origin-personal root sequence, same component hierarchy for stored mode, exact stored root/team/Agent values, disabled mutation, operable disclosures, removed `Stored*` UI, and unavailable-catalog fallbacks | Existing hierarchy/edit/readiness assertions |
| Draft/store/resolver/launch/backend/migration files | Hierarchy, lifecycle, launch, persistence, transition | No source change | All SR-008/DR-003 passed behavior |

## Runtime / Probe Findings

- During the initial historical-base design investigation, no server/browser process was started and executable evidence was not manufactured. Later implementation/API-E2E/reviewer rounds installed and exercised the relevant environment as recorded below.
- Historical-base source-render tracing confirmed the original `MemberOverrideTree.vue` problem. The integrated implementation replaced it with `TeamMemberConfigTree.vue` and `TeamScopeConfigEditor.vue`; CRR-010 confirms the ordinary hierarchy path.
- A bounded `jq` probe inspected all five repository V1 migration fixtures. Each contains nested configured Teams at `/architecture`, `/qa`, and `/qa/automation`; Agent launch configurations consistently contain runtime/model/config/auto-execute/skill/workspace, while Team nodes contain no default configuration.
- The fixtures include heterogeneous Agent runtime/model values inside the same Team, so the original Team default is not mathematically recoverable. The user explicitly selected the persisted direct coordinator snapshot as the deterministic historical fallback and accepts that a coordinator-specific override, if one existed, becomes the reconstructed default.
- CRR-010 reviewer execution passed 7 focused frontend files / 106 tests for IR-006, then a real-Pinia topology probe demonstrated the CR-008 ordering defect: completing workspace load at removed `/Research` rejects before launch reconciliation.
- CRR-010's server ordering probe demonstrated CR-009: an invalid GraphQL-style address caused one root TeamRun ID allocation before planner rejection, while Agent allocation and manager creation remained zero.
- SR-008/ARCH-REV-002/IR-008/CRR-012/API-REV-007 resolved and revalidated both historical defects; the probes remain diagnosis evidence rather than current failures.
- DR-003's live Electron startup recorded app-data migration `20260824_team_run_execution_tree_v2` as `SUCCEEDED`: scanned 522, migrated 514, skipped 8 missing files, failed 0. A read-only audit found 514/514 current files at schema V2 with zero invalid JSON, missing Team defaults, or unsupported Team default runtimes.
- The user's Electron screenshot reproduces the current integrated root UI exactly. Source inspection confirms the unwanted title/badge/address/summary are pure rendering; removing them does not alter the resolved configuration or launch state.
- A live render of the original personal checkout confirmed the actual visual baseline rather than relying on memory: no hierarchy-specific root chrome exists, the member section is already collapsible, and nested Teams already have a stable group identity/indentation pattern. The evidence was copied into `solution-evidence/`, and the temporary browser/dev-server session was closed without source edits.
- A second live integrated render used the user-running Electron server on port 29695 and reproduced the selected-run Settings regression exactly. The three standalone stored components render the rejected card/key-value UI.
- Directly rendering the same current V2 run in the old personal frontend is impossible because that frontend correctly rejects schema V2 at its strict V1 transport parser. Static source and focused tests nevertheless prove the old presentation reused `TeamRunConfigForm` with `readOnly=true`.
- CRR-017's focused cohort passed 11 files / 134 tests and the Nuxt production build, proving the failure is not a general render/build break. The green cohort lacked the partial-schema exact-value assertion and cannot override the supported probe.
- CRR-017's mounted-component probe passed mechanically but recorded the wrong product outcome: `selectedValue="__default__"`, stored `ultra` absent from rendered text, and the removed persisted key absent. No mutation emitted, so the failure is truthfulness rather than accidental editing.

## External / Public Source Findings

N/A. Repository-native behavior governs this change.

## Reproduction / Environment Setup

- No services or accounts required for bootstrap.
- Dedicated latest-base worktree created before artifact authoring.
- No temporary investigation files outside the ticket package.

## Findings From Code / Docs / Data / Logs

1. The original nested-Team-as-visual-container defect is implemented: nested Teams are now first-class configuration scopes.
2. Canonical Team addresses remain the shared subject identity; no new naming system was introduced.
3. Editable configuration now has root Team, nested Team, and exact Agent levels under one pure nearest-Team resolver.
4. Authoring intent remains partial/inherited while runtime/V2 persistence store complete snapshots.
5. Backend runtime, persistence, restore, and return projection now retain containing-Team defaults required by the later Dynamic AgentTeam ticket.
6. The executable field boundary remains runtime, model, model-specific config, auto-execute, skill access, and workspace; nested workspace UI still keeps skill access root-inherited.
7. Mobile, application-profile, external, and backend-preset surfaces preserve their approved compact/root-only Team authoring while sharing the common backend hierarchy path.
8. The approved V1 transition is implemented as migration-only coordinator reconstruction; current runtime/readers are V2-only.
9. The existing migration runner/ledger/writer/Retry boundary remains the forward-only owner and is unchanged by SR-010; live migration completed successfully.
10. CR-008 is resolved: exact-address Team workspace selection, operation state, canonical replacement, and topology repair now share the draft/store lifecycle and launch-owner sequence.
11. CR-009 is resolved: the planner validates before configured root/nested Team and Agent allocation, and application binding consumes the returned root identity.
12. The current root UI duplicates context and values: Team Definition identifies the selected root; the root title/badge and `/` add no task information; the effective summary repeats visible controls.
13. The original personal UI establishes the correct information density: use the existing Team group/disclosure to show where nested controls live, and show the controls themselves on expansion. The user does not want a separate effective summary in either state.
14. The original personal selected-run path also establishes presentation continuity: `TeamRunConfigForm` is reused read-only. The current separate `Stored*` UI is ticket-created, not inherited baseline behavior.
15. `TeamRunConfigurationView` is the correct stored data boundary and must remain immutable/current-definition-independent. The correction is presentation reuse, not data-model collapse.
16. Stored Agent effective values must be supplied explicitly to shared Agent controls; current stored views intentionally have no editable override intent, so recomputing from `override: null` would display false parent values.
17. IR-010 removed the parallel stored components but did not preserve the designed capability split: one common Team scope model forces the stored projector to manufacture authoring-state-shaped data. Runtime no-op behavior does not make that ownership dependency acceptable.
18. Historical model-config truth cannot be classified only by whole model/schema availability. A present schema can remove a key or reject one previously valid value; exact inspection requires a union of persisted keys and current schema with per-value representability.
19. API-REV-009 did not establish that free-text model fields are reachable in the current product. It mutated the browser catalog and persisted arbitrary JSON specifically to create `ordinary_prompt`/`multiline_prompt`; the user's screenshot contains the fixture marker `API-REV-009`.
20. The generic renderer observation remains useful defensive evidence, but mechanical GraphQLJSON/schema-type possibility is not the same as a supported user behavior. Blocking historical-truth acceptance applies to product-originated configuration accepted by a supported catalog/launch path at write time; hypothetical future/custom schemas need their own requirement and coverage when introduced.
21. IR-012 is wholly premise-driven: compared with IR-011, it adds one CR/LF predicate, one `whitespace-pre-wrap` class, and synthetic ordinary/LF/CR fixtures/assertions across four frontend files. No current or released producer was identified. Retaining this dead-path complexity would contradict the production-reachability rule; SR-014 therefore removes it rather than relabeling it as a product feature. The cleanup also replaces retained invented removed-key fixtures with the actual emitted `service_tier` disappearance path.
22. IR-011 remains independently grounded. The production Codex runtime derives `reasoning_effort` schema/enum values from live `model/list` metadata, the user can select those emitted values, and the stored TeamRun is later compared with the current catalog. That named producer/user/consumer path justifies the small per-field stale-value classifier without relying on invented free-text fields.

## Persisted Data Transition Evidence (When Applicable)

- Pre-transition stored subject, location, representative shape, and approximate volume: one `team_run_execution_tree.json` V1 file per durable root TeamRun package under the Team memory layout. Five repository fixtures were inspected; user installation volume is unbounded by this ticket and migration traversal must use the existing bounded package/migration conventions.
- Relevant code-model, serialization, semantic, or physical-store change: add complete effective launch configuration to root and configured nested-Team execution nodes and expose it through restore/history.
- Pre-transition readers and writers, including unknown/extra-field behavior: `buildInitialTeamRunExecutionTree`, exact V1 `validateTeamRunExecutionTreePayload`, `TeamRunExecutionTreeStore`, package loader/catalog, manager restore, Team stream/history projector, strict shared DTO, and frontend configuration-view factory. Extra/missing Team keys are rejected.
- Representative direct-read or compatibility evidence: V1 fixtures validate the current shape and show complete Agent snapshots but no root/nested Team default. The historical-base frontend chose the root coordinator as a synthetic baseline; no nested baseline existed.
- Required semantics and invariants preserved by direct use: V1 cannot be directly used as the target because the required Team default meaning is absent and exact shape validation blocks extension.
- Physical storage, privacy/security, disposal, rebuild, or operational constraints: run history is durable user data and must not be discarded solely for schema convenience.
- Concrete benefit, cost, and risk of migration: migration establishes one current schema and preserves old runs by copying each Team's direct coordinator snapshot into its reconstructed default. This is deterministic and normally matches the former global configuration, but a historical coordinator-specific override becomes the reconstructed default. The user explicitly accepts that tradeoff. Current definition defaults or non-coordinator Agents must not participate.
- Existing migration framework or lifecycle constraints: register after the existing TeamRun V1 promotion; use startup execution and ordinary retry; current package catalog/runtime must consume only the new schema. Do not retain V1 runtime readers.
- Decision: `Migration Required`; coordinator-based reconstruction policy and the complete requirements basis were explicitly approved by the user on 2026-08-24, implemented in the integrated source, and successfully executed in the live DR-003 build. SR-010–SR-012 do not alter the migration decision or shape.

## Constraints / Dependencies / Compatibility Facts

- Latest hierarchical AgentTeam addressing is authoritative.
- Every Team placement has a canonical rooted address and concrete TeamRun identity at runtime.
- Agent runtime construction requires complete resolved settings.
- Root-only presets must remain meaningful through inheritance.
- Dynamic AgentTeam Runtime depends on this ticket, not vice versa.

## Open Unknowns / Risks

- The user-approved visual contract remains unchanged: root and stored Settings visually match the personal form baseline; nested editors extend existing Team groups and default collapsed; no effective summary or separate stored-card inspector is rendered; existing TeamRuns show the same controls locked.
- Application SDK, GraphQL, V2, migration, workspace lifecycle, identity-allocation, origin-personal presentation, and SR-013 capability boundaries are integrated and unchanged by SR-014.
- The completed structural guards remain relevant: discriminated models, explicit narrowing, and forbidden-import/sentinel checks prevent stored history from becoming authoring state.
- A future provider may add real free-text or another novel schema field. That future catalog change must define its own field presentation and browser coverage; this ticket removes CR/LF-specific dead-path code rather than preserving speculative complexity.
- Root/nested accessibility must not regress: removing redundant root text must preserve labels for each actual control, while nested disclosures retain textual Team identity/state and `aria-expanded` semantics.

## Notes For Architecture Reviewer

Architecture re-review is requested for SR-015 after ARCH-REV-006 / AR-001. The design now lists four mandatory removal/retargeting rows, qualifies the hard block and other absolute exactness statements to named supported current/released producer paths, and explicitly keeps the runtime classifier generic/provenance-free. Preserve IR-011's capability/classifier architecture and the unchanged UI. If Pass, route the bounded simplification to implementation, followed by source review. API-REV-010's real-path Pass remains the coverage baseline; no synthetic CR rerun is authorized.
