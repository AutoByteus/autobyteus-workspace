# Investigation — ORG-STOPPED-CONFIG-20260917-001

## Bootstrap and evidence scope
2026-09-17. Fresh fetched origin/requirements/flat-agent-organization-model at 36c149b26c429a0ca6689442fe2aea067533a638; dedicated worktree /Users/normy/autobyteus_org/autobyteus-worktrees/stopped-org-member-header-actions; branch codex/stopped-org-member-header-actions. Initial worktree clean. Finalization target is the same feature branch, NOT personal. autobyteus-web/AGENTS.md read; use colocated tests, --run, no blanket staging. Only ticket documentation/evidence authored. No source/test implementation, live app action, migration, provider input, build, Git commit/push or own browser reproduction.

## User evidence
Four supplied screenshots copied unchanged under evidence/: offline-org-direct.png, offline-org-mounted.png, offline-team-control.png, active-org-control.png. They show stopped Org header lacks gear/plus for both placements, Team stopped has them, active Org has them. Full original filenames remain in the incoming user message; screenshots contain user conversation context and should remain scoped to this ticket.

## Confirmed source findings (paths relative to worktree)
1. autobyteus-web/components/workspace/org/AgentOrgWorkspaceView.vue:44,53 sets both surfaces' show-header-actions to target.access === 'live'. openMemberConfiguration:124–126 repeats live-only gate. This directly explains stopped header/entry absence, not a CSS hypothesis.
2. autobyteus-web/stores/agentOrgContextsStore.ts:164–174 gives stopped configured members historical+inactive → continuable. Task members remain read_only. Live access requires active root, live leaf and ready stream. Thus message/stream access is not synonymous with Settings eligibility. submit restores root before continuation; opening Settings must not use activation as a visibility workaround.
3. autobyteus-web/components/workspace/org/AgentOrgMemberRunConfigPanel.vue: AgentRunConfigForm workspace/runtime locked, existing-model-config-editable=false; config forcibly isLocked=true. It has no Save path. Merely enabling gear cannot satisfy model editing.
4. autobyteus-web/components/workspace/team/TeamWorkspaceView.vue always passes show-header-actions=true for selected standalone Team member and opens configuration without live-only restriction. Standalone Agent view similarly exposes actions. WorkspaceHeaderActions.vue defines gear(editConfig) and plus(newAgent) events.
5. Org openNewOrgRun resolves enclosing root definition ID and routes /workspace with rootSubjectKind=agent_org, definitionId, mode=configuration. It is a new Org run form, NOT an add-member action. Standalone Team plus instead seeds a new Team draft from the selected Team configuration; identical copy semantics are not claimed for Org.
6. autobyteus-web/components/workspace/config/ExistingRunConfigEditor.vue and stores/existingRunModelConfigStore.ts implement standalone Agent/Team canonical load, model validation, explicit Save and uncertain-persistence reconciliation. The editor supports agent_run/team_run, not Org member selections. Org currently uses its separate locked panel.
7. autobyteus-server-ts/src/api/graphql/types/agent-org-run.ts exposes create/restore/terminate and inspection/projection/checkpoint queries; no stopped model-save mutation in this resolver. run-history/services/studio-run-model-config-service.ts provides Agent and Team operations/options; its application live-ownership guards do not by themselves establish safe Org-owned leaf writes. agent-team-execution/services/team-run-model-config-mutator.ts resolves configured flat Team scopes and applies model/config patches to Team snapshot. Do not route Org member IDs to standalone writers without verifying canonical ownership/restore authority.
8. Existing Org member panel tests affirm locked direct/mounted configuration; workspace tests exercise live actions. No executed regression or actual saved-Org configuration result is claimed.

## Method and limits
Read files with cat/sed/rg, verified git status. Two guessed backend service paths did not exist; discovery identified standalone-agent-run-lifecycle-service.ts; no conclusion relies on missing files. One overly broad earlier source command was truncated; authoritative findings above use bounded reads. User reachability is supported by actual supplied screenshots plus production gates. No independent UI rerun yet. Root cause of hidden controls and current read-only integration gap are confirmed; exact canonical Org save/restore design still requires post-approval investigation.

## Interpretation and next action
A real parity gap spans header visibility, configuration entry and persistent model-edit capability. Do not label this a two-button-only fix. Preserve root liveness authority and exact selected member; distinguish historical tasks. SR-001 proposes focused configured-member model edit parity and unchanged new-run action. Await explicit approval before design and downstream assignment.

## Supplement inventory
- evidence/*.png: user-supplied observations, REQ-001/006; evidence only, no separate intended-behavior authority.
- bootstrap-handoff.md: provenance and scope/approval hold; current.
- requirements-doc.md: SR-001 proposed intended behavior, Ready for Approval.

## Post-approval architecture investigation — SR-003
Read-only git comparison uses recorded origin/personal = 5645b49d6f51faa60bd3545bc8e3f0e7e3f96793 (same previously pinned September personal reference); no checkout/fetch/history runtime replay of personal. Current ticket base remains 36c149b26c429a0ca6689442fe2aea067533a638.
- Personal `autobyteus-web/components/workspace/config/ExistingRunConfigEditor.vue`: canonical Team draft, editable only inactive and not reconciliation-required; explicit Save. Personal `services/runConfigEditing/existingTeamModelConfigDraft.ts`: recursively indexes configured Team/Agent scopes by address, excludes task executions, creates model-only patches; parent propagation is an existing feature but not requested for this focused-member Org correction.
- Personal server `agent-team-execution/services/team-run-model-config-mutator.ts`: recursive configured-node resolution, kind validation and immutable model/config replacement; manager `agent-team-run-manager.ts` saves under root transition lane, rejects managed/archived roots, validates with RunModelSelectionService, writes authoritative tree, reads back, distinguishes pre-rename failure/indeterminate publication. Current flat Team retains this pattern without recursive configured Teams.
- Both personal and current `llm-management/services/run-model-selection-service.ts`: same-runtime catalog selection; replacement needs known target/current capacities and target >= current. Same model skips capacity replacement check but validates its parameter schema. listOptions supplies replacement choices and unavailable reason. Reuse this policy, not new capacity logic or arbitrary provider model switches.
- Current `agent-org-execution/services/agent-org-run-manager.ts`: owns root active registry and withTransition lane used by create/restore/inspection/terminate; mounted Teams explicitly never enter standalone Team root manager. restore reads current Org package via AgentOrgStatePackageLoader, then scope builder. getInspection reads and validates tree/tasks/messages without starting providers or repair. Existing settings writes must join this owner/lane, not standalone Agent/Team writers.
- `agent-org-run-service.ts` public facade currently create/restore/terminate/inspection; validates creation with existing model validator. `agent-execution/runtime/general-process-run-supervisor.ts` constructs manager/service and already holds modelSelectionValidator. `run-history/services/studio-run-model-config-service.ts` is standalone Agent/Team, not an Org writer.
- Current tree type/schema: `agent-org-execution/domain/agent-org-run-execution-tree.ts`, `run-history/domain/run-execution-tree-shared-records.ts`, `run-history/store/agent-org-run-execution-tree-store.ts`. Canonical file `agent_org_run_execution_tree.json` under exact Org memory directory. Direct Agent and mounted Team's Agent each have resolved launchConfiguration (runtimeKind, model identifier, llmConfig, workspace/tool policy), persistent Agent IDs and optional platform binding. Root/Team defaults and task executions are separate. Representative source fixture `tests/fixtures/current-agent-org-run-fixtures.ts` exercises this current strict shape; not a read of user live data or volume census.
- Store writes validate same schema and use AtomicRunPackageFileCommitWriter: not_renamed / renamed_finalization_indeterminate / committed. Only one current tree needs model edit; no sidecars, trace files or definitions need rewriting. Schema remains v1.
- Org restore -> AgentOrgExecutionScopeBuilder -> projectAgentOrgConfiguredAgentNode/projectAgentOrgConfiguredTeamNode -> configured execution handles. `configured-agent-execution-handle.ts:buildAgentRunConfig` uses execution.llmModelIdentifier/llmConfig/runtimeKind. Thus changing the canonical configured leaf affects subsequent restore/first input; settings must not materialize a scope. Existing binding IDs must be retained. Real external-provider continuation still needs actual validation, not inferred from construction.
- `services/agentOrgExecution/agentOrgExecutionContext.ts`: owns view/index and AgentContext objects; adoptLocalContexts replaces config/state on full rehydration. Use a narrow configuration-only application method for a successful Save, not full hydration which reloads conversation/Activity. `agentOrgContextsStore.ts` owns root operation exclusion and deferred disposal, and accessFor disables sending during operations. Extend that existing mechanism for the bounded Save; no second lifecycle mutex.
- Current `existingRunModelConfigStore.ts` is standalone Agent/Team global draft with history ownership and propagation. Reuse its value helpers and form/schema components, not its standalone canonical load/writers for Org IDs. A focused Org editor controller avoids widening all standalone selection unions or fabricating Team roots.
- ApplicationRunOwnershipService handles Agent/Team bindings, not Org binding subjects. Do not claim it verifies application-bound Orgs. Their previously read-only Settings remain read-only outside the requested Studio-history scenario; no application ownership/schema extension in this ticket.
- Web/server AGENTS testing instructions read. No executable checks, user app actions or source implementation in this solution round. Initial bounded source queries using guessed filenames were corrected via rg --files; no conclusions depend on nonexistent paths.

Supplement inventory additions: personal-stopped-config-comparison.md (evidence/rationale only, approved REQ-002/003 reference); design-spec.md (DS-001 technical basis, approval SR-002); solution-handoff.md (current complete design/review package). No new behavior-defining supplement.

## Recovery intake — SR-004 / CRR-003 / F-001 and F-002
Read canonical code-review-report.md in full, f001-plus-inheritance.md/f002-empty-model-diagnostic.md, revision records and implementation-handoff. Prior chain: ARCH-REV-001 Pass, IR-001, CRR-001 source Pass on narrower contract; API-REV-001 Fail83.6% confidence (not pass percentage). Reviewer sent first complete upstream CRR-003, no CRR-002 transport. Do not characterize implementation as drifting from original design: DS-004 explicitly prescribed definition-only navigation. Root problem is my narrower Plus requirement/design decision, exposed by user's later explicit clarification.
Read current source: OrgWorkspaceView plus forwards definitionId, OrgRunConfigPanel watch(org) always begins from definition defaults, OrgRunConfigStore.begin clears overrides/workspace/tool policy. Both actual direct/mounted API Plus journeys lose nonempty source root/member settings. Their exact source/AX evidence remains in validation/api-live. Independently read current and pinned personal useDefinitionLaunchDefaults.ts: buildEditableTeamRunSeed projects root authorable configuration and parent-relative Team/Agent overrides, excludes runtime identities/history, sets editable. This is useful source pattern, not old/current runtime certification.
F-002 confirmed source: MemberOverrideItem combines empty effective model and missing nonempty model in unavailable branch. Collapsed direct row remains mounted, emits schema state, drives Org store/panel error. RuntimeModelConfigSchemaState currently loading/ready/invalid/unavailable; correction must distinguish incomplete selection without marking empty scope ready or hiding actual errors. Current Team readiness distinguishes model-required. Team screenshot contains model-required hint, not absence of all feedback. No new executable or browser run by Designer.
Feasibility constraints for later design: current Org form rootConfig fixes PRELOADED_ONLY, launch authoring supports runtime/model/config/tool policy/root and Team workspaces; sparse override canonicalizer inserts explicit null llmConfig on model/runtime override. A seed must preserve null vs inheritance and zero/false values. Org begin/init must not overwrite consumed source seed on async definition arrival. No need to write backend configuration or clone source run. Keep seed UI-authorable and check exact current topology instead of guessing by display name. These are technical investigation, not final design decisions before consolidated approval.
Preserve API successes: real native direct/mounted same-model parameters and equal/larger replacement Save→reopen→Send, actual200 model/config requests, identities/tokens/Activity/drafts/attachment, leaf-only writes, no inference on inspect/save; active-root/offline-leaf lock; committed response loss→uncertain→explicit Retry without replay. Carry narrow26server/27web, adjacent107server/152web and server build as reported, not rerun. B04 external and live standalone/task controls incomplete; no credential blocker inferred. No API durable test additions. Other-owner files/evidence/generated state untouched.
Coordination: sent hold to exact existing implementation_engineer_f84b5074541a47fea830604d1bcb77c3, tool confirms DELIVERED. No new assignment/delegation or second reviewer/API/Delivery notification. No claim of recipient acknowledgment until received. Requirements revised Ready for Approval; dependent design Needs Revision; routine approval hold, no specialist outcome handoff.
Supplement inventory: recovery-handoff.md current intake/status; history/sr003/{requirements-doc,design-spec,solution-handoff}.md frozen historical Designer basis, not competing current authority. API/reviewer reports remain their owners' unchanged artifacts.

## SR-006 post-confirmation architecture evidence
User explicitly confirmed SR-004 in SR-005 and reiterated original-personal user-experience parity, not copy/paste. Re-read pinned personal TeamWorkspaceView.createNewTeamRun: sets buildEditableTeamRunSeed(activeTeamContext.view.getConfigurationView()), clears Agent config/selection. Helper carries root and difference overrides, not execution IDs. Same pattern selected for Org with its native source authority.
Read Org panel runtime launch serialization: root skillAccessMode fixed PRELOADED_ONLY, current authorable fields include runtime/model/config/tool policy/root workspace and Team overrides. Source inheritance must not silently claim arbitrary skills/per-Agent workspace support; block nonrepresentable source rather than add unrelated editor features. Root/Team source paths can use existing path-mode workspace selections without writes until Create. editableAgentOrgRunFormModel uses root baseline for direct Agents, effective Team config for mounted children; current sparse patch utility clears llmConfig for model/runtime override unless explicit config provided. DS-REV-002 therefore requires explicit source child config when model/runtime changes, including null.
Existing readAgentOrgRunInspection is network-only, strict correlated DTO, no restore/publication: usable source read. Current panel initializer is definition-id-only and wipes overrides; revise intent key to definition+sourceOrgRunId, one draft install/generation, not a new seed registry. Exact definition refs are already loaded by existing owner; compare placement and definition identity rather than parse owned IDs or use public catalog alone.
RuntimeModelConfigSchemaState has four blocking/readiness statuses. MemberOverrideItem empty/nonempty-missing branch confirmed; root RuntimeModelConfigFields currently avoids unavailable for blank model but can emit ready absent selection and lacks explicit identifier in state watcher dependencies. DS-REV-002 uses a typed model_required reason on blocking incomplete state, propagated to neutral Org feedback; no string parsing/hide-all-errors. Existing Team readiness tests model absence before catalog membership. Forwarding consumers inspected by source search; implementers must exercise shared controls, not infer live parity.
Designer authored only requirements/design/evidence/revision/handoff docs; no source/tests/runtime or Git actions. Full inherited IR/API evidence preserved. DS-REV-002 addresses CRR-003 F-001/F-002, Medium/High cumulative; architecture re-review next. No claim current fixes or review have passed.


## SR-007 / CRR-005 F-003 — standalone Team canonical seed boundary (2026-09-17)
Evidence versus intended behavior: current Code Reviewer CRR-005 confirms actual API-REV-002 Stop→Settings Save670→Back→Plus→Create747 loses low to null in the NEW Team only. Original canonical tree and reopened Settings retain low. F-001/F-002 resolved in actual browser; current API result Fail84.3% validation confidence. Diagnostic2 tests reproduce the defect, not acceptance. Preserve native Settings/response-loss Retry and actual Claude direct/mounted continuation evidence, with external model-change capability limitations. See code-review-report.md, validation/crr005-owner-probe.spec.ts/.log, validation/api-r2/f003-team-plus-parameter-loss.md and f003-team-transport.json. Designer performed source reads only, no executable or runtime rerun.

Independently inspected current forward path:
- stores/existingRunModelConfigStore.ts saveTeam330–349 caches parsed canonical result and refreshes editor only; syncTeamCanonical197–214 does not publish retained config. Save Agent uses patchConfigOnly. Canonical failure/read paths likewise feed editor/cache.
- services/teamExecution/teamExecutionContextFactory.ts freezes detached configured values; teamExecutionViewState.ts405 returns input.configuration. No config-adoption operation exists. Replacing the context to change this would touch unrelated history/activity lifecycle.
- TeamWorkspaceView.vue47–53 reads the retained snapshot. Full production search for buildEditableTeamRunSeed found a SECOND source-copy entry in RunningAgentsPanel.vue createTeamRun: selected or most-recent retained Team. Both need the same source authority, not a header-only workaround. Its no-source branch uses definition template and stays unchanged.
- Other getConfigurationView consumers are definition identity/grouping (RunningAgentsPanel) and workspace metadata/path (workspace.ts and runHistoryTeamHelpers). None of these require editing model parameters in the retained snapshot to satisfy F-003. Focused Agent contexts remain runtime/presentation state; their replacement is not necessary to seed a new run.
- runHistoryStore.refreshTeamResumeConfig358–380 already makes a network-only GetTeamRunResumeConfig and strict schema parse, but does not correlate returned teamRunId/tree root before caching. Hydration does perform correlation. Reuse and strengthen this existing reader, not duplicate query or use cached editor draft as authority.
- Server TeamRunHistoryService.getTeamRunResumeConfig reads canonical tree for active OR inactive roots; isActive/editability do not deny reads. StudioRunModelConfigService annotates application ownership; it does not restore/start provider. No new server API required.
- Workspace metadata resolver workspaceMetadataActions.ts92+ reads existing metadata and caches metadata only. Do not call ensure/create/activate workspace while seeding. createTeamConfigurationView accepts a metadata map; buildEditableTeamRunSeed projects only authorable fields. This reuses pure factories without hydrateCurrentTeamRunContext (which would fetch histories/projections and construct replacement contexts).
- Existing agentSelectionStore.beginSelectionIntent supplies invalidation for asynchronous selection; it is not a runtime lock. Caller must also check its captured selection and mounted state because not every raw select action advances that intent. No new generation registry needed.

Pinned personal5645b49d6 read-only comparison: TeamWorkspaceView uses the same pure seed pattern; existingRunModelConfigStore.saveTeam also updates history/editor only. Useful projection principle does NOT establish canonical freshness. This is source evidence, not an original-personal live reproduction or exact introducing-commit claim. Earlier personal comparison failed to verify this cross-owner boundary; correct that rationale without dismissing user experience.

Selected technical decision (design authority in DS-007): canonical read at BOTH Team source-copy entry points, not mutable config adoption PLUS fresh read. Preserve retained context verbatim as hydrated execution/presentation snapshot, remove it as authority for new authorable model values. Only metadata whose path matches canonical source may be reused. Existing Settings/read/save remain canonical separately; no new cache-authority chain. All34 IR-002 manifest hashes verified exact before Designer documentation changes. Implementation hold delivered to existing run; no new assignment.
