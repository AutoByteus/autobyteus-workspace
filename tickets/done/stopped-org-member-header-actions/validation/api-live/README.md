# Actual application validation — API-REV-001
Result Fail/F-001, not renderer replay. See canonical report and f001-plus-inheritance.md.

Temporary scripts launch real built backend51081, transparent observer51082, Nuxt51083; isolated `.local/api-stopped-config/data` and HOME. Start from workspace via `python3 tickets/in-progress/stopped-org-member-header-actions/validation/api-live/launch.py <backend|proxy|frontend>` after documented build prerequisites (SDK dist was cleaned after run). Script files are execution scaffolding, not durable test code. Use a fresh free port/owned environment or verify stopped ownership before reuse. Never point this setup at user data.

14 authored seed-file hashes in seed-manifest.json; synthetic Config Lab with direct guide and owned mounted squad/lead. No tools/skills authored. Initial runtime selected via UI AutoByteus/gpt5.4mini/manual approval/tempworkspace. Baseline replies and later real replacement gpt5.4 low use user-authorized official secret import into only isolated DB. No values in evidence; DB must not be attached/staged.

Browser automation: CUA native Chrome AX because browser connector unavailable, actual Run/Create/Send/Stop/header/Settings/native picker. No script invoked app mutations. proxy control={} normally. One response-loss case sets fail=true/delayMs2000 on exact model config operation; request forwarded to actual backend, response observed then withheld503. Explicit Retry only reads; control reset{}. No fabricated successful data. Provider observer is pass-through globalfetch safe model/config/status metadata only.

Native root config_lab_55cf971aeb934826aedbc1a7983e7ecc. Four actual OpenAI inference requests. Five UI model-config Saves. Source tree snapshots demonstrate leaf-only edits. Prefix f001 files capture clarified inheritance failure; no screenshots/private browser chrome necessary. Old root stopped and owned tab/services closed; cleanup.json verifies ports. All23 source hashes and14 authored hashes unchanged. .local data retained isolated ignored for rerun; no automatic service restart.

No browser external-runtime completion, actual standalone Team control or all-provider claim. Narrow/full relevant source suite logs in parentvalidation. Confidence83.6, Fail; source-gate10/10 not acceptance.
