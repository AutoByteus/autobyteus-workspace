# API/E2E Test-Case Ledger — claude-sdk-background-task-lifecycle

Round: API-REV-001 (execution complete; reconciled into the execution report). Worktree HEAD `b041e34df`.
Clean env for live cases: the inherited `CLAUDECODE`, `CLAUDE_CODE_*`, `CLAUDE_PID`, `CLAUDE_EFFORT` and `CLAUDE_AGENT_SDK_VERSION` are unset.

| Case ID | Case | AC IDs | Status | Expected | Observed | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | Unit client + backend suites | AC-001, AC-005 | Pass | Pass (backend 1 pre-existing failure) | client 26/26; client+backend 130/131. The only failure, `claude-session.test.ts > switches an opened but unconfirmed first query…`, also fails with the `src` change reverted to base `9267d11c8` (24/25) | console |
| C2 | E2E-POL-01 on PATH CLI 2.1.281 | AC-001, AC-006, QR-001 | Pass | No `run_in_background`; 30 min max | Bash props `command,timeout,description,dangerouslyDisableSandbox`. timeout: "Optional timeout in milliseconds (max 1800000)". Description: "up to 1800000ms / 30 minutes … default 120000ms (2 minutes)". No tool (incl. Agent) offers `run_in_background`. Caller env `0`/`600000` overridden | `api-e2e-evidence/c2-c3-pol-01-fix.log` |
| C3 | E2E-POL-01 on bundled CLI 2.1.280 | AC-001, AC-006, QR-001 | Pass | Same | Same as C2 | `api-e2e-evidence/c2-c3-pol-01-fix.log` |
| C4 | E2E-BG-01 on PATH CLI | AC-002 | Pass | Foreground, marker, same-turn result, no `[killed]` | Bash args `{command, description}` (no `run_in_background`). SUCCEEDED "(Bash completed with no output)" about 20 s after start. The agent then Read the marker, which held `done` at TURN_COMPLETED. The reply contains `done`. No `[killed]`. 28.8 s | `api-e2e-evidence/c4-c5-bg-01-fix.log` |
| C5 | E2E-BG-01 on bundled CLI | AC-002, QR-001 | Pass | Same | Same as C4 (28.2 s) | `api-e2e-evidence/c4-c5-bg-01-fix.log` |
| C6 | Regression: live client integration + websocket interrupt E2E | Preserved | Pass (with 3 pre-existing unrelated failures) | Pass | Live client integration 4/4: `listModels` (model discovery), live turn + session messages, project skill, resume, custom MCP. Live websocket interrupt/resume 1/1. The 3 mocked-SDK interrupt tests in `claude-agent-websocket-interrupt-resume.e2e.test.ts` fail. They fail identically on base `9267d11c8` with the `src` change reverted: the real CLI rejects `claude-test-model` and the fake query is never used. Pre-existing and out of scope | `api-e2e-evidence/c6-regression-live.log`, `api-e2e-evidence/c6-interrupt-e2e-mocked-base.log` |
| C7 | Control on base (fix reverted) | Discrimination | Pass (both new tests fail on base as expected) | New tests fail | POL-01 on base: both CLIs fail, Bash has `run_in_background`. BG-01 on base: both CLIs fail. The model sent `run_in_background: true` and got "Command running in background … You will be notified when it completes". The turn ended in about 9 s, and both CLI task output files read `\n[killed]\n`. This reproduces the reported bug through AutoByteus | `api-e2e-evidence/c7-pol-01-control-base.log`, `api-e2e-evidence/c7-bg-01-control-base.log` |
| C8 | AC-003 overrun probe | AC-003 | Pass | Visible timeout, no background | Both CLIs: Bash `timeout: 5000` with `sleep 40; …` produced TOOL_EXECUTION_FAILED "Exit code 143 / Command timed out after 5s" within the turn (about 14 s). The agent reported the timeout. There was no `run_in_background`, no background text and no `[killed]`. The marker was absent 45 s after the turn, so the command was terminated, not left running. Temporary probe file deleted after the run | `api-e2e-evidence/c8-ac003-overrun-probe.log` |
| C9 | User-settings `env` override probe (design residual risk) | Design risk / escalation-trigger check | Done (informational) | Characterize the documented residual risk | Temporary `CLAUDE_CONFIG_DIR` whose `settings.json` has `env: {CLAUDE_CODE_DISABLE_BACKGROUND_TASKS: "0", BASH_MAX_TIMEOUT_MS: "600000"}`. On both CLIs, Bash again advertised `run_in_background` and "max 600000": the settings `env` overrides the spawn env. The user's real `~/.claude/settings.json` has no `env` block, so the risk is not present in practice here. Temporary probe file and config dir deleted | `api-e2e-evidence/c9-user-settings-env-probe.log` |

## Observations

- OBS-1 (C2/C3): with the policy applied, the CLI still advertises other CLI-process-scoped tools: `Monitor` (streams a background command's output as notifications), `ScheduleWakeup`, `CronCreate`, `Workflow`, `PushNotification`, `TaskStop`. These are outside REQ-001 (Bash only), and the builtin tool list is owned by `claude-sdk-builtin-tool-restriction` (out of scope). Recorded as residual risk.
- OBS-2 (C6/C1): pre-existing mocked-test failures (1 unit, 3 websocket E2E) reproduce on the base with the fix reverted. Not caused by this change.
- Default (non-gated) run: the new live tests skip cleanly (2 files, 4 tests skipped).
- Cleanup: temp workspaces were removed by the tests. CLI task dirs under `/private/tmp/claude-501/*claude-live-background-bash-*|*claude-cli-runtime-policy-*` and `~/.claude/projects/` session dirs for the same patterns were removed (22 dirs). No lingering `sleep` processes. The temporary `tmp-ac003-timeout-overrun-probe.e2e.test.ts` was deleted.
