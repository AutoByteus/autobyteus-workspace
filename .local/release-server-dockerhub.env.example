# Fill this file locally, save it as `.local/release-server-dockerhub.env`, then run:
#   bash scripts/github-set-release-server-docker-secrets.sh

DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=

# Optional. Leave empty to keep the workflow default:
#   autobyteus/autobyteus-server
DOCKERHUB_IMAGE_NAME=
