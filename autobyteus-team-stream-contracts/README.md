# `@autobyteus/team-stream-contracts`

Strict, browser-safe WebSocket DTOs and runtime validation for `/ws/agent-team`.
This package owns transport spelling only; it has no routing, topology, persistence,
provider, or UI responsibility.
